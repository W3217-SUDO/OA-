# OA 全站源码能力对齐台账

更新时间：2026-08-02

## 当前工作方式

- 业务基准：`旧系统归档源码/SH.CRM.WEB`。
- 定位顺序：旧源码仓库存在 `.codegraph`，先用 CodeGraph，再读取必要的 Controller、View、JavaScript、DTO 和权限代码。
- 对齐对象：旧源码路由、字段、校验、权限、状态、失败文案、文件处理、分页以及跨模块参数和回写。
- 不再执行网站逐动作比对、登录、验证码、旧站造数或清理。
- 本地已有而旧源码没有的额外能力保留；旧源码存在而本地缺失的能力必须补齐。
- 每批处理 10–20 个相关缺口，任务只跑定向测试和类型检查；主会话统一运行生产构建、Python 编译和 Git 提交。
- 所有改动只进入 `dev`；禁止 `git add .`，全站源码矩阵清零前禁止部署。

## 第一批源码矩阵提交

| 模块 | 提交 | 本批范围 | 根验证 |
| --- | --- | --- | --- |
| 案件中心 | `c348053` | 列表分页/排序、查询重置、导出、当事人、阶段、法院和权限矩阵 | 案件回归 35/35；类型检查、构建通过 |
| 系统中心 | `2e284b6` | 缓存、菜单、系统参数、案由和角色权限 20 项 | 系统回归 17/17；类型检查、构建通过 |
| 财务中心 | `6dda33e` | 付款查询状态、服务端分页、筛选/清空、失败文案和选择矩阵 | 付款查询回归 25/25；类型检查、构建通过 |
| 用印中心 | `d251544` | 附件列表、预览/下载、审核记录、空态和权限矩阵 | 新批次 21 项通过；类型检查、构建通过 |
| 合同应收 | `39cb834` | 六类金额入口、明细上下文、汇总、空态和案件返回 | 应收回归 5/5；类型检查、构建通过 |
| 客户中心 | `379f70b` | 回收站名称编辑、分页 0 值归一化和 14 项客户矩阵 | 客户回归 15/15；Python 编译、类型检查、构建通过 |
| 合同审批 | `5967b51` | 已审批合同详情保留来源并正确返回 | 专测 1/1；构建通过 |
| 人事提成 | `50ef5b5` | 提成维护权限、字段、日期/金额校验和分页 | 前端专测 3/3；构建通过 |

统一门禁：`npm.cmd run build` 成功，5502 modules transformed；`python -m py_compile app/main.py` 成功；`git diff --check` 无错误。

## 第二批源码矩阵提交

| 模块 | 提交 | 本批范围 | 根验证 |
| --- | --- | --- | --- |
| 案件中心 | `53cfe14` | 创建/编辑 DTO、线索转案、复制合并、执行状态和付款上下文 | 专测 5/5；完整构建通过 |
| 系统中心 | `4d58b01` | 参数/案由写入反馈、角色权限保存、管理员边界和审计矩阵 | 专测 15/15；完整构建通过 |
| 财务中心 | `e518125` | 付款详情、打印核销、审批回写、来源参数和有界详情请求 | 专测 23/23；完整构建通过 |
| 用印中心 | `c2e7248` | 上传校验、审核失败、删除/下载权限和行政状态矩阵 | 专测 2/2；完整构建通过 |
| 合同中心 | `bb15148` | 新建默认值、审批载荷、附件锁定及客户/案件候选策略 | 专测 5/5；完整构建通过 |
| 客户中心 | `a3d6b80` | 联系人六档分页、联系人/文档/回收公海权限和失败路径 | 专测 12/12；完整构建通过 |

第二批统一门禁：`npm.cmd run build` 成功，5504 modules transformed；`python -m py_compile app/main.py` 成功；`git diff --check` 无错误。

## 第三批源码矩阵提交

| 模块 | 提交 | 本批范围 | 根验证 |
| --- | --- | --- | --- |
| 案件中心 | `039bd9b` | 执行状态专用 API、白名单、批量预检、权限/归档阻止及前端状态流转 | 前端 3/3、后端 4/4；Python 编译通过 |
| 系统中心 | `ee48833` | 用户邮箱/手机/电话、15 条分页、空态新建、管理员与本人删除边界 | 专测 11/11；完整构建通过 |
| 财务中心 | `c92829b` | 发票申请/审批/开票状态、详情、失败文案、导出与有界请求 | 专测 14/14；财务回归 80/80 |
| 用印中心 | `291e0c7` | 批量下载、删除失败、查询/路由切换清理选择及行政用印权限 | 专测 2/2；用印回归 25/25 |
| 合同中心 | `7e6f902` | 审批列表分页、动作权限、安全返回及详情附件；分页模块改为可测试 ESM | 专测 6/6；合同回归 50/50 |
| 客户中心 | `8465369` | 客户范围权限、回收/公海/共享、联系人/文档错误及详情加载恢复 | 专测 12/12；客户回归 54/54 |

第三批统一门禁：`npm.cmd run build` 成功，5504 modules transformed；`python -m py_compile app/main.py` 成功；`git diff --check` 无错误。

## 第四批待处理范围

- 案件：阶段专用 API、阶段 ID 映射、权限/归档阻止和审计回写。
- 系统：部门、职位及组织架构的字段、分页、引用阻止和权限边界。
- 财务：结算、归档、退款等下一组完整菜单及跨模块回写。
- 用印：剩余附件删除/下载、官方/行政菜单和状态失败路径。
- 合同：合同文件、审批失败、部门/公司合同页面和权限边界。
- 客户：共享、领取、归档等真实动作的 UI 状态流转与失败恢复。

## 完成判定

只有以下条件同时满足，才能将全站目标标记完成：

1. 旧源码可达能力全部进入模块矩阵，每项都有源码位置和本地映射。
2. 所有本地缺口已实现，本地额外能力未被删除。
3. 每项有独立测试或稳定矩阵断言，模块回归、完整构建和 Python 编译全部通过。
4. 共享工作树没有未审计业务改动，全部提交只位于 `dev`。
5. 最后统一部署测试服务器并完成验收。
