# 申浩服务器连接说明

更新时间：2026-07-19

## 服务器身份

- 标注名称：**申浩服务器 / 申浩旧 OA 服务器**
- 所属机构：上海申浩律师事务所
- 公网 IP：`139.224.35.141`
- 操作系统：Windows Server 2012 R2 Datacenter 64 位
- Windows 主机名：`iZ74h50i5e5ww2Z`
- 管理员账号：`Administrator`
- 旧 OA 应用目录：`D:\SH.CRM\GD.CRM.WEB`
- 真实业务数据库：`PRD_CRM_SH_20190320`
- 数据库内网地址：`172.19.228.55`

## 首选连接方式：SSH

当前电脑已配置 SSH 别名 `oa-legacy`，直接执行：

```powershell
ssh oa-legacy
```

执行单条远程命令：

```powershell
ssh oa-legacy "whoami"
ssh oa-legacy "sc.exe query sshd"
```

SFTP 文件访问：

```powershell
sftp oa-legacy
```

上传或下载文件：

```powershell
scp 本地文件 oa-legacy:'C:/目标目录/'
scp oa-legacy:'D:/SH.CRM/目标文件' 本地目录
```

SSH 服务名称为 `sshd`，端口为 `22`，已设置开机自动启动和故障自动重启。当前客户端配置包括 30 秒心跳、3 次失败判定、3 次连接尝试及 10 秒连接超时。

## SSH 密钥

- 私钥路径：`C:\Users\Administrator\.ssh\codex_oa_139_224_35_141_ed25519`
- 公钥路径：`C:\Users\Administrator\.ssh\codex_oa_139_224_35_141_ed25519.pub`
- 客户端公钥指纹：`SHA256:ApFWTtLNebWFR/MAPLifo/cEOZoArLiriZw46XOpY74`
- 服务器 ED25519 主机密钥指纹：`SHA256:U8U66+tLzb7k7ejKzEi9T0D/Dw6Nf5smXKTkA5nXdAI`
- SSH 配置文件：`C:\Users\Administrator\.ssh\config`
- 主机密钥记录：`C:\Users\Administrator\.ssh\known_hosts`

私钥内容不得复制进本项目、Markdown、聊天记录、GitHub 或普通迁移包。更换电脑时，应通过安全介质单独迁移私钥，并限制为当前 Windows 用户、SYSTEM 和 Administrators 可访问。

当前 `~/.ssh/config` 对应配置：

```sshconfig
Host oa-legacy
    HostName 139.224.35.141
    User Administrator
    IdentityFile ~/.ssh/codex_oa_139_224_35_141_ed25519
    IdentitiesOnly yes
    ConnectTimeout 10
    ConnectionAttempts 3
    ServerAliveInterval 30
    ServerAliveCountMax 3
    TCPKeepAlive yes
```

## 服务器密码

- Windows 管理员密码已保存到当前电脑的 **Windows 凭据管理器**。
- 凭据目标名称：`申浩OA服务器-139.224.35.141`
- 用户名：`Administrator`
- 本文不保存或显示密码明文，避免项目迁移、压缩或上传时泄露。

检查凭据是否存在：

```powershell
cmdkey /list:申浩OA服务器-139.224.35.141
```

需要修改密码时，打开“控制面板 → 凭据管理器 → Windows 凭据”，修改上述目标；不得把新密码写入项目文件。

## 备用文件共享

当前已验证以下管理员共享可访问：

```text
\\139.224.35.141\C$
\\139.224.35.141\D$
\\139.224.35.141\F$
```

如果共享凭据失效，可交互式重新连接：

```powershell
net use \\139.224.35.141\C$ /user:Administrator *
```

命令中的 `*` 会安全地提示输入密码，不要把密码直接写在命令或脚本中。SSH/SFTP 是日常维护首选，SMB 仅作为旧文件读取和应急备用。

## 数据库访问纪律

- 数据库不直接暴露公网；通过申浩 OA 服务器内部连接 `172.19.228.55`。
- 应用连接配置位于 `D:\SH.CRM\GD.CRM.WEB\Web.config`，其中包含数据库凭据，读取时不得输出、复制或写入普通文档。
- 旧 OA 和旧数据库默认只读。查询使用 `READ UNCOMMITTED`、短超时和明确字段，不执行新增、修改、删除、审批、上传或建表操作。
- 已核实 `Legal_Case` 的全部案件均属于 `CompanyId=1`，机构主数据为“上海申浩律师事务所”；沪知位于独立的 `HZ.CRM` 应用/数据库，不得混用。

## 快速健康检查

```powershell
ssh -o BatchMode=yes -o ConnectTimeout=10 oa-legacy "echo SSH_OK && whoami && sc.exe query sshd"
```

正常结果应包含：

```text
SSH_OK
iz74h50i5e5ww2z\administrator
STATE : 4 RUNNING
```

如果 SSH 不通，按顺序检查本机网络、TCP 22、阿里云安全组、Windows 防火墙和服务器 `sshd` 服务。不要为了排障把数据库 1433、SMB 445 或管理员共享直接扩大到公网。
