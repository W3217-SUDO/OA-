# 合同中心生命周期闭环证据（2026-08-01）

## 双页面只读对照

- 旧系统 `https://sh.021ipr.com/Console/Index`：在合同中心只读展开“合同新建 / 我的合同 / 合同审批”，核对合同编号、名称、类型、客户、案号、收费类型、日期范围、案源人、合同主体筛选，以及合同查看、变更、用印、付款、开票、归档和分页入口；未提交任何旧站表单。
- 本地 `http://127.0.0.1:5173/`：合同列表使用唯一前缀筛选，列表显示两条测试合同的“已通过/已拒绝”状态；打开已通过合同后点击“审批信息”，表格回显 `admin / 已通过 / approved`，并核对合同标的、事项记录、合同附件、审批信息四个标签。

## 本线程真实数据闭环

测试前缀 `CODEX-CONTRACT-C-20260801225536`，客户 ID 36，合同 ID 37/38，附件 ID 1/2；均为本线程新建，未触碰既有账号或业务数据。

- 合同 37：新建草稿 → 上传合同附件 → 提交 `admin` 审批 → 审批通过 → 变更申请 → 变更审批通过 → 第二次变更申请 → 变更驳回 → 归档，最终状态为“已归档”。
- 合同 38：新建草稿 → 上传合同附件 → 提交 `admin` 审批 → 驳回，最终状态为“已拒绝”；本地列表真实显示该状态。
- 详情“审批信息”真实回显审批人、状态和意见；合同审批接口分别返回通过与拒绝步骤。
- 附件下载：ID 1 返回 HTTP 200、`text/plain; charset=utf-8`、`Content-Disposition: attachment; filename="CODEX-CONTRACT-C-20260801225536-ATT-1.txt"`、12 字节，SHA-256 `6b9af93708359b3140cbedbdecb4622d937c5e13be6248a13fc166cbdf5d74a5`；ID 2 返回 HTTP 200、同类 Content-Type、12 字节，SHA-256 `d7ab81112889a9d9a679e65a6898a548c2a0cc491e807b72defa12bd5f219256`。
- 删除阻断：已通过、已拒绝及归档合同调用 `DELETE /contracts/{id}/draft` 均返回 HTTP 409；审批中合同在列表归档操作弹出“只有审批全部通过且处于履行或完成阶段的合同可以归档”。

## 清理与自动化验证

精确删除客户 36、合同 37/38、审批步骤、工作流事件、附件 1/2 及上传文件；数据库跨表按此前缀检索为 0，业务记录及附件 ID 计数均为 0。

新增 `apps/api-server/contract_lifecycle_test.py`，覆盖草稿、附件、通过/驳回、变更通过/驳回、下载哈希、归档和删除 409，并在 `finally` 精确清理及断言零残留。运行：

```text
CONTRACT_TEST_PASSWORD=<process-only> python -m unittest apps/api-server/contract_lifecycle_test.py -v  # 1/1 OK
```
