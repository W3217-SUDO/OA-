# 拆分版发布整合（2026-09-05）

用户明确要求将拆分的前后端部署至 8089。发布内容来自“新系统”，候选基于当前运行提交 5bceb12f9315498d687e538bcff90ec7d3000a9b。

## 分支整合

原远端 dev 为 dd1c15ede79813c4b3f11c67fb957c41b7acf181，与线上分叉（线上独有 65 个提交，dev 独有 9 个提交）。从线上提交构建线性候选，保留线上全部历史，再顺序重放六个业务提交：9b1b5e7、f9056c5、5757852、4fb0369、7cda444、6c337b5。另三个提交仅修改正式版本号，不复用旧版本；旧 tags 保留，发布时重新分配新版本。

整合已保留 XLS 预览、任务人员目录交接和动作限制、用印关联附件及 HR 修复脚本的 PostgreSQL 兼容。HR 修复脚本仅随代码保留，本次不执行。

## 数据与回滚

- 本次无 schema/data 迁移，无本地数据合并；不上传本地 SQLite、测试记录或附件，不更改 DATABASE_URL。
- 不进行 MySQL 切换，也不启用前期暂停的原生 PostgreSQL 迁库方案。
- models、database、config 和启动初始化函数对线上基线做 AST 等价检查。
- 数据库备份不需要（纯应用代码发布）；当前前端回滚包保存在本机，SHA256 6730f8f8dc2cdf90a3cfc0c7b381b73eeceb886efb40d8552f15c4ef568a31ad。
- 回滚代码点为 5bceb12，回滚时恢复对应前端产物，数据库保持不动。

## 验证与限制

前端 26 项定向测试及 TypeScript/Vite 生产构建通过。后端编译、import、OpenAPI 比对、XLS 和用印等定向验证完成。API 保持 763 routes / 614 paths。

任务测试有一条既有预期不一致：已拒绝任务重启后，测试期望“处理中”，原有实现返回“待处理”。原基线隔离测试可复现相同失败，本次未改变此业务语义。旧拆分生成器 hash 验收不适用于后续故障修复，不重写历史 baseline 掩盖差异。

CodeGraph 在隔离树未初始化，采用 Git 差异、AST、编译和定向测试审计。浏览器业务验收由用户完成，状态为待用户验收。

正式版本、发布提交、静态产物 SHA256、服务器健康检查和安全引用记录见本机“新系统/说明/拆分版部署-20260905”发布清单。
