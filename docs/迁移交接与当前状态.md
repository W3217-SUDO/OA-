# 迁移交接与当前状态

## 2026-07-30 案件新建人员来源约束（测试站待验证）

- 案件新建的案源人由关联合同预填，但可按本案实际情况编辑并真实保存；客户仍保持随关联合同锁定。
- 经办律师在新建、编辑、人员分配与批量修改中只能从系统内启用且岗位为律师的人员选择；律师助理只能从系统内启用人员选择。服务端同步拒绝绕过页面提交的非律师或停用人员。
- 本地 Docker 与浏览器已验证下拉候选范围和案源人可编辑性，未创建测试案件；测试站发布后仍需复验并保留浏览器证据。

### 2026-07-30 测试站发布复验

- 已发布提交 `327c055`，测试站 Web、API、Worker 均已切换至 `20260730-131834` 镜像并保持健康；发布未迁移或改写业务数据。
- 管理员在测试站“新建案件”打开经办律师下拉，页面只展示系统在职律师候选，不再存在自由录入入口；案源人显示可编辑预填提示。本次仅查看页面，未创建测试案件。

## 2026-07-30 合同审批人配置与员工号规则

- 当前工作树已新增管理员专用合同审批人配置 API，并把合同审批字段旁的“设置审批人”统一用于合同新建、草稿续办和提交弹窗；普通角色无入口且服务端拒绝越权。
- 新建员工不再预填时间戳员工号，员工号由用户填写且后端唯一；冒烟已真实证明重复员工号 `409` 且零残留。
- 本地完整静态门禁和 21 组 API 冒烟已通过；浏览器已实际证明本地和测试服务器新建员工的员工号默认空白。
- 已发布提交 `2649d0c` 到测试服务器 `dev`，当前目录 `/opt/sunhold-oa/releases/20260730-104356`；API/Worker 为 `sunhold-api:20260730-104356`，Web 为 `sunhold-web:20260730-104356`。六服务正常，API/Web/PostgreSQL/Redis/MinIO 健康，公网 `/health` 返回 200，运行中 API 源码已核对包含两条审批人配置路由。管理员设置弹窗的服务器端点击证据仍未留存，状态为“已部署、接口已证明、页面弹窗仍未证明”。
## 2026-07-30 合同审批流程人员纠偏（本地与测试服务器已验证）

- [旧源码证据] `Areas/FCM/Views/ContractDraft/ToAudit.cshtml` 从合同审核流程读取人员，选项值为登录名、显示为中文姓名；`ContractController.ToAudit` 只保存所选审核人并提交，没有按“合伙人律师”等岗位名称自动授权，也没有禁止已配置流程人员选择本人。
- [已完成代码] 普通合同审批人必须同时满足：系统账号启用、人事档案处于在职状态、管理员在人事档案中单独开启“合同审批流程人员”。`admin` 作为最高权限保留合同审批能力，不受普通员工档案配置降低。合同页已移除临时创建审批账号入口；仅有岗位名称、仅有系统角色、离职/停用或没有正式员工档案的普通账号不会出现在下拉框，也无法绕过前端直接提交。
- [已完成页面] 管理员可在“人事中心 → 员工管理”的新建、修改、查看和列表中维护、查看该资格；合同审批下拉只显示员工姓名，空目录明确引导管理员回人事中心配置。员工宽表同步扩展横向滚动宽度，避免新增列挤压重叠。
- [已验证自动化] 2026-07-30 完整 `verify-local.ps1` 通过：生产构建、Python 编译、277 节点菜单审计、522 个前端 API 调用覆盖、21 组业务冒烟、Docker 配置全部成功。新增证据包括“仅岗位账号拒绝”“显式配置的启用在职员工可审批”“流程人员本人提交并本人审批”“管理员代办保留审计”；最终 `SMOKE_CLEANUP_OK`。
- [已部署验证] 提交 `01c7c23` 已推送测试服务器 `dev`；当前稳定目录 `/opt/sunhold-oa/releases/20260730-101400`，API/Worker 使用经镜像内源码指纹复核的 `sunhold-api:20260730-101100-current`，Web 使用 `sunhold-web:20260730-100500-current`。六容器运行，API/Web/PostgreSQL/Redis/MinIO 健康，公网 `/health` 返回 200；Web 在 API 重建的数秒窗口留下过一次上游拒绝记录，重建完成后的页面刷新和公网健康检查均正常。
- [已验证测试站] 公网页面已自行刷新并确认新版合同新建向导可达；服务器人员目录复核确认临时账号 `archive_fee_approver_20260725a` 已不再具备合同审批资格。当前普通员工尚无人被配置为合同审批流程人员，因此下拉仅应保留最高权限管理员；后续由管理员在人事中心为实际在职员工配置，无需再创建临时审批账号。

## 2026-07-30 IPR 案件文件目录与待转文（本地已验证）

- [旧源码证据] `CaseFileList.cshtml` 的有效表格列是上传人、文档类型、文件名称、文档日期、待转文；有效菜单动作包括上传、查看/下载、标记已转、删除、刷新和按权限生成申请文件。解锁及“生成转文文档”在该旧视图中已被注释，未作为有效页面动作实施。
- [已完成代码] `FileAttachment` 已增加可审计的 `document_date`、`requires_transmission`、`is_transmitted`、转文时间和操作人字段；新建 `/ipr/cases/{case_id}/files`、`mark-transmitted`、删除和列表接口。通用附件上传/删除对 IPR 案件返回阻断，防止绕开在办状态与责任范围。现有数据库启动时自动补列与索引。
- [已验证自动化] 2026-07-30 完整 `verify-local.ps1` 通过：前端构建、Python 编译、菜单/API 覆盖审计、21 组业务 API 冒烟、`SMOKE_CLEANUP_OK` 与 Docker 配置均成功。冒烟新增并验证“通用入口旁路返回 409 → 专用上传 → 文档日期/待转文回显 → 标记已转 → 下载 → 删除 → 空目录”。
- [已验证页面] 管理员从页面创建唯一 `SMOKE` 专利案件并走完审核，在详情文件目录验证格式阻断、真实文件选择上传、待转文、标记已转及删除；随后完整冒烟精确清理，浏览器刷新确认临时案件为 0。
- [仍未完成] 批量上传、自定义文件名导入、与文件类型配置关联的自动对冲、以及旧 `Transmit/Inform` 所依赖的专利/商标申请文件生成工厂仍是 P1 缺口；继续审计对应类型配置、模板和前端入口后再集中实现。
- [下一实施规格] 自定义导入采用“源文件上传 → 文件名解析候选 → 行级补正（案件、文档类型、日期、处理人、金额、费用类型、结算人）→ 勾选确认原子导入”，而不是直接上传落库。批量上传采用“多案件 + 一个文件 + 文件类型 + 文档日期”，必须先完整预检重复规则与文件/费用对冲后再整体提交。需要先建立 IPR 文件类型主数据及对冲关系，避免自由文本分类破坏旧规则。

## 2026-07-30 IPR 案件提醒与不监控（本地已验证）

- [已完成代码] 旧 `CaseEvent` 是 IPR 专用提醒，不等同于普通案件提醒；旧 `CaseEventNotIn` 提供 22 种自动提醒的按案件不监控选择。新系统已建立 `IprCaseReminder`、`IprCaseReminderSuppression` 和专用 REST 接口，权限为案件负责人、同部门负责人或管理员；提醒删除进一步限制为创建人、负责人或管理员。
- [已验证自动化] 完整 `verify-local.ps1` 通过。冒烟实际验证 IPR 立案后新增/查询/删除提醒，读取 22 项类型，保存 `[申请费, 办理专利资助]` 两项不监控并复核，再恢复为空；最终 `SMOKE_CLEANUP_OK`。
- [已验证页面] 本地页面完成“草稿 → 提交审核 → 通过 → 案件详情 → 新增提醒 → 设定不监控”闭环，两个已选类型正确回显。清理脚本已补 `ipr_case` 枚举，随后完整门禁和浏览器刷新确认临时 IPR 案件、临时客户均不存在。

## 2026-07-30 IPR 案件资助费用专用生命周期（本地页面与接口证据）

- [已完成代码] 依据旧 `IPR/CaseController.cs` 的资助费用创建、列表、办理、删除和回执视图，新增 `IprCaseAssistedFee` 及 IPR 案件专用新建、列表、办理回执上传、下载、待办理删除接口；案件详情增加“资助明细”表、新建、办理、回执下载和删除入口。它与通用财务费用严格隔离。
- [已验证自动化] 完整 `verify-local.ps1` 已通过。冒烟实际创建临时在办 IPR 案件，验证“新建 → 待办理删除 → 新建 → 上传 PDF 办理 → 回执下载 → 已办理删除被 409 阻断 → 列表回显”，最终 `SMOKE_CLEANUP_OK`。
- [已验证页面] 管理员已仅通过本地页面创建带 `SMOKE` 标识的客户、专利案件并完成“草稿 → 提交审核 → 通过”；案件详情中的资助费用“新建专利资助”已实际创建并回显为待办理。随后在 IPR 文件目录已确认本浏览器可写入受控文件选择器；但资助回执本身尚未重新完成“页面办理/上传/下载”，仍标记为未证明，不与 API 证据混同。

## 2026-07-30 官文历史与检查清单补齐（本地已验证）

- [已完成代码] 以旧 `PatentOfficeController.cs`、`PatentOfficeFileController.cs`、`FIO.Patent.OfficeFile.js` 及对应 Razor 页面为依据，知识产权官文新增“历史已办结”和“检查清单”两种真实视图。历史页保留旧站的导出、选中 ZIP 打包下载、批量标记业务已处理、批量标记流程已处理；两个标记均只更新独立处理维度，不会倒退或改变“已办结”主状态，且写入工作流审计事件。
- [已完成代码] 检查清单以旧站十列为准：申请号/不受理编号、通知书名称、发明创造名称、发文序列号、电子发文日、下载状态、系统状态、案号、入库时间、错误消息；支持真实查询、候选文件上传入口和 Excel 导出。案号存在时仍是可点击关联对象。
- [已验证自动化] 2026-07-30 完整 `verify-local.ps1` 通过：前端生产构建、Python 编译、菜单与客户端 API 覆盖审计、21 组业务冒烟和 Docker 配置均成功。官文冒烟增加并通过“候选解析/修正/确认导入 → 双状态办结 → 历史业务标记 → 历史流程标记 → 精确清理”；`SMOKE_CLEANUP_OK` 确认没有测试残留。
- [已验证页面] 本地重建 API/Web 后，在“知识产权官文”页面实际切换到“历史已办结”，确认导出、ZIP、业务/流程双标记按钮可达；切换到“检查清单”，确认上传、导出和十列检查表可达。当前没有遗留测试数据，两个列表正确显示“暂无数据”。
- [仍未完成] 旧系统的官文解锁、标签转换、对冲及其更完整批量导入格式仍未逐项对齐；本轮没有将 CSV 候选导入误称为对旧 Excel 格式的全覆盖。

## 2026-07-30 IPR 案件资助费用专用生命周期（接口已验证，页面闭环待补）

- [已完成代码] 新增独立 `IprCaseAssistedFee`，不复用通用财务记录。资助费用绑定 IPR 案件，持久化资助类别、申请人/日期、办理人/日期、办理状态、回执附件和说明；详情页已增加“资助明细”表、创建、办理、回执下载和待办理删除入口。
- [已完成规则] 只有在办案件的负责人、同部门负责人或管理员可新建/办理/删除；办理必须同时提交日期与真实回执文件，办理后不可重复办理或删除；每个写操作写入案件工作流。回执作为明确类别的案件附件保存，不以文本或成功提示替代。
- [已验证自动化] 完整 `verify-local.ps1` 于 2026-07-30 通过；冒烟在临时 IPR 案件上验证“新建 → 办理并上传 PDF 回执 → 下载回执 → 已办理删除被 409 阻断 → 列表回显”，另验证待办理记录可删除。测试案件和附件随既有 `SMOKE` 精确清理机制清零，最终 `SMOKE_CLEANUP_OK`。
- [已验证页面入口 / 仍未证明] 后续已在“专利案件”详情通过页面创建可清理 IPR 临时样本并新增资助费用，详情真实回显待办理记录；但页面回执上传/下载仍受浏览器连接器文件选择能力限制，不能以接口冒烟代替该最后一段浏览器证据。

## 2026-07-30 知识产权官文候选确认与双状态闭环（本地自动化已验证，页面待接入）

- [已完成代码] 以旧 `FIO.Patent.OfficeFile.js` 的“先解析候选行、显示错误、人工匹配案件、修正后确认导入”为准，新增 `ipr_official_import_batches` 与 `ipr_official_import_candidates`。上传 UTF-8 CSV 后只生成候选行和源文件留痕，绝不直接生成正式官文；候选行会校验申请号、官文类型、日期格式、案件匹配和案件在办状态。
- [已完成动作] 候选行可修正字段、人工匹配当前账号可见的在办知识产权案件，再由确认动作原子生成正式官文；正式记录保留来源批次、候选行、源文件名、导入人和时间。官文的业务处理与流程处理状态均被持久化，校验、转发、办结分别推进对应状态并写工作流。
- [已验证自动化] `verify-local.ps1` 完整通过：前端生产构建、Python 编译、菜单与前端 API 覆盖审计、21 组 API 业务冒烟及 Docker Compose 配置均通过。新增冒烟闭环已实际证明“IPR 立案审核 → CSV 候选解析 → 自动匹配/人工匹配/字段修正 → 确认导入 → 校验 → 转发 → 办结”。
- [已精确清理] 为该闭环增加仅非生产管理员可用、且仅接受 `smoke-` 源文件名的批次清理入口；它只删除本批候选、由本批生成的官文工作流/附件、源 CSV，不可清理普通导入批次。冒烟结束报告 `SMOKE_CLEANUP_OK`，知识产权临时案件仍交由既有 `SMOKE` 记录清理机制清除。
- [已验证页面完整闭环] 管理员仅通过页面创建临时专利案件 `ZL2607300648522D06`（申请号 `SMOKE-UI-IPR-0730`）并完成“保存草稿 → 提交审核 → 管理员通过”。随后在“候选导入”页面上传三行临时 CSV：首行自动匹配；第二行显示“缺少通知书名称/官文类型”，经页面补填后转为可确认；第三行显示“未匹配到知识产权案件”，经页面选择上述在办案件后转为可确认。勾选确认后页面真实生成三条正式官文，批次显示“已完成 3/3 已导入”。首条再由页面办理“校验 → 转发 → 办结”，列表可见业务处理、流程处理从“未处理/未处理”到“处理中/未处理”、再到“处理中/处理中”、最终“已处理/已处理”。
- [已精确清理] 使用仅非生产管理员且仅 `smoke-` 文件名可调用的批次清理入口，删除本轮候选、由其生成的三条正式官文和保留的源 CSV；随后使用既有仅限 `SMOKE` 记录的清理入口删除该唯一临时知识产权案件。API 按申请号复查官文、案件均为 `0`，页面刷新后官文列表恢复“暂无数据”。未操作任何既有案件、客户或官文。
- [仍未完成] 旧系统的官文历史清单、检查清单、文件解锁/标签转换/对冲及更完整批量导入导出仍未补齐；因此该模块不能称为旧系统全量对齐或部署测试服务器完成。

## 2026-07-30 四方代码审计收口：账户与 Lawsuit 依赖域（仅记录，未改业务）

## 2026-07-30 合同标的专用闭环第一阶段（本地页面已验证）

- [已完成代码] 以旧 `FCM_Contract_Object`、合同草稿/详情/归档页面的“合同标的”标签为准，新增独立 `contract_objects` 和 `contract_object_logs` 实体；合同详情通过专用 API 提供标的创建、修改、删除、完整日志和关联案件回看，不再把多条标的压缩为合同备注。
- [已完成规则] 标的仅可关联当前合同客户范围内、当前账号可见的案件；同一合同下“案件 + 费用类型”禁止重复。合同草稿、已拒绝、已通过、履行中、已完成可由合同负责人/管理人员维护；审批中和已归档合同严格只读。每次新增、修改和删除都记录合同标的日志和合同工作流事件。
- [已完成可用性] 新增专用“合同标的候选案件”接口，返回全部数据范围内的同客户案件，不再通过通用分页记录接口只读取前 100 条而遗漏候选；详情表增加关联案号真实跳转和“日志”入口，归档/审批中的详情仍可只读查看。
- [已验证页面闭环] 管理员从“新建案件”页面以现有已通过合同创建唯一临时案件 `SHMS2600390`（标题含 `SMOKE`）；回到合同详情的“新增标的”时，候选清单同时出现既有同客户案件和该临时案件，证明候选不是截断的通用前 100 条。页面创建标的后真实回显案号、案件名称、阶段、费用类型、金额、管理人、备注和“日志/编辑/删除”操作；日志显示新增快照。修改费用类型、金额和备注后回显新值；点击标的案号实际打开 `民事案件详情：SHMS2600389`，包含真实案件、客户和合同关联信息；最后通过页面删除标的并回显“暂无合同标的”。
- [已精确清理] 页面删除临时标的后，用仅允许 `SMOKE` 标记记录的本地测试清理入口删除临时案件 `SHMS2600390` 并复查 `404`；为不污染被复用的既有合同，只删除本轮确定的三条临时工作流日志（新增、修改、删除合同标的），复查该三条、合同标的和标的日志均为 `0`。未删除或修改任何既有案件、客户、合同或历史日志。
- [仍未完成] 审批中/归档状态的页面禁用和服务端阻断尚未建立可清理样本的页面证据；旧付款申请读取标的、合同专用 Excel 交付仍是独立缺口。
- [新锁定 P0 缺口] 旧合同付款不是通用费用创建：源码要求先选付款类型，再按合同标的逐案件展示官费/提成待付余额、逐行勾选和填写本次支付，提交专用请款申请明细。当前本地“合同付款”仍是通用 `/finance/fees` 弹窗，缺逐标的余额、付款类型、逐行金额与专用阻断；已登记到 `docs/旧系统前后端与新系统全量功能差异总表.md`，后续按专用流程整改。
- [2026-07-30 合同付款 P0 首轮实施] 已新增独立 `ContractPaymentLine` 明细与合同付款申请专用接口：候选接口按合同标的金额减去待审批/待付款/已付款申请的已占用金额计算真实余额；提交接口校验合同状态、付款类型、可见范围、重复标的、金额大于零及不超过余额；审批从待审批到待付款，付款必须填写凭证号并生成 `FinanceTransaction`，驳回自动释放占用。通用 `/finance/fees` 不再由合同详情调用。合同详情已改为付款类型、收款资料、申请日期、逐标的选择和“本次支付”表格。
- [已验证页面入口] 本地 Docker 重建后，浏览器重新加载合同列表并打开“合同付款”，实际显示新专用表单及“按合同标的逐项申请”说明，不再显示旧的费用名称/金额/费用类型通用费用弹窗。所选既有合同没有合同标的，故空态正确显示“当前合同没有可付款的合同标的”；提交、审批、付款、超额阻断及精确清理仍需以临时合同标的完成，不得把本次入口验证写成全流程已验收。
- [页面数据与余额验证] 管理员通过合同详情的“新增标的”页面，选择现有同客户案件并创建临时标的 `SMOKE合同付款费用` 金额 `123.45`；合同付款页真实回显该行。页面勾选后提交 `100.00` 的合同付款申请，再次打开页面回显“已占用 100.00 / 待付余额 23.46”，证明专用明细已实际落库并参与余额计算。随后尝试输入 `23.47` 时组件将数值限制为可支付最大值 `23.46`，从而又产生一笔合法剩余额申请；这不是服务端超额放行，但不适合作为超额拒绝证据。两笔临时申请、付款明细、临时标的及精确对应合同工作流日志均已删除复查为零，既有合同/案件未变更。
- [本轮修正] 为防止存在待审批、待付款或已付款申请的标的被修改/删除造成外键或业务断链，服务端现会明确 `409` 阻断该标的维护；前端提交前也增加“本次支付金额不能超过待付余额”的显式校验。最新前端生产构建、Python 编译、菜单审计与差异检查通过；仍需重建本地 API/Web 后继续做审批、付款、服务端超额及标的锁定的闭环验证。
- [正式发文首轮后端] 已增加独立 `OfficialOutgoingDocument`，以专用 `official_outgoing` 记录承载正式编号、来源合同/案件、选中文件、送审标记与盖章附件占位；`POST /official-outgoing` 严格验证来源模块和选中文件归属，送审进入待审批、免审进入已通过；另有列表、审批通过/拒绝和申请人/管理员撤回接口，所有动作写独立工作流。打印/下载、盖章文件上传、独立附件页、合同/案件页面入口及浏览器闭环仍未完成，不能将本轮后端骨架视为正式发文模块已交付。
- [正式发文列表入口] `documents-outgoing` 已注册到后端菜单及前端导航，文档中心将该路由映射至独立“正式发文”标签，调用 `/official-outgoing` 展示编号、来源、客户、状态、申请人与创建时间，并提供明确空态；前端生产构建通过。创建弹窗、合同/案件页面入口、审批操作按钮及文件生命周期仍待接入。
- [正式发文创建与审批页面] 正式发文工作区已可从可见合同/案件下拉选择真实来源，创建时调用专用接口并默认送审；列表增加待审批“通过/拒绝”和待审批/已拒绝“撤回”操作，均调用专用后端状态接口并刷新数据。前端生产构建通过。来源文件选择、盖章文件、正式附件、预览/打印/下载和从合同/案件详情页直接发起仍未完成。

## 2026-07-30 动作级代码审计增量：正式发文（仅记录，未改业务）

- [已完成代码比对] 已读取旧 AWS 的 `OfficialDocumentController`、`OfficialDocumentAuditController`、`OfficialDocumentFileController` 及正式发文视图目录，并对照当前 `DocumentCenterPage.tsx`、`App.tsx` 与 FastAPI。确认旧站的“正式发文”独立覆盖从合同/案件文件创建、送审、三类审批清单、预览/编辑/审批记录、批量打印/下载、撤回、盖章文件和独立附件生命周期；当前通用“发文登记/送达确认”与官文收文流程均不是等价实现。
- [已登记问题] 上述动作已细化写入 `docs/旧系统前后端与新系统全量功能差异总表.md` 的 P1“正式发文（对外文书）全流程缺失”。本轮只完成源码证据补强，未修改业务代码、未创建数据、未部署；后续应与其他已确认缺口集中实施，避免把通用收发文误当成覆盖。

## 2026-07-30 动作级代码审计增量：合同标的与合同交付（审计结论已部分实施）

- [已完成代码比对] 旧 CMS/FCM 的 `ContractController`、`ContractDraftController`、`ContractArchiveController`、`ContractFileCenterController`、`ContractFileController`、`ContractPaymentController`、`ContractEventController` 与当前合同中心、FastAPI 已逐项检索。当前合同草稿、提交审批、附件、事项、应收、付款/开票跳转、用印、调查、归档和撤销草稿均有专用实现；这部分不因本轮审计而重复验证。
- [已部分实施] 旧站独立 `BizContractObject` 已在当前 React、FastAPI 和模型中补为专用合同标的实体、接口、详情表、日志及同客户案件关联；见本文件顶部“合同标的专用闭环第一阶段”。但付款申请读取标的的场景尚未补齐页面/接口证据，且没有页面闭环验收，不能据此关闭本项。
- [仍确认缺失] 旧 `ContractFileCenterController` 的合同、待审、已审、被拒及应收 Excel 交付，在当前合同页只存在通用 CSV 导出；当前虽有其他财务 Excel 生成器，但没有合同专用导出 API。

## 2026-07-30 动作级代码审计增量：付款单交付与银行来源（仅记录，未改业务）

- [已完成代码比对] 已读取旧 FAM 的 `APController`、`InternalFeeController`、`PaymentPackingController`、`PaymentAuditController`、`PaymentController` 及五家银行回款 Controller。旧付款单同时支持付款包、单付款单号、多个付款单号和内部费用的服务端 Word 输出；当前支付状态流转与浏览器打印入口存在，但前端仅拼接 HTML 后调用 `window.print()`，FastAPI 没有任何付款单 Word/DOCX 生成接口，故打印不能视为旧 Word 交付的等价实现。
- [确认缺失] 旧 FAM 具有工行、中信、广发工行、中行、招商五个来源独立的列表、导入、到账和删除动作；当前银行来源页面/接口仅覆盖工行、中信、中行，广发工行和招商银行仍无来源标识及专用入口。两项保持差异总表 P1，未改业务代码、未创建测试数据。

## 2026-07-30 动作级代码审计增量：调查取证记录（仅记录，未改业务）

- [已完成代码比对] 已读取旧 CIT 调查、线索、取证、取证文件、线索文件、调查任务 Controller 及当前 `InvestigationCenterPage.tsx`、FastAPI。当前调查的线索建档、审核、登记取证、转案件、公证、材料上传、任务、费用、报告和基础证据目录已有对应动作，不将这些已有链路重复记为缺失。
- [确认缺失] 旧调查取证记录的单条/批量创建、编辑、删除、取证地点、公证书号，以及证据原件与发票文件的独立导入、删除、下载，在当前代码没有等价专用模型、API 或页面。仓库实体证物动作与调查取证记录不是同一业务对象，不能代替。已将动作级证据写入全量差异总表 P1“证据/取证记录缺少编辑、删除与完整元数据维护”；本轮未改业务代码、未创建数据。

## 2026-07-30 代码审计更正：系统主数据与法院工作人员（仅记录，未改业务）

- [更正旧结论] 当前代码已实际具备客户类型、案件文件类型、地区、法院工作人员四类系统参数维护；客户创建/筛选及案件附件上传已读取有效参数。法院工作人员已按法院和职务维护，民事/行政案件页面按所选法院筛选法官、书记员，服务端再次阻断跨法院、停用或职务不匹配的值。因此差异台账不再把这些能力列为“完全缺失”。
- [仍未证明] 系统参数删除没有已引用保护和历史快照；地区层级没有发现业务表单受控联动。法院工作人员仍缺管理员维护→案件选择→非法值阻断的真实页面闭环证据。本轮只修正审计台账，未改业务代码、未创建数据。

## 2026-07-30 代码审计更正：知识产权案件与官文（仅记录，未改业务）

- [更正旧结论] 当前已存在 `IprCenterPage`、`IprOfficialFilePage` 和专用 FastAPI 路由：专利/商标案件草稿、审核、结案/重开、期限/年费/费率维护、四类 DOCX、案件清单 Excel，以及知识产权官文上传、校验、转发、办结、批量动作、ZIP、Excel 均已有代码实现。此前“没有 IPR 专用路由/页面/API”及“ZIP/Excel 未实现”的台账内容已过时，已纠正。
- [仍未完成] 旧 IPR 的协作关系/协作费、年费/费率/期限明细、专利与商标官费/发票文件、批量解析与检查清单、自动匹配、历史清单、文件解锁/标签转换/对冲、完整批量导入导出等仍未对齐；商标和新增交付按钮缺页面闭环证据。本轮不重复已有专利闭环，仅更新源码审计结论。

## 2026-07-30 集中整改起始：系统参数引用删除保护（本地已验证）

- [已完成代码] 系统参数删除前会按分类检查客户类型、案件类型、费用类型、法院、法院工作人员、公证处、案由、付款类型、地区、案件阶段及案件文件类型的业务引用；已引用时返回 `409` 并要求停用，避免历史客户、案件或附件失去主数据语义。法院工作人员仍保留既有“必须属于启用法院且职务匹配”的创建/案件提交校验。
- [已验证运行] API 重建并健康后，尝试删除正在被客户使用的默认客户类型，接口返回 `409`，随后复查该参数仍存在。验证过程中旧容器曾删除该默认参数；已在同一轮立即按原始代码、代码和名称恢复，复查仅存在一条默认客户类型，未修改任何客户记录。
- [已验证静态] Python 编译、`git diff --check` 与菜单审计通过；菜单审计新增参数引用删除保护结构断言。

## 2026-07-30 批次 A：律所档案、联系人与营业执照（本地，待页面闭环）

## 2026-07-30 批次 A：客户联系人照片（本地，待页面闭环）

## 2026-07-30 批次 A：员工绩效完整字段与案件适用方案（本地，待页面闭环）

- 旧员工绩效的开庭、文书、案源、调查、品管“比例＋固定额”、基本工资和生效区间已扩展到当前专用提成记录；新增“员工＋案件”查询接口，按案件创建日期选择生效区间内最新方案，无匹配明确返回，不以任务超期事实替代绩效方案。
- 已完成 Python 编译与前端构建；案件查询入口的页面化选择与逐角色页面证据尚未完成，不能标为已验证。

- 已按旧 `CustomerContactsController` 和联系人编辑/查看页面补齐照片的独立上传、替换、预览和下载链路。照片附件通过联系人 ID 绑定，并写入客户事件；普通“客户文档”列表明确排除该内部附件，删除联系人时同步删除其照片记录和受控文件。
- 当前已完成 Python 编译、前端生产构建与静态覆盖约束；仍未做真实客户的页面端上传→替换→查看/下载→删除联系人精确清理，不能标为已验证或部署测试服务器。

- 已将旧 `CRM/LawFirmController` 的机构资料、联系人、新建/修改、默认联系人、有效状态和营业执照图片维护，落为独立管理员专用实体与 API；不能再用客户档案或启动配置中的静态律所信息替代。
- 页面已新增“系统设置 → 律所档案”，可在详情抽屉维护联系人、切换默认联系人、上传/替换/下载 JPG/PNG/GIF 营业执照；默认联系人直接停用、停用联系人设默认、非管理员访问均在服务端阻断，所有写入均生成律所审计记录。
- 已验证：菜单覆盖审计 `274 nodes / 228 leaves`、Python 编译、前端生产构建和 Docker API/Web 健康检查；本地页面已完成“新建律所 → 自动默认首位联系人 → 新建第二联系人 → 切换默认联系人 → 受控删除并回到空列表”的真实闭环，临时机构及两名临时联系人已清理。仍未证明：营业执照替换下载，以及律所资料在合同、用印、财务打印和文书生成中的历史快照引用；因此不得标记为完成或部署测试服务器。

- 已核实旧 `Account` 的账户资料、改密、消息中心、沟通记录与当前用户中心、通知中心、沟通日志和安全策略专用 API 对应；管理员重置一次性密码和首次强制改密为当前更严格实现，不形成新缺口。
- 已核实旧 `Lawsuit` 并非孤立废弃目录：`Scripts/Legal/Case/Case.Create.CaseInfo.js` 实际调用其案件客户、法院、事项、费用确认/票据确认、费用/文件解锁、附件和内部费用动作。它是旧 Legal 案件流程的底层依赖，已纳入“案件复合操作尚未逐项证明”和“财务收付款与打印仍缺全状态证据”两项 P0，不另建会重复的平行案件模块。
- 旧业务 Area（排除 `bin`、`AFM` 无 Controller 和 `Console - 副本`）均已至少完成 Controller 级盘点；仍在收口的是对已登记 P0/P1 的旧脚本参数、旧页面显示条件、新接口/数据落点和验证证据的动作级矩阵，而不是继续依赖菜单名称判断。

## 2026-07-30 四方代码审计增量：系统、仓储与报表（仅记录，未改业务）

- 旧 System/BAS 的参数、地区、法院工作人员、缓存、配置、菜单和角色动作均能在当前系统管理模块找到对应专用实现；该结论不替代既有参数引用、停用保护与页面证据工作。
- 旧 WMS 的库存列表/货位库存计数与当前仓库证物生命周期不构成新的确定缺口；已验收的证物入出库闭环不重复操作。
- 旧 RPT 统计与当前六类真实统计 API/页面可对应；独立 `LargeScreen` 大屏入口未在当前菜单、路由或组件发现，已写入差异总表 P2“报表大屏独立入口缺失”。

## 2026-07-30 四方代码审计增量：客户与人事（仅记录，未改业务）

- CRM 客户、联系人、附件、事项、分配、共享、回收/恢复、门户开停与律所 Controller 已与当前客户中心、客户专用 API 核对。联系人照片、独立律所档案/执照/联系人仍为既有 P1 缺口；其余客户核心生命周期已有专用实现，未重复登记。
- HR 员工、请假、档案、事项、员工文件、角色、部门和密码重置已有当前专用实现。新增确认不一致：旧员工绩效设定含开庭/文书/案源/调查/品管的比例与固定额，并可按“员工+案件”查看适用绩效；当前仅有比例型提成设定，任务超期事实不能替代。已写入全量差异总表 P1“员工绩效设定与按案件绩效查看不完整”。

## 2026-07-30 四方代码审计增量：任务、用印与正式发文（仅记录，未改业务）

- 旧 TP 任务 Controller 的审核、回退、完成、拒绝、停止、重新开启、办理、删除、节点、文件和消息已与当前 `TaskCenterPage.tsx` 及 FastAPI 批量生命周期、任务节点、消息已读/未读、附件和三条时间规则逐项检索。当前已有对应专用接口；已验证的任务规则不重复验收，剩余以逐角色/状态页面证据为准。
- 旧 AWS 正式文书 Controller 复核发现一项新增明确缺口：旧系统可从合同或案件创建正式发文，完成预览/编辑、审批、打印/下载、撤回、盖章文件上传及附件生命周期；当前收发文台只有“官文收文”处理，二者不能等价。已新增至全量差异总表 P1“正式发文（对外文书）全流程缺失”。
- 用印申请、审批、实际用印、归档、印章资产与 ZIP 文件交付在当前系统均有专用实现；本轮未发现可由收发文替代的用印缺口，也没有进行页面写操作或创建测试数据。

## 2026-07-30 四方代码审计增量：合同与财务 Controller 动作复核（仅记录，未重复修补）

- 已逐 Controller 抽取旧 CMS 合同动作：草稿、提交审批、逐节点审批、附件上传/下载、合同事项、合同变更、合同付款、归档、合同标的、合同/审批/应收 Excel 和合同级 Excel；与当前 React/FastAPI 核对后确认草稿、审批、变更、附件、事项、用印衔接、调查任务、付款申请和归档已有专用实现。合同标的明细、合同及应收的真实 Excel 仍为既有 P1 缺口，未发现新的独立缺口，避免重复登记。
- 已逐 Controller 抽取旧 FAM/FAS/FSC/FIO 的收款、请款、付款审核、付款打包/校验/撤回、内部费用、结算、归档结算、开票、现金、银行流水、专利官文等动作。当前差异总表中的“付款单 Word”“广发工行/招商银行来源”“专利官文检查清单”和“财务全状态证据”均有对应旧代码依据，未因通用财务页面存在而关闭；本轮未修改财务代码，也未创建或变动测试数据。

## 2026-07-30 四方代码审计增量：普通案件文件与清单交付（仅记录问题，未零散修补）

- 以旧 `Areas/Legal/Controllers/CaseFileController.cs`、`CaseListController.cs` 和 `Scripts/Legal/Case/Legal.Case.js` 对比当前 `CaseCenterPage.tsx`、`main.py`。已确认旧站普通案件不仅有附件上传下载：还具备授权委托书、归档函、广东版授权委托书、赔偿函、身份证明、结算提成表、一审/二审双方及执行律师函（含广东版）等真实 Word 生成；单案生成会自动归入案件文件。
- 旧文件控制器还提供批量上传、重命名、解锁、删除、分类转换、通知日期、Word 保存和送用印；旧案件列表提供按筛选导出、选中导出、清单导出真实 Excel 以及选中案件二维码 Word。当前普通案件只具备附件上传/下载/删除/重命名和 CSV 导出，不能以已有知识产权 DOCX 或通用附件动作声称对齐。
- 已将该批动作和新旧差异补充到 `docs/旧系统前后端与新系统全量功能差异总表.md` 的 P0“案件专用文书生成及案件清单交付能力缺失”。本批次遵循“先全量审计、后集中修改”：未为任何单一截图追加临时按钮，未创建测试数据，也未部署服务器。

## 2026-07-30 案件复制动作补齐（本地，待页面闭环证据）

- 已按旧系统 `Legal/Case/Merge`、`Merged`、`Duplicate` 的代码证据补齐其中的“复制案件”专用动作：仅案件负责人/同部门管理角色可发起，且非管理员还必须拥有该案件类型的新建菜单权限；前端按服务端返回的 `can_duplicate_case` 能力决定是否显示，不以隐藏按钮替代接口鉴权。
- 新案件始终由服务端生成新的紧凑案号，保留原案件编号、原记录 ID、复制人和复制时间；关联合同重新按当前可见范围、合同状态和有效部门校验。任务、附件、费用、提醒、排期、归档信息及流程历史不会复制，避免把已发生的工作事实带入副本；新旧案件各自写入可追溯流程事件。
- 已完成静态与部署验证：Python 编译、前端生产构建、菜单覆盖审计（273 节点、227 叶子、0 未处理）、`git diff --check` 以及 `docker compose up -d --build api web` 均通过；API、Web、PostgreSQL、Redis、MinIO、Worker 均为 healthy。
- 页面点击证据尚未完成：当前受控验收标签仍保持旧版页面脚本，虽已确认本地 API 对管理员返回 `can_duplicate_case=true`，但新按钮尚未在该失效标签中渲染。不得把此项标记为完整页面验收；后续必须在新静态资源标签中从案件详情点击“复制案件”、确认新案号和原案追溯，再精确删除本次副本。

## 2026-07-30 案件合并动作补齐（本地，待页面闭环证据）

- 已按旧 `CaseController.Merged` 和 `PartialView/Merge.cshtml` 的业务语义新增案件详情“合并案件”：只接受同一客户、同一案件类型、未归档且未合并的来源案件；当前案件和来源案件均需由当前负责人/管理员权限控制，服务端同时校验可见范围，不能以界面限制替代鉴权。
- 合并会把来源案件的 `finance` 记录（包含案件费用和内部费用）及案件附件迁入当前案件，并保留来源案件编号、ID、迁移时间和费用迁移事件；来源案件不物理删除，而是置为“已合并”并记录合并目标，以保留审计、通知和历史关联。任务、提醒、开庭排期和原有流程历史不迁移，避免篡改已发生工作事实。
- 已完成 Python 编译、前端生产构建、菜单覆盖审计、`git diff --check`、Docker API/Web 重建及健康检查。管理员能力接口返回 `can_merge_case=true`；对“当前案号作为待合并案号”的专用接口阻断已返回预期错误。尚未创建临时来源案件并完成实际费用/文件迁移与页面点击/精确清理证据，因此不得标记为完整闭环。

## 2026-07-30 旧案件详情维护动作补齐（本地，待页面闭环证据）

- 从旧 `Legal/Case/Legal.Case.js` 及 `Views/Case/View.cshtml` 核对后，新增民事案件“修改公证信息”（公证书号、存放位置）和诉讼类案件“修改诉讼/判决金额”（诉讼标的、判决/和解金额）两个独立受控动作；两者均不再依赖通用记录更新接口。
- 服务端按案件负责人/管理权限、案件类型和归档/合并状态拦截不合规操作，变更前后值和说明均进入案件流程事件；详情页提供对应表单，保存后重新加载真实详情数据。
- Python 编译、前端生产构建、菜单覆盖审计、差异检查和 Docker API/Web 重建均通过。浏览器当前可控标签被确认连接到旧系统页面而非本地 React 容器，未对旧系统执行写操作；因此本项仅有代码与运行时部署证据，尚无新页面点击与临时数据清理证据。

## 2026-07-30 知识产权案件文书与清单导出补充（本地）

- 已实现、待页面闭环证据：知识产权官文新增真实清单 Excel 与选中原件 ZIP 打包下载。两类导出都按当前账号的记录范围执行；ZIP 对全部选择项逐条验证可见性、存在性和原件路径，任一原件缺失即拒绝生成不完整包。前端已提供“导出 Excel”“打包下载（ZIP）”按钮，文件使用 Blob 下载，不伪造成功结果。
- 已实现、待页面闭环证据：在办知识产权案件新增独立“维护期限/年费/费率”动作。后端仅允许记录负责人或管理人员对“在办”案件维护，草稿、审核中、驳回和已结案均被专用接口阻断；每次变更写入原值、新值和维护说明的流程日志。前端案件详情提供对应受控表单，不再错误复用草稿编辑接口。
- 已修复官文到案件的精确关联：知识产权官文列表和详情均携带关联案件 `record_id`；目标专利/商标案件页面会在当前可见列表内解析，未命中时按受权限保护的详情接口回查并自动打开真实案件详情。目标不存在、无权限或类型不符时保留当前页并提示，不能再只跳转到案件列表。
- 菜单覆盖审计已新增该关联链路的静态回归断言；前端生产构建、Python 编译、菜单审计和 `git diff --check` 均通过。本地 Web 已重建，浏览器页面点击证据仍待在新静态资源标签中补录。
- 已实现并页面验证：知识产权案件详情可生成案件信息表、授权委托书、律所函、主体身份证明核对单四种真实 DOCX；文件以案件附件保存，带下载接口和流程日志。页面测试样本及其唯一上传文件已按编号和附件路径精确清理，数据库回查为零。
- 已实现、已构建部署但尚未获得页面点击证据：专利/商标案件台账的“导出 Excel”入口和受同一菜单、角色数据范围、案件类型、状态、关键字约束的 SpreadsheetML `.xls` 导出接口。浏览器当前标签保留旧版静态脚本缓存，重进业务菜单已确认测试数据清理，但该按钮未从缓存版本出现；不得把这一项标记为页面验收通过。
- 已运行：后端 Python 编译、前端生产构建、`docker compose up -d --build api web`；API healthy，Web 容器已启动。后续先补导出按钮点击和文件内容证据，再继续旧系统未对齐的 IPR 导入/去重、官费/发票、年费/费率/期限与协作生命周期。

## 2026-07-30 四方代码审计启动：仅记录，不提前零散修补

### 2026-07-30 批次 A 开始：基础参数扩展（本地未部署）

- 已把旧系统的客户类型、案件文件类型和地区层级加入 FastAPI 可维护参数白名单、默认参数补种逻辑、系统参数菜单及 React 管理页；现有数据库启动时会补齐缺少的默认项，不覆盖用户已有参数。
- 客户列表、新建及编辑表单现从受限的 `/customers/reference-options` 读取启用客户类型；创建和列表筛选均由后端按同一启用参数校验，不再写死“客户/当事人”。
- 案件中心的上传分类现从同一案件参考接口读取启用案件文件类型；后端案件附件上传同步拒绝不存在或已停用的分类（票据/发票等专用类别除外）。
- 已运行 Python 编译与前端生产构建，均通过。本轮尚未执行完整冒烟、浏览器点击或服务器部署；客户、案件文件上传和地区选择对新参数的业务引用仍在同一批次后续实现，不能标记为完整闭环。

### 2026-07-30 审计增量：知识产权案件专用代码缺口已确认

- 旧系统 `Areas/IPR` 的 37 个 Controller 覆盖专利/商标案件、年费、费率、期限、协作费、官文、官费/发票文件、批量导入导出等独立生命周期；对当前 React 和 FastAPI 以相应代码标识进行全局检索均无匹配。
- 该结论已写入 `docs/旧系统前后端与新系统全量功能差异总表.md` 的 P0 条目；本轮仅记录证据，未改业务代码、未创建测试数据、未部署。

- 当前执行目标已切换为：以旧系统 `cshtml + JavaScript + Controller/业务处理` 与新系统 `React + FastAPI` 的实际代码为四方证据，先完成全量能力映射，再集中整改；不以旧截图、菜单存在或历史说明文字代替代码核对。
- 本轮已确认并单独记录到 `docs/旧系统前后端与新系统全量功能差异总表.md`：专利官文专用导入/处理/检查清单流程缺失（P0）；案件四类真实文书生成缺失（P0）；法律官文选中后发起案件费用及批量案件维护缺失（P1）；客户联系人照片缺失（P1）；合同标的明细、合同 Excel、付款单 Word、取证记录完整维护等差异；员工档案无受控归档替代旧删除操作（P2）。
- 已复核为不应误判“缺失”的范围：系统角色/菜单权限已有专用后端模型和页面；普通收发文已有登记、签收/送达、归档、附件、官文收文处理及日期修正；但专利官文不是其等价物，仍需独立补齐。
- 本轮未改业务代码、未创建测试数据、未部署服务器。下一步继续完成旧 FAM/FAS/FSC、CMS/FCM、Legal/Lawsuit/IPR、TP、AWS、CRM、HR/System、WMS/RPT 的动作级代码映射；只有审计清单达到可集中实施的程度才进入修复与验证。

## 2026-07-29 会话继续：跨入口财务关联详情补齐

- 本轮修复尚未提交：附件关联业务、全局检索结果和站内通知的财务专用记录此前只完整支持普通费用、发票和退款。现在付款包（`finance_package`）、普通结算（`finance_settlement`）、归档结算（`finance_archive_settlement`）也携带精确详情上下文，统一在“费用查询”加载对应的只读详情。
- 同步调整：消息通知路由、全局搜索模块白名单、详情导航类型和菜单覆盖审计均已对齐；`scripts/smoke-api.py` 的启动清理与结束零残留检查已包含三种专用财务模块。
- 当前验证：`powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 已通过（前端生产构建、Python 编译、菜单覆盖审计、19 组 API 冒烟、`SMOKE_CLEANUP_OK`、Compose 配置）。随后 `docker compose build web`、`docker compose up -d web` 完成，`docker compose ps web` 显示 healthy。
- 证据边界：本轮尚未用浏览器对付款包/普通结算/归档结算附件、搜索项和通知逐项点击；当前状态只能写作“已部署、自动化已验证、页面点击待补证”，不得表述为三类入口已完成页面验收。

## 2026-07-29 仓储关联回归证据

- 仓储失效案号的页面点击已证明：保持当前页并明确提示；临时页面数据已精确清理。

## 2026-07-29 会话续接：关联空跳转横向整改

- 已把仓储、用印、平台财务、收发文、合同应收和合同详情的显示型关联字段接入统一预解析；避免目标失效后误跳到无筛选列表。
- `verify-local.ps1` 已完整通过且无测试残留。此轮属于代码/自动化证据；各模块仍需在可安全清理的页面样本上补浏览器点击证据。

## 2026-07-29 会话续接：财务失效关联空跳转修复

- 财务案件、合同和客户三类关联字段已统一为“先解析、后导航”。不存在、已删除或无权限的关联对象不会再将用户带到无目标列表。
- 页面复验已覆盖失效案号：停留当前费用查询页并提示原因；完整本地校验和 Docker Web 重建均完成。
- 本轮未创建测试业务数据；费用查询中发现的历史测试样本不作为成功验收样本，继续优先使用可安全清理的页面新建数据或现有正式可见样本。

## 2026-07-29 会话续接：仓储证物页面闭环新增证据

- 已完成真实页面证据：`SMOKE-WH-20260729-01` 经“登记 → 入库 → 出库 → 重新入库 → 流程记录”闭环，所有状态均由专用仓储接口写入，未使用数据库造数。
- 已完成真实关联跳转：用临时证物关联现存案号 `SHMS2600388` 后，案号按钮可进入并自动打开完整案件详情；此前填入不存在案号的跳转不作为成功证据。
- 已完成精确清理：受限非生产测试清理后，仓储页面刷新确认临时记录为零；无代码改动，本轮不需重建服务。后续继续处理尚未证明的角色权限和财务/文档等页面闭环，不重复已验收流程。

## 2026-07-29 会话持续交接：印章资产页面闭环已补

- 管理员页面创建未引用临时印章资产 `SMOKE-SEAL-0729`，列表回显编号、名称、0 份累计用印、空最近使用与可用状态；同一行删除后，经受权限保护的只读列表复查不存在。该项不涉及用印申请、审批或资产引用阻断，后续仍按生命周期台账验收。

## 2026-07-29 会话持续交接：合同附件草稿页面闭环已补

- 页面创建临时合同草稿 `HT20260729165202` 时真实选择 TXT 附件；合同详情回显附件、草稿状态与基础信息。随后通过详情“撤销草稿 → 确认撤销”完成精确清理，服务端只读回查为零，本地临时文件也已删除。
- 无附件前置阻断、带附件草稿保存/详情回显/撤销清理均已有页面证据；“指定审批人 → 提交审核 → 审批状态流转”的完整合同审批分支不在本条范围，继续按跨角色合同台账验收。

## 2026-07-29 会话持续交接：合同无附件前置阻断已补

- 页面填写临时合同基本信息但不选择附件，点击“下一步”后明确提示“请上传合同附件”，页面保持在第一步且未创建任何合同数据；该项只证明无附件前置阻断，不替代附件上传、审批或草稿清理闭环。

## 2026-07-29 会话持续交接：AI 智能文档下载确认门禁已补

- 修复并页面验证：未人工确认的 AI 文档没有 DOCX 入口，接口同样返回 `409`；在页面编辑并人工确认后才显示 DOCX 并可下载。确认后的内容如被修改，后端会要求再次确认。
- 临时页面文档 `AI202607291646211255`（`SMOKE` 标记）已精确清理；该项不再列为后续未证明页面。

## 2026-07-29 会话持续交接：任务资料附件页面证据已补

- 已完成未重复验收项“任务新建资料附件”：页面创建临时任务 `RW20260729163936728042`，新建时真实选择 TXT 文件，详情回显文件、上传人、上传时间及历史事件；下载动作已在页面触发，删除二次确认后附件列表回到“暂无任务资料附件”。
- 已使用任务专用受状态保护入口精确清理该单条“待接收、无子任务”临时任务（`deleted=1`、`failed=0`），并删除本地临时附件。不要将该验证重复列入后续未证明项。

## 2026-07-29 账号生命周期依赖阻断（本地）

- 新增账户依赖保护：合同当前审批人不可从系统用户页停用或删除；关联 HR 员工档案的账号不可从系统用户页直接停用或删除，必须通过 HR 办理状态同步。
- 已通过完整自动化：`verify-local.ps1` 全绿，19 组 API 流程与零残留清理均通过；其中新增待审批合同阻断与员工档案关联阻断断言。
- 已通过页面补证：页面新建临时员工账号后，系统用户页删除被“关联员工档案”明确阻断；HR 办理离职后账号同步显示“停用”。临时 HR 记录和账号均已精确清理，页面刷新确认无残留。
- 证据边界：合同待审批人停用/删除的阻断目前已有独立 API 自动化证据；仍待有安全可清理的页面合同审批场景时补浏览器提示，不重跑已验收合同主闭环。

## 2026-07-29 会话继续：任务反馈附件页面闭环（本地）

- 已完成并已完整验证：任务附件上传从“只取 `originFileObj`”修复为同时接受原始 `RcFile`，覆盖新建任务资料、反馈附件和详情任务资料三处。
- 页面证据：以页面创建临时任务 `RW20260729161323251899`，修复后提交文字与 TXT 附件，详情回显附件记录和历史；下载接口真实返回文件，页面删除后附件列表为空。
- 清理证据：受保护清理接口仅删除该条处于“待接收”且无子任务的临时任务，结果 `deleted=1`；刷新页面后该编号不再出现，临时文件已删除。未重复验收任何既有任务闭环。
- 自动化验证：`verify-local.ps1` 已通过前端构建、Python 编译、页面审计、19 组 API 冒烟和零残留清理。
- 下一步：确认容器健康并提交 `dev` 到测试服务器；随后继续未证明页面，而不是重复已验收的案件—线索—调查任务闭环。

## 2026-07-29 案件/合同客户失效关联阻断补证（本地）

- 已完成：案件详情、合同详情统一使用受保护客户解析；关联客户不存在或当前用户无权限时，保持在来源详情并显示明确提示，不再导航到无关客户列表。
- 已通过页面验证：以已有案件 `SHMS2600388` 为只读样本，点击其无法解析的客户后，案件详情仍保持打开、地址仍为 `?page=case-company`，且页面显示“未找到关联客户或当前账号无权查看”。未创建、修改或删除业务数据。
- 已通过完整本地验收：`verify-local.ps1` 全部通过（含 19 组 API 流程与 `SMOKE_CLEANUP_OK`）；本地 Docker 的 API、Web、PostgreSQL、Redis、MinIO、Worker 均健康。新增静态防回归项 `CASE_CONTRACT_CUSTOMER_PREFLIGHT_OK`。

## 2026-07-29 全局搜索案件—合同关联页面证据（本地，只读）

- 已通过页面验证：搜索现有案号 `SHMS2600388` 后，案件结果进入该案完整详情；详情内合同号 `HT2026060097` 继续打开对应合同的完整只读详情，未退回列表或通用页面。
- 数据边界：该案展示的客户名称在当前可见客户主数据中无可解析对象，未将其误标记为客户详情跳转已通过；未创建、修改或删除任何业务数据。

## 2026-07-29 组织设置页面验收补证（本地）

- 已通过页面闭环：管理员分别在“部门管理”“角色管理”创建本轮唯一临时部门/岗位角色，完成负责人、排序和岗位动作权限的修改回显，再从对应行经二次确认删除。
- 已精确清理：页面回查 `页面验收临时部门0729` 与 `页面验收临时岗位0729` 均为 `0`；未修改既有部门、岗位、账号或员工档案。删除保护已核对：被账号/业务记录使用的部门或被员工档案使用的岗位不能删除。
- 未扩大结论：本轮不重复既有人事、任务或角色菜单验证；“系统角色变更 → 新会话菜单/数据范围回读 → 清理”的跨会话证据仍在未证明台账中。

## 2026-07-29 合同事项记录与草稿专用撤销页面闭环（本地）

- 已新增专用撤销：`DELETE /contracts/{id}/draft` 仅允许合同负责人、同部门负责人或管理员撤销“草稿”状态合同；已提交合同、审批节点、应收计划、到账记录或下游案件/用印/财务关联均由服务端 409 阻断。通用记录删除继续拒绝合同，不能绕过合同专用流程。
- 已通过页面闭环：管理员在“合同新建”选择已登记客户，填写“UI合同事项验收-20260729”，上传有效 PPT 附件并进入草稿；在“我的合同”打开详情，确认附件真实回显，新增一条独立事项记录并在详情时间线真实显示。
- 已精确清理：同一合同详情点击“撤销草稿 → 确认撤销”，列表从 5 条恢复为 4 条，页面提示“合同草稿已撤销，附件和事项记录已一并清理”。临时合同、附件、事项记录和工作流均不再保留；未触碰既有合同。
- 自动化回归：`verify-local.ps1` 已通过（前端生产构建、Python 编译、菜单审计、19 组 API 冒烟、零测试残留、Compose 校验）；API/Web 容器已重新构建并健康。

## 2026-07-29 首次改密刷新恢复页面闭环（本地）

- 已修复：首次登录强制改密账号刷新页面时，后端已返回改密状态但前端仅清除令牌、未退出已挂载工作区的问题。现统一清除本地会话、重置工作区、回到根登录页；用户重新登录后仍会进入不可绕过的首次改密窗口。
- 已通过内置浏览器页面验收：管理员从“系统用户管理”创建临时普通账号，列表真实回显一次性初始密码提示；该账号首次登录出现改密窗口；刷新后回到登录页而非保留主框架；重新登录后仍被要求改密；完成改密后才进入普通用户可见菜单。随后由管理员在同一系统用户页面删除临时账号，列表确认零残留。
- 已补静态门禁：覆盖刷新改密分支必须清理会话并将 `loggedIn` 设为 `false`，防止后续只清令牌却保留已挂载页面。

## 2026-07-29 强制首次改密刷新恢复与签发边界整改（本地）

- 已修复：账号被要求首次改密时，刷新或重新打开页面后不再因前端丢失 `must_change_password` 状态直接进入主框架；系统会清除旧会话并要求重新登录，随后展示不可关闭的首次改密窗口。后端仍对除个人改密接口外的全部业务 API 返回 428，形成双重阻断。
- 已统一管理员签发密码语义：系统用户页面新增账号、员工页面新建登录账号、系统用户编辑时填写新密码，均标记为一次性密码并要求本人首次登录修改；管理员不得通过“重置密码”重置自己，必须使用个人设置修改，避免把当前会话锁死。
- 已补自动化门禁：静态审计覆盖会话恢复、系统/人事账号签发和当前管理员重置保护；冒烟测试验证当前管理员重置自己返回 409，并继续验证重置后的首次改密与业务 API 428 阻断。完整 `verify-local.ps1` 已通过（19 组 API 闭环、零残留清理）；Docker API/Web 已重建并健康。
- 仍待页面证据：内置浏览器当前标签接管异常，本轮不能把自动化结果冒充为页面点击证明；后续须用页面创建临时账号，验证“首次登录 → 改密 → 刷新保持阻断 → 改密完成进入系统”，再精确清理。
- 浏览器恢复纪律：已将“标签失效先自行弃用旧对象并重建可控验收标签”的规则写入 `AGENTS.md`。本轮已在本地重新建立可控标签，并以页面证据确认当前 `admin` 的“重置密码”按钮禁用；后续不再将常规标签恢复工作交由用户。
- 人事账号同步补强：离职/停用状态现在同步普通用户、部门负责人和审计人员账号；仅系统管理员账号保留专门保护。冒烟测试已扩展为将员工提升为部门负责人后完成离职，并证明该账号同时被停用。

## 2026-07-29 系统管理员账号删除保护整改（本地）

- 已修复：系统用户删除接口现在拒绝删除任何 `admin` 角色账号，而非仅阻止删除当前登录账号；系统用户页面同步隐藏管理员行的删除按钮，避免把最高权限账号误作为普通人员账号处理。
- 已撤回不安全方案：员工档案页面不再尝试直接删除关联登录账号。该方案无法可靠解析账号关联、可能遗留在职档案，并会绕过人事停用流程；后续如需清理独立测试账号，应使用带后端一致性校验的专用流程。
- 自动化证明：冒烟测试动态提升一名临时经理为管理员后，断言管理员删除返回 409，再恢复其角色继续全量任务权限验收；完整 `verify-local.ps1` 通过，19 组 API 闭环及测试数据清理均通过。Docker API/Web 已重建且健康。

## 2026-07-29 未读任务批量已读与叶子路由权限页面闭环（本地）

- 已修复：普通员工访问“我接受的任务”等深层叶子路由时，原先错误按父级菜单键拦截，造成侧栏显示入口而页面报“无权访问”。路由权限现在优先按实际叶子菜单键判定，再兼容容器菜单键；管理员全权限不受影响。
- 已通过页面闭环：管理员在“我发起的任务”创建唯一临时任务，普通员工可进入“我接受的任务”、打开任务并提交沟通反馈；管理员在“未读新消息的任务”看到对应未读行，勾选一条后执行“标记已读”，页面提示已处理 1 条，列表由 11 条变为 10 条、顶部未读角标同步下降。
- 清理状态：临时任务已通过其专用页面流程撤回，不再处于可流转状态；临时普通账号已从本地系统精确删除并二次查询确认不存在。任务专用流程禁止通用物理删除，这是预期的生命周期保护，不能为测试便利绕过。
- 安全复核结论：员工页不应直接删除登录账号。员工档案与账号存在独立生命周期，不能由前端推断账号 ID；已撤回该入口，后续测试账号清理必须使用受控的专用人事停用/解绑流程。另需补“不可删除任何管理员、不可遗留在职人事档案”的服务端约束后，才可考虑独立账号删除能力。
- 证据边界：内置浏览器在 Docker Web 更新后仍反复保留旧入口脚本；本轮不以该缓存异常或接口清理替代页面证据。完整 `verify-local.ps1` 已通过（前端构建、Python 编译、菜单审计、19 组 API 闭环与冒烟数据零残留）。

## 2026-07-29 通用记录菜单授权边界整改（本地）

- 已修复：通用记录的直接“新建、编辑、流转、批量导入、导入模板下载、导出”入口现按业务模块映射到其所属菜单族，并在服务端校验实际角色菜单授权；隐藏或移除菜单后，不能再通过直接调用通用接口写入、导入或导出对应模块。
- 映射覆盖客户、合同、案件、任务、调查/线索/公证/证物、收发文、用印、财务/平台财务、人事、仓库、报表和系统记录；未定义的模块直接拒绝通用操作，避免新增模块默认放行。
- 未采用对通用只读 `GET /records` 的粗粒度封锁：合同、案件、任务等表单仍需要在严格数据范围内读取关联客户、案件等选择器数据。后续如需进一步收紧读取边界，应以受限字段的专用关联检索接口替换选择器调用后再实施，不能以破坏现有业务关联为代价。
- 自动化证明：新增普通用户对仓库通用新建和导出的 403 防绕过断言；本轮已重建 API/Web，Docker 服务健康，完整 `verify-local.ps1` 通过（前端生产构建、Python 编译、菜单覆盖审计、19 组 API 闭环和测试数据零残留）。

## 2026-07-29 关联详情与角色数据范围整改（本地）

- 已修复全局搜索：命中附件或文书模板后不再只进入泛列表；现在保存精确对象 ID，切换到收发文对应页签并打开真实只读详情。附件详情提供下载及返回其关联业务的真实入口；模板详情显示版本、状态、字段和说明。
- 已修复关联附件：收发文附件若关联开票申请或诉讼费退款，现分别进入已存在的开票/退款真实详情，不再显示“暂不支持详情查看”。站内消息通知现进入“消息通知”页面，而非只标已读停留在通知抽屉。
- 已修复角色数据范围：服务端只接受“全所数据、本部门数据、授权审批数据、本人及共享数据”四种范围；历史数据库中的非法范围会在启动时回填为该角色默认范围，避免继续落入隐式兜底范围。管理员全量菜单、字段和全所数据锁定不受影响。
- 自动化证明：重建本地 API/Web 后，`verify-local.ps1` 已通过（前端构建、Python 编译、静态审计、19 组 API 闭环和零残留清理）；新增附件/模板精确详情、非法数据范围 422 和历史范围回填的防回归断言。
- 仍待页面证据：本轮新增的附件/模板搜索详情、消息通知跳转及发票/退款附件跳转尚未重新用浏览器逐项点击；只能标记为“已部署、自动化已证”。用印真实附件上传/下载/删除仍受当前内置浏览器没有文件选择能力限制，不能以接口替代页面证据。
- 后续安全整改项：静态审计仍发现通用 `/records` 读写接口未按模块菜单统一授权。该问题不能直接用粗粒度父菜单校验修补，否则会破坏合同、案件、任务等跨模块选择器；需先为“列表/关联只读/创建/编辑”建立精确菜单—动作映射，再补跨角色 API 与页面验收。

## 2026-07-29 用印草稿、附件前置阻断与精确清理页面证据（本地）

- 已通过管理员页面创建：从“用印中心 → 我的用印申请”选择真实关联案件和可用印章，填写必填项后保存唯一临时草稿；列表真实回显草稿状态、关联案号、客户和“查看 / 提交 / 删除”操作，详情页显示持久化的申请信息、关联对象跳转和“用印文件”区域。
- 已通过页面验证阻断：未上传附件时点击“提交”，页面明确提示“请先上传至少一个用印文件后再提交审批”，草稿仍保持未提交状态，未绕过专用用印流程。
- 已精确清理：从同一临时草稿行执行删除并二次确认，列表由 2 条恢复为原有 1 条，未操作已有待审核申请；本地临时附件文件也已删除。
- 证据边界：当前内置验收浏览器不提供本机文件选择/上传能力，不能以接口或数据库替代页面证据；真实“上传 → 下载 → 删除附件 → 有附件提交”仍标记为待该浏览器能力可用时补证，不得据此宣称附件闭环已通过。

## 2026-07-29 Dify 未配置状态页面证据（本地）

- 已通过管理员页面验证：进入“收发文台 → AI 智能文档”，页面显示“Dify 尚未配置：仅创建可编辑提纲”，并明确说明当前操作不会调用智能体或生成正式文书；主按钮实际文案为“创建可编辑提纲”。
- 本轮未提交创建操作，未产生业务数据。该证据证明未配置时的前端表达与实际行为一致；真实 Dify 生成、人工确认及附件回写仍以外部提供方配置后页面闭环为准。

## 2026-07-29 Dify 未配置状态文案与响应语义整改（本地）

- 已修复：未配置 Dify/生成提供方时，AI 智能文档入口、弹窗确认、成功提示和状态均明确为“创建可编辑提纲 / 待配置”，不再把只创建待配置提纲误称为“已生成文档”；配置完成时仍明确使用“开始生成正式文书 / Dify 已生成”。
- 已补接口语义：创建与重试响应返回是否已配置、生成模式和操作结果，前端按真实响应展示提示，不再依赖前端假定成功。
- 自动化证明：完整 `verify-local.ps1` 已通过（前端构建、Python 编译、菜单审计、19 组 API 冒烟及零残留清理）；相关 Docker API/Web 已重建且健康。
- 仍待页面证明：Dify 外部提供方当前未配置，本轮不能宣称真实模型已调用；后续配置后需补“实际生成、人工确认、附件回写”的端到端证据。

## 2026-07-29 全局搜索发票真实详情浏览器证据（本地）

- 已通过管理员页面验证：在全局搜索输入现有发票编号前缀，检索结果显示对应开票申请；点击结果后侧栏自动展开“财务中心 → 开票管理”，进入“我的开票”并加载该记录完整只读详情，实际显示申请号、客户、票据状态、服务项、金额及合同信息。
- 结论：发票搜索结果不再回到控制台或只进入列表，已证明可打开对应真实详情。本轮仅读取既有记录，未创建、修改或删除业务数据。
- 仍待：退款与短信结果仍需在可安全的页面场景分别补同等级点击证据。

## 2026-07-29 印章资产页面新增与精确清理证据（本地）

- 已通过管理员页面验证：进入“用印中心 → 行政用印 → 印章管理”，新增唯一临时印章资产，页面提示“印章已入库”，资产数量从 7 枚变为 8 枚且列表真实回显编号、名称、类别、保管人、位置和零使用量。
- 已精确清理：同一临时资产行点击删除，二次确认明确提示“已被任何用印申请引用的印章将被系统阻断删除”；确认后提示“印章资产已删除”，资产数量恢复为 7 枚。未操作既有资产。
- 证据边界：本轮证明未引用资产的创建、真实持久化和删除；“已被用印申请引用时服务端 409 阻断”仍由自动化覆盖，待有可安全清理的引用场景时补浏览器证据。

## 2026-07-29 印章资产安全删除与测试清理能力补齐（本地）

- 已修复：印章资产此前只有新增、修改，没有可受控的删除路径，导致页面新增的误录或临时资产无法精确清理。现增加管理员专用删除入口和二次确认；无引用资产删除前保留审计快照，已被用印申请引用的资产被服务端以 409 阻断并提示改为停用、维修或遗失，避免破坏既有用印记录。
- 自动化证明：完整 `verify-local.ps1` 已通过（前端生产构建、Python 编译、菜单审计、19 组 API 冒烟和测试数据零残留）；静态审计加入 `SEAL_ASSET_DELETE_OK` 断言。
- 仍待页面证明：本轮未在资产页面创建临时印章并执行删除/引用阻断，因此该专用删除链路为“已部署、自动化已证，仍待浏览器点击证明”。

## 2026-07-29 部门页面创建与精确清理证据（本地）

- 已通过管理员页面验证：进入“人事中心 → 部门管理”，新增唯一临时部门（名称、代码、排序号均由表单填写），保存后页面提示“保存成功”，列表从 18 条变为 19 条并真实回显该部门。
- 已精确清理：在同一临时部门行点击删除，二次确认后页面提示“删除成功”，列表恢复为 18 条；未修改既有部门。
- 证据边界：本轮仅证明部门的页面新增、回显和删除真实落库；岗位创建、角色菜单改动后的新会话权限回读仍待独立页面闭环，不能据此宣称组织与权限全量验收完成。

## 2026-07-29 全局搜索发票、退款与短信详情落点整改（本地）

- 已修复：全局搜索命中发票不再回到控制台，现进入“我的开票”并打开对应开票只读详情；命中诉讼费退款进入对应退款只读详情，详情中的案号与客户仍可进入真实关联详情；命中开庭短信时按其关联案件定位到案件详情，不再落到控制台。
- 已同步：受控业务详情导航上下文扩展到发票、退款；搜索 API 返回上述模块的有效路由和短信关联案件标识；菜单覆盖审计加入对应断言，防止回归为 Dashboard 兜底。
- 自动化证明：完整 `verify-local.ps1` 已通过（前端生产构建、Python 编译、菜单覆盖审计、19 组 API 冒烟及精确清理）；API 与 Web Docker 镜像已重建，两个容器均为 healthy。
- 仍待页面证明：重建后的浏览器会话尚未用现有可见发票、退款、短信数据逐项点击，因此本条为“已部署、自动化已证，仍待浏览器点击证明”，不能替代各模块的流程验收。

## 2026-07-29 官文收文批量业务处理页面闭环（本地）

- 已通过页面创建与验证：上传两条唯一临时 TXT 附件后，列表真实生成两条“已导入 / 未处理”官文收文记录；勾选两条记录并点击“标记已处理”后，页面提示“已标记 2 条官文为已处理”，两条记录的生命周期状态仍为“已导入”、业务处理状态变为“已处理”。
- 已通过页面回退验证：重新勾选同两条记录并点击“标记未处理”，页面提示“已标记 2 条官文为未处理”，两条记录均恢复为“已导入 / 未处理”。
- 已精确清理：从同一页面勾选两条临时记录，确认弹窗明确提示“将永久删除 2 条正式记录及其附件”，确认后提示“已删除 2 条正式收文记录”，列表从 4 条恢复为原有 2 条；两份本地临时附件也已删除，未操作既有收文数据。
- 证据边界：本轮证明批量处理状态与导入生命周期状态独立、可往返且可由专用页面删除清理；不替代已验收的单条日期修改、附件关联或其他收发文流程。

## 2026-07-29 官方收文页面闭环与专用删除修复（本地）

- 已通过页面验证：上传唯一临时 TXT 附件后，用页面日历将收文日期从 2026-07-29 修改为 2026-07-28，提示“已修改 1 条收文日期”，导入状态仍为“已导入”、业务处理状态仍为“未处理”。
- 已修复：删除按钮原先错误调用通用记录删除入口，现改为专用 `/documents/official/delete`；仅可删除可见的未处理官方收文，负责人/管理员权限受控，已处理记录明确阻断，附件与流程日志同步清理。
- 已通过页面复验：重建 Web/API 后确认删除唯一临时记录，提示“已删除 1 条正式收文记录”，列表由 3 条恢复为 2 条；临时附件文件也已删除，未修改既有收文数据。`verify-local.ps1` 返回 `VERIFY_LOCAL_OK`；受影响 Docker Web/API 已重建。

## 2026-07-29 系统账号重置密码、首次改密与跨角色阻断页面闭环（本地）

- 已通过管理员页面创建：在“系统用户管理 → 新增账号”创建唯一临时普通账号，页面默认角色为“普通用户”，状态为可用；未通过数据库或通用记录接口造数。
- 已通过管理员页面验证：打开该账号“重置密码”弹窗，明确显示“重置后将解除登录锁定，且该账号下次登录必须修改密码”；两次新密码不一致时表单显示“两次密码输入不一致”且未提交，填写一致后提示“密码已重置”。
- 已通过普通账号页面验证：使用重置后的临时密码登录，系统强制弹出“首次登录修改密码”，并明确提示“修改前不能进入任何业务页面”；修改成功后才进入控制台。普通账号直达 `system-users` 显示“无权访问 / 当前角色没有该功能的菜单权限”，侧栏不显示系统中心，证明前端菜单和服务端路由边界同时生效。
- 已精确清理：重新登录管理员后，从同一“系统用户管理”页面删除该唯一临时账号，确认弹窗后提示“账号已删除”，账号列表由 3 个回到 2 个；未删除或修改既有账号。
- 证据边界：本条补足账号创建、重置、首次改密、普通角色系统管理阻断及页面删除闭环；不替代员工档案、人事状态、角色菜单配置或其他业务模块的多角色验收。

## 2026-07-29 跨模块详情打开抗旁路失败与案件宽表可读性整改（本地）

- 已修复：案件、合同、客户和调查中心的跨页详情导航不再依赖附件、历史、人员目录、印章、权限等辅助请求全部成功。现在先受权限保护地读取并打开核心业务对象，再以独立可降级请求加载附加区域；合同、客户、调查在当前分页列表未命中时会按记录 ID 或稳定编号回退读取，避免第 101 条以后或跨模块跳转仅落在列表页。
- 已修复：合同新建/续办向导先以审批主体初始化；附件和历史记录改为附加面板，临时失败不会阻断草稿恢复、审批后刷新或合同用印入口。
- 已修复：案件列表案号链接改为占满本单元格的单行省略，防止长案号越入相邻列；案件详情的“案件任务/客户任务”与案件任务抽屉改为固定表格布局和明确横向滚动，避免窄详情区的任务编号、名称挤压负责人、截止日和状态；未读任务表改用与实际七列相符的 1306px 宽度，不再出现无业务内容的超宽横向滚动。
- 已通过页面验证：管理员从“我发起的任务”真实点击 `RW20260722235517819414D255` 的案号 `SHMS2600388`，进入 `case-company` 后出现完整“案件详情”“基本信息”“修改基本信息”“案件任务”区域；该验证使用既有数据且未产生写入。
- 已通过自动化验证：`verify-local.ps1` 返回 `VERIFY_LOCAL_OK`，覆盖前端生产构建、Python 编译、菜单/客户端接口审计、19 组 API 业务闭环、精确清理与 Docker Compose 配置；Docker Web/API 均已重建并健康。菜单审计新增跨页详情抗旁路失败与案件详情窄表断言。
- 仍未证明：合同、客户、调查三个中心的本轮“辅助请求故障但基础详情仍打开”页面场景尚未人为制造可安全的旁路接口故障，因此仅有代码、静态审计和全量门禁证据；后续以页面创建的临时数据完成对应模块闭环时一并补一次，不重复既有正常关联点击。

## 2026-07-29 任务关联案号打开案件详情抗旁路失败整改（本地）

- 已修复：任务列表中的关联案号原本会保存目标案件后跳转至“公司案件”，但案件中心把目标详情的消费放在案件、合同、开庭、附件、客户、费用等一组并行加载全部成功之后；其中任一无关附加请求失败时，页面只显示案件列表，目标详情被跳过。
- 已整改：案件列表与待打开详情目标改为优先处理；合同、开庭、附件、客户、费用等补充区域随后并行加载。这样任务、搜索、通知等复用同一详情上下文的跨页关联，不会因无关列表加载而丢失详情打开动作；找不到目标或无查看权限时仍明确提示。
- 已通过页面验证：管理员在“我发起的任务”中点击真实任务 `RW20260722235517819414D255` 的案号 `SHMS2600388`，页面进入 `case-company` 并自动打开该案件完整详情，实际出现“案件详情”“基本信息”“修改基本信息”等详情区域，不是仅跳转列表。
- 已通过自动化验证：完整 `powershell -ExecutionPolicy Bypass -File .\\scripts\\verify-local.ps1` 返回 `VERIFY_LOCAL_OK`；包含前端生产构建、Python 编译、菜单覆盖审计、19 组 API 业务闭环、精确清理和 Docker Compose 配置检查。Docker Web/API 已重建并健康。

## 2026-07-29 收发文附件全模块关联详情整改（本地）

- 已修复：文档中心全局附件列表的“关联编号／关联业务”原本仅支持案件、合同、客户、任务、线索和收发文自身；关联财务、用印、仓库、人事、公证或证据时会显示“暂不支持详情查看”。现财务、用印、仓库和人事均进入其受权限保护的真实详情上下文；公证、证据与线索／调查统一进入调查详情。
- 已防回归：菜单覆盖审计明确断言上述模块、详情上下文和目标路由存在，避免重新出现仅有文字按钮或不支持的假入口。
- 已验证自动化：本地 Web/API 已重新构建并健康；完整 `verify-local.ps1` 通过，含前端构建、Python 编译、菜单审计、19 组 API 业务闭环、精确清理及 Docker 配置。浏览器对本轮新 Web 会话发生连接错误，尚未形成附件列表逐模块点击页面证据；此项保留为“已部署、仍待页面证明”。

## 2026-07-29 财务动态宽表空关联链接整改（本地）

- 已修复：财务原始布局的内部请款、结算、开票、归档费和费用查询动态列此前会把空案号、空合同号或空客户渲染成链接按钮，点击后可能进入无目标详情。现统一为“关联值存在才显示真实详情链接；为空显示 `—`”，结算展开明细的案号、合同号也采用同一规则。
- 已防回归：菜单覆盖审计新增财务动态列的值守卫断言，确保案件、合同、客户三类关联字段不会回退为空链接；有值关联仍复用既有真实详情导航。
- 已验证自动化：完整 `verify-local.ps1` 通过，包含前端生产构建、Python 编译、菜单覆盖审计、19 组 API 闭环、精确清理与 Docker Compose 配置。浏览器分配明细的正向案号／合同号点击仍待在含可精确清理分配数据的场景补证。

## 2026-07-29 用印页面选择、关联回看与草稿精确清理（本地）

- 已通过页面创建：管理员在“待审批 → 申请用印”填写唯一验收标题，选择客户、案件、合同、用印类型“案件用印”和印章“申浩律师事务所财务专用章”。印章下拉真实列出财务三排章、财务专用章、发票章、法人章、公章、合同章及所函专用章，确认此前“无法选择印章类型”不再复现；案件用印切换后页面即时将关联案号标为必填。
- 已通过页面保存与关联回看：填写用途后保存草稿，列表即时出现唯一申请 `YY202607291107322AF`，回显草稿状态、案件、合同、客户、用印类型和印章类型；点击该行案号 `SH191000382B` 实际打开完整民事案件详情（含基本信息、文档、费用、提醒、日志、任务等真实区域），不是列表跳转或空壳详情。
- 已精确清理：验证完成后仅删除该唯一、未提交草稿；受限删除接口只允许删除未触碰草稿，随后以申请编号回查为 `0`。未提交审批、未产生用印操作，未修改已有申请或印章资产。
- 证据边界：本轮证明选择器、草稿落库、案件关联详情与清理；不重复既有附件上传、提交审批、审核、实际用印及归档闭环。用印前端必填和服务端无效关联 422 阻断继续由既有自动化覆盖。

## 2026-07-29 平台回款页面创建、合同关联回看与精确清理（本地）

- 已通过页面创建：管理员在“新增回款”选择客户 `test` 后，合同候选仅保留该客户的合同；选中合同再切换到另一客户时，原合同立即清空。随后从页面填写付款单位、金额、回款方式、唯一银行流水号和合同，提交后得到“回款登记并认领客户成功”，没有直接写数据库。
- 已发现并修复：平台路由实际复用 `FinanceCenterPage` 的原始回款管理表，而不是独立的回款创建组件；该表此前漏读到账记录顶层 `contract_no`，故已落库合同号没有展示。现回款管理、领取、待分配和已分配四个同源列表均展示“合同编号”，并统一提供真实合同详情链接；菜单审计锁定此字段与跳转，防止回归。
- 已通过页面回看与清理：临时回款在“回款管理”中回显客户、金额、合同号；点击合同号打开真实只读合同详情 `SHHT1810328`，并显示关联客户。随后从同一页面点击“删除”，列表变为“没有查询到符合条件的记录”，提示“到账记录及其分配已撤销”。浏览器控制台无错误；临时到账已精确清理。
- 已验证自动化：前端生产构建、Python 编译、菜单覆盖审计、Docker Web/API 重建与健康检查通过。本轮只证明回款登记/认领、合同关联显示跳转和删除清理；不重复既有回款分配/结算闭环，且不将其扩大为全部财务页面已验收。

## 2026-07-29 通用关联入口与平台回款选择联动整改（本地）

- 已完成：通用兼容业务页的客户列、客户详情字段，以及其中的案号、合同号、线索号等语义字段，统一复用真实详情上下文；客户跳转不再错误携带“当前业务记录 ID”冒充客户 ID。首页“开庭排期”中的客户也改为真实客户详情入口。
- 已完成：平台财务“新增回款”以受控表单监听客户选择；选择客户后仅展示该客户合同，若原先选择的合同与新客户不匹配会立即清空，避免前端残留错误合同。服务端既有“合同存在且客户一致”的 422 阻断继续保留。
- 已验证：本地 Docker Web/API 已重建并健康；浏览器已实际登录本地系统并进入“新增回款”，确认客户、合同两个选择器和回款表单真实加载。前端生产构建、Python 编译、菜单覆盖审计与差异检查通过。
- 已更新：上述页面闭环已在本文件顶部“平台回款页面创建、合同关联回看与精确清理”补证；本条不再列为未证明。该证据不替代既有回款分配和结算闭环。

## 2026-07-29 案件排期客户关联与归档宽表整改（本地）

- 已完成：案件开庭排期中的客户可打开真实客户详情；归档审核表横向宽度调整为 1900，匹配案件基础列、归档列及固定操作列，不再以 1100 宽度压缩长案号和归档字段。
- 已验证静态：前端生产构建、菜单覆盖审计和差异检查通过。需后续浏览器补排期客户点击、归档横向滚动及右侧操作可点击证据。

## 2026-07-29 回款分配与平台财务关联入口补齐（本地）

- 已完成：回款分配历史中的合同、案件改为真实详情入口；平台财务全部列表的客户/客户名称列及财务详情抽屉客户字段均改为真实客户详情入口，案号和合同号沿用已有真实入口。
- 已更新：平台回款客户、合同关联的页面正向点击已在本文件顶部补证；回款分配历史的案件/合同入口和权限提示仍待在产生安全临时分配数据时定向补证。

## 2026-07-29 平台回款合同真实关联整改（本地）

- 已完成：平台“回款登记”的合同编号由自由输入改为可见合同选择；创建到账时合同编号与合同记录 ID 独立落库，不再仅拼入备注。服务端校验合同存在，并在选择客户时校验合同客户一致；绕过页面提交不存在或不匹配合同会被 422 阻断。
- 已验证自动化：数据库兼容字段迁移、API/Web 容器重建后，完整 `verify-local.ps1` 通过，含 19 组 API 业务闭环和精确清理；不影响既有到账认领、分配、应收同步及权限规则。
- 已更新：已用页面选择既有可见客户/合同完成“客户过滤→登记回款→自动认领→合同详情回看→页面删除清理”；不需要为了证明该选择器另建客户或合同。合同不存在/客户不一致的服务端阻断继续由自动化覆盖。

## 2026-07-29 用印与归档材料关联/宽表整改（本地）

- 已完成：用印列表和用印详情的客户均改为真实客户详情入口；案件归档材料完整度表的案号、客户也改为真实案件/客户详情入口，空值统一显示“—”。
- 已完成：收发文登记宽表的横向滚动宽度由 1400 调整为 1700，覆盖现有列宽和 260px 固定操作列，避免右侧操作列遮挡关联字段的点击区域。
- 已验证静态：前端生产构建、菜单覆盖审计、差异检查通过。仍待浏览器可访问本地页面后补链接点击、横向滚动和固定操作列可点击性证据。

## 2026-07-29 审计日志业务编号死关联整改（本地）

- 已完成：审计日志中的案件、合同、客户、任务、线索、公证、证据、财务、用印、收发文、证物和人事业务编号均进入对应真实详情。报表等没有可查看业务详情的记录不再伪装为可点击链接。
- 已验证静态：前端生产构建、菜单覆盖审计及差异检查通过；该改动复用全局搜索和通知中心已经使用的受时效保护详情上下文，不新增通用越权读取接口。
- 已通过页面验证：管理员在“操作日志”点击客户业务编号 `SHKH1810649`，实际打开“公司客户”的只读详情，回显客户名称 `test`、客户编码及资料分区；不是泛化业务页、空白页或客户列表。此处使用既有可见客户，只读回看，不创建或修改数据。
- 证据边界：本轮已证明客户模块的审计回看。其余业务模块的统一跳转仍有静态覆盖，后续以有代表性的真实记录定向补点击证据，不能把本条扩大为全部审计模块均已页面验收。

## 2026-07-29 财务关联字段统一补齐（本地）

- 已完成：费用、开票、退款兼容列表以及归档费结算展开明细中的客户、案号均不再为纯展示文本；有可见关联对象时分别进入真实客户或案件详情，空关联统一显示“—”。既有合同号链接保持不变。
- 已验证自动化：重建 Web 容器后完整运行 `verify-local.ps1`，前端构建、Python 编译、菜单覆盖审计、19 组 API 闭环、Docker 配置和精确清理均通过；Web/API 容器均健康。
- 已更新：平台回款合同与客户入口已有浏览器正向点击证据。回款分配历史、开票详情合同列等其余财务入口仍需各自使用可精确清理的页面数据补证，不能把本条视为财务模块全部完成。

## 2026-07-29 财务结算新建案件任务关联整改（本地）

- 已完成：归档费结算行与结算分配明细中的“新建任务”不再只是跳转到任务列表。点击后会携带关联案号、客户和明确的案件费用跟进标题，打开真实新增任务弹窗，预填“案件任务”来源与七日截止日期；未关联案件时明确阻断，不会产生悬空任务。
- 已验证静态：前端生产构建、Python 编译、菜单覆盖审计及差异检查通过。审计已锁定两个财务入口、临时上下文和任务页消费逻辑，防止回退为无上下文跳转。
- 仍未证明：内置浏览器仍无法访问本地页面，故未使用页面创建临时任务；后续只需补一次“财务结算明细 → 新建案件任务 → 预填检查 → 提交 → 任务/案件关联回看 → 精确清理”的页面证据，不重复已验收任务状态规则。

## 2026-07-29 角色叶子菜单权限实际生效（本地）

- 已完成：菜单权限从“拥有一级菜单即显示全部子页”改为具体菜单项授权。服务端导航、前端侧栏和深层路由均按有效叶子权限处理；仅作为已授权子项容器的父级菜单可见，但不能借父级直接访问未授权页面。
- 兼容：历史角色的一级菜单授权已一次性展开为可单独撤销的叶子权限；保留用户中心基础权限。管理员继续固定拥有全部菜单和全所数据，不能被角色配置降权。
- 已验证自动化：回归先发现旧一级授权会重新继承被撤销的“客户利益检索”，已通过一次性迁移修复。完整 `verify-local.ps1` 通过：普通用户撤销该叶子后导航和接口均不可访问；仅保留用户中心时导航仅返回控制台、用户中心及其三项子页；历史“案件”一级授权仍兼容显示其既有子项。19 组 API、构建、审计与精确清理均通过。

## 2026-07-29 刑事案件司法机关资料维护补齐（本地）

- 已完成：依据旧系统刑事案件创建后的公安机关、检察院及法院维护入口，补齐新系统“维护刑事资料”中的缺失字段。公安机关可维护名称、承办人、地址、联系电话和备注；一审、二审及再审检察院可分别维护名称、承办人、地址、联系电话和备注；一审、二审及执行法院可分别维护是否启用、法院名称、案号、承办人、地址、联系电话、立案日期、开庭日期和备注。
- 已完成：维护接口仍仅允许案件负责人或经办律师在刑事案件完成创建后使用，归档案件继续阻断。日期按真实日期值传递，布尔“是否启用”按布尔值落库，不再把 `True`／`False` 错存为字符串；三类维护均写入案件操作日志。
- 已验证自动化：新增刑事案件创建、审批后维护公安机关／检察院／法院资料的 API 断言，并要求操作历史包含三类维护记录。首次回归捕获法院启用状态被字符串化的问题后已修正；完整运行 `verify-local.ps1` 后前端生产构建、Python 编译、菜单覆盖审计、19 组 API 业务闭环、Docker Compose 配置和精确清理全部通过（`SMOKE_CLEANUP_OK`）。
- 仍未证明：内置浏览器本轮对 `127.0.0.1` 的访问仍受平台 URL 策略拦截，因此尚未形成“维护刑事资料”弹窗逐字段点击、保存回显和归档阻断的页面证据；未以接口测试替代页面证据，也不重复既已验收的案件→线索→调查闭环。

## 2026-07-29 用印关联对象强校验与选择器整改（本地）

- 已完成：用印申请的客户、案号和合同号不再允许保存任意文本。页面改为仅展示当前账号可见的客户、案件和合同可搜索选择项；案件用印必须选择案件、合同用印必须选择合同。
- 已完成：服务端创建和草稿修改均独立校验关联对象存在且在当前数据范围内；绕过页面提交不存在或无权对象会得到明确 422，不会产生悬空关联。若选择案件或合同而未单独选择客户，客户名称会从关联业务自动规范回填。
- 已验证自动化：电脑重启后完整运行 `verify-local.ps1`。首轮发现用印冒烟数据错误地传入了与关联案件不一致的客户名，现已改为使用关联案件的真实客户值；复跑后前端生产构建、Python 编译、菜单覆盖审计、19 组 API 业务闭环、Docker Compose 配置及精确清理均通过（`SMOKE_CLEANUP_OK`）。菜单审计已改为对用印页使用格式无关的结构归一化，避免格式化造成假失败。
- 已更新：页面新建临时用印草稿、案件／合同选项、印章选择、草稿回显、案件详情回看和精确清理的证据已补在本文件顶部。前端必填及后端无效关联 422 仍由自动化覆盖；附件与审批流程不在本条重复执行。

## 2026-07-29 未验收缺口整改（本地，待页面定向回归）

- 已修复收发文全局附件列表的关联字段：有稳定 `record_id` 的“关联编号”和“关联业务”均改为真实详情入口；打开前回读受权限保护的业务记录，再按案件、合同、客户、任务、线索/调查或收发文自身进入对应只读详情。关联对象不存在或无权查看会显示真实原因，公共文件继续明确为“公共文件”。
- 已修复“我的未读任务”沿用普通任务 14 列表头的问题：现仅显示任务编号、关联案号、未读内容、发送人、发送时间、任务状态和负责人七个真实字段，不再将未读消息伪装进“状态／优先级／标题”或保留六列空白单元格；长编号仍保留任务与案件详情入口。
- 已同步调整菜单审计：断言未读页必须使用真实未读字段并禁止空白占位列，防止后续回归为截图中的错列架子。前端生产构建、Python 编译、菜单覆盖审计和差异检查均通过。
- 历史状态更新：用印申请关联对象已改为受范围保护的选择器；平台回款合同也已完成真实选择、落库、列表展示和合同详情回看。仍待定向审查的是回款分配弹窗中的案号、合同号以及其它未列举财务抽屉/明细入口，不可把历史问题清单误读为当前缺陷。
- 验收纪律更新：案件→线索→调查、HR 状态、客户公海/联系人、案件合同选择和案件附件重命名已有页面证据，除受影响回归外一律不再重复。

## 2026-07-28 案件合同选择、文件重命名与联系人宽表整改（本地）

- 已修复案件新建合同下拉为空：案件页同时请求客户、线索参考数据时使用 `page_size=200`，超过通用记录接口上限 `100` 后使 `Promise.all` 失败，连带丢失已成功返回的可用合同。现统一为合法页大小；管理员页面重新显示两份可建案件合同。
- 已修复合同候选显示：合同下拉改为显式渲染合同号、客户和合同名称，避免下拉实际显示内部记录 ID。管理员从页面选择 `HT20260722204437` 后，客户、案源人和案件名称均自动真实回填。
- 已通过页面验证：创建临时民事案件 `SHMS2600389`，完成新建步骤并在案件列表选中后执行“立案审批通过”；案件详情从“尚未通过审批”变为附件、提醒、日志“可办理”。随后页面打开重命名弹窗，将临时附件名称改为 `SMOKE-UI-case-rename-verified.txt`，列表即时回显并提示成功；再从页面勾选并删除该附件，提示“已删除 1 个文件”。临时案件按唯一 ID 使用本地受限清理接口删除，回查为 `404`。
- 上传证据边界：本轮内置浏览器不具备文件选择器写入能力，故案件与审批均由页面创建，临时附件以真实上传接口补入后才继续页面重命名和删除。不得将“页面上传”标为已证明；后续在浏览器支持文件选择后，只需补一次上传选择器、下载和归档写入阻断，不重复本条已验证的创建、审批、重命名和删除。
- 已整改客户联系人宽表：只读详情、创建页和抽屉联系人表统一使用固定表格布局、明确横向宽度与单行省略策略，并为操作列预留宽度，避免长姓名、邮箱或电话挤压编辑/删除入口；仍待后续在 640px 页面实际抽检一次，不重复客户联系人业务闭环。

## 2026-07-28 客户公海联系人权限页面证据（本地）

- 已通过页面建立并验证：管理员创建带固定验收标记的临时客户和联系人，按“我的客户 → 客户删除 → 个人回收站 → 进入公海”完成真实状态流转；普通用户登录后可在“公海客户”查看该客户和联系人，但联系人表的“操作”列没有编辑入口，页面仅保留查看与关闭动作，符合公海客户只读边界。
- 已精确清理：临时客户 `UI临时联系人验收-20260728-E/F` 及联系人、两条本轮公海权限测试账号均按唯一 ID 删除；随后只读回查临时客户和账号均为 `0`，未触及既有业务数据或账号。
- 后续验收纪律：客户联系人编辑、普通用户私有数据隔离和本条公海只读边界均已有页面证据，后续不重复执行；仅在实现发生改变或出现同类回归时重测。

## 2026-07-28 人事状态办理页面证据（本地）

- 已通过页面创建并验证：管理员在“新建员工”创建临时员工与低权限登录账号；在“员工管理”对应行点击“办理状态”，填写离职生效日期、原因和说明后提交。页面提示“人事状态及关联登录账号已同步更新”，该员工列表“是否可用”由“是”变为“否”。
- 已验证阻断：以该已离职临时账号登录，页面停留在登录页并返回“账号或密码错误”，证明关联登录账号已被同步停用；管理员会话已恢复。
- 已精确清理：临时员工档案与系统账号均按唯一 ID 删除，并只读回查为 `0`。本条仅补人事状态办理与账号联动证据，不重跑先前已通过的人事 API 状态机、任务或调查流程。

## 2026-07-28 客户中心扩展功能复核（本地）

- 已核对：以 PPT 第 4–13 页、`原OA系统业务逻辑总览_20260717.md` 的 CRM 控制器证据，以及现有客户专用接口逐项复核客户资料、联系人、事项记录、客户文档、管理人分配、共享、回收／恢复、公海领取与客户服务端。客户创建、资料区字段、联系人新增／编辑／删除、事项新增／删除、分类文档上传／下载／删除、管理人分配、共享与公海生命周期均已有专用真实接口、数据范围校验和工作流审计；共享收件人仅可只读，不能借详情页写入。
- 已核对：PPT 的“客户服务端开通”有明确业务依据，因此保留现有开通／停用实现；开通仅允许管理员或客户主管，并要求签约等级或已审批通过合同，激活码只在响应中一次显示且仅保存哈希，客户服务端可真实读取本客户合同／案件／文件并将需求落为客户负责人待接收任务。该项不是无证据伪造的外部门户。
- 审计结论：本轮未发现能够由旧源码或 PPT 证明、但当前缺少真实实现的客户扩展按钮；没有为“事项编辑”、单个客户文件详情、外部客户门户额外能力等未获证据的功能虚构接口或弹窗，也不重复执行已验收的客户跨角色链路。
- 已通过本地浏览器页面验收：管理员从“新建客户”创建带固定验收标记的临时客户，页面新增联系人后进入“编辑联系人”，将职务由“法务助理”更新为“高级法务助理”；弹窗正常关闭、联系人表立即回显新值，Web 日志同时确认新增联系人 `201` 与专用更新 `PUT /customers/{id}/contacts/{contact_id}` 返回 `200`。此前临时记录已被清理导致过一次 `404`，该现象确认是已删除数据的陈旧页面状态，并非联系人更新功能缺陷。
- 已通过本地浏览器审计回看：另建带固定验收标记的临时客户与联系人后，更新职务为“审计验证法务”；“操作日志”页顶部真实出现“修改联系人”记录，包含客户业务编号、操作人 `admin` 与“修改字段：职务”，同时保留相邻的“创建客户／新增联系人”事件，证明不是页面伪造提示。
- 已精确清理：两轮验收均仅删除固定验收标记的临时客户及其联系人，按各自唯一记录 ID 回查为 `404`，无残留。
- 已通过本地浏览器跨角色验收：管理员从“系统用户管理”页面创建临时普通用户，并从页面创建另一条仅归管理员管理的临时客户及联系人。以该普通用户登录后，“我的客户”页客户管理人被锁定为该用户且显示 `0` 条，管理员客户完全不可见，因此不存在联系人“编辑”入口；这证明数据范围先于前端操作入口生效，不依赖隐藏按钮。
- 已精确清理：临时普通用户与管理员客户均按唯一 ID 删除并回查为 `0`，未改动既有账号或业务记录。
- 已更新（2026-07-28）：公海领取及客户数据范围/联系人操作入口均已补页面证据并精确清理。后续不再重复该闭环；只有“公海客户存在关联业务时的额外阻断规则”仍未单独证明。

## 2026-07-28 仓库库位导航缺口审计（本地）

- 已核对：旧源码 `SH.CRM.WEB/Areas/WMS/Views/Warehouse/GoodsList.cshtml` 与 `Scripts/WMS/WareHouse/WMS.WareHouse.js` 有真实“仓库 → 库位 → 货物数量”树；选择仓库或库位会将该条件写入查询后刷新货物列表。旧页面没有证物生命周期办理按钮，因此现有专用登记、入库、出库、重新入库、销毁流程不在本轮重复验收范围。
- 已完成：新“仓库一览表”现按已加载的真实证物记录动态汇总仓库、库位及数量。选择仓库节点会筛选到该仓库；选择库位节点会同时筛选仓库和库位；“全部”可清除两项筛选。汇总不写库、不改状态、不伪造库存数据，登记表单的仓库选项也会纳入已有真实仓库名称。
- 已验证：前端生产构建、Python 编译、菜单覆盖审计（新增 `WAREHOUSE_STORAGE_TREE_OK`）、客户端 API 覆盖和差异检查通过。
- 仍未证明：本轮遵循“已验收的不重复验收”，未新建证物、未重复执行证物生命周期，也未在浏览器逐节点点击留证；后续仅在产生新的可精确清理证物数据时补一次“树节点筛选 → 列表结果”的页面证据。

## 2026-07-28 客户旧源码有效操作只读审计

- 已核对：旧 CRM 客户详情包含联系人、事项记录、客户文件等页签；现系统的客户详情已有联系人新增／删除、事项新增／删除、客户文档上传／下载／删除，以及客户分配、共享、回收、公海、合同和案件关联入口。本轮未重测这些既有客户链路。
- 已完成：旧 `Scripts/CRM/Customer/CRM.Customer.js` 的 `CustomerContacts.Open(contactsId)`／`ContactsCreateUpdate` 已补为专用 `PUT /customers/{customer_id}/contacts/{contact_id}`。接口先按客户数据范围解析客户，再按负责人／客户管理人／同部门负责人／管理员写入权限校验；直接更新原联系人 ID，不允许通过删后重建替代。姓名、职务、项目角色、移动／办公电话、IM、邮箱、联系状态、有效状态、主要联系人和备注均可维护；重复姓名＋电话会被阻断，设主要联系人会同步取消其余主要标识，并写“修改联系人”审计事件及变更字段。
- 已完成：客户详情页、客户编辑关联页和抽屉详情的联系人表均按前端权限预判显示“编辑”；编辑弹窗保存后调用专用更新接口并刷新真实详情，接口拒绝时直接展示原因。服务端仍为最终权限边界，未放宽其它客户或联系人操作。
- 已验证：前端生产构建与 Python 编译通过；管理员页面已完成“创建临时客户 → 新增联系人 → 原位编辑职务 → 表格回显 → 操作日志回看”的真实链路，新增和更新分别得到服务端 `201`／`200`，临时数据已精确清理。
- 已验证：跨角色页面数据范围已补证。临时普通用户无法看到管理员所有且未共享的客户，故无法触达联系人编辑入口；临时账号和客户已精确清理。
- 仍未证明：公海阻断尚待使用新的临时数据单独补证；不得把本次管理员正常路径扩大表述为全角色、全状态完成。

## 2026-07-28 案件旧源码剩余功能只读审计

- 已核对：排除已完成的民事／刑事／行政／仲裁基本信息编辑和刑事资料维护后，旧站 `Legal.Case.js` 的开庭律师编辑、执行进度、案件提醒／日志、案件文件上传下载和案件费用均已在现有专用案件流程中有对应入口或接口。
- 已完成：旧站案件文档列表的文件名编辑已补为专用 `PUT /cases/attachments/{attachment_id}/rename`。接口只接受归属 `case` 的附件，复用案件详情写入门禁（案件团队角色、创建审批完成、待归档审核／已归档阻断），拒绝路径、空名和扩展名变更；只更新展示／下载名称，不移动存储文件或改变文件内容，并写入“重命名案件文件”审计事件。其它模块附件接口未放宽。
- 已完成：案件详情“文档信息”每行在具备案件详情写入权限时显示“重命名”入口；弹窗与后端均校验文件名、路径字符和扩展名，成功后重载真实附件列表，失败直接展示接口原因。
- 已更新（2026-07-28）：已使用页面创建的临时案件完成附件详情打开、真实文件改名、列表回显和删除的浏览器证据，并按精确案件 ID 清理。文件选择器受浏览器能力限制，页面上传、下载及归档阻断仍单列为未证明项；不得因本条重复附件改名验收。

## 2026-07-28 本轮本地验收与测试服务器 Git 同步

- 已验证：主线已重新执行 `powershell -ExecutionPolicy Bypass -File .\\scripts\\verify-local.ps1` 并得到 `VERIFY_LOCAL_OK`。覆盖前端生产构建、Python 编译、264 菜单节点/219 叶子页面结构审计、392 个前端 API 路径覆盖、19 组 API 业务闭环、`SMOKE_CLEANUP_OK` 与 Docker Compose 配置检查。
- 已同步：截至提交 `3e4b4dc` 的本地 `dev` 已推送至测试服务器裸仓库 `test-server/dev`；本轮仅同步 Git 源码，尚未切换测试服务器运行版本或声明上线。
- 仍未证明：完整页面验收不是 API 冒烟的替代。尤其需用页面创建临时数据完成普通案件专用编辑、案件→线索→调查任务、员工状态办理、多角色任务/调查接收与完成，以及搜索/通知中尚无详情上下文模块的后续补齐；完成后精确清理临时数据。

## 2026-07-28 人事列表宽表可读性审计（本地）

- 已完成：员工列表各列固定宽度合计为 1515px，原 `scroll.x=1450` 小于列宽总和，且新增“办理状态”后右侧操作列会随横向滚动离开可视区。现将滚动基准明确为 1590px，并固定右侧 190px 操作列；“查看／修改／办理状态”保持单行按钮和独立点击命中区域，横向滚动时仍可操作。
- 已完成：固定列的表头和单元格补齐不透明背景，避免宽表滚动时左侧内容透入右侧操作按钮；1366px 及更宽屏仍保持页面内容区的水平滚动而不压缩字段列。
- 已验证：前端生产构建和菜单静态审计通过。仍未证明：本地浏览器控制通道当前不可用，尚无实际 1366px 截图／点击证据；后续应以管理员在员工管理页确认固定操作列及“办理状态”弹窗可打开、关闭和提交校验。
- 已补浏览器证据：在 1280px 宽本地页面，员工表格右侧固定操作区保持在可视范围，“查看／修改／办理状态”均具有独立可点击区域；本轮人事状态页面闭环亦已实际打开并提交“办理状态”弹窗。因此该项不再作为未证明的宽表操作列问题重复验收。


## 2026-07-28 人事状态入口收口与任务／调查派发审计（本地）

- 已完成：员工列表新增“办理状态”专用入口，按既有人事状态机办理“试用→在职／离职”与“在职→离职／停用”；页面要求填写生效日期、办理原因和可选交接人。关联普通登录账号仅在该专用流转中同步启停，资料编辑页不再能绕过人事状态直接启停账号。
- 已完成：后端拒绝员工资料编辑请求中与人事状态不一致的 `is_active`，避免遗漏离职／停用原因、有效日期和工作流事件；`admin` 与遗留高权限账号仍不受人事状态流转修改。调查授权、调查任务派发和普通任务派发均继续通过有效账号解析，停用账号会被专用 API 明确拒绝。
- 已验证：前端生产构建、Python 编译、Docker API/Web 重建与健康检查、完整 API 冒烟（19 组）及菜单覆盖审计均通过；冒烟验证了编辑页直接停用被 409 阻断、试用转正恢复账号、离职后账号不可登录，且 `SMOKE_CLEANUP_OK` 无临时数据残留。
- 已更新（2026-07-28）：管理员“办理状态”弹窗已实际提交离职并验证关联登录账号不可登录，临时员工和账号已精确清理；不再重复该人事状态链路。调查与任务派发的跨角色细分分支仍按各自未证明项推进。


## 2026-07-28 并行修复集成验证（本地）

- 已完成：任务反馈附件改为参与人专用授权和原子提交。发起人、负责人、协作者及 `admin` 可以在任务详情真实提交一条文字及多个附件；附件列表、下载和本人删除不再错误继承通用记录 owner 范围。前端不再在附件接口失败时静默显示“暂无数据”，而是保留独立失败提示。特殊处理审批已限制为 `admin`、发起人或任务所属部门负责人，跨部门经理不能通过猜测任务 ID 审批。
- 已验证：API 冒烟覆盖纯协作者跨范围反馈附件的提交/查看/下载/删除、非法多附件原子回滚，以及特殊处理审批的跨部门拒绝和同部门放行；浏览器多角色页面回归仍待完成，不能据此声称页面闭环已证明。

- 本轮已将案件详情写入门禁、调查子任务页面办理、任务发起人/管理员撤回和任务批量生命周期合并到同一工作树；未向测试服务器部署。
- 已由主线重新执行 `powershell -ExecutionPolicy Bypass -File .\\scripts\\verify-local.ps1`：前端生产构建、Python 编译、菜单覆盖、388 个前端 API 路径覆盖、18 组 API 业务流、临时数据精确清理及 Docker Compose 校验全部通过（`VERIFY_LOCAL_OK`）。
- 已在本地浏览器刷新“我发布的调查子任务”页面，确认页面不依赖创建弹窗状态：重载后可列出真实子任务，并呈现“状态 / 办理 / 接收任务”入口。仍需以页面新建的专属临时数据完成接收、提交完成和清理，才能把该完整页面链路标为已证明。
- 后续顺序：先继续按角色完成页面创建与跨角色联动证据，再将已确认的 `dev` 提交按 Git 同步到测试服务器；不得跳过本地页面证据直接宣称上线。

## 2026-07-28 案件详情写入权限收敛（本地，待浏览器多角色回归）

- 已完成：案件详情的附件上传/删除、提醒新增/删除、日志新增与智能文档回写案件附件已复用一条后端写入门禁。它同时校验案件新建三步已完成且审批通过、负责人/同部门负责人/`admin` 办理权限、以及归档中和已归档的不可写状态。
- 已完成：详情请求新增 action capability；前端不再用本地状态或角色硬编码控制写入按钮，而是只展示后端判定可办的上传、删除、提醒和日志入口。查看/下载仍按既有可见范围保留。
- 已验证：新增 API 冒烟覆盖可写案件 capability、归档审核中 capability 以及归档中日志/附件写入阻断；完整 `verify-local.ps1` 仍待本轮代码构建后执行。
- 仍未证明：没有浏览器多角色证据前，不得把“同部门负责人可办、旁观者不可办、管理员可办”的可见性和实际提交宣称为完成；后续只可用页面创建临时案件数据，并精确清理。

## 2026-07-28 案件详情文档入口补齐（本地，待浏览器回归）

- 已完成：`CaseCenterPage.tsx` 的案件详情“文档信息”接入既有附件 API，提供分类选择、真实上传、单文件查看/下载、批量 ZIP 下载和受归档状态限制的删除；PDF/图片预览使用带当前认证的附件响应，不依赖公开文件 URL。
- 已验证：前端生产构建通过。
- 仍未证明：需在本地浏览器选取一个可编辑案件，以页面上传临时 PDF/图片和非预览文件，核对列表回显、查看、下载、归档阻断和删除后精确清理；旧站“转文/解锁”尚未确认业务语义，未实现伪入口。

## 2026-07-28 宽表与窄屏共性布局整改（本地）

- 已完成：调查线索列表的横向滚动宽度由 2320px 调整为 2920px，与全部业务列和固定右侧操作列匹配，避免固定操作区覆盖案号、来源任务、权利人等可点击关联字段。任务主表同步以 2040px 作为滚动/最小表格宽度，任务编号、案号链接会在列内省略而不撑破相邻列；客户主表滚动宽度调整为 2100px。
- 已完成：客户只读详情、创建页和编辑抽屉内的联系人、事项记录、客户文档表均补齐独立横向滚动；四列/五列信息区在 1100px 以下降为两列、640px 以下降为单列，避免侧栏场景压缩或遮挡操作列。
- 已验证：Docker Web 镜像已重新构建，Compose 全部服务健康；`verify-local.ps1` 完整通过（前端生产构建、Python 编译、菜单/客户端 API 覆盖、18 组 API 流程、精确清理与 Compose 配置）。
- 仍未证明：本次尝试浏览器视觉抽检时本地浏览器 WebView 未能附着，故没有把截图级的宽屏/窄屏可点击性标为已证明；后续需在可附着的本地浏览器分别抽检调查线索、任务主表和客户详情。

## 2026-07-28 调查子任务列表回显与路由状态修复（本地）

## 2026-07-28 客户审核确认通过页面终态（本地）

- 已验证：管理员仅通过页面创建 `SMOKE-UI` 父调查 `DC20260728053544`、首个任务 `RW20260728133558372124` 与线索 `XS20260728053612`，完成“提交审批 → 内部审批通过 → 客户审核确认通过”。客户审核确认弹窗真实提交后，线索在“待取证线索”列表回显，证明正向状态实际落库并同步到后续页面。
- 已完成清理：待取证状态按业务规则不支持普通页面删除；在页面证据完成后，仅在本地非生产环境对上述三个明确测试编号使用受管理员限制的测试清理接口，按“线索 → 任务 → 父调查”顺序删除并复查为 0。未修改既有账号、客户或业务记录。
- 仍未证明：当前客户审核仍是内部客户管理人代录，不等同于外部客户门户自行确认、签署或消息回执。

- 根因与修复：调查页面在同一组件内切换路由时保留了旧页面的查询条件和选中行；父调查页的“授权日期”等隐藏筛选继续作用于子任务，导致 API 与任务抽屉有记录而子任务列表被前端过滤。现已在路由切换时清空查询/选择状态，并以路由作为查询表单 key 重新挂载；管理员同时不再被子任务个人负责人过滤错误排除。
- 已验证：本地 Web 重建后，管理员打开“我的调查任务”页面真实回显 36 条子任务，任务编号、权利人和调查员列可见；菜单静态审计新增 `INVESTIGATION_ROUTE_STATE_RESET_OK`，防止隐藏筛选再次跨路由残留。
- 已清理：此前为重提链路创建的临时父调查、任务和审计账号均按精确范围清理并复核为 0；“驳回后修改重提”本身仍需在后续页面链路中完成。

## 2026-07-28 任务公司范围重派与挂起恢复权限对齐（本地）

- 已验证并清理：管理员在页面创建临时任务 `RW20260728134122700630` 后，普通负责人真实执行“拒绝任务”；管理员通过“我发起的任务 → 进行中-拒绝”打开“重新派发任务”并提交，负责人再次从“我接受的任务”点击“接受任务”，任务回到“处理中”。随后负责人申请挂起，管理员页面通过特殊处理，负责人在“停止”页签点击“恢复挂起任务”，最终再次回到“处理中”。
- 已完成清理：临时账号 `taskflow_ui_0728` 已由“员工管理”页面删除，账号数回到 2。终态任务没有普通页面删除入口，故在浏览器证据完成后仅于本地非生产环境按精确任务编号使用管理员受限测试清理接口；接口新增的匹配仅限标题前缀 `UI任务流转验收-`，未允许泛化“验收”记录，复查任务记录为 0。
- 已修复待页面证明：管理员在“公司发起的任务”中对被拒绝任务可见“重新派发”；“恢复挂起任务”仅在负责人实际可办理的接受视图或管理员公司发起视图显示，避免前端显示无权限死入口。
- 已通过：前端生产构建、Python 编译与菜单静态审计。尚未使用页面创建临时任务完成该两项按钮的跨角色状态闭环，因此不标记完成。

## 2026-07-28 跨角色线索驳回与管理员范围修复（本地）

- 已验证：管理员页面创建并提交临时线索，临时审计人员实际驳回并填写原因；公司驳回列表及服务端状态均回显驳回结果、审核人和原因。
- 已修复：管理员“我的已驳回线索”错误继承普通用户负责人过滤，导致全所管理员看不到应可见数据。现管理员保留全所调查数据范围，浏览器复验已回显同一驳回线索；静态菜单审计已覆盖。
- 已清理：临时账号由系统账号管理页面删除；临时线索、任务和父调查按精确 ID 清理，残留复核为 0。驳回后改后重提与客户审核尚待验证。

## 2026-07-28 跨角色客户公海领取（本地）

- 已验证：管理员页面创建临时客户、移入回收站并释放至公海；临时普通用户在“公海客户”看到同一客户后执行“拾回”，成功进入其“我的客户”，且负责人/管理人均更新为该用户。领取后公海页不再显示该客户。
- 已清理：临时账号由系统账号管理页面删除；临时客户按精确 ID 清理，客户及账号复核均为 0。领取冲突和关联业务阻断尚待验证。

## 2026-07-28 跨角色客户共享、回收与恢复（本地）

- 已修复：客户共享服务端与弹窗已存在，但“我的客户”的页面操作菜单漏了“共享客户”。现已补入口并加入菜单审计。
- 已验证：管理员页面创建临时客户并共享给临时普通用户；接收人“我的共享客户”真实回显该客户。管理员回收后接收人的共享列表不再显示该客户，随后管理员从个人回收站执行恢复并收到成功提示。
- 已清理：临时账号通过系统账号管理页面删除；临时客户按精确 ID 清理，客户和账号均复核为 0。客户附件、联系人及关联业务的共享字段权限尚未证明。

## 2026-07-28 跨角色调查线索审批与子任务父级关联（本地）

- 已验证：管理员从页面创建临时调查、首个调查任务和线索并提交；临时审批人员仅在“调查线索审核”看到待审批线索，审批通过后管理员“待取证线索”真实回显该记录。审批人员直达系统账号管理页面被明确拒绝。相关线索、任务、调查和临时账号均已精确清理。
- 已修复并验证：原“待我分配的调查任务 → 新增子任务”在尚无任务时会生成父任务为空的记录，容易形成脱离层级的假子任务。现首次进入会明确创建首个调查任务；已有任务后再次点击“新增子任务”会自动带入首个任务为父任务，并将父任务设为必填。浏览器已确认父任务编号与名称实际预填。
- 仍未证明：本轮验证审批通过与任务父子关联入口；审批驳回、客户审核、取证/公证/转案的全部跨角色分支仍应继续逐项页面验收。

## 2026-07-28 部门负责人客户写操作

- 已完成：同部门普通用户页面创建临时客户后，部门负责人从“部门客户”列表真实执行“分配客户”，回显新管理人为部门负责人。
- 已验证：部门负责人直达“公司客户”被明确阻断，未泄露全所客户。
- 已清理：客户按精确 ID 清理，两个临时账号均由系统账号管理页面删除；客户审批、共享和回收等后续操作尚未证明。

## 2026-07-28 普通用户客户闭环与权限提示整改

- 已完成页面闭环：临时普通用户创建 `SMOKE-UI 跨角色客户`，在“我的客户”回显；管理员在“公司客户”看到同一编号并进入只读详情。
- 已整改：普通用户直达“公司客户”的 403 以前被前端泛化成“客户数据加载失败”，现展示服务端真实提示“只有系统管理员可以查看公司客户”。本地 Web 重建并以临时账号复测成功。
- 已清理：临时客户按精确 ID 清理，两个临时普通用户均由系统账号管理页面删除。共享、部门分配及客户附件等角色写操作仍未证明。

## 2026-07-28 跨角色任务规则页面证据

- 新增并已清理一条页面创建的 `SMOKE-UI` 临时任务：管理员接受后转交给临时普通用户；接收人只在本人“待处理”列表看到任务，点击“接受任务”后系统实际执行“重新开始”，任务在“进行中”回显为“处理中”。
- 交接弹窗和提交结果均已在浏览器中验证，分别明确提示 5 日自动完成规则和“任务已交接，5 日内未重新开始将自动完成”。
- 已补验证：页面创建第二条临时任务并从页面交接后，仅推进该记录的本地测试计时，任务列表真实触发“已完成”；详情新增系统事件“交接任务自动完成”，回显“待接收 → 已完成”和“交接满 5 天且未重新开始，系统自动完成”。临时任务和接收账号已精确清理。
- 已补验证：新增任务日期控件中，超过 30 天的 2026-08-28 点击不生效且日期面板保持打开；允许日期 2026-08-04 点击后正常关闭并选中。后端拒绝规则已在 API 冒烟中覆盖。至此三条任务硬规则均已有页面或端到端证据，但这不代替任务模块其他状态组合和逐角色权限验收。

## 2026-07-28 会话持久化检查点：AI 智能文档关联公证（已完成并清理）

- 已完成：本地页面链路父调查 `DC20260727093220`、子任务 `RW20260727173307763488`、线索 `XS20260727093342` 已通过专用页面完成取证登记与建立公证，生成公证 `GZ20260728100228437335`；随后在“AI 智能文档”页面选择模板并关联该公证，创建临时文档任务 `AI20260728100352C51F`。
- 已验证：智能文档关联字段真实回显公证编号与事项；点击后进入“公证管理”并打开对应的只读“调查详情”，回显状态、事项、权利人、调查员和来源说明。Dify 未配置时状态真实显示为“待配置”，没有伪造生成或回写结果。
- 已完成清理：智能文档由页面“删除 → 确定”删除；本地非生产环境仅对该条明确 `SMOKE-UI` 链路的公证、线索、子任务、父调查使用受管理员限制的测试清理接口。数据库复核四条业务记录、智能文档及其附件均为 0，未触及既有数据。

### 后续角色页面验收最小集（待执行）

| 角色 | 页面创建的临时账号 | 页面验收重点 | 必须证明的阻断 |
| --- | --- | --- | --- |
| `manager`（部门负责人） | `smoke_ui_manager`（已清理） | 已验证任务中心的部门接受任务仅显示同部门负责人/成员任务；其余部门审批、批量处理待测 | 已验证异部门任务不出现在部门任务列表，且直达全所任务路由被服务端阻断；修改其他部门数据仍待测 |
| `auditor`（审批人员） | `smoke_ui_auditor`（已清理） | 已验证授权合同审批菜单与待审批空数据范围；同意/拒绝及审批痕迹待岗位权限链路复验 | 已验证直达系统账号管理被拒绝；非授权合同未泄露 |
| `user`（普通用户） | `smoke_ui_investigator`、`smoke_ui_customer_user`（均已清理） | 已验证本人调查任务与公司客户空数据范围；本人/共享客户、字段级权限待测 | 已验证系统账号管理被拒绝、未授权公司客户未泄露；全所/部门数据、越权审批及接口旁路待测 |
| `admin`（系统管理员） | 既有管理员会话 | 全部菜单、全所数据、全部字段和账号管理 | 管理员不能被停用、降权或删除；角色配置不能移除其全所/全部字段权限 |

- 执行规则：账号、业务样本和删除动作均须先从页面完成；每个角色完成后仅清理带上述精确 `smoke_ui_`/`SMOKE-UI` 标记的测试账号和链路，并在交接记录中分别标注“已验证”与“仍未证明”。

### 2026-07-28 跨账号工作区标签隔离（已完成并清理）

- 已修复并验证：管理员打开系统账号管理、公司客户等多页签后退出，再以临时普通用户登录，普通用户工作区只显示“控制台”，不会继承管理员的页签、深层路由或展开菜单。
- 已完成清理：临时普通用户 `smoke_ui_workspace_user` 已通过系统用户管理页面删除。完整本地验收和 Web 容器重建均已通过；该项静态回归门禁已纳入菜单覆盖审计。
- 已交接：修复提交已推送到测试服务器裸仓库的 `dev` 分支；服务器运行版本尚未切换，待后续测试发布窗口按版本目录流程部署。

### 2026-07-28 已完成的普通用户调查任务抽样

- 已验证：临时普通用户 `smoke_ui_investigator` 通过页面接收管理员分派的调查任务、办理并提交完成；管理员完成验收。普通用户直达系统用户管理页被拒绝，侧栏无系统管理入口。
- 已完成清理：临时调查、任务和账号已精确清理；数据库复核业务记录、工作流事件、附件、通知和账号均为 0。
- 仍未证明：该抽样不等同于普通用户全部菜单、客户/案件/财务范围、转交、挂起、取消、逾期自动化及接口旁路的完整权限验收。

## 2026-07-27 会话持久化检查点：证据整理—入卷页面流转（已完成并已清理）

- 已由本地页面完成完整正向流转：线索 `XS20260727085705` 经“提交审批 → 管理员审批通过 → 取证登记”，在“已取证”专用页面建立证据目录 `ZJ20260727171020221727`（`SMOKE-UI 证据目录材料`），再从证物列表完成“整理”。在尚未上传附件时，点击“证据入卷”由真实接口明确阻断并提示“入卷前必须上传证据目录及证据原件或扫描件”。
- 已由页面上传并回显两条材料：类别“证据目录”的 `SMOKE-UI-证据材料.pdf`、类别“证据原件”的 `SMOKE-UI-证据原件.pdf`；随后再次点击“证据入卷”成功，列表状态真实变为“已入卷”，材料入口显示“材料（2）”，提示“证据已入卷”。创建、审批、登记、目录建立、整理、上传与入卷全程均未使用数据库直写。

- 已由本地页面创建并真实回显新的、可精确清理的调查链路：父调查 `DC20260727084914`（`SMOKE-UI-证据入卷链路`）、子任务 `RW20260727165241349073`（`SMOKE-UI-证据入卷子任务`）、线索 `XS20260727085705`（`SMOKE-UI-证据入卷线索`）。线索保存动作已关闭表单，未使用数据库直写。
- 已完成精确清理与复核：证物生命周期页面没有可用删除入口，故仅在本地非生产环境使用受管理员限制的测试清理接口，按“证物 → 线索 → 子任务 → 父调查”顺序删除 `ZJ20260727171020221727`、`XS20260727085705`、`RW20260727165241349073`、`DC20260727084914`；每次删除后均按原模块和编号只读查询确认结果为零。证物记录删除时其两份派生附件一并清理，最终输出 `SMOKE_UI_EVIDENCE_CHAIN_CLEANUP_OK`。未触及任何既有用户记录。
更新日期：2026-07-27

## 2026-07-27 AI 智能文档关联证物页面闭环

- 已完成：通过本地页面新建临时调查 `DC20260727092001`、子任务 `RW20260727172236039112`、线索 `XS20260727092314`，依次完成“提交审批 → 管理员审批通过 → 取证登记 → 建立证据目录”，得到证物 `ZJ20260727172659425730`。全程采用专用页面，未直接写入数据库。
- 已验证：在“AI 智能文档”页面选择既有“民事起诉状”模板，检索并选择“证物｜ZJ20260727172659425730｜SMOKE-UI 智能文档关联证物”，成功创建任务 `AI202607271728506EC6`。列表真实回显编号、标题、关联证物和“待配置”状态；点击关联字段后打开只读“调查详情：ZJ20260727172659425730”，回显证物状态、权利人、调查员与说明。Dify 未配置时，回写按钮禁用，未伪造生成、确认或附件回写成功。
- 已完成清理：智能文档任务由页面“删除 → 确定”删除，列表恢复“暂无数据”。证物生命周期没有可用页面删除入口，故仅在本地非生产环境按“证物 → 线索 → 子任务 → 父调查”顺序，使用受管理员限制的测试清理接口精确删除上述四个 `SMOKE-UI` 记录；逐项只读核验为零，输出 `SMOKE_UI_AGENT_EVIDENCE_CHAIN_CLEANUP_OK`。未改动既有模板、用户数据或原系统。
- 仍未证明：Dify 的真实生成、人工确认与附件回写依赖后续安全配置；本项只完成 AI 智能文档关联证物的“页面创建 → 回显 → 只读详情 → 页面删除”闭环，不能替代 PPT 全部模块验收。

## 2026-07-27 本地页面调查—公证—证物链路（进行中）

- 已完成：以管理员身份仅在本地重构系统的“我发布的调查任务”页面，真实填写并保存一条带明确测试标识的父调查任务 `DC20260727081724`（事项：`SMOKE-UI-调查公证证物链路`；权利人：`SMOKE-UI-权利人`）。授权开始/结束日期均通过日期控件实际选择后保存，列表真实回显编号、权利人、权利类型、客户审核标记、日期及区域，并提示“调查任务已创建”。未操作原系统、测试服务器或既有用户记录。
- 已验证：此前直接向日期文本框填值不能使表单提交；改为真实选择日期控件中的日期后，表单关闭、列表回显且出现成功提示。该过程确认了该页面创建动作实际调用接口并落库，而非仅前端假提示。
- 已完成：继续通过“待我分配的调查任务 → 新增子任务 → 我的调查任务 → 新增线索”页面创建子任务 `RW20260727162512903794`（`SMOKE-UI-公证证物子任务`）和线索 `XS20260727082603`（`SMOKE-UI-公证证物调查线索`）。子任务真实回显为“待接收”，线索创建表单真实校验了标题、调查平台、侵权产品并提示“调查线索已创建”；均关联到本次测试链路，未触碰既有数据。
- 已完成并已在本地浏览器验证：线索 `XS20260727082603` 已依次经专用页面完成“提交审批 → 管理员审批通过 → 取证登记”，页面分别明确提示“线索已提交审批”“线索审批通过，进入待取证”“取证信息已登记，线索进入已取证”。日期通过真实日期控件选择，取证单位填写为带 `SMOKE-UI-` 标识的本地测试值；未绕过页面流转或写库伪造状态。
- 已完成并已在本地浏览器验证：在线索的“已取证”页，通过真实操作先建立证据目录 `ZJ20260727163310077225`（标题：`XS20260727082603 取证材料`，来源：调查取证），再建立公证 `GZ20260727163346358557`（标题：`SMOKE-UI-公证证物调查线索—公证审核`，状态：等待材料）。两条记录均在专用列表实际回显并关联本线索。
- 已修复并已在本地浏览器验证：`clue-my-collected` 专用“已取证线索”页面此前没有“建立证据目录”入口，导致证物生命周期无法从该页面继续。现已补入真实“建立证据目录”按钮，自动带入线索、负责人及来源；点击“创建证据”后进入证据管理列表并回显真实编号。前端生产构建及本地 Web 容器重建通过；本项代码提交、完整门禁和推送仍待本轮闭环后统一执行。
- 已完成并已在本地浏览器验证：以证物 `ZJ20260727163310077225` 创建临时 AI 文档任务 `AI202607271638012155`，列表关联列真实回显“证物｜编号｜事项”，点击后进入只读“调查详情：ZJ20260727163310077225”，回显状态、来源、线索和权利人；随后通过“删除 → 确定”从页面精确删除临时 AI 文档，列表恢复为空。
- 已完成并已在本地浏览器验证：以公证 `GZ20260727163346358557` 创建临时 AI 文档任务 `AI2026072716404243D8`，关联列点击后真实进入只读“调查详情：GZ20260727163346358557”，回显“等待材料”、来源线索和“由调查线索自动生成”说明；随后通过页面删除临时 AI 文档，列表恢复为空。Dify 未配置时状态明确为“待配置”，未伪造文件生成或回写成功。
- 已完成精确清理：两个临时 AI 文档均已由页面删除；本地非生产环境中，仅对本链路的公证 `GZ20260727163346358557`、证物 `ZJ20260727163310077225`、线索 `XS20260727082603`、子任务 `RW20260727162512903794`、父调查 `DC20260727081724` 按精确内部编号调用受管理员限制的测试清理接口。清理后逐模块只读查询 `investigation/task/clue/evidence/notary` 的 `SMOKE-UI-` 记录数为 `0`，输出 `SMOKE_UI_CHAIN_CLEANUP_OK`；本轮无上传附件，未发现需单独清理的通知或 AI 文档残留。
- 后续：本条“页面创建调查→子任务→线索→提交审批→取证→证物/公证→AI 关联只读详情→清理”闭环已完成。仍需按 PPT 台账继续逐模块验收，且不能把该单条调查链路扩大为全系统交付完成。
- 清理约束：完成页面证据后，优先走专用页面删除；若高级生命周期没有可用的页面删除入口，只允许在非生产本地环境，对这一条及其后续同一 `SMOKE-UI-` 链路的精确记录使用受管理员限制的测试清理接口，随后只读确认业务记录、附件、事件和通知均无残留。不得影响既有线索、案件、公证、证物或用户数据。

更新时间：2026-07-27

## 2026-07-27 AI 智能文档关联公证信息可用数据检查

- 已完成：在本地“生成文档”真实选择器中执行公证信息可用数据检查，确认关联选择器已加载六类业务对象的检索范围；本次仅检查，不创建智能文档任务、不修改既有调查或公证业务数据。
- 已验证：选择“民事起诉状”模板并在“关联业务”输入 `GZ` 后，页面返回“暂无数据”，本地当前没有可用于正向点击验证的公证记录。已取消创建窗口，AI 智能文档列表仍为“暂无数据”。
- 仍未证明：不能将空结果当作公证关联闭环。后续必须先通过专用调查线索的“取证 → 建立公证”流程获得可精确清理的本地公证记录，再完成“页面创建 → 关联回显 → 只读公证详情 → 专用删除”的正向验证；不得直接写库或改变现有用户记录。

## 2026-07-27 AI 智能文档关联证物可用数据检查

- 已完成：在本地“生成文档”真实选择器中执行证物可用数据检查；本次只检查关联候选，不创建智能文档任务，不修改证物、线索或其他既有业务数据。
- 已验证：打开关联选择器并输入证物编号前缀 `ZJ` 后，页面返回“暂无数据”。取消创建窗口后，AI 智能文档列表仍为“暂无数据”。
- 仍未证明：不能将空结果当作证物关联闭环。后续须从专用线索取证流程建立可删除的证据目录，完成“页面创建 → 关联回显 → 只读证物详情 → 专用删除”验证；不得以通用记录接口或直接写库绕过证物生命周期。

## 2026-07-27 本地完整门禁复验

- 已验证：再次执行 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 全部通过：前端生产构建（5479 modules）、Python 编译、菜单与客户端 API 覆盖审计、18 组 API 端到端业务流、自动测试数据清理和 Docker Compose 配置检查均成功，最终输出 `VERIFY_LOCAL_OK` 与 `SMOKE_CLEANUP_OK`。
- 仍未证明：自动化门禁不替代公证/证物关联的正向页面闭环，也不替代 PPT 全部页面、角色、状态和按钮的最终验收。

## 2026-07-27 AI 智能文档关联调查线索页面闭环

- 已完成：补足 AI 智能文档的“关联调查线索”页面闭环。关联业务选择器可按线索编号检索并显示“调查线索｜线索编号｜事项”；创建后的关联列可点击进入对应线索的只读调查详情，而不是退回筛选列表、编辑表单或无上下文页面。
- 已验证：本地浏览器通过“生成文档”页面创建临时任务 `AI202607271608176221`，选择“民事起诉状”模板并关联既有线索 `XS2026070015`。列表真实回显线索编号、事项与“待配置”状态；点击关联列实际打开“调查详情：XS2026070015”，调查编号、状态、事项、权利人、调查员和取证状态均由真实接口回显。随后从智能文档页面执行“删除 → 确定”，列表恢复“暂无数据”并显示“智能文档任务已删除”；既有线索未修改。
- 仍未证明：案件、合同、客户和调查线索关联已有浏览器证据；公证信息、证物两类关联对象仍须各完成同样的“页面创建 → 回显 → 只读详情 → 专用删除”闭环。Dify 真实生成、人工确认和附件回写依赖后续安全配置，当前只证明未配置时的真实阻断。

## 2026-07-27 AI 智能文档关联客户页面闭环

- 已完成：补足 AI 智能文档的“关联客户”页面闭环。关联业务选择器可按客户名称检索并显示“客户｜客户编号｜客户名称”；创建后的关联列保持为可点击业务入口，进入客户管理页后自动打开对应的只读客户详情，而不是错误跳往列表、编辑表单或空页面。
- 已验证：本地浏览器通过“生成文档”页面创建临时任务 `AI2026072716051064B2`，选择“民事起诉状”模板并关联既有客户 `KH202607222044012889538D3D`（合同审批客户-20260722124313）。列表真实回显关联客户、模板、创建人和“待配置”状态；点击关联列实际打开“客户详情：合同审批客户-20260722124313”，编号与客户档案字段均由真实接口回显。随后通过智能文档页面“删除 → 确定”精确清理临时任务，列表恢复“暂无数据”并显示“智能文档任务已删除”；既有客户未修改。
- 仍未证明：案件与合同关联已有浏览器证据；调查线索、公证信息、证物三类关联对象仍须各完成同样的“页面创建 → 回显 → 只读详情 → 专用删除”闭环。Dify 真实生成、人工确认和附件回写依赖后续安全配置，当前只证明未配置时的真实阻断。

## 2026-07-27 AI 智能文档关联合同页面闭环

- 已完成：补足 AI 智能文档的“关联合同”页面闭环。关联业务选择器可按合同号搜索并显示“合同｜合同号｜合同名称”，创建后列表关联列可点击进入真实只读合同详情；未配置 Dify 时仍保存可编辑提纲并将状态明确标为“待配置”，回写保持禁用，不伪造生成或附件成功。
- 已验证：本地浏览器通过“生成文档”页面创建临时任务 `AI20260727155913CEF9`，选择“民事起诉状”模板并关联既有合同 `HT20260722204437`。列表回显合同号、名称、待配置状态和真实操作；点击关联列后进入“合同查看：HT20260722204437”，合同状态、客户、类型、金额等均由真实接口回显。随后从智能文档页面执行“删除 → 确定”，列表恢复“暂无数据”并显示“智能文档任务已删除”；既有合同及模板未修改。
- 仍未证明：案件关联此前已有浏览器证据；客户、调查线索、公证信息、证物四类关联对象仍须各完成同样的“页面创建 → 回显 → 只读详情 → 专用删除”闭环。Dify 真实生成、人工确认和附件回写依赖后续安全配置，当前只证明了未配置时的真实阻断。

## 2026-07-27 历史 AI 文档路由归一

- 已完成：修复旧工作区或旧地址中的内部别名 `agent-document`。该别名现在会在初始路由、浏览器历史恢复、页面跳转和已保存工作区标签恢复时统一归一为正式菜单路由 `documents-agent`；重复的旧/新标签会按正式路由去重，地址由既有路由同步逻辑回写为正式键。
- 已验证：前端生产构建（5479 modules）、菜单覆盖审计和本地 Web 容器重建通过。内置浏览器直接打开旧地址 `?page=agent-document` 后，左侧“收发文台”自动展开，页面标题和工作区标签均为“AI 智能文档”，真实“智能文档中心”及生成文档操作正常显示，未创建或修改业务数据。
- 仍未证明：本项只处理历史路由兼容与标签语义；Dify 生成、人工确认、附件回写以及智能文档关联六类业务对象的页面闭环仍按 PPT 台账继续验收。

## 2026-07-27 用印草稿关联真实合同页面闭环

- 已完成：补足用印模块“合同号仅验证目标缺失”的遗留项。管理员可在“申请用印”中选择实际可用合同章并保存合同用印草稿；草稿列表的合同号会作为真实关联入口打开只读合同详情，不再只是文本或跳转至错误列表。
- 已验证：本地浏览器从“公司合同”只读取得既有已通过合同 `HT20260722204437`，随后在“我的用印申请 → 待审批”通过页面创建临时合同用印草稿 `YY20260727155035018`。列表回显合同号、合同章和草稿状态；点击合同号后实际进入“合同查看：HT20260722204437”，合同状态“已通过”、客户、合同类型与金额字段由真实接口回显。临时草稿未提交、未上传附件，随后通过页面“删除 → 确认删除”专用流程清理，列表恢复为空并显示“用印草稿已删除”；既有合同未修改。
- 仍未证明：合同用印草稿的正向关联详情已证明，但完整用印审批、实际用印、归档及各角色权限仍需按 PPT 用印/合同相关流程继续逐状态回归；不得将本项扩大为完整用印生命周期验收。

## 2026-07-27 菜单配置死路由阻断

- 已完成：系统菜单的可用路由集合改为从 264 个已实现的预置菜单定义派生。导航接口只返回该集合中的有效菜单；管理员接口拒绝创建没有已实现页面的任意菜单标识；菜单管理页取消“新增菜单”，改为只维护已有系统路由的名称、图标、排序、显示和启用状态。历史菜单记录不删除、不自动改写，单列为“未纳入当前导航的历史菜单”，由管理员在业务确认废弃后按需删除。
- 已验证：前端生产构建（5479 modules）、Python 编译和菜单覆盖审计通过。浏览器以管理员身份打开本地“菜单管理”页，页面显示 264 个系统路由及“不能新增没有页面功能的菜单入口”提示；“新增菜单”按钮计数为 0，未创建或修改任何菜单、业务数据。
- 仍未证明：本项防止配置生成新的死入口，不代替 264 个菜单各自的逐页面功能闭环；历史菜单是否应永久保留或删除，仍需业务方按旧站实际使用情况确认。

## 2026-07-27 工作区深层路由中文标签恢复

- 已完成：工作区恢复已保存标签时，深层路由会统一按中文路由标签表重写；此前浏览器可能显示的 `notary-import-info` 等内部键不再保留为可见标签。公证信息导入、取证信息文件导入、公证书文件导入、发票文件导入和公证书文件列表均在覆盖范围内。
- 已验证：前端生产构建（5479 modules）、菜单覆盖审计（新增 `WORKSPACE_ROUTE_LABELS_OK`）和 Docker API/Web 重建通过。浏览器直接打开 `?page=notary-import-info` 后，工作区标签和页面标题均为“公证信息导入”，未创建或修改业务数据。
- 仍未证明：本项只修复工作区标签语义，不代替公证导入、文件关联、审核或业务流转的完整页面闭环。

## 2026-07-27 完整本地交付门禁恢复通过

- 已完成：本机私有测试配置已恢复为可用状态，未写入仓库、普通文档或 Git 提交；此前因本机登录配置不一致而中断的验收不再阻塞。
- 已验证：执行 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 全部通过：前端生产构建（5479 modules）、Python 编译、264 个菜单节点/219 个叶子页面结构覆盖、380 个前端 API 路由映射、18 组 API 端到端业务流、测试数据零残留和 Docker Compose 配置检查，最终输出 `VERIFY_LOCAL_OK` 与 `SMOKE_CLEANUP_OK`。
- 仍未证明：该门禁证明接口业务流和自动清理，并不代替“页面创建 → 页面流转 → 页面删除”的逐模块浏览器证据；Dify 真实生成/人工确认/附件回写和全 OA 的逐角色、逐状态、逐按钮最终验收仍未完成。

## 2026-07-27 原布局财务关联字段统一整改

- 已完成：付款审核列表的案件编号、合同编号，内部费用原布局列表的案件编号，以及费用查询原布局列表的案件编号、合同编号、客户名称，均由纯文本改为真实只读关联详情入口；动态财务表格也统一覆盖案号、案件编号、客户、客户名称和客户编号，不再因页面使用不同表格渲染而回退为纯文本。空值仍显示“—”。
- 已验证：前端生产构建（5479 modules）、菜单覆盖审计（新增并通过 `FINANCE_ORIGINAL_RELATION_LINKS_OK`）和 Docker API/Web 重建通过。本地“费用查询”中有效客户“合同审批客户-20260722124313”已从表格点击进入只读公司客户详情，客户编号 `KH202607222044012889538D3D` 与基础资料正确回显；未创建、修改或删除数据。
- 仍未证明：付款审核、内部费用和费用查询中的案件/合同入口仍需在有有效关联对象且可精确清理的页面数据下逐项取得正向浏览器证据；不得把静态门禁或目标缺失提示标为正向闭环。

## 2026-07-27 发票详情客户关联统一整改

- 已完成：我的开票的处理、作废和普通详情三种申请信息视图中，“客户名称”均由纯文本改为真实只读客户详情入口；三处使用同一客户编号解析逻辑，并新增 `FINANCE_INVOICE_CUSTOMER_RELATION_OK` 静态门禁，防止仅修复其中一类发票状态。
- 已验证：前端生产构建、菜单覆盖审计和 Docker API/Web 重建通过。本地“我的开票”详情已显示可点击客户入口；现有记录关联的客户已被此前测试清理，点击后得到“未找到关联客户或当前账号无权查看”的明确提示，未跳往错误详情或产生写操作。
- 仍未证明：公司开票当前无记录，无法在不直接造库数据的前提下取得“有效客户关联 → 点击真实只读客户详情”的正向浏览器证据；待通过可清理的页面创建链路产生有效发票后，必须补处理、作废和普通详情三种状态的正向闭环。

## 2026-07-27 合同审批客户关联统一整改

- 已完成：合同审批列表中的“客户编号”和“客户名称”此前仅供阅读，现已统一接入该合同保存的客户标识，点击后打开真实只读客户详情；汇总行保持非业务记录，不显示伪链接。
- 已验证：本地 Web 容器重新构建后，合同审批页已显示客户编号与客户名称按钮；点击合同 `HT20260722204437` 的客户“合同审批客户-20260722124313”后，实际进入客户档案 `KH202607222044012889538D3D`，页面字段均为禁用只读。前端生产构建、菜单覆盖审计（新增 `CONTRACT_AUDIT_CUSTOMER_RELATION_OK`）和 Docker API/Web 健康检查均通过，未创建、修改或删除数据。
- 仍未证明：合同审批的完整发起、审批、合同变更、用印同步及后续案件/财务闭环仍须按总台账继续验收；本项仅证明客户关联详情入口。

## 2026-07-27 收发文客户关联详情统一整改

- 已完成：收发文登记列表与收发文详情抽屉中的“客户/主体”此前为纯文本，现已统一改为受当前账号数据范围约束的客户检索和真实只读客户详情入口；目标不存在或无权限时明确提示，未把名称猜测成可编辑页面。
- 已验证：本地 Web 容器重新构建后，使用既有收文 `SW20260722184503154925` 实测列表中的“光明乳业股份有限公司”可打开客户档案 `KH20260714001`；随后打开同一收文详情抽屉，再点击“客户/主体”也进入相同的禁用字段只读客户详情。未创建、修改或删除任何数据。前端生产构建、菜单覆盖审计（新增 `DOCUMENT_CUSTOMER_RELATION_OK`）及 Docker API/Web 健康检查均通过。
- 仍未证明：收发文的其他关联对象与全部状态流转仍按总台账继续验收；本项浏览器证据仅证明当前可见记录的列表与详情抽屉客户入口，不替代完整收发文创建、签收、归档与附件闭环。

## 2026-07-27 案件特殊列表案号关联统一整改

- 已完成：案件中心的“待认领案件”“退费案件”“收款案件”及发票附件列表中，原先仅展示的案件编号均调整为真实案件详情入口；点击后统一通过既有 `openSpecialCaseDetail` 解析当前记录或关联财务记录，不再错误跳至创建任务、案件列表或无上下文页面。关联目标不存在时沿用既有明确提示，不伪造详情。
- 已验证：前端生产构建、`git diff --check` 和菜单覆盖审计通过；审计脚本新增 `CASE_SPECIAL_RELATION_LINKS_OK`，覆盖上述四类特殊列表的案号渲染与真实详情跳转结构，防止后续回退为纯文本。
- 仍未证明：本轮尚未对四类特殊列表分别取得可精确清理的页面测试数据，故浏览器逐入口点击证据仍待补；在取得可删除的真实测试链路前，不得将其标记为完整业务闭环。

## 2026-07-27 调查、公证、证物关联详情统一整改

- 已完成：调查模块中线索的关联公证、公证的来源线索、证物的关联线索、公证书文件列表中的公证书号，以及公证/证物自身编号与客户字段，均改为真实详情入口；仓库证物页及其入库/出库/销毁操作弹窗中的线索编号、公证书号、案件编号、权利人也均按同一规则跳转。公证书号新增受当前账号数据范围限制的服务端解析接口，避免用外部证书号在前端猜测或越权打开记录。
- 已验证：前端生产构建（5479 modules）、Python 编译、菜单覆盖审计和 Docker API/Web 重新构建均通过；新增 `INVESTIGATION_RELATION_LINKS_OK` 与仓库证物关联字段结构断言防回归。
- 仍未证明：当前本地公证和仓库列表无可用记录，无法在不留下无法通过页面精确清理的数据的前提下完成该组入口的浏览器点击证据；待有可清理的真实测试链路后，必须补“线索 → 公证 → 证物/仓库 → 各关联详情”的页面闭环，不能以静态门禁代替。

## 2026-07-27 本地 AI 智能文档关联选择可辨识性整改

- 已完成：`AgentDocumentPage` 的“关联业务”下拉选项由“编号｜名称”调整为“业务类型｜编号｜名称”，覆盖案件、合同、客户、调查线索、公证信息与证物；同一页面中存在相近编号时，操作人员可以在创建前明确辨识目标业务，避免错误关联。
- 已验证：本地页面下拉已实际显示“案件｜SHMS2600388｜常年法律顾问合同案件”等“业务类型｜编号｜名称”选项；随后通过页面创建临时智能文档，关联既有案件 `SH191000382B`，列表回显关联业务并点击进入真实只读案件详情（含案件编号、阶段、客户与文档/费用/任务等标签），最后从页面删除该临时智能文档并复查列表无残留。前端生产构建通过（5479 modules）；本地 Docker API/Web/PostgreSQL/Redis 均健康。
- 门禁状态：本轮前端生产构建、菜单覆盖审计和前端/API 路由覆盖均通过；完整 `verify-local.ps1` 仍在 API 冒烟登录阶段因本机安全环境配置与当前管理员账号状态不一致返回 401 而停止，未创建任何冒烟数据。未猜测密码、未重置账号、未绕过认证。
- 仍未证明：公证信息、证物、合同、客户、调查线索五类关联对象仍需各自按同样的“页面创建 → 回显 → 点击真实只读详情 → 页面删除”闭环留证；Dify 未配置，因此真实生成、人工确认与回写附件不能标记为已证明。

## 2026-07-27 本地用印合同关联持久化与服务端查询整改

- 已完成：普通“申请用印”页面此前虽显示“关联合同号”和“用印类型”，但后端输入模型未接收这两个字段，草稿保存后会丢失关联；现已在专用用印申请创建/修改接口中持久化合同号和用印类型，列表、详情和合同详情跳转均可使用真实保存值。
- 已完成：用印查询由“最多已加载 20 条数据后再前端过滤”改为服务端筛选，支持申请编号、申请人、申请日期、案件编号、合同编号、客户、用印类型、文件名和单状态路由筛选；状态标签显示值与接口真实状态值已分离，避免“待审核/已审待用印”等显示文案被错误作为 API 查询值。
- 已完成：冒烟脚本新增普通用印申请的合同号持久化和按合同号、草稿状态服务端查询断言；菜单审计新增这组接口契约和页面查询参数断言，防止后续回退为纯前端筛选或保存丢字段。
- 已验证：本地前端生产构建（5479 modules）、Python 编译和菜单覆盖审计均已通过；本地 Docker API、Web、PostgreSQL、Redis、MinIO 均 healthy，Worker 已启动，近 5 分钟 API/Web 日志没有业务异常。
- 仍未证明：本地管理员浏览器凭据与测试站凭据不一致，页面登录已明确返回认证失败后停止，未猜测密码、未直接改数据库；本地 `SMOKE_PASSWORD` 未配置，`smoke-api.py` 在创建任何数据前安全退出。因此本项尚未完成“本地页面创建临时用印申请 → 保存合同号 → 按合同号查询 → 点击合同详情 → 页面删除并精确清理”的浏览器证据，也尚未提交或发布到测试服务器。

## 2026-07-27 会话切换交接：智能文档公证关联浏览器闭环进行中

### 当前总目标

- 以旧 OA 业务证据为准，继续完成网页端重构的逐模块业务闭环；验收必须覆盖真实按钮、权限、状态流转、数据落库、关联跳转、异常阻断和精确测试数据清理，不能以菜单可打开或接口存在代替完成。
- 本轮直接目标是补足“AI 智能文档关联公证/证据”的真实浏览器点击回归：从调查任务、子任务、线索、取证到公证记录，再创建智能文档并点击“关联业务”进入真实只读详情；随后从页面删除临时对象，并用只读核验确认没有残留。

### 本轮已完成并已在测试站页面验证

- 已使用测试站管理员页面创建临时调查父任务 `DC20260726123001`（事项“智能文档公证关联验证调查”）、临时子任务 `RW20260726055721981317`（“公证关联验证子任务-20260726”）和临时线索 `XS20260725215905`（“公证关联验证线索-20260726”）。
- 已通过页面完成该线索的“提交 - 审批通过 - 登记取证”流转；取证日期为 `2026-07-26`，页面提示“线索审批通过，进入待取证”和“取证信息已登记，线索进入已取证”。
- 已从“已取证线索”页面执行“建立公证”，生成临时公证记录 `GZ20260726062422621954`；在“公证管理”页面刷新后，列表真实回显该记录，状态为“等待材料”，来源线索为 `XS20260725215905`。
- 已通过页面创建并回显临时文书模板“浏览器公证关联模板-20260726”（诉讼文书，版本 `1.0`）。
- 本轮没有修改原系统；所有上述业务数据均只在重构测试站页面创建。测试凭据仅用于页面会话，未写入仓库、交接记录、提交信息或回复。

### 当前停点与下一步

- 当前浏览器已打开“AI 智能文档 → 创建智能文档任务”弹窗，标题已填写为“浏览器公证关联智能文档-20260726”；模板下拉已显示“诉讼文书｜浏览器公证关联模板-20260726”，但尚未选中，关联业务尚未选择 `GZ20260726062422621954`，因此智能文档任务尚未创建。
- 继续时应在同一弹窗中选择上述模板和公证记录，创建任务后刷新列表，确认关联业务字段回显公证编号，并点击该字段。验收通过的标准是进入真实只读调查/公证详情，不能落到编辑页，也不能提示“未找到关联业务”。
- Dify 未配置是已知环境条件：可以验证任务创建、字段提纲和关联跳转，但不得把“实际 Dify 生成、人工确认、回写附件”标记为已证明。

### 必须精确清理的临时数据

- 浏览器页面验证结束后，按专用页面流程删除智能文档任务和模板；再清理由本轮创建的公证、线索、子任务和父调查任务。不得删除既有记录，尤其不得删除既有案件 `SHMS2600002`、`SHMS2600003` 或用户数据。
- 删除后仅允许做只读核验：以上 4 个业务编号、对应 `agent_documents`、`document_templates`、附件、工作流事件和通知均应为零残留；如专用页面不提供某个带关联对象的删除动作，应记录为“无法安全清理/仍未证明”，不得绕过专用业务流程直接用通用接口或数据库删除。

### 已完成范围与未完成范围

- 已完成：智能文档关联公证/证据的代码加载与只读导航修复、菜单审计断言，以及此前智能文档关联案件的浏览器点击和删除事件清理闭环。
- 仍未完成：本轮公证关联智能文档的真实点击回归及精确清理；证据关联的独立浏览器点击回归；Dify 真实生成/确认/附件回写；全 OA 的 220 个叶子页面及逐角色、逐状态、逐按钮最终验收。

## 2026-07-26 智能文档列表与删除事件清理闭环

- 修复智能文档中心的加载隔离：`/agent/documents` 任务列表先独立呈现；模板或关联业务选项加载失败时仅提示警告，不再清空任务列表。提交 `4852a83` 已推送并发布。
- 修复删除智能文档任务时的工作流残留：按 `record_id`、`action=创建智能文档` 和任务编号前缀精确删除创建事件，不影响案件或其他工作流记录。提交 `8f13956` 已推送到 `test-server/dev`。
- 测试站发布目录为 `/opt/sunhold-oa/releases/20260726-051015`，API/Web/Worker 使用对应版本镜像；API、Web 健康，`http://127.0.0.1:8080/health` 返回 `{"status":"ok"}`，数据库、Redis、MinIO 未重建且数据卷未改动。
- 浏览器真实验证：创建临时文书模板和关联既有案件 `SHMS2600002` 的智能文档任务，列表回显任务编号 `AI202607260521352264` 及可点击关联案件；随后从页面删除任务和模板。只读核验：该任务、模板和对应创建事件均为 0，案件 ID 43 仍存在。
- 已完成证明：智能文档任务列表加载、关联业务字段进入真实案件详情（此前浏览器验收已显示“民事案件详情：SHMS2600002”）、任务删除和模板删除。仍未证明：Dify 实际生成、人工确认、回写附件，以及其他模块的全角色业务验收。

## 2026-07-25 合同审批菜单变更审批入口修复

- 浏览器验收发现：`contract-audit` 菜单路由没有被识别为审批视图，页面错误显示普通合同操作，管理员无法审批已提交的合同变更。
- 已将根路由纳入审批视图判定，并补充菜单审计断言，防止合同变更的通过/驳回入口再次从“合同审批”菜单消失。
- 已通过前端生产构建、Python 编译与菜单覆盖审计；待发布后用归档费联动测试合同的待审批变更完成页面复验。

## 2026-07-25 公司发起固定任务验收入口修复

- 浏览器验收发现：管理员在“事务中心 → 公司任务 → 公司发起的任务”中可以看到负责人提交完成的固定任务，但页面未提供“确认完成/退回重启”操作，导致固定任务无法被验收，也不会推进关联案件阶段。
- 已将该视图的已完成、待确认任务纳入管理员的确认与退回操作范围；个人发起任务原有的创建、重新派发权限边界未改变。
- 已执行前端生产构建、Python 编译和菜单覆盖审计；菜单审计结果为 `264 nodes / 219 leaves / 0 unhandled`。测试站已发布 Web 镜像 `sunhold-web:20260725-114959`，健康检查通过；页面已验收 `SHMS2600003` 的“立案登记”“送达跟踪”两条固定任务，关联案件已由“新案待分配”自动推进至“一审准备开庭”。

## 2026-07-22 案件归档案号与案件详情目录点击修复

- 修复 `case-archive` 归档审核页通用案件列表“案号”列仍为纯文本的问题；现在点击案号会打开完整案件详情工作台。同步修复开庭排期列表中的案号入口，避免同类纯文本残留。
- 修复案件详情左侧文档目录树不可点击的问题；现在“全部文档、客户文档、合同文档、调查文档、鉴别资料、取证文档、案件文档、主体及委托资料、起诉材料及证据、答辩材料及证据、法院诉讼文书、庭审及庭后文件”均为真实按钮，点击后右侧“文档信息”表按分类筛选并清空当前选择，避免误下载或误删其他分类文件。
- 针对测试服务器截图中的“页面资源加载失败”，服务器日志确认浏览器仍在请求旧版本哈希分包并返回 404；前端资源恢复逻辑已从普通 `window.location.reload()` 升级为带时间戳的 `reloadAppShell()`，错误页“刷新页面”会重新拉取 no-store 入口页，降低旧标签页缓存导致的分包失效。
- 已同步更新菜单审计脚本的版本恢复断言，并完成完整本地门禁 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1`：前端构建、Python 编译、菜单覆盖、369 个前端 API 映射、18 组业务冒烟、测试数据清理和 Docker Compose 配置全部通过，最终输出 `VERIFY_LOCAL_OK` 与 `SMOKE_CLEANUP_OK`。

## 2026-07-22 跨模块关联入口整改测试服务器上线

- 本地提交 `4234c3b fix cross-module business navigation` 已推送到测试服务器裸仓库 `test-server/dev`。
- 测试服务器已从 `dev` 创建新版本目录 `/opt/sunhold-oa/releases/20260722-130141`，对应提交 `4234c3b68490c4cf9a17475c049560f6964259fa`，并切换 `/opt/sunhold-oa/current` 指向该版本。
- 本轮仅涉及前端与文档/审计脚本改动，服务器只重建并切换 Web 镜像 `sunhold-web:20260722-130141`；API/Worker 继续沿用已健康运行的 `sunhold-api:20260721-171307`，未重启数据库、Redis、MinIO，也未修改业务数据卷。
- 已同步更新服务器私有 `/opt/sunhold-oa/private/.env.production` 中的 `WEB_IMAGE` 与当前运行镜像一致，并把 `API_IMAGE` 对齐到当前健康 API 镜像；密钥、密码和业务配置未写入仓库。
- 验证结果：服务器 Web 容器健康，六个容器运行正常；服务器本机 `http://127.0.0.1:8080/health` 返回 `{"status":"ok"}`；公网 `http://150.158.3.104:8088/health` 返回 200；公网首页返回 200 并加载新前端资源 `index-h1Hz-4O1.js`。近 5 分钟 API/Worker/Web 日志未发现 Traceback、Exception、Critical、Error。

## 2026-07-22 旧系统按钮跳转与关联入口审计继续推进

- 已新增审计文档 `docs/旧系统按钮跳转与关联入口审计表.md`，把跨模块关联入口分为“已闭环”“本轮新增补齐”“旧源码已发现但仍需逐按钮验收的复合动作”三类，避免后续把入口存在误判为整站完成。
- 本轮继续补齐三类同源问题：智能文档中心的“关联业务”可按案件、合同、客户、线索、公证、证据打开对应详情；沟通记录的客户 ID/客户名称可进入客户详情；合同应收的合同号、客户名称、案号可进入合同、客户、案件详情。
- 已执行完整本地门禁 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 并通过：前端生产构建 5479 个模块、Python 编译、菜单覆盖 `264 nodes / 219 leaves / 0 unhandled`、前端 API 覆盖 `369`、18 组 API 端到端冒烟、测试数据清理和 Docker Compose 配置均通过，最终输出 `VERIFY_LOCAL_OK` 与 `SMOKE_CLEANUP_OK`。
- 仍未证明：旧源码案件列表、调查中心、合同/调查新建、财务收付款中的复合按钮还需要继续按权限、状态、接口、落库、文件生成和失败提示逐项验收；不能据此宣称整站最终交付完成。

## 2026-07-22 案号点击进入完整案件信息工作台

- 用户确认原系统中点击案件编号后，应打开完整“案件信息”界面，而不是简化详情或任务创建入口。本轮已将 `CaseCenterPage` 中案号详情抽屉改为接近正式系统的完整工作台结构：顶部为案件基本信息表格式区域，中部为文档信息、律所费用、平台费用、内部结算、案件提醒、案件日志、系统日志、案件任务、客户任务标签页，左侧增加客户/合同/调查/案件文档目录树，右侧增加案件提醒与案件日志快捷区。
- 本轮仅调整前端详情承载界面与样式，复用已有真实接口和现有按钮行为：修改基本信息、下载选中 ZIP、删除选中、新增提醒、新增日志、费用与任务列表均仍走既有后端数据，不新增假按钮、不写入原系统。
- 已验证：`npm --prefix apps/admin-web run build` 通过，TypeScript 与 Vite 生产构建正常，5473 个前端模块完成构建。
- 仍未证明：本轮尚未通过浏览器逐项点击正式系统同款按钮做人工回归，也未发布到测试服务器；后续需在登录会话中点开民事案件案号，核对工作台布局、字段、文档目录、右侧提醒日志和各标签页按钮是否符合业主验收口径。

## 2026-07-21 本地 Git 与测试服务器源码通道

- `C:\Users\Administrator\Desktop\系统重构` 已初始化为有效 Git 仓库，当前开发分支为 `dev`；首次源码基线提交为 `6a8e80d`。
- 本地远程名 `test-server` 指向测试服务器裸仓库 `/opt/sunhold-oa/git/sunhold-oa.git`，`dev` 已推送并设置上游跟踪。以后必须先在本地修改、验收、提交，再推送服务器；不得直接在服务器版本目录中改源码。
- `.gitignore` 已排除 `.env`、所有非示例环境文件、`sfhc/`、腾讯云服务器密钥目录、数据库、上传数据、备份、日志、临时目录、构建产物和私钥常见格式；已知服务器密码、云密钥及私钥头扫描结果为零匹配。
- 服务器私有环境配置已独立保存到 `/opt/sunhold-oa/private/.env.production`，权限 `0600`，不进入 Git；后续发布从裸仓库 `dev` 创建新版本目录并复制该私有配置。
- 当前仅建立 `dev` 开发/测试流程。正式生产上线前另建受保护的 `main` 分支和发布标签，并在合并前执行完整交付验收。

## 2026-07-21 客户列表列重叠热修复

- 当前稳定发布目录更新为 `/opt/sunhold-oa/releases/20260721-162804`，Web 镜像 `sunhold-web:20260721-162804`；后端和数据卷没有变化。
- 客户列表采用固定列布局，客户编号操作元素与内部文本均被约束在第一列内，所有数据单元格强制裁切溢出，解决编号覆盖客户名称和表头/数据视觉错位。
- 本机完整验收输出 `VERIFY_LOCAL_OK`，18 组业务测试数据全部清理；服务器 Web/API 健康，公网 `/health` 返回 200。页面会话失效导致服务器真实数据页视觉复核未执行，需用户刷新登录后确认实际窗口效果。

## 2026-07-21《新系统测试2》修复与测试服务器版本

- 当前稳定发布目录：`/opt/sunhold-oa/releases/20260721-160926`，`/opt/sunhold-oa/current` 已指向该目录；API/Worker 镜像为 `sunhold-api:20260721-160926`，Web 镜像为 `sunhold-web:20260721-160926`。
- 本轮修改范围：管理员员工用户名编辑与关联迁移；用印审批通过/拒绝后的列表流转和审批元数据；案件类型、案由、权利类型参考资料；恢复民事案件新建入口；关联合同后锁定客户并自动带出案源人。
- 本机完整验收为 `VERIFY_LOCAL_OK`：5473 个前端模块、Python 编译、264/219/0 菜单覆盖、364 个前端 API 匹配、18 组真实业务冒烟和测试数据零残留均通过。
- 服务器六容器正常，公网健康检查返回 200，近期 API/Worker/Web 日志无严重错误。服务器低配且 Python 软件源下载缓慢，本轮后端依赖未变化，因此沿用上一版已验证依赖镜像并替换应用代码生成新镜像；没有改动 PostgreSQL、Redis、MinIO 数据卷。
- 浏览器容器切换后登录会话失效，内置浏览器和 Chrome 都停在登录页。本轮未擅自输入或保存密码，服务器侧页面视觉抽查留待用户恢复登录会话后进行；接口业务闭环已由本机端到端测试证明。
- 整站最终验收仍未完成，后续必须继续按模块证明角色权限、状态流转、阻断规则、文件生成和数据落库，不能把本轮四项修复扩大表述为整站交付。

## 2026-07-21 客户列表布局热修复

- 用户截图确认公司客户列表的长客户编号覆盖客户名称列。已为客户编号、案源人、客户管理人及通用表格单元格增加省略与边界裁切，完整文本可通过悬停查看。
- 修复版已发布到 `/opt/sunhold-oa/releases/20260721-095842/sunhold-oa-deploy-20260721-095842`，稳定软链接 `/opt/sunhold-oa/current` 已切换；Web 和依赖 API 均健康，外部浏览器实测无重叠、无控制台错误。
- 本地完整五段验收再次通过，18 组冒烟数据已全部清理。

## 2026-07-20 PDF 业务蓝图最低集对齐

- 用户最终口径：两份案件管理 PDF 作为最低功能集合，只补缺少项，现有更多功能不删。用印保持独立模块。
- 已完成客户分级和关键字段审批、签约客户服务端、合同多外部编号与变更审批、手工立案审批、案件固定任务与阶段推进、任务挂起/取消审批及绩效提醒、特殊解档、调查关闭并生成 DOCX 报告、开庭短信待发通道、13 类蓝图角色种子。
- 已重建本地 API/Web Docker 镜像；API、Web、PostgreSQL、Redis 均健康。18 组端到端业务流程通过且测试数据零残留；结构审计为 263 节点、218 叶子、0 未处理，前端 359 个 API 调用全部可映射到后端路由。
- 未证明项必须单独保留：真实短信到达依赖业主选择服务商并配置 `SMS_WEBHOOK_URL` 与令牌，目前只证明规则、待发记录、审计和失败状态；PDF 不能证明的岗位具体数据范围仍需合伙人逐角色确认。
- 本轮版本已发布到腾讯云宝塔测试机，当前发布目录为 `/opt/sunhold-oa/releases/20260720-172535/sunhold-oa-deploy-20260720-172535`。服务器 Python 软件源在首次构建时波动，但新旧 `requirements.txt` SHA256 完全一致，因此后端沿用既有已验证依赖镜像并只替换应用代码；API、Web、PostgreSQL、Redis、MinIO 均健康，Worker 运行，外部健康检查和客户服务端页面均返回 HTTP 200，新客户服务端接口返回预期参数校验 422，近期日志无 Traceback/Critical/Exception。
- 测试管理员 `admin` 的随机一次性密码已按用户指令完成修改，并用新密码重新登录验证成功；旧凭据文件 `/root/sunhold-oa-test-admin.txt` 已删除，当前密码不落入仓库或交接文档。
- 测试管理员初始显示名称和部门曾因服务器初始化时的字符编码变成 `???/????`；已通过管理员专用接口修正为“管理员/上海分所”，并用 UTF-8 客户端读取复核。服务器私有 `.env.production` 的初始化名称和部门也已同步修复，避免以后重建空库再次出现问号。
- 完整对齐与验收边界见 `docs/案件管理PDF逻辑对齐清单.md`。

## 2026-07-19 腾讯云宝塔轻量服务器接入

- 已接入腾讯云轻量实例 `lhins-56emf98u`（上海二区，公网 `150.158.3.104`，实例名“宝塔Linux面板-KtqG”，OpenCloudOS 9.4，2 核 2GB，50GB系统盘）。本机 SSH 别名为 `baota-lighthouse`，已实际登录验证主机名 `VM-0-17-opencloudos`、用户 `root`。
- 本机没有云端原密钥 `lhkp-gewq563o`，已利用在线的腾讯云 TAT 自动化助手追加独立维护公钥；未覆盖原密钥、未重启实例。完整连接说明见 `docs/腾讯云宝塔轻量服务器连接说明.md`，私钥内容和腾讯云 API Secret 未写入仓库。
- 首次登录时系统盘可用约 39GB，物理内存可用约 1.2GiB，但1GiB Swap 已使用约557MiB，存在内存压力；在决定是否承载 OA 前须另行只读盘点现有宝塔网站与服务。

## 2026-07-19 OA 腾讯云测试服务器接入

- 已接入腾讯云实例 `ins-ggzft8rn`（上海，公网 `101.35.24.73`，OpenCloudOS 9.4，4 核 8GB）。服务器 SSH 仅允许公钥认证，用户提供的密码不能直接用于 SSH；已利用在线的腾讯云 TAT 自动化助手追加独立维护公钥，未覆盖原 `gold` 密钥、未重启实例、未开放密码认证。
- 本机 SSH 别名为 `oa-test-tencent`，已实际登录验证主机名 `VM-0-14-opencloudos`、用户 `root`。完整连接说明见 `docs/OA腾讯云测试服务器连接说明.md`；密码明文、私钥内容和腾讯云 API Secret 均未写入仓库。
- 服务器首次检查时约有 6.4GB 可用内存，足以承载 OA 六容器测试环境；系统盘仅 20GB、可用约 14GB，部署前建议扩容至至少 60GB、推荐 80GB。用户在聊天中提供过腾讯云 API Secret，连接完成后应立即在腾讯云访问管理控制台轮换。
- 服务器现有业务不是空白：`MT5 Signal Sharing API Service` 位于 `/opt/mt5-signal-api`，以 Python 3.11/systemd 运行并直接监听80端口，约占 208MB；MariaDB 10.11 业务库 `mt5_signals` 约占 247MB。当前没有 Docker、PM2 或运行中的容器。OA 部署必须与该系统隔离，不能覆盖目录、复用数据库或抢占80端口；建议 OA Web 仅绑定 `127.0.0.1:8080`，再按独立域名配置反向代理。

## 2026-07-19 sfhc 测试服务器承载 OA 可行性检查

- 仅做了只读环境检查，尚未上传 OA、创建目录、启动容器或修改 sfhc 测试服务器现有配置。服务器为 Ubuntu 22.04，2 核、1.6GiB 内存、3GiB Swap，检查时可用内存约 260MiB；系统盘 40GiB，已使用 29GiB，仅余约 8.9GiB。
- Docker 29.0.2 与 Compose 2.40.3 可用；当前仍有 Portainer、MySQL、Redis、Nginx、Uvicorn、Node/PM2、PHP 等服务占用资源。OA 生产 Compose 六服务的内存上限合计约 6.25GiB，完整部署到现状服务器会明显挤压既有测试业务，不能视为安全可交付方案。
- 2026-07-19 内存只读复核：物理内存共 1,608MiB，已用约 1,111MiB、可用约 297MiB，3GiB Swap 尚未使用。最大单进程为 MySQL `mysqld` 约 317MB，其次为 Uvicorn 约 184MB；PHP-FPM 7.2/7.4/8.0/8.1 四组进程合计约 247.5MB。宝塔面板约 43.9MB，Docker daemon 与 containerd 合计约 62.5MB，Nginx 约 28.9MB，阿里云安全代理约 47.5MB；唯一 Docker 容器 Portainer 仅约 23MB，因此 Docker/Portainer 不是主要内存来源。系统缓存约 420MB可部分回收，但不足以支撑完整 OA 六容器。
- 业务应用映射进一步确认：Uvicorn 约 184MB 对应 `Xinghe AI Embedding Service`（目录 `/www/wwwroot/embedding-service`，本机端口 18080）；PM2 中 `pig-backend` 在线约 31 天，约 82MB（目录 `/opt/pig/releases/20260605-145014/backend`，端口 3000）。网站目录另有 `xinghe-miniapp`、`wx-miniapp`、`xinghe-shipping-fix`、`digital-human-web`、`personal-website`，主要由 Nginx/多版本 PHP-FPM 共用运行，不能仅按目录准确拆分 PHP 内存。MySQL 约 317MB是这些应用共用数据库；宝塔面板及其 Python 任务约 70MB；Portainer 约 13–23MB。静态前端目录自身不常驻大量内存，主要占用来自数据库、Embedding 服务、PHP 进程池和 `pig-backend`。
- 2026-07-19 按用户指令仅停止 `xinghe-embedding.service` 当前实例，未删除文件、未修改其他应用，也未禁用其开机自启。停止结果为 `inactive/dead`、`MainPID=0`，本机 18080 端口不再监听；服务器可用内存由约 297MiB提升至约 494MiB。服务仍为 `enabled`，服务器重启时会再次启动；如需长期腾出内存，须另行确认后执行禁用。
- 端口可以隔离：OA Web 可绑定 `127.0.0.1:8080`，检查时该端口空闲；PostgreSQL、Redis、MinIO、API 可仅使用 Compose 内网，不直接暴露公网。真正阻塞项是内存与磁盘，不是 Docker 或 SSH。
- 源码白名单包约 19.18MB，可以上传保存，但构建镜像、数据库卷、附件卷和运行日志需要额外空间。若要完整启动，建议至少升级为 4 核 8GiB、80GiB 系统盘或使用独立 Linux ECS，并保证部署前有 30GiB 以上可用磁盘。上传必须使用 `scripts/export-delivery-source.ps1` 的脱敏白名单包，排除真实 `.env`、`sfhc/`、数据、附件、备份、日志和私钥。
- 停止 Embedding 服务后再次用本地真实容器数据比对：OA 六容器当前合计约 841.4MiB（API 107.8、MinIO 179.1、PostgreSQL 87.0、Redis 14.6、Web 14.9、Worker 438.0），而测试服可用内存仅约 476MiB。即使去掉 Worker，核心五容器仍约 403.4MiB，只剩约 72MiB给现有业务波动和系统，不能安全运行；依赖 3GiB Swap 强行启动只代表“可能拉起”，不代表稳定可用。磁盘仍仅余 8.9GiB，端口 `127.0.0.1:8080` 空闲且 Docker/Compose 版本满足要求，因此结论仍是资源不足，Docker 和端口不是阻塞项。

## 2026-07-19 原 Windows OA 服务器稳定维护通道

- 申浩服务器的长期连接说明已独立记录在 `docs/申浩服务器连接说明.md`，包含 SSH/SFTP/SMB 命令、账号、凭据管理器目标、密钥路径与指纹、数据库入口和只读纪律；密码明文与私钥内容不进入仓库或普通文档。
- 已在原 Windows Server 2012 R2 OA 服务器安装微软签名的 Win32-OpenSSH 9.8.3，新增 `sshd` 服务和 SFTP 子系统；服务设置为开机自动启动，并配置 5 秒、15 秒、60 秒三档故障自动重启。安装未修改旧 OA、IIS、数据库或业务文件。
- 本机已建立独立密钥登录别名 `oa-legacy`，SSH 客户端配置 30 秒心跳、3 次失败判定和 3 次连接尝试；服务器仍保留原管理员密码登录作为应急回退。密码、私钥和连接字符串均未写入仓库或交接文档。
- 验证证据：连续 5 次密钥 SSH 登录均成功，远程身份为服务器管理员；`sshd` 状态为 `RUNNING`；TCP 22 防火墙规则启用；SFTP 已实际列出 `C:/ProgramData/ssh/sshd_config`。该通道用于后续只读盘点与经授权的部署维护，不改变原系统只读对照纪律。
- 已通过该通道从 OA 应用服务器内部只读连接真实业务数据库 `PRD_CRM_SH_20190320`（数据库服务器为内网主机，凭据未输出或落盘）。数据库引擎为 SQL Server 2012 Enterprise 64-bit `11.0.3000.0`，共有 253 张表、16 个视图、124 个存储过程；数据文件约 46,655.06MB、日志文件约 68.13MB。最大表 `SYS_Log` 约占 44,548.55MB，说明此前看到的本机小型系统库并非真实 OA 业务库。
- 只读规模抽样确认：`Legal_Case` 精确状态汇总为 10,691 行；`Legal_Case_Task`、`Legal_Case_File`、`Legal_Case_Log` 等案件任务、文件和日志表均存在，另有合同、调查、财务、知识产权案件等业务表。早期按分区与分配单元聚合会因同一分区存在多个分配单元而重复累计，已不再采用其案件行数；本轮精确状态汇总仍使用 `READ UNCOMMITTED`，未执行任何增删改。
- 案件归档字段已由数据库字段与原程序集枚举双重确认：`ArchiveStatus=5` 待提交、`7` 待内部审核、`10` 待审核、`20` 审核通过、`30` 审核拒绝，空值表示尚未进入归档流程。按“空值或状态不等于 20”只读筛选，历史未归档共 5,007 件，其中有效案件 3,485 件、停用案件 1,522 件；有效未归档进一步分为尚未进入流程 2,659 件、待内部审核 183 件、待审核 529 件、审核拒绝 114 件。该统计未修改任何案件状态。
- 机构归属已进一步核实：`Legal_Case` 全部 10,691 件（含已归档与未归档）的 `CompanyId` 均为 `1`；关联 `IPR_Company` 主数据为机构代码 `31280`、简称 `SH`、名称“上海申浩律师事务所”。因此上述 5,007 件历史未归档及其中 3,485 件有效未归档均属于申浩库，不是沪知案件；沪知须从独立的 `HZ.CRM` 应用/数据库另行只读统计。

## 2026-07-17 全系统审计与集中整改启动

- 用户要求先找出全系统问题，再按依赖一次性集中整改，不再零散扩页。已完成前端、后端、测试/部署三路静态审计，统一台账见 `docs/全系统问题审计与集中整改清单.md`。
- 第一批已封堵通用记录接口绕过、无关联财务流水和智能文档未确认回写；合同与用印草稿改为专用接口，合同审批中不能归档。案件归档已改用真实办结、四类附件、费用/应收和财务流程计算并在提交/审核时二次校验。收发文、调查等前端专用命令、统一权限、服务端分页、Celery/MinIO/Alembic 和生产迁移仍未完成。
- 源码、登录页和冒烟脚本中的默认管理员密码已移除；初始管理员密码只从私有环境变量注入，首次登录改密前统一阻断业务接口。生产环境仍须单独生成、注入和轮换强密码，不能复用本机测试凭据。
- 最新完整 `verify-local.ps1` 已通过：前端 5471 modules、Python 编译、263/218/0 菜单覆盖、343 个前后端接口静态匹配、17 组 API 冒烟和零测试残留。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，最近日志无严重错误；Worker 仍以 root 运行且只承载 health 示例任务，不能视为生产就绪。

## 2026-07-17 验收口径调整为业务逻辑

- 用户已明确：服务器旧 OA 页面代码和数据库关系继续作为业务证据，但本地运行的是 React + FastAPI 重构系统，并非直接运行旧站源码。
- 用户进一步确认最终交付口径：视觉可以与旧系统不同，但旧系统全部有效按钮、功能和业务逻辑必须在新系统中完整覆盖。覆盖范围包括查询筛选、弹窗、明细、导入导出、打印、文件上传下载、审批、撤回/退回/终止等状态动作、角色权限、数据范围、异常阻断和跨模块联动；每项必须是真实功能，不能使用死按钮或伪成功提示。
- 旧站页面源码在 `D:\OA系统源码归档_20260717\source-extracted-v1` 可直接读取，现已确认包含 1,817 个 Razor `.cshtml` 页面、389 个 CSS 和 3,567 个 JavaScript 文件；例如控制台 `SH.CRM.WEB\Views\Dashboard\Index.cshtml` 以及独立用印 `Areas\AWS\Views\OfficialDocument` 下的列表、新建、审批、预览和文件页面。读取源码可以确认字段、按钮、页面分区和前端条件，但若不恢复旧 .NET 运行环境、配置、服务及数据库，不能把这些文件等同于已完整运行的旧网站。
- 后续不再以旧站颜色、布局、图标、间距或逐像素页面一致作为交付标准。验收重点调整为：角色责任、客户→合同→案件主链、调查取证、独立用印、任务交接、财务结算、归档条件、权限范围、状态流转、阻断规则及按钮真实行为。
- 2026-07-17 已在 `http://127.0.0.1/` 查看本地控制台，当前页面可正常打开并显示管理员身份及客户、合同、案件、调查、独立用印、财务、人事、仓库、报表等主要菜单。该检查只能证明入口与页面当前可见，不能代替上述业务闭环验收。

## 2026-07-17 合伙人业务蓝图确认稿

- 已新增面向非技术业主的 Word：`D:\OA系统源码归档_20260717\律所管理系统业务蓝图确认稿_20260717.docx`。文档不使用代码、数据库和接口术语，按“谁负责、事情如何流转、必须守住什么、需要合伙人拍板什么”说明律所管理逻辑。
- 文档共14页，包含一页摘要、业务地图、角色责任、客户/合同/案件、任务与交接、调查取证、独立用印、财务、典型场景、11项拍板清单以及可打印的签字页。30天、交接重启、交接5天未开始自动处理等尚需业主确认的规则均标为待拍板，不写成既定制度。
- 标准 LibreOffice 渲染器因本机未安装 LibreOffice 无法启动；已使用 Microsoft Word 导出 PDF，并逐页检查全部14页。第二版已消除模块表、拍板清单及签字备注的孤立分页，未发现文字重叠、表格截断、流程图缺字或异常空白页。

## 2026-07-17 原 OA 业务逻辑 Word 报告

- 已将业务逻辑总览、深度推断和33条证据矩阵合并为正式 Word：`D:\OA系统源码归档_20260717\原OA系统业务逻辑分析报告_20260717.docx`。
- 文档共10页，包含封面、执行摘要、整体业务流程图、客户/合同/案件/任务/调查/用印/财务/权限章节、高风险待验证项和横向证据矩阵附录。
- 标准 LibreOffice 渲染器因本机未安装 LibreOffice 无法启动；已改用本机 Microsoft Word 导出 PDF，并将全部10页逐页转为 PNG 检查。最终版未发现文字重叠、表格截断、流程图缺字或异常分页。

## 2026-07-17 原 OA 业务逻辑深度推断

- 在源码、页面、程序集枚举、数据库字段和状态分布的基础上，新增 `docs/原OA系统业务逻辑推断报告_20260717.md` 与 `docs/原OA业务规则证据矩阵_20260717.csv`，将规则分为 A 级直接证明、B 级强推断和 C 级待验证，避免把业务常识写成原系统事实。
- 新增直接证据包括：归档合同不能建案、不同客户案件不能合并、已生成案件的线索不能重复建案、案件费用未到账不能提交归档、发票金额为零不能提交、合同缺少外部合同号不能开票、多案件回款分配必须由财务处理、调查项目创建需要合同。
- 任务程序集和数据访问层直接存在 `GetFinishedTaskListAfter5Days`，SQL 会查询 `TaskStatus=3` 且完成时间超过指定天数的任务；这证明“完成超过 N 天”分组存在，但尚未取得调用它的 Windows Service 主程序，不能据此完全证明自动转换的准确动作。项目现有三条任务不变量继续保留，5 天和 30 天精确触发仍列为待补证据。
- 已把推断报告和证据矩阵同步复制到 `D:\OA系统源码归档_20260717`，未读取或保存客户姓名、案情等生产业务明细。

## 2026-07-17 原 OA 源码与业务逻辑盘点

- 已从原 Windows 服务器只读归档 OA 应用源码，保存为 `D:\OA系统源码归档_20260717\oa-source-20260717.zip`，SHA-256 为 `D8C5BFA673312D70F9198540B0EC1219DBBB3FF9E0E5256A335DDF51D4DCA3C9`；本地哈希与服务器导出哈希一致。解压目录含主站、API、Windows Service 和 VIP 站点，共 9,541 个文件、1,108 个 C# 文件、1,817 个 Razor 页面和 3,567 个 JavaScript 文件。
- 已单独保存 8 个一方业务程序集及 PDB，并完成静态元数据分析：2,264 个类型、20,289 个方法、1,208 个枚举值。未执行这些旧程序集。
- 已从生产数据库只读提取元数据和安全聚合状态：253 张表、4,141 个字段、16 个视图、124 个存储过程、334 个索引、201 组状态分布；没有复制客户姓名、案情等业务明细，也没有修改原数据。数据库没有声明外键，重构关联必须由服务层和 PostgreSQL 约束补足。
- 已生成 `docs/原OA系统业务逻辑总览_20260717.md`，并复制到归档根目录 `D:\OA系统源码归档_20260717\业务逻辑总览.md`；同时生成数据库表、状态分布、存储过程名称和“页面—控制器—数据表—生命周期”对应 CSV。
- 已直接证明客户→合同→案件/调查→任务/文件/费用→发票/付款/结算/归档的主链；用印已确认是拥有独立编号、审批、附件和打印状态的独立模块，只可选关联合同或案件。完整角色权限矩阵、所有非空页面视觉条件和服务 DLL 的全部内部分支仍未完全证明，不能据此宣称整站复刻完成。
- 生产数据库全量备份及约 619 GiB 附件尚未复制；当前本地保存的是源码、业务程序集、数据库元数据与聚合状态，均不包含服务器密码或连接字符串。

## 2026-07-17 行政案件新建流程阶段状态

- 本轮继续案件中心“新建案件 → 行政案件及国家赔偿”。前端已把行政新建从刑事复用中拆出关键差异：默认客户诉讼地位为“原告/申请人”，权利类型随第一步提交，客户为“被告/被申请人”或“第三人”时会预填到对应当事人栏；行政第二步不允许直接空司法信息完成，完成后跳转到 `case-company-administrative`。
- 行政第三步页面不再展示刑事专属“公安机关/检察院”字段，只保留一审、二审、再审法院信息；刑事流程仍保留公安、检察院和法院三类字段。
- 后端已补行政案件客户诉讼地位白名单、权利类型规范化保存、行政当事人原告/被告必填、行政司法机关至少一项法院信息、禁止行政案件写入公安或检察院字段。`CASE_NEW_ADMINISTRATIVE_FLOW_OK` 已加入结构门禁。
- 验证状态：完整 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 已通过，包含前端生产构建 5471 modules、Python 编译、菜单/页面结构审计 `263 nodes / 218 leaves / 0 unhandled`、`CASE_NEW_ADMINISTRATIVE_FLOW_OK`、17 组 API 冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。Docker API/Web/Worker 已按新代码重建启动。
- 本轮同时修复验收环境稳定性：`scripts/smoke-api.py` 默认 API 地址改为 `http://localhost:8000`，避开 Windows `127.0.0.1:8000` 端口代理残留导致的非 Docker 数据库路径误报。当前不能写成整站完全完成；行政案件三步业务闭环已验证，但原站行政非空样本、完整字段初始值和列表视觉仍需后续只读证据校准。

## 当前运行架构

- 前端：React 19、TypeScript、Vite、Ant Design、Recharts。
- 后端：FastAPI、SQLAlchemy Async、JWT、Celery。
- Docker：PostgreSQL/pgvector、Redis、MinIO、API、Worker、Nginx Web。
- 本地入口：`http://127.0.0.1/`；API 建议使用 `http://localhost:8000`；API 文档：`http://localhost:8000/docs`。
- Docker Compose 项目名：`sunhold-legal-platform`。
- 持久化卷：`postgres_data`、`redis_data`、`minio_data`、`uploads_data`。

## 已完成并有验证证据的工作

- 财务“结算管理 → 待结算”完成首轮原站只读对照：原站 `/5001007001/FAM/Settlement/PendingList` 的 10 项筛选、14 列加占位、全局合计、两行回款上下文、10 条六档分页、三枚行图标、四个底部按钮及行内展开的 15 列分配明细均已确认；原站没有执行申请结算等写操作。
- 本地新增通用结算专用候选、申请和三类 Excel 导出接口。候选由银行回款/案件费用分配实时生成，明确保存每笔结算金额与归档费；申请创建 `finance_settlement` 待审批记录并防止重复，通用记录接口不能绕过专用流程。浏览器临时回款验证 `500/500/0`、官费 `100`、代理费 `200`、其他 `200`、代理结算 `160`、归档费 `16`、实际结算 `444`，以及展开明细、空选提示、勾选、确认和真实申请后移出列表；全部临时记录已精确清理并复查为零。
- `FINANCE_SETTLEMENT_PENDING_OK` 与第 16 组 API 冒烟已加入门禁。最新统一验收为 5471 modules、265/220/0、44 个财务原布局路由、16 groups passed、零测试残留和 Docker Compose 配置通过；六个 Docker 服务正常，API/Web/PostgreSQL/Redis 健康，近期 API/Web/Worker 严重日志为 0，Docker 生产页控制台错误为 0。
- 财务“结算管理 → 待审核”完成首轮原站只读对照：原站 `/5001007002/FAM/Settlement/ProcessingList` 的十三项筛选、十四列加占位、提交信息/回款信息上下文、10 条六档分页、三枚行图标、五个底部按钮、15 列行内分配明细，以及“同意结算/拒绝结算”弹窗的标题、备注和按钮均已确认；原站未执行任何审批写操作。
- 本地新增结算申请专用查询、服务端分页、十三项过滤及批量审批接口；同意转待付款、拒绝转已拒绝并记录审核意见和流程历史，权限范围、非法状态、重复审批及通用接口绕过由后端拦截。三类 Excel 支持按选中申请导出真实数据，`FINANCE_SETTLEMENT_AUDIT_OK` 和第 16 组冒烟覆盖列表、筛选、合计、导出、同意/拒绝和防绕过边界。
- 浏览器临时申请 `4017` 已完成查询、明细展开、空选提示、同意/拒绝弹窗、全部导出入口和真实拒绝回归；关联申请、回款 `142`、应收 `152`、案件 `4016`、合同 `4015`、客户 `4014` 已精确删除，ID 复查为 404、关键词及两种申请状态复查为 0，页面控制台日志为 0。统一验收通过 5471 modules、265/220/0、44 个财务路由、16 groups passed、零测试残留和 Docker Compose 配置；Docker 六服务正常，近期严重日志为 0。
- 财务“结算管理 → 待付款”完成首轮原站只读对照。原站 `/5001007003/FAM/Settlement/PendingPaymentList` 的十三项筛选、十四列加占位、全局合计、审核/提交/回款八项上下文、15 列行内明细、10 条六档分页均已确认；原站 HTML 和 `FAM.AR.js` 进一步证明四个行操作、底部“标记已支付”与三类导出、空选“请选择回款.”、回退必须填写审核备注。原站当前无数据，全程未执行写操作。
- 本地待付款已从错误的通用费用源切换到待付款结算申请专用列表，新增专用回退/支付接口。回退进入已退回并恢复待结算候选，支付进入已付款；操作人、时间、备注和流程历史真实持久化，状态、权限、空批次、重复处理和通用接口绕过均有后端校验。`FINANCE_SETTLEMENT_PAYMENT_OK` 及第 16 组冒烟覆盖列表、筛选、三类导出、回退、标记支付、重复处理和零残留。
- 浏览器临时数据 `UIPAY-210641` 验证金额与原站空合计列、四个行图标、八项上下文、15 列明细、空选提示、回退必填校验、两种弹窗、全部导出入口、真实回退和真实支付；申请 `4154/4155` 及关联回款 `147`、应收 `157`、案件 `4153`、合同 `4152`、客户 `4151` 均已精确删除并复查为 404/0，控制台日志为 0。统一验收为 5471 modules、265/220/0、44 个财务路由、16 groups passed、零测试残留和 Docker Compose 配置通过；Docker 六服务正常，近期严重日志为 0。
- 财务“结算管理 → 已付款”完成首轮原站只读对照。原站 `/5001007004/FAM/Settlement/ProcessedList` 当前 5893 条；14 组筛选、14 列加占位、全局空合计列、审核/提交/付款/回款上下文、10 条六档分页、三枚行图标、三类底部导出和 15 列行内分配明细均已确认。官方 `FAM.AR.js` 证明已付款行只提供查看、导出、回退，不提供再次支付；原站没有执行任何写操作。
- 本地已付款改用结算申请专用数据，不再读取 `fee_type=结算费用` 的通用记录；新增 `paid_from/paid_to` 服务端筛选，并修复专用列表被旧前端二次过滤导致案件编号查询为空的问题。已付款可通过专用接口回退到已退回，流程历史准确记录 `已付款 → 已退回`，原回款重新成为待结算候选。`FINANCE_SETTLEMENT_PAID_OK` 与第 16 组冒烟覆盖日期边界、三类导出、重复支付、备注必填、真实回退和候选恢复。
- 浏览器临时申请 `4362` 完成案件编号/付款日期查询、合计、三行操作、付款上下文、15 列明细、空选提示、全部导出入口、回退弹窗与真实回退；申请、回款 `154`、应收 `164`、案件 `4361`、合同 `4360`、客户 `4359` 已精确删除，ID/关键词复查均为 404/0。最新统一验收为 5471 modules、265/220/0、44 个财务路由、`FINANCE_SETTLEMENT_PAID_OK`、16 groups passed、零测试残留和 Docker Compose 配置通过；六个容器正常，Web/API/PostgreSQL/Redis 健康且近期严重日志为 0。下一页继续“结算管理 → 已拒绝”，整体 220 个叶子页面仍未全部逐页证明。
- 财务“内部费用 → 待付款列表”已完成首轮只读对照：原站 `/5001004003/FAM/InternalFee/PaymentObjectList` 的九项筛选、固定待付款、勾选加 13 列、实际提成合计、15 条六档分页、打包付款提示和空选弹窗已复现；同一收款人打包进入“付款单打印”，恢复打包流水号/打印日期/总金额、逐笔明细/备注/小计及收款人/审批人/出纳签字栏。原站未执行提交写操作。
- 本地付款包已成为真实专用财务流程：预览无写入，创建会原子生成待核销付款包、将所选费用转为已付款并双向留痕；管理员撤销会恢复费用待付款状态。通用记录创建、编辑、流转、删除均不能伪造或破坏付款包。`FINANCE_INTERNAL_PAYMENT_OK` 与 API 冒烟覆盖空选、异收款人、非法状态、重复提交、专用撤销、通用接口绕过拦截和零残留清理。
- 浏览器临时记录 `2321/2322` 已完成待付款列表、合计、空选弹窗、全选、付款单预览、提交并打印、返回后移出列表的闭环；付款包 `P260715-21098393` 与两条费用已精确删除，复查均为零。完整验收为 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、零测试残留；Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，最近严重日志匹配为零。
- 财务“内部提成(退费)-待审核”完成首轮只读对照：原站 `/5001004014/FAM/InternalFee/PaymentAuditList` 的九项筛选、八个付款状态选项、勾选加十列业务表、空结果行为和默认 15 条分页已复现。新增专用退费候选 API，负数内部费用明确标记 `is_refund`，正数内部提成不会混入退费页，审核流程写入专用退费审批日志；`FINANCE_INTERNAL_REFUND_AUDIT_OK` 锁定结构与接口契约。
- 本地浏览器用正数/负数临时记录完成候选隔离、状态切换、行内审批面板、拒绝及默认待审批空状态回归；记录 `2144/2145` 已精确删除且关键词复查为零。15 组 API 冒烟包含负数候选、正数排除和退费驳回日志，结果通过并零残留。
- 本轮完整验收：5471 modules、265/220/0、44 个财务原布局路由、`FINANCE_INTERNAL_REFUND_AUDIT_OK`、15 groups passed、测试数据零残留；Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，近 10 分钟严重日志匹配为零。Docker 生产页九项筛选、默认待审批及生产控制台零错误已复验。
- 财务“内部提成-已作废”已完成首轮只读对照：原站 `/5001004013/FAM/InternalFee/PaymentList` 的九项筛选、默认已作废、12 列（含末尾空占位列）、金额合计、15 条分页以及行内“查看”/请款单号/案件编号入口已在本地复现。详情页恢复“申请付款”、四段流程、案件/合同/客户资料和八列付款信息；`FINANCE_INTERNAL_VOID_OK` 锁定该页结构。
- 本地临时记录完成“已作废列表 → 查看详情 → 返回列表 → 请款单号详情 → 案件入口”浏览器回归；记录 ID `2086` 已精确删除，关键词复查为零残留。原系统全程仅查看，未发生写操作。
- 本轮完整验收结果：5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、`FINANCE_INTERNAL_VOID_OK`、15 groups passed、测试数据零残留；Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 近 10 分钟严重日志匹配为零。Docker 生产页面九项筛选、默认已作废和生产控制台零错误均已复验。
- 财务“内部提成-已拒绝”已完成首轮只读对照：原站 `/5001004012/FAM/InternalFee/PaymentList` 的九项筛选、12 列（含末尾空占位列）、当前页金额合计、15 条分页、请款单详情、案件入口和“作废”行为均已在本地复现。新增 `/finance/fees/{fee_id}/void`，只允许有权限角色作废已拒绝内部费用，写入操作人、时间、备注和流程日志；`FINANCE_INTERNAL_REFUSED_OK` 与 API 冒烟锁定结构和状态边界。
- 本地临时记录已完成“已驳回 → 已拒绝页展示 → 作废确认 → 已作废页展示”浏览器闭环，详情中的状态、关联案件阶段和名称均已核对；两次临时验收记录均已删除，关键词复查为零残留。
- 页面滚动已拆分：根节点、`.app-shell` 与 `.app-body` 固定在视口内且不承担滚动；左侧由 `.sidebar > .ant-layout-sider-children` 独立滚动，右侧由 `.content` 独立滚动，顶部导航固定，两侧使用稳定滚动条槽避免出现滚动条时内容横移。浏览器在控制台长页面实测：左侧滚至 `272` 时右侧保持 `0`，随后右侧滚至 `300` 时左侧仍为 `272`，`window.scrollY` 始终为 `0`；菜单审计输出 `INDEPENDENT_SCROLL_OK`。
- 财务“内部费用-待审核”已完成首轮只读对照：原系统右侧 580px 审批滑板、无遮罩、审批意见与十列提成明细已在本地实现，并由 `FINANCE_INTERNAL_FEE_AUDIT_OK` 锁定关键尺寸和结构。
- 管理员最高权限已做成后端运行时强制、启动修复旧配置、前端忽略旧缩减会话三层保护。
- 控制台按原系统结构重排：230px 侧栏、管理员在线面板、绿色顶栏、4×2 指标卡、待办/趋势对半、开庭排期整行；1200/1366/1920 宽度验收。
- 侧栏多级菜单、深层路由父级自动展开、折叠布局、顶部搜索、消息和身份显示已接通。
- 客户、合同、案件、任务、调查、公证、证据、收发文、文件、用印、财务、人事、组织、仓库、报表、系统参数均已有真实 API 和主要闭环。
- 客户共享/导入/导出、合同审批/应收、用印草稿/提交/单 ZIP、案件任务与日志分流、官文上传与导出、调查专用导出已补齐。
- 系统用户、角色权限、安全策略已从错误占位页改为真实管理页。
- 消息支持发送、已读、删除；沟通日志支持管理员全所范围、筛选、编辑和删除。
- 平台财务不再使用空表占位；六类报表均有实时图表、明细、刷新和 CSV 导出。
- Docker 六个服务已在本机成功运行；服务器迁移底座见 `compose.prod.yml` 与 `deploy/`。

## 当前验收证据

- 财务“内部提成-待结算”：`FINANCE_INTERNAL_SETTLE_OK`；原站 9 项筛选、14 列、底部操作区和默认 15 条分页契约通过；专用待结算列表 API、标识提成已发 API、重复/非法状态拦截及流程日志均通过端到端测试。
- 待结算“更多操作”已按原站补齐上传案件文档、四类案件费用、四类案件批量修改、四类文书生成、案件任务和案件日志；菜单浏览器证据、结构审计和真实 API 冒烟均已通过。
- 本地浏览器已验证待结算页标题 41px、筛选区三行 26/26/34px、表格相对面板顶部 127px、完整表头和空选提示；控制台无错误。页面验收后未保留测试业务数据。
- 财务“内部提成-待归档”：`FINANCE_INTERNAL_ARCHIVE_OK`。原站只读证据确认真实标题“请款单审批”、三列九项筛选、固定待审批状态、勾选加十列业务表、行内/底部审批、默认 15 条分页，以及“提成审批”弹窗中的意见框、同意/拒绝、十列提成明细和取消按钮；原站筛选控件坐标为 42/68/94px，查询按钮 120px，表格 149px。
- 本地待归档页已补齐单条和批量审批/拒绝 API、状态与流程日志，浏览器使用临时记录验证勾选列、审批入口、15 条分页和完整弹窗后删除该记录；最终控制台 0 错误。最新完整验收为 5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 groups passed、测试数据零残留；Docker 六个服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 最近日志无严重错误。
- 财务“内部提成-待审核”：`FINANCE_INTERNAL_AUDIT_OK`。原站真实路由 `/5001004002/FAM/InternalFee/PaymentAuditList` 的只读证据确认标题“请款单审批”、九项筛选、勾选加十列业务表和默认 15 条分页；该入口的“案件阶段”输入框禁用，空结果时不显示批量审批区。原站控件坐标为 42/68/94px、查询 120px、表格 149px；本地已将表格上移 2px并移除空状态多余审批区。
- 本地待审核页使用临时记录验证了行内审批、批量审批入口、分页和完整“提成审批”弹窗，随后删除测试记录并确认零残留。最新完整验收继续为 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed，并新增待审核页独立结构门禁。
- 已修复管理员动态通知残留：任务、费用、合同或案件来源记录被删除后，对应过期通知会由同步逻辑清除；`smoke-api.py` 已把通知残留纳入清理失败条件，本轮复验为 0 条残留。
- 2026-07-15 待结算页修正后完整验收通过：5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 groups passed、测试数据零残留；Docker 六个服务均运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 最近日志未匹配严重错误。
- 前端生产构建：5471 modules，通过。
- 菜单审计：265 nodes、220 leaves、0 unhandled。
- 财务原布局路由：44 个配置路由，通过。
- 财务“内部费用 → 我的请款单”：`FINANCE_INTERNAL_MINE_OK`，三列九项筛选、11 列表头、真实查看详情和默认 15 条分页通过。
- API 端到端冒烟：15 groups passed，测试数据清理为零残留。
- Docker：API/Web/PostgreSQL/Redis 健康，Worker/MinIO运行。
- 最近运行日志未发现 Traceback、Exception、FATAL、panic 或 ERROR。
- 2026-07-15 恢复开发后再次执行完整验收：5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 groups passed、测试数据零残留，Docker 六个服务全部运行且 API/Web/PostgreSQL/Redis 健康。
- 品牌资金运营情况统计已完成一轮原站只读 DOM/尺寸对照：原站只有筛选、查询、按品牌页签和 4 个图表；本地已去除额外工具栏与明细表，并按原站调整筛选排布、24px 控件、灰色图表标题栏、328px 面板与 16/20px 间距。
- 报表中心其余 5 个入口已继续完成原站只读 DOM/尺寸对照：律师资金页 8 组筛选、按开庭律师页签和 4 个 328px 图表一致；退费页 4 个 348px 图表一致；执行进度 1/2/3 原站均展示相同的完整 10 个 348px 图表，本地前后端已由原来的 4/4/2 修正为 10/10/10。
- `scripts/audit-menu-coverage.py` 新增 `REPORT_LAYOUT_OK` 门禁；`scripts/smoke-api.py` 已将执行三个 API 视图的期望图表数提升为 10。本轮完整验收再次通过 15 组流程且测试数据零残留。
- 管理员深层报表页刷新已在浏览器验证：首屏 `hasForbidden=false`、真实报表内容存在、动态菜单加载后 6 个报表子项完整显示。
- 财务“内部费用 → 我的请款单”已按原站页面完成只读 DOM/尺寸取证与本地浏览器回归：标题 41px，三行筛选控件相对页面顶部为 42/68/94px，查询按钮 124px，表格 181px；控件宽度、费用类型文本框和 11 个表头一致。
- 本地临时内部费用记录已验证列表只显示“查看”、详情弹窗、费用类型过滤和六档分页；临时记录已删除。浏览器控制台 0 错误，完整验收再次为 5471 modules、265/220/0、44 个财务路由、15 groups passed、零测试残留。
- Docker 六个服务均在运行，API/Web/PostgreSQL/Redis 健康；最近 10 分钟 API/Web/Worker 日志未匹配 `ERROR`、`CRITICAL`、`Traceback`、`Unhandled` 或 `panic`。
- 财务“内部费用 → 待核销列表”已完成首轮原系统只读对照及本地闭环。原站真实入口 `/5001004004/FAM/InternalFee/PackingList` 的五项筛选、八列表头、15 条和六档分页、金额合计、“核销”入口及 600px“付款核销”弹窗已复现；付款包号码复用真实“付款单打印”详情。
- 后端新增 `/finance/payment-packages/{package_id}/writeoff` 专用接口：严格校验角色、待核销状态、确认金额、三种付款方式、非空单据号和费用关联完整性；付款包写入已付款/已核销信息，关联费用写入核销凭证并分别生成“付款核销/付款包核销”历史。普通删除不能撤销已核销付款包，管理员必须显式 `reverse_paid=true` 冲正并留下“冲正已核销付款包”记录。
- 浏览器真实验收临时付款包 `P260715-29286234`：待核销页显示 `100` 金额和 `100.00` 合计，付款单展示两笔 `61.25/38.75` 明细，空单据号显示原站校验文案，真实核销后待核销页移除且已核销页显示 `BROWSER-VOUCHER-260715` 和核销备注。验收发现已核销页仍有重复核销入口，已修正为只读“查看”。临时费用 `2443/2444`、付款包 `2445` 已精确清理并复查不存在。
- 本轮结构门禁新增 `FINANCE_INTERNAL_WRITEOFF_OK`；统一验收通过：5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、测试数据零残留、Docker Compose 配置通过。整体 220 个叶子页仍未全部逐页人工对照，不能宣称完整复刻完成。
- 财务“内部费用 → 付款单查询”已完成首轮原系统只读对照及本地浏览器闭环。原站 `/5001004005/FAM/InternalFee/PaymentList` 的九项筛选、12 列（含末尾空占位列）、申请金额合计、默认 15 条和六档分页已复现；行内“查看”和请款单号进入“申请付款”详情，案件编号进入真实案件页。
- 查询详情复用只读付款详情结构，已按原站恢复四段付款流程、案件/合同/客户资料、四类已付提成及八列付款信息；浏览器发现并修正待审批记录显示伪审核日期、详情金额带 `¥` 前缀、查询页使用多余通用操作按钮等差异。临时记录 `2505` 的 `64.30` 合计、三个入口和详情格式均已复验，随后精确删除并确认 404。
- 结构审计新增 `FINANCE_INTERNAL_QUERY_OK`；本轮统一验收通过 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、测试数据零残留及 Docker Compose 配置。下一页继续核对“内部费用 → 已核销列表”；整体目标仍未完成。
- 页面框架已改为固定视口：顶部栏固定，左侧导航和右侧内容分别滚动。自动门禁 `INDEPENDENT_SCROLL_OK` 锁定外壳高度、左右 `overflow` 容器及滚动链隔离；1280×720 浏览器实测左侧 `180` / 右侧 `0`，再滚动右侧后为左侧 `180` / 右侧 `320`，确认互不带动。统一验收通过 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、测试数据零残留和 Docker Compose 配置；六个容器运行正常且近期严重日志为 0。
- 财务“内部费用 → 已核销列表”已完成首轮原系统只读对照及本地浏览器闭环。原站 `/5001004006/FAM/InternalFee/PackingList` 标题为“付款单-查询”，默认付款包状态为可选“请选择”，因此列表应展示全部付款包状态而非只显示已付款；五项筛选、八列、表头下全局金额合计、页尾当前页合计、默认 15 条及六档分页已复现。
- 浏览器以临时费用/付款包 `2624–2627` 验证待核销与已付款同时显示、全局/当前页总额 `175.00`、已付款筛选后两处均为 `75.00`，并验证“查看”和付款包号码都进入付款单打印页。四条临时记录已精确撤销/冲正删除并确认 404。新增 `FINANCE_INTERNAL_DONE_OK` 及核销后列表 API 断言；统一验收通过 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、零残留和 Docker Compose 配置，六个容器近期严重日志为 0。下一页继续核对“内部费用 → 内部费用明细”；整体目标仍未完成。
- 财务“内部费用 → 内部费用明细/内部费用明细(公司)”已完成首轮原系统只读对照及本地浏览器闭环。原站 `/5001004007/FAM/InternalFee/InternalFeeList` 与 `/5001004008/FAM/InternalFee/InternalFeeList` 的十二项筛选、多选树、个人收款人只读规则、14 列加末尾占位、上下金额合计、15 条六档分页、案件入口和 Excel 导出均已复现。
- 后端新增 `/finance/internal-fees` 与 `/finance/internal-fees/export`，个人/公司数据范围、十二项过滤、付款状态与时间、服务端分页、金额字段权限及真实 `.xls` 均有自动测试。浏览器临时数据 `2687–2689`/流水 `364` 验证个人 `120.00`、公司 `200.00`、未付 `80.00`、清空保留个人收款人、案件跳转和选中导出；现已全部清理并确认业务记录 404。
- `FINANCE_INTERNAL_DETAIL_OK` 与 `INDEPENDENT_SCROLL_OK` 均通过；左侧菜单和右侧内容保持独立纵向滚动。最新统一验收：5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 groups passed、测试数据零残留、Docker Compose 配置通过。整体目标仍未完成，下一轮继续下一个未逐页验证的财务入口。
- 2026-07-15 已完成财务“归档费结算 → 待归档”首轮原站只读对照和本地浏览器闭环。原站 `/5001008001/FAM/ArchiveSettlement/PendingList` 的十一项筛选、15 列含勾选/末尾占位、回款与归档费全局合计、10 条六档分页、行内“新建任务”/案号入口和页尾“导出选中”均已复现；原站未执行写操作。
- 后端新增待归档专用查询/Excel 导出，不再从通用费用列表猜测数据；已付款结算申请按分配明细生成归档费行，管理员全所范围、字段权限、十一项过滤、分页、合计、日期边界和空选导出均有自动断言。浏览器临时链路 `UIAF-123487` 验证 `200.00/16.00` 合计、查询、六个案件类型、空选提示、勾选导出及任务/案件跳转；申请 `4638`、到账 `164`、应收 `174`、业务记录 `4635–4637` 已精确清理并复查为 0/404。新增 `FINANCE_ARCHIVE_PENDING_OK`，API 冒烟为 16 groups passed 且零残留。下一页继续“归档费结算 → 待支付”，整体目标仍未完成。
- 2026-07-15 已完成财务“归档费结算 → 待支付”首轮原站只读对照与本地真实审核闭环。原站 `/5001008002/FAM/ArchiveSettlement/ProcessingList` 的 12 项筛选、15 列、两项合计、归档审核四块展开上下文、三枚行内按钮、三项页尾按钮和同意/拒绝弹窗均已确认；原站全程未提交写操作。
- 本地新增待支付专用列表/支付审核/Excel 接口，案件归档审核通过后归档费从待归档移入待支付；同意/拒绝按明细原子落地，拒绝备注必填，重复处理、越权和通用记录接口绕过均拦截，源申请删除时级联清理决定。浏览器临时链路 `UIAP-116999` 验证 `200.00/16.00`、精确列宽、日期格式、筛选、导出、任务/案件入口、两个弹窗、空备注及真实同意支付；决定 `4778` 与业务链 `4774–4777`、到账 `169`、应收 `179` 已精确清理并复查 404/0。新增 `FINANCE_ARCHIVE_PAYMENT_OK`，16 组 API 冒烟及零残留通过。下一页继续“归档费结算 → 已支付”，整体目标仍未完成。
- 最新统一验收为 5471 modules、265/220/0、44 个财务原布局路由、16 groups passed、测试数据零残留和 Docker Compose 配置通过；Docker 六个服务正常，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 近期严重日志为 0。
- 2026-07-15 已完成财务“归档费结算 → 已支付”首轮原站只读对照。原站 `/5001008003/FAM/ArchiveSettlement/ProcessedList` 的 13 项筛选、15 列、两项合计、三层归档/支付展开上下文、行内与批量回滚、选中导出及“回滚支付”弹窗均已确认；只打开回滚弹窗，未在原系统提交写操作。
- 本地新增已支付专用列表/Excel/回滚 API 和页面。自动验收覆盖已支付查询及日期边界、真实 Excel、空批次/空备注/错误记录拦截、回滚历史、恢复待支付、再次同意并复用原决定 ID，以及删除源结算申请后的决定级联清理。浏览器确认本地深层路由、13 项筛选、15 列和空状态布局。新增 `FINANCE_ARCHIVE_PAID_OK`，16 组 API 冒烟与测试数据零残留通过。
- 最新统一验收为 5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、`FINANCE_ARCHIVE_PENDING_OK`、`FINANCE_ARCHIVE_PAYMENT_OK`、`FINANCE_ARCHIVE_PAID_OK`、16 groups passed、测试数据零残留；Docker API/Web/Worker 已重建。下一页继续“归档费结算 → 已拒绝”，整体复刻目标仍未完成。
- 2026-07-16 已完成财务“归档费结算 → 已拒绝”首轮原站只读对照与本地闭环。原站 `/5001008004/FAM/ArchiveSettlement/RejectedList` 的 12 项筛选、勾选/操作/14 个业务列及占位、三层归档/支付上下文、行内回滚、页尾重新申请与选中导出均已确认；官方脚本补充证明回滚备注必填、重新申请备注可空及两类空选提示，原站全程未提交写操作。
- 本地新增已拒绝专用查询/Excel/回滚/重新申请接口及精确页面布局。API 自动链路证明同一归档费决定可依次经历“已支付 → 已回滚 → 已拒绝 → 回滚为已支付 → 再次拒绝 → 重新申请为待支付 → 再支付”，并补上源结算生命周期拦截：归档费为已支付或已拒绝时不能通过通用结算接口直接回退。错误状态、空批次、重复编号、空回滚备注、日期倒置和无效导出均被拦截，最终测试数据零残留。
- 浏览器同屏核对本地与原站空状态，12 项筛选、查询位置、表头宽度、横向滚动和父菜单展开一致；左侧菜单容器 `clientHeight=900/scrollHeight=1338/overflow=auto`，右侧内容容器独立为 `overflow=auto`。新增 `FINANCE_ARCHIVE_REJECTED_OK`；最新统一验收为 5471 modules、265/220/0、44 个财务原布局路由、16 groups passed、零残留。API/Web/Worker 新镜像已重建，六容器运行正常，API/Web/PostgreSQL/Redis 健康；Worker 仅保留既有 root 用户安全警告，无启动失败。下一页继续“财务中心 → 费用查询”，整体目标仍未完成。

- 2026-07-16 已完成事务中心“我的任务 → 我发起的任务”首轮原站只读对照和本地闭环。原站 `/9001001010/TP/Task/TaskList` 已确认 6 页签、12 筛选、14 个业务字段、15 条六档分页、11 项更多操作及“案件任务”四阶段详情；本地使用专用 `/tasks` 查询、关系范围、分页和状态计数，不再依赖前端全量过滤。
- 任务生命周期已修正为“处理中 → 已完成待验收 → 已验收”，兼容旧“待确认”；逾期展示与真实流程状态分离，避免逾期任务失去接收、拒绝、完成或交接按钮。验收日期返回 `verified_at`，发起人批量修改权限、真实负责人校验、终态保护和通用记录防绕过均已补齐；创建、交接、接收、拒绝、完成、验收、重派及沟通增加重复提交保护。
- `TASK_MY_CREATED_OK`、完整 `verify-local.ps1` 均通过：5471 modules、265/220/0、17 groups passed、零测试残留和 Docker Compose 配置通过。API/Web/Worker 已按最新代码重建，六服务运行，API/Web/PostgreSQL/Redis 健康且近期严重日志为 0；Docker 生产任务页和 11 项更多操作已浏览器复验，控制台错误为 0。下一页继续“我接受的任务”，整体目标仍未完成。
- “我接受的任务”原系统只读取证完成：4 个页签、12 项筛选、14 列、四个排序字段、默认 15 条及 6 种页容量、固定“接受任务”、11 项更多操作、四阶段详情和逐条标记未读均已确认；完成页可见其他负责人的任务，证明管理员不得被 `owned` 关系缩权。原系统未执行任何写操作。
- 本地已对齐接受页的服务端排序、分页与状态计数、整格标题/终态颜色、日期及 `N/天` 格式；管理员保留全所数据。历史标记未读为幂等真实通知，自动逾期提醒在任务进入终态后清除而手工 `task-history-*` 未读保留；CSV、负责人/协作人、30 天期限、交接重启、5 天自动完成、终态流转与专用流程防绕过均有 API 断言。
- `TASK_MY_ACCEPTED_OK` 和完整验收通过：5471 modules、265/220/0、17 groups passed、测试数据零残留及 Docker Compose 配置通过。API/Web/Worker 已重建，本地生产接受页已实看 4 页签、12 筛选、14 列、真实数据、11 项菜单和详情；整体目标仍未完成，下一页为“我协作的任务”。
- “我协作的任务”原站 `/9001001030/TP/Task/TaskList` 只读取证完成：精确两个页签“进行中/完成”、12 项筛选、可勾选 14 列、四个排序字段、空结果隐藏表尾分页和操作区；页面与其他任务页共用 `Legal.Task.js` 和 `Legal.Task.List.Invoke.js`。原系统未执行任何业务写操作。
- 本地已修复管理员可见/选中双源、备用菜单深链和纯协作人错误入口；纯协作人数据隔离、分页/稳定排序/状态计数、沟通/历史/标记未读正向权限，以及接收/拒绝/重启/完成/确认/转交/批量修改七类 403 均有冒烟证据。管理员在 initiated/collaborating 关系下仍保持全所数据。
- 原站“更多操作”11 个顶层项及二级结构已按 HTML 证据对齐：律所/平台费用各含官费、第三方费用、代理费、其他费用；案件批量修改含开庭律师、经办律师、律师助理、案件阶段。本地费用归属/子类型真实持久化，案件支持按案号原子批量修改，旧的错误任务批量入口已移除。`TASK_MY_COLLABORATING_OK`、完整构建、17 组冒烟和生产页面实看均通过。
- 明日交付候选版新增 `verify-delivery.ps1` 和生产 `preflight-production.sh`。生产启动拒绝占位 `SECRET_KEY`、默认管理员密码和演示业务数据；空库管理员使用私有一次性强密码。迁移包显式排除环境文件/私钥/旧备份并扫描压缩内容；生产备份先停写、禁止落盘插值秘密、全部产物 SHA256 校验；Caddy HTTPS 模板及回滚说明已加入。完整复刻目标仍未完成，下一页为“未读新消息的任务”。
- 2026-07-16 已生成 2026-07-17 明日交付候选版脱敏源码 ZIP。`scripts/export-delivery-source.ps1` 仅收录源码、文档、部署配置、锁文件和两个安全环境示例；真实 `.env`、业务数据/附件、数据库导出、Docker 镜像、备份、日志和私钥均排除。目录与 ZIP 敏感路径扫描均为 0，包内 100 个被交付文件的 SHA256 清单错配为 0；业务数据迁移继续使用独立迁移脚本并通过加密通道传输。
- “未读新消息的任务”已完成原站 `/9001001040/TP/Task/TaskList` 只读取证。原页标题“任务列表”且无页签，具有 12 项筛选、勾选列加 14 个通用列、默认 15 条与六档分页、11 项更多操作；`NewestUnReadMessage`/发送人/时间投影到状态/优先级/标题，后六格留空。0 条时精确显示“没有查询到符合条件的记录 。”并隐藏表尾分页/操作区；原站未执行写操作。
- 本地已改为专用 `GET /tasks/unread-messages` 和 `POST /tasks/{task_id}/messages/read`，返回真实未读消息/发送人/时间/条数，严格按接收人隔离且管理员不扩大个人未读范围；仅纳入 `task-message-*`/`task-history-*`，到期提醒不会混入。任务全生命周期和两类系统自动完成都生成幂等接收人消息；越权查询/已读被拦截。浏览器发现并修复了顶部铃铛徽标跨组件延迟：读取任务消息或重新标记未读后派发 `sunhold:notifications-updated`，`NotificationCenter` 立即重载未读数，并在卸载时正确移除监听。`TASK_MY_UNREAD_OK`、5471 modules、265/220/0、17 groups passed 和零残留已通过。本地页键 `task-my-unread` 可深链并自动展开“事务中心/我的任务”，菜单切换、前进和后退与 `?page=` 同步。整体复刻仍未完成，下一页为“部门任务 → 部门发起的任务”。
- 2026-07-16 封版复验：使用隔离接收人账号在 Docker 生产页面验证未读角标为 `2`、列表显示真实最新消息；点击任务后列表立即变为空、任务详情正常打开且顶部角标立即归零。临时任务、两个临时用户及关联通知已精确删除，复查 `TEST_USERS/TEST_RECORDS/TEST_NOTICES/ORPHAN_TASK_NOTICES` 均为 `0`。完整 `verify-delivery.ps1` 退出码为 `0`：5471 modules、265 nodes / 220 leaves / 0 unhandled、17 groups passed、六服务与健康端点、生产安全检查、交付/备份/恢复产物及近期严重日志全部通过。当前只能交付本地 Docker 演示/UAT 候选版，不能宣称 220 个叶子页面已全部逐页复刻完成。
- “部门任务 → 部门发起的任务”已完成原站 `/9001002010/TP/Task/TaskList` 只读取证。原页具有 6 个状态页签、12 项筛选、勾选列加 14 个业务列、4 个排序字段、默认 15 条及六档分页、11 项更多操作；隐藏条件 `SearchCondition_Initiator_DepartmentId=6` 证明按发起部门限定。原页非空时底部只有“更多操作”，没有个人“新增任务”；0 条时精确显示“没有查询到符合条件的记录 。”并隐藏分页和底部操作。原站筛选已恢复，全程未执行业务写操作。
- 本地修复部门范围被个人关系二次缩窄的问题：部门负责人查询发起任务按创建时任务部门、接受任务按负责人部门、协作任务按任一协作人部门；普通用户访问部门范围返回 403，`admin` 保持全所最高权限。前端部门/公司页不再再次按当前个人过滤，部门发起页不再显示个人新增/重新派发/确认/重启控制，空结果隐藏表尾。Docker 生产页已实看 6 页签、12 筛选、14 列、2 条非空数据、只有更多操作；空值筛选时精确空文案且无表尾，恢复查询后控制台错误为 0。`TASK_DEPT_CREATED_OK`、5471 modules、Python 编译、265/220/0、17 groups passed 与零残留均通过。下一页为“部门任务 → 部门接受的任务”，整体复刻目标仍未完成。
- “部门任务 → 部门接受的任务”已完成本轮原站只读取证与本地成品验收。原站页键 `/9001002020`，隐藏条件为负责人部门 `SearchCondition_Officer_DepartmentId=6`；确认四状态、12 筛选、14 列、4 个排序列、六档分页、11 项更多操作，以及有数据时不出现个人接受/拒绝/完成/交接、空结果精确文案和底部隐藏。原站筛选已恢复且无业务写入。本地同步移除部门查看页个人生命周期按钮并匹配空态；另修复同部门第二经理借调查分配、批量修改或批量删除夺取/删除他人任务的越权旁路，新增反越权冒烟断言。`TASK_DEPT_ACCEPTED_OK`、5471 modules、265/220/0、17 groups、零测试残留、六服务健康及近期日志检查全部通过。下一页为“部门任务 → 部门协作的任务”，整体复刻目标仍未完成。
- “部门任务 → 部门协作的任务”已完成原站只读取证、本地成品和后端权限验收。原站页键 `/9001002030`，参与人部门隐藏条件 `SearchCondition_Participant_DepartmentId=6`；确认进行中/完成两页签、12 筛选、14 列、4 排序列、六档分页、11 项更多操作及精确空态，临时筛选已恢复，无业务写入。本地非空/空态与原站一致且控制台错误为 0；跨部门与纯协作者冒烟锁定“部门列表可读不等于可写”，旁观经理/纯协作者无法通过生命周期、批量、调查或通用接口改负责人、状态或删除。`TASK_DEPT_COLLABORATING_OK`、5471 modules、265/220/0、17 groups、零残留、六服务健康及近期日志检查全部通过。下一页为“公司任务 → 公司发起的任务”，整体复刻目标仍未完成。
- “公司任务 → 公司发起的任务”已完成原站只读取证、本地修正和权限验收。原站准确页键 `/9001003010`，发起人公司条件 `SearchCondition_Initiator_CompanyId=1`；六页签、12 筛选、14 列、4 排序列、六档分页和 11 项更多操作已取证。关键差异为原站所有非空页签固定显示“完成任务、转交任务、更多操作”，本地已补齐管理员两项真实生命周期入口及无选择提示；空态隐藏底部。公司范围仅当前数据库角色为 `admin` 可访问，跨部门任务和旧令牌升降级均有自动断言。Docker 成品非空/空态、恢复状态和零控制台错误已实测；`TASK_COMPANY_CREATED_OK`、5471 modules、265/220/0、17 groups、零残留、六服务和近期日志完整门禁通过。下一页为“公司任务 → 公司接受的任务”，整体复刻目标仍未完成。
- “公司任务 → 公司接受的任务”已完成原站只读取证与本地范围/流程验收。原站准确地址 `/9001003020/TP/Task/TaskList`、`PageId=9001003020`；待处理/进行中/完成/停止四页签均为 0，确认 12 筛选、勾选加 14 列、4 排序字段、默认 15 条与六档分页。空态原文为“没有查询到符合条件的记录 。”且整个底栏隐藏；DOM 只定义隐藏的“更多操作”，11 项依次为新增律所费用/平台费用/内部费用、批量修改、生成授权委托书/律所函/身份证明/结算提成表、案件任务、案件日志、导出案件打印表，未定义接受/拒绝/完成/转交生命周期按钮。原站未执行写操作。
- 原页全部 `Initiator/Officer/Participant` company/department 隐藏值为空，本轮不得声称原站 `CompanyId=1`。本地 `company + owned` 只允许当前数据库角色为 `admin` 的账号读取；晋升/降级后旧 JWT 无需重新签发即可立即按当前角色收放权限。管理员任务链已自动证明“拒绝 → 重派 → 交接 → 接收 → 完成 → 验收”全流程及非法状态 409；前端零结果隐藏分页/底栏，非空保留 11 项更多操作且不暴露原站不存在的生命周期按钮。下一页为“公司任务 → 公司协作的任务”，整体复刻目标仍未完成。
- “公司任务 → 公司协作的任务”已完成原站只读取证与本地前端/权限验收。原站准确地址 `/9001003030/TP/Task/TaskList`、`PageId=9001003030`；进行中/完成计数为 187/11215，确认 12 筛选、勾选加 14 业务列、4 排序字段、默认 15 条及六档分页，当前非空页脚总计显示 11670 条。底部只有含 11 个共享入口的“更多操作”，没有个人生命周期按钮；空态原文“没有查询到符合条件的记录 。”且隐藏分页和整个底栏。临时筛选已恢复，原站无业务写入。
- 原页全部 `Initiator/Officer/Participant` company/department 隐藏字段为空，禁止推断原站 `CompanyId`。本地 `company + collaborating` 只允许当前 `admin`，账号升降级后旧 JWT 权限即时变化；纯协作者只可沟通/查看历史，任务、通用记录和调查接口均不能绕过写权限。管理员自动链路通过“接收 → 交接 → 再接收 → 完成 → 验收”，非法状态返回 409。本地 Docker 页实看两页签 2/0、12 筛选、含勾选的 15 列、仅更多操作、精确空态和底栏隐藏，页面日志为 0；门禁 `TASK_COMPANY_COLLABORATING_OK`。原系统事务中心侧栏任务分组至此已核对完毕，整体复刻目标仍未完成。
- 原系统事务中心侧栏只有“我的任务、部门任务、公司任务”三个分组，共 10 个可见任务叶子，没有独立可见的“任务提醒”。本地已从静态备用菜单和动态菜单配置中移除 `task-reminders`，但为通知中心保留内部兼容路由；该路由请求 `/tasks` 时强制携带 `reminder_only=true`，空结果不显示表尾，不会把全部任务误当提醒。
- 后端提醒查询严格按当前接收人隔离，管理员也不扩大个人提醒数据；过滤条件、服务端分页和权限边界均已验证。当前菜单审计目标为 264 个节点、219 个叶子、0 个未处理路由；`smoke-api.py` 为 17 组通过且测试数据零残留。
- “客户管理 → 新建客户”已完成原站只读取证、本地实现和本轮验收。准确 URL 为 `/6001000/CRM/Customer/CreateUpdate`；本地恢复基本信息、法人信息、开票信息、控制信息四个可见区块，保持原 DOM 中“专利必填信息/附加信息”默认 0 高度隐藏，不新增原页不存在的所属部门字段。客户名称必填；客户编号初始为空并显示“自动生成”，保存后由服务端生成。控制信息默认建档日期为当天、客户来源“管理者”、是否共享“否”、客户等级“立案客户”、上海市资助信息“否”，客户管理人来自真实用户目录且至少一人。
- 三个标签的精确空状态为：“没有查询到联系人，可以去 新建联系人”、“没有查询到事项记录，可以去 新建”、“没有查询到客户文件，可以去 上传客户文件”；对应空表分别锁定联系人 12 列、事项 5 列、文档 6 列的原表头顺序。客户未保存时点击关联新增入口显示“提示 / 请先保存客户基本资料. / 确定”。保存接入专用 `POST /customers`，后端强制必填、真实管理人、数据权限与自动编号，通用记录接口不能绕过客户专用创建/编辑/流转/删除规则。
- 最新验证通过前端 5471 modules、264 nodes / 219 leaves / 0 unhandled、17 组 API 冒烟及测试数据零残留；原站没有执行任何写操作。本页可以标记为本轮已实现并验证，但整站仍未完成。下一页为“客户管理 → 我的客户” `/6001001/CRM/Customer/CustomerList`。
- “客户管理 → 我的客户”原站列表与查看页已完成只读取证。准确列表地址 `/6001001/CRM/Customer/CustomerList`；确认三项筛选、勾选加 13 个业务列、上下金额合计、默认 15 条及六档分页，底部仅“客户查看”和含客户删除/客户编辑/新增合同的“更多操作”。现有客户查看打开 `/6001001/CRM/Customer/View?customerId=1729`，为基本信息、法人信息、开票信息、控制信息及联系人/事项记录/客户文档三个标签组成的只读页，没有新增、上传或保存入口；原站未执行写操作。
- 本地新增专用 `GET /customers?scope=mine`：普通用户按本人案源人或管理人精确匹配，`admin` 保留全所在管客户和全部字段的最高权限，均排除已回收与公海客户；接口完成客户/当事人、名称和管理人筛选、分页及金额合计。前端已复现原列表、空态、上下合计、底部操作、六档分页和只读客户查看页。Docker 成品浏览器已复验四区块、“请选择”状态、开票地址 `test`、三个标签各自表头和空态、无写入口、关闭返回及控制台零错误；`CUSTOMER_MINE_OK`、5471 modules、264/219/0、17 组 API 冒烟及零测试残留均通过。本页本轮范围已实现并验证，但整站仍未完成。
- “客户管理 → 个人回收站”已完成原站 `/6001002/CRM/Customer/CustomerList` 只读取证和本地闭环。原页使用同款三筛选、勾选加 13 列、上下合计、15 条六档分页并额外显示末页 `»`；非空底栏只有“客户查看”和含“客户恢复/进入公海”的更多操作，空结果精确隐藏底栏和分页，记录状态文案为“已删除”。本地新增 `/customers?scope=recycle` 专用范围/筛选/分页/合计，普通用户精确按本人关系、`admin` 仍为全所最高权限；恢复及从回收站直接进入公海均使用受保护的真实专用流程。浏览器临时客户 `9385` 已完成只读查看、真实恢复、再次回收和真实进入公海，并精确删除确认 404/关键词 `0`；清理后空页无底栏且控制台零错误。`CUSTOMER_RECYCLE_OK`、5471 modules、264/219/0、17 组冒烟和零残留通过。下一页为“部门客户”，整站尚未完成。
- “客户管理 → 部门客户”原站 `/6001003/CRM/Customer/CustomerList` 已完成只读取证：三筛选中客户管理人可编辑，列表为勾选加 13 业务列、上下金额合计、15 条默认及六档分页；非空底栏为“客户查看”和仅含“分配客户”的更多操作。原站分配弹窗精确为“客户分配 / 客户编码 / 客户名称 / 原客戶管理人 / 现客戶管理人 / 确定 / 取消”，现管理人为单值；空结果不显示金额合计、底栏或分页。临时筛选已清除，原站没有提交分配或其他写操作。
- 本地部门页已切换到 `/customers?scope=department` 服务端范围/筛选/分页/合计：普通用户拒绝，部门负责人只看本部门在管客户且隔离跨部门第二管理人，`admin` 保留全所最高权限。前端已复现末页 `»`、空态、共用只读客户查看和单选分配弹窗；分配调用受保护的真实管理人接口，部门负责人真实分配后已恢复且零残留。Docker 成品浏览器复验非空、四字段弹窗、四区块只读查看和无合计/底栏/分页空态；`CUSTOMER_DEPT_OK`、5471 modules、264/219/0、17 组冒烟、`VERIFY_LOCAL_OK` 均通过。下一页为“部门回收站”，整站仍未完成。

## 仍未证明完成的范围

- 220 个叶子页面尚未全部与原系统逐页完成视觉像素级和每个弹窗的人工对照。
- 微信小程序页面尚未开发；当前仅预留可复用 REST API。
- Dify 尚未配置真实地址和密钥；未配置时仅生成可编辑字段提纲。
- “我的客户 → 客户删除”生产 UI 已使用专用回收接口并能进入个人/部门/公司回收站；但管理员通用 `DELETE /records/{id}` 对 customer 的物理删除仍为测试清理所依赖，尚未隔离成仅测试环境可用的能力。生产发布前必须增加环境门禁或迁移到测试专用清理接口。
- 生产模板、强密钥/一次性管理员门禁和 HTTPS 反向代理模板已具备；真实域名/DNS、真实秘密、Caddy 证书签发、Alembic 迁移和目标服务器恢复演练仍未完成。
- 因此不得将整个目标标记为“完全完成”，新电脑应继续逐页验收和修正。

## 关键文件

- `AGENTS.md`：Codex 工作纪律。
- `README.md`：启动与总体说明。
- `docs/功能实现清单.md`：详细功能清单。
- `scripts/audit-menu-coverage.py`：菜单/页面覆盖审计。
- `scripts/smoke-api.py`：17 组端到端接口验收。
- `scripts/verify-local.ps1`：本地统一验收。
- `scripts/verify-delivery.ps1`：明日交付候选版统一门禁。
- `docker-compose.yml`：本地 Docker。
- `compose.prod.yml`：服务器部署模板。
- `deploy/`：备份、恢复与服务器部署说明。
- `deploy/preflight-production.sh`：生产秘密、权限和 Compose 启动前门禁。
- `deploy/Caddyfile.example`：公网 HTTPS 反向代理模板。

## 原系统只读参照

- 原系统地址：`http://sh.021ipr.com/Console/Index`。
- 凭据不写入迁移包；由项目所有者在浏览器中登录。
- 只能查看页面结构、字段、按钮和布局，禁止在原系统执行任何写操作。
- 2026-07-15 用户再次要求左右分开滑动，已在 1280×720 的 5173 开发页复验：右侧 `180→360` 时左侧保持 `240`，左侧继续滚动到可达上限 `272` 时右侧保持 `360`，证明左右滚动容器互不带动；5173 进程指向当前仓库的 Vite，Docker Web/API 均健康。统一验收通过 5471 modules、265/220/0、`INDEPENDENT_SCROLL_OK`、`FINANCE_INVOICE_MINE_OK`、15 groups passed、测试数据零残留及 Docker Compose 配置。
- 财务“开票管理 → 我的开票”本轮已补齐专用列表/导出/撤回 API、原布局筛选和表格、有效状态金额合计、查看发票申请单详情、取消状态删除线及真实 `.xls` 导出。浏览器临时记录 `2958/2959/2960` 已全部清理并确认 404。下一页继续“待处理开票”；220 个叶子页面尚未全部逐页人工对照，整体目标不得标记完成。
- 2026-07-15 已完成财务“开票管理 → 待处理开票”首轮原站只读对照。本次证据来自 `/5001006002/FAM/InvoiceProcess/PendingList` 和 `/5001006002/FAM/InvoiceProcess/Process?invoiceId=6154`：九项筛选、八列加占位、上下金额合计、六档 15 条分页、两种 Excel 导出，以及包含开票结果/领票人/高开金额/发票号/开票意见、发票内容、申请信息、服务项、合同信息和“开票/驳回”的完整处理页均已确认。原站未执行任何写操作。
- 本地已将原先混入“待审批”的通用列表改为只返回“待开票”的专用服务端分页；新增申请人显示名筛选、待处理 Excel、完整处理页和 `/finance/invoices/{id}/reject-issue`。浏览器完成空值提示、真实驳回、真实开票及选中导出；临时记录 `3083/3084` 和关联流水已全部清理。`FINANCE_INVOICE_PENDING_OK`、5471 modules、265/220/0、44 路由、15 groups、零测试残留、Docker Compose 均通过，六容器正常且近期严重日志为 0。下一页继续“公司开票”；220 个叶子页面仍未全部逐页证明，整体目标保持未完成。
- 2026-07-15 已完成财务“开票管理 → 公司开票”首轮原站只读对照。原站 `/5001006003/FAM/InvoiceProcess/InvoicedList` 的九项筛选、勾选列、十一列加末尾占位、上下开票/高开合计、15 条六档分页、导出全部/选中及“查看/作废/修改发票号/修改发票日期”四个行入口均已确认；`InvoiceNoChange`、`InvoiceDateChange` 和 `InvoiceCancel` 的字段、按钮与空值文案通过页面和静态脚本只读取证，原站未执行写操作。
- 本地新增公司开票专用查询/分页/Excel 数据路径，完整作废页面，发票号和申请/开票日期修改接口及流程历史。浏览器临时记录 `3208` 验证四个入口、查看详情、号码/日期真实修改、空值提示、作废页和真实作废，作废后只保留“查看”；临时记录 `3207/3208`、关键词和关联流水已复查为 404/0。结构门禁新增 `FINANCE_INVOICE_COMPANY_OK`，API 冒烟补齐公司范围、导出、重复号、流水同步、历史和作废边界。下一页继续“开票管理 → 未开票”；220 个叶子页尚未全部逐页验收，整体目标保持未完成。
- 2026-07-15 已完成财务“开票管理 → 未开票”首轮原站只读对照。原站 `/5001006004/FAM/CaseFee/InvoceCaseFeeList` 的十五项筛选、费用类型树、十八列加末尾占位、四项上下合计、15 条六档分页、两种导出及十项更多操作均已确认；空选导出提示为“请选择需要导出的费用.”，其他空选操作提示为“请选择案件.”，原站未执行写操作。
- 本地新增案件费用开票状态专用查询/导出接口和发票 `case_fee_ids` 显式关联，真实联结案件、发票、回款分配与付款流水，支持个人/公司范围、服务端分页、四项合计和 `.xls`。浏览器临时记录 `3483–3487` 验证未开票 `5000.00`、已开票 `6000.00`、费用类型树、导出/更多操作、发票凭证、清空默认值及修正后的付款状态；记录与关联流水 `463` 已精确清理并复查为 404/0。
- 最新统一验收通过：5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、`FINANCE_INVOICE_UNISSUED_OK`、`INDEPENDENT_SCROLL_OK`、15 groups passed、测试数据零残留和 Docker Compose 配置。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 最近严重日志匹配为 0。下一页继续“开票管理 → 公司未开票”；220 个叶子页仍未全部逐页证明，整体目标保持未完成。
- 2026-07-16 已完成财务中心“费用查询”首轮原站只读对照。本地现有十二项服务端筛选、费用类型多选树、十六列表格、上下五项合计、六档 15 条分页、选中/全部真实 Excel，以及上传案件文档、新增案件/内部费用、批量修改、四类文书、案件任务和案件日志十项下拉操作。浏览器验收发现并修正金额误带 `¥` 及下拉仅悬停不便点击，现均与原交互口径一致。
- 新增 `FINANCE_FEE_QUERY_OK` 和费用查询专用冒烟，验证组合筛选、分页、日期/金额边界、权限脱敏、选中 Excel、空选/无效 ID 以及测试数据清理。完整验收通过 5471 modules、265/220/0、17 groups passed、零残留和 Docker Compose 配置；Docker API/Web/PostgreSQL/Redis 健康。下一轮继续按原系统逐叶子页面取证，整个系统仍不得标记“完全完成”。
- 2026-07-15 已完成财务“开票管理 → 公司未开票”首轮原站只读对照。原站 `/5001006005/FAM/CaseFee/InvoceCaseFeeList` 当前 491 条、金额合计 `7050070.35`，而个人 `/5001006004/...` 同时只有 2 条、合计 `11000.00`；两页布局、十五项筛选、十八列、四项合计、分页、导出与更多操作同构，核心差异是公司全所范围，原站未执行写操作。
- 本地已修正 `/finance/case-fees/invoice-status` 的 `scope` 参数未实际生效问题：个人范围按费用负责人限定，公司范围继续执行角色数据范围且管理员可见全所。浏览器临时费用 `3615/3616` 验证公司 2 条 `10303.00`、个人 1 条 `5101.00`，并通过选中导出、空选提示、案件详情和案件日志；长到账单位不再撑宽表格，固定列宽现为 104/207/83/155 等，与原站 1904px 表格契约一致。
- 临时费用 `3615/3616` 已删除并逐条确认 404，`BROWSER-COMPANY-UNISSUED-1784116577` 关键词复查为 0。最新统一验收通过 5471 modules、265/220/0、44 个财务原布局路由、`FINANCE_INVOICE_UNISSUED_OK`（含独立个人/公司范围）、15 groups passed、零测试残留和 Docker Compose 配置；Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，最近严重日志为 0。下一页继续“结算管理 → 待结算”，整体目标保持未完成。
- 2026-07-15 已完成财务“结算管理 → 已拒绝”首轮原站只读对照。原站 `/5001007005/FAM/Settlement/RejectedList` 的十三组筛选、十四列加占位、六项合计、审核/提交/回款上下文、默认 10 条分页、三项行内操作和四项页尾操作均已确认；官方脚本还确认空选提示“请选择回款.”及重新申请备注必填“请输入备注.”，原站未执行写操作。
- 本地新增拒绝组合状态查询和 `/finance/general-settlements/applications/reapply` 专用接口，拒绝类记录可携备注重新进入待审批并保留准确历史。浏览器链路 `UIRJ-122620` 验证三条费用明细、红色审核备注、三类导出、空选择/空备注及真实重新申请；业务记录 `4497–4500`、到账 `159`、应收 `169` 已全部清理，记录复查 404、相关列表复查为 0。`FINANCE_SETTLEMENT_REJECTED_OK`、265/220/0、5471 modules 和 16 groups passed 均已通过；下一页继续“归档费结算”，整体目标保持未完成。
- 2026-07-16 正式服只读盘点确认四个微信小程序共用 `https://apifox.sfhcip.com`：思法汇成使用 `/sfhc`、伙伴端使用 `/partner`、全面打假使用 `/quan`、会议小程序使用 `/huiyi`；会议网页后台另为 `https://meeting.sfhcip.com`。全面打假三个 `/quan` 业务接口抽测均返回 200 JSON。微信小程序前端包由微信平台发布，服务器没有独立 `quan.sfhcip.com` 或小程序工程文件属正常。四个小程序 API 均由 `sfhcip.com` 与 `*.sfhcip.com` 通配符证书覆盖并纳入 acme.sh 自动续期。仍需处理：部分 HTTP 入口未强制跳 HTTPS、伙伴端协议/隐私接口 500、伙伴端访问日志路径误配，以及生产日志记录完整令牌的严重敏感信息风险。
- 2026-07-16 正式服网页入口复核：`www.sfhcip.com`（平台官网）、`console.sfhcip.com`（管理端）、`client.sfhcip.com`（客户端）、`partner.sfhcip.com`（伙伴端）、`meeting.sfhcip.com`（视频会议）、`hao.sfhcip.com`（知识产权平台）均以 HTTPS 返回 200、证书校验通过，并共用已自动续期的 `sfhcip.com` 通配符证书；根域 `sfhcip.com` 最终跳转至 `www`。`apifox.sfhcip.com` 根路径 404 属 API 无首页，业务子接口正常。另有 `www.huzhilawfirm.com`、`admin.huzhilawfirm.com` 和 `bug.sfhcip.com` 当前仅配置 HTTP；前两者强行使用 HTTPS 会发生域名证书不匹配，`bug` 的 HTTPS 落入默认 404，均不能视为已完成 HTTPS 部署。
- 2026-07-16 正式服新闻异常只读诊断：新闻 `id=853`（slug `7-eleven`）正文为 MySQL `TEXT`，现值恰好达到 65,535 bytes；正文约第 1.7 KB 起为从剪贴板粘贴产生的 `data:image/png;base64`，图片数据在字段上限处被截断且 HTML 未闭合，因此官网在“涉案鞋款的争议细节”后显示破图并丢失后续正文。官网模板未截字；根因是 Quill 粘贴图片绕过 OSS、API 仅校验 `required|string`、MySQL 连接 `strict=false` 导致超长数据静默截断后仍返回成功。787 条有效新闻中 46 条含内嵌 Base64，已有 6 条达到 65,535 bytes。待授权修复：字段迁移为 `MEDIUMTEXT`，粘贴图片统一上传 OSS，后端拒绝 Base64 并增加字节数/HTML 完整性校验，审计另外 5 条满字段新闻；本篇已丢失部分需从作者原稿重新录入。
- 2026-07-16 正式服官网新闻截断防护已部署并验证。`t_website_news.content` 已由 `TEXT` 迁移为 `MEDIUMTEXT`；管理后台 Quill 粘贴图片现在先上传 OSS，提交前拦截 `data:`/`blob:` 图片并显示 UTF-8 字节数；API 同时实施 8 MB 上限、内嵌图片拒绝、事务保存及落库 SHA-256 完整性复核。测试服与正式服均用 150,007 字节正文完成哈希一致写入并回滚，正式服新闻 `853` 回滚后与部署前备份哈希一致，未产生测试残留；`nginx -t`、正式后台全部 7 个 JS/CSS 资源、后台登录页及官网新闻页均通过。正式服备份位于 `/data/web/sfhc-backups/news-hotfix-20260716-151150`，切换前静态包位于 `/data/web/sfhc/sfhc-console/dist-pre-news-hotfix-20260716-153700`。GitHub 版本控制：后端 `b6b543dc` / PR `W3217-SUDO/sfhc--#1`，后台 `c2a419f` / PR `W3217-SUDO/sfhc-console#1`。仍未恢复：历史上已被静默截断的 6 篇新闻无法从数据库、服务器备份或 binlog 找回缺失部分，必须取得作者原稿后逐篇补录；当前 `7-eleven` 页面仍显示原先截断内容，但新的保存链路不会再发生同类静默截断。
- 2026-07-17 已修复新闻编辑器“Ctrl+A 后粘贴仍提示含直接粘贴图片”。根因是首版粘贴接管调用 `preventDefault()` 后只从 `range.index` 插入，没有删除 `range.length`，导致新正文插入但旧新闻的 Base64 残留仍在末尾；另有父表单延迟 500 ms 取到旧内容的竞态。本次改为图片成功上传后再删除完整选区并插入，支持将仅存在于剪贴板 HTML 的 `data:image` 转为 JPG/PNG 文件上传 OSS，编辑器 ready 时确保绑定监听，保存时直接读取 Quill 当前 HTML，并在 STS 临近过期时刷新凭证。前端 ESLint、生产构建、测试服及正式服全部入口 JS/CSS 200、`nginx -t` 均通过。GitHub 提交 `00f3a22` 已推送至 `W3217-SUDO/sfhc-console#1`；正式服回滚备份位于 `/data/web/sfhc-backups/news-paste-fix-20260717-001800`，切换前静态包位于 `/data/web/sfhc/sfhc-console/dist-pre-paste-fix-20260717-001800`。用户需关闭旧编辑抽屉并硬刷新后台，再重新打开新闻执行全选粘贴；旧 SPA 标签页不会自动获得新代码。
- 2026-07-17 已按用户授权同步重置测试服与正式服的指定后台账号。两边账号均为既有记录，已统一为角色 `1`（超级管理员），并确认 `is_login=1`、`is_office=1`、`is_forbidden=0`、`is_active=1`，新密码仅以安全哈希入库且校验通过；明文密码未写入代码、普通文档、GitHub 或服务器脚本。更新前记录分别以 0600 权限备份到 `/data/web/sfhc-test-backups/admin-account-20260717/` 和 `/data/web/sfhc-backups/admin-account-20260717/`。
- 2026-07-17 新闻管理外层列表的 `Slug` 列已恢复。根因是前两次热修只同步了编辑抽屉 `Add.vue` 和富文本组件，生产构建目录中的列表 `index.vue` 仍为服务器旧版；GitHub `master` 自 `5d176250` 已存在 Slug 列，数据和 API 均未丢失。本次将受版本控制的列表页同步至构建目录，生产构建通过后先部署测试服，确认构建包精确包含 `label:"Slug"` 与 `prop:"slug"`、SEO 编辑字段仍在、所有入口资源 200，再原子切换正式服；正式服新入口为 `app.e7e969f9.js`，`nginx -t` 通过。回滚备份位于 `/data/web/sfhc-backups/news-slug-column-20260717-094000`，切换前静态包位于 `/data/web/sfhc/sfhc-console/dist-pre-slug-column-20260717-094000`。

## 2026-07-16 部门回收站阶段交接

- 已实现并验证 `customer-dept-recycle`：后端 `scope=department_recycle`、部门/管理员数据范围、三项筛选、服务端分页/合计，以及恢复、重新回收、进入公海专用生命周期；通用记录接口防绕过已补齐。
- 前端已按原页复现 13 列、已删除状态、半角逗号管理人、只读查看、恢复/进入公海菜单、六档分页、末页 `»` 和精确空态隐藏规则。
- 验收证据：5471 modules；264 nodes / 219 leaves / 0 unhandled；Python 编译通过；`SMOKE_API_OK: 17 groups passed`；`SMOKE_CLEANUP_OK`；`VERIFY_LOCAL_OK`。Web Docker 镜像已重建并启动。
- 原站事故必须随迁移保留：只读取证期间误点 `SHKH1910235` 的无确认“客户恢复”，随后已用唯一记录 ID `2148` 精确放回部门回收站并复核总数恢复为 3；但最后修改日期已从 `2026-04-16` 变为 `2026-07-16`，无法通过界面恢复。后续禁止点击原站任何可能写入的操作按钮，只能读 DOM、静态脚本和安全的查询结果。
- 当前整站目标保持进行中；下一目标为“客户管理 → 公司客户” `/6001005/CRM/Customer/CustomerList`。

## 2026-07-16 公司客户阶段交接

- 原站 `/6001005/CRM/Customer/CustomerList` 已完成只读取证：三筛选、勾选加 13 列、正常与已删除混合、上下金额合计、六档 15 条分页和末页 `»`、只读客户查看、唯一“分配客户”菜单及四字段单值分配弹窗。精确空态不显示合计、底栏和分页；本页未在原站提交任何写操作。
- 后端新增 `scope=company`，管理员查看全所正常与已回收客户并排除公海；普通用户/部门负责人 403。服务端筛选、分页、合计、全部字段、管理人精确匹配、跨部门分配恢复及生命周期旁路保护均已进入 17 组冒烟。
- 前端已进入原客户列表模式，状态“已回收”映射“已删除”，多管理人半角逗号，复用只读四区块三标签查看和单值分配弹窗。结构门禁新增 `CUSTOMER_COMPANY_OK`。
- 浏览器临时客户 `10362/10363` 已验证并精确删除；两个 ID 为 404，关键词残留 0。统一门禁结果为 5471 modules、264/219/0、17 groups、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。
- 当前整站目标继续进行；下一目标为“客户管理 → 公海客户” `/6001006/CRM/Customer/CustomerList`。

## 2026-07-16 公海客户阶段交接

- 原站 `/6001006/CRM/Customer/CustomerList` 已完成只读取证：三项筛选、勾选加 13 列、精确空态隐藏规则、只读四区块三标签详情，以及静态脚本证明的唯一无确认“拾回”动作。当前管理员账号公海列表为 0 条，所以默认 15 条和六档分页仅由同族共享模板证明；未在原站执行领取或其他写操作。
- 后端新增 `scope=public`，只返回全所公海客户；管理员、负责人、普通用户按角色字段权限读取，其他角色 403。服务端筛选、分页、合计、管理人精确匹配、未领取写保护、专用领取、重复领取 409 和 PostgreSQL 行锁均已进入 17 组冒烟。SQLite 会忽略 `FOR UPDATE`，若以后切换 SQLite 运行极端并发领取，应改用条件更新或数据库约束；当前 Docker/服务器 PostgreSQL 路径受行锁保护。
- 前端已复现公海客户列表、半角逗号管理人、只读详情、唯一“拾回”、同族分页和精确空态；结构门禁新增 `CUSTOMER_PUBLIC_OK`。
- Docker 浏览器临时客户 `10564` 已完成查看与真实拾回，随后删除并确认 404、关键词残留 0。统一验收为 5471 modules、264/219/0、17 groups、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`；六容器运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 严重日志为 0。
- 当前整站目标继续进行；下一目标为“客户管理 → 我的共享客户” `/6001007/CRM/Customer/CustomerList`。

## 2026-07-16 我的共享客户阶段交接

- 原站 `/6001007/CRM/Customer/CustomerList` 已完成只读取证：GET 表单、PageId `6001007`、默认 15 条、三项筛选、禁用且空的客户管理人、勾选加 13 列，以及精确空态隐藏合计/底栏/分页。隐藏模板仅有客户查看，更多操作菜单为空；直接 View 页为四区三标签且主字段全部禁用。原站当前账号 0 条，非空金额合计、六档分页和多管理人分隔符仅有同族模板间接证据；未在原站执行任何写操作。
- 前端 `customer-shared` 已接入专用 `scope=shared`，移除通用 `/records?page_size=100` 和客户端共享过滤；服务端分页/合计、禁用空管理人、只读详情、唯一客户查看、不可点击空模板提示、空态隐藏及 `CUSTOMER_SHARED_OK` 已落地。
- 后端共享范围改为精确 JSON 成员语义，修复用户名子串越权；管理员保留全所共享审计，普通/负责人仅看明确收件关系。`/share` 校验启用用户并采用 PostgreSQL 行锁，重复幂等；公海、回收和重新领取清除共享。跨部门共享仅获得客户详情和现有附件只读，所有客户写路径受守卫保护。
- 安全旁路已修：沟通日志旧操作人撤权后不能改删；共享人及撤权后的智能文档创建人不能访问客户关联 Dify 智能文档全文、下载或任何写回路径。SQLite 会忽略 `FOR UPDATE`，若以后切换 SQLite 运行并发共享仍应改用条件更新；当前 Docker/服务器 PostgreSQL 路径已由真实行锁验证。
- Docker 浏览器临时客户 `11086`、用户 `978`、附件 `2434` 已完成非空列表与四区三标签验收并全部清理；记录/附件 404、共享关键词与用户残留 0。统一验收为 5471 modules、264/219/0、17 groups、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`；六容器运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 严重日志为 0。
- 当前整站目标继续进行；下一目标为“客户管理 → 最近联系的客户” `/6001008/CRM/Customer/CustomerList`。

## 2026-07-16 最近联系的客户阶段交接

- 原站 `/6001008/CRM/Customer/CustomerList` 当前管理员为 0 条，只读取证确认三筛选、禁用空管理人、13 列、精确空态隐藏规则、只读四区三标签详情，以及唯一“客户查看 + 更多操作→客户编辑”；原站未执行写操作。
- 后端新增 `scope=recent_contact`：有效联系时间投影、排除回收、管理员全所权限、普通用户可见范围、服务端筛选/分页/合计/字段权限和统一时间轴倒序。联系记录新增/修改/删除均重算最大事件时间与有效次数；新增联系人不计联系，通用客户创建/修改不能伪造或覆盖联系指标。
- 前端接入专用服务端数据源，恢复禁用空管理人、精确空态、六档分页、只读详情和编辑入口；结构门禁为 `CUSTOMER_RECENT_CONTACT_OK`。浏览器临时客户 `11300–11302` 已验证排序、排除无联系客户、详情、真实编辑保存和空态，并已清理为关键词残留 0。
- 统一验收结果：5471 modules、Python 编译、264/219/0、17 groups passed、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`；Docker API/Web/PostgreSQL/Redis 健康。原站当前无非空样本，普通事项口径、额外时间窗口、公海和各角色精确范围仍需以后用原站非空/角色证据校准，不能写成已完全证明。
- 当前整站目标继续进行；下一目标为“客户管理 → 最近更新的客户” `/6001009/CRM/Customer/CustomerList`。

## 2026-07-16 最近更新的客户阶段交接

- 原站 `/6001009/CRM/Customer/CustomerList` 当前 1 条，只读取证确认锁定空管理人、13 列、上下合计、15 条六档分页、只读查看、唯一编辑和精确空态隐藏规则。唯一记录正是当前管理员曾恢复再回收的已删除客户；公司页同日另外两条他人更新客户未进入，直接证明页面按当前最后修改人投影且包含回收，不是管理员全所更新时间流。原站本轮未执行写操作。
- 后端新增 `scope=recent_update`，当前身份最后修改者语义、回收纳入、公海排除、普通角色可见范围、权威 `updated_at/id` 排序、服务端筛选/分页/合计和历史修改事件回退均已落地。所有现有客户行变更入口维护 `last_modified_by`，创建/PATCH 不能伪造或清空修改人/日期；最近联系公海旁路同时修复。
- 前端已用专用 API 替换 `/records?page_size=100` 与客户端排序，复现锁定空管理人、已删除、半角逗号、精确空态、只读四区三标签、唯一编辑和六档分页；门禁为 `CUSTOMER_RECENT_UPDATE_OK`。浏览器临时客户 `11641–11643`、用户 `1030` 完成修改人隔离、公海排除、回收纳入、查看、真实编辑和空态验收，全部清理后记录/用户残留为 0。
- 统一验收为 5471 modules、Python 编译、264/219/0、17 groups passed、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`；Docker API/Web/PostgreSQL/Redis 健康。原站尚未逐路径证明联系人、事项、沟通、导入是否都计作更新，当前统一标记是合理实现而非全部直接证据；未来新增客户写入口必须继续调用 `_mark_customer_modified` 或 `_customer_event` 并补 smoke。
- 当前整站目标继续进行；下一目标为“客户管理 → 公司回收站”。

## 2026-07-16 公司回收站阶段交接

- 原站 `/6001010/CRM/Customer/CustomerList` 已完成严格只读证据采集：当前 17 条全所已删除客户，三项筛选、13 个业务列、上下金额合计、15 条默认与六档分页、末页 `»`、精确空态、四区三标签只读详情和“客户恢复/进入公海”菜单均已确认；没有点击原站写操作。
- 本地已落地管理员专用 `scope=company_recycle`、全所回收范围、服务端筛选/分页/合计、唯一显示姓名解析和专用恢复/释放生命周期。创建及通用 PATCH 的联系、修改、回收、恢复、释放、领取、共享系统字段均不可伪造，负责人和客户管理人也不能绕过专用接口变更。
- 前端已对齐已删除状态、半角逗号管理人、可编辑管理人、上下合计、精确空态、只读详情、双动作和完整分页；末页最后记录被恢复或释放后会自动回到新的末页。`CUSTOMER_COMPANY_RECYCLE_OK` 已加入结构门禁。
- 上游“我的客户 → 客户删除”已修正为真实回收而非物理删除。Docker 浏览器临时客户 `12363` 证明从我的客户删除后进入公司回收站；`12229–12244` 证明 16 条两页、第二页恢复自动收敛、进入公海、详情与空态。所有临时数据均已删除，复查残留为 0。
- 统一验收结果：5471 modules、Python 编译、264/219/0、17 groups passed、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`；六容器运行，API/Web/PostgreSQL/Redis 健康，严重日志 0。
- 当前整站目标继续进行；下一目标为“客户管理 → 客户利益检索”。唯一已登记的相关生产风险是管理员通用 customer 物理删除仍服务于测试清理，发布前必须使用测试专用端点或环境门禁隔离。

## 2026-07-16 客户利益检索阶段交接

- 原站 `/6001011/CRM/Customer/CustomerSearching` 已完成严格只读取证：两步条、固定提示、按钮点击而非回车触发、完整企业名称精确匹配、未找到原文，以及命中后仅企业名称/最新立案号/最新立案日期/原告/被告/第三人/我方客户/客户管理人八个只读字段。原站没有风险表、记录表、详情抽屉、分页或返回按钮；没有执行任何原站写操作。原站空名称泄露记录属于缺陷，本地明确拒绝复制。
- 后端已落地 `/customers/conflicts`：全所案件精确实体匹配、最新立案日期与 ID 稳定选择、最小固定响应、案件多字段兼容和“所检索企业自己的客户管理人”解析。空名称 422，独立 `customer-conflict` 叶子权限由后端强制，管理员最高权限保留；角色权限迁移、编辑与冒烟已覆盖移除叶子后 403、恢复后可用。
- 前端已替换原有臆造界面，复现原站两步与单列八字段，去零日期、逗号拼接、点击触发及未找到文案；并补齐旧请求/失败清空和叶子菜单/深链守卫。结构门禁为 `CUSTOMER_CONFLICT_OK`。
- Docker 浏览器临时数据 `12803–12807` 与用户 `1158` 已验证：回车不触发、部分名未命中、空名只提示、完整名命中最新 `UIC-CASE-NEW-200158`，日期显示 `2026-7-6`；同案原告与被告分别显示不同客户管理人。临时 5 条业务记录与 1 个用户均已删除，记录 404、查询与用户残留 0。
- `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 已通过：5471 modules、Python 编译、264/219/0、17 groups、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`；Web Docker 镜像已重建。整站目标仍未完成；客户管理 12 个叶子入口阶段结束，下一阶段从案件中心首个真实叶子开始只读取证和实现。

## 2026-07-16 新建刑事案件基本信息阶段交接

- Chrome 连接恢复后已重新接回原站登录会话和本地 Docker 页。原站 `/1001000002/Legal/Case/Create?caseTypeId=120` 只读取证确认独立新建页、三步条、首步八字段及默认值、唯一下一步和精确“请选择客户.”提示；未在原站创建或修改案件。
- 本地已移除错误可见的 `case-new-civil`，真实新建子叶为刑事、行政及国家赔偿、法律顾问、仲裁；旧 civil capability 仅用于兼容，启动时隐藏遗留数据库菜单。前端刑事首步已改为独立页面并经 Docker Chrome DOM 复验，不再出现旧 Modal、提示条、保存/取消死控件或保存后空白页。
- 后端 `/cases` 已补真实类型与叶权限、审批合同白名单、合同启用部门、负责人、空白/长度、固定待分配状态、经办律师和唯一案号并发保护；非管理员不能伪造负责人或把部门改成当前用户部门。新增 `/cases/{case_id}/litigants` 专用入口；通用案件 PATCH/transition 被阻断并有冒烟证据。
- 最新统一门禁：5471 modules；263 nodes / 218 leaves / 0 unhandled；17 groups passed；测试数据零残留；`VERIFY_LOCAL_OK`。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 严重日志 0。
- 未证明：刑事当事人页与司法机关页的完整字段、动态分支和阶段式保存/跳转仍缺少原站非空刑事样本；当前后两步只是兼容骨架。整站目标保持进行中，不能标记“完全完成”。

## 2026-07-16 新建刑事案件三步流程阶段交接

- 原站继续严格只读取证：共享“当事人信息”页确认原告、原告代理人、被告、被告代理人、第三人、第三人代理人六组及“下一步/完成”；共享“法院信息”页确认一审默认勾选、二审/再审可选，各级均包含法院、法庭、法官、书记员、案号、立案日期、开庭日期。原站刑事静态脚本补充证明公安机关及一审/二审/再审检察院各自的名称、案件编号、地址、电话和承办人字段。原站未执行任何业务写操作。
- 本地前端已完成真实三步提交：首步调用 `POST /cases` 并采用服务端高熵案号；第二步向 `PUT /cases/{id}/litigants` 提交六组当事人/代理人，允许刑事空当事人并提供“下一步/完成”；第三步向 `PUT /cases/{id}/judicial` 提交公安、三级检察院及一审/二审/再审法院数据。旧通用新增 Modal、案件 CSV 导入按钮和物理删除按钮已移除。
- 后端强制 `basic → litigants → completed` 顺序，下游分配、进展、排期、归档只有完成创建后可用；通用案件创建、PATCH、transition、CSV 导入、导入模板和物理删除均不能绕过专用流程。管理员未显式提交负责人时默认当前账号；可选合同改为专用 `/cases/eligible-contracts`，返回全部当前身份可见且状态真实可建案的合同，不再受前 100 条客户端过滤影响。
- Docker Chrome 已用临时案件 `13585 / SMOKE-BROWSER-刑事三步流程` 完整走通三步并跳转公司刑事列表。数据库复核 `case_creation_step=completed`，被告代理人、公安机关、一审检察院和一审法院均与页面输入一致；随后按精确 ID 与标题清理，未保留该验收数据。
- 当前自动证据：最新 API/Worker/Web 镜像已重建；前端生产构建 5471 modules、Python 编译通过、菜单审计 `263 nodes / 218 leaves / 0 unhandled` 并输出 `CASE_NEW_CRIMINAL_FLOW_OK`；API 冒烟 `17 groups passed`、`SMOKE_CLEANUP_OK`，完整 `verify-local.ps1` 输出 `VERIFY_LOCAL_OK`。
- 未证明：原站当前账号没有非空刑事案件样本，因此刑事页面是否会根据案件阶段隐藏某些当事人或司法机关区块、以及所有非空字段的具体初始值，仍需以后取得合法只读样本校准；本阶段不能写成整站完全一致。

## 2026-07-17 新建行政案件及国家赔偿流程推进

- 本轮继续案件中心新建流程，优先补“行政案件及国家赔偿”。前端已按行政语义拆分默认诉讼地位、当事人标签、客户预填、权利类型提交和完成后跳转；行政案件不再显示刑事公安/检察院司法机关组。
- 后端已补行政案件专用边界：客户诉讼地位必须是“原告/申请人、被告/被申请人、第三人”之一；当事人阶段必须有原告/申请人与被告/被申请人；司法机关阶段必须至少录入一个法院信息，且不能混入刑事公安或检察院字段。
- `scripts/audit-menu-coverage.py` 和 `scripts/smoke-api.py` 已加入行政案件三步流程门禁。2026-07-17 本轮已完成验收：`powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 通过，输出 5471 modules、Python 编译通过、`CASE_NEW_ADMINISTRATIVE_FLOW_OK`、`263 nodes / 218 leaves / 0 unhandled`、17 groups passed、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。
- 验收稳定性修复：`scripts/smoke-api.py` 默认 API 地址从 `http://127.0.0.1:8000` 改为 `http://localhost:8000`。本机曾出现 Windows 端口代理残留，`127.0.0.1` 会打到非 Docker 数据库路径，导致任务提醒、交接 5 日自动完成等依赖 Docker Postgres 回拨的规则误报；`localhost`/`::1` 已验证能写入 Docker Postgres。
- Docker 当前六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 最近 10 分钟严重日志匹配为 0。整站目标仍未完成；行政案件三步业务闭环已验证，但原站行政案件非空样本、完整字段初始值和列表视觉仍未直接证明。

## 2026-07-19 腾讯云宝塔轻量服务器部署评估

- 已通过 `baota-lighthouse` 对 `150.158.3.104` 完成只读盘点，未安装、上传、停止、修改或开放任何服务。该机为 2 核、约 1.9GiB 内存、1GiB Swap、50GB 系统盘；检查时物理内存已用约 685MiB、Swap 已用约 556MiB、磁盘可用约 39GB。
- Docker/Compose 当前未安装；现有宝塔 Nginx 占用 80/443，宝塔面板占用 8888，127.0.0.1:8000 已由 Couple Diary 应用使用，同时运行 MariaDB 和其他系统服务。
- 本地 OA 六容器同一时点实测合计约 841MiB，其中 Celery Worker 约 439MiB。生产 Compose 已默认把 Web 绑定到 `127.0.0.1:8080`，端口与现有 Nginx 可安全衔接；磁盘也足够初始测试，但 2GB 内存叠加现有服务存在严重换页/OOM 风险。
- 当前结论为“技术上可部署，但不能在现配置上视为稳定可交付”。测试环境建议先升级至至少 4GB，正式交付优先 8GB；升级后再执行 Docker 安装、生产密钥配置、镜像构建、宝塔反向代理、HTTPS、数据备份及完整验收。整站业务目标状态不受本次只读评估影响，仍保持未完成。

## 2026-07-20 腾讯云宝塔测试 OA 部署

- 用户明确限定为非正式测试环境后，已在 `150.158.3.104` 安装 Docker Engine 29.6.2 / Compose 5.3.1，并用 `compose.test-server.yml` 启动 PostgreSQL、Redis、MinIO、API、Celery 单并发 Worker、Web 六容器。现有 Nginx、MariaDB、Couple Diary 和宝塔服务均未停用或覆盖。
- 部署前 `verify-delivery.ps1` 已完整通过：5471 modules、263/218/0 菜单覆盖、343 个前端 API 匹配、17 组业务冒烟、零测试残留、六服务健康、生产配置及近期日志门禁全部通过。期间修复了 Windows 在午夜把 `.env` 的 `Asia/Shanghai` 误解释为 UTC 导致日期断言差一天的问题；Docker 容器时区配置不受影响。
- 当前发布软链接为 `/opt/sunhold-oa/current`，指向已扫描的 `20260720-0030` 干净源码包；`.env.production` 与一次性管理员凭据均为服务器 root 私有文件、权限 0600。首次包遗漏 SQLite 后缀排除的问题已修复，服务器旧 ZIP 和旧包中的开发库已删除，运行镜像与全新 PostgreSQL 从未导入该库。
- 健康证据：API/Web/PostgreSQL/Redis/MinIO 均健康，Celery 运行；服务器内外 `/health` 返回 `{"status":"ok"}`，管理员登录返回 `role=admin` 且强制首次改密，个人资料返回 admin 最高角色。新库仅 1 个管理员、0 条业务记录；容器严重错误日志为 0。
- 当前电脑仍可使用 `http://oa-test.sunhold.local/`；用户已于 2026-07-20 放行腾讯云 TCP 8088，公网 `http://150.158.3.104:8088/health` 外部复验返回 200 和 `{"status":"ok"}`，公网测试入口正式改为 `http://150.158.3.104:8088/`。数据库、Redis、MinIO 和 API 仍未直接暴露；测试入口仅为 HTTP，正式上线仍必须使用真实域名与 HTTPS。
- 六容器稳定实测约占 326MiB，主机剩余 available 约 869MiB，根盘可用约 36GB。该部署可以支撑 1～3 人测试，但批量导入、大文件、智能体任务和多人并发不可同时压测，也不能据此宣称正式环境或整站业务已经交付完成。

## 2026-07-20 两份案件管理逻辑 PDF 对照结论

- 已渲染并阅读《案件管理系统_逻辑拓扑图.pdf》与《案件管理系统_逻辑框架与功能模块.pdf》。两份材料适合作为“知识产权维权主线”的业主沟通草案，但不能直接视为当前重构系统的完整事实或验收基准；它们注明基于 89 页培训 PPT 梳理，其中部分内容属于扩展推断。
- 与当前代码和冒烟证据一致的主干包括：合格合同前置建案、已取证线索按条转为案件且禁止重复、案件官方费用三要素校验、付款/回款/开票/退费/对账/归档结算、归档审核与常规修改锁定，以及任务 30 天、交接重启、交接 5 天自动完成规则。
- 需要修订的关键差异：当前系统有 15 个一级中心而不是 PDF 的六模块，用印是独立一级模块；当前权限模型是 4 类系统角色加 6 类岗位，不是 13 类独立权限角色；客户等级为立案/高级/中级/低级，尚无 PDF 所述潜在/目标/签约分级审批和客户服务端开通；外部合同号目前是单值字段，不是一合同多个外部号；手工案件没有独立“案件创建审批”状态；案件阶段只有部分要素触发推进并允许受控人工调整，尚非完整任务驱动自动状态机；每案固定任务集、开庭短信、1/3 日提醒、绩效惩罚和解档特殊审批均未形成完整可验证闭环。
- PDF 还遗漏当前独立的事务中心、用印中心、收发文台、平台财务中心、用户中心、人事中心、系统中心、仓库管理和报表中心。后续如用于合伙人确认，应改名为“维权案件主业务蓝图”，并把上述差异作为待业主确认事项，而不是直接要求系统按 PDF 文字倒推实现。
## 2026-07-21 新系统测试问题闭环

- 已读取《新系统测试.docx》正文及 7 张截图，并按问题逐项修复，不以静态页面或假按钮代替真实行为。
- 前端完成：刷新保持登录与页签、业务多页签、系统账号合并员工列表、员工连续新增重置、客户编号列布局、客户到合同的上下文传递、合同客户可检索选择、岗位权限业务动作矩阵。
- 后端完成：合同保存前校验客户 ID、名称、启用状态和当前用户可见范围，并以客户主数据回填合同客户编号及管理人，禁止手工伪造客户绕过。
- 测试服务器当前发布目录为 `/opt/sunhold-oa/releases/20260721-102324/sunhold-oa-deploy-20260721-102324`，稳定软链 `/opt/sunhold-oa/current` 已指向该版本。启动时必须同时使用 `docker-compose.yml`、`compose.prod.yml`、`compose.test-server.yml` 和 `--env-file .env.production`。
- 自动验收：前端生产构建 5472 modules；Python 编译通过；菜单覆盖 263/218/0；客户端 API 覆盖 362；18 组 API 业务冒烟全部通过且清理完成；Docker Compose 配置通过；最终输出 `VERIFY_LOCAL_OK`。
- 服务器页面验收：`admin` 在员工管理显示；刷新仍保持登录；员工管理、新建员工、我的客户、合同新建等页签可累计；客户编号完整；从“测试客户1”发起合同后客户名称与编号自动带入；角色修改弹窗显示五组细分权限。验收过程中没有保存角色、合同或新增员工，未改变用户业务数据。
- 仍未证明：本轮只闭环《新系统测试》列出的问题，不能据此宣称整个 OA 的所有业务模块已经完成最终业主验收；公网正式上线仍需真实域名、HTTPS、正式容量和生产备份方案。
- 用户二次反馈客户表“每一列”仍有视觉错位后，已将全部客户业务列的表头、数据行、合计行统一居中；随后针对右侧金额区再次校准，保留正确的合计列索引并把代理费、官费顶部合计、数据金额、底部合计统一居中。服务器左右两端截图均确认中心线一致；前端 5472 modules、18 组冒烟、零残留及 `VERIFY_LOCAL_OK` 再次通过。
## 2026-07-21 合同审批人权限修复

- 合同新建第二步已恢复启用人员选择；根因是目录响应缺少 `is_active`，前端过滤后列表为空。
- 管理员可在审批步骤直接创建审批账号并自动选中；普通角色不显示入口，后端创建用户接口继续强制管理员权限。
- 本地 API/Worker 镜像已重建且健康，完整验收通过并清理全部冒烟数据。整站业务目标仍按模块继续验收，不能仅凭本项修复宣称全部交付完成。
- 测试服务器已切换到 `/opt/sunhold-oa/releases/20260721-131929`，API/Worker 使用 `sunhold-api:20260721-131929`，Web 使用 `sunhold-web:20260721-131929`；稳定软链 `/opt/sunhold-oa/current` 已更新。六个服务正常，API/Web/PostgreSQL/Redis/MinIO 健康，Worker 正常运行。
- 首次静态文件写入镜像后发现权限为 700，浏览器 JS 请求返回 404/Permission denied；已修为 644/可遍历目录，并恢复 Nginx ENTRYPOINT/CMD。最终主 JS 返回 200，管理员页面浏览器验收通过。

## 2026-07-21《新系统测试1》整改交接

- 四项问题已闭环：合同客户只显示名称且历史选项去重、客户所有改名入口统一阻止同名、员工修改覆盖完整人事资料、合同只允许一名启用部长审批；合同用印七类印章继续可用。
- 本地完整验收最终为 `VERIFY_LOCAL_OK`，18 组业务冒烟全部通过并清理测试数据。测试服务器稳定发布为 `/opt/sunhold-oa/releases/20260721-151315`，镜像为 `sunhold-api:20260721-151315`、`sunhold-web:20260721-151315`，`/opt/sunhold-oa/current` 指向该版本。
- 页面复核采用已登录管理员会话且未保存任何表单：合同客户下拉仅剩一个“测试客户1”；员工 `SYS-0001` 修改弹窗显示身份证、学校、社保、证照、日期、年休、提成、联系方式、权限及启停等完整字段。后端审批和去重边界由冒烟证明。
- 没有删除测试库已有的两条同名客户。当前处理是禁止新增/改名形成新重复，并在合同选择器按规范化名称去重；历史主数据合并必须获得业主确认后另行实施。整站最终验收状态仍为未完成。

## 2026-07-21 合同完成后连续新建修复交接

- 根因是合同向导本地存储的合同 ID 在生成用印申请后没有清除，导致刷新、关页签或重新点击“合同新建”都恢复上一份已完成合同。
- 已在用印申请生成成功后清除向导 ID；对旧浏览器残留的已完成合同也会在进入时自动识别并重置。完成页提供“继续新建合同”，再次点击当前活动的“合同新建”菜单同样执行完整重置，不影响已完成合同和用印数据。
- 自动验收最终为 `VERIFY_LOCAL_OK`：前端 5473 modules、Python 编译、菜单 263/218/0、客户端 API 363、18 组业务冒烟和零测试数据残留全部通过。
- 测试服务器当前稳定目录为 `/opt/sunhold-oa/releases/20260721-154607`，Web 镜像 `sunhold-web:20260721-154607`；API/Worker 沿用 `sunhold-api:20260721-151315`。六个服务正常，健康接口返回 `{"status":"ok"}`。
- 整站最终验收状态仍为未完成；本次只证明合同连续新建故障已闭环。
## 2026-07-21《新系统测试3》问题闭环

- 已读取《新系统测试3.docx》的正文与 5 张截图，确认并处理四类问题：客户页带入合同后的客户锁定异常/空白选择器显示数字、合同审批误按“部长职务”筛选、合同审批与同步用印顺序错误、案件编号过长且案号链接误进入任务创建。
- 合同客户上下文现在同时保存客户 ID、名称和编号；从“我的客户”发起合同时显示真实客户名称并锁定，不再出现数字占位。直接从合同中心创建仍可检索选择已登记客户。
- 合同审批改为岗位角色权限控制：只有角色权限包含“合同审批”的启用人员可被选择，发起人不能审批自己提交的合同，管理员可以配置角色和查看全所数据，但不能代替指定审批人操作当前审批节点。
- 发起合同审批后进入指定审批人的待审批事项；发起人可选择是否同步办理用印。选择同步用印时先保存用印草稿，合同最终审批通过后系统自动把关联用印申请推进到“待审批”，不再由合同发起人自己完成合同审批。
- 新案件未手工填写编号时按 `SH + 案件类型代码 + 两位年份 + 五位流水号` 自动生成，例如刑事案件 `SHXS2600001`；案件列表点击案号进入真实案件详情抽屉，不再打开创建任务页面。
- 已加强结构审计与端到端冒烟：覆盖角色审批资格、发起人与审批人分离、管理员不得代审、审批中同步用印草稿、合同通过后自动提交用印、短案号连续生成及案号详情入口。完整 `scripts/verify-local.ps1` 最终输出 `VERIFY_LOCAL_OK`，18 组业务流程通过且测试数据零残留；Docker API/Web/PostgreSQL/Redis 健康，Worker 正常运行。
- 测试服务器已发布到 `/opt/sunhold-oa/releases/20260721-171307`，API/Worker 使用 `sunhold-api:20260721-171307`，Web 使用 `sunhold-web:20260721-171307`；`/opt/sunhold-oa/current` 已切换。服务器六服务正常，公网 `http://150.158.3.104:8088/health` 返回 200，最近日志未发现严重错误。服务器 Python 软件源临时无法解析 `uvicorn==0.35.0`，因依赖清单未变化，API/Worker 从上一版已验证依赖镜像复制本次 `app` 代码构建，未修改数据库或数据卷。
- 修复 Windows Node 24 在 npm 子进程结束后偶发 libuv 断言造成的假失败：本地验收脚本改为直接执行仓库内 `tsc` 和 `vite`，实际构建内容不变。
- 本轮只证明《新系统测试3》列出的四项问题已闭环，不能据此宣称整站最终业务验收已经完成。
## 2026-07-21 首次进入页面白屏修复

- 服务器 Web 访问日志确认白屏根因：Edge 保留了发布前加载的主程序，发布后第一次进入懒加载页面仍请求旧哈希分包，`CaseCenterPage`、图标及公共分包等多个 `/assets/*.js` 返回 404，React 因动态导入失败未能渲染；不是案件数据或权限接口故障。
- Web 入口页和所有前端路由回退响应改为 `no-store, no-cache, must-revalidate`，避免浏览器跨版本保存旧 `index.html`；带哈希的静态资源继续使用一年不可变缓存。
- 所有懒加载业务页统一增加版本恢复：分包加载失败时仅自动刷新一次以获取当前入口和新哈希资源；若刷新后仍失败，则显示“页面资源加载失败”和人工刷新按钮，不再留下无法操作的纯白页。
- 结构审计新增 `VERSIONED_ASSET_RECOVERY_OK`；完整 `scripts/verify-local.ps1` 输出 `VERIFY_LOCAL_OK`，5473 个前端模块、365 个客户端 API 映射、18 组端到端业务流程和测试数据零残留均通过。
- 测试服务器 Web 已发布为 `sunhold-web:20260721-203237`，稳定目录为 `/opt/sunhold-oa/releases/20260721-203237`；API/Worker 沿用 `sunhold-api:20260721-171307`。公网案件新建直达地址返回 200，响应头含 `no-store`，全新浏览器会话实测出现登录页而非白屏，控制台无错误，六个服务正常。
## 2026-07-22 旧系统源码对照页面跳转第一批修复
- 已建立 `docs/旧系统页面跳转与前端关联差异清单.md`，按旧 OA 源码记录页面跳转、按钮关联和当前系统差异；旧源码路径为 `D:\OA系统源码归档_20260717\source-extracted-v1\SH.CRM.WEB`，只读使用，不向旧系统写入数据。
- 已修复跨模块案号入口：财务中心、调查/公证中心、任务中心点击案号时，会携带目标案号进入案件中心，并自动打开完整案件信息工作台；案件中心第一页未命中时会按案号二次查询，避免大数据量下无法定位。
- 保留原有业务按钮语义：任务中心“案件任务/案件日志”仍打开对应任务/日志办理弹窗，只有“案号”本身进入完整案件详情。
- 已发现但未全部闭环：旧系统案件列表的上传案件文档、新增三类案件费用、新增内部费用、批量修改、生成授权委托书/律所函/身份证明/结算提成表等按钮，需要继续按源码逐项核对权限、状态、生成文件、落库和后续同步。
- 已验证：`npm --prefix apps/admin-web run build` 通过，5474 个前端模块完成生产构建。完整本地验收仍需继续运行。

## 2026-07-22 旧系统关联入口第一批补齐

- 本轮按用户强调的“这一类型都要关联上”处理，不再只针对单个截图按钮。已把蓝色编号、客户名、合同号、全局搜索结果、通知消息、看板案号、财务明细中的案号/合同号/客户等，统一改为可携带上下文跳转到对应业务对象详情。
- 已新增并接入对象详情导航上下文：案件、合同、客户之间可以按编号、ID 或名称传递目标；进入目标中心后自动定位并打开详情，找不到时给出明确提示，避免只跳菜单但不打开对象。
- 已完成的首批关联包括：控制台案号进入案件详情；全局搜索结果进入案件/合同/客户详情；通知中的案件/合同消息进入对应详情；案件详情里的客户、合同号可跳客户/合同详情；合同详情里的客户、关联案号可跳客户/案件详情；财务列表和费用详情中的案号、合同号、客户编号/名称可跳对应详情。
- 已预留但尚需下一批逐列替换的入口：用印中心列表/详情中的关联案号和合同号、仓库/证物列表中的关联案号、文档中心中文档关联对象，以及旧源代码中“上传文件、生成操作、更多操作、批量修改、新增费用/内部费用”等复合按钮的全量状态和权限对齐。
- 验证结果：完整执行 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 已通过，包含前端生产构建、Python 编译、菜单覆盖审计、前端 API 覆盖审计、18 组 API 冒烟、测试数据清理和 Docker Compose 配置检查，最终输出 `VERIFY_LOCAL_OK`。本轮不能声明整站最终验收完成，只证明第一批跨模块对象关联入口已闭环且未破坏现有验证门禁。

## 2026-07-22 跨模块关联入口统一整改完成

- 用户确认的清单已按“编号/蓝色文字/关联对象必须能进入对应业务详情”统一整改：首页看板案号、全局搜索、消息通知、案件详情客户/合同、合同详情客户/案号、财务案号/合同号/客户、客户合同数量/民事案件数量、用印案号/合同号、收发文案号、调查线索案号、任务沟通案号、仓库证物案号、平台财务案号/合同号、审计日志业务编号均已补为可携带目标上下文的跳转入口。
- 新增轻量前端导航上下文：`taskDetailNavigation.ts`、`investigationDetailNavigation.ts`、`customerRelationNavigation.ts`；原有案件、合同、客户详情上下文继续使用。目标页消费上下文后会自动定位并打开详情或带筛选进入关联清单，避免“只跳菜单、不打开对象”。
- 客户列表“合同数量/民事案件数量”不再单纯跳“我的合同/我的案件”，而是写入当前客户上下文并进入公司合同/民事案件列表，自动按当前客户筛选。
- 全局搜索与通知中心已补任务、线索、公证、证据的目标上下文；任务中心进入后可自动打开沟通详情，调查中心进入后可自动打开对应调查记录编辑/详情办理入口。
- 调查中心原 `尚无可用办理接口` 兜底文案已移除；现有按钮清单内动作均有对应分支，异常配置只提示“未配置在当前页面动作清单”，避免把未闭环按钮伪装成可办理入口。
- 审计脚本已同步更新客户数量跳转断言，防止门禁继续要求旧的“只跳列表”行为。
- 验证结果：完整执行 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 已通过，包含前端生产构建、Python 编译、菜单覆盖审计、369 个前端 API 映射检查、18 组 API 端到端冒烟、测试数据清理和 Docker Compose 配置检查，最终输出 `VERIFY_LOCAL_OK`。本轮证明上述跨模块关联入口已完成并通过自动门禁；整站最终业务交付仍需继续按旧系统按钮全量清单验收。
- 复核补充：通用 `BusinessPage` 兜底详情中的客户、案号、合同号、线索号字段也已接入同一套业务详情导航；补充后重新执行完整 `verify-local.ps1`，仍输出 `VERIFY_LOCAL_OK`。
# 2026-07-24 OA 公证导入浏览器验收（历史记录，已闭环）

- [已通过浏览器验证] 测试线索 `XS20260724082809` 已在测试服务器按真实页面操作完成“待审批 → 审批通过 → 待取证 → 已取证 → 建立公证”。取证日期为 2026-07-24，公证记录为 `GZ20260724164025094375`。
- [已通过浏览器验证] 在“公证管理”中为该记录登记公证书号 `GZ-TEST-20260724-001`、签发日期 2026-07-24 与存放位置“上海档案室 A-01”；列表回显并显示成功提示。
- [已通过浏览器验证并部署] 发现旧接口仍由文件名推断编号后，已提交并推送 `2ab878c`：公证书/发票文件接口改为强制接收页面填写的显式编号，页面说明同步改为“文件名不参与编号匹配”。测试服务器 API、Web 已切换至 `20260724-165019` 镜像并健康。使用文件名不含编号的 `notary-certificate-20260724.pdf`，页面成功提示“按公证书号匹配 GZ20260724164025094375 并上传”，列表显示该文件。
- [已通过浏览器验证] 在“公证书号仓库信息导入”页面上传 CSV，为同一公证记录真实登记发票号 `FP-TEST-20260724-001`；页面提示“成功更新 1 条公证仓库信息”并回显线索号、公证书号和发票号。随后以文件名不含发票号的 `notary-invoice-20260724.pdf` 完成发票文件导入，页面提示已按发票号匹配 `GZ20260724164025094375` 并上传。
- [已通过浏览器验证] “公证文件查询”页面回显该公证记录、发票号、线索号 `XS20260724082809`、权利人及公证书号；当前列表按公证记录聚合，只显示一行，尚未证明其是否逐附件展示两份文件名。
- [已通过只读核验] 数据库确认公证记录 `GZ20260724164025094375` 的 `certificate_no`、`invoice_no`、来源线索均正确；两份附件分别为“公证书扫描件 / notary-certificate-20260724.pdf”和“公证发票 / notary-invoice-20260724.pdf”，且备注明确为显式编号匹配。工作流从提交线索、审批、取证、建公证、登记公证书到两次导入均有完整事件证据。
- [测试数据已清理] 仅删除本轮精确创建的 5 条业务记录、2 个附件、14 个流程事件、1 条通知及两个服务端文件；复查记录、附件、事件、通知均为 `0`，本地两个 PDF 和 CSV 临时文件也已移除。
- [历史状态，已闭环] 当时“公证文件查询”尚按公证记录聚合。该缺口已由后续“2026-07-25 公证附件查询与调查关联查看补证”完成：查询改为附件逐条展示，并已通过浏览器验证后精确清理测试数据；本条不再作为待办。

## 2026-07-24 财务内部费用创建前置校验

- [已完成] 浏览器在测试服务器“财务中心 -> finance-fees”创建内部费用草稿时，发现“关联案号”前端可留空，但后端提交审批会按全部费用的共通规则拒绝缺少关联案号，造成草稿可保存却无法继续流转。前端创建表单现将“关联案号”设为必填，并沿用后端有效案件校验，不放宽财务专用流程。
- [已验证] `npm --prefix apps/admin-web run build`、`python -m compileall apps/api-server/app scripts` 和 `python scripts/audit-menu-coverage.py` 已通过；菜单审计新增断言，防止费用创建页回退为可空案号。提交 `f273477` 已推送至测试服务器 `dev`，仅更新 Web 镜像为 `sunhold-web:20260724-173500`，保留 API 热修镜像；容器健康。浏览器重新打开新增费用弹窗、填写其他必填字段并保存时，页面在创建前显示“请选择或填写关联案号”，未产生新数据。
- [测试数据已清理] 测试服务器仅创建的草稿 `FY20260724171830911747`（数据库记录 ID `33`）及其唯一工作流事件已按精确 ID 删除；复核该记录计数为 `0`。
- [历史状态，已闭环] 内部费用的审批、付款包与核销闭环已在下一节“2026-07-24 财务内部费用付款与核销页面验收”完成；归档费专用链路已在后续“2026-07-25 归档费联动完整页面闭环验收”完成，均含浏览器证据和精确清理，不再作为待办。

## 2026-07-24 财务内部费用付款与核销页面验收

- [已完成并验证] 在测试服务器按真实页面操作创建临时案件 `SHMS2600002`，完成立案审批通过；案件状态由“待立案审批”进入“新案待分配”，页面确认固定任务已生成。
- [已完成并验证] 创建内部费用 `FY20260724180331211376`（100.00 元，关联 `SHMS2600002`），依次完成保存草稿、提交审批、审批通过；待付款列表正确显示收款人、金额、案号及案件阶段。
- [已完成并验证] 在待付款列表勾选该唯一记录并生成付款包 `P260724-06467362`，付款单预览正确回显案件、收款人、明细金额与备注；提交后进入待核销列表。
- [已完成并验证] 在付款核销弹窗录入单据号 `V20260724-FINANCE-ACCEPTANCE`、付款方式“自动扣款”和核销备注后，已核销列表正确回显付款包、100 元金额、状态“已付款”、单据号及备注。服务端只读核验确认费用记录含 `payment_status=已付款`、付款包号、`writeoff_status=已核销` 和核销凭证；工作流事件完整记录创建、提交、审批、打包与核销。
- [测试数据已清理] 已精确删除本轮客户（ID 34）、合同（ID 35）、案件（ID 36）、费用（ID 39）、付款包（ID 40）以及唯一通知、13 条工作流事件和唯一合同附件；数据库复查四类残留均为 `0`，本地临时 PDF 已移除。既有已审批合同 `HT20260721163139` 未修改。
- [历史状态，已闭环] 本轮费用付款核销与归档费专用链路原本分段验收；归档费的案件归档、回款分配、结算、支付审核已在后续“2026-07-25 归档费联动完整页面闭环验收”通过真实页面完成并精确清理。

## 2026-07-24 归档费联动页面造数（历史记录，已闭环）

- [已通过浏览器验证] 为归档费联动链路创建了唯一测试客户 `归档费联动验收客户-20260724`（已知数据库 ID `41`），并在“合同新建”页上传临时 PDF、创建合同草稿 `HT20260724184142`。合同草稿页真实回显客户、合同名称和草稿状态。
- [已通过浏览器验证] 当前环境无既有合同审批人时，管理员在合同提交页创建临时审批账号 `archive_fee_contract_184142`，将其自动选中并提交合同审批；使用该账号首次登录修改密码后，在“待审批合同”页面通过当前审批节点，审批意见为“归档费联动页面验收通过”。列表随后移除待办，审批详情显示第 1 级已通过。
- [已通过浏览器验证] 切回管理员后在“新建案件”页选择已审批合同、锁定对应客户、选择案由“侵害商标权纠纷”、填写当事人与备注，创建案件 `SHMS2600002`，初始状态为“待立案审批”。
- [历史状态，已闭环] 该批早期测试数据未作为最终证据继续使用；后续已从新的精确标识数据完成全部页面闭环。最终结果与清理范围见“2026-07-25 归档费联动完整页面闭环验收”。

### 2026-07-24 后续进展

- [已通过浏览器验证] 临时案件 `SHMS2600002` 已在案件列表中执行“立案审批通过”，页面回显案件阶段和归档状态均由“待立案审批”转为“新案待分配”。
- [已通过浏览器验证] 在“新增回款”页面为测试客户、合同 `HT20260724184142` 登记 1000.00 元银行转账回款，银行流水号 `BANK-ARCHIVE-FEE-20260724`。再次提交时页面提示“银行流水号已经登记”，证明首次提交已经真实落库而非前端假成功。
- [历史状态，已闭环] 当时 `finance-receipts-manage` 的渲染异常已在后续修复后解除；2026-07-25 的归档费完整链路已通过该类回款、分配和结算页面完成并清理。当前会话登录失效前的页面复核也未出现该错误边界。
# 2026-07-25 案件归档入口可点击性修复

- [已完成并已验证] `案件中心 -> 全所案件` 底部的“更多操作”改为显式点击触发，避免下拉菜单只依赖悬停而使“案件归档”专用入口无法稳定打开。
- [已验证] 已通过前端生产构建、Python 编译和菜单覆盖审计。归档费联动的页面验收将继续从该专用入口完成案件办结、归档申请、归档审核和归档费支付审核，完成后清理本轮临时数据。

# 2026-07-25 归档关联边界修复（历史记录，已闭环）

- [已完成并已部署] 归档检查、案件办结和案件任务读取统一优先按持久化 `case_id` 关联；只有旧数据缺少案件 ID 时才回退按案号关联。此前两个不同案件复用同一案号时，历史任务会错误出现在当前案件的归档阻断与任务弹窗中；现已提交并推送 `c0779a6`、`89c92a9`，测试站 API 已切换至 `sunhold-api:20260725-071941` 且健康。
- [已验证] 页面选择临时案件后，点击“更多操作”可见并可进入“案件归档”；归档检查正确显示费用结清、财务完成等真实核验项。原有办结操作被任务前置规则阻断，未产生错误状态变更。
- [历史状态，已闭环] 后续已在页面完成固定任务、案件办结、归档审核和归档费支付审核；最终闭环和精确清理证据见“2026-07-25 归档费联动完整页面闭环验收”。
# 2026-07-25 归档费联动续验：环境状态复核

- [已验证] 测试站管理员页面已发布 Web 修复：`a825d56 fix: allow admin task actions in company queue` 允许管理员在“公司接受的任务”执行接收和完成；`dc0c7f9 fix: classify uploaded case archive materials` 为案件上传入口提供“委托材料、证据材料、诉讼文书、裁判文书”分类，解决归档检查要求四类材料而页面只能上传“案件文件”的闭环缺口。前端构建通过，测试服务器 Web 镜像为 `sunhold-web:20260725-083000` 且健康。
- [已验证] 只读查询测试服务器当前 `business_records` 未发现案件记录；新浏览器会话的“公司案件”列表也为空。因此此前归档费联动临时案件不能继续推进，不能把浏览器仍显示的旧合同向导状态当作可操作数据。
- [历史状态，已闭环] 该续验客户及关联合同、案件、财务、附件、通知和临时账号已用于完成后续完整页面链路，并按精确关联范围清理；最终结果见“2026-07-25 归档费联动完整页面闭环验收”。

## 2026-07-25 归档费联动续验：合同审批权限与案件创建

- [已完成并已部署] 修复合同审批岗位账号的权限载荷：登录和个人资料接口均按用户的岗位权限补充合同中心菜单；同时修复深层 `contract-audit-pending` 路由按规范化父菜单判定权限，避免“合同中心可见但待审批合同被误判无权访问”。提交为 `4e0f30f`、`03a9f9b`，本地前端构建、Python 编译与菜单覆盖审计均通过。
- [已通过浏览器验证] 临时合同审批账号重新登录后可正常进入“待审批合同”页面；审批前列表显示合同 `HT20260725081514`，通过当前审批节点后页面提示成功，重新登录后的待审列表已不再显示该合同。该合同由页面创建，客户为 `归档费联动续验客户-20260725`；全过程未对原 OA 写入。
- [已通过浏览器验证] 管理员从“新建案件”选择已审批合同，客户与案源人自动锁定回填；完成民事案件向导后，`SHMS2600003` 在公司民事案件列表真实回显，状态为“待立案审批”。
- [历史状态，已闭环] `SHMS2600003` 的立案审批、回款及归档费分配、结算、固定任务、四类归档材料、案件办结/归档审核和归档费付款审核均已在后续真实页面完成；客户、合同、案件、账号、附件及关联财务记录也已精确清理。最终证据见“2026-07-25 归档费联动完整页面闭环验收”。

## 2026-07-25 固定任务公司视图流转修复

- [已修复并通过静态门禁] 页面续验发现管理员在“公司发起的任务”选中待接收固定任务时仍显示“完成任务”，该操作会被后端状态机拒绝，且不应作为可办理入口；现仅在任务处于“处理中”时显示该页的完成/转交操作。
- [已修复并通过静态门禁] 任务 API 对管理员保留公司发起视图的全所范围，但“公司接受的任务”和“公司协作的任务”恢复按当前管理员作为负责人/协作人的参与关系筛选，使固定任务进入实际可接收、完成和验收的队列。
- [已验证] `npm.cmd --prefix apps\\admin-web run build`、`python -m compileall apps\\api-server\\app scripts` 和 `python scripts\\audit-menu-coverage.py` 已通过；尚未部署到测试服务器，需部署后从 `SHMS2600003` 的两条固定任务重新执行接收、完成、验收，再继续归档费链路。
# 2026-07-25 归档费联动完整页面闭环验收

- [已完成并通过浏览器验证] 测试站按真实页面完成 `回款分配 → 结算申请/审核/付款 → 案件办结/归档审核 → 归档费支付审核`。案件 `SHMS2600003` 的归档费 100.00 元在“已支付”页显示为已支付，且页面回显归档号、归档审核信息、支付审核人和审核时间。
- [已发现并修复] 归档检查弹窗原先依赖未渲染的 `Modal.actions`，导致“提交归档审核”入口消失；已改为显式底部按钮并提交 `921cea1`，前端生产构建、Python 编译、菜单覆盖审计均通过，测试站 Web 已发布。
- [已完成清理] 仅按本轮客户、合同、案件、归档费、结算、付款流水、临时审批账号和附件精确删除：8 条业务记录、6 个附件及服务器文件、3 条测试回款、2 条财务流水、4 条通知和 1 个临时账号；复核业务记录、附件、回款、流水、通知和账号残留均为 0。本机 5 个归档材料/付款凭证临时文件也已移除。
- [注意] 归档费待支付页面首轮数据加载约需 4 秒；过早读取会暂时显示空表，复验时须等待数据加载完成，不能据此判断归档审核状态未落库。

## 2026-07-25 公证附件查询与调查关联查看补证

- [已通过浏览器验证] “公证文件查询”现按附件逐条显示：同一公证记录的 `notary-certificate-250725D.pdf`（公证书扫描件）和 `notary-invoice-250725D.pdf`（公证发票）分别显示为两行，并各自回显线索号、权利人、公证书号、发票号和导入时间；此前“只能按公证记录聚合、未证明文件名”的遗留项已闭环。
- [已完成清理] 上述带“公证文件查询链路验证-20260725”标识的临时线索（ID 61）、公证记录（ID 62）、附件（ID 18/19）、10 条流程事件及两份服务器上传文件已按精确 ID 和路径删除；删除前只读核对未发现通知或其他关联记录。
- [已完成] 调查任务、调查子任务和线索列表中的编号均可点击读取并打开真实调查详情；权利人点击前会查询对应客户档案，存在且有权限时进入客户详情，不存在则明确提示。公证文件查询中的线索号、案号和权利人也分别进入调查、案件和客户详情，不再是没有后续的文本字段。
- [已验证] 本地前端生产构建、Python 编译和菜单覆盖审计通过；提交 `d5a6ba1` 已同步测试服务器并重建 Web。管理员页面点击 `DC20260725081645` 后实际打开“调查详情”弹窗，字段来自接口真实记录；测试完成后该父调查记录（ID 59）及 2 条流程事件也已精确清理并复核为 0。
- [已通过浏览器验证] 管理员在“我发布的调查任务”创建临时记录 `DC20260725131325` 后，列表中的调查编号呈可点击按钮；点击实际请求 `GET /records/69` 并打开只读“调查详情：DC20260725131325”，没有进入“修改调查记录”表单。记录回显的权利人、权利类型、授权日期和调查区域均来自接口返回。
- [已完成清理] 上述临时调查记录（ID 69）及其唯一工作流事件（ID 186）已按精确 ID 删除；已逐项核对全部引用表无附件、财务、沟通、审批、任务或其他关联记录，删除后业务记录与工作流事件残留均为 0。
- [已完成并已部署] 线索数据已持久化 `source_task_id/source_task_no`，但列表此前未展示来源调查任务，导致这项跨页关联不可消费。现新增“来源调查任务”可点击列，并将目标消费扩展为任务模块：写入详情目标后进入“我发布的调查子任务”并打开同一只读详情弹窗；提交 `088434d` 已同步测试服务器并重建 Web。
- [已通过浏览器验证] 管理员通过真实页面创建父调查 `DC20260725133737`、子任务 `RW20260725214641836288` 与线索 `XS20260725135206`。在线索“待提交线索”列表点击该线索的来源任务编号后，实际跳转到“我发布的调查子任务”并自动打开只读“调查详情：RW20260725214641836288”，未进入修改表单。
- [已完成清理] 本轮临时父调查（ID 70）、子任务（ID 71）、线索（ID 72）、4 条工作流事件和 1 条通知已按精确 ID 清理；复核业务记录、工作流、附件、财务、智能体文档、通知、应收、庭审、审批与沟通记录残留均为 0。

## 2026-07-25 公证文件显式编号冒烟门禁修正

- [已完成] 服务端公证书/发票文件导入已要求页面提交显式公证书号或发票号，不再允许从文件名推测编号；冒烟脚本此前仍沿用旧的“仅上传带编号文件名”请求，无法证明现行接口契约。现已改为：缺少编号分别断言 422、带显式公证书号/发票号的非编号文件名可分别导入、填写不存在编号断言 422。
- [已验证] `python -m compileall -q apps/api-server/app scripts` 与 `python scripts/audit-menu-coverage.py` 通过。本机 API 健康；完整 `scripts/smoke-api.py` 未运行，因为本机未配置 `SMOKE_PASSWORD`，脚本在启动前即安全退出且未产生测试数据。测试站已有页面端到端导入、逐附件查询与精确清理证据，见上节；恢复隔离冒烟账号配置后应补跑完整脚本。

## 2026-07-25 用印关联合同详情可点击性修复

- [已完成并已部署] 用印申请列表的合同号此前被右侧固定“查看”操作列覆盖；视觉上仍显示合同号，但指针命中操作列而不会执行合同详情跳转。现已取消该宽表操作列的固定定位，保留横向滚动，避免合同号、案号及后续关联字段落在不可点击的遮挡层下。
- [已完成并已部署] 合同详情目标改为应用级事件传递，并保留会话存储作为刷新回退；会话存储不可用时不再阻断关联跳转。合同页加载到目标记录后打开真实只读“合同查看”弹窗并清理一次性目标，覆盖用印、案件、财务、客户、审计日志等现有合同关联入口。
- [已通过浏览器验证] 管理员在测试站“用印查询”使用既有用印记录 `YY20260721152059B02` 点击合同号 `HT20260721114720`，实际进入公司合同并打开只读 `合同查看：HT20260721114720` 弹窗；未修改、创建或删除任何业务数据。
- [已验证] `npm.cmd --prefix apps\\admin-web run build`、`python -m compileall -q apps\\api-server\\app scripts` 与 `python scripts\\audit-menu-coverage.py` 均通过；测试站 Web/API 容器均为 healthy。相关提交 `62b7526`、`cbde865`、`012166a` 已推送并部署。

## 2026-07-25 智能文档关联字段遮挡预防

- [已完成并已部署] AI 智能文档宽表的“关联业务”可点击字段此前位于右侧固定操作列之前，存在与用印列表相同的覆盖风险。已取消该表操作列固定定位，保留横向滚动，关联编号不再被操作区域遮挡。提交 `34b5e40` 已同步测试站，Web/API healthy。
- [仍未证明] 测试站“AI 智能文档”当前无任何记录，无法使用已有业务数据做浏览器点击回归；本轮未创建测试记录。后续出现真实关联文档后，应验证按钮命中自身并进入对应只读业务详情，再将本项改为已验证。

## 2026-07-25 同类点击与遮挡修复纪律固化

- [已完成] 应用户要求，已将“相同改动必须一并处理”的要求补充进仓库级 `AGENTS.md`：凡涉及可点击字段、命中区域、表格固定列、滚动容器或浮层层级的修复，均须审计所有同布局模式和同语义字段入口；宽表右侧操作列不得遮挡左侧关联字段。
- [已验证] 该规则与既有的“关联对象必须可点击进入真实只读详情、同语义入口同步修复”规则并行执行，明确了布局遮挡类问题不能只针对当前页面或截图修补。
- [仍未证明] 此次为研发纪律与交接规则更新，不产生业务数据，也不替代后续各页面的实际浏览器点击回归。

## 2026-07-25 智能文档公证与证据关联跳转补全

- [已完成] 审计发现 AI 智能文档页面的关联跳转已声明支持公证与证据，但关联记录加载仅查询案件、合同、客户和线索，导致关联到公证或证据的文档必然提示“未找到关联业务”。现已将 `notary` 与 `evidence` 纳入同一真实记录加载范围，使关联字段可定位目标并沿用调查详情的只读导航。
- [已完成] 关联记录列表每类仅加载前 100 条，仍可能遗漏较早的目标。关联跳转现优先按智能文档持久化的 `record_id` 请求真实详情；列表查询仅作无 ID 的兼容回退，因此不会因分页截断误报目标不存在。
- [已验证] `npm.cmd --prefix apps\\admin-web run build`、`python -m compileall -q apps\\api-server\\app scripts` 和 `python scripts\\audit-menu-coverage.py` 已通过；API 冒烟脚本因本机未配置 `SMOKE_PASSWORD` 在创建数据前安全退出。
- [已验证] 菜单覆盖审计新增 `AGENT_DOCUMENT_RELATION_OK`：持续断言六类关联记录的加载范围、按 `record_id` 的直读回退和公证/证据只读调查导航，防止本轮修复在后续重构中回退。
- [仍未证明] 测试站浏览器登录会话已失效，未创建仅用于布局验证的智能文档测试数据。重新登录后，应使用存在关联公证或证据的真实文档验证点击命中和只读详情目标，再将本项更新为已通过浏览器验证。

## 2026-07-26 测试站登录验收规则

- [已完成] 应用户明确要求，仓库级 `AGENTS.md` 现要求：测试站页面验收需要认证时，Codex 在已授权范围内自行登录后继续验证；密码、令牌及其他秘密不得写入仓库、交接记录、提交信息或回复内容。
- [已修复并已通过浏览器验证] 经只读核对，管理员账号存在、启用且未锁定，认证失败原因为测试站管理员凭据与授权测试凭据不匹配。现已仅在测试站恢复管理员认证状态；内置 Codex 浏览器已实际登录并进入管理员控制台，未改动任何业务记录。密码、令牌及其他秘密均未写入仓库、交接记录、提交信息或回复内容。

## 2026-07-26 调查任务专用创建入口修复

- [已完成，待测试站浏览器回归] 排查“我发布的调查任务”点击新建后保存无请求的问题，确认专用弹窗状态 `investigationCreateOpen` 已被打开，但组件未渲染对应 Modal，导致用户无法进入保存动作。现已补齐“新建调查任务”专用表单：调查编号、负责人、调查事项、权利类型、授权起止日期、调查区域及客户审核选项均由真实 `/investigations/records` 接口提交；结束日期早于开始日期会在前端明确阻断。
- [已验证] `npm.cmd --prefix apps\admin-web run build`、`python -m compileall -q apps\api-server\app scripts` 和 `python scripts\audit-menu-coverage.py` 通过；菜单审计新增 `INVESTIGATION_TASK_CREATE_OK`，防止专用弹窗、授权日期字段或日期顺序校验回退。
- [仍未证明] 测试站管理员认证现已恢复，浏览器已验证可登录；尚未在新部署测试站用真实页面完成创建、关联智能文档、点击进入只读详情与精确清理；不得据此宣称该浏览器闭环已经完成。

## 2026-07-26 调查任务创建、详情与清理闭环

- [已通过浏览器验证] 管理员在测试站真实页面创建调查任务 `DC20260725194337`（数据库记录 ID `73`），列表成功回显；点击调查编号实际打开只读“调查详情”弹窗，服务器日志确认 `GET /api/v1/records/73` 返回 200，详情字段来自真实接口。
- [已完成并部署] 未开始调查任务新增管理员专用“删除”动作；后端 `POST /api/v1/investigations/batch-delete` 仅允许管理员删除“待分配”且无子任务/关联业务的调查，拒绝其他状态和有关联记录的删除。修复提交为 `2096a2b`，测试站 Web/API 已部署并健康。
- [已通过浏览器验证] 浏览器重新载入最新 Web 包后勾选 `DC20260725194337`，点击“删除”、确认弹窗，页面提示“已删除 1 条记录”，列表刷新为空。服务器数据库只读复核：`business_records`、`workflow_events`、`file_attachments`、`agent_documents` 和 `notifications` 对该 ID/编号均为 `0`；未删除其他用户数据。
- [仍未证明] 智能文档关联闭环仍待后续使用真实调查子任务/线索数据验证；本轮仅完成调查任务创建、只读详情与精确清理，不将智能文档关联标记为已完成。
## 2026-07-27 本地用印关联、管理员任务范围与标签标题整改（待推送测试服务器）

- 已完成并在本地浏览器验证：管理员从“申请用印”页面创建合同用印草稿，合同号与用印类型会真实保存；用合同号查询可返回该草稿。点击不存在的关联合同会明确提示“未找到关联合同或当前账号无权查看”，不会静默跳转或假装打开详情。
- 已完成并在本地浏览器验证：用印草稿列表提供“删除”操作；仅允许删除无附件的草稿，确认后删除对应专用工作流事件和草稿记录。上述临时草稿已从页面删除，列表为空。
- 已完成：用印列表筛选已改为服务端查询，覆盖申请编号、申请人、日期、案号、合同号、客户、用印类型、文件名和状态；创建与修改接口持久化合同号、用印类型，避免草稿保存后关联丢失。
- 已完成：管理员在任务“发起/接收/协作”三种关系视图中，无论从“我的”“部门”还是“公司”入口进入，都保持全所数据范围；普通用户的参与人过滤仍保留。该问题由完整冒烟测试发现并修复。
- 已完成并在本地浏览器验证：深层公证导入路由的工作区标签不再显示内部键名 `notary-import-info`，现显示“公证信息导入”；其他四个同类导入/查询路由也补齐中文标签。
- 本地验证：`verify-local.ps1` 全部通过（前端构建、Python 编译、菜单与客户端 API 覆盖、18 组 API 业务闭环、测试数据自动清理、Compose 配置）；本地 API、Web、Worker 已重建，核心依赖健康。尚未提交或发布到测试服务器。
- 仍未证明：真实存在的合同号点击后进入合同详情的浏览器证据；本轮使用的合同号仅用于验证“目标缺失时明确阻断”。全站逐页面、逐角色、逐状态和逐按钮验收仍未完成。
## 2026-07-27 PPT 对照台账与智能文档公证关联运行版复核

- 已读取只读业务证据 `思法汇成法律服务机构管理系统2022.pptx`：共 89 页、39 页含操作截图；已新增 `docs/思法汇成法律服务机构管理系统2022_PPT对照台账.md`，按客户、合同、调查、公证、案件、任务、控制台、财务和归档列出 PPT 页码、页面入口、关键业务规则与当前验收方法。
- 已通过测试站管理员浏览器只读复核：父调查 `DC20260726123001`、子任务 `RW20260726055721981317`、线索 `XS20260725215905` 与公证 `GZ20260726062422621954` 均仍真实存在；公证处于“等待材料”。
- 发现运行版缺口：AI 智能文档“生成文档 → 关联业务”下拉只显示案件、合同等对象，未显示上述真实公证记录，因此当前运行版无法从页面创建“关联公证”的智能文档任务。本次只打开选择器并读取可见选项，未提交表单、未创建或修改业务数据。
- 本地当前 `AgentDocumentPage` 已包含 `case/contract/customer/clue/notary/evidence` 六类记录加载与按 `record_id` 直读回退；仍未完成本地浏览器页面创建数据的真实证明，且当前测试站运行版本尚未证明已包含该行为。后续必须在本地页面完成“创建链路 → 创建智能文档 → 点击关联详情 → 专用页面删除/只读核验”，完整验证后再提交、推送和发布。
- 仍未证明：AI 文档关联公证/证据的浏览器闭环、Dify 实际生成/人工确认/附件回写，以及 PPT 各页逐角色、逐状态、逐按钮的最终验收。
## 2026-07-28 任务反馈附件闭环（本地，待浏览器页面回归）

- 实现范围：`TaskCenterPage.tsx`、通用附件接口的任务专用权限分支，以及 `scripts/smoke-api.py`。任务参与人（发起人、负责人、协作人、管理员）可提交反馈并多文件上传、查看和下载反馈附件；旁观者不显示会被服务端拒绝的反馈/未读操作。
- 服务端保障：任务附件只能以“任务反馈附件”分类写入；上传、列出、下载均校验参与人。上传后写入 `上传任务反馈附件` 工作流事件，并使用现有任务消息通知机制通知参与人。
- 验证状态：前端 build、Python 编译、API Docker 重建和健康检查已通过。完整本地验收本轮在既有 `/customers?scope=recent_update` 断言处失败（早于新增任务反馈段），不是本改动的失败证据；需要在该不稳定验收前置项恢复后重跑 `verify-local.ps1`，并补本地浏览器真实页面“提交反馈 + 多附件 + 下载 + 旁观者只读”证据后，才可把本项标记为完全验证。
# 2026-07-28 任务撤回 P0 补齐（本地，待完整验收）

- 已增加任务专用撤回接口和前端两个真实入口（列表、任务详情）。撤回不经过通用记录流转：发起人/admin 只可撤回待接收/处理中任务，原因必填，服务端写状态、事件、参与人通知并清空后续自动处理时点。
- 已补脚本断言：普通发起人撤回待接收任务、管理员撤回处理中任务、负责人越权拒绝、空原因拒绝、重复撤回拒绝、通用流转无法绕过；临时记录复用现有冒烟精确清理列表。
- 已验证：API 重建后 `verify-local.ps1` 全量通过（含 18 组 API 闭环和 `SMOKE_CLEANUP_OK`）。验收前发现一名此前遗留、明确标识为临时调查员的测试账号，已按其精确系统用户 ID 经受控系统接口删除；重跑后零残留。
- 当前待办：以浏览器用页面创建临时任务并检查列表/详情入口、原因校验和已撤回页签；未经浏览器证据不得将页面闭环标为“已证明”。
# 2026-07-28 收发文/模板未验收缺口审计与官文收文日期专用整改（本地）

- [已审计] 以归档旧源码 `SH.CRM.WEB/Areas/FIO/Controllers/LegalOfficialFileController.cs` 与 `Views/LegalOfficialFile/MetaFileList.cshtml` 为证据核对收发文、附件与模板：官文收文的有效批量动作包括删除和“修改收文日期”；文件查询/附件仅有查询、上传、下载、删除，未发现单条附件业务详情；现有模板的创建、编辑、启用/停用、删除已覆盖，未补造没有旧站证据的模板版本流转或单条详情。
- [已修复] 新系统原“修改收文日期”虽有入口，却仅支持单条并经由通用 `/records/{id}` 更新，无法保持专用业务边界。现改为可选择多条官文收文的 `POST /documents/official/receipt-date`：逐条校验收文类型、数据范围与负责人/同部门负责人/管理员权限，只更新 `document_date/received_at`，独立写入“修改官文收文日期”审计事件，绝不改变待登记、待签收、已签收、已归档生命周期。
- [已验证自动化] 前端生产构建、Python 编译、菜单覆盖审计与差异检查通过；审计脚本新增 `OFFICIAL_RECEIPT_DATE_OK`，冒烟脚本补日期修改后“状态不变、日期更新、审计事件存在”的断言。
- [仍未证明] 遵照“已验收流程不反复验收”的要求，本轮未重跑既有收发文/模板闭环，也未创建页面临时数据。待下一次只针对这个新批量动作的页面验收时，用临时收文验证多选、权限拒绝、状态不变与精确清理后再标为页面已证明。
## 2026-07-28 人事／系统账户未验收功能缺口审计（本地）

- [已完成代码] 对归档旧源码 `Areas/System/Views/User/CreateUpdate.cshtml`、`Scripts/System/SystemManagement/User.js` 和 `SystemManagementController` 做只读审计。旧系统员工资料字段、请假记录、用户文档、提成设定、部门/岗位及账号修改，在新系统已有对应的完整员工资料、四类附属记录、组织管理与账号/角色配置，未发现需要伪造的新模块。
- [已补齐明确缺口] 旧系统账号列表存在独立“重设密码”动作；新系统现增加管理员专用 `POST /system/users/{id}/reset-password` 与“系统用户管理 → 重置密码”弹窗。管理员必须两次输入临时密码；接口复用安全策略最小长度校验、拒绝与原密码相同的值，清除失败次数/登录锁定，并强制该账号下次登录修改密码。资料修改中的密码字段仍是可选的资料内修改，二者不再混淆。
- [已验证静态] Python 编译、前端生产构建、菜单覆盖审计、客户端 API 路径覆盖和 `git diff --check` 均通过；冒烟脚本已补“重置后锁定清除 → 首次登录受限 → 改密后恢复”断言。本轮遵循“已验收不重复验收”要求，不重跑既有人事状态、账户同步或跨角色链路，也未进行浏览器数据操作。
- [仍未证明] 新增“重置密码”弹窗的浏览器点击及临时账号首次改密页面证据待后续一次新增账号页面验收时补录并精确清理；不得把本次静态构建写成既有人事流程的重复页面验收。
## 2026-07-29 系统账号首次改密后端防绕过（本地）

- 已修复：`POST /system/users` 不再信任客户端提交的 `must_change_password=false`。管理员通过系统用户页或直接调用兼容接口创建的任何账号，均固定为一次性初始密码并必须在首次登录时修改；用户自行改密后才解除业务 API 的 428 阻断。
- 自动化证明：冒烟测试显式以旧客户端格式提交 `must_change_password=false`，服务端仍返回 `true`；随后覆盖首次改密、重置后再次强制改密、当前管理员不得重置自己密码，以及多个角色完成首次改密后继续权限/数据范围测试。完整 `verify-local.ps1` 已通过（19 组 API 闭环、零测试数据残留）。
- 测试脚本同步修复：角色/数据范围测试中的临时账号会先完成一次强制改密，再执行各自业务断言；员工登录真实性检查后会明确恢复管理员会话，避免把权限拒绝误判为人事功能故障。
## 2026-07-29 合同事项归档阻断页面证据（本地）

- 已通过内置浏览器页面验证：从“我的合同”打开一份既有的“已归档”合同详情，事项记录区域显示真实入口；填写事项内容后点击“保存”，页面明确提示“已归档合同不可新增事项记录”，事项列表仍为空。该操作未写入任何合同事项或修改既有业务数据。
- 证据边界：本条仅证明归档状态的专用阻断，不把它替代为正常合同的“新增事项—时间线回显”闭环。后者必须以可精确清理的临时合同完成，不能在用户既有合同上制造测试事项。
## 2026-07-29 人事员工宽表窄内容区布局修复（本地）

- 已修复：员工管理页在侧栏展开、内容区较窄时，查询条件仍错误按多列分配，用户名、中文名和手机号输入框被压缩；现改为按实际容器宽度自动换行，所有查询输入框保持完整可用宽度。
- 已修复：员工表统一保持完整列宽、文本省略和底部横向滚动，右侧操作列不再与左侧员工字段重叠。页面实测显示员工号、用户名、中文姓名、部门等列均完整可读，剩余列可通过滚动访问。
- 防回归与验证：菜单审计新增 `HR_NARROW_LAYOUT_OK`，锁定查询容器、输入框包装层和宽表滚动契约；完整 `verify-local.ps1` 已通过（前端构建、Python 编译、菜单审计、19 组 API 闭环、零测试数据残留和 Compose 配置）。
- 页面入口复核：管理员在员工列表逐项点击既有 `admin` 行的“查看”“修改”“办理状态”。查看打开真实员工详情；修改打开完整资料表单，包含基础资料、执业资料、日期、提成及系统角色字段；因该账号尚未建立独立人事档案，办理状态明确提示需先补建档案，并未产生任何状态写入。
## 2026-07-29 AI 智能文档关联入口页面验收（本地）

- [已验证] 使用页面在“AI 智能文档”创建一条可编辑提纲，选择有效模板“案件归档目录”并关联现有案件 `SHMS2600388`；Dify 未配置时页面明确保持“待配置（可编辑提纲）”，没有伪造正式文书生成成功。
- [已验证] 列表中的关联业务不是纯文本，呈现为可点击案件按钮；点击后已写入案件详情目标，进入公司案件页后实际打开对应 `SHMS2600388` 案件详情。该页面创建的临时智能文档已按唯一任务编号精确删除，并由 API 复查无残留。
- [仍未证明] 当前本地可见数据仅有案件候选，公证和调查证物候选为空；公证/证物与 AI 文档的同类页面点击证据尚不能以现有安全样本取得。后续只能从页面走“线索→取证→建立公证/证据目录”的最小临时路径后补证，并精确清理；不得把本次案件关联验收扩大写成公证/证物已验收。
## 2026-07-29 审计角色首次登录与清理页面验收（本地）

- [已验证] 管理员在“系统用户管理”页面创建临时审计角色账号；首次以该账号登录时，系统不进入任何业务页面，而是弹出“首次登录修改密码”并要求填写原一次性密码、新密码和确认密码。
- [已验证] 成功修改一次性密码后才进入审计角色控制台；随后管理员在同一系统用户页面删除该临时账号，并复查账号列表不存在该用户名。全程未修改用户既有账号或业务数据。
- [范围] 本条只证明审计人员的首次登录强制改密和管理员清理路径；经理、普通用户、人事状态联动及跨角色业务审批仍按未重复验收原则保留在后续独立清单。
## 2026-07-29 普通用户首登与系统管理拒绝页面验收（本地）

- [已验证] 管理员页面创建临时普通用户；首次登录同样先被一次性密码修改门禁拦截，成功改密后才可进入普通用户控制台。
- [已验证] 登录后直接访问 `system-users` 深层路由，页面显示“无权访问 / 当前角色没有该功能的菜单权限”，没有泄露系统用户列表或管理动作。
- [已验证] 管理员重新登录后从系统用户页面删除临时普通用户，并由页面列表复查用户名不存在。未修改既有账号和业务数据。
# 2026-07-30 旧/新代码全量差异整改：批次 A（本地，待页面验证）

- [已完成代码] 系统参数新增客户类型、案件文件类型、地区和法院工作人员四类主数据；管理员可从系统参数页面维护，法院工作人员必须绑定一个当前启用的法院代码及职务，服务端拒绝孤立人员记录。
- [已完成代码] 客户创建和筛选不再写死“客户/当事人”；案件附件分类改由案件文件类型主数据提供，服务端拒绝停用或不存在的案件文件类型。
- [已完成代码] 刑事案件新建的各审级法院、法官、书记员改为受控选择：法官/书记员候选随所选法院过滤；服务端同时校验启用法院、所属法院和职务，不能用直接接口提交跨法院或错误职务的人员。
- [已验证静态] `apps/admin-web` 生产构建、`python -m py_compile apps/api-server/app/main.py`、`git diff --check` 均通过。
- [仍未证明] 尚未为本批新增真实测试数据，也未做页面创建、跨法院拒绝和精确清理；因此不得标记为页面闭环完成，亦未部署测试服务器。
# 2026-07-30 知识产权案件台账第一阶段（本地，页面已验证）

- [已完成代码] 新增独立 `ipr_case` 模块、专利/商标/立案审核三级菜单、专用 REST API 与 React 台账页面；不通过通用案件或通用记录接口创建、修改、审核、结案或重新开启知识产权案件。
- [已完成状态边界] 新建仅生成草稿；草稿/已驳回才能修改或提交；提交必须有申请号/注册号；管理员或管理人员才可审核；审核通过进入在办；在办才可结案；仅管理员或管理人员可从已结案重新开启。每个命令均写入 `WorkflowEvent`。
- [已验证页面] 管理员在“专利案件”页面创建临时草稿 `SMOKE-IPR-PATENT-0730`，经“提交审核 → 知识产权立案审核通过 → 结案 → 重新开启”逐步回显正确状态；菜单、路由、表格、审核页与操作按钮均可用，未出现白屏。
- [已精确清理] 仅删除该页创建的唯一临时 `ipr_case`（编号 `ZL2607300115454C7B`）及其级联工作流事件；页面刷新后列表为空，未修改客户或既有业务记录。
- [已验证静态/容器] 前端生产构建、Python 编译、`git diff --check` 通过；API 与 Web 容器已重新构建，API healthy。
- [仍未完成] 商标页面同类闭环、草稿编辑/审核驳回、权限拒绝、附件/官文、年费/费率/期限维护、批量导入导出、专利/商标官费及收据发票、旧 IPR 协作/文件动作仍待按旧代码逐项实现和验证；不得以本阶段宣称知识产权 P0 已完成。
# 2026-07-30 知识产权官文第一阶段（本地，页面已验证）

- [已完成代码] 新增独立 `ipr_official_file` 模块与“知识产权官文”页面；官文只能关联处于在办状态的专利/商标案件，原件、官文类型、官文号、接收日期和期限均以专用 API 保存，不混入普通收发文。
- [已完成状态边界] 导入后固定进入待校验；仅管理员/管理人员可校验并转为待转发；待转发才能转发；已转发才能办结。每步写入工作流事件，关联案件已结案或不在办时禁止校验。
- [已修复页面阻断] 初版在未填写接收日期/期限时错误提交空字符串，FastAPI 正确拒绝。前端现仅在用户选择日期时提交字段，修复后已在页面复验成功。
- [已验证页面] 管理员通过页面新建临时专利案件、提交并审核为在办，再导入 `受理通知书` 原件，依次完成“待校验 → 待转发 → 已转发 → 已办结”；表格、菜单、路由、文件选择和动作按钮均真实调用接口并回显状态。
- [已精确清理] 删除唯一测试案件 `ZL2607300121480519`、测试官文 `GW26073001240782D3` 及级联事件/附件记录；同时从 Docker 上传卷删除唯一临时原件，并删除工作区临时源文件。刷新官文页面后确认无残留。
- [仍未完成] 旧系统的批量解析导入、案号/费用类型自动匹配、检查清单、未导入/历史打包、按官文类别批量转发、ZIP/Excel 导出，以及专利/商标官费、收据、发票专用流程仍待实现；本阶段不能替代这些旧功能。
# 2026-07-30 知识产权官文批量流转与草稿编辑（本地，页面已验证）

- [已修复代码] 根据旧系统 `IPR/CasePatentOfficeFile/MenuList.cshtml` 与 `CaseTrademarkOfficeFile/MenuList.cshtml` 的专利/商标转文菜单，知识产权官文导入页现按关联案件类型提供对应官文类型清单；新增状态筛选、可多选的批量校验/转发/办结、详情抽屉、原件下载及关联知识产权案件回看。批量接口使用专用 `actions/batch` 路径，避免被动态官文编号路由误匹配；后端逐条验证权限、原状态和关联案件状态，任一不满足即整体拒绝，不能用通用记录接口绕过。
- [已修复代码] 页面实测发现“知识产权案件草稿”创建后没有编辑入口，而提交审核必须具备申请号/注册号，用户会被困在草稿。现已补充草稿/驳回状态的列表和详情编辑入口，编辑仍由专用案件接口控制，不能修改已进入审核或在办案件的关键基础归属。
- [已验证页面] 管理员在专利案件页创建临时草稿 `SMOKE-IPR-BATCH-0730`，通过“编辑”补填申请号后提交并在“知识产权立案审核”通过；随后从“知识产权官文”选择专利申请受理通知书并上传临时原件，勾选行后依次点击“批量校验 → 批量转发 → 批量办结”，状态实际依次为 `待校验 → 待转发 → 已转发 → 已办结`。点击官文编号打开详情，真实回显关联案件和原件下载按钮。
- [已精确清理] 仅删除本轮临时案件 `ZL2607300132244AC2`、临时官文 `GW260730013753CF1D` 及其附件 ID `10297` 的容器内原件；数据库复查剩余数为 `0`，未修改任何既有客户、案件、官文或附件。
- [仍未证明] 旧系统的 Excel 官文清单上传/去重比对、历史官文迁移、分类转文统计、官费发票联动、批量导出及各官文类型专属办理动作尚未实现，仍保持为全量差异表 P0/P1 项，不得将本批验收扩大为知识产权官文全部完成。
# 2026-07-30 知识产权案件 DOCX 文书交付（本地，页面已验证）

- [已修复代码] 知识产权案件详情新增专用 DOCX 文书生成：案件信息表、代理授权委托书、律师事务所函、主体身份证明核对单。后端仅允许当前数据范围内的在办或已结案案件生成，DOCX 从已保存案件字段生成，缺失字段明确标为“待补充”，生成文件作为 `知识产权案件文书` 附件落库并写入案件流程日志；不能用通用记录接口绕过。
- [已验证页面] 管理员从页面创建并审核临时专利案件后，在案件详情点击“生成案件信息表（DOCX）”；附件真实回显，下载接口返回 `200` 且文件实体存在。
- [已精确清理] 仅删除临时案件 `ZL260730014553421F`、生成附件 ID `10298` 和对应容器内 DOCX；数据库复查剩余数为 `0`。
- [仍未证明] 旧系统所有专用模板、按客户/文件类型批量转文、案件清单 Excel、二维码 Word、模板配置和批量导出仍未完成；本条仅为 P0 文书交付能力的首批实现。
# 2026-07-30 批次 A：客户联系人照片删除防绕过、员工提成案件适用与员工删除（本地）

- [已完成代码] 客户联系人照片只允许通过客户联系人页面上传、替换或随联系人删除；通用附件删除接口对“客户联系人照片”明确拒绝，避免联系人 JSON 留下指向已删除附件的失效引用。
- [已通过页面验收] 管理员从“新建员工”创建唯一临时员工 `SMOKE-HR-0730`，在“提成设定”页面新建 2026-01-01 至 2026-12-31 的方案（基本工资 5000、开庭比例 12.5）；选择真实案件 `SHMS2600388` 后点击“按案件查看适用提成”，页面按案件日期 2026-07-23 回显该方案。随后在页面删除该提成记录。
- [已完成并通过页面验收] 按旧系统 `StaffController.Delete` 补齐员工删除：仅管理员可操作；当前登录/系统管理员不可删除；仍有员工附属记录、档案文件或案件/任务/调查/合同/用印/财务业务关联时明确阻断；无引用时原子删除员工档案、流程事件和关联低权限账号。管理员在员工列表点击本轮临时员工的删除并确认后，页面回显“员工档案及未被引用的关联登录账号已删除”；系统用户页复核只剩原有 4 个账号，临时员工与账号均无残留。
- [已验证静态与容器] 前端生产构建、`python -m py_compile apps/api-server/app/main.py`、`git diff --check` 和菜单审计通过；API 已健康，Web 已重建（重建后的健康状态仍需在本轮结束前复核）。
- [已通过页面验收] 管理员从客户页面创建带 `SMOKE-` 标识的临时客户及联系人，上传 PNG 后页面由“上传照片”切换为“替换照片 / 查看照片”；点击“查看照片”弹出真实图片预览，再替换为 GIF，最后从页面删除联系人，联系人行数回到 `0`。旧系统 `CustomerContacts/ContactsPhoto.cshtml` 仅提供上传/替换和查看场景，没有独立下载按钮；当前结论不把下载列为旧站页面验收要求。
- [已精确清理] 使用仅限非生产、仅限 `SMOKE-` 标识记录的本地测试清理接口删除本轮临时客户及其依赖测试数据；复查该记录返回 `404`。不影响已有客户数据。

## 2026-07-30 正式发文来源附件流转首轮补齐（本地，页面入口已验证）

- [旧代码证据] 只读比对 `OfficialDocumentController.CreateByContract`、`CreateByCase`、`Print`、`Download`、`Rollback`、`FileUpload` 与 `StampFileUpload`。旧系统从合同创建时，未明确勾选文件会带入合同全部附件；从案件创建时仅带入所选案件附件。
- [已完成代码] 新增“用印中心 → 正式发文”菜单、专用正式发文记录、创建/审批/撤回接口与页面。创建表单按合同或案件选择来源业务及随文附件；合同不选附件时按旧规则带入全部合同附件，案件仅带入明确选中的附件。服务端逐个校验附件确属来源业务、文件实体在受控上传目录内，再复制为正式发文自身的 `正式发文附件`，并写入来源、数量和状态工作流；复制失败会回滚数据库并清理已写出的临时文件。
- [已验证页面入口] 本地容器重建后，以已登录管理员打开“正式发文”，可点击“新建正式发文”并打开真实表单；表单包含来源类型、来源业务、动态随文附件、多选提示和“提交后进入正式发文审批”选项，不再只是空架子。OpenAPI 同时确认创建、审批、撤回三条专用路由存在。
- [已验证静态/容器] 前端生产构建、Python 编译、菜单覆盖审计（275 节点、229 叶子）、`git diff --check` 均通过；API 与 Web 已重新构建，API healthy。
- [仍未证明] 本轮未在既有合同/案件上提交正式发文，以免复制或改变用户已有附件；待从页面创建带 `SMOKE-` 标识且可精确清理的合同/案件与附件后，补“创建 → 附件回显/下载 → 审批或撤回 → 精确清理”的闭环证据。旧系统的盖章文件上传、预览、打印和 ZIP 下载尚未实现，仍保留为全量差异表待办，不能宣称正式发文模块完成。

## 2026-07-30 正式发文独立状态机与文件交付补齐（本地，页面表单已验证）

- [已完成代码] 正式发文创建改为“草稿”而非直接伪造送审；只有草稿、已拒绝或已撤回可维护正式发文附件。提交专用命令必须同时校验至少一份附件、当前可用印章和来源数据范围，再按“待审批 / 已通过（免审）”进入下一状态；审批通过后才允许管理角色上传盖章文件，状态转为“已盖章”。普通附件上传/删除接口对该模块增加了类别、状态和负责人/管理权限门禁，不能绕过专用流程。
- [已完成代码] 正式发文详情可读取来源、印章、份数、正文、备注和真实附件；已通过/已盖章的多条发文可按编号分目录生成真实 ZIP 流，其他状态由服务端拒绝下载。盖章文件、附件复制和附件上传均落入受控上传目录并写入工作流。
- [已验证页面] 重建 Web/API 后，已登录管理员打开“正式发文”并点击“新建正式发文”；真实表单已显示来源、随文附件、印章类型、盖章份数、电子/打印盖章、审批选项及“创建草稿”动作，确认不再是直接送审或空壳页面。OpenAPI 确认详情、提交、盖章文件和批量下载专用路由均已公开。
- [已验证静态/容器] 前端生产构建、Python 编译、菜单覆盖审计（275 节点、229 叶子）和 `git diff --check` 通过；API 与 Web 容器已重建并健康。
- [仍未证明] 尚未用“页面创建的临时合同/案件 + 临时附件”走完整草稿、附件新增/删除、提交、审批、盖章、ZIP 下载和精确清理；当前不会把表单点击或 OpenAPI 代替这个闭环。旧系统的编辑字段完整回填、预览以及与用印台账的双向回看仍待继续对照实现。

## 2026-07-30 正式发文草稿必填印章回归修复（本地）

- [已验证页面创建与精确清理] 管理员从“正式发文”页面创建临时草稿 `FW20260730042008523035`，来源为既有合同 `HT20260729173657`。系统真实复制该合同的一份附件为正式发文附件，草稿列表、详情与附件区均可打开；未修改来源合同或其附件。
- [发现并修复] 页面端虽标记“印章类型”为必填，但一次键盘验收未选中印章仍能被后端创建为草稿，导致提交时才暴露阻断。现已将服务端创建命令改为直接拒绝空印章类型，草稿与提交两层均校验印章可用状态，避免产生无法继续办理的记录。
- [已精确清理] 仅删除本轮正式发文记录、独立工作流、复制的附件记录及其上传卷文件；数据库复查剩余 `0`，未删除来源合同/来源附件。工作区临时测试文件也已删除。
- [仍未证明] 浏览器自动化当前无法向受控文件选择器写入本地文件，因此本轮不把“新增本地附件 → 提交 → 审批 → 盖章 → ZIP”写成已验收；后续将继续使用页面可控制的文件选择能力补齐，不以数据库写入代替页面流转。

## 2026-07-30 正式发文创建至审批通过页面闭环（本地）

- [已修复] 表单虽然显示了印章选项，但创建请求此前遗漏 `seal_asset_id`、盖章份数和电子/打印盖章参数，导致后端正确拒绝为“请先选择可用印章类型”。现已把全部字段纳入正式发文专用创建请求；印章选择同时通过 `onChange/onSelect` 回写表单，避免仅有视觉选中而请求缺值。
- [已通过页面验收] 管理员在“正式发文”从页面创建临时草稿 `FW20260730043155409758`（来源合同 `HT20260729173657`，已选可用“财务三排章”）。系统真实复制一份来源附件，草稿经页面“提交”变为“待审批”，再经“通过 + 确定”变为“已通过”；详情页真实回显来源、印章、附件下载和盖章文件上传入口。数据库复核该记录状态为“已通过”、`seal_asset_id=7`、附件数为 1。
- [已修复显示] 正式发文详情的“正式发文附件”“盖章文件”标题曾被 JSX 字面量当作转义文本显示，现改为 JavaScript 表达式并以浏览器复验显示正常中文。
- [已精确清理] 仅删除本轮临时记录 ID `49475`、其 3 条工作流事件、1 条正式发文扩展记录、1 条复制附件记录及上传卷内对应 DOCX；复查业务记录残留为 `0`，没有改动来源合同或来源附件。
- [已验证静态与容器] 前端生产构建、菜单覆盖审计（275 节点、229 叶子）和 `git diff --check` 通过；Web/API 容器重新构建后均为 healthy。
- [仍未证明] 盖章文件实际上传并转为“已盖章”、ZIP 下载、草稿/拒绝后编辑回填、撤回后重提、以及从案件来源创建的页面闭环仍待逐项补证；不可将本条审批闭环扩大为正式发文全部完成。

## 2026-07-30 正式发文草稿编辑回填（本地，页面已验证）

- [已完成代码] 新增正式发文专用更新命令与“编辑”入口，仅草稿、已拒绝、已撤回状态下的申请人/管理角色可编辑。更新保存标题、是否送审、可用印章、电子/打印盖章、份数、正文和备注，并写入前后快照工作流；来源合同/案件与既有复制附件不允许在编辑时替换，防止破坏来源可追溯性。
- [已通过页面验收] 管理员创建临时草稿 `FW20260730044137045515` 后，点击“编辑”，将标题改为 `SMOKE-正式发文编辑已保存-0730`、正文改为“页面编辑后的正式发文正文”，点击“保存修改”；详情页真实回显新标题和正文，草稿仍可上传附件和提交审批。
- [已精确清理] 仅删除记录 ID `49476`、2 条工作流、1 条扩展记录、1 条复制附件和上传卷内对应 DOCX；复查业务记录残留为 `0`，来源合同及来源附件未受影响。
- [已验证静态与容器] Python 编译、前端生产构建、菜单覆盖审计和 `git diff --check` 均通过；Web/API 已重新构建并健康。
- [仍未证明] 拒绝后编辑、撤回重提、盖章文件上传、ZIP 下载、附件删除、案件来源创建，以及旧站预览/打印/审批历史页面仍待独立页面证据。

## 2026-07-30 正式发文预览语义、关联跳转与审批历史（本地，页面已验证）

- [旧源码对齐结论] 旧 `OfficialDocumentController.Preview` 仅加载当前正式发文的只读 PartialView；并不生成新的预览文件。现有正式发文“详情”抽屉承接这一只读预览语义，且同时展示正文、印章、份数、来源和附件。
- [已完成代码] 正式发文编号、来源合同/案件和客户均改为真实可点击关联对象；详情中新增“审批与办理记录”，直接读取当前记录的工作流历史，展示动作、状态迁移、办理人、时间与备注。
- [已通过页面验收] 管理员创建临时草稿 `FW20260730044551624880`，打开详情后真实看到“创建正式发文草稿”工作流记录和来源合同链接；点击 `合同｜HT20260729173657` 实际进入合同中心路由。未把关联字段留作纯展示文本。
- [已精确清理] 仅删除记录 ID `49477`、1 条工作流、1 条扩展记录、1 条复制附件及上传卷内 DOCX；复查残留为 `0`，来源合同和附件未改变。
- [仍未证明] 旧站的实际盖章上传/“打印”操作、ZIP 下载、案件来源和拒绝/撤回分支仍需逐项页面闭环；当前不将只读预览和历史展示等同于全部发文流程验收。

## 2026-07-30 正式发文附件删除（本地，页面已验证）

- [已通过页面验收] 管理员从页面创建临时草稿 `FW20260730045014867347`，打开正式发文详情并对其复制出的“正式发文附件”执行“删除 → 确定”。删除后详情页不再显示该附件的删除操作；这项操作针对正式发文自身的复制件，不会删除来源合同的原附件。
- [已复核落库与审计] 数据库复核该正式发文记录的附件数为 `0`，工作流保留“删除正式发文附件｜正式发文附件：新建 DOCX 文档.docx”事件；来源合同 `HT20260729173657` 的原附件数仍为 `1`，未受影响。
- [已精确清理] 仅删除本轮临时记录 ID `49478`、2 条工作流和 1 条正式发文扩展记录；复查该记录与其工作流残留均为 `0`，来源合同附件仍为 `1`。
- [仍未证明] 盖章文件实际上传并转为“已盖章”、ZIP 下载、案件来源创建以及拒绝/撤回后的重提仍需独立页面闭环。

## 2026-07-30 正式发文审核意见与驳回重提准备（本地）

- [旧源码对齐] 旧系统 `OfficialDocumentAuditController` 的通过、拒绝动作均接收并保存 `auditRemark`；旧 `AWS.OfficialDocument.js` 从审核弹窗读取 `OfficialDocument_Audit_Remark` 后提交。原先新系统将意见硬编码为“同意正式发文/拒绝正式发文”，申请人无法看到具体补正要求，不能视为已对齐。
- [已完成代码] 正式发文“通过/拒绝”改为真实审核弹窗，显示当前文书编号与名称，审核人可输入可追溯意见；拒绝必须填写具体原因，服务端也同步拒绝空白驳回请求，审批历史保存实际意见。审批完成后会刷新当前详情，申请人可据该意见编辑后重新提交。
- [已部署，仍未证明] 本轮已完成前端生产构建、Python 编译、`git diff --check`，并重新构建本地 Web/API 容器；菜单覆盖审计通过（275 节点、229 叶子）。浏览器自动化在重新创建验收标签后仍出现可见控件无法接收点击的外部控制异常，已尝试新建标签、刷新、重新定位控件；未以 API 或数据库伪造页面闭环。因此“拒绝 → 查看意见 → 编辑 → 再提交”暂记为待页面证据，不标记完成。
- [冒烟状态] `scripts/smoke-api.py` 未执行完整验证：本机环境未配置其要求的 `SMOKE_PASSWORD`，脚本按设计中止；未写入、显示或保存任何凭据。后续在现有受控测试凭据可用时再运行完整 15 组冒烟。

## 2026-07-30 正式发文“标记用印”动作对齐（本地）

- [旧源码对齐] 旧 `OfficialDocumentController.Print(List<string>)` 直接将所选已审批文书标记为已用印；`StampFileUpload` 仅在用户有文件时先上传文件，随后调用同一 `Print`。两者是独立动作，不能把“有无扫描件”作为完成盖章状态的前置条件。
- [已完成代码] 新增受专用权限保护的批量 `POST /official-outgoing/mark-stamped`：仅管理员、管理人员、审核人员可将“已通过”正式发文变为“已盖章”，逐条写入工作流；非已通过记录会整体阻断并明确列出异常编号。页面新增“标记已盖章”批量动作，同时保留原有“上传盖章文件并标记”路径。并修复正式发文勾选框误挂在“收发文登记”表、导致正式发文无法选中下载/标记的问题；勾选框现属于正式发文表。
- [已部署，仍未证明] 前端生产构建、Python 编译、差异检查均通过，Web/API 已重建。由于同一浏览器控制异常尚未恢复，本条暂不宣称页面批量盖章或下载已验收；后续须从页面创建临时记录并精确清理后补证。

## 2026-07-30 正式发文列表查询对齐（本地）

- [旧源码对齐] 旧 `PrintList.cshtml` 提供申请编号、申请人、申请日期、案件编号、合同编号、客户名称、用印状态、用印类型/印章类型、文件名称等定位条件；不能以“正式发文列表能打开”代替这些业务查询入口。
- [已完成代码] 正式发文页新增上述查询表单；后端 `/official-outgoing` 按当前角色可见数据范围后，再受控过滤编号、申请人、日期区间、关联案号、关联合同号、客户、状态、印章类型和实际附件名称。日期区间倒置由服务端拒绝；文件名查询仅命中当前正式发文自身的附件。
- [已通过页面验收（局部、无测试数据）] 管理员进入正式发文页，输入不存在的申请编号 `NO-SUCH-OFFICIAL` 并通过回车提交，页面真实显示空结果；清空条件再次提交后恢复无筛选列表。该验证没有创建、修改或删除任何业务数据。
- [仍未证明] 多条件组合、跨角色数据范围、查询后批量选择与下载/盖章的页面证据仍待补齐；不将局部空结果验证扩大为列表功能已全部验收。

## 2026-07-30 正式发文列表审核回看字段（本地）

- [旧源码对齐] 旧正式发文列表明确展示审核人、审核时间、审核意见；这些信息不能仅藏在详情里，否则审批清单无法快速判断处理结果。
- [已完成代码] 正式发文专用返回模型从真实“审批通过/审批拒绝”工作流事件提取最近审核人、审核时间和审核意见，列表新增三列并允许横向滚动，不遮挡编号、来源、客户和固定操作列。没有审批事件的记录明确显示“—”，不伪造审核信息。
- [仍未证明] 当前本地列表无保留正式发文样本，尚未用页面记录证明三列回显；后续需用临时审批记录页面验证并精确清理。

## 2026-07-30 普通案件文书生成（本地，第一批动作对齐）

- [旧源码对齐] 旧 `Legal/CaseFileController` 明确提供授权委托书、归档函、广东版授权委托书、赔偿函、律所函、主体身份证明、结算提成表、一审/二审双方律师函、执行律师函及广东版一审、二审、执行律师函等 Word 生成动作；生成文件会归入案件文件，不能由普通 CSV 导出替代。
- [已完成代码] 新增普通案件专用 DOCX 生成接口和案件详情“生成案件文书”菜单，覆盖上述 17 类文书。服务端只读取已保存案件数据生成 DOCX，将文件保存为“案件生成文书”附件并写工作流；无数据字段明确标记“【待补充】”。已合并/已归档案件及非案件负责人/管理角色均被服务端阻断。
- [已验证静态/容器] 前端生产构建、Python 编译、菜单覆盖审计与差异检查通过；普通案件文书路由已由本地 OpenAPI 确认注册，API 与 Web 容器均为 healthy。金额为 `0` 的已保存字段也会保留为 `0`，不会误标为待补充。
- [已完成代码] 对照旧 `CaseFileWordToSeal`，案件详情的 Word 附件新增“提交用印”动作。操作者必须选择当前可用印章、份数及电子/线下盖章方式；系统以当前案件和当前附件创建正式发文草稿，复制附件而非移动原文件，后续复用既有正式发文审批状态机。不会再以旧系统硬编码默认印章替代明确选择。
- [仍未证明] 本轮未通过数据库伪造文书或修改用户已有案件；真实生成、下载内容、权限拒绝和临时数据精确清理仍待页面闭环。

## 2026-07-30 合同审批管理员权限阻断（本地页面证据）

- [已通过页面流转] 管理员从页面新建带真实附件的 `SMOKE` 客户与合同，选择可用审批人后将合同提交到“审批中”；合同审批页可真实检索并勾选该记录。
- [确认问题] 打开审批弹窗后页面明确显示管理员只能查看、不能替代指定审批人。此行为违反 `AGENTS.md` 的不可破坏规则：`admin` 必须保持最高权限。该问题已记入全量差异总表，待按前端入口与 FastAPI 专用审批校验统一整改；本条不是已完成验收。
- [已精确清理] 临时客户、合同、附件、审批步骤与工作流均已使用本地非生产测试清理入口删除，未删除用户已有数据；工作区临时上传源文件也已移除。

## 2026-07-30 动作级源码审计续查（未开始零散修复）

- [已读取代码] 本轮继续读取旧 `Account`、`TP`、`FAS/FAM/FSC`、`System`、`IPR`、`Lawsuit` 控制器及对应 React/FastAPI 实现；不以菜单、页面文案或历史说明作为功能存在的证据。
- [对齐结论] 旧 TP 的 `TaskNodeCreate/MultipleTaskNodeCreate` 对应的是任务转交/批量转交；当前专用任务交接及批量交接已具备对应状态和时间规则，不重复登记为缺失。旧到款登记、认领、分配、历史与撤销分配，当前已由 `IncomingPayment` 的登记、认领、逐应收/案件分配、流水与受控删除实现承接，不把字段名称不同误判为缺失。
- [新增审计范围] 旧 `Legal/Lawsuit` 的 `CaseEvent` 实际是案件提醒，当前民事案件提醒的页面和接口已对齐，不再误报为缺失；旧 `IPR/CaseEvent` 另有案件提醒及“提醒不监控”设置，当前 IPR 尚未实现，已按代码证据登记到差异总表 P1。
- [清单纠偏] 当前合同付款已不是通用费用弹窗：前端使用专用候选/申请接口，后端存在 `ContractPaymentLine` 明细、合同标的余额校验、审批、付款流水和工作流状态机。差异总表已从“P0 功能缺失”降为“P1 页面闭环与菜单可发现性待证”，后续不再按已被代码实现的缺口重复开发。
- [清单纠偏] 案件复制与合并已有专用页面入口、服务端事务、状态/范围阻断及双向工作流，不再以“操作缺失”描述；仍仅欠临时页面数据的真实迁移回看和精确清理证据。知识产权官文也已有原件导入、校验、转发、办结、批量动作、Excel 与 ZIP，真实缺口收敛为检查清单、历史清单、自动匹配及业务/服务双状态线。
- [审计收口] 已从只读归档生成 `docs/旧系统Controller审计覆盖矩阵.md`：覆盖旧系统 21 个 Area 的 Controller 数量、当前对应实现与待动作级对照范围。它用于防止遗漏业务域；具体差异仍以全量差异总表及后续页面闭环证据为准。

## 2026-07-30 管理员合同审批最高权限：自动闭环与门禁回归（本地）

- [已完成代码] 后端合同审批节点改为：指定审批人仍只能处理自己的待审节点；`admin` 在节点属于其他审批人时可按最高权限代办通过或驳回。工作流办理人保留实际操作人，并在备注中明确写入“管理员代办 {指定审批人}”，不能伪装为原审批人操作。前端审批弹窗同步显示管理员代办说明。
- [已验证自动闭环] `scripts/smoke-api.py` 以临时合同指定另一名真实可用审批人，管理员代办通过后断言合同变为“已通过”、审批步骤记录实际意见与办理时间、工作流同时包含“合同审批完成”、管理员操作者及“管理员代办 {指定审批人}”。测试结束后脚本精确清理全部 `SMOKE` 记录、附件和测试账号，输出 `SMOKE_CLEANUP_OK`。
- [已通过完整门禁] `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 于本轮通过：前端生产构建、Python 编译、菜单/客户端接口覆盖审计、19 组 API 业务闭环、测试数据清理及 Docker Compose 配置均成功。冒烟脚本也改为从当前有效法院、案件文件类型主数据取值，不再用已被服务端正确拦截的硬编码失效字典值。
- [仍未证明] 管理员代办的“页面点击通过/驳回 → 列表、详情、审批步骤、工作流回显 → 精确清理”尚缺浏览器证据；本条自动闭环不替代页面闭环。

## 2026-07-30 IPR 动作级代码审计续查（未实施，已入集中整改池）

- [旧系统只读证据] 已直接阅读旧 `Areas/IPR/Controllers/CaseController.cs`、`CaseFileController.cs` 及对应 Razor 视图；确认有效动作不仅是当前已有的案件台账、期限、年费、费率和官文基础流程，还包括多案件同文件批量归档、按文件名匹配后人工确认导入、案件文件/收费通知/收费发票三类转文 Word、协作律所、协作费，以及资助费用的新建、删除、办理确认、办理附件和检索。
- [当前代码对比] 当前 `/ipr/cases`、`IprCenterPage.tsx` 和 `/ipr/official-files` 已覆盖专利/商标基本生命周期、四类单案文书及官文的导入/校验/转发/办结；未发现上述批量文件、三类转文、协作律所/费用或资助费用的等价模型、专用 API 和页面入口。
- [处理状态] 已写入 `docs/旧系统前后端与新系统全量功能差异总表.md` 的 P0 IPR 生命周期项，等待完成其余业务域动作级审计后统一设计数据模型、权限、状态、附件和批量原子性；尚未实现，不能标为完成。

## 2026-07-30 法律案件、财务与调查动作审计纠偏（代码复核）

- [法律案件] 旧 `Legal/CaseFileController.cs` 与 `CaseListController.cs` 已逐个复核；当前 17 类案件 DOCX 覆盖了大部分旧文书生成，真实剩余缺口为普通案件选中/筛选 Excel、清单 Excel、二维码 Word，以及文件目录、锁定、通知日期和 Word 编辑等，不重复登记已实现的文书动作。
- [财务] 旧 FAM 付款、结算、到款、发票控制器与当前 FastAPI 专用接口对照后，合同付款、收款认领/分配、发票、结算、付款包、核销及付款打印页均已有实现；“财务收付款与打印”从 P0 下调为 P1 的逐状态/逐角色/页面证据项。旧付款单 Word 与当前浏览器打印的交付差异继续单列，不把它隐去。
- [调查] 旧 CIT 控制器与当前专用接口对照后，线索/交接导出、线索导入审核、批量删改/生成案件、调查任务、费用、分配、结案报告等核心动作已有实现；“调查复合动作”从 P0 下调为 P1 证据项。取证记录的编辑/删除、地点/公证书号、批量取证和证据/发票双文件仍是明确 P1 功能缺口，未被仓库证物或线索状态替代。

## 2026-07-30 IPR 费用与官费文件动作复核（未实施）

- [旧系统只读证据] 已阅读 IPR `CaseFeeController`、专利/商标官费与发票文件 Controller、应收/应付费用 Controller。旧系统分别维护案件费用、批量费用文件、发票确认、专利/商标官费与发票文件清单，以及费用对冲文件解除；这些不是财务中心通用记录的别名。
- [当前代码对比] 当前 IPR 台账/官文没有费用子账、官方费用/发票文件、批量费用文件或发票确认/对冲解除的专用模型、API 或页面。本项已并入 P0 IPR 生命周期，后续与协作费、资助费用和转文文件统一设计，避免将这些互有关联的动作拆成不一致的孤立页面。

## 2026-07-30 IPR 官文旧前端脚本复核（未实施）

- [四方证据补全] 除旧 FIO Controller 外，已读取旧 `Scripts/FIO/FIO.Patent.OfficeFile.js`。该脚本真实绑定“上传解析候选后勾选导入”、业务未处理/处理中/已处理、服务未处理/处理中/已处理、历史官文的双状态处理/打包/导出，以及检查清单上传、错误筛选和 Excel 导出按钮。
- [当前代码差异] `IprOfficialFilePage.tsx` 与 `/ipr/official-files` 的单一状态机已经覆盖原件、校验、转发、办结、ZIP、Excel，但不含双状态线、历史处理、检查清单和候选解析确认；这些已被明确保留为 P0，不以单状态“办结”冒充完成。

## 2026-07-30 CRM 与律所资料动作复核（代码已对齐，待页面证据）

- [旧系统只读证据] 已复核 CRM 的客户、客户联系人、客户文件与律所 Controller；其有效动作包括客户回收/恢复/启停、联系人新建编辑/默认/启停/照片、客户文件上传下载删除，以及律所联系人和执照上传。
- [当前代码对比] `CustomerCenterPage.tsx`/FastAPI 已具备客户生命周期、联系人增删改、客户文件、联系人照片及权限/范围校验；`LawFirmPage.tsx` 与专用实体/API 已覆盖律所、联系人、默认联系人、启停和执照。当前无新的功能缺口；保留已登记的执照替换、历史快照、联系人照片和跨页面闭环证据，不重复实现基础能力。

## 2026-07-30 HR、系统与仓库动作复核（代码已对齐，待页面证据）

- [旧系统只读证据] 已读取 HR 部门/角色/员工 Controller、SystemCenter/SystemManagement/User Controller 和 WMS 仓库 Controller。重点包括员工请假、事项、档案、绩效、重置密码，部门/角色权限/菜单/参数/缓存，以及仓库清单。
- [当前代码对比] 新系统已有员工档案、请假/事项/提成子记录、员工文件、部门/岗位角色、角色权限、重置密码、系统参数/菜单/缓存和完整证物仓储生命周期；未发现新的功能缺口。保留已登记的角色/参数引用、员工文件、离职账号和仓储跨模块页面闭环，不重复实现基础功能。
## 2026-07-30 普通案件专用导出补齐（本地，自动化闭环已验证）

- [已完成代码] 对照旧 `Legal/CaseListController` 的选中案件 Excel、归档清单 Excel 和二维码 Word 动作，新增受案件菜单权限与当前数据范围保护的专用接口：`/cases/export/excel`、`/cases/export/archive-manifest`、`/cases/export/qr-word`。导出对象必须由页面选中的可见案件 ID 明确指定；空选、越权或不存在记录均被服务端阻断，法律顾问案件不能误用普通案件导出。
- [已完成页面入口] 普通案件列表新增“导出选中（Excel）”“导出当前查询（Excel）”和“导出选中二维码（Word）”；归档列表新增“导出选中归档清单（Excel）”。原有 CSV 保留并明确标注格式，避免把 CSV 伪装成 Excel。
- [已验证自动化] `verify-local.ps1` 全部通过：前端生产构建、Python 编译、菜单/客户端接口覆盖审计、20 组 API 业务闭环、`SMOKE_CLEANUP_OK` 与 Docker Compose 配置均成功。冒烟测试确认 SpreadsheetML Excel 实际包含案件数据、二维码 Word 为真实 DOCX（ZIP 容器），并确认空选导出返回阻断；测试数据均已精确清理。
- [已部署本地 Docker] API 与 Web 已重新构建且 health 正常；新依赖 `qrcode[pil]` 已写入 API requirements，因此后续测试服务器部署必须重建 API 镜像。
- [仍未证明页面] 尚未使用浏览器从案件列表实际点击三种新按钮、保存下载结果并核对二维码扫描内容；该项必须在创建可精确清理的普通案件页面样本后补齐，不能以本条自动化闭环代替页面验收。
## 2026-07-30 知识产权官文双状态与导入清单复核（只读审计，未实施）

- [旧代码证据] 已直接复核旧 `FIO/PatentOfficeFileController`、`PatentOfficeController`、`FIO.Patent.OfficeFile.js` 和列表视图。旧官文列表、历史列表与 Excel 明确分别显示“流程/服务处理”和“业务处理”两条状态；检查清单额外维护申请号、通知书、发文序列号、电子发文日、下载状态、系统状态、案号、入库时间和解析错误。
- [旧导入证据] 旧脚本的“上传 → 解析候选行 → 人工校正/选择案件 → 导入”不是直接把源文件落为正式官文：候选行可包含未匹配案件、客户不一致提示、文件级错误、同通知书下多原件和费用明细；导入动作逐项选择 `fileId, caseId`，因此当前“上传时直接创建正式官文”的语义不等价。
- [当前差异确认] 现有 `/ipr/official-files` 只有单一 `待校验 → 待转发 → 已转发 → 已办结` 状态和单文件直接入库；虽已有 ZIP/Excel、原件附件和批量动作，但尚无候选解析确认、业务/服务双状态、历史专用查询或检查清单/错误筛选，不能以现有“办结”宣称对齐。
- [下一实施包] 先建立官文候选导入批次及候选行，选择已可见在办 IPR 案件后才生成正式官文；正式官文保留独立业务状态和服务状态及各自审计；随后补历史、检查清单、错误筛选、Excel 和页面分区。此项仍为 P0，尚未实现，不应部署测试服务器。
## 2026-07-30 知识产权官文候选确认底座（代码完成，运行验证待补）

- [已完成代码] 新增 `IprOfficialImportBatch` 与 `IprOfficialImportCandidate` 专用表；CSV 源文件上传后仅生成候选批次和候选行，记录行号、解析字段、原始列、日期错误、自动申请号匹配、人工匹配结果与正式官文 ID。候选行不是 `BusinessRecord` 官文，因此不会在未人工确认前污染官文台账。
- [已完成接口] 候选批次上传、候选列表、候选纠错、人工匹配和批次确认均使用知识产权官文菜单权限及当前数据范围。确认只接受无错误、已匹配、在办案件的候选行，并在单次事务中创建正式 `ipr_official_file`、回写来源批次/候选行和导入人；重复确认被状态阻断。
- [已完成状态基础] 新创建或确认导入的官文均保存 `business_process_status` 和 `service_process_status` 初始值；原有校验/转发/办结及其批量动作开始同步写入双状态字段，避免原单状态继续掩盖旧系统的双线语义。
- [仅静态验证] 当前仅完成 Python 编译和 `git diff --check`；尚未重建 Docker、执行 API 冒烟、创建并精确清理候选批次数据，也尚未接入 React 页面。因此本条不能标记为已验收或部署。
# 2026-07-30 案号点击改为独立工作页签

- [已完成代码] 普通案件列表、归档列表及复用 `openCounselDetail` 的同类案号入口，点击后不再把详情覆盖在原列表页内，而是新增一个以案号命名的顶部工作页签；原列表页签保持打开，可直接切回。
- [已完成代码] 动态案件详情路由支持刷新恢复，并按案件 ID 重新读取真实详情；管理员及具备案件菜单权限的角色可进入，未授权角色不能借动态路由越权。
- [已验证并部署] 前端生产构建与 277 节点/231 叶子菜单覆盖审计通过；测试站已发布。浏览器从“民事争议”实际点击 `SHMS2600002` 后，顶部同时保留“民事争议”和 `SHMS2600002` 两个页签，案号页签加载真实案件详情；切回“民事争议”后案号页签继续保留。该验证未创建或修改业务数据。

## 2026-07-30 系统管理员岗位权限矩阵（本地已验证，待测试站发布）

- [已完成] “系统管理员”（`SYSTEM-ADMIN`）岗位的业务动作权限改为与最高权限身份一致的全量 67 项。服务启动会修复历史空矩阵；岗位在页面中显示全部已勾选的权限。
- [已完成] 系统管理员岗位名称、启用状态和权限矩阵在前端锁定，并由服务端拒绝降权、改名或停用，防止配置页与实际 `admin` 最高权限出现矛盾。
- [已验证] 本地角色管理页已实际打开系统管理员岗位：67 项权限均已勾选且禁用编辑，页面明确说明其菜单、数据范围和敏感字段也保持最高权限。前端生产构建、Python 编译、菜单与客户端 API 覆盖审计、Docker API/Web 健康检查均通过；未写入测试业务数据。
- [已部署并验证] 已发布提交 `997f541` 至测试站稳定目录 `/opt/sunhold-oa/releases/20260730-134402`；API、Web、PostgreSQL、Redis、MinIO 均健康，Worker 正常运行，公网健康检查返回成功。管理员在测试站角色管理页实际打开系统管理员岗位，确认 67 项权限展示为已勾选且禁用编辑；本次未改写业务数据。
