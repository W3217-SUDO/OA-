# [本地已修复，待统一发布及用户验收] CPI-05 合同请款撤销与回滚调用错误类型接口

## 当前集成状态
### 财务前端实施结果
- 撤销/回滚按 record.module 分派到合同专用API；动作成功后刷新对应当前列表。
- 对应文件：apps/admin-web/src/finance/services/paymentsActions.tsx、paymentLifecycle.mjs；同名.d.mts提供新纯函数模块类型声明。
- 定向证据：apps/admin-web/src/finance/cpiChain.test.mjs 中本条用例，合计12/12通过；联合合同前端为26/26，主任务已复核。无浏览器业务验收，由用户确认线上表现。

- 用户已确认整批整改；本条实现已纳入统一工作树。下文“待确认/未开始/构建失败”等为早期取证或过程快照，当前以本节和汇总发布回执为准。
- 前端26项、后端29项隔离检查通过；TypeScript、菜单审计、828处API覆盖、Python编译及FastAPI路由加载通过。最终版本构建和服务健康结果见[统一清单](contract-finance-chain-audit-20260914.md)。
- 无业务数据库操作或结构迁移，无浏览器业务测试；不标记已验收。发布后由用户测试对应前端入口，若有返工在本条续记。

## 2026-09-14 任务 C 已获授权实施
- 用户已统一确认九项整改。本线程仅实现合同付款后端，不提交、不发布、不操作业务库或浏览器。
- 重新读取 PaymentController.PaymentCanceled/PaymentRollback、APController.PaymentCancel 及反编译 PaymentService.ApplicationCanceled/PaymentRollback。旧回滚仅改状态而未释放累计金额，不复制此缺陷。
- 精确范围：areas/contract/router.py 原路由加锁与末尾追加生命周期路由；新 core/contract_payment_lifecycle.py 负责合同/付款锁、已支付阻断和状态事件；main.py 仅末尾追加路由 slice；新增独立隔离测试。
- 采用现有按有效状态计算占用的规则：撤销/驳回/回滚释放，提交重新校验并占用；不修改未知审批配置，不增加合同审批前提。
- 前端由 B 负责；数据库沿用 BusinessRecord、ContractPaymentLine、FinanceTransaction、WorkflowEvent，无迁移。
- 验证计划：隔离 SQLite 内存库调用真实路由，覆盖重复操作、支付/核销阻断、余额恢复、越权与状态竞态；编译和 app.main 实际导入验证新增路由。业务验收留给主会话/用户。

### C 后端交付（待集成/用户验收）
- 已完成：专用 cancel/rollback 路由；待审批/待付款回滚到待提交；撤销到已撤回；保留原 ID/单号，重复目标状态不重复事件；真实支付流水或付款/核销状态阻断。
- 创建、审核、支付、核销及新增写操作统一先锁合同再读取付款最新状态；SQLite 使用不改变 updated_at 的锁定 UPDATE，PostgreSQL 同合同事务锁；锁冲突返回409。未增加合同审批前提、未改变现有菜单权限配置。
- apps/api-server/contract_payment_lifecycle_cpi_test.py 最终 `python -m unittest contract_payment_lifecycle_cpi_test -q`：14/14通过，75.492秒。覆盖真实路由、重复撤销/回滚/提交、原单变更、支付/核销阻断、陈旧会话及并发70+70申请仅一单成功。
- `python -m py_compile app/areas/contract/router.py app/core/contract_payment_lifecycle.py app/main.py contract_payment_lifecycle_cpi_test.py` 通过；app.main 在 DATABASE_URL=sqlite+aiosqlite:///:memory: 下实际导入，8条尾部新增路由各注册一次。
- CodeGraph 起始 status/explore 完成，精确函数和调用链覆盖不足时用 rg/文件读取补足；修改后 sync 已运行，图谱不替代业务验收。
- 测试库仅内存及 TemporaryDirectory 中唯一 CODEX-CPI-ISOLATED 临时 SQLite，finally dispose/自动删除；零业务库操作、零浏览器标签，无业务数据或附件残留。未提交/push/改版本/部署，未运行完整前端构建，统一交主会话。
- 尚未证明：实际 PostgreSQL 并发执行、真实角色与线上页面行为；现有审批节点配置保持不动。本次测试不宣称全链路线上已验收。

## 基本信息
- 发现日期：2026-09-14。
- 基线：dev 5544ceec，包含最新 FC-01 至 FC-11 修改。
- 当前状态：源码确认不一致，待用户确认；未修改业务代码。
- 页面位置：财务中心 / 请款单 / 撤销请款、回滚请款。

## 用户依据
- 原文：“这不能光对齐这一个后续的页面都要对齐 逻辑也要对齐了”。
- 关联截图：codex-clipboard-9360b857-939f-4f99-b37b-cc179cc2b09c.png（旧付款单位列表）；codex-clipboard-f19d89f3-7aa5-42d0-8d93-1290106381a4.png（新付款页面）。这些截图不单独证明本条下游缺陷，本条来自源码追踪。
- VibeHub：已查询脱敏候选 state machine；返回 UI 状态词条，非业务状态机精确匹配，不作为业务证据。

## 新旧对比
- 旧源码根：C:/Users/Administrator/Desktop/OA系统/OA系统_跨电脑继续开发_20260804_完整交接/旧系统归档源码/SH.CRM.WEB；反编译服务根为同级 SH.CRM.Service.Decompiled/Dchien.Legal.Service/Dchien/Legal/Service/FAM。
- 旧系统证据：Areas/FAM/Controllers/APController.cs:117 PaymentCancel；PaymentController.cs:219 PaymentCanceled、:262 PaymentRollback；AP/Payment/PaymentService.cs:624 回滚原请款单。
- 新系统证据：apps/admin-web/src/finance/FinanceCenterPage.tsx:2290 附近展示撤销/回滚；finance/services/paymentsActions.tsx:370、:397 调 /finance/fees/{id}/cancel、rollback；apps/api-server/app/areas/finance/router.py:4567、:4608 要求 module=finance。新合同付款在 contract/router.py:1226 附近创建 module=contract_payment。
- 根因：合同付款与普通费用混合展示，撤销/回滚事件未按来源分流。合同专用路由当前只有创建、审核、付款、核销，无撤销和回滚。

## 拟整改
- 文件和处理点：finance/services/paymentsActions.tsx、FinanceCenterPage.tsx、areas/contract/router.py、core/finance.py；补合同请款生命周期及对称额度释放，不绕过专用流程。
- 聚焦检查：新合同请款撤销、回滚能到正确接口；原单编号不混淆；恢复相应费用余额并保留操作历史；已付款/已核销走明确阻断规则。
- 整改情况：尚未实施，等统一清单确认。

## 后续状态
- 本地验证：仅源码追踪，未运行接口/浏览器业务测试。
- 测试数据：没有创建或修改。
- 部署/提交：本条未部署，未提交；不以既有发布代表本条已修。
- 线上验收：由用户执行，当前未验收。
