# [本地已修复，待统一发布及用户验收] CPI-09 请款查询混合两类记录时分页会截掉记录

## 当前集成状态
### 财务前端实施结果
- 查询调用统一后端API一次，使用服务端总数和稳定排序；所有现有筛选参数同步传入服务端，分页最高500条。
- 对应文件：apps/admin-web/src/finance/services/paymentsActions.tsx、constants.ts、FinanceCenterPage.tsx；同名.d.mts提供新纯函数模块类型声明。
- 定向证据：apps/admin-web/src/finance/cpiChain.test.mjs 中本条用例，合计12/12通过；联合合同前端为26/26，主任务已复核。无浏览器业务验收，由用户确认线上表现。

- 用户已确认整批整改；本条实现已纳入统一工作树。下文“待确认/未开始/构建失败”等为早期取证或过程快照，当前以本节和汇总发布回执为准。
- 前端26项、后端29项隔离检查通过；TypeScript、菜单审计、828处API覆盖、Python编译及FastAPI路由加载通过。最终版本构建和服务健康结果见[统一清单](contract-finance-chain-audit-20260914.md)。
- 无业务数据库操作或结构迁移，无浏览器业务测试；不标记已验收。发布后由用户测试对应前端入口，若有返工在本条续记。

## 2026-09-14 任务 C 已获授权实施
- 主审追加 CPI04 合同 invoice-candidates 的后端独占修复：去掉整公司 active_invoices/reserved_ids 循环；以当前合同可见费用 ID 调 D 的 _invoice_fee_details，映射剩余开票额、到账/已开票额、合同内外号及真实案件人员/法院/收款查询字段。该补充不改变 A 的前端或 D 的财务核心文件。
- 已重新读取旧 PaymentController.PaymentList：统一结果集、总数、金额合计及分页。
- 已读取前端 paymentQueryRequestParams / contractPaymentQueryRequestParams：现有参数 keyword、record_status、page、page_size；新接口必须对两种来源一致应用。
- 新增 GET /finance/payment-applications/query 到 contract/router.py 原39条末尾，并由 main.py 尾部 slice 注册。SQL 对 finance(ap_payment) 与 contract_payment 同时筛选、按 created_at/id 倒序分页，复用用户数据范围及字段过滤；不全表载入 Python 合并。
- 独立测试覆盖混合来源多页无漏重、总数、筛选、同时间稳定排序和普通用户数据范围；前端接入归 B，主汇总由主会话负责。

### C 后端交付（待集成/用户验收）
- 追加授权核对（CPI04）：双用户隔离测试已复现100元费用、本人占10元、他人有效占30元时错误余额90（应60）。仅接管core/finance.py的_invoice_case_fee_rows：以已授权费用IDs限定占用聚合，取消占用金额的申请人范围限制，发票记录ID/票号/日期仍只能来自可见发票；补他人有效/撤回/无权费用占用及详情不泄露测试。
- 追加核对结果：修前双用户用例失败90!=60；修后定向两项2/2通过（10.307秒），包括原部分余额用例。本人10元+他人30元得剩余60；他人已撤回40元和无权费用90元不计入；本人票撤回后剩余70且invoice_record_id=None、invoice_no/invoice_date为空，不泄露隐藏票详情。测试使用真实helper和内存库，权限配置使用独立mock，数据库范围条件仍实际执行。
- 补丁仅改变传入明确ids的费用明细聚合路径，使用重新鉴权后的fees ID构造查询；不带ids的原调用行为不变。新增/更新文件仅core/finance.py此函数、contract_payment_lifecycle_cpi_test.py及本CPI文档；编译通过，未提交、发布、浏览器或业务数据库操作；未重跑整套14项，不把两项定向结果写成全套已重验。
- GET /finance/payment-applications/query 已实现：普通原生finance与legacy_kind=ap_payment、contract_payment在同一SQL条件集内过滤；其他legacy_kind不混入。created_at DESC,id DESC稳定排序后OFFSET/LIMIT；count/金额合计同条件，绝无两源独立分页后Python截断。page_size最大500。
- 参数：keyword（请款单号）、record_status/statuses、scope（all/mine/department/company/audit/firm/platform）、finance_scope、applicant、stage/case_stage、fee_type、payee、contract_no、case_no、customer、title、handler；application_date/payment_date/deadline/audit_date各有_start/_end日期参数。既有module参数不再分拆数据源。
- 复用_record_scope_conditions和字段权限；无金额权限不返回totals金额。合同费用/案件/阶段条件可匹配lines中的真实字段；普通/合同使用同一筛选条件再计数、分页。
- PostgreSQL json_array_elements先CASE json_typeof=数组再展开，否则使用[]，避免历史JSON null/对象/标量报错；金额聚合先正则校验十进制文本再CAST，空串/非数字不引发整体500。已有SQL编译形态测试，未连接实际PostgreSQL业务库。
- 最终独立测试14/14通过（75.492秒）：225条两源交错记录连续翻页无漏重、同时间ID稳定排序、500页长、501阻断、同过滤总数与合计、个人范围/字段权限、非数组历史数据与空金额兼容。
- CPI04补充已完成：contract_invoice_candidates只传当前合同可见fee IDs到D的_invoice_fee_details，不再读取全公司发票reserved_ids；100元费用已有30元开票时仍返回70元候选。真实映射费用/合同内外号、案件/阶段、到账/已开票额及court_name/court_lawyer/investigator/case_assistant/payer_name/payment_mode/received_date，隔离实际helper测试通过。
- main.py尾部仅追加contract 39:45、finance 140:142，保留原顺序，app.main实际导入确认共8条新路由各注册一次（含D两GET）。`python scripts/audit-client-api-coverage.py`通过：828 frontend calls match FastAPI routes。
- 精确交付文件：apps/api-server/app/areas/contract/router.py、apps/api-server/app/core/contract_payment_lifecycle.py、apps/api-server/app/main.py、apps/api-server/contract_payment_lifecycle_cpi_test.py、docs/parity-issues/cpi-05-20260914.md、cpi-07-20260914.md、cpi-09-20260914.md。未修改D的core/finance.py、finance/router.py或models_shared.py。
- VibeHub脱敏查询idempotency命中 https://vibe-hub.org/idempotency；用户本次已准确使用术语，不追加教学。
- 零业务库/浏览器操作；隔离临时库已关闭并自动清理。未提交/push/改版本/部署，前端构建/统一发布/用户验收由主会话处理。尚未证明实际PostgreSQL执行及线上真实角色数据行为。

## 基本信息
- 日期：2026-09-14；源码基线 dev 5544ceec。
- 状态：源码已定位，待统一确认；未修改业务代码。
- 前端位置：财务中心 / 请款查询 / 查询、总条数、翻页。

## 用户原文与依据
> 这不能光对齐这一个后续的页面都要对齐 逻辑也要对齐了

- 来源：在用户指出付款单位不对齐后，主动追踪下游源码；不声称截图直接复现本条。
- 旧系统证据（归档源码根）：SH.CRM.WEB/Areas/FAM/Controllers/PaymentController.cs:151 PaymentList 对同一请款查询结果分页；旧列表按该页真实结果展示。
- 新系统证据（oa-auto-rules-dev-20260913）：apps/admin-web/src/finance/services/paymentsActions.tsx:239 loadPaymentQueryPage 对 finance 和 contract_payment 各自取同页，再合并并 slice(0,pageSize)，total 累加。两源第一页都满时，第二源第一页记录被截掉，下一页却请求第二页，这些记录不会被补回。
- VibeHub：已查询 state machine，未采用非精确匹配为业务证据。

## 拟整改与检查
- 使用服务端统一过滤、稳定排序、统一分页；或等价的可证明正确的跨来源合并，不能对独立页直接截断。也核对 paymentQueryServerPagePlan 多请求时总数不重复相加。
- 定向检查：各源都超过一页时，连续翻页覆盖全部记录，不漏、不重；总数与筛选一致；排序在翻页后稳定。
- 影响范围：上述前端入口及共用组件、事件与其专用接口；不修改旧系统。

## 后续状态
- 整改：未开始，待用户确认整批清单。
- 验证：源码证据；未执行接口业务测试、浏览器验收。
- 数据：没有创建或修改业务数据。
- 部署/提交：本条未提交、未部署，旧发布不代表已修。
- 下一步：与汇总清单一并确认后统一实现，再由用户线上验收。
