# 2026-09-14 会议整改统一发布

## 授权与范围

用户明确要求“把这些修改部署到服务器上去”。本次发布全部已实现的会议整改和后续提成人员修复，不再等待单独发布授权。

- 案件显示建案合同，费用按自身选定合同归集，付款/开票候选遵循逐笔归属。
- 合同开票创建后提交审批，失败保留同一草稿。
- 自动提成读取对应员工方案，草稿人员变更重算，历史手工/已审批/已付款记录不自动覆盖。
- 开庭律师显示与提成计算采用相同人员优先级；同名/重复账号不随意取首条。
- 移除案件资助费用、事件、提醒入口和VIP筛选，保留历史数据。
- 线索转案件必须存在唯一有效合同。

明确排除：未归档留存5%的金额基数尚未确认，现有公式不变；不补写员工方案、不替换已有业务数据。人员没有有效方案时仍提示缺配，不能将部署说成具体人员已经生成提成。

## 发布前核对

- 本地dev基线05a719d0，包含服务器活动fb906a5b及此前GitHub修复；GitHub/dev=05a719d0，origin/dev=53754c35。均为当前候选祖先，无需强推或重写既有合并。
- GitHub已有v1.1.110标签指向05a719d0，不复用该版本；先镜像该标签到服务器远端，再使用版本锁分配下一号。
- 实际API/Web服务均在production-current，均active。服务器release.json目前错误返回SPA，发布时恢复真实版本文件并校验资源哈希。
- 本地产物构建、Python编译、diff检查已通过；正式改版本后重新本地构建。按用户要求不运行浏览器或业务测试。
- 纯代码发布，无models.py/schema/alembic/requirements变更，无数据迁移和覆盖，数据库备份不需要。
- 非本轮文件auto-task-fix-evidence-20260913.md不提交。

## 验证与状态

发布执行中。最终版本、提交、服务健康及同步证据以服务器queue/ledger与本记录后续补充为准；业务结果待用户验收。

## 1.1.111启动失败及修复

- 发布提交26c2d0fb启动检查失败：新增POST commission-preview后legal router共143条，但main.py最后注册范围仍到142，导致末尾POST cases/batch-delete漏挂。编译不能覆盖模块加载时的注册校验。
- 发布脚本已回滚活动代码至fb906a5b及原前端产物，/health返回ok；queue记录1.1.111 failed、rollback-health-1。无数据库变更。
- 本地`import app.main`已复现同一缺路由错误。修正main.py最后的legal切片结束位置至143，继续保留verify_route_coverage对缺失/重复注册的硬校验，未关闭检查。
- 重发前要求本地应用模块加载和路由注册检查通过（不启动lifespan、不连接数据库、不执行业务请求），然后领取新版本并重新本地构建。失败版本1.1.111保留，不复用、不修改既有标签。
