# 招行原生导出模板适配

用户原文：再添加一个招行的模版。

已结构化读取所提供工作簿 Sheet1 全部 19 行、36 列，无附加截图。第 13 行为表头，第 14–19 行为 6 笔交易，2 笔贷方、4 笔借方。字段位置：D 交易日、H 借方金额、I 贷方金额、K 摘要、L 流水号、T 收(付)方名称。

旧源码 FAM/AR/CMB/PaymentService.cs Process 从零基行 13 开始，取列 3 日期、8 贷方金额、11 流水号、19 付款人，与实表对应。

新系统存在平台财务中心/回款管理/回款(招商)权限菜单，App 将其映射 finance-receipts-cmb，但财务页面原始路由、列表配置、标题、后端银行白名单及模板均缺 CMB；因此入口和解析链未接通。

整改：补齐现有招商入口对应页面与上传配置，添加实表表头指纹和字段映射，按贷方减借方区分收入支出，保留原流水号去重。未知招行模板不套其他银行列号，返回可定位错误。沿用现有权限校验、100MB 限制、导入结果、数据存储。无需新增菜单或修改数据库。

VibeHub 已查询脱敏术语“模板映射”，无可靠匹配，不另加术语。

验证：按工作区直接问题规则，仅生产构建和运行健康检查，不实际导入用户交易、不进行业务验收。发布后待用户验收：在回款(招商)上传原文件，核对收入行及重复导入提示。预期原文件 2 笔收入、4 笔支出跳过；这不是已执行导入结果。

## 返工：普通财务中心入口遗漏

用户原文：没有啊、、

唯一截图 codex-clipboard-46584f96-efae-4f1d-a613-3ed48d84f46a.png 已逐图核对：回款管理展开，依次显示回款(工行)、回款(中信)、回款(中行)、新增回款、回款管理、回款领取、待分配回款；中行选中，招商入口缺失。对应的是普通财务中心，不是上一版接通的平台财务中心。上一版状态改为线上未通过/返工。

源码证据：App.tsx 普通 finance-receipts 子菜单和 constants.py DEFAULT_SYSTEM_MENUS 均只包含三家银行；已有 finance-receipts-cmb 页面、API 白名单及实表解析无需重复修改。旧 CMB PaymentService.Process 与此前表列映射证据继续适用。

整改：在普通财务中心中行后新增回款(招商)，完整维护后端菜单注册及前端备用菜单。权限仍按既有角色/菜单授权体系，管理员和父菜单授权包含新节点，显式叶节点权限可从系统权限树配置。启动会新增 system_menus 节点并刷新管理员 role_permissions，发布前分别导出这两张表到本机并校验 SHA256，不备份业务库；回退只删除新增菜单和管理员授权中的新菜单键。

VibeHub：导航菜单 https://vibe-hub.org/menu，用于定位页面入口，不替代源码证据。按直接问题规则仅构建和运行检查，待用户验收普通财务中心招商入口。

发布补充：一次性给已同时拥有工行/中信/中行三个叶节点的系统角色、人事角色及个人菜单覆盖补充招商节点，其他用户不扩权。脚本 scripts/migrate_cmb_menu.py；备份范围 system_menus、role_permissions、job_roles、users，关联为菜单父节点及角色软链接，均在导出范围。仅更新菜单键，不改账户字段或业务数据。一次性迁移允许发布后独立撤销招商权限，不会每次启动重新授予。
