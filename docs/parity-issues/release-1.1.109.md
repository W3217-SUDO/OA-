# 1.1.109 发布清单

- 授权：用户在确认本地整合结果后要求“bushu”（部署）。
- 来源：dev `255ba90268c712a91cc53126d7d495b14e7f5d5c`，包括自动任务规则及此前保留的客户/案件修复，也保留最新调查线索修复 `e9949ed1`。
- 版本：服务器版本分配锁预留1.1.109；正式发布提交以注释标签v1.1.109解析为准。
- 上一线上版本/回滚点：v1.1.108，`3533b8f07d5e71ada22ab173ba19badf5d0efdd4`。
- 本地验证：后端自动任务36项、客户/案件31项、前端57项、审计工具8项通过；Python编译、菜单及822调用API审计通过。本机重新执行1.1.109的TypeScript/Vite生产构建成功，存在原有大分包警告。
- 数据库：无模型/迁移/启动同步脚本变更，无数据迁移；不备份整个数据库，也不创建服务器数据库备份。
- 部署目标：systemd确认的 `/opt/sunhold-oa/worktrees/production-current`，服务sunhold-dev-api、sunhold-dev-web，公网8089。只从本机上传dist构建包，不上传源码目录，不在服务器构建前端。
- 激活门禁：独占formal-release.lock、验证当前提交/标签/远端dev、归档SHA256及入口资源、核对三个package版本后快进代码并替换构建产物；服务启动或资源检查失败时恢复上一代码与原dist。
- 操作证据：实际激活结果、提交、产物SHA256和时间追加至服务器 `/opt/sunhold-oa/releases/ledger.tsv` 及queue.tsv；发布包与临时回滚产物验证后清理。
- 验收状态：待用户验收，不操作浏览器，不填表格状态1。28类生成宿主和旧节点链等缺口仍见 `auto-task-new-coverage-20260913.md`，本版本不宣称全量旧规则完成。
