# 2026-09-12 新旧系统主动对齐审计记录

- 审计范围：客户、合同、案件、工作区、角色权限、菜单与前端页面结构
- 审计工具：`scripts/audit-menu-coverage.py`
- 当前状态：进行中，未部署
- 前端对应要求：每项必须落到页面、路由、按钮、字段、组件或事件处理器

## 最新复核结果（覆盖此前暂定状态）
- CRM 共享保存从旧名单并集合并改为完整名单保存，支持显式清空；本地 19 项相关自动化及构建通过，见独立记录。共享页面旧只读动作与用户后续权限要求不同，按用户要求保留扩展动作。已登记十个客户列表的页面族矩阵，并发现最近联系/最近更新时间窗口缺失，后者待复现修改。
- 公司案件多选删除：已从单条请求改为完整选择集、整批加锁预检和一次数据库提交；后端隔离 HTTP/数据库测试 10/10、前端 handler/契约测试 9/9，通过构建。详见 [独立问题记录与验证矩阵](./case-delete-selection-truncation.md)。未动已有业务数据、未部署，未浏览器验收。
- 已找到并实际读取完整交接包中的 `旧系统归档源码/SH.CRM.WEB`，新建 [区域基线](./legacy-area-baseline.md)，不再用补丁目录误作完整旧源码。
- 根据实际 Razor 模板、脚本和新系统服务端筛选语义，撤回本轮“任务列表增加新建按钮”和“个人线索管理员取消 owner 过滤”两项证据不足的改动。它们不再计入修复成果，原有系统额外功能没有删除。
- TaskDetail.cshtml 中的删除链接位于 Razor 注释内，不按有效页面功能补造批量删除。对应旧测试改为核对实际生命周期和附件边界。
- 修正三个定向测试文件的模块来源和历史语义冲突后，五个文件合计 14/14 通过；仍仅为源码契约/默认值测试，不代表浏览器验收。
- 接口审计工具的 8 项解析测试及两项静态全量审计继续作为工具级验证；完整 API 业务验证环境仍待补齐。

## 静态检查结果（不代表业务验收）

已通过客户权限错误、客户关联跳转、案件/合同客户预检、工作区路由标签、会话隔离、退出同步、角色数据范围、通用记录菜单边界、案件导出、合同事项记录、合同对象和合同草稿撤回等检查。

## 已发现并处理的审计误报

以下问题属于审计器仍引用旧的单体源码路径、旧 JSX 片段或旧文案断言，已改为当前实际模块证据：

| 范围 | 当前实际对应 |
|---|---|
| 合同审批 | `core/contracts.py`、`areas/contract/router.py`、合同工作流服务和向导组件 |
| 客户唯一性 | `core/permissions.py`、CRM/案件路由、客户列表页面 |
| 客户详情/分配弹窗 | `CustomerDetailView.tsx`、`CustomerModals.tsx` |
| 合同新建与印章 | `ContractCenterPage.tsx`、`ContractCreateWizard.tsx`、合同工作流服务、`core/constants.py` |
| 客户列表范围 | CRM 路由的 `scope` 查询和服务端数据范围逻辑 |

## 待确认的前端差异

`CustomerCenterPage.originalActionItems` 将“我的共享客户”纳入完整客户操作集。已核实旧模板只读，但用户后续明确开放菜单内所有动作，因此保留，不作回退。相关操作是否正确仍需逐个验证，详见 [customer-shared-actions-parity.md](./customer-shared-actions-parity.md)。

## 本次续跑结果

- 菜单静态审计：`python scripts/audit-menu-coverage.py` 全部跑完，退出码 0。财务、归档结算、人事、案件维护和报表的旧来源已校正。
- 案件复制/合并的经理专属断言已按用户菜单授权要求更新；保留活动状态、案件类型和数据范围约束。此处只是断言更新，没有修改生产业务代码。
- 接口覆盖审计：识别真实分片注册和工厂重导出，822 处静态调用匹配成功，新增解析测试 8/8 通过。见独立记录。
- 前端 `npm.cmd run build`：版本 1.1.108，TypeScript 与 Vite 构建通过；有大块资源警告。
- Python `compileall`：后端与两项审计脚本通过。
- 全流程 `verify-local.ps1`：未通过，当前工作树缺少 `apps/api-server/.venv/Scripts/python.exe`。
- API 冒烟：未执行。旧脚本包含跨轮次 SMOKE 数据清理，需先准备隔离数据库，不能对共享业务库直接运行。
- 前端五个原定向测试文件：历史首次 4 通过、10 失败；完成真实来源和语义复核后 14/14 通过。新增公司案件三份定向文件 9/9 通过，不能替代页面验收。
- CodeGraph：确认审计脚本为本次修改范围；每次修改后同步。图谱只能辅助源码定位，不作为业务证据。
- 客户回收站进入公海收尾：后端 `customer_recycle_release_test.py` 6/6 通过（原 5/7，修正了非 admin 对公海 restore 得 403 的错误断言；新增 `test_page_menu_capability_owner_flow` 覆盖菜单能力身份路径）；前端 `customerRecycleRelease.test.mjs` 6/6 通过；D6 定向两项（`test_release_accepts_migrated_and_recycled_status_but_rejects_public`、`test_recycle_blocks_customers_with_linked_contracts_or_cases`）通过。完整 D6 回归 16 项中 1 项失败（`test_guid_events_and_files_list_and_download`），与本轮无关，为预先存在的 GUID 文件下载路由问题。其他本轮 API 测试 29/29、前端 16/16、菜单审计 0 未处理、API 覆盖 822/822、前端构建 v1.1.108 均通过。仍未浏览器验收、未提交、未部署。残余风险：真实登录身份链的 `_page_menu_capability` 注入路径仅单元测试覆盖，未经过真实认证中间件端到端验证。

## 9.13 续跑：服务层完整反编译 + 三大核心模块初步审计

- 全量反编译完成：旧系统 `bin` 目录 72 个 DLL 全部反编译，产出 13,650 个 .cs 文件，约 82 MB。存放于 `SH.CRM.Service.Decompiled/`。服务层/DAO 层/SQL 不再是黑盒。
- 反编译 README 已更新，附关键服务索引和使用说明。

**CRM 客户模块**（服务层源码级已核实）：
- 分配（CustomerOwnerChange）：3字段更新 + 级联案件/合同/调查 owner
- 拾回（ClaimCustomer）：公海→个人，管理员例外
- 共享（CustomerSharing）：全量替换（删除+插入协调人）
- 关闭（CloseCustomer）：不改部门/管理人
- 进入公海（ReleaseToPublic）

**案件模块**（初步审计，见 `case-module-parity-audit.md`）：
- 删除：旧系统逻辑删除（IsActived='F'，无级联，无回收站）；新系统物理删除（级联+事务+归档校验）。用户视角一致（均不可恢复），但旧系统已归档案件也能删除 vs 新系统禁止——需业务确认。
- 列表组织：旧系统按阶段拆页面（待开庭/待结案/等待上诉/等待执行），新系统统一列表+scope+状态过滤。
- 参与人机制：旧系统 Legal_Case_Participant 独立表（全量替换），新系统 collaborators 字段。
- 案件分配：旧系统批量修改多字段，新系统单条分配+自动任务生成。
- 合并：旧系统 Merge 功能完整。

**合同模块**（初步审计，见 `contract-module-parity-audit.md`）：
- 删除：旧系统逻辑删除（无级联/无检查）；新系统物理删除（严格关联检查+级联+事务）。新系统安全性更优。
- 审批：旧系统固定审批流+多轮审批+审批通过自动批公函；新系统待深入对比。
- 客户分配级联合同owner：已核实 SQL（ContractOwnerChange），增强客户分配文档证据链。
- 状态：1=待提交 / 2=审批中 / 3=审批通过 / 4=已驳回 / 5=已归档。

- 基线文档已更新：CRM、Legal、FCM 三行证据状态升级。

## 当前未完成项

- 客户回收站进入公海已本地修复，后端 6/6、前端 6/6、D6 定向两项通过；本轮 API 29/29、前端 16/16、菜单审计 0 未处理、API 覆盖 822/822、前端构建 v1.1.108 通过。详见 `customer-recycle-release-blocked.md`，未浏览器验收或部署，不算全模块对齐。
- 最近联系/最近更新的日历月过滤已本地修复，6 项真实 HTTP 定向测试和 7 项共享回归通过；生产构建、Python 编译及两个静态审计通过。详见 `customer-recent-time-window.md`，未浏览器验收或部署，不算全模块对齐。

- 任务创建入口及个人线索管理员例外的本轮改动已撤回，保持原有功能，不计入业务修复成果。
- 共享操作范围差异已解释并保留；共享保存本地自动化已通过，真实页面及全部其他动作仍待验证。
- 新增“公司案件删除仍限管理员/经理”候选，已落到前端按钮、能力字段与删除接口；未修改，先审查删除关联与状态保护。
- 尚未建立并完成全系统逐页面、按钮、字段和流程的完整证据矩阵，不能声称全系统对齐。
- 无 Git 提交、无 GitHub 推送、无服务器部署、无线上验收。当前工作保持本地待集成状态。
