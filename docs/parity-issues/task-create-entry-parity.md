# 任务中心新建入口对齐记录

## 当前结论（2026-09-12 源码复核后，覆盖下面的初步判断）
- 初步判断证据不足，已撤回本轮给 TaskList 新加的“新建任务”按钮及回调，原有创建弹窗和案件上下文创建流程未删除。
- 旧源码根与精确页面证据见 [legacy-area-baseline.md](./legacy-area-baseline.md)。TaskList.cshtml:600-625 仅证明底部生命周期操作，不证明列表必须有通用新建按钮；独立 TaskCreate/Create 模板确实存在。
- 状态：排除为本轮已确认缺口，不再计入“已修复”。未来若发现明确可达入口遗漏，应补齐完整调用链后重新打开。
- `taskMyCreatedA.test.mjs` 改为读取实际组件/模型/路由；去掉对已注释旧删除入口的批量删除期待，保留撤回人、状态、原因、批量流转和资料上传约束。
- 与调查范围相关的五个测试文件合计 14/14 通过；这是源码契约及默认值测试，不是浏览器或数据库验收。

## 早期记录（保留以追踪误判，以下结论已被上文纠正）

- 页面位置：任务中心个人、部门和公司任务列表底部操作区。
- 旧系统对照：有发起权限的任务页面提供新建任务入口，未读消息页面不显示普通任务创建入口。
- 新系统原问题：`TaskCenterPage.tsx` 定义了 `openCreateTask`，但未传递给 `TaskList`，任务创建流程不可达。
- 根因定位：任务列表组件缺少创建回调和入口渲染。
- 整改内容：增加创建回调；非未读任务页且具备发起权限时显示“新建任务”，复用既有创建流程。
- 状态：已修改，待本地构建和页面验收。

## 2026-09-12 验证补记
- 本地 TypeScript/Vite 生产构建通过，菜单静态检查通过；不代表页面验收。
- `taskMyCreatedA.test.mjs` 六项失败，当前仍读取根目录重导出组件和后端 main.py，需修正测试来源后核对实际结果。
- 旧系统对照说明尚未在本记录中提供具体模板/控制器或截图位置，应视为待补证，不是已完成旧站核对。
- 空列表仍受 `hideTaskFooter` 控制，新建按钮是否应在空列表出现需要结合旧站与用户要求确认，不能只证明有回调就关闭问题。
- 未部署、未真实页面验收。
