# 9.15 财务问题逐图复核后的整改

范围：9.15 第2–8行。用户确认重新定位后实施。原始文字、15张原图的逐张分析、旧源码定位和只读数据证据保留在本地逐行证据目录，不上传业务资料。

|行|修改|验证与边界|
|---|---|---|
|2|普通申请单条或合并提交都生成付款包；待付款、待核销、打印列表沿同一付款包取数；补充详情字段|接口验证申请消失于待付款后进入待核销，金额和来源案件/合同保持关联|
|3|核销操作直接打开表单；核销包更新成员，票据关联支持合同付款来源费用|单条、合并、合同来源、附件持久化、金额及权限拒绝路径通过|
|4|固定付款申请单按申请和合同分组，补充打印日期、属性、官费/其他费用列、交款人及签字区|React 静态渲染验证分组、金额和字段；实际打印布局待用户验收|
|5|限制法院退费分配费用域，保留退费标记、部分/全额及撤销链|测试通过；只读查询发现当前关联是普通客户回款，未擅自改为法院退费，历史截图金额不能据此补写|
|6|筛选命中申请后保留该申请全部提成人员，再分页；详情展示基数、参考/实际提成、本次支付及案件概览|验证同申请汇总不被单一提成类型筛选截断，不跨申请合并|
|7|财务回款入口仅提供并接收当前客户案件中的律所/平台费用，排除内部费用和独立应收计划|前端传入业务范围参数，后端候选与写入均验证；共用合同回款接口保留兼容|
|8|人员提成设置恢复取消按钮，保留已有五类比例/固定额与有效期|生产编译通过，既有人员绑定逻辑未改；真实方案与页面待用户验收|

## 验证

- 合并接口回归：24项通过；使用隔离内存 SQLite、临时上传目录，结束清理。
- 付款表单渲染：3项通过。非浏览器验收。
- TypeScript 与 Vite 生产构建通过；保留既有大 chunk 提示。
- 菜单结构审计通过。修正三处过期断言：已在历史提交移除的直接标记提成按钮、已扩展的表格固定布局、已分离聚合与详情权限的发票查询。保留状态和权限限制，不据静态字符串认定业务正确。
- CodeGraph 未初始化，status/explore/sync 无图可用；使用直接源码与接口测试，未声称完成图谱验证。

## 发布与数据

整批统一发布，所有行仍为待用户验收，不写已验收。纯应用代码，无数据库迁移或数据补丁，无需数据库备份。回滚到前一正式版本代码和对应静态资源。正式版本、提交及运行检查写入发布台账。
