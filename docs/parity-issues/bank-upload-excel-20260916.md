# 银行回款 Excel 无法上传

## 原文
回款识别不到我的文件,没办法上传

## 逐图分析
1. e6d60569：回款（工行）→上传→下载目录，文件选择器为自定义文件，目标银行文件没有出现。先单独记录此图后再读取下一张。
2. 66a2f3ba：同一下载目录今天有三份银行文件，右侧明确中信文件为XLSX。另两份仅有Excel图标，未据此猜测扩展名。

## 旧系统与新系统定位
旧 ICBC/BOC/CITIC PaymentService.cs 明确使用 XSSFWorkbook/HSSFWorkbook 解析 xlsx/xls，分别跳过工行5行、中行8行、中信14行说明。字段涉及对方户名、收款日期、金额、流水号。新 FinanceCenterView 文件选择器只接收.csv；workflowActions原样上传；finance/router.py导入端点也只接受UTF-8 CSV。根因是前后端都漏了Excel，不是文件不存在。

当前账号此前只读证据为基础user、有配置菜单权限。导入接口固定admin/manager还会导致选中文件后403，应按对应银行菜单授权，后台仍验证银行来源和有效授权。

## 修改清单
- 工行/中信/中行共用文件选择器接受xlsx/xls/csv，传入当前银行来源。
- 后台使用已有openpyxl/xlrd依赖解析真实工作簿，CSV保留UTF-8并兼容GB18030；扫描说明行后的表头，按实际列名映射，兼容Excel日期及千分位金额。
- 明确拒绝未知格式、缺失关键表头、损坏或过大文件；无可导入行不报成功。逐行报告失败原因，付款/支出不作为到账导入；流水重复继续拒绝。
- 对应银行菜单授权的普通账号可上传，未授权账号仍403；不伪造身份、不改账号权限。
- IncomingPayment保留真实流水号、付款方、到账日期/金额、银行来源；未改认领/分配生命周期，不执行用户文件导入。

## 验证及限制
用户文件未附给当前任务，当前本机Downloads亦未找到同名文件，不声称读取过实际表格内容。依据截图扩展名和旧源码支持实现。VibeHub脱敏查询“文件类型过滤”服务失败，无词条附加。CodeGraph未初始化。直接问题遵守只生产构建和部署运行检查，无浏览器或业务写入测试。无数据库迁移、无需数据库备份；发布后待用户用原文件验收。回滚点v1.1.135。

## 2026-09-16返工：大小限制及小文件表头
原文：还是无法导入；允许大文件上传；小文档也报错。
状态：v1.1.136线上未通过/返工。
单图d5f364d0：工行银行到账列表，上传后泛化提示“银行流水导入失败”。Nginx两次请求29662898字节返回413，21m限制拦截；接口还限制5MiB，属于上次遗漏代理与接口限制。
单图45bf193a：同一工行入口提示“未识别银行流水表头，需要对方户名、银行流水号、到账日期及到账金额列”。这证明该次小文件已进入解析，是独立格式问题，不能归因文件大小。真实文件尚未提供，不猜测其列名，不宣称已修复该格式。
旧系统ICBC PaymentService.Process读取第一张表第6行起，固定列5付款方、6金额、10日期、13流水（零基）。新系统强制同一行具备四个表头别名；需要真实文件列头才能安全判断与旧模板的区别，禁止无条件按位置导入。
当前修改清单：前端100MiB上限及明确413/超时提示、持续导入进度；API100MiB有界读取、线程池解析；Excel解压保护提高到512MiB，保留每表50000行限制；Nginx仅银行导入端点101m及600s超时，其他路由不变。配置变更带原文件校验、备份、回滚。数据库不涉及迁移或补丁。
实际入口覆盖：工行/中信/中行共用上传函数和接口；平台相同。线上数据假设：不依赖人员主数据；不修改历史流水。原文件表头仍待补齐。
VibeHub已调用“上传大小限制”，未返回可靠词条，无需附加。
直接问题依据用户规则不做业务测试、业务写入或浏览器操作，仅构建与上线运行检查。大文件改动可独立发布，小文件表头问题保持未完成，等待原始表头/文件。

### 按用户“参考旧系统啊”补充定稿
单图864147c0：工行入口再次同样表头错误，作为重复返工证据独立记录。依据三家旧PaymentService.Process实际源码补齐固定布局兼容，而非等待用户重新制作表格。
源码落点：旧SH.CRM.Service.Decompiled/Dchien.Legal.Service/Dchien/Legal/Service/FAM/AR/{ICBC,CITIC,BOC}/PaymentService.cs。
固定模板（零基列）：ICBC第6行起付款方5/金额6/日期10/流水13/方向8/备注12；CITIC第15行起付款方3/金额6/日期0/流水11/备注12；BOC第9行起付款方5/金额13/日期10/流水17/备注25。都仅第一工作表，第二列非空才作为候选数据。
修改：API传银行来源到解析器；优先保留可识别表头导入，无法匹配时仅按所选银行恢复旧模板列位置。固定位置回退逐行验证列数、日期、金额数值、付款方和流水，格式不匹配明确报银行/行号，不伪造流水或盲目成功。限额保持100MiB。无旧库/新库数据改动，不复刻旧系统删除当前用户全部临时到账记录的行为。
当前兼容范围已由旧源码确认，实际用户文件尚未读取，发布后仍待用户验收，不声称实际文件导入成功。

## 文件选择窗口返工（2026-09-16）
原文：正常我要上传文件，肯定我点一个地方所有文件都能显示出来供我选择的。
唯一截图c0715df2：旧系统管理员/回款工行/上传，Windows下载目录同时显示Excel、PDF、ZIP、Word等；红框为下载入口和文件列表。目标是选择器展示所有文件，不是所有格式均解析为银行流水。此前以接口权限定位没有覆盖尚未提交请求的文件选择阶段。
新系统FinanceCenterView第1035行accept限定Excel/CSV，导致操作系统选择器隐藏其他文件。工行/中信/中行及平台共用该输入。修改清单：移除该input的accept；workflowActions选择后明确检查xlsx/xls/csv，其他格式提示而不发请求；保留100MB及后台真实性解析。数据库/权限不涉及，不改变文件导入业务格式。
VibeHub文件类型过滤已查询，无匹配。构建和运维检查后自动发布，不执行业务测试；待用户验收。
