# [本地已修复，待统一发布及用户验收] CPI-07 合同请款被驳回后没有原单修改重提入口

## 当前集成状态
### 财务前端实施结果
- 被驳回合同请款进入完整编辑页，加载原ID上下文，PUT保存后对同一ID重新提交；原编号、来源、逐行金额保留。
- 对应文件：apps/admin-web/src/finance/ContractPaymentEditPage.tsx、paymentLifecycle.mjs、FinanceCenterPage.tsx、services/paymentsActions.tsx；同名.d.mts提供新纯函数模块类型声明。
- 定向证据：apps/admin-web/src/finance/cpiChain.test.mjs 中本条用例，合计12/12通过；联合合同前端为26/26，主任务已复核。无浏览器业务验收，由用户确认线上表现。

- 用户已确认整批整改；本条实现已纳入统一工作树。下文“待确认/未开始/构建失败”等为早期取证或过程快照，当前以本节和汇总发布回执为准。
- 前端26项、后端29项隔离检查通过；TypeScript、菜单审计、828处API覆盖、Python编译及FastAPI路由加载通过。最终版本构建和服务健康结果见[统一清单](contract-finance-chain-audit-20260914.md)。
- 无业务数据库操作或结构迁移，无浏览器业务测试；不标记已验收。发布后由用户测试对应前端入口，若有返工在本条续记。

## 2026-09-14 任务 C 已获授权实施
- 重新读取旧 FAM.AP.Payment.js 的 P/D/R 编辑条件、APController.PaymentEdit 和 PaymentService.PaymentCreateUpdate：更新原 PaymentId、费用差额并递增审批轮次。
- 后端修改清单：新增 edit-context、PUT 原单、submit 路由；原 ID/单号/历史不变；编辑与重提重新校验收款单位、费用关联及其他单占用。回滚进入待提交，兼容草稿、创建待提交、已驳回、已退回。
- 新 core/contract_payment_lifecycle.py 保留来源快照、合同标的行及完整 WorkflowEvent，统一创建/审核/付款/核销的合同锁和付款锁。main.py 只追加尾部注册；不修改共享 finance.py、finance/router.py、models_shared.py。
- 前端由 B 实现；沿用现有数据模型，无业务库/浏览器操作，无审批节点配置变更。
- 测试计划：隔离库原单驳回编辑重提、其他单占用导致重提失败、重复提交不增加轮次/事件、费用行同步和付款后修改阻断；完成状态仍待整合及用户验收。

### C 后端交付（待集成/用户验收）
- 已完成 GET /contract-payment-applications/{payment_id}/edit-context 返回 payment/contract/payment_types/items；PUT 同原ID使用现有 ContractPaymentApplicationInput；POST submit 使用 {comment}。回滚待提交、已驳回/已退回及草稿均可编辑重提。
- 创建与重提共用 normalized_payment；重查收款单位、真实费用归属及余额。验证已证明自身占用被排除、其他单占用保留、80元原单在其他单占40元时重提拒绝；撤销其他单后恢复原单重提。
- 原付款/单号不变，现有标的明细按标的ID更新，去掉的明细才删除；case_fee明细保留JSON快照。WorkflowEvent记录前后状态、操作者、原单号、审批轮次、变更前后快照及意见，重提递增轮次，重复提交不递增。
- 独立测试 contract_payment_lifecycle_cpi_test.py 最终14/14通过（75.492秒）；Python编译通过；无业务库/浏览器操作，无提交或部署。原始源码审计章节为实施前快照，以本节为本线程当前状态。
- 前端集成、完整构建及用户验收由主会话负责；不改未知审核节点、付款包或其他范围。

## 基本信息
- 日期：2026-09-14；源码基线 dev 5544ceec。
- 状态：源码已定位，待统一确认；未修改业务代码。
- 前端位置：财务中心 / 我的请款单 / 编辑、重新提交。

## 用户原文与依据
> 这不能光对齐这一个后续的页面都要对齐 逻辑也要对齐了

- 来源：在用户指出付款单位不对齐后，主动追踪下游源码；不声称截图直接复现本条。
- 旧系统证据（归档源码根）：SH.CRM.WEB/Scripts/FAM/AP/FAM.AP.Payment.js:825 按 P/D/R 显示 edit；APController.cs:94 PaymentEdit；反编译 FAM/AP/Payment/PaymentService.cs:37 PaymentCreateUpdate 在已有 PaymentId 上更新并记录审批轮次。
- 新系统证据（oa-auto-rules-dev-20260913）：apps/admin-web/src/finance/FinanceCenterPage.tsx:2197 编辑仅 module=finance 且草稿；合同审核 contract/router.py:1250 返回已驳回，专用接口无 edit/resubmit。普通提交检查草稿/已退回而非该合同已驳回。
- VibeHub：已查询 state machine，未采用非精确匹配为业务证据。

## 拟整改与检查
- 合同申请页支持以 payment_id 加载原单；新增专用编辑/重提，保留单号、操作与审批历史，重新校验余额；统一前后端状态。
- 定向检查：驳回后修改原费用金额/收款单位再提交，不新增重复单、不重复占用费用；已付款不可直接修改。
- 影响范围：上述前端入口及共用组件、事件与其专用接口；不修改旧系统。

## 后续状态
- 整改：未开始，待用户确认整批清单。
- 验证：源码证据；未执行接口业务测试、浏览器验收。
- 数据：没有创建或修改业务数据。
- 部署/提交：本条未提交、未部署，旧发布不代表已修。
- 下一步：与汇总清单一并确认后统一实现，再由用户线上验收。
