# M03 诉讼页面仍展示会议明确不需要的入口

## 1. 清单原文
“发现系统中存在源自旧代码的冗余功能，如‘资助费用’、‘案件提醒’、‘案件事件’及‘VIP筛选’，这些在现行诉讼系统逻辑中无需体现，需进行屏蔽或删除。”引号样式以 `meeting-20260913-source.md` 保存的完整原文为准。状态：当前页面源码确认入口仍存在，属于会议明确的业务裁剪。

## 2. 截图分析
本轮无新截图；以实际渲染组件确认入口，不声称已在浏览器复现。

## 3. 旧系统实现
旧源码存在相关功能：W/Areas/Lawsuit/Views/Case/CaseInfo.cshtml:225有案件提醒，PartialView/CaseAssistedFeeList.cshtml:93-103有资助费用；Legal分支也有相近模板。旧文件存在不意味着现行业务必须保留。本项以会议裁剪意见为目标，不将其记成“旧系统没有这些代码”。

## 4. 新系统当前实现
- 案件详情主标签：`src/legal/CaseCenterPage.tsx:3994/4009/4023` 直接加入assisted-fees、case-events、reminders三个标签，并绑定新建/修改/删除动作；未按现行诉讼业务隐藏。
- 右侧还有 `CaseCenterPage.tsx:4089` 的案件提醒和新增提醒按钮，只删标签会留下重复入口。对应编辑弹窗仍在3811、4274、4281附近。
- 案件任务与客户任务：`src/legal/CaseDetail/CaseTasksPanel.tsx:35/89` 分别有VIP筛选；CaseCenterPage.tsx:4482另有任务弹窗VIP筛选。
- 请求与状态：`services/queriesActions.tsx:376/383/390` 将VIP状态转换为is_vip过滤参数；后台 `legal/router.py:3921-3957` 真实执行过滤。只隐藏控件但不复位筛选会留下不可见过滤条件。
- 后端数据：`legal/router.py` 的assisted-fees、events、reminders接口及对应历史记录已经存在。本轮仅需定义页面范围，不删除数据表、日志、提醒记录或不相关知识产权模块。

## 5. 新旧差异和根因
新系统将旧代码中存在的可选功能直接作为常驻诉讼详情入口，没有落实本次会议的功能取舍。存在多处入口和筛选状态，不能只删一个标签或一行文字。知识产权独立提醒/VIP业务未被会议明确取消，不跨模块删除。

## 6. 精确修改清单（待确认）
- [ ] 明确仅现行诉讼案件页面隐藏上述三个标签、侧栏提醒及其可达新建入口；保留日志和实际案件任务。
- [ ] 案件/客户任务面板及任务弹窗移除VIP筛选，默认查询全部，清空可能遗留的is_vip状态；是否同时隐藏VIP列需确认，不擅自扩大为全站VIP删除。
- [ ] 审计深链/历史激活标签和重复弹窗入口，避免隐藏标签后仍能误入。
- 不涉及数据库修改：仅屏蔽入口并停止不必要的页面加载，不物理删除旧记录/API或其他业务模块的能力。

## 7. 验证清单（本轮不运行）
- [ ] 民事/刑事/仲裁等会议适用范围内的详情入口一致，旧激活标签可回到有效标签。
- [ ] 任务默认全部数据，无隐藏VIP过滤，总数与列表一致。
- [ ] 历史费用/提醒/事件不丢失，知识产权提醒与其他既有业务不受影响。
- [ ] 前端构建、同类入口结构检查与用户验收。

## 8. 实施记录
只创建定位文档；未修改业务代码、未操作浏览器或数据库、未测试、未提交或部署。
