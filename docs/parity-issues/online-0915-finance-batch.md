# 9.15 财务整批整改（第 2–8 行）

用户已确认法院退费口径，并授权“解决问题，并且部署”。本批一次集成、一次发布；所有行均待用户验收。

## 整改映射

| 行 | 问题与实现 | 自动化覆盖 |
|---|---|---|
| 2 | 待付款补上整页请款详情、单号和操作右键查看/回滚、付款模板预览、提交及提交并打印；提交后进入待核销 | 普通/合同付款状态流转、重复提交、角色权限 |
| 3 | 核销填写日期、方式、单据号、备注并上传凭证；金额核对后持久化付款与费用凭证关联 | 单笔及合并付款核销、合同费用关联、文件持久化、重复核销和错误金额阻断 |
| 4 | 使用固定付款申请单模板展示开户行、账号、合同案件费用、合计与签字区；支持同收款对象合并提交打印 | 合并提交与关联核销、生产构建；打印布局待用户确认 |
| 5 | 法院退费分配使用独立 is_refund 标记；部分到账显示剩余未退，全额到账显示已退；撤销回减 | 50 元申请、30 元部分到账、50 元全额到账、撤销；普通客户回款不得作为法院退费 |
| 6 | 我的请款单在后端按申请号、案件、来源费用和申请人分组；保留逐人详情，分页和导出沿用相同申请范围 | 实际数据库/API 分组、不同申请隔离、汇总金额及分页 |
| 7 | 到账分配候选和提交接口共同排除内部费用 | 候选排除及直接提交内部费用 ID 阻断 |
| 8 | 点击新增提成中的人员，展示案件日期对应有效方案的五类比例及固定金额 | 原人员方案预览回归、五类方案字段覆盖 |

同时补齐待核销合同付款的金额占用，避免同一笔资金在提交付款与核销之间被重复申请；详情页面按钮遵守角色权限。打印样式仅作用于付款申请单。

## 证据与验证

原表完整 A1:AP8、15 张逐图核对截图、7 份定位单及 4 份源码补证保存在本地证据目录。原始客户资料、截图、工作簿及数据库快照不提交到源码仓库。

发布前再次读取在线 A1:AP8，与原始单元格结构完全一致；评论接口返回空列表且无下一页。旧源码核对覆盖 PaymentPacking 打印/核销、InternalFee 申请分组、法院退款 IsRefund 分配及撤销、人员 PerformanceView 和回款费用范围。

自动化套件：finance_batch_0915_test、finance_payment_approval_transition_test、case_commission_from_agency_fee_row12_test、incoming_allocation_cases_row15_test、contract_payment_lifecycle_cpi_test，以及前端 caseFeeLegacyProjection901Row3.test.mjs。API 测试使用隔离测试数据库，凭证文件使用临时目录并清理；生产环境不写业务测试数据。

## 发布与回退

这是应用代码发布，无数据库结构迁移或历史业务数据补丁，无需数据库备份。禁止以全库覆盖方式回退。上线核对活动提交、正式标签、静态资源哈希、API/Web 健康和服务日志；不执行浏览器或线上业务验收。回退仅恢复上一个已验证版本的应用代码和静态资源。

状态：待用户验收。自动化验证及证据清单校验均不能代替用户业务验收。
