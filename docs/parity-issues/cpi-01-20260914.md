# [本地已修复，待统一发布及用户验收] CPI-01 合同开票的多案件、多合同选择仍被前端截断

## 当前集成状态
- 用户已确认整批整改；本条实现已纳入统一工作树。下文“待确认/未开始/构建失败”等为早期取证或过程快照，当前以本节和汇总发布回执为准。
- 前端26项、后端29项隔离检查通过；TypeScript、菜单审计、828处API覆盖、Python编译及FastAPI路由加载通过。最终版本构建和服务健康结果见[统一清单](contract-finance-chain-audit-20260914.md)。
- 无业务数据库操作或结构迁移，无浏览器业务测试；不标记已验收。发布后由用户测试对应前端入口，若有返工在本条续记。

## 基本信息
- 发现日期：2026-09-14。
- 基线：dev 5544ceec，包含最新 FC-01 至 FC-11 修改。
- 当前状态：用户已确认整改，A 合同前端已本地实现；待主会话统一构建/部署及用户验收。
- 页面位置：合同中心 / 我的、部门、公司合同 / 合同开票 / 费用勾选与提交。

## 用户依据
- 原文：“这不能光对齐这一个后续的页面都要对齐 逻辑也要对齐了”。
- 关联截图：codex-clipboard-9360b857-939f-4f99-b37b-cc179cc2b09c.png（旧付款单位列表）；codex-clipboard-f19d89f3-7aa5-42d0-8d93-1290106381a4.png（新付款页面）。这些截图不单独证明本条下游缺陷，本条来自源码追踪。
- VibeHub：已查询脱敏候选 state machine；返回 UI 状态词条，非业务状态机精确匹配，不作为业务证据。

## 新旧对比
- 旧源码根：C:/Users/Administrator/Desktop/OA系统/OA系统_跨电脑继续开发_20260804_完整交接/旧系统归档源码/SH.CRM.WEB；反编译服务根为同级 SH.CRM.Service.Decompiled/Dchien.Legal.Service/Dchien/Legal/Service/FAM。
- 旧系统证据：Scripts/FCM/Contract/FCM.Contract.js:2969 汇总同客户的多个 contractNos；Areas/FAM/Controllers/InvoiceController.cs:76 将 contractNos 传入费用查询。
- 新系统证据：apps/admin-web/src/contract/ContractCenterPage.tsx:947 仅取 selectedRowKeys[0]；:1150 传单合同；contract/services/financeActions.tsx:175-179 对多案件直接 return。
- 根因：财务中心 FC-09 已修后端，不等于合同入口已修。合同入口独立维护单合同路由和同案件限制。

## 拟整改
- 文件和处理点：ContractCenterPage.tsx、ContractList.tsx、contractPaymentNavigation.ts、ContractModals.tsx、contract/services/financeActions.tsx；复用最新发票多来源契约，保留同客户校验。
- 聚焦检查：同客户两合同两案件可从合同入口选入一张发票，逐条保留合同与费用 ID；不同客户明确阻断。
- 整改情况：恢复 ContractList 和 ContractCenterPage 两层多选截断；开票读取所有选中合同并校验同客户、同财务主体和每份合同能力；其他单合同动作明确要求一份。兼容原 contract-invoice-apply-id 前缀，将多合同 ID/编号编码到 URL，刷新无需 session 上下文。以各合同现有 invoice-candidates 合并候选，任一失败整页失败。真实 handler 经 buildInvoiceApplicationPayload 提交多案件/多合同来源，多来源摘要为 null/空，不误绑第一份合同。

## 2026-09-14 已授权实施（A）
- 用户已确认九项统一整改；本代理只改合同开票前端及本独立记录，不提交、推送、改版本或部署，不开浏览器、不访问业务数据库。
- 实际重新读取旧 FCM.Contract.js 多合同入口、InvoiceDetailCreate.cshtml 完整页面、FAM.Invoice.js 金额/选择事件、InvoiceController.InvoiceDetailCreate/InvoiceCreateUpdate，以及反编译 InvoiceService.InvoiceCreateUpdate 的服务项与逐费持久化。
- 精确实施范围：ContractCenterPage.tsx 加载全部选中合同及客户资料；ContractList.tsx 恢复多选且保护其他单合同动作；ContractModals.tsx 使用整页共享表单；services/financeActions.tsx 提交逐费金额与多来源；新增 contractInvoiceApplication.ts、InvoiceApplicationForm.tsx、InvoiceServiceItemsTable.tsx 及独立样式/隔离测试。
- 共享服务表接口：InvoiceServiceItemsTable({ form, fieldName = "service_items", disabled? })；service_name、quantity、unit_price、amount、tax_rate、tax_amount；保留历史扩展字段。共享表单使用 case_fee_allocations: [{ fee_id, amount }]，B 可独立复用服务表。
- 后端/数据库：A 不修改；已发现当前输入模型未接收显式逐费金额、候选缺少查询/统计字段，通知主会话交 D 协调，不以现有自动分配冒充逐费分配保存。
- CodeGraph status 为 up to date；explore ContractCenterPage 有调用图；createContractInvoice 返回泛化符号无法定位，按规则用 rg 和文件读取补足。修改后继续 sync。
- VibeHub 已运行脱敏 form validation 查询；返回普通表单词条，不足以精确说明业务金额校验，不追加术语。

## A 实施验证回报
- 本次新增代码精确文件：apps/admin-web/src/contract/contractInvoiceApplication.ts、contractInvoiceData.ts、InvoiceApplicationForm.tsx、InvoiceServiceItemsTable.tsx、invoice-application.css、contractInvoiceApplication.test.mjs。
- 本次修改代码精确文件：apps/admin-web/src/contract/ContractCenterPage.tsx、ContractList.tsx、ContractModals.tsx、services/financeActions.tsx。未改付款 UI、finance/、后端、App.tsx、types.ts 或 contractPaymentNavigation.ts。
- 对接：复用 B 更新后的 financeInvoiceHelpers.mjs 的 buildInvoiceApplicationPayload；D 负责 service_items 数量乘单价/合计及 case_fee_allocations 校验；C 已将现有 invoice-candidates 接入 D 明细 helper，A 查询字段映射一致。
- 隔离验证命令：node --test src/contract/contractInvoiceApplication.test.mjs（apps/admin-web 下执行），14/14 通过。覆盖兼容URL、同客户身份、完整预填、查询/分页保留金额、部分余额、空金额阻断、高开、非法分配、服务金额、真实 create handler、提交失败保留唯一草稿、多合同权限、候选读取/错误及共享服务控件事件。
- 前端生产构建：已运行3次，最近仍因其他代理进行中的 finance/invoiceDetails.mjs、paymentLifecycle.mjs 缺类型声明，以及 InvoiceDetailTables.tsx 类型错误失败；输出未报告 A 授权代码错误。不能将此写为构建通过。
- 菜单静态审计：python scripts/audit-menu-coverage.py 已运行，在603行合同付款静态断言失败；未改动范围外脚本或付款 UI。
- git diff --check -- apps/admin-web/src/contract 和四份 CPI 文档通过；CodeGraph 修改后已继续 sync。源码/单测不作为页面验收证据。
- 测试数据：仅内存 mock 与合成 CODEX-CPI-A 标识，无业务库/服务器请求、无浏览器标签、无业务数据或附件残留。
- 未解决/未证明：主会话需解决共享树构建与菜单审计、统一验证后提交/部署；真实页面、角色/数据落库及线上状态本代理按授权未测试，不标记已验收。没有额外 App 路由修改需求。

## 初始审计快照（以下为实施前状态）
- 本地验证：仅源码追踪，未运行接口/浏览器业务测试。
- 测试数据：没有创建或修改。
- 部署/提交：本条未部署，未提交；不以既有发布代表本条已修。
- 线上验收：由用户执行，当前未验收。
