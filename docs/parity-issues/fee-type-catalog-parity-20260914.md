# 费用类型参数同步整改

## 1. 原文
> 这个参数没有同步

独立问题，截图987bee50为旧系统费用类型列表；fbb93786为新系统显示11个顶层分类。待用户验收。

## 2. 逐图与旧系统证据
旧图：类型代码、名称、大类、创建人/时间、修改人/时间、修改操作；首行-300/-200/-100为停用的选择占位项，检索费属第三方费用。
新图：OFFICIAL等默认分类与LEGACY-FEE-GROUP分类混排，子项默认折叠，创建/修改人为迁移程序，未展示旧编号与时间。
已读旧GD.CRM.WEB/Areas/System/Controllers/SystemCenterController.cs::CaseFeeTypeList：读取BAS_Case_FeeType、筛选CaseTypeId>0后按明细分页。
已读Scripts/System/SystemCenter/CaseFeeType.js：逐行使用FeeTypeId、TypeIdName、CreateUser/CreateTime、ChangeUser/ChangeTime。
已读反编译Dchien.Legal.Entity/BAS/Type/CaseFeeTypeId.cs：1官费、2代理费、3其他费用、4内部提成、5第三方费用。

## 3. 新系统与数据库证据
本地旧SQL Server61条，新系统本地61条legacy_id均存在且名称一致；另有初始化配置。
线上系统参数105条：58条旧费用明细、5条合成旧分类以及初始化配置；缺少3条负编号停用占位项。错误迁移映射将3标为提成费用、4标为第三方费用、5标为内部费用。
scripts/migrate_legacy_system_parameters.py映射与旧枚举不一致，且丢弃负编号和原始审计字段。
SystemParameterManagement.tsx调用feeTypeTreeRows并直接显示storage code，造成只见根分类。
修改系统参数的_normalized_fee_type_extra只留下parent_code，编辑会丢失legacy_id等元数据，亦需修复。

## 4. 修改清单
- 参数页直接分页显示费用明细；已有旧数据时展示旧费用及人工新增项，初始化根/叶配置保留在存储中用于其他业务，不冒充旧费用。
- 展示原始编号及正确大类，查询不再要求上级代码。修改旧项时编号只读；保留其存储code/主键和业务引用。
- 修正原导入器映射、占位项与源审计字段，避免后续重复错误。
- 新增限定fee_type的幂等修复脚本：按legacy_id更新已有行、仅补缺项；修正5个合成分类，保留所有原id/code。同步原始创建/修改字段，保留其他extra。
- 编辑API保留服务端旧映射元数据，按新父类重算大类，负编号保持不可选。
- 对已有同名旧项，未修改名称时允许维护其他字段；旧编号服务端禁止变更。费用页不提供旧系统没有的删除操作，类型大类只选合法分组。
- 源审计时间按中国时区转UTC保存，参数序列化为SQLite的UTC时间补时区，避免本地与生产显示相差8小时。

## 5. 备份/数据范围与回滚
仅变更system_parameters中的旧fee_type项及5个合成分类；其他类别、业务记录不改。
执行前本地保存完整system_parameters表和引用相关费用ID的关联表最小行，JSON非空和SHA256校验后才应用。生产直接流式传回本地，禁止整库备份。
回滚只恢复清单中的受影响旧行并删除本次新增占位项；不整体覆盖参数表或业务库。

## 6. 验证与验收
独立问题不执行本地/线上业务测试和浏览器操作。执行生产构建、迁移前只读对照及必需的迁移完整性/幂等校验、部署健康检查。
用户验收：61个旧参数逐项可见，搜索名称，编号/分类/创建修改信息与旧系统一致；修改后映射不丢失，费用录入不能选择三条停用占位项。
本地与线上均应用同一修复；页面待用户验收。VibeHub已查tree view，无可靠匹配。

## 7. 实施记录
已完成页面、编辑API、费用可选项防护、原导入器和专用修复器。再次查询VibeHub data mapping返回不相关页面元数据，未采用。
本地生产构建通过：5836 modules。未运行业务测试或浏览器。
本地限定备份：scoped-backups/fee-parameters-local-20260914/parameters.json，SHA256（规范JSON正文）4ab09c55a77809ec36695d6aa558de02ca70d77f93c8610624928a21fe55acf4。
范围：完整system_parameters 560条；全部7张外键/软关联表查询结果为0条，仍保存表名和空数组。修复后565条，更新61旧项、新增5分类，原ID/code保留；事务内重新读取并验证幂等通过。
线上只读预检：更新63条（58旧项+5分类）、新增3停用占位项。发布前必须生成对应版本的独立生产备份，不使用本地库备份替代。
v1.1.130生产备份已直接流式保存到本地scoped-backups/v1.1.130-fee-parameters：完整参数表7189条、关联财务记录120条，其余6张外键关系表各0条；JSON非空且源/本地SHA256一致：09dc2b61e3a22171f2ce42f8bb59751df9d29ac764fead679d6bed44e6f92208。
激活时将已验证备份通过SSH标准输入送入服务端内存文件描述符，仅用于基线校验及失败时精确回滚，不落地服务器备份。正式构建v1.1.130通过。

## 8. 用户验收入口
系统中心 → 系统参数 → 费用类型：列表、按名称查询、新增费用类型、修改费用类型。
逐图回指：旧图平铺明细/原编号/大类/维护人和时间对应列表修正；新图初始化根项与错误旧分类对应显示范围与数据库修复。
编辑：旧编号只读，类型名称与大类可维护；占位项保持停用；保存失败显示后端原因。新增仍使用真实API保存，禁止把仅界面调整当作参数同步。
