# 回款查询字段重叠

用户原文：这里有字段重叠。
截图bc4aa04b：回款查询的客户名称、客户管理人、回款单位长文字跨列覆盖，客户链接与相邻人员混在一起；日期和操作仍须清晰可用。
入口：财务中心回款管理各列表，route config source=incoming，配置列由configuredColumns生成。与提成人员绑定是独立问题，后者待确认正确人员。
根因：列宽仅由表头长度推算（约110px），finance-original-table-wrap的td white-space:nowrap；自定义Button及普通文本缺少溢出限制。此为新前端布局缺陷，无需旧库或业务数据变更。
修改：仅incoming配置列的名称/单位/管理人/编号等设置列宽；非操作内容增加列内单行省略和完整title；客户管理人显示第一位并悬停显示全部；客户/合同按钮仍可点击，操作列不裁切。共享回款查询、认领、待分配、已分配列表适用。
后端/API/数据库不涉及，保留金额、付款人与人员实际绑定。生产构建及部署健康检查，按独立问题规则不执行业务测试或浏览器。状态待用户验收。

实施补充：回款列表使用固定表格布局，横向滚动宽度由实际列宽之和计算，避免窄窗口重新压缩各列。VibeHub核对文本截断：https://vibe-hub.org/text-truncate。
生产构建5836模块通过；正式版本v1.1.131。纯前端变更，不需数据库备份。回滚点v1.1.130；本地运行目录快进同步同一发布提交。
