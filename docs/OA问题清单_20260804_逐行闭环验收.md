# OA 问题清单逐行闭环验收（2026-08-04）

依据：`C:\Users\Administrator\Desktop\OA系统\问题\OA系统对接.xlsx`。当前工作簿仅 `7月31日` 工作表有有效问题行，`8月4日` 工作表为空。每行均已同时查看文字与嵌入截图；截图已用于定位入口、弹窗、列名和按钮位置。

## 本地环境

- 前端：`http://127.0.0.1:5173/`
- 后端：`http://127.0.0.1:8000/docs`
- 验收账号：`admin / ..123456`
- 分支：`dev`
- 当前关键提交：
  - `217824e fix(customer): close July issue parity gaps`
  - `229c25a fix(hr): align department and employee issue parity`
  - `d4a6928 fix(customer): clarify civil case count column`
  - `fc17923 fix(contract): align customer submit approval flow`
  - `b9351ee fix(contract): add atomic attachment batch delete`
  - `0e8e5fc fix(ipr): expose page envelopes for case lists`
  - `e99b96d fix(seal): page and copy application attachments`

## 逐行结果

| Excel 行 | 模块 | 问题摘要 | 当前处理结果 | 验收证据 |
| --- | --- | --- | --- | --- |
| 2 | 人事中心 | 员工删除限制原因不清楚，附属记录/系统用户管理入口难懂 | 删除影响弹窗解释阻断类型，支持跳转清理可清理 HR 附属记录，系统用户入口在员工操作列可见 | `229c25a`；`hrEmployeeDeletionImpactExplanation.test.mjs`、`hrAdminNavigationGuard.test.mjs`；浏览器 `hr-all` 验证“系统用户/删除/是否可用” |
| 3 | 人事中心 | 新建员工提成设定应按原系统默认比例显示且可编辑 | 提成新建默认值为开庭 0.10、文书/案源/调查 0.05、品管 0.02，字段仍为可编辑 InputNumber | `229c25a`；`hrCommissionRecordD4.test.mjs` |
| 4 | 人事中心 | 员工管理增加“是否可用”列 | 员工列表和筛选均显示“是否可用”，用于查看关联账号是否允许登录 | `229c25a`；`hrDepartmentEmployeeApprovalState.test.mjs`；浏览器 `hr-all` 验证 |
| 5 | 人事中心 | 部门管理不应配置权限，部门用于部门数据范围 | 部门弹窗移除权限来源/权限勾选区，改为说明“菜单和业务动作权限统一在角色管理配置”；部门切换清理旧选择和审批上下文 | `229c25a`；`hrDepartmentListD4.test.mjs`、`hrDepartmentEmployeeApprovalState.test.mjs`；浏览器新增部门弹窗验证 |
| 6 | 人事中心 | 部门管理新增是否产生逾期扣款，并在列表显示 | 部门列表新增“是否逾期扣款”，部门弹窗新增开关 | `229c25a`；`hrDepartmentListD4.test.mjs`；浏览器 `hr-departments` 验证 |
| 7 | 用户中心 | 沟通日志新增附件上传，文件夹必须压缩，其他格式不限 | 新增/编辑沟通记录弹窗提供附件上传，提示文件夹需 ZIP；支持上传/下载/删除 | `217824e`；`communicationLogParity.test.mjs`；浏览器新增沟通记录弹窗验证“附件/上传附件” |
| 8 | 用户中心 | 沟通日志客户名称和客户 ID 重叠 | 客户 ID 与客户名称列有独立宽度和样式，避免重叠 | `217824e`；`communicationLogParity.test.mjs`；浏览器 `user-communications` 验证两列 |
| 9 | 用户中心 | 沟通日志右侧新增查看 | 列表操作包含“查看”，查看弹窗为只读入口 | `217824e`；`communicationLogParity.test.mjs`；浏览器 `user-communications` 验证 |
| 10 | 客户管理 | 新建客户人员类输入无法识别系统员工 | 客户来源、客户管理人、联系人账号接入目录候选，按账号/显示名匹配 | `217824e`；`customerUiBatchI14.test.mjs`、`customerUiBatchI15.test.mjs` |
| 11 | 客户管理 | 客户编号过长，应按原系统规则 | 新建客户编号改为旧规则式短编号，避免无意义长号 | `217824e`；`customerUiBatchI14.test.mjs`、`customer_backend_alignment_d6_contract_test.py` |
| 12 | 客户管理 | 我的客户显示合同数量/案件数量并可点击跳转 | 客户列表显示“合同数量”“民事案件数量”，点击写入客户上下文并进入对应合同/案件列表 | `d4a6928`、既有关系实现；`customerRelationParityI9.test.mjs`；浏览器 `customer-mine` 验证完整列名 |
| 13 | 客户管理 | 顶部突兀白框与搜索框间距 | 顶部样式已调整，避免白框突兀并保留搜索布局 | `217824e`；前端构建与浏览器客户页抽验 |
| 14 | 客户管理 | 我的客户不应有客户删除 | 我的客户主列表不直接展示“客户删除”，删除动作只在允许的配置/菜单范围出现 | `217824e`；`customerMineDeleteA.test.mjs`；浏览器 `customer-mine` 验证 |
| 15 | 客户管理 | 客户编辑界面应和新建一致并能改客户管理人 | 编辑表单保留客户管理人多选，保存时同步 `/customers/{id}/managers` | 既有实现 + `217824e` 回归；`customerUiBatchI14.test.mjs`、`customerRelationParityI9.test.mjs` |
| 16 | 客户管理 | 我的客户新增合同最后一步不要拆成两个字段 | 合同创建步骤为“合同基本信息 / 提交审批”，不再拆出同步用印步骤 | `fc17923`；`contractCustomerSubmitFlowIssueI19.test.mjs`；浏览器 `contract-new` 验证 |
| 17 | 客户管理 | 不选择同步用印后应进入合同查看，不应进入错误审核页 | 提交成功后进入合同详情/查看页；审批操作仅当前审批人可见 | `fc17923`；`contractCustomerSubmitFlowIssueI19.test.mjs`、`contractApprovedDetailReturn.test.mjs` |
| 18 | 客户管理 | 客户服务端激活仅有激活码，无法账号密码登录 | 客户门户开通返回账号和一次性激活码，并有激活设置密码流程 | `217824e`；`customerPortalActivationParityI18.test.mjs` |
| 19 | 客户管理 | 我的客户/部门客户范围未区分 | 本人、部门、公司、共享、最近联系/更新等 scope 分离；本人按管理人/案源人，部门按部门人员关系 | 既有范围实现 + 复验；`customerScopeParitySourceI6.test.mjs`、后端 `customer_backend_alignment_d6_contract_test.py` |
| 20 | 客户管理 | 公司客户中不应显示已删除客户 | 公司客户排除“已回收/公海”，回收站单独显示 | 既有范围实现 + 复验；`customerScopeParitySourceI6.test.mjs`；浏览器 `customer-company` 抽验 |
| 21 | 客户管理 | 公司客户新增编辑/删除，删除需受权限和关联合同/案件阻断 | 公司客户“更多操作”包含客户编辑/客户删除；后端有关联合同/案件时拒绝删除 | `217824e`；`customerCompanyActionParityI3.test.mjs`、`customer_backend_alignment_d6_contract_test.py`；浏览器菜单验证 |
| 22 | 客户管理 | 我的共享客户增加被共享人字段 | 共享列表新增“共享接收人”列，显示 `shared_with` 接收人 | 既有实现 + 复验；`customerShareObjectParityI10.test.mjs`、`customerRelationParityI9.test.mjs`；浏览器 `customer-shared` 验证 |
| 23 | 客户管理 | 客户利益检索无法正常检索 | 利益冲突检索页面保留旧系统两步提示和完整名称检索流程，空/无结果保持提示状态 | `217824e`；`customerConflictQueryPersistenceParity.test.mjs`；浏览器 `customer-conflict` 验证 |

## 本轮复验命令

- `node --test communicationLogParity.test.mjs`
- `node --test hrCommissionRecordD4.test.mjs hrDepartmentListD4.test.mjs hrEmployeeDeletionImpactExplanation.test.mjs hrDepartmentEmployeeApprovalState.test.mjs hrAccessGuard.test.mjs hrAccountApprovalGuard.test.mjs hrAdminNavigationGuard.test.mjs hrEmployeePasswordResetAction.test.mjs`
- `node --test customerScopeParitySourceI6.test.mjs customerRelationParityI9.test.mjs customerNextBatchParityI13.test.mjs customerUiPermissionFailureParityI7.test.mjs customerShareObjectParityI10.test.mjs customerUiBatchI14.test.mjs customerUiBatchI15.test.mjs customerUiBatchI16.test.mjs customerPortalActivationParityI18.test.mjs customerConflictQueryPersistenceParity.test.mjs contractCustomerSubmitFlowIssueI19.test.mjs contractNewDefaults.test.mjs contractCustomerCreateNavigation.test.mjs contractDetailPreviewNavigation.test.mjs contractApprovedDetailReturn.test.mjs`
- `.venv\Scripts\python.exe -m unittest customer_backend_alignment_d6_contract_test.py customer_backend_contract_matrix_I4.py customer_file_contact_backend_d11_contract_test.py customer_note_update_contract_test.py customer_conflict_empty_query_test.py`
- `$env:CONTRACT_TEST_PASSWORD='..123456'; .venv\Scripts\python.exe -m unittest contract_attachment_batch_delete_test.py contract_attachment_status_test.py contract_lifecycle_test.py contract_event_backend_d9_contract_test.py`
- `npm.cmd run build`
- `git diff --check`

## 浏览器验收

使用本机 Chrome + Playwright 登录 `admin / ..123456` 后抽验：

- `/?page=hr-all`：员工管理、是否可用、系统用户、删除。
- `/?page=hr-departments`：部门管理、是否逾期扣款；新增部门弹窗显示角色管理说明，不再显示“权限来源部门”。
- `/?page=user-communications`：沟通日志、客户 ID/客户名称列、查看；新增沟通记录弹窗显示附件/上传附件。
- `/?page=customer-mine`：合同数量、民事案件数量、无直接客户删除。
- `/?page=customer-shared`：共享接收人列。
- `/?page=customer-company`：更多操作中有客户编辑、客户删除。
- `/?page=customer-conflict`：客户利益冲突检索入口和提示。
- `/?page=contract-new`：仅“合同基本信息 / 提交审批”两步，不显示同步用印步骤。

## 未部署说明

本台账只证明本地闭环。测试服务器部署仍需用户明确要求后执行；部署前应再次确认 `dev` 最新提交、生产构建、后端回归和服务健康。
