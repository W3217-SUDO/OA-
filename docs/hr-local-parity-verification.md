# 人事中心本地对照验收记录

日期：2026-08-01

本阶段只在本地 `dev` 工作副本验证，未推送、未部署、未在服务器构建。

- 本地页面：`http://127.0.0.1:5173/?page=hr-new`，页面正常加载。
- 员工新建页包含旧系统核心字段：员工号、用户名、中文姓名、角色、密码、公司、部门、职务、数据级别、是否可用、账号类型、身份证号、手机、英语等级、专业学历、分机、籍贯、外语、毕业时间、社保情况、毕业学校、住址、身份证地址。
- 开发系统已有的额外字段保留，未因对照而删除或强制设为必填。
- 员工子页签“请假记录、事项记录、员工档案、提成设定”均可点击并显示对应页面。
- 空表点击“保存”显示“提示 / 请输入员工号.”，并在表单校验及 API 请求前返回；本次未提交任何业务数据。

结论：员工基本信息保存拦截与本地页面结构已完成证据验证；其他人事中心页面继续按同样的本地对照闭环处理。服务器同步延期至全部本地模块完成后由用户明确触发。

## 2026-08-01 员工子记录四页本地契约复核

旧系统只读入口证据：已在 `https://sh.021ipr.com/Console/Index` 展开“人事中心”，可见“新建员工”和“员工管理”入口；旧站进入新建员工时浏览器导航中断，未执行任何写入操作。

本地源码与测试证据：`apps/admin-web/src/HrCenterPage.tsx` 已核对四页的真实交互挂钩，并新增 `apps/admin-web/employeeSubrecordsParity.test.mjs` 覆盖：

- 请假记录、事项记录、提成设定的真实子记录 GET/POST/PATCH/DELETE；
- 员工档案的附件列表、上传、下载、删除接口；
- 未保存员工时请假/提成新建门禁及员工档案上传门禁；
- 提成按案件查询适用方案；
- 保存、编辑、删除和取消后的表单清理。

执行命令：`node --test apps/admin-web/employeeSubrecordsParity.test.mjs`。本项只证明本地页面的四页交互契约与旧入口范围，未替代旧系统逐按钮浏览器验收；旧系统导航阻断和本地开发页未启动，故页面点击证据仍标记为“已部署，仍未证明”。

## 2026-08-01 部门与角色双页面复核

通过内置浏览器同时打开申浩旧系统与本地 `dev` 页面，旧系统仅浏览、不提交表单：

- 部门列表：旧系统列为序号、部门名称、部门代码、操作；本地保留这些字段并增加上级部门、逾期扣款、负责人、排序、可用等扩展字段。
- 部门新增弹窗：旧系统包含部门名称、部门代码、上级部门、确定、取消；本地包含对应字段，并保留负责人、逾期扣款、排序号、可用、保存、取消。
- 角色列表：两边均为序号、角色名称、角色权限、操作；本地额外保留删除操作，旧数据只读。
- 角色新增弹窗：旧系统包含角色名称、角色名称描述、确定、取消；本地包含角色名称、说明、保存、取消，并保留角色代码、可用状态等扩展字段。

本次只打开并关闭旧系统弹窗，未填写、保存、修改或删除旧数据；本地页面字段与按钮已复核。

## 2026-08-01 新建员工双页面复核

- 旧系统新建员工页包含员工号、用户名、中文姓名、角色、密码、公司、部门、职务、数据级别、是否可用、账号类型、身份证号、手机及英语等级、专业学历、分机、籍贯、外语、毕业时间、社保情况、毕业学校、住址、身份证地址等字段，并提供请假记录、事项记录、员工档案、提成设定入口和保存按钮。
- 本地新建员工页逐项保留上述字段和四个子页签，并保留现有扩展字段（代理人/律师证照、日期、职称、社会职务、政治面貌、年休、提成比例、性别、婚姻、生育、合同到期日、在职情况、Email、IM 等）。
- 旧系统仅浏览入口与字段，本次未填写或保存；本地页面可见保存操作由现有校验和测试门禁保护。

## 2026-08-01 员工列表服务端分页复核

- 旧系统只读页面确认员工列表提供六项筛选（公司、部门、用户名、中文名、手机号、是否可用）、查询入口、旧版字段列及分页区域；未发现独立“重置”按钮。
- 本地 `HrCenterPage` 改用专用 `GET /hr/employees`，将六项筛选、数据范围和 `page/page_size/total` 交由服务端处理，表格按返回的 `current/total` 翻页；系统账号-only 行由接口一并返回，未丢失原有显示。
- 服务端在应用数据范围后执行筛选和分页，额外列（合同审批流程、Email、扩展电话等）仍由本地页面保留。
- 旧系统未执行任何写入；本地验证命令：`node --test apps/admin-web/employeeListServerPagination.test.mjs apps/admin-web/employeeListBulkDeleteUi.test.mjs apps/admin-web/employeeBulkDelete.test.mjs apps/admin-web/hrAccessGuard.test.mjs`（8/8）；后端 `compileall` 与前端 `npm.cmd run build` 通过。

## 2026-08-01 权限与状态按钮复核

- 本地按角色保留旧系统可见的查看、修改、状态办理、密码重置、系统用户和删除入口；管理员拥有账号维护权限，部门负责人仅办理人事状态，普通用户仅查看。
- 服务端同步执行同等权限拦截：新建/编辑/删除/批量删除/密码重置/系统用户仅管理员；状态办理和员工附属记录允许管理员或部门负责人；停用账号不能继续登录，管理员账号不能被自身停用或删除。
- 旧系统只读检查未写入业务数据；本地 `hrAccessGuard` 与 API 权限测试通过，未部署服务器。

## 2026-08-01 员工四个子页归档源码只读证据

本节使用 `旧系统归档源码/SH.CRM.WEB` 中 HR 员工视图、局部视图和脚本做只读核对，未修改旧源码、未提交旧系统表单。由于当前子线程无法接管主线程的内置浏览器标签，旧系统与本地四页的“逐个点击”仍由主线程补做并记录；以下不是浏览器点击的替代证明。

- 请假记录：旧 `OffWorkDayList.cshtml` 表头为“请假开始日期、请假结束日期、请假小时、请假类型、备注、操作”，非空行提供新增和删除，空态提供“新建”；`OffWorkDay.cshtml` 弹窗包含开始时间、结束时间、小时数、请假类型、备注，按钮为“确定”“取消”及右上角关闭，脚本在员工未保存时提示“请先保存员工基本资料.”。
- 事项记录：旧 `StaffEventList.cshtml` 表头为“序号、内容、操作人、操作日期、操作”，非空行提供新增和删除，空态提供“新建”；`StaffEvent.cshtml` 弹窗只有必填事项内容（最多 1000 字、四行文本框），按钮为“确定”“取消”及右上角关闭，脚本为空内容时阻断提交。
- 员工档案：旧 `StaffFileList.cshtml` 表头为“序号、上传人、文件名称、文档日期、查看、操作”，空态提供“上传员工文档”，保留分页容器；`StaffFileUpload.cshtml` 弹窗包含必选员工文档文件选择框，按钮为“上传”“取消”及右上角关闭。脚本提供真实上传、查看/下载、删除入口。
- 提成设定：旧 `PerformanceList.cshtml` 表头为 15 列（序号、起止日期、基本工资、开庭/文书/案源/调查/品管五类比例和固定提成、操作），非空行提供新增、删除、编辑，空态提供“新建”，保留分页容器；`Performance.cshtml` 弹窗包含起止日期、基本工资及上述五类比例/固定额，按钮为“确定”“取消”及右上角关闭。归档脚本还保留按案件查看适用提成的入口语义。

本地 `HrCenterPage.tsx` 已保留四页真实 API、编辑/删除、取消清理、档案上传/下载/删除和提成按案件查询；本轮补齐档案“查看”在线预览（图片、PDF、文本/DOCX，格式不支持时提示下载），并补齐未保存员工时“事项记录”的创建门禁，避免生成孤立子记录。浏览器逐项点击（包括旧/本地每页标题、按钮、弹窗、分页及空态）仍待主线程在已有双页面会话中完成。
