# 8.12 Excel 问题逐行验收台账

来源：`C:\Users\Administrator\Desktop\OA系统\问题\8.12OA系统对接.xlsx`，工作表“8月12日”。

| 行号 | 问题 | 同行截图 | 新系统定位与修改 | 验证 | 本地浏览器验收 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 2 | 固定查询/表头，仅滚动数据区 | 已读取同一行截图 | `App.tsx`、`styles.css`、`HrCenterPage.tsx`、`hr-center.css` | 已完成定向检查 | 已在本地员工管理验证 | 已验收 |
| 3 | 修改员工信息保存失败 | 已读取同一行截图 | `main.py`：仅在职务变更时校验启用岗位 | 历史职务改手机号保存回归通过 | 已在本地验证保存及刷新回写 | 已验收 |
| 4 | 离职后可复职 | 已读取同一行截图 | `main.py`、`hrAccessGuard.mjs`：增加离职/停用到在职流转并同步登录账号 | HR 状态回归通过 | 已在本地验证离职员工可改为在职且账号启用 | 已验收 |
| 5 | 当前系统管理员无密码重置路径 | `image118.png` | `main.py` 允许管理员重置当前账号；`HrCenterPage.tsx`、`SystemCenterPage.tsx` 解除前端阻断并移除错误提示 | `system_hr_backend_gap_contract_test.py -k reset_the_current` 通过；`node --test hrEmployeePasswordResetAction.test.mjs systemUsersSecurityD5.test.mjs` 17/17 通过；`py_compile` 通过 | 2026-08-13，`http://127.0.0.1:5178/?page=hr-all`：以 `CODEX-812` 当前管理员登录，筛选本人并成功打开“重置密码”弹窗；显示新密码输入与确认按钮，未显示禁止提示。测试用户及 HR 档案已清理。 | 已验收 |
| 6 | 未授予角色权限仍可访问全部功能 | `image119.png` | `App.tsx`：非管理员授权集合只采用登录响应的 `menu_keys`，不再把全部导航路由当作隐式授权 | `node --test appSidebarPermissionParity.test.mjs` 通过；`system_hr_backend_gap_contract_test.py -k user_permission_overrides` 通过；前端生产构建通过 | 2026-08-13，`http://127.0.0.1:5178/`：以仅有 `user-center` 的 `CODEX-812` 受限账号登录，侧边栏仅显示控制台和用户中心；直接访问 `?page=contract-mine` 显示“无权访问”。测试用户及 HR 档案已清理。 | 已验收 |
| 7 | 我的客户列表显示条数/页面宽度 | `image120.png`、`image121.png` | 现有 `CustomerCenterPage.tsx` 使用独立数据区滚动、总数与每页条数控件（10/15/20/50/100/200）；未发现需改动的产品逻辑 | 使用 16 条 `CODEX-812` 客户验证服务端分页 | 2026-08-13，`http://127.0.0.1:5178/?page=customer-mine`：显示“共有16条，每页显示：15条”，第一页 15 行，点击下一页后仅显示第 16 行且下一页禁用；表头完整、表格数据区单独滚动。测试客户、账号和 HR 档案已清理。 | 已验收 |
| 8 | 客户编辑修改客户来源后首页仍显示原人员；编辑界面应与新建界面一致 | 已读取同行 3 张截图：编辑页客户来源字段、首页案源人字段、新建客户完整字段布局 | `CustomerCenterPage.tsx`、`customerParity.mjs/.d.mts`：编辑保存同步 `customer_source` 与 `source_person`；新建保存也同步提交来源字段；列表/详情优先使用后端 `customer_source_display_name`。`main.py`：客户列表返回后端解析的 `customer_source_display_name`。 | `node --test customerSourceEditParity.test.mjs` 2/2；`npm.cmd run build` 通过；`python -m py_compile app/main.py` 通过；`git diff --check` 通过（仅行尾提示） | 2026-08-13，`http://127.0.0.1:5173/?page=customer-new` 新建 `CODEX-812-COMPANY-SOURCE-FIX` 并选择系统人员来源，保存后进入公司客户列表，案源人实际显示 `CODEX-HR-D2-COMMISSION-20260802-02`；新建表单字段与同行截图一致。临时客户已精确清理。 | 已验收 |
| 9 | 客户中新建合同选择同步用印后，“我的用印申请-待审批”不显示 | 已读取同行截图 `row-9-image-10.png`：旧系统“用印中心 > 我的用印申请 > 待审批”应显示同步生成的申请 | `main.py`：合同审批完成时，同步用印草稿直接转为 `待审批`，不再以未上传用印文件阻断；合同、用印申请和审批人关联不变。 | `python -m unittest contract_sync_seal_pending_contract_test.py -v` 1/1；`python -m py_compile app/main.py`；`node --test sealWorkflowPolicy.test.mjs sealCreateDefaultsParity.test.mjs` 18/18；`npm.cmd run build` 均通过。 | Codex 内置浏览器 `http://127.0.0.1:5173/?page=seal-my` 已验收“我的用印申请”列表显示本轮申请：申请号 `YY202608131308499AE`、合同号 `SHHT2610069`、客户 `CODEX-812-ROW9-CUSTOMER`、状态后端 `待审批`（页面列显示 `待审核`）、用印类型 `合同用印`、印章类型 `合同章`；合同附件 `CODEX-812-ROW9-CONTRACT.pdf` 已上传并参与审批。同行截图无额外嵌入截图文件，已明确记录。验收后 `CODEX-812-ROW9-*` 客户/合同/用印/审批/附件/流水/旧版投影及临时文件精确清理，数据库残留计数全为 0。 | 已验收 |
| 10 | 待用印登记实际用印时不应强制填写归档号 | 已读取同行截图 `tmp-812-images/row-10/image-12-image126.png`：旧系统登记实际用印弹窗将归档号标为必填 | `SealCenterPage.tsx`：单条和批量“登记实际用印”归档号均改为可选；后端 `SealStampInput` 本已支持空值，后续归档动作仍独立校验归档号。 | `node --test sealStampArchiveOptional.test.mjs` 1/1；`python -m py_compile app/main.py`；`npm.cmd run build` 均通过。 | 2026-08-13，Codex 内置浏览器本地 `127.0.0.1:5173` 创建 `CODEX-812-STAMP-EMPTY-ARCHIVE`，在“登记实际用印”窗口保持归档号为空并提交，提示“实际用印已登记”；“已用印”列表确认状态为“已用印”，数据库记录 `archive_no` 为 `""`。测试申请、2 条事件、附件记录、测试印章资产及审计、临时上传文件均已清理为零。 | 已验收 |
| 11 | 取消客户分级调整、关键字段变更的独立审批，统一在客户编辑直接修改 | 已读取同行截图 `tmp-812-images/row-11/image-13-image127.png`：旧系统“更多操作”含两条申请入口；本行要求新系统取消 | `CustomerCenterPage.tsx`：从“更多操作”移除两条申请入口及审批动作；遗留兼容弹窗不会提交而提示改用客户编辑。`main.py`：四个旧审批 URL 统一返回 HTTP 410 和直接编辑指引，避免继续写入待审批数据。 | `node --test customerEditOnlyParity.test.mjs` 2/2；`python -m py_compile app/main.py`；`npm.cmd run build` 均通过。 | 2026-08-13，Codex 内置浏览器本地 `http://127.0.0.1:5173/?page=customer-mine`：选中 `CODEX-812-COMPANY-SOURCE-FIX` 打开“更多操作”，菜单仅含客户编辑、释放到公海、新增合同、沟通/联系人/共享/客户服务端操作，两条旧审批入口不存在；进入客户编辑将等级从“立案客户”改为“高级客户”并保存，提示“客户已更新”；刷新后重新打开编辑窗口，等级仍为“高级客户”。三条 `CODEX-812` 测试客户、5 条工作流事件及旧表映射已精确清理；用户总数未减少，其他 CODEX 测试数据未触碰。 | 已验收 |
| 12 | 客户服务端开通账号、激活码、登录入口及客户联系人账号关联 | 已读取同行旧系统截图 `tmp-812-images/row-12/row12-image-14-image128.png`：旧系统开通后交付服务账号和一次性激活码，客户首次登录用二者设置密码；Excel 备注旧入口为 `http://vipuser.021ipr.com/` | 新系统原先按客户编号生成 `vip-{serial_no}`，未使用客户编辑中的客户联系人账号。`main.py`：开通时必须从已绑定联系人账号中选取，多个账号要求选择，账号必须存在且启用；无联系人或停用账号明确阻断。`CustomerCenterPage.tsx`：兼容单值 `contact` 和多值 `contact_accounts`，多个联系人显示账号选择弹窗，单个账号直达开通，开通弹窗显示当前域名下的客户服务端地址 `/?page=customer-portal`、账号和一次性激活码。 | `node --test customerPortalContactAccountParity.test.mjs customerEditOnlyParity.test.mjs customerSourceEditParity.test.mjs`：7/7；`python -m py_compile apps/api-server/app/main.py`；`npm.cmd run build` 通过（5624 modules）；`git diff --check` 通过（仅换行提示）；CodeGraph 同步通过。 | 2026-08-13，Codex 内置浏览器本地验收：创建 `CODEX-812-PORTAL-R2-CUSTOMER`，绑定两个启用联系人账号；开通弹窗显示两个账号，选择 `codex-812-portal-r2-a` 后生成服务地址、服务账号和一次性激活码；进入 `http://127.0.0.1:5173/?page=customer-portal`，填入账号和激活码并停在“激活并设置密码”前，未写入密码。客户、门户配置、事件、通知和两个测试账号均精确清理，`CODEX-812-PORTAL-R2-*` 残留为 0。 | 已验收 |
| 13 | 公司客户页面显示条数有问题，需要拉宽一点 | 已读取 `tmp-812-images/row-13/row13-image-1.png`：红框显示公司客户页底部“每页显示”下拉被宽表横向布局挤压。 | `customer-center.css`：客户列表页脚改为可换行布局；分页区不可压缩，窄桌面宽度下独占一行并可横向滚动，避免“每页显示”选择器和页码被截断。客户筛选、数据范围与分页逻辑不变。 | `npm.cmd run build` 通过（5624 modules）；`git diff --check -- apps/admin-web/src/customer-center.css` 通过。 | 2026-08-13，Codex 内置浏览器在本地 `http://127.0.0.1:5173/?page=customer-company` 验收。默认宽度和 1080px 窄桌面宽度下均完整显示“共有41条、每页显示、15 下拉、页码、GO”；分页选择器不再被挤出可见区域。 | 已本地验收 |
| 14 | 公司客户客户编辑与我的客户一致：来源回写、编辑界面一致 | 已读取同行截图 `tmp-812-row14-readonly/row14-image-1.png`：公司客户编辑窗口应与新建客户使用基本信息、法人信息、开票信息、控制信息四个分区及同一字段顺序 | `apps/admin-web/src/CustomerCenterPage.tsx`：公司客户编辑表单与新建客户完整表单统一为四分区、字段和控件；保留编辑态编号/统一社会信用代码只读、客户管理人更新接口及 `synchronizeCustomerSource` 保存逻辑。新增 `customerCompanyEditParity.test.mjs` 防止分区/字段顺序漂移 | `node --test customerCompanyEditParity.test.mjs customerEditOnlyParity.test.mjs customerSourceEditParity.test.mjs` 7/7；`npm.cmd run build` 通过（5625 modules）；`python -m py_compile apps/api-server/app/main.py`；`git diff --check` 通过（仅 CRLF 提示）；CodeGraph 同步完成 | 2026-08-13，Codex 内置浏览器 `http://127.0.0.1:5173/?page=customer-company`：创建唯一 `CODEX-812-ROW14-COMPANY`，新建页显示四个分区和完整字段；公司客户列表显示来源中文名 `CODEX-HR-D2-COMMISSION-20260802-02`；打开客户编辑显示同样四个分区、来源字段和中文列表；保存来源修改后刷新，列表仍正确回写来源。测试客户、3 条工作流事件及旧 CRM 客户映射已精确清理，`CODEX-812-ROW14-*` 残留为 0 | 已验收 |
| 15 | 管理员新建合同后由律师助理2审批，但审批人账号没有待审批合同 | 已读取同行 2 张截图：审批人账号待审批列表为空；管理员我的合同状态为审批中 | `ContractCenterPage.tsx`、`contractAuditScope.ts`：待审批前端按当前审批人过滤；`main.py`：合同列表范围补充当前审批节点的审批人可见条件；新增 `contract_pending_approver_scope_contract_test.py`。 | 前端合同审批回归 2/2；后端内存 SQLite 回归 1/1；`py_compile`、构建、`git diff --check` 通过 | 2026-08-13，内置浏览器切换到隔离审批账号 `CODEX-812-律师助理二`，进入 `?page=contract-audit-pending`，实际显示 `CODEX-812-APPROVAL-CONTRACT` 且状态为“审批中”；临时账号、合同、审批步骤已精确清理。 | 已验收 |
| 16 | 部门合同按客户管理人所属部门筛选 | 已逐图读取；以客户管理人所在部门为筛选口径 | `main.py`：部门合同关联客户管理人所属部门；新增 `contract_customer_manager_department_row16_test.py`，测试构造使用真实 `auditor` 授权数据范围，不放宽可见性条件。 | 后端定向测试 1/1；`py_compile` 通过；前端生产构建通过（5625 modules）。 | 2026-08-13 复核（LUNA）：Codex 内置浏览器本地 `http://127.0.0.1:5173/?page=contract-dept` 确认“部门合同”页面加载成功；隔离测试以授权审批真实可见权限验证跨部门合同仅返回客户管理人在当前部门的合同；未改动并发文件。 | 已验收 |
| 17 | 合同审批通过状态显示为“审批通过” | 已读取同行截图 | `ContractCenterPage.tsx`、`contractStatusPresentation.mjs`：保持原始工作流状态为“已通过”，仅将所有合同列表、详情、审批与向导展示映射为“审批通过”。`main.py`：修复本地 SQLite 历史 `FCM_Contract` 的 BIGINT 主键投影写入，避免新建合同 500。 | `node --test contractApprovalStatusPresentationRow17.test.mjs` 3/3；后端合同创建回归、`py_compile`；`npm.cmd run build` 通过。 | 2026-08-13，Codex 内置浏览器隔离本地环境：新建 `CODEX-812-ROW17-BROWSER` 合同成功进入提交审批，提交真实审批节点并审批通过后，“我的合同”实际显示“审批通过”。测试客户、合同、附件、审批步骤、兼容投影、审批人账号及 HR 档案均已精确清理。 | 已验收 |
| 18 | 合同新建案件填写的被告应在案件中显示 | 已读取同行旧系统截图，确认合同新建案件的被告应进入创建后案件列表/详情 | 新 OA：`caseSecondBatchParity.ts` 创建载荷补传 `opponent`；`main.py` 合同创建落库 `opponent` 与 `defendants` 投影；创建旧表 `Legal_Case` 时使用负业务记录 ID，兼容旧库 `CaseId NOT NULL` 且保持稳定关联；新增 `case_contract_pending_litigant_projection_test.py` 及路由回归覆盖 | 案件创建/合同预填/被告编辑前后端定向测试通过；兼容投影回归 18/18；Python 编译、前端生产构建（5625 modules）、`git diff --check` 均通过 | 2026-08-13，Codex 内置浏览器仅使用隔离的当前代码环境 `http://127.0.0.1:5180` / `8020`：从真实合同 `HT2026070018` 新建民事案件，进入当事人页保存 `CODEX-812-ROW18-R2-DEFENDANT`，选择上海市徐汇区人民法院后完成；案件列表和详情均回显该被告，业务记录 `defendants/opponent` 与旧表 `Legal_Case.AppelleeNames` 一致，旧表 `CaseId=-236`。测试案件 `SHMS2600428`（ID 236）、旧表投影、参与人、旧表日志及 3 条工作流事件均已精确删除，残留为 0。 | 已实现并完成本地浏览器验收；未提交、未部署 |
| 19 | 公司案件增加删除案件功能 | 已读取同行截图 `image136.png`：公司案件列表底部“更多操作”应提供删除案件入口 | `apps/api-server/app/main.py`：新增 `DELETE /api/v1/cases/{case_id}`，仅管理员/管理人员可删，拒绝已归档/已合并案件，并级联清理案件任务、通知、附件、费用、开庭排期、工作流记录；`apps/admin-web/src/CaseCenterPage.tsx`：公司案件选中记录后在“更多操作”显示“删除案件”，确认后刷新列表；新增 `companyCaseDeleteRow19.test.mjs`、`company_case_delete_row19_test.py` | `node --test companyCaseDeleteRow19.test.mjs` 2/2；`npm.cmd run build` 通过；`python -m unittest company_case_delete_row19_test.py -v` 1/1；`python -m py_compile app/main.py` 通过；`git diff --check` 通过（仅行尾提示） | 2026-08-13，本地 `http://127.0.0.1:5173/?page=case-company` 内置浏览器：管理员选中案件后打开“更多操作”，已看到“删除案件”菜单项；定向删除级联测试使用 CODEX 测试数据并自动清理，现有业务数据未删除。 | 已验收 |
| 20 | 案件详情按案件信息、法院信息、归档信息与下方功能重排 | 已读取 Excel 同行文字及 `问题/tmp_8_12_row20/image-22.png`、`image-23.png` 两张截图。截图确认详情顶部依次为案件信息、法院信息、归档信息，下方为文档、费用、结算、提醒、日志、任务、线索等功能区；8091 本机访问返回 Invalid Hostname，未修改旧系统 | `apps/admin-web/src/CaseCenterPage.tsx`：详情标题由“基本信息”改为“案件信息”；在法院信息后新增独立“归档信息”区，读取现有 `archive_status/archive_no/archive_submitter/archive_submitted_at/archive_submit_comment/archive_reviewer/archive_reviewed_at/archive_review_comment/archived_at` 字段；下方现有功能区保留 | `caseDetailLayoutRow20.test.mjs`：1/1；`apps/api-server/.venv/Scripts/python.exe -m py_compile app/main.py`：通过；`git diff --check`：通过；2026-08-13 全批复核 `npm.cmd run build` 通过（5625 modules） | 2026-08-13 Codex 内置浏览器 `http://127.0.0.1:5173/?page=case-detail-767-SHMS2600395`：打开真实案件详情，确认“案件信息”→“法院信息”→“归档信息”顺序；归档状态、归档号、提交人/时间/备注、审核状态/审核人/时间/备注、归档日期均显示；下方功能标签及案件文档目录显示正常。截图已留存于本次验收记录；未创建测试数据、无需清理 | 已验收（本地）；未提交、未部署 8089 |
| 21 | 案件详情修改操作统一收纳到右上角“操作” | 已读取同行截图 `_issue_images/812-row21/image-25.png`、`image-26.png`：旧系统将案件详情的修改、分配、文书、合并、归档等入口集中在右上角“操作”下拉菜单 | `apps/admin-web/src/CaseCenterPage.tsx`：普通案件详情原先直接渲染 `caseDetailActionButtons`，现统一通过右上角 `data-testid="case-detail-operation-menu"` 的“操作”下拉渲染；公司排期详情继续保留专用菜单 | `node --test caseDetailOperationMenuRow21.test.mjs` 1/1；`npm.cmd run build` 通过（5625 modules）；`git diff --check` 通过 | 2026-08-13，Codex 内置浏览器 `http://127.0.0.1:5173/?page=case-mine` 打开案件 `SHMS2500709D`：详情头部仅显示“案件智能体/操作”；展开“操作”后确认登记进展、人员分配、修改当事人、修改基本信息、生成案件文书、复制案件、合并案件、修改公证信息、修改诉讼/判决金额、申请归档均在菜单内 | 已验收 |
| 22 | 案件文档信息提供上传文档入口 | 已读取Excel同行文字及两张截图：新OA空案件文档列表缺少上传入口；8091旧系统在空列表中央显示“没有查到文档。上传文件”。8091仅作只读对照，未修改 | `apps/admin-web/src/CaseCenterPage.tsx`：案件文档表格空状态增加“没有查到文档。”与“上传文件”按钮，沿用现有案件文档分类、权限及 `/attachments` 上传接口；保留底部上传工具栏。新增 `caseDocumentUploadRow22.test.mjs` | `node --test caseDocumentUploadRow22.test.mjs` 1/1；`npm.cmd run build` 通过（5625 modules）；`git diff --check` 通过 | 2026-08-13 Codex内置浏览器本地 `http://127.0.0.1:5173/?page=case-detail-183-SHMS2600395`：打开案件文档信息，切换至空目录“庭审及庭后文件”，看到“没有查到文档。上传文件”；点击该入口选择并上传 `CODEX-812-ROW22-upload.txt`，列表出现文件且提示“案件文件已上传”。附件、上传文件及关联工作流记录已精确清理为0 | 已验收（本地）；未提交、未部署 8089 |
| 23 | 律所费用新增费用完整、绑定合同号并支持费用类型 | 已读取同行两张截图 `tmp_8_12_row23/image143.png`、`image144.png`：旧系统弹窗含费用名称、费用归属、费用类别、金额、经办人员、收款单位、缴费法院/机构、缴费通知文号、说明；费用页按合同号展示，新增入口支持官费、第三方费用、代理费、其他费用及延伸官费类型 | `CaseCenterPage.tsx`：新增费用弹窗改为可增删多条费用，合同号使用当前客户合同下拉并默认回填案件合同，截止日期和旧系统字段齐全；快捷费用类型会回填第1行；创建逐条提交并绑定案件/合同 ID。`main.py`：费用请求模型允许全部延伸类型；新增/编辑均校验合同归属当前案件客户，并保存 `contract_id/contract_no`。新增/更新 `caseLawFirmFeeRow23Parity.test.mjs`、`case_law_firm_fee_row23_contract_test.py`。8091 只读访问返回 400 Invalid Hostname，未修改旧系统。 | `node --test caseLawFirmFeeRow23Parity.test.mjs caseFirmFeeRow23.test.mjs` 3/3；`python -m unittest case_law_firm_fee_row23_contract_test.py -v` 2/2（含跨客户合同返回 409）；`py_compile app/main.py case_law_firm_fee_row23_contract_test.py`；`npm.cmd run build`（5625 modules）均通过。 | 2026-08-13，Codex 内置浏览器 `http://127.0.0.1:5173/?page=case-detail-183-SHMS2600395` 打开真实案件 `SHMS2600395` 的“律所费用”页：分类菜单可选“诉讼费”等延伸类型；新增弹窗显示合同号下拉、费用类别、截止日期、收款单位、缴费法院/机构、缴费通知文号、说明；快捷选择“诉讼费”后第1行类别显示“诉讼费”；点击“新增费用”出现第2行（两组合同号字段），点击删除后恢复单行。仅使用现有业务数据做可见性和表单验收，未点击创建，未产生 `CODEX-812-ROW23-*` 数据，无需业务数据清理。 | 已验收 |
| 24 | 新增律师代理费支持新建员工提成 | 已读取同行文字；该行无嵌入图片、批注或超链接 | `apps/admin-web/src/CaseCenterPage.tsx`：案件详情“律所费用 > 新增案件费用 > 代理费”增加员工提成明细编辑器，支持多条系统在职员工、提成类型、金额、备注；编辑草稿回显 `commission_details`。`apps/admin-web/src/FinanceCenterPage.tsx`、`apps/api-server/app/main.py` 同步支持财务代理费表单及后端校验。 | `node --test financeAgencyFeeEmployeeCommissionRow24.test.mjs`：3/3；`python -m py_compile app/main.py`；`npm.cmd run build`：5625 modules；`git diff --check` 通过 | 2026-08-13，Codex 内置浏览器在隔离的当前代码环境 `http://127.0.0.1:5180/?page=case-detail-183-SHMS2600395` 完成正向验收：进入“律所费用 > 新增案件费用 > 代理费”，创建 `CODEX-812-ROW24-AGENCY-R2`，选择系统在职员工 `CODEX-HR-D2-COMMISSION-20260802-02（诉讼一部）`，填写员工提成 10 元及备注；保存后进入“其他操作 > 修改”，员工、提成类型、提成金额和备注均正确回显。旧 API 遗留测试记录和本次 `CODEX-812-ROW24-*` 记录均已精确删除。 | 已实现并完成本地浏览器验收；未提交、未部署 |
| 25 | 新增费用后显示提交日期 | 已读取同行截图 `问题/row25-image148.png`：旧系统案件详情“律所费用”表格的“提交日期”列应显示新增费用日期；只读对照旧系统截图，未修改 8091 | 根因一：费用列只读取 `row.data.created_at`，现改为优先 `submitted_at`，回退顶层及兼容数据内创建时间；提交审批时持久化 `submitted_at/submitted_by`。根因二：公司案件“归档信息-查看”误绑 `openCaseTasks`，已改为 `openCounselDetail`，可进入真实案件详情。改动：`apps/admin-web/src/CaseCenterPage.tsx`、`apps/api-server/app/main.py`、`apps/admin-web/caseFeeSubmittedDateRow25.test.mjs` | `node --test caseFeeSubmittedDateRow25.test.mjs` 1/1；`python -m py_compile app/main.py`；`npm.cmd run build`（5625 modules）；`git diff --check` 均通过；CodeGraph 已同步 | 2026-08-13，Codex 内置浏览器本地 `http://127.0.0.1:5173/?page=case-company`：点击 `SHMS2600395` 行“归档信息-查看”进入 `?page=case-detail-183-SHMS2600395`，未再出现案件任务弹窗；进入“律所费用”，创建唯一 `CODEX-812-ROW25-FEE` 其他费用 25 元，列表实际显示提交日期 `2026-08-13`。随后通过“其他操作-删除-确认删除”精确清理，列表恢复“暂无数据”，测试记录零残留。 | 已验收（本地）；未提交、未部署 8089 |
| 26 | 案件其他操作功能按旧系统补齐 | 已读取同行两张截图：旧系统案件费用“其他操作”包含“法院退费、申请付款、申请开票、修改、删除、标记不缴费”六项 | `apps/admin-web/src/CaseCenterPage.tsx` 补齐六项菜单及前端动作；`apps/api-server/app/main.py` 补齐费用修改、删除、标记不缴费接口及状态/权限审计；新增 `apps/admin-web/caseOtherOperationsRow26.test.mjs`、`apps/api-server/case_other_operations_row26_test.py` | `node --test caseOtherOperationsRow26.test.mjs` 2/2；隔离 API `8020` 动态后端用例 `case_other_operations_row26_test.py` 1/1；`python -m py_compile apps/api-server/app/main.py`；`npm.cmd run build` 通过（5625 modules）；`git diff --check` 通过 | 2026-08-13，Codex 内置浏览器本地案件详情 `http://127.0.0.1:5173/?page=case-detail-183-SHMS2600395`：律所费用页实际显示六项菜单；创建临时其他费用后，实际完成“修改”金额 1→2 保存、“标记不缴费”确认提交、“删除”确认提交，删除后列表恢复“暂无数据”。临时费用及相关测试数据已清理，未提交、未部署 | 已验收（本地）；未提交、未部署 8089 |
| 27 | 法院退费应绑定原案和官费，金额同步 | 已读取 Excel 同行文字及两张截图；8091 仅作只读参考，未修改旧系统 | `CaseCenterPage.tsx`：选中官费后“其他操作 → 法院退费”打开专用弹窗，展示原案、原官费编号和原金额；提交携带 `fee_record_id`、案件号、客户及原缴费凭证号。`main.py`：强制校验官费归属当前案件、退费累计不超过官费金额，并把 `case_id/case_record_id/case_no` 保存到退费记录；案件关系接口返回官费累计退费金额供费用表“退费”列同步显示 | `case_court_refund_row27_test.py` 1/1；`caseCourtRefundRow27.test.mjs` 1/1；`py_compile app/main.py`；`npm.cmd run build` 5625 modules；均通过 | 2026-08-13，Codex 内置浏览器在隔离的当前代码环境 `http://127.0.0.1:5180/?page=case-detail-183-SHMS2600395` 完成正向验收：创建 `CODEX-812-ROW27-OFFICIAL` 官费 100 元；选中后进入“其他操作 > 法院退费”，弹窗正确展示原案 `SHMS2600395`、原官费编号和原金额 100；提交退费 40 元后，费用表“退费”列立即同步显示 40。官费及退费两条 `CODEX-812-ROW27-*` 关联记录已按精确 ID 删除，残留为 0。 | 已实现并完成本地浏览器验收；未提交、未部署 |
| 28 | 费用申请付款可填写、绑定金额和付款账号 | 已逐行读取 Excel 文字及同行两张截图；8091 仅作只读业务参考 | `apps/admin-web/src/CaseCenterPage.tsx`：草稿/已退回律所费用可打开付款申请弹窗并填写本次金额、付款账号；`apps/api-server/app/main.py`：允许对应状态提交付款审批并持久化 `payment_requested_amount/payment_account`；`apps/api-server/case_finance_closure_contract_test.py` 补真实接口断言 | 前端定向测试 2/2；后端定向测试 6/6；`git diff --check` 通过 | 2026-08-13，Codex 内置浏览器仅使用隔离的当前代码环境 `http://127.0.0.1:5180` / `8020` 完成正向验收：创建 `CODEX-812-ROW28-PAYMENT`（费用号 `FY20260813151635531438`，金额 88.88），申请付款 66.66 至 `CODEX-812-ROW28-ACCOUNT`；页面提示“付款申请已提交审批”，费用行真实回写金额 66.66 和付款账号。记录 `id=241`、本次事件 `279/287` 及关联流水已精确删除，残留均为 0，刷新后费用表显示暂无数据。 | 已实现并完成本地浏览器验收；未提交、未部署 |
| 29 | 申请开票绑定合同与数据，已申请不可重复申请 | 已逐行读取 Excel 文字及同行截图，并按旧系统开票关联链路核对 | `apps/admin-web/src/FinanceCenterPage.tsx`：开票关联数据请求统一限制为后端允许的 `page_size: 100`，申请时绑定合同、案件和费用，并在前端拦截同一案件费用的重复开票申请；`apps/admin-web/caseFinanceClosure.test.mjs` 补充接口参数与重复申请回归 | `node --test caseFinanceClosure.test.mjs` 目标用例通过；`npm.cmd run build` 通过 | 2026-08-13，Codex 内置浏览器在本地当前代码环境完成正向验收：首笔开票正确绑定合同、案件及费用，金额 100；再次申请时页面明确提示“所选案件费用已经申请开票，不能重复申请”，开票列表仍仅保留首笔。`CODEX-812-ROW29-*` 4 条业务记录及 3 条流程事件已精确删除，残留均为 0。 | 已实现并完成本地浏览器验收；未提交、未部署 |

说明：全批本地验收完成前不递增版本、不提交、不部署 8089；每行继续补充本表。
