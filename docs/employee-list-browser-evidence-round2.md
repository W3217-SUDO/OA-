# 员工列表第二轮浏览器验收证据（2026-08-01）

## 范围与数据隔离

- 旧系统：`https://sh.021ipr.com/Console/Index`，只读打开并查看人事中心/员工管理入口、筛选区、列表、分页控件；未执行创建、编辑、保存、审批、上传或删除。
- 本地：`http://127.0.0.1:5173/?page=hr-all`。
- 本轮唯一临时前缀：`CODEX-HR-LIST-1785591804762`；对应账号 `codex_hr_list_1785591804762`。没有操作 `CODEX-HR-SUBREC` 数据。

## 本地操作证据

1. 新建员工保存后出现“员工档案与低权限登录账号已同步保存”，回到员工管理可查询到 `共 1 条` 的唯一前缀行。
2. 查看弹窗显示员工号、用户名、中文名、部门、职位、公司、入职日期、在职/可用状态等完整字段。
3. 完整修改弹窗填写 Email `codex_hr_list_1785591804762@example.test`，保存后出现“员工档案与登录账号已同步修改”，列表 Email 列即时回显。
4. 办理状态弹窗显示目标状态、生效日期、办理原因、交接给、办理说明；空原因提交显示“请填写至少两个字符的办理原因”，证明异常校验存在。随后因浏览器控件超时未将成功流转作为已验证证据，未据此声称成功。
5. 列表显示查看、修改、办理状态、修改密码、系统用户、删除及勾选框；分页显示 `共 1 条`、第 1 页以及上一页/下一页禁用状态。
6. 浏览器删除按钮在多次重开标签后仍发生 selector deadline timeout，因此未将 UI 删除误报为成功。经用户授权，最后仅按本线程唯一账号精确清理本地数据库用户 id=3，并复核查询为空；未删除其他用户。

## 差异与代码状态

本轮未发现需要新增代码的确定性缺口；批量选择/删除门禁和旧版分页大小已有实现及回归测试覆盖。浏览器标签超时作为验收限制记录，不改变旧系统只读约束。

## 验证

- `node --test employeeListBulkDeleteUi.test.mjs employeeBulkDelete.test.mjs employeeListLegacyPagination.test.mjs`：4/4 通过。
- `npm.cmd run build`（本地 admin-web）：通过；仅有既有 chunk size warning。
- 清理复核：`users` 中 `codex_hr_list_1785591804762` 与 `CODEX-HR-LIST-1785591804762` 均无残留；未部署服务器。
