# 8.13 Excel 问题逐行验收台账

来源：`C:\Users\Administrator\Desktop\OA系统\问题\8.13OA系统对接.xlsx`，工作表“8月13日”。旧系统仅使用本机 8091 只读参考；所有代码修改仅作用于新 OA。

| 行号 | 模块 | 问题 | 同行截图/批注 | 定位与修改 | 定向测试 | Codex 内置浏览器验收 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2 | 案件中心 | 我的案件各案件类型列表缺少导出、上传文件和更多操作 | 已逐行核对 Excel 第 2 行及同行截图 `image176.png`（缺失状态）、`image177.png`（旧系统完整入口）、`image178.png`（历史完成截图）；旧系统底部包含导出、上传文件、更多操作，更多操作含案件文档、费用、委托书/律师函、公证/调查表、任务、日志及打印表等入口 | “我的案件”各类型列表固定显示底部操作区；补齐选中案件的案件文档、委托书/律师函、公证/调查表、案件日志入口，并保留费用、任务、人员分配、归档等操作；详情入口实际进入案件详情文档区 | `node --test caseMineListToolbarRow2.test.mjs` 通过；`git diff --check` 通过 | Chrome `http://127.0.0.1:5182/?page=case-mine-civil` 已逐项复验；5 个“我的案件”类型均显示导出/上传/更多操作；选中第 1 条后菜单显示新增入口；点击“案件文档”进入 `case-detail-244-SHMS2600428` 并显示案件文档区；截图：`问题/_返工验收/8月13日第2行-我的案件工具栏.png` | 已完成 |
| 3 | 案件中心 | 公司案件各案件类型列表需具备我的案件全部功能，并增加删除案件 | 已逐行核对 Excel 第 3 行及同行截图 `image179.png`（公司案件旧系统完整菜单）和 `image180.png`（另一状态下的完整菜单）；底部明确为导出、上传文件、删除案件、更多操作，更多操作包含案件文档、各类费用、任务、日志及打印表 | 公司案件各类型复用完整底部工具栏；删除仅公司案件、管理员/管理人员且 `can_delete_case=true` 时展示，并保留二次确认与后端能力校验；补齐与第 2 行一致的文档、委托书/律师函、公证/调查表、案件日志入口 | `node --test companyCaseListToolbarRow3.test.mjs companyCaseDeleteRow19.test.mjs` 4/4；`git diff --check` 通过 | Chrome `http://127.0.0.1:5182/?page=case-company-civil` 已复验；选中第 1 条后显示导出、上传、更多操作及删除案件，更多菜单显示案件文档、委托书/律师函、公证/调查表、案件日志、案件任务、费用、人员分配、归档；未执行实际删除以避免破坏验收数据；截图：`问题/_返工验收/8月13日第3行-公司案件工具栏.png` | 已完成 |
| 4 | 案件中心 | 案件阶段无法修改，存在限制 | 已逐字读取；同行唯一截图 `问题/tmp_8_13_row4/row4-col3-image1.png` 已查看，修改民事案件基本信息/案件阶段时明确提示“案件创建尚未通过案件主管审批” | 拆分“新建三步完成”与“立案审批通过”条件：普通/仲裁基本信息和案件阶段修改仅要求三步完成，不再被待审批阻断；角色、归档、已合并保护保留 | `.venv\Scripts\python.exe -m unittest case_basic_phase_approval_independence_row4_test.py -v` 通过；`py_compile app/main.py`、前端构建、`git diff --check` 通过 | 本地隔离 `5182/8021`：`CODEX-813-R4` 审批仍为待审批时，阶段从“新案待分配”保存为“一审准备开庭”，PUT 200、弹窗关闭并回显；测试数据及工作流记录已精确清理 | 已完成 |
| 5 | 案件中心 | 复制案件编号按原号追加 A/B/C 顺序生成 | 已逐字读取；同行截图 `问题/tmp_8_13_row5/row5-col3-image1.png` 已查看，红框显示当前把 `SHMS2600424` 复制成新流水号 `SHMS2600425`，与要求不符 | 复制时沿复制链追溯原案号根号，按首个可用后缀生成 `A..Z、AA..`；已存在后缀自动跳过，唯一键并发冲突自动重试；复制来源元数据保留根案件 | `.venv\Scripts\python.exe -m unittest case_duplicate_number_row5_test.py -v` 4/4；`py_compile app/main.py`、前端构建、`git diff --check` 通过 | 本地隔离 `5182/8021`：从原案件 `SHMS2600424` 连续两次执行“操作 → 复制案件 → 确认复制”，页面依次提示 `SHMS2600424A`、`SHMS2600424B`；数据库记录与工作流事件核对后精确清理，来源合同状态已恢复 | 已完成 |
| 6 | 案件中心 | 案件日志和提醒需在详情右侧提供新增入口 | 已逐字读取；同行两张新系统截图 `问题/tmp_8_13_row6/row6-image162.png`、`row6-image163.png` 标出详情右侧仅显示空列表，旧系统截图 `row6-image164.png` 明确右侧应提供“新增”“新增日志”“新增退费日志” | 复核当前新 OA 已在案件详情右侧固定展示提醒和日志区，并按能力权限提供上述三个新增入口；提醒支持提醒日/截止日校验与删除，普通日志和退费日志写入专用接口并即时回显 | `node --test caseReminderLogActions.test.mjs` 3/3 通过 | 本地隔离 `5182/8021`：右侧“新增”打开提醒表单，填写日期与 `CODEX-813-R6-提醒` 后保存并回显；“新增日志”打开日志表单，保存 `CODEX-813-R6-日志` 后即时回显；测试提醒及两条审计事件已精确清理 | 已完成 |
| 7 | 案件中心 | 缺少发布案件任务入口 | 已逐字读取；新系统截图 `问题/tmp_8_13_row7/row7-image165.png` 标出案件任务页签仅有空列表，旧系统截图 `row7-image166.png` 显示列表底部“发布任务”入口 | 复核新 OA 已在案件任务页签底部按 `can_create_case_task` 权限显示“发布任务”；复用统一任务弹窗和 `/tasks` 接口，自动绑定当前客户、案号及“案件任务”来源，成功后刷新当前详情列表 | `node --test caseDetailTaskPublishRow7.test.mjs` 通过；`npm.cmd run build` 通过；`git diff --check` 通过（仅 CRLF 提示） | 本地隔离 `5182/8021`：进入 `SHMS2600424` 的“案件任务”页签，底部入口可见；填写 `CODEX-813-R7-案件任务`、负责人、截止日期和说明后发布，列表即时回显任务编号、类型、标题、日期、人员和状态；测试任务及审计事件已精确清理 | 已完成 |
| 8 | 调查大厅 | 调查主管新增子任务界面不完整，调查区域不应手填 | 已逐字读取；新 OA 截图 `问题/tmp_8_13_row8/row8-image167.png` 显示省/市/区自由手填且缺少授权提示与连续分配语义，旧系统截图 `row8-image168.png` 显示调查员、受控调查区域、授权区域/期限提示、备注及“完成/继续分配” | 子任务自动带入父调查任务授权期限和默认名称；负责人改为系统人员选择；调查区域复用中国省市数据并限制在可解析的父授权范围内，旧迁移范围无法解析时仍提供完整省市；提交所选省市而非父范围文本；后端明确选择优先、否则继承父任务；增加“完成/继续分配”并即时刷新 | `node --test investigationSubtaskAssignmentRow8.test.mjs` 3/3；`.venv\Scripts\python.exe -m unittest investigation_subtask_region_row8_test.py -v` 通过；`py_compile app/main.py`、`npm.cmd run build`、`git diff --check` 通过 | 本地隔离 `5182/8021`：创建 `CODEX-813-R8` 父调查任务并从“待我分配”打开新增子任务；页面自动带入授权时间/默认名称，负责人可从系统人员选择，省份选上海市、城市选市辖区；“继续分配”创建后列表即时回显负责人“卢愿”、区域“上海市、市辖区”、起止时间及待接收状态，表单保留可继续分配；测试合同、父任务、子任务、通知及审计事件已精确清理 | 已完成 |
| 9 | 调查大厅 | 我的调查任务混入其他调查员任务 | 已读取文字及 `image169.png`：范文玲账号列表混入分配给“律师助理2”的任务 | 前端子任务路由未传 `investigation_view`，后端也未对 `task` 模块执行 assigned/published 视图隔离 | 已修复：我的调查任务按当前账号 owner 严格过滤；我发布的子任务按发布人过滤；管理员仍可查看全部；补前后端回归测试 | 已通过：隔离账号 `codex813r9a` 登录后仅显示自己的 `CODEX-813-R9-T1`，不显示同部门且同发起人的 `T2`，分页共 1 条；已恢复管理员并精确清理测试任务、事件、通知和账号 | 完成 |
| 10 | 调查大厅 | 新增线索需补齐旧系统全部填写要素 | 已读取同行 `image170.png`、`image171.png`：对比新旧系统新增线索窗口，旧系统包含销售渠道、店铺名称、有无产品、调查时间、调查辅助、备注及暂存/提交双路径 | 新表单缺少旧系统关键字段，`platform` 为自由文本，修改接口白名单也未允许新增字段持久化 | 已补销售渠道选项、店铺名称、有无产品、备注及真实保存/修改回显/详情回显；后端允许 `sales_channel`、`shop_name`、`has_product`；补前后端定向测试 | 已通过：Codex 内置浏览器完整填写并提交 `XS20260813210303`，待审核列表和详情回显“淘宝”、测试店铺、有产品、调查日期、链接、主体、来源和备注；测试记录、事件、通知、附件精确清理为零 | 完成 |
| 11 | 调查大厅 | 已有菜单/动作权限时提交线索不应再按岗位阻断 | 已读取同行 `image172.png`：律师助理2能进入任务并打开新增线索，但提交时报“当前岗位没有创建调查线索权限” | 创建与提交接口只检查岗位动作“线索提交”，未复用账号已生效的菜单权限，形成重复且冲突的授权判断 | 已统一授权：拥有 `investigation-task-sub-mine` / `clue-my-draft` 有效菜单或岗位“线索提交”动作即可创建、提交和驳回后重提；完全无权限仍 403；保留只能在本人任务下创建 | 已通过：隔离普通“律师助理”账号仅凭有效菜单成功提交 `XS20260813211153`，待审核列表立即显示且共 1 条；管理员已恢复，测试账号、任务、线索、事件和通知精确清零 | 完成 |
| 12 | 调查大厅 | 调查线索生成案件不要求合同审批通过，审批中也可创建 | 已读取同行 `image173.png`：已取证线索生成案件提示“生成 0 个案件，来源任务关联合同尚未审批通过” | 线索转案接口另写状态黑名单，把“审批中”误判为不可用，与系统统一可建案件合同状态冲突 | 已改为统一复用 `CASE_SOURCE_CONTRACT_STATUSES`：审批中、已通过、履行中、已完成可转案；草稿、拒绝、撤回、作废仍拒绝；补审批中成功和草稿拒绝测试 | 已通过：审批中合同 `CODEX-813-R12-CONTRACT` 自动解析并生成案件 `SHMS2600428`，提示“已生成 1 个待分配案件”，案件中心即时回显，合同/客户/线索关联正确；案件、固定任务及所有测试数据精确清零 | 完成 |

流程：每次只处理一行；先读该行文字、批注和截图，再定位、修改、定向测试、Codex 内置浏览器验收并更新本表。全部本地验收完成后统一递增版本、提交并部署 8089，最后完成服务器浏览器验收。

## 统一发布与服务器验收

- 发布版本：`1.0.167`。8.13 修复提交为 `eb5a44d`；发现服务器已运行 `1.0.166-attachment-cleanup` 后，先保留并合入线上新增内容，再生成发布合并提交 `376a56d`，避免覆盖或降级服务器已有功能。
- 发布目录：`/opt/sunhold-oa/worktrees/release-v1.0.167-8.13`；`sunhold-dev-api` 与 `sunhold-dev-web` 的实际工作目录和启动命令均已切换到该目录。
- 服务检查：API `http://127.0.0.1:8001/health` 返回 `{"status":"ok"}`；内部 Web `8090` 与外部入口 `8089` 均返回 HTTP 200；两个 systemd 服务均为 `active`。
- 服务器 Codex 内置浏览器：登录 `http://150.158.3.104:8089/?_v=167` 成功；`case-mine-civil` 与 `case-company-civil` 显示导出、上传和更多操作工具栏；`investigation-task-mine` 显示授权起止时间、调查区域、案源和中文人员；`investigation-task-unassigned` 显示完整授权字段与新增子任务入口；从线上调查任务打开“新建调查线索”，确认销售渠道、侵权产品、店铺链接/名称/ID、有无产品、调查日期、生产商、主体信息、调查辅助员、来源、附件、备注以及暂存/提交双入口均存在。验收只打开页面和弹窗，未提交或修改线上业务数据。
