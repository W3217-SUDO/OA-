# 9.1 rows 35-37 remediation record

Source workbook: `OA系统对接9.1.xlsx`

Source SHA256: `35211ECE14EBA21CA87EC6DA5636C8311D6F545F1E5B0838BB5F81655EBFEE16`

Git baseline: `f65de6964891a60e176998ab158e1db7ad1e62b4`

Scope: worksheet `9.1`, rows 35-37 only. The workbook completion cells are not edited.

## Row 35

### Reading confirmation

- Scan range: `A35:I35`.
- Raw OOXML: `xl/worksheets/sheet16.xml` contains no `<row r="35">` element.
- Row metadata: not hidden; no custom height; the worksheet-level row lookup inherits style id 33, but no cell record exists in the source XML.
- Formulas/cached values: none.
- Comments/notes/hyperlinks/merged-cell ownership: none.
- Drawing audit: `xl/drawings/drawing16.xml` has no anchor starting on or covering row 35.
- Historical `H=1`: not used. `H35` has no cell record or value.

| Column | Header | Verbatim text | Format/comment | Anchored images | Business use |
| --- | --- | --- | --- | ---: | --- |
| A | 模块 | 无 | No cell record; no comment | 0 | No module supplied |
| B | 问题 | 无 | No cell record; no comment | 0 | No issue text supplied |
| C | 截图 | 无 | No cell record; no comment | 0 | No new-system screenshot supplied |
| D | 原系统 | 无 | No cell record; no comment | 0 | No legacy screenshot supplied |
| E | 发起时间 | 无 | No cell record; no comment | 0 | No initiation date supplied |
| F | 测试 | 无 | No cell record; no comment | 0 | No test/rework text supplied |
| G | 截图 | 无 | No cell record; no comment | 0 | No test/rework screenshot supplied |
| H | 完成状态 | 无 | No cell record; no comment | 0 | No completion state supplied |
| I | 完成时间 | 无 | No cell record; no comment | 0 | No completion date supplied |

### VibeHub terminology check

- Sanitized query: `空白行`.
- Resolver result: successful call, zero candidates; no terminology needed adding.
- The result is recorded only as a terminology check and does not create a requirement or diagnosis.

### Locating and parity gates

- Verbatim issue/supplement/rework text: none.
- Coarse locating expression: worksheet row 35 is structurally blank and supplies no module, entry, role, record, observable defect, or expected result.
- Image semantic comparison: not applicable; there are no current-system, legacy-system, or rework images on this row.
- Legacy dynamic reproduction: not executable without a named role, entry, record, action, or expected behavior. No substitute route was used.
- Legacy source/data trace: not executable because the row supplies no business target. No source inference was made.
- New source/data trace: prohibited for the same reason. No source search or code edit was performed for this row.
- Concrete root cause: not asserted. A blank backlog row is not treated as an application defect.

### Legacy UI equivalence matrix

| Comparison item | Evidence/result |
| --- | --- |
| Navigation carrier | Not supplied by row 35 |
| Page title | Not supplied by row 35 |
| Step count/transitions | Not supplied by row 35 |
| Section hierarchy | Not supplied by row 35 |
| Field order | Not supplied by row 35 |
| Read-only/editable state | Not supplied by row 35 |
| Default values | Not supplied by row 35 |
| Buttons | Not supplied by row 35 |
| Validation | Not supplied by row 35 |
| Persistence | Not supplied by row 35 |
| Downstream result | Not supplied by row 35 |

### Outcome

No business remediation, browser acceptance, database patch, or test-data cleanup can be validly attached to row 35. The row checkpoint records the absence of a requirement without changing the workbook or claiming acceptance.

## Row 36

### Reading confirmation

- Scan range: `A36:I36`.
- Merged-cell ownership: `A36:G36`; the only text is owned by `A36`.
- Row metadata: height 16.5 points, not hidden.
- Formulas/cached values: no formulas; the cached value equals the verbatim text below.
- Comments/notes/hyperlinks: none.
- Drawing audit: no image starts on or covers row 36.
- Historical `H=1`: not used. `H36` is empty.

| Column | Header | Verbatim text | Format/comment | Anchored images | Business use |
| --- | --- | --- | --- | ---: | --- |
| A | 模块 | 这个后面直接套模版即可，暂时不需要改，先记录在这 | Owner of merged range `A36:G36`; red font `#FE0300`, 10 pt; thin black border; vertically centered; no comment | 0 | Explicit deferred-note text; no module or business entry supplied |
| B | 问题 | 无（归属合并区域 `A36:G36`） | Merged child; no comment | 0 | No independent issue text |
| C | 截图 | 无（归属合并区域 `A36:G36`） | Merged child; no comment | 0 | No new-system screenshot |
| D | 原系统 | 无（归属合并区域 `A36:G36`） | Merged child; no comment | 0 | No legacy screenshot |
| E | 发起时间 | 无（归属合并区域 `A36:G36`） | Merged child; no comment | 0 | No initiation date |
| F | 测试 | 无（归属合并区域 `A36:G36`） | Merged child; no comment | 0 | No test/rework text |
| G | 截图 | 无（归属合并区域 `A36:G36`） | Merged child; right edge of merged border; no comment | 0 | No test/rework screenshot |
| H | 完成状态 | 无 | Empty cell; no comment | 0 | No completion state |
| I | 完成时间 | 无 | Empty cell; no comment | 0 | No completion date |

### VibeHub terminology check

- Sanitized query: `模板复用`.
- Resolver result: successful call, zero candidates; no terminology needed adding.
- Parallel professional locating expression: a deferred template-reuse note, with no current implementation request.
- The expression does not replace or narrow the verbatim instruction `暂时不需要改`.

### Locating and parity gates

- Verbatim issue/supplement/rework text: `这个后面直接套模版即可，暂时不需要改，先记录在这`.
- Coarse locating expression: future work may reuse a template, but this row intentionally provides no current module, entry, role, record, observable defect, or expected behavior.
- Image semantic comparison: not applicable; there are no current-system, legacy-system, or rework images on this row.
- Legacy dynamic reproduction: not executable and not authorized by this row because no entry or behavior is named and the row explicitly says no change is currently needed.
- Legacy source/data trace: not executable because no business target is supplied. No source inference was made.
- New source/data trace: not executable for the same reason. No source search or code edit was performed for this row.
- Concrete root cause: not asserted. A deferred implementation note is not treated as an application defect.

### Legacy UI equivalence matrix

| Comparison item | Evidence/result |
| --- | --- |
| Navigation carrier | Not supplied by row 36 |
| Page title | Not supplied by row 36 |
| Step count/transitions | Not supplied by row 36 |
| Section hierarchy | Not supplied by row 36 |
| Field order | Not supplied by row 36 |
| Read-only/editable state | Not supplied by row 36 |
| Default values | Not supplied by row 36 |
| Buttons | Not supplied by row 36 |
| Validation | Not supplied by row 36 |
| Persistence | Not supplied by row 36 |
| Downstream result | Not supplied by row 36 |

### Outcome

No code, browser acceptance, database patch, or test-data cleanup is required for row 36 in this batch. The checkpoint preserves the explicit deferment without changing the workbook or claiming acceptance.

## Row 37

### Reading confirmation

- Scan range: `A37:I37`.
- Row metadata: height 162.75 points, not hidden; no merged cells.
- Formulas/cached values: no formulas; cached values equal the cell values.
- Comments/notes/hyperlinks: none.
- Historical `H37=1`: explicitly rejected as completion evidence because the later red test/rework text in `F37` states that document templates are absent and generated files cannot be produced or downloaded.

| Column | Header | Verbatim text | Format/comment | Anchored images | Business use |
| --- | --- | --- | --- | ---: | --- |
| A | 模块 | 案件中心 | 10 pt; thin black border; vertically centered; no comment | 0 | Module |
| B | 问题 | 案件，文档信息模块，第一，需要新增一个生成操作，生成操作里面的功能根据原系统截图配置；第二，需要增加一个更多操作，界面也是根据原系统截图配置，操作的功能需点开原系统查看，截图内容太多容易杂乱；注意一点，更多操作里面的删除功能，若要删除文件，这里有一个限制，就是本人只能删除本人上传的文件，无法删除别人上传的文件 | 10 pt; wrapped; thin black border; vertically centered; no comment | 0 | Full issue and deletion-ownership constraint |
| C | 截图 | 空字符串 | 10 pt; thin black border; no comment | 1 | New-system problem screenshot |
| D | 原系统 | 空字符串 | 10 pt; thin black border; no comment | 2 | Legacy target screenshots |
| E | 发起时间 | 2026.8.28 | 10 pt; thin black border; no comment | 0 | Initiation date |
| F | 测试 | 案件信息里面的生成操作，目前只有一个按钮，实际上文档模版并没有上传，也不能根据案件信息生成对应的文件下载，暂时先放那，后面再直接套模版 | Red font `#FE0300`, 10 pt; wrapped; thin black border; no comment | 0 | Rework text invalidating the old completion result |
| G | 截图 | 空字符串 | 10 pt; thin black border; no comment | 1 | Rework screenshot |
| H | 完成状态 | 1 | Numeric value; no comment | 0 | Historical status only; not completion evidence |
| I | 完成时间 | 无 | Empty styled cell; no comment | 0 | No completion date |

### Workbook image evidence

| File | Anchor/header | Size | SHA256 | Visual observation |
| --- | --- | ---: | --- | --- |
| `9.1_row37_img56_C.png` | `C37` / 截图 | 1917x932, 138843 bytes | `1acac8e9c41a7ed780221049a0b6f9357ae2688b87af66063055ad5664023ca5` | New system case detail, 文档信息 tab. It has the document tree and Upload/Download selected/Delete selected controls, but no 生成操作 or 更多操作 control. |
| `9.1_row37_img57_D.png` | `D37` / 原系统 | 1918x1004, 165598 bytes | `aa25dfc827464e417c8db3a178c3610b979c3d20462df0e9e1e1034af9a6ee19` | Legacy case detail, 文档信息 tab. The 生成操作 dropdown contains 生成归档封面、生成授权委托书、生成一审所函（我方原告/我方被告/我方上诉/对方上诉）、生成执行所函、生成身份证明、生成结算提成表、生成代收代付赔偿款申请单. |
| `9.1_row37_img58_D.png` | `D37` / 原系统 | 1916x994, 157537 bytes | `03e2b07ddfc46c4d390a41818f7855a61dfb3df218fa72ceeeda1f6b92260884` | The same legacy tab. The 更多操作 dropdown contains 删除、申请用印、更改文档目录. |
| `9.1_row37_img59_G.png` | `G37` / 截图 | 1913x950, 160286 bytes | `98b7aaec707518b198f552697c0ff87e7b0d9615c05b95509173407c23edd76e` | Reworked new system shows the 生成操作 dropdown and matching labels, but the red test text states the templates are absent and no case-derived downloadable document is produced. A 更多操作 button is visible but not expanded in this image. |

Evidence directory: `C:\oa901-rows35-37-rework-evidence\row37\workbook` (kept outside Git).

### VibeHub terminology check

- Sanitized queries: `文档模板生成`, `批量操作菜单`, `基于所有权的访问控制`.
- Resolver result: only [下拉菜单（Dropdown）](https://vibe-hub.org/dropdown) matched; it describes the 生成操作/更多操作 entrances as click-triggered operation menus. The other two queries returned zero candidates, so no terms or links were invented.
- Parallel professional locating expression: on the case-detail 文档信息 tab, restore the legacy generation and more-operation dropdown workflows, including real case-context template output and uploader-ownership authorization for deletion; preserve the complete verbatim issue and red rework text as the controlling requirement.

### Coarse locating and image comparison

- Named entry: 案件中心 -> 案件详情 -> 文档信息.
- Current observable defect: the original new-system screenshot lacks both operation menus; the later rework adds the generation menu labels but does not perform real template generation/download.
- Legacy target: a document table with selection, per-file operations, Upload, 生成操作, 更多操作, pagination, and the two dropdown command sets shown above.
- Explicit authorization rule: a user may delete only files uploaded by that same user; files uploaded by others must remain protected.
- Rework item: menu presence alone is insufficient. Generation must use an actual template and current case data to create a downloadable file.
- No root cause or implementation is asserted at this stage. Dynamic legacy reproduction and both source/data traces remain mandatory before editing.

### Legacy UI equivalence matrix (pre-reproduction)

| Comparison item | Workbook evidence | Dynamic trace status |
| --- | --- | --- |
| Navigation carrier | Dedicated case-detail work area, 文档信息 tab | Pending localhost:8091 reproduction |
| Page title | Case information/detail with case number | Pending |
| Step count/transitions | Select document(s), then use a dropdown command; generation likely produces a file in context | Pending all commands and results |
| Section hierarchy | Case summary -> tab strip -> document tree + document table -> action bar | Pending |
| Field order | Selection, sequence, uploader, file name, document date, per-file operations | Pending |
| Read-only/editable state | Existing rows show View/Download and sometimes Edit; generation/more actions are command menus | Pending role and ownership checks |
| Default values | Current directory and selected rows provide context | Pending |
| Buttons | Upload, 生成操作, 更多操作; legacy per-file View/Download/Edit | Pending exact enabled/disabled conditions |
| Validation | Delete restricted by uploader ownership per issue text | Pending live success/failure behavior |
| Persistence | Generated document must be downloadable and belong to the case/document directory | Pending live data relation trace |
| Downstream result | 更多操作 includes delete, seal application, and directory change | Pending live destination/state results |

### Legacy dynamic reproduction and source/data trace

- Runtime/role/entry: `http://localhost:8091`, `Admin 管理者`, 案件中心 -> an existing civil case detail -> 文档信息. The live page uses the same dedicated case-detail carrier and tab hierarchy as the workbook legacy screenshots.
- The live document table shows the legacy columns and per-file actions from the screenshots. Its action bar contains Upload, 生成操作, 生成操作(国定), 更多操作, page-size selection, pagination, and page jump.
- 生成操作 dynamically exposes 10 commands: 生成归档封面、生成授权委托书、生成一审所函(我方原告)、生成一审所函(我方被告)、生成二审所函(我方上诉)、生成二审所函(对方上诉)、生成执行所函、生成身份证明、生成结算提成表、生成代收代付赔偿款申请单.
- 生成操作(国定), which is not visible in the two workbook target screenshots but exists on the same live legacy entry, exposes six commands: 生成授权委托书 and the five first/second/execution-instance letters.
- 更多操作 renders 删除、申请用印、更改文档目录. `Legal.Case.js` only shows these commands for supported document-directory type ids; the controller/service remains authoritative for action authorization.
- Selection/default behavior: generation uses the current case number without requiring a document selection; more actions require selected file ids. With no selected file the action list cannot execute. A foreign-uploaded existing file was selected only to inspect the authorization state and then unselected; no legacy data was changed.
- Saved Chrome evidence: `9.1_row37_legacy_entry.png`, `9.1_row37_legacy_generate_menu.png`, `9.1_row37_legacy_national_generate_menu.png`, `9.1_row37_legacy_more_menu_initial.png`, `9.1_row37_legacy_foreign_file_more_menu.png` under `C:\oa901-rows35-37-rework-evidence\row37\legacy`.

Legacy source chain:

- View: `GD.CRM.WEB/Areas/Legal/Views/CaseFile/PartialView/CaseFileList.cshtml` lines 235-380 defines the document table action bar and all three dropdown menus.
- Client workflow: `GD.CRM.WEB/Scripts/Legal/Case/Legal.Case.js` lines 3120-3186 projects file id, uploader, file name and date from the API; lines 3483-3507 validate selection, confirm deletion, call `CaseFileDelete`, and refresh the selected directory; lines 3577-3625 open and persist directory changes; lines 3628-3943 call each generation endpoint, show the result, trigger temporary-file download, and in relevant flows reopen the generated file for editing.
- Seal workflow: `GD.CRM.WEB/Scripts/AWS/OfficialDocument/AWS.OfficialDocument.js` lines 171-181 opens the case-linked seal application with the case number and selected file ids.
- Controller: `GD.CRM.WEB/Areas/Legal/Controllers/CaseFileController.cs` lines 526-551 delegates deletion to `CaseFileService`; lines 653-1167 implement the generation endpoints. They validate required case context where applicable, select a concrete `CaseFileType` process, export the document, return a downloadable file name, and for single-case document flows persist uploader/time/size and bytes through `CaseFileService.Create`.
- Service authorization: the archived `Dchien.Legal.Service.dll` method `CaseFileService.CaseFileDelete(long)` was inspected at IL level. It loads the file row, permits deletion when the caller has legacy permission `1001001905` or when `CaseFile.UploadingUser` equals the current login, otherwise throws the legacy no-permission message. Successful deletion writes an immutable case log with case id/no, file name, actor and time.

Legacy data chain:

- SQL Server table `dbo.Legal_Case_File` stores `FileId`, `CaseId`, `CaseNo`, `FileName`, `FileTypeId`/`CaseFileTypeId`, `FullPath`, `FileSize`, `UploadingUser`, `UploadingTime`, `Actived`, `FileGuid`, and audit columns.
- `dbo.Legal_Case_FileType` stores the case-local directory tree (`FileTypeId`, `FileTypeName`, `ParentFileTypeId`, `CaseNo`, `TypeId`, active/audit fields).
- The live sample has active and inactive generated Word rows plus active uploaded rows. Its generated authorization document has a real non-empty file in the case directory, while source templates exist under `CaseFiles/TemplateFiles`; this proves the legacy menu is backed by template materialization rather than labels alone.
- The workbook's ordinary-uploader deletion rule and the service IL align as follows: ordinary users may delete only their own uploads; the legacy delete permission is an explicit administrative override. This preserves the project-wide rule that the highest administrator is not silently downgraded.

Legacy UI equivalence matrix update:

| Comparison item | Live legacy result |
| --- | --- |
| Navigation carrier | Dedicated case-detail work area within the OA shell; 文档信息 remains a tab in the case context |
| Page title | Case information/detail identified by the current case number |
| Step count/transitions | Generation: choose command -> server validation/template export -> success/error -> download -> list refresh/persisted row where applicable. More actions: select file(s) -> choose command -> confirm/form -> result -> list/downstream refresh |
| Section hierarchy | Case summary -> court/archive sections -> tab strip -> document tree + table -> action bar -> reminder/log side rail |
| Field order | Select, sequence, uploader, file name, document date, per-file operations |
| Read-only/editable state | View/download are read paths; generated editable office files may expose Edit; mutation commands are gated by directory, role, selection and uploader ownership |
| Default values | Current case number and selected document directory supply context; selected file ids supply more-action context |
| Buttons | Upload, 生成操作, 生成操作(国定), 更多操作, View, Download, conditional Edit, pagination controls |
| Validation | Required case fields are validated before generation; no selected file is rejected; ordinary deletion requires matching uploader, with explicit privileged override |
| Persistence | Generated bytes are stored in a case directory and metadata in `Legal_Case_File`; deletion deactivates/removes through the service and writes a case log |
| Downstream result | Generated file download/list refresh; seal application opens with case/file linkage; directory change updates file type and refreshes the tree/list |

### New-system source/data trace and concrete root cause

New-system source chain:

- `apps/admin-web/src/CaseCenterPage.tsx` lines 2813-2835 posts the selected generation key to `/cases/{case_id}/documents/{document_type}`, inserts the returned attachment into the visible list, refreshes from the server, and exposes the server validation error without claiming success. Lines 2974-3012 implement selected-file deletion, seal application, and directory change. Lines 5977-6014 render the persisted attachment table plus the two operation dropdowns.
- `apps/admin-web/src/caseDocumentGenerationActions.mjs` defines the exact 10-command legacy menu and translates Ant Design menu events into the corresponding server route key; `apps/admin-web/caseDocumentGenerationEventRow37.test.mjs` protects the first-menu-item dispatch path that previously rendered a label without invoking generation.
- `apps/api-server/app/main.py` lines 32157-32527 define all 10 document types, required persisted case fields, case/customer/company context projection, DOCX materialization, non-empty file write under `UPLOAD_ROOT`, `FileAttachment` persistence, workflow audit, transactional rollback/physical cleanup, and the downloadable attachment response.
- The same server file lines 26456-26529 implements selected-file deletion and directory changes; lines 27552-27680 copies the selected real case file into a formal seal-application draft in the same transaction. Generic attachment download resolves only an authorized, existing attachment file.
- Focused pre-existing tests `case_document_generation_row13_test.py` and `case_document_operations_row5_test.py` prove all 10 generated DOCX files are distinct, non-empty, persisted, downloadable, audited, and atomically absent on required-field failure; they also cover directory movement and uploader ownership.

New-system data chain (isolated SQLite `C:\oa901-rows35-37-rework-runtime\row37\legal_platform_row37.db`, API `127.0.0.1:8377`):

- `business_records` is the authoritative case row; the isolated seed contains 12 case records with case number, title, status, owner, department, and case-context JSON used by generation.
- `file_attachments` is the authoritative document row and stores `record_id`, directory/category, original/stored names, content type, byte size, physical path, uploader, remark, and creation time. The fresh isolated database starts with zero attachments, so later acceptance can prove every generated/uploaded row belongs to this task.
- `system_parameters(category='case_file_type')` supplies active case-document directory names used by upload and directory change validation.
- `document_templates` contains three metadata-only templates and no uploaded template bytes. The 10 legacy case commands do not claim to consume those rows: the current implementation materializes real DOCX content from persisted case/customer/company data. This resolves the red screenshot's immediate “cannot generate/download” defect while preserving row 36 and `F37`'s explicit deferment of later uploaded-template substitution.
- Generated files are distinguishable through `remark='系统生成案件文书：{document_type}'`, uploader, case relation, byte size, and a matching `WorkflowEvent(action='生成案件文书')`; the physical path remains under the task-owned upload root.

Concrete root cause after both traces:

- The old screenshot defect had two historical causes already repaired in the selected base: the menu event once failed to dispatch its action key, and generation once stopped at presentation instead of producing a persisted attachment. Current source and focused tests show those defects are no longer present; no duplicate generation rewrite is justified.
- One live parity defect remains in `delete_case_attachments`: its uploader comparison rejects every foreign upload before considering role. That differs from the legacy service's permission `1001001905` override, differs from the new generic attachment endpoint's administrator behavior, and downgrades the same `Admin 管理者` role used for legacy reproduction.
- Required repair: keep the uploader equality check authoritative for ordinary users, but allow the highest administrator to delete a visible foreign-uploaded case file, atomically and with the existing workflow audit. No menu, template, schema, version, or workbook-status change is required.

### Row 37 implementation scope

- Change only the case batch-delete authorization condition to preserve the administrator override.
- Retain ordinary-user ownership denial, case visibility, related-record validation, archive-state gate, all-or-nothing preparation, workflow audit, and physical-file cleanup.
- Add row-specific regression coverage for ordinary denial/atomicity, administrator override, real generation/download/persistence, and refresh-visible attachment metadata.

### Final local verification and boundary

- `python -m unittest case_document_operations_row37_test.py case_document_operations_row5_test.py case_document_generation_row13_test.py`: 9 tests passed.
- `python -m py_compile app/main.py case_document_operations_row37_test.py case_document_operations_row5_test.py`: passed.
- `npm run build`: passed (`tsc -b && vite build`, 5643 modules transformed). The existing Ant Design bundle-size warning is unchanged and is not row 37 scope.
- Existing evidence check: all four workbook images and all five task-owned legacy Chrome screenshots are non-empty and have SHA256 values. The five Chrome screenshots were visually reviewed after saving; the case-detail document table, normal generation menu, national generation menu, and selected foreign-upload row are readable and correctly framed.
- At the user's final stop instruction, no further new-system browser exploration or screenshot was performed. New-system behavior in the submitted scope is supported by the focused API integration tests and production build; this record does not mislabel the interrupted new-system Chrome exploration as completed acceptance.
- `F37` remains controlling: formal uploaded templates are deferred (`暂时先放那，后面再直接套模版`). This batch creates no formal template, template file, template database row, schema patch, version change, workbook status, deployment, or online data change.
