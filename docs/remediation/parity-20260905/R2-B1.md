# R2-B1 官文独立处理状态整改

## 1. 清单原文
- 工作簿/工作表/行号：不涉及，直接功能核对问题R2-B1。
- 原始问题：查看我的新系统还有哪些地方没有跟旧的系统同步
- 补充说明：历史数据不同步不计问题。
- 用户实施授权：修改 然后部署上去
- 返工意见：无。
- 当前状态：本地实现完成，待根任务统一构建发布；待用户验收。

## 2. 截图分析
本项未提供截图，使用已核实的旧View/JS/Controller与新代码，未运行浏览器。

## 3. 旧系统实现
旧GD.CRM.WEB.VIP/Areas/FIO/Views/PatentOffice/PatentOfficeFileInList.cshtml:119–127业务处理中/已处理/未处理，PatentOfficeFileAllList.cshtml:124–130流程同三态。Scripts/FIO/FIO.Patent.OfficeFile.js:629–750分别提交六个专用动作。PatentOfficeController.cs:484–605调用业务/流程两组独立服务方法。选中多条后批量调整对应维度；另一维不应被改变。旧服务DLL未包含该类，不能推测其全部内部权限，新增操作沿用新系统可见范围及负责人/管理权限。

## 4. 新系统当前实现
### 前端
apps/admin-web/src/IprOfficialFilePage.tsx只有校验、转发、办结和历史已处理两个动作。
### 后端
apps/api-server/app/areas/ipr/router.py:3264–3402把处理中状态绑定生命周期，办结强制两维已处理。历史接口只支持已处理。
### 数据库
business_records.data已有business_process_status/service_process_status。workflow_events已有审计。无需模型或数据库迁移，旧缺字段显示未处理。

## 5. 新旧差异和根因
|编号|页面/功能|旧行为|新行为|根因|
|---|---|---|---|---|
|D1|当前官文业务与流程三态调整|两个独立维度各三态|仅生命周期动作间接设置|缺独立状态命令|

## 6. 精确修改清单
- [x] M1 IprOfficialFilePage新增业务处理/流程处理状态菜单，批量所选/单条详情可操作，详情展示两维状态，显示保存中及服务失败，成功回读；保留全部原校验/转发/办结/历史动作。详情使用函数式更新避免请求期间打开其他记录被覆盖。
- [x] M2 ipr/router.py尾部新增processing/{dimension}路由，局部请求模型限定三态及100个ID；校验模块菜单、先锁定全部记录、逐条校验可见/负责人或管理员，全部通过后更新一维与操作时间/人并写审计，统一commit。
- [x] M3 main.py追加ipr的121..122切片，既有顺序不变。
- 数据库不涉及结构变更；操作仅按用户所选ID写已有JSON和事件，不批量修改历史数据。
- 风险：处理状态与生命周期现为独立维度；原生命周期动作继续按原逻辑更新，两维手工标记不自动改变生命周期。

## 7. 验证清单
- 自动化业务测试/API写入：按根任务要求本轮不运行。
- 生产构建：根任务合并后统一构建。
- API成功/失败路径：代码复核仅接受两个维度/三个状态，先全量验证再写入，失败不commit。
- 持久化回读：前端成功后回读列表和打开详情。
- 权限/角色：管理员、管理人和负责人按既有权限函数校验；无权限不更新。
- 清理：无业务测试记录和端口进程。
- 用户验收：对同一官文独立切换两维三态并刷新；拒绝无权记录；保留旧生命周期按钮。

## 8. 实施记录
2026-09-05：本地完成以上三项。Python py_compile、TypeScript tsc --noEmit、git diff --check 通过。未运行业务测试、未写业务数据库，生产构建由根任务统一执行。事务提交前异常不会commit，get_db的session上下文关闭将回滚；数据库提交失败同样关闭回滚，不吞掉异常。未提交、未发布，待用户验收。
