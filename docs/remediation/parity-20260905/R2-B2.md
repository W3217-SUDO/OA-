# R2-B2 官文原始文件批量解析（受阻，未实现）

## 1. 清单原文
- 原始要求：查看我的新系统还有哪些地方没有跟旧的系统同步。
- 后续授权：修改 然后部署上去。历史数据不同步不计问题。
- 本项差异：旧原始官文批量解析上传、人工确认、附件入库，新系统只有CSV元数据导入。
- 当前状态：旧解析实现缺档，未实施，不能计为修复完成。

## 2. 截图分析
本项没有附图。依据旧页面、JavaScript、控制器及本地程序集核对，未打开浏览器。

## 3. 旧系统实现
旧源码根目录为 legacy-gdcrm-101-local-20260812/source。
GD.CRM.WEB.VIP/Areas/FIO/Controllers/PatentOfficeController.cs:47–82 上传原始bytes，67创建BizOfficeMetaFile，76调用OfficeMetaFileService.Create，78调用Dchien.Legal.Service.FIO.PatentOffice.PatentOfficeFileService.ProcessPatentOfficeMetaFile(metaFile,bytes)，82遍历解析案件与文件，160起确认导入选中项。PatentOfficeFileImport.cshtml:113–130上传入口未声明文件格式。

追查证据：
- 对 legacy/source 与 OA系统_跨电脑继续开发_20260804_完整交接/旧系统归档源码/SH.CRM.WEB 下全部DLL，按SHA256去重枚举174个程序集；包含WEB、VIP、API各bin/副本/历史目录及DqService，并非仅搜索Dchien文件名。
- obj/Debug/Dchien.Legal.Web.dll包含PatentOfficeController；读取PE元数据TypeRef确认PatentOfficeFileService与OfficeMetaFileService都引用Dchien.Legal.Service, Version=0.0.0.0。
- 归档各Dchien.Legal.Service.dll均无上述类型定义，只有拼写不同的OfficalMetaFileService。反编译该类型证实其按文件名下划线前缀匹配普通法律案件CaseNo并建附件，是另一套法院来文流程，不能代替专利官文原始解析。
- 源码没有两个服务类定义。CaseFiles/CPCTemplateFiles中的XML为模板，不能据此推定通知书上传格式；未找到可证明该接口所用格式的实际上传样本。
- 本地证据：隔离工作树 .codex-evidence/decompiler/patent-type-inventory.json、OfficalMetaFileService.cs、decompile.ps1。证据工具及旧反编译源码不提交产品仓库。

## 4. 新系统当前实现
apps/api-server/app/areas/ipr/router.py:2806起仅CSV导入，3039起确认候选建立BusinessRecord和事件；3198起单条原件上传是独立入口，不能算作批量原文解析。models.py:946/965批次与候选已存在，raw_data可保存原文件关联元数据，无需先升级模型。

## 5. 新旧差异和根因
批量原始文件解析链路未迁移；原始格式和附件提取规则位于缺失旧服务程序集，现有控制器仅能证明调用协议，不能证明支持ZIP、XML或具体字段。凭猜测添加通用XML解析会造成错误关联和丢附件，因此未制造替代实现。

## 6. 精确修改清单
- [ ] 获取与obj控制器相匹配且含PatentOfficeFileService的Dchien.Legal.Service.dll或其服务源码，或可授权查看的真实原始上传样本及对应解析结果。
- [ ] 基于已证实的格式解析原件，生成可校正候选，保存来源与各附件关系；保留CSV额外入口。
- [ ] 人工确认时原子建立业务记录、附件及审计，失败回滚并删除本次暂存文件，阻止重复确认。
- [ ] 补齐权限、失败提示和候选附件预览下载，统一构建后发布。

## 7. 验证清单
尚未实施，不宣称解析验证通过；没有业务测试、业务数据库写入或迁移。后续需依真实格式完成成功/损坏文件/重复确认/附件失败清理验证；本轮按根任务要求不进行浏览器或业务验收。

## 8. 实施记录
2026-09-05：完成本地所有名称程序集类型盘点及控制器外部程序集引用追踪，确定归档缺少目标解析器。已报告根任务，B1可独立发布，B2保留未完成。剩余有效来源是匹配旧版本服务源码/程序集或真实原始样本，不是继续推测通用文件格式。
