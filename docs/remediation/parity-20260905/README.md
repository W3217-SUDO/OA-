# 第二轮功能修复与统一发布

用户授权：“修改 然后部署上去”。依据上一轮功能审计7项，排除历史数据数量同步；本批次统一发布。

已完成代码：

- C2-01：归档审核支持多条案件，逐条校验权限、状态、归档号与意见，单事务提交；任一失败全部回滚。
- C2-02：归档查询字段与日期进入后端查询；列表和导出共用条件。
- C2-03：归档服务端分页返回total，已审核先按归档状态过滤再分页，跨100条可查；导出筛选全集独立于当前页。
- R2-B1：官文业务/流程分别支持未处理、处理中、已处理，当前/历史列表和详情均可操作，保留权限与审计。
- S2-01：按旧DAO和数据库视图还原资金运营口径，资金回报率使用指定非官费回款、官费支出/亏损及实际周期；缺少日期或除零时提示不可计算。
- S2-02：提成新建默认今天至一年后，起止日期必填且先后合法；不批量改写既有历史方案。

**未完成 R2-B2：官文原始文件批量解析。** 旧控制器依赖的PatentOfficeFileService/OfficeMetaFileService在现有174个去重程序集内没有类型定义；Web DLL只保留指向Dchien.Legal.Service的引用。需要匹配版本服务源码/程序集或可核对的真实原始文件样本。未用猜测解析器冒充修复，现有CSV候选和单份导入保留。证据见R2-B2.md。

变更涉及7个前端文件、6个后端文件，无模型、启动迁移或历史数据补丁。无需数据库备份。新增4条路由追加到原切片末尾，保留原接口顺序；解决main.py集成冲突时同时保留官文和归档新增注册。

检查：各组TypeScript/Python编译、根任务路由组合导入通过（767条路由、618条OpenAPI路径）。已有客户共享接口重复Operation ID告警仍属原基线。本批次按直接问题规则不运行浏览器或业务写入测试，用户验收待部署后进行。正式生产构建、版本、激活与静态资源核验记录在本地“新系统/说明/功能修复部署-20260905”。

期间另恢复8089连接故障：Docker数据库/MinIO地址变化导致/etc/hosts过期，已更新受管映射并重启API，数据库身份读取及网站恢复200；这发生在本批次代码激活前。

发布协调：主任务统一线性集成dev，领取新版本、部署8089，健康后将GitHub main快进到同一发布提交。回滚点为v1.1.70（8efc6ca），前端回滚包已保存本地并校验SHA256。子任务未单独发布。
