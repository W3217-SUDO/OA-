# C2-01 归档批量审核

## 1. 清单原文
- 工作簿/工作表/行号：不涉及，直接审计整改 C2-01。
- 原始问题（逐字）：查看我的新系统还有哪些地方没有跟旧的系统同步
- 补充说明（逐字）：修改 然后部署上去
- 返工意见：无。
- 当前状态：用户已授权按审计结果修改，统一由主任务发布。

## 2. 截图分析
本项无新增截图；采用完整旧/新源码链路证据，不把历史浏览器图片当当前复现。

## 3. 旧系统实现
案件中心归档申请/待审核/已审核/已拒绝；Legal.Case.js:6912/7027遍历Cases；CaseArchiveController:103/131批量审核。归档审核人员处理选中案件，正常归档费用到账，亏损归档带原因。旧源根为工作区legacy-gdcrm-101-local-20260812/source/GD.CRM.WEB.VIP。详细调用链见新旧同步审计-20260905/round2-case.md。

## 4. 新系统当前实现
### 前端
apps/admin-web/src/legal/CaseCenterPage.tsx、services/queriesActions.tsx、services/workflowActions.tsx；新CaseCenterPage:2533 find只取首条，单条接口每次commit。
### 后端
apps/api-server/app/areas/legal/router.py：records、review_case_archive；通用records不接归档完整筛选；单条审核内commit。
### 数据库
business_records.id/module/status/data、workflow_events.record_id及旧案件投影；归档审核相关data键保留。仅查询/既有流程写入，不修改schema和历史数据。

## 5. 新旧差异和根因
|编号|页面/功能|旧|新|根因|
|---|---|---|---|---|
|D1|归档批量审核|旧链上述|新CaseCenterPage:2533 find只取首条，单条接口每次commit|前端列表状态和单条API未组成完整批量/分页契约|

## 6. 精确修改清单
- [x] apps/api-server/app/core/case_archive.py及areas/legal/router.py：提取无commit审核函数；新增批量DTO及接口逐条权限/状态/原因校验，锁定记录并单次commit；前端多行审核表单逐条归档号/备注，保留单条行为。
- [x] apps/admin-web/src/legal/CaseCenterPage.tsx及专用hook：接入上述API/表单与刷新。
- [x] apps/api-server/app/main.py：只注册追加归档接口切片，不改原路由顺序。
- 数据库：不新增表或迁移；沿用BusinessRecord/WorkflowEvent和旧投影。
- 风险：角色可见范围、亏损两阶段审核、混合批次失败原子性、日期边界、页面切换竞态。

## 7. 验证清单
- 自动化业务测试/API写入/浏览器：按本轮根指令不执行，保留用户验收。
- [x] Python编译、TypeScript静态检查通过（tsc --noEmit、py_compile，2026-09-05）；git diff --check通过。
- [ ] 生产构建：主任务统一执行。
- API成功/失败与持久化：源码检查每行校验、整批回滚与提交；用户部署后验收。
- [x] 不启动业务进程或创建业务数据。临时依赖junction只指向既有node_modules，编译缓存精确清理。
- 用户验收：多条归档批准/拒绝、混合无权限或无归档号整批阻断；筛选含日期/第三人；超过100条翻页。

## 8. 实施记录
- 提交：待主任务协调。
- 修改清单差异：无。
- 验证结果：静态检查通过；CodeGraph status/sync均报告本隔离树未初始化，使用直接源码链路复核。业务测试/浏览器未执行，生产构建由主任务统一进行。
- 术语核对：分页（Pagination），https://vibe-hub.org/pagination 。专用查询先筛选计数再取页；导出复用相同条件但不分页。
- 实际文件：core/case_archive.py；areas/legal/router.py；main.py；legal/hooks/useArchiveCases.ts；legal/CaseCenterPage.tsx；legal/services/workflowActions.tsx。追加3个归档接口，main切片138..141。
- 发布状态：未发布，由主任务统一发布；待用户验收。
