# S2-02 提成方案有效期整改

## 1. 清单原文
- 原始问题：查看我的新系统还有哪些地方没有跟旧的系统同步。
- 当前明确授权：修改 然后部署上去。
- 工作簿/行号：不涉及，用户直接功能审计确认后的整改S2-02。
- 补充/返工：排除历史数据同步；不改现有历史空结束日期。
- 状态：已实施，待主任务统一构建发布、用户验收。

## 2. 截图分析
本项无新增截图；采用旧页面、JS与新前后端代码证据。业务入口为员工提成设定和绩效管理“新增/编辑”。

## 3. 旧系统实现
GD.CRM.WEB/Areas/HR/Controllers/StaffController.cs Performance默认AnnualFrom今天、AnnualEnd加一年；Views/Staff/PartialView/Performance.cshtml绑定日期；Scripts/HR/Staff/HR.Staff.js:700结束为空阻断、728提交performanceForm。PerformanceCreateUpdate把完整model.Performance保存。结束日期是有效期边界。

## 4. 新系统当前实现
### 前端
HrCenterPage.tsx内嵌提成和HrPerformancePage.tsx独立绩效，结束日期允许空，前者新建还缺日期默认。通过hr subrecords/performance接口保存。
### 后端
core/system.py::_validate_hr_subrecord校验commission，开始必填结束可空；areas/hr/router.py创建/修改HrSubrecord。按案件匹配方案遇到空结束视为无限期。
### 数据库
hr_subrecords.data保存start_date/end_date，employee_id关联员工。无新增表或列；不批量修改既有数据，不改变旧记录读取语义。

## 5. 新旧差异和根因
|编号|功能|旧|新|根因|
|---|---|---|---|---|
|D1|新增有效期|今天至明年、结束必填|结束可空|前端默认及后端校验缺失|
|D2|修改既有方案|有结束日期|历史空值存在|修复必须要求用户明确补日期，不能打开页面自动填充并静默落库|

## 6. 精确修改清单
- [x] HrCenterPage.tsx：新建commission默认今天及一年后；编辑保留原日期；结束必填且不得早于开始；清空残留表单状态。
- [x] HrPerformancePage.tsx：仅新建默认一年，已有空结束保持空供用户补；必填结束及日期范围校验。
- [x] core/system.py::_validate_hr_subrecord：commission写入开始/结束均必填并验证范围，影响新建和用户主动保存编辑。
- 数据库：无schema迁移/数据补丁。既有空结束记录仍可读取；用户编辑时提示补齐后才能保存。
- 风险：历史空结束方案编辑现在需要选择结束日期；这是显式保存校验，不后台改写历史。

## 7. 验证清单
- Python语法编译、前端类型检查及根任务统一生产构建。
- 按用户直接问题工作流不执行业务测试/数据库写入/浏览器验收。
- 用户验收：两入口新建默认一年；结束空/早于开始不可保存；历史空日期显示不被自动修改，主动编辑需补齐。
- 权限仍由现有admin/manager及记录范围校验。
- 不创建测试业务数据/进程，无对应清理。

## 8. 实施记录
未提交，主任务统一发布；CodeGraph status显示本隔离树未初始化，采用直接源码定位。2026-09-05 TypeScript tsc -b、Python py_compile及system模块导入解析通过。未执行业务测试/浏览器操作，无业务数据、数据库变更或临时服务。仅隔离树依赖junction供类型检查使用，未修改共享依赖。现有空结束日期数据保持原样。
