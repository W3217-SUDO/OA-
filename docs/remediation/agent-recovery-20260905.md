# 智能体中心无回复整改

## 1. 清单原文
独立截图问题：智能体中心发送“概括业务空间现状”，只有用户气泡，没有答复。不涉及Excel。后续用户明确“已经恢复了”，按主任务要求停止扩大修改和发布，将现有变更独立保留，暂不纳入六项功能修复发布。

## 2. 截图分析
截图a95aecb5：智能体中心/统一业务空间，左侧案件、顶部流程条、右侧用户气泡、底部通用办公和输入框，精确对应AgentCenterPage。没有可见错误回复或生成进度。发送按钮在输入为空时为灰色，截图本身不能证明输入框被禁用。

## 3. 旧系统实现
本项为新系统智能体故障，不涉及旧业务系统功能对等。比较同一新系统的案件详情智能体：已调用stream:true并消费NDJSON，但中心仍为非流式一次性响应。

## 4. 新系统当前实现
前端AgentCenterPage.send用无超时axios.post，最终才整体显示state；切业务空间不取消消息请求和加载请求，旧响应可覆盖新空间。错误只toast且移除问题。后端legal/router.py的消息接口依据stream调用CaseAgentRuntime.invoke或invoke_stream。case_agent.py用90秒httpx读取超时，但排队/整个请求没有总期限。状态ready仅表示graph初始化，不代表模型可用。
数据库为现有LangGraph检查点，角色消息与pending_actions持久化；本轮不改结构、不补历史记录、不运行写入测试。

## 5. 差异和根因
已确认中心没有采用已存在的流式反馈链路，且无整体超时和切空间隔离。主任务线上只读证据：目标会话有1条用户消息和1条1728字助手消息，last_response也为1728字，模型实际成功，不能定性为模型宕机。前端map没有过滤助手角色，消息组件也会渲染普通文本，因此不能从截图直接证明回复内容被隐藏。单次现象可能与旧请求覆盖或生成完成前读取的检查点有关，但未取得足以确定单次原因的前端请求时序。用户报告已经恢复，不宣称本地改动解决了本次线上故障。
VibeHub已调用“流式响应”，匹配 https://vibe-hub.org/streaming-response 。专业定位：逐段显示答复，并在完成、失败、取消、超时时恢复可操作状态。

## 6. 精确修改清单
- [x] AgentCenterPage：调用已有stream:true；逐行消费delta/state/error，处理末尾无换行、缺终态；保留用户问题并显示错误；总期限恢复输入，取消与切空间隔离，不让旧响应覆盖新空间。
- [x] case_agent.py：为invoke增加包含排队和检查点的总期限；流发出等待心跳、保证取消清理；现有HTTP失败不伪装成正常答案。
- [x] 根据主任务运行证据补充边界；仅静态校验，不业务测试，不发布。
- 尚未实施的审阅建议：生成期间切空间返回后对只有用户消息的检查点有限回读、增量内容滚动、审批响应空间隔离、服务状态标签更精确。用户报告恢复后明确停止，未继续扩展修改；后续若启用本分支，需先完成审阅。

## 7. 验证清单
仅静态检查与主任务生产构建；无浏览器、无业务数据库写入。用户验收：发送问题、看逐段输出或明确失败；可停止；切空间后无串话；超时后可重新发送。保留权限与人工审批约束。

## 8. 实施记录
技能已读。CodeGraph未初始化，使用源码直接追踪，sync同样无法执行。Python py_compile、TypeScript tsc --noEmit、git diff --check通过；没有业务测试和数据写入。用户已报告恢复，现有改动作为未发布候选保存在独立分支，单次根因未证实，不纳入当前发布。
