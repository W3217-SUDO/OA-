# 8.27 工作表第 3 行读取确认单

工作表：8.27

行号：3

模块：控制台

问题文字：控制台待缴官费指的是案子里面申请付款的官费，现在界面显示待缴官费13件，实际上点进去却不止，而且开庭律师的名字全部显示待维护，这里有两个问题，一个是控制台数据和实际数据对不上，另一个姓名显示问题，如果该案子没有填某一个人员，那就横杠或者空白，不要写个姓名待维护

补充/返工文字：无。F“测试”、G“截图”、H“完成状态”、I“完成时间”均为空；该行无批注、无超链接、无隐藏列、无合并单元格，行高 108.75，非空文字单元格无特殊字体色或填充色。

新系统现状图：C“截图”列共 2 张。`image100.png`（保存为 `8.27_第3行_新系统现状_1.png`）显示控制台“待缴官费”为 13 件；`image101.png`（保存为 `8.27_第3行_新系统现状_2.png`）显示点击后费用查询共有 317 条，且“开庭律师”列大量显示“姓名待维护”，与控制台数量及空人员展示要求不符。

原系统目标图：D“原系统”列共 2 张。`image102.png`（保存为 `8.27_第3行_原系统目标_1.png`）显示控制台“待缴官费”为 6 件，并在开庭排期中显示真实开庭律师姓名，未填写人员的单元格为空；`image103.png`（保存为 `8.27_第3行_原系统目标_2.png`）显示从待缴官费进入的费用查询仅有 6 条，列表使用真实开庭律师姓名，空人员不显示占位姓名。

文字要求 -> 当前错误 -> 旧系统目标 -> 验收动作：

1. “待缴官费指案子里面申请付款的官费” -> 控制台卡片与费用查询采用了不一致或过宽的数据口径 -> 卡片数量与点击后的待缴官费记录集合严格一致，且仅统计案子中申请付款的官费 -> 在同一运行实例、同一角色下记录卡片数量，点击卡片进入列表，核对筛选状态、总数和数据库/API 同口径结果。
2. “控制台数据和实际数据对不上” -> 卡片显示 13 件而目标入口列表显示 317 条 -> 卡片和目标列表同源、同权限、同过滤条件 -> 浏览器实际点击卡片并证明目标 URL、筛选条件、列表总数与卡片数字一致，刷新后仍一致。
3. “开庭律师的名字全部显示待维护” -> 人员映射失败时统一回退为“姓名待维护” -> 有人员时显示系统中文姓名；未填写人员时显示横杠或空白 -> 使用真实记录核对开庭律师列；证明有效人员显示中文姓名，空人员显示横杠/空白，页面中“姓名待维护”数量为 0。
4. 截图同时覆盖控制台排期和费用查询 -> 同一人员展示缺陷出现在共享展示链路 -> 同义开庭律师字段应遵循一致规则 -> 验收控制台开庭排期和点击后的费用查询两个入口，不能以其中一个入口成功替代另一个。

图片锚点映射：`image100.png -> 8.27!C3`、`image101.png -> 8.27!C3`、`image102.png -> 8.27!D3`、`image103.png -> 8.27!D3`。仅提取并查看了第 3 行锚定图片。
