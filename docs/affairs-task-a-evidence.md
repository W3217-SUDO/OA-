# 事务中心 A：我的任务验收证据

## 双页面入口

- 旧站只读页：`https://sh.021ipr.com/Console/Index`（保留 1 个标签）。展开“事务中心”后菜单顺序为“我的任务、部门任务、公司任务”；点击“我的任务”仍停留在旧站客户 iframe，记录为旧站 iframe 导航限制，未执行任何写入。
- 本地 dev 页：`http://127.0.0.1:5173/?page=task-my-created`（保留 1 个标签）。事务中心菜单顺序与旧站一致；展开“我的任务”后为“我发起的任务、我接受的任务、我协作的任务、未读新消息的任务”。

## 本地真实闭环

- 在“我发起的任务”点击“新增任务”，空内容提交被必填规则阻止；填写唯一前缀 `CODEX-AFFAIRS-A-1785597846842` 后成功，提示“任务已发起，等待负责人接收”。列表回显任务号 `RW20260801232406984657`、标题、状态“待接收”、优先级“普通”、发起人/负责人 `admin`。
- 在任务编号按钮打开“案件任务”详情，回显任务流程、任务编号、标题、负责人、截止日期、状态和沟通记录；任务资料/反馈附件区域显示“暂无任务资料附件/暂无任务反馈附件”，上传按钮与反馈必填控件可见。
- 使用任务标题筛选后点击“查 询”，结果仅回显该前缀任务；清空筛选并查询后列表恢复。分页区域真实显示总条数、上一页/下一页禁用状态及“15 条/页”页码选择。
- 点击详情“撤回任务”，空原因被阻止；填写“事务中心闭环测试撤回”并确认后列表回显状态“已撤回”。

## 清理与限制

- 仅清理本线程创建的 `business_records.id=44`（任务号 `RW20260801232406984657`）及其 workflow/附件关联；SQLite 复核 `CODEX-AFFAIRS-A-%` 记录为 0。未触碰既有 `CODEX-CASE-B-1785597000000-TASK` 或服务器数据。
- 旧站始终只读；本轮没有修改本地业务代码，仅新增专测与证据文档。浏览器最终保留旧站/本地各 1 个标签。

## 尚未完成

- 旧站 iframe 无法进入任务列表，因此无法逐项取得旧站详情/附件控件证据；已如实记录入口与限制。
- 本轮未上传真实附件（详情区域已验证上传入口、禁用态及空态）；未伪造下载结果。
