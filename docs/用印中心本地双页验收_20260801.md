# 用印中心本地双页验收（2026-08-01）

## 证据

- 旧系统只读打开 `https://sh.021ipr.com/8101001001/AWS/OfficialDocument/List`（“我的用印申请/待审批”）：可见申请编号、申请人、申请时间、待审核状态、用印类型、印章类型、文件数、案号、合同号、客户、审核人/时间/意见；底部显示“申请用印/修改/撤回”、每页条数（10/15/20/50/100/200）、分页和 GO。
- 本地打开 `http://127.0.0.1:5173/?page=seal-my-pending`：同样显示筛选（申请编号、申请人、日期、案件/合同编号、客户、状态、类型、文件名）、查询按钮、申请列表列以及空态“申请用印”。点击“申请用印”打开弹窗，验证标题、客户/案件/合同关联、用印类型、印章、日期、份数、办理方式、电子印章、打印盖章、用途、说明、取消、保存草稿；并显示“先保存草稿，再上传文件，未上传不能提交审批”的门禁提示。
- 差异：旧系统样本有 12 条历史数据且列表底部提供“修改/撤回”；本地当前账号无待审批样本，空态仅提供“申请用印”，因此未执行写入、审批、撤回或删除操作，避免制造测试数据。
- 菜单顺序证据：两侧均展开“用印中心→我的用印申请→待审批/待用印/已用印/已拒绝/已撤回”，本地路由成功变为 `?page=seal-my-pending`；旧站待审批直达页成功加载。

## 结论

本轮完成“我的用印申请/待审批”最小只读闭环及本地新建表单门禁检查。旧站保持只读；未发现需立即修改的独立缺口。后续继续用印审核、行政用印及各状态页。

## CODEX-SEAL-D- 本地真实闭环

- 通过本地登录会话创建 `CODEX-SEAL-D-20260801-01` 草稿，接口返回业务记录 ID `30`、编号 `YY202608012239491C8`；按唯一前缀查询列表得到 1 条，证明保存后列表回显。
- PATCH 编辑后详情返回标题带“草稿编辑”、份数 `2`，证明编辑回显；未执行保存按钮以外的旧站写操作。
- 对该无附件草稿调用提交接口返回 HTTP `409`，页面门禁对应“未上传文件不能提交审批”。随后删除草稿，DELETE 再次返回 404，记录 GET 也返回 404，证明本地记录零残留。
- 本轮未上传附件：本地上传/下载仍需下一轮在浏览器文件选择可用时补证；旧系统始终只读。

## CODEX-SEAL-D-20260801-02 附件闭环

- 本地创建新草稿后上传 `.txt` 用印文件，记录 ID `29`，附件 ID `1`；详情回显附件数量 `1`。
- 认证 HTTP 下载返回 `200`，`Content-Disposition: attachment; filename="CODEX-SEAL-D-20260801.txt"`，`Content-Type: text/plain; charset=utf-8`，`Content-Length: 39`；下载文件 SHA-256 与源文件均为 `8C33DE1ACF9D40F3E894093D312917CDD22D29FCF5A88F096D5DC788B0592A5E`。
- 删除附件和草稿均返回 `204`；临时源文件、下载文件、响应头文件已删除，未部署服务器。
- 接口只读核对：用印审核 `view=audit` 返回 1 条待审批、summary pending=1；行政用印使用 `view=admin` 被接口拒绝“无效的用印视图”，前端实际使用 `view=all`，应在后续浏览器页继续核对行政待用印按钮与状态。

## CODEX-SEAL-D-20260801-CHAIN 状态链

- 新建记录 ID `29`、附件 ID `1`：草稿→提交审批返回“待审批”→管理员审核通过返回“待用印”→执行用印返回“已用印”。`view=all` 列表分别捕获待用印和已用印记录各 1 条，证明行政用印页使用全量视图进行状态归类。
- 终态记录不允许普通删除（附件 DELETE/记录 DELETE 均返回 `409`），符合业务门禁；随后仅针对本线程唯一 CODEX 标记使用本地 SQLite 清理，删除 1 个附件文件、workflow_events、记录及附件行，复核 `business_records(id=29)=0`、`file_attachments(record_id=29)=0`。
- 临时链路文件已删除；旧站对应页面保持只读。本轮未执行拒绝/撤回分支，以免增加不必要测试数据，后续可用独立草稿补测阻断。
