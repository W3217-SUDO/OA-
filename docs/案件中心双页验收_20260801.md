# 案件中心双页验收记录（2026-08-01）

## 页面与安全边界

- 申浩旧系统只读：`https://sh.021ipr.com/100110120/Legal/CaseList/List?caseTypeId=120`、`https://sh.021ipr.com/1001000002/Legal/Case/Create?caseTypeId=120`。
- 本地 dev：`http://127.0.0.1:5173/?page=case-mine`、`http://127.0.0.1:5173/?page=case-new-civil`。
- 旧系统仅点击/读取，未创建、编辑、审批、上传或删除数据。

## 本轮本地闭环

使用唯一前缀 `CODEX-CASE-B-1785595782679`，仅本地创建：临时律师岗位、在职员工/登录账号、客户、合同及案件。

1. 合同上传“合同附件”，提交审批并由 admin 审批通过；接口验证合同进入 `eligible-contracts`，状态为“已通过”。
2. 新建案件页面选择自建合同：客户、合同号、案源人、案件名称自动带入；案源人改为 `CODEX-CASE-B-1785595782679-SOURCE-EDITED`，合同号显示自建编号且不是 `99`。
3. 选择临时律师，完成基本信息、当事人信息、司法机关信息；未选法院时页面阻断并提示“启用的法院信息必须选择有效法院”，选择法院后保存成功。
4. 列表回显案件号 `SHMS2600388`；点击案件号进入详情，核对客户、合同号、案源人、经办律师和案由。进入“案件任务”关联子页面，显示真实空状态。
5. 打开“修改基本信息”，修改案件名称并保存，详情回显修改结果。
6. 点击“下一步”未选经办律师时，页面真实提示“请按顺序录入经办律师”。

## 清理与接口证据

- 已清理案件、客户、合同、审批步骤、合同附件、工作流事件、临时律师员工/账号和岗位角色。
- SQLite 精确查询：`CODEX-CASE-B-1785595782679-*` 业务记录、用户、岗位角色及附件均为 0。
- 未修改既有员工、账号或业务记录；未部署服务器。

## 验证命令

- `node --experimental-strip-types apps/admin-web/caseContractPrefill.test.mjs`：2/2 通过。
- `npm.cmd run build`：通过。
- `python -m py_compile apps/api-server/app/main.py apps/api-server/app/models.py`：通过。
- `git diff --check`：通过。
# 清理阻塞登记（2026-08-02）

- 系统：申浩旧系统（sh.021ipr.com）
- 自建记录：案件类型 ID=160，前缀 `CODEX-SYS-D-TYPE-20260802-01`
- 状态：保留显著 CODEX 测试名称，不再改动
- 原因：CaseTypeList 页面单条记录仅提供“修改”，无删除/停用按钮；归档 `SystemCenterController` 仅有 `CaseTypeList`、`CaseType`、`CaseTypeCreateUpdate`，无删除/停用 Action。无法安全清理，禁止猜测隐藏批量接口或操作其他记录。

# 系统参数菜单核对（2026-08-02）
- 旧站 live 页面：`https://sh.021ipr.com/7001001/System/SystemCenter/Index`。逐项读取菜单 DOM，实际入口为案件类型、费用类型、案件阶段、法院设置、公证处设置、案由设置、付款类型、公司设置；页面内无“权利类型”或“业务类型”文字/链接。
- 本地页面：`http://127.0.0.1:5173/?page=system-parameters`。当前显示共享时间配置表单，未显示“权利类型/业务类型”入口。
- 结论：旧站 live 不存在该入口，故本轮不创建测试数据、不扩展本地功能；保留本地既有内容。

# 费用类型逐动作核对（2026-08-02）
- 旧站动作：打开 `https://sh.021ipr.com/7001004/System/SystemCenter/CaseFeeTypeList`，列表显示查询、新增费用类型、分页（10/15/20/50/100/200、1-5页）及每行“修改”；逐行未发现删除/停用入口。
- 本地动作：打开 `http://127.0.0.1:5173/?page=system-parameters-fee-type`，显示查询、新增费用类型、修改及分页控件；未发现删除/停用入口。
- 差异/处理：未发现旧站要求而本地缺失的安全删除能力；因旧站无单条删除/停用入口，本轮不创建数据、不产生残留。

# 法院设置逐动作核对（2026-08-02）
- 旧站动作：打开 `https://sh.021ipr.com/7001007/System/SystemCenter/CourtList`，确认法院名称、法院代码查询，新增法院、修改、分页（10/15/20/50/100/200及多页）；列表行仅有“修改”，无删除/停用入口，未创建数据。
- 本地动作：打开 `http://127.0.0.1:5173/?page=system-parameters-court`，确认同名/代码查询、新增法院、修改及分页；无删除/停用入口。
- 差异/处理：未发现可安全清理的单条删除/停用能力或本地缺项，本轮不新增测试残留。

# 公证处设置逐动作核对（2026-08-02）
- 旧站动作：打开 `https://sh.021ipr.com/7001011/System/SystemCenter/NotaryOfficeList`，确认公证处名称查询、新增公证处、修改、分页（10/15/20/50/100/200及多页）；单条记录无删除/停用入口。
- 本地动作：打开 `http://127.0.0.1:5173/?page=system-parameters-notary`，确认公证处名称查询、新增公证处、修改及分页；无删除/停用入口。
- 差异/处理：旧站无安全清理能力，本轮只读核对，未创建测试数据。

# 案由设置逐动作核对（2026-08-02）
- 旧站动作：打开 `https://sh.021ipr.com/7001008/System/SystemCenter/CauseList`，确认案由名称、上级案由查询，新增案由、修改、分页（10/15/20/50/100/200及多页）；单条记录无删除/停用入口。
- 本地动作：打开 `http://127.0.0.1:5173/?page=system-parameters-cause`，确认案由名称、上级案由查询，新增案由、修改及分页；无删除/停用入口。
- 差异/处理：旧站无安全清理能力，本轮只读核对，未创建测试数据。

# 付款类型逐动作核对（2026-08-02）
- 旧站动作：打开 `https://sh.021ipr.com/7001009/System/SystemCenter/PaymentTypeList`，确认付款类型名称查询、新增付款单位、修改、分页（10/15/20/50/100/200及多页）；单条记录无删除/停用入口。
- 本地动作：打开 `http://127.0.0.1:5173/?page=system-parameters-payment`，确认付款类型名称查询、新增付款单位、修改及分页；无删除/停用入口。
- 差异/处理：旧站无安全清理能力，本轮只读核对，未创建测试数据。

# 公司设置逐动作核对（2026-08-02）
- 旧站动作：打开 `https://sh.021ipr.com/7001010/System/SystemCenter/Company`，只读确认公司名称、公司代码、简称、联系地址、电话、传真、邮箱、邮编、银行名称、银行账号、网站等字段及“保存”按钮；未修改、未上传 logo/印章，未提交保存。
- 本地动作：打开 `http://127.0.0.1:5173/?page=system-parameters-company`，确认对应公司配置表单与“保存”按钮；页面未提供独立取消按钮，未修改任何值。
- 差异/处理：旧站配置无删除/停用入口，属于全局配置；为避免无法精确恢复，本轮严格只读，不使用临时值、不产生数据库变化。
