# HR 权限真实会话终验

- 对照页：旧系统 `https://sh.021ipr.com/Console/Index`；本地 `http://127.0.0.1:5173/`。旧系统只打开部门列表并查看分页/操作列，未提交任何新增、编辑、保存或删除。
- 本线程只在本地创建带 `CODEX-HR-ACCESS-` 前缀的经理与普通用户账号，首次登录均完成强制改密；密码未写入文件。账号 ID：经理 `4`，普通用户 `5`。
- 管理员：创建前系统用户页显示 `admin` 为系统管理员，并可见修改、重置密码、解锁、删除等最高权限操作。
- 经理：员工页可访问但按本部门数据范围返回“暂无数据”；部门、角色页可读且没有新增/编辑/删除按钮。未发生状态提交，因为无可办理的本部门员工记录。
- 普通用户：直达员工页显示“无权访问”，侧栏没有人事中心入口，员工/部门/角色写操作均不可见。
- 服务端 HR 员工新增/编辑/删除、部门及角色写接口统一调用 `_require_admin`，越权为 HTTP 403；`node --test apps/admin-web/hrAccessGuard.test.mjs` 4/4 通过。
- 清理：仅删除本线程创建的 users.id `4`、`5`，只读复核 `username like 'codex-hr-access-%'` 结果为空；既有账号未触碰。
