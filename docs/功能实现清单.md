# 功能实现清单

## 2026-07-28 合同事项记录补齐（本地，待页面回归）

- [已完成代码] 对照旧 FCM `ContractEventController` 与合同草稿页的“事项记录”入口，新增合同详情内独立事项记录区及“新增事项”弹窗；不再把审批、归档等系统流程时间线伪装为人工事项记录。
- [已完成代码] 新增受合同数据范围保护的 `GET /contracts/{id}/events` 与专用 `POST /contracts/{id}/events`。查看沿用合同可见范围；写入仅限合同负责人、同部门负责人或管理员，已归档合同被 409 阻断。事项内容限制 1000 字；每次新增同时留下“新增合同事项”审计事件，但不修改合同状态、审批节点、附件或付款数据。
- [已验证自动化] 前端生产构建、Python 编译、菜单/静态审计及客户端 API 覆盖通过；冒烟脚本已补空列表 → 创建 → 列表回显断言。本轮遵循“已验收的不重复验收”，未重跑合同审批、合同到案件、用印、付款、开票或归档流程。
- [仍未证明] 待后续在页面创建可精确清理的临时合同后，补一次“合同查看 → 新增事项 → 列表回显 → 归档阻断”的页面证据；不能用本次静态验证代替该新入口的页面证明。

## 2026-07-28 财务流水回退入口（本地，待页面实操）

- [已完成代码] 旧财务源码 `Scripts/FAM/AP/FAM.AP.Payment.js` 的 `PaymentRollback` 证明付款办理需要回退能力。新系统原有的管理员受限 `DELETE /finance/transactions/{id}` 已真实删除流水及关联凭证，并会按剩余付款金额恢复关联请款单状态；本轮将该能力接入“财务流水”表的管理员专用“回退”按钮。
- [权限与阻断] 非 `admin` 不显示入口，服务端仍以管理员权限作最终校验。操作必须二次确认；付款流水会明确提示将同步恢复关联请款单状态，其他流水会明确提示删除其凭证。接口拒绝、权限或数据错误均展示真实服务端原因。
- [已验证/仍未证明] 前端生产构建、Python 编译、菜单覆盖与客户端 API 覆盖审计通过（415 个前端 API 路径）；遵循“已验收的不重复验收”，未回退任何既有财务数据。后续仅在页面创建的临时付款流水上补一次“回退 → 请款状态/凭证同步变化 → 精确清理”的页面证据。

## 2026-07-28 用印／印章资产未验收缺口审计与整改（本地）

- [已审计] 对归档旧系统合同用印入口（`Areas/FCM/Views/ContractDraft/ToOfficialDocumentAudit.cshtml`）及现有用印专用接口做只读对照：合同用印申请、草稿/提交/审批/实际用印/归档、撤回、附件打包、案号/合同号关联和管理员印章维护均已有对应真实接口。未发现旧源码证据支持额外的独立“电子签章”或印章借还模块，本轮不伪造功能。
- [已修复] 新系统的 `seal-admin` 路由此前虽设置为“印章管理”，但实际仍渲染用印申请表，且“新增印章入库”弹窗没有可达入口。现该路由渲染独立印章资产台账：按编号/名称/类别/保管人实时查询、刷新、管理员新增入库和逐条维护；资产表显示保管人、位置、状态、累计用印和最近使用时间。新增/维护继续调用既有受管理员权限保护的 `/seals/assets` 专用接口，不触碰已验收的用印审批/流转。
- [已验证静态] 前端生产构建、Python 编译、菜单覆盖审计与差异检查通过；菜单审计新增资产台账、真实资产数据源和无固定操作列的断言，防止再次退化为申请表或被右侧操作列遮挡。
- [仍未证明] 本轮遵循“不重复验收”要求，未创建印章资产或重新运行用印审批链路。待下一次仅针对新增资产台账页面的临时资产创建/维护验收时，补页面回显、非管理员拒绝和精确清理证据。

## 2026-07-28 任务资料附件（本地，待页面回归）

- [已完成代码与静态验证] 新增“任务资料附件”专用接口与前端入口：创建任务时可选择多文件，任务详情将资料附件与反馈附件分开列表；任务参与人可上传、查看、下载，删除仅限上传者或管理员。资料附件上传、删除均写入独立审计事件和参与人通知，不改变任务状态，也不伪造任务反馈。
- [已验证自动化] 前端生产构建、Python 编译、菜单覆盖审计、客户端 API 覆盖审计通过；冒烟脚本已补纯协作者多文件上传、列表、下载、删除和非参与人拒绝的精确清理断言。
- [仍未证明] 本轮未执行既有任务流转，也未在浏览器完成“创建任务并上传资料 → 详情回显/下载 → 删除”的页面闭环；待统一页面验收时创建临时数据并精确清理后再标记为已证明。

## 2026-07-28 官文收文批量业务处理状态（本地）

- [已完成代码] 官文收文在原有“待登记 → 待签收 → 已签收 → 已归档”生命周期之外，新增独立 `business_process_status`（未处理/已处理）。官文上传默认未处理；官文收文台可按该字段筛选、显示，并对勾选记录批量“标记已处理/标记未处理”。该字段不会修改签收、归档等文档生命周期状态。
- [已完成代码] 新增专用 `POST /documents/official/process` 命令：仅接受收文记录，逐条校验可见范围及负责人/同部门负责人/管理员权限，写入处理人和处理时间，并留下“标记官文已处理/未处理”审计事件；非收文、无权或不存在的目标均由真实接口阻断。官文 CSV 导出同步包含业务处理状态。
- [已验证自动化] Python 编译、菜单/静态审计（`OFFICIAL_RECEIPT_PROCESS_OK`）与前端生产构建通过；冒烟脚本已覆盖处理→取消处理、生命周期不变及审计事件，但本机未配置 `SMOKE_PASSWORD`，脚本在登录前安全停止，尚未执行该 API 冒烟，不将其记作已验证。

## 2026-07-28 合同附件查看与案件附件重命名（本地）

- [已完成代码] 所有合同查看入口现统一只读加载该合同的真实附件，详情弹窗提供受既有权限保护的下载；切换/关闭详情会取消陈旧响应，加载失败保留接口原因。不创建或删除附件，也不改变审批状态。
- [已完成代码] 案件详情附件新增独立“重命名”入口与 `PUT /cases/attachments/{id}/rename`。接口仅接受案件附件，复用案件团队写入权限、创建审批完成及归档阻断；拒绝路径、空名和扩展名变更，只更新原文件名并保留内容/存储路径，同时写审计事件。
- [已验证/仍未证明] 最新 API/Web 容器已重建健康，前端生产构建、Python 编译、菜单审计和 406 个客户端 API 路径覆盖通过。合同附件下载、案件附件重命名及归档阻断的浏览器实操仍待下次新增页面数据时一次性补证；不为此重复既有合同、案件流程验收。

## 2026-07-28 刑事案件完成后专用资料维护（本地）

- [已完成代码] 刑事案件创建完成后，详情页“维护刑事资料”提供当事人、公安机关、检察院、审级法院四个独立维护表单；分别调用 `/cases/{id}/criminal/litigants`、`/public-security`、`/procuratorates`、`/courts`，不放宽新建阶段接口，也不允许通用记录更新绕过。
- [权限与阻断] 仅负责人、同部门负责人或 `admin` 可维护；案件须已完成创建审批且未处于归档流程。每类保存均写独立审计事件，公安/检察院/审级字段继续保留刑事专用校验。
- [已验证/仍未证明] 前端生产构建、Python 编译通过；尚未以页面创建的刑事临时案件逐项修改、刷新回显和归档阻断，后续统一页面验收时补一次并精确清理，不重复新建流程验收。

## 2026-07-28 收发文附件与模板单条详情证据审计（本地，只读）

- [已审计/无需伪详情] 针对归档旧源码 `D:\OA系统源码归档_20260717\source-extracted-v1\SH.CRM.WEB\Areas\FIO\Controllers\LegalOfficialFileController.cs` 及对应 `Views\LegalOfficialFile` 的定向核对显示：收文元文件仅有列表、上传、删除和批量修改收文日期；未见单条附件只读详情 action/view。相关专利文件页同样是列表、点击下载、打包下载和导出下载。第三方上传控件自带的预览不能视为业务对象详情。
- [已审计/无需伪详情] 《思法汇成法律服务机构管理系统2022》PPT 对照台账的第 32--34、44--46 页描述的是文件查询、收集、拖拽上传和重命名，未给出“单条附件详情”或“单条文书模板详情”的页面/操作证据；在上述旧源码的业务控制器与视图中也未找到 `DocumentTemplate`、模板详情页或模板详情 action。此结论是“已核对范围内未发现需求”，并不把缺证据误报为旧系统功能。
- [当前对应] 新系统附件已有真实列表、所属收发文详情中的关联附件、上传、下载和受权限控制的删除；文书模板已有真实列表、创建、编辑、启用/停用、删除，并供智能文档任务按模板 ID 使用。因此附件/模板搜索结果仍只进入其真实工作列表，不伪造没有业务依据的只读抽屉。若业务方后续确认需审计单个附件元数据或模板版本，应先补受权限保护的按 ID 查询接口和只读消费器，再接入精确详情跳转。

## 2026-07-28 任务编号与案号列防串列整改（本地）

- [已完成代码] 任务主列表、未读任务列表和案件上下文任务表统一改为固定表格布局；任务编号/案号列加宽为 200/180，并对两类业务链接强制块级宽度、最小宽度和省略显示。横向滚动基准按完整列宽调整，避免长编号覆盖相邻案号列。
- [已验证自动化与页面加载] 前端生产构建、菜单审计、客户端 API 审计及完整 `verify-local.ps1` 均通过（399 API 调用、19 组冒烟、零测试残留）。本地“我发起的任务”页面重载后，长任务号与案号已分别出现在独立单元格，未见截图中的串列。
- [仍未证明] 本轮浏览器自动点击该标签页持续超时，未产生任何业务状态变更；待可用点击通道恢复后，以长编号样本补“任务号打开详情、案号打开案件详情”的实际点击与无权限证据。

## 2026-07-28 搜索与通知的业务详情精确跳转（本地）

- [已完成代码] 全局搜索中的费用、用印、收发文、仓库记录现会保存稳定 `record_id`，再进入各自真实详情：费用详情弹窗、用印详情抽屉、收发文登记详情抽屉、证物基本信息及流程详情弹窗；财务待审批通知也按通知的 `source_id` 打开对应费用详情。详情均先通过受数据范围保护的 `GET /records/{id}` 回读并核验模块，不存在、模块不符或无权限会显示真实接口错误，不会退化为伪详情或仅列表。
- [已验证自动化] 前端 TypeScript/生产构建、Python 编译、菜单静态审计及 2026-07-28 完整 `verify-local.ps1` 通过（399 个前端 API 调用、19 组 API 业务冒烟、`SMOKE_CLEANUP_OK`）。浏览器逐入口点击证据仍待补录。
- [已完成代码] 人事搜索结果现按稳定 `record_id` 回读员工档案并打开既有只读“员工查看”弹窗；不以系统账号名称或列表索引猜测员工对象。报表搜索结果经只读审计仍仅能进入报表模块列表，当前没有单份报表详情/执行记录查看入口，本轮不伪造跳转。

## 2026-07-28 全局搜索、通知与审计日志精确跳转审计（本地）

- [已完成代码与静态验证] 全局搜索对于案件、合同、客户、任务、线索、公证和证物均携带稳定业务记录 ID 进入对应只读详情；通知中心对案件、合同、任务及调查类通知同样按 `source_id` 保存具体详情目标。审计日志接口本来已返回 `record_id`，前端现已保留该 ID 并在以上七类业务的编号入口中使用，避免仅按业务编号误解析到其他记录。
- [已验证自动化] 前端生产构建、菜单/关系静态审计通过；API 冒烟增加审计事件的 `record_id` 与报表记录 ID 一致性断言。2026-07-28 已通过完整 `verify-local.ps1`：392 个前端 API 调用覆盖、19 组 API 业务冒烟及 `SMOKE_CLEANUP_OK` 均成功。
- [仍未证明/缺少详情路由] 用印、财务、收发文、仓库搜索结果及财务通知现已具备真实详情上下文；人事、报表搜索结果尚缺专用详情消费入口。浏览器点击与无权限阻断证据仍待补录，不得以自动化验证代替页面证据。

## 2026-07-28 案件详情浏览器验收准备复核（本地）

- [已修复] 案件详情原本会向所有具备基本信息权限的案件显示“修改基本信息”，但提交端点仅适用于法律顾问案件；现仅法律顾问显示该入口，普通民事/刑事/行政/仲裁案件不会误开顾问表单或调用错误接口。
- [当前真实可用（代码与接口已对照）] 详情顶栏的登记进展、开庭排期、人员分配受逐案件 capability 与归档状态控制；文档支持目录筛选、上传、预览、下载、ZIP 下载和受权删除；提醒、案件日志分别有新增/删除或新增入口；费用可在详情中按律所/平台/内部查看，具备权限者可从案件操作创建案件费用；案件/客户任务可打开真实任务详情并由任务中心承担流转。
- [关联入口状态] 客户、合同、存在关联值时的线索与原案件号均为可点击详情入口；案源律师目前是人员展示字段，未发现旧源码中可打开的独立案源业务详情，暂不伪造跳转。
- [只读对照证据] 旧源码 `SH.CRM.WEB/Scripts/Legal/Case/Legal.Case.js` 存在 `CaseBasicEdit`、`CaseFiles` 的上传/查看/下载/编辑/删除及 `CaseTasks.CaseTask` 的列表、新建、编辑、`TaskDetail` 查看；PPT/功能清单中的案件详情页同样包含文档、费用、提醒、日志和任务分区。
- [已完成代码与自动化验证] 民事、刑事、行政及国家赔偿案件现有独立“基本信息修改”表单与 `/cases/{id}/normal-basic` 专用接口；客户/线索选择、案型字段、负责人/同部门负责人/管理员权限、审批完成与归档阻断、审计事件均已实现，并已纳入 2026-07-28 完整 `verify-local.ps1`（399 前端 API、19 组冒烟、零残留）。
- [仍未证明/待补] 普通案件编辑的浏览器逐案型提交、费用编号逐条详情跳转，以及“案件详情 → 关联线索 → 调查中心”办理闭环仍待页面证据；后续需按管理员、案件负责人、经办律师、助理及无权限人员验证可见性、阻断和真实提交，并精确清理页面创建数据。

## 2026-07-28 案件详情任务入口复核（本地）

- [已完成代码] 案件任务、客户任务及案件任务抽屉内的任务编号/名称仍进入真实任务沟通详情；入口不再固定跳转仅管理员可见的“公司接受任务”，而是按当前用户在该任务中的发起、负责人、协作身份进入“我的发起/我的接受/我的协作”页面，管理员保留全所入口。
- [已完成代码] 案件内新建任务受案件团队能力控制，并使用真实 `/tasks` 接口；任务的接受、拒绝、完成、验收、交接、重启和反馈由目标任务详情按参与身份及状态显示并调用专用接口。
- [只读对照证据] 旧源码 `SH.CRM.WEB/Scripts/Legal/Case/Legal.Case.js` 的 `CaseTasks.CaseTask` 提供任务列表、新建、编辑和 `TaskDetail` 查看；当前“案件任务”已覆盖新建与真实详情入口，详情内流转动作由任务中心统一承接。
- [仍未证明] 当前案件详情未单列“调查任务”表；旧源码的 `CaseClues` 是从已选线索打开/新建调查任务，而不是仅凭案件直接列出。新系统案件详情可打开关联线索，调查中心具备调查任务的新建、子任务、分配与完成入口；仍需使用实际关联线索在浏览器完成“案件详情 → 线索 → 调查任务”闭环点击证据，未将其误报为已完成。
- [已验证部分门禁] 前端生产构建、Python 编译和菜单静态审计 `CASE_CROSS_MODULE_RELATIONS_OK` 已通过。

## 2026-07-28 案件跨模块关联入口复核（本地）

- [已完成代码] 案件列表、法律顾问案件列表、票据收发页及案件详情中的合同号、客户均改为打开对应只读详情；优先携带稳定记录 ID，缺失时保留业务编号回退。
- [已完成代码] 案件详情新增存在关联值时的线索、原案件号入口；案件任务、客户任务和“案件任务”抽屉中的任务编号、任务名称均打开任务沟通详情，而非停留在纯文本或仅打开案件任务列表。
- [已验证部分门禁] 前端生产构建、Python 编译和菜单静态审计 `CASE_CROSS_MODULE_RELATIONS_OK` 已通过。
- [仍未证明] 本轮未在浏览器以实际合同、客户、线索、原案件和任务样本逐项点击验证；后续页面验收应使用可见样本核实目标详情、无权限提示和无关联值提示，再精确清理测试数据。

## 2026-07-28 开庭排期关联案件 ID 解析（本地）

- [已完成代码] 开庭排期、排期专用列表、特殊案件号及退费“案件任务”入口统一以稳定 `case_record_id` 解析真实案件；不会把开庭排期自身 ID 当作案件 ID，也不再依赖首屏 100 条案件列表。
- [已完成代码] 解析器先按已关联案件使用，再按 `case_record_id` 调用受数据范围保护的记录详情接口；仅缺少稳定 ID 的旧记录才按案号精确查询。关联目标不存在、记录类型不为案件或当前账号无权访问时均提示明确原因，不伪造详情或错误打开其他案件。
- [已验证部分门禁] 前端生产构建、Python 编译、菜单静态审计 `CASE_HEARING_CASE_RESOLVER_OK` 已通过。
- [已补冒烟，尚未执行] 已补开庭记录 `case_record_id`、案号及通过稳定 ID 回读案件一致性的 API 断言；全量 API 冒烟在执行至本断言前被并行 `/records` 数据范围改动阻断。
- [仍未证明] 本轮未操作浏览器，仍需以第 101 条后的排期样本完成“案号点击、选中排期后更多操作、无权案件提示”页面证据并精确清理。完整 `verify-local.ps1` 本次在 API 冒烟前段被并行的通用 `/records` 数据范围改动阻断（既有客户最近更新样本 PATCH 返回 404），与本排期改动无关；该主线修复后须重跑全量门禁。

## 2026-07-28 调查任务创建校验可见反馈（本地）

- [已完成代码] 调查任务抽屉创建按钮在任务名称、负责人、截止日期或子任务父任务校验不通过时，明确提示首个缺失字段并自动滚动定位；不再出现未发送 POST 却无任何说明的静默失败。
- [已完成代码] 校验通过后仍调用专用 `POST /investigations/{record_id}/tasks`，成功后重新读取任务列表并重置表单；日期格式无效和服务端业务阻断继续展示真实错误原因。
- [已验证] 静态审计新增 `INVESTIGATION_TASK_VALIDATION_FEEDBACK_OK` 门禁；待浏览器分别验证空字段无 POST/有定位提示，以及填齐字段后真实 POST、成功回显和精确清理。

## 2026-07-28 案件任务创建授权与案号防绕过（本地）

- [已完成代码] 通用任务创建接口在收到关联案号时，必须解析真实案件、校验案件可见范围，并以案件团队 `can_create_case_task` capability 决定是否可创建；不存在的案号明确返回 404，案件任务未关联案号明确返回 422，受派律师助理及无关人员不能通过构造案号绕过权限。
- [已完成代码] 合法主办律师、案件负责人、同部门负责人和管理员保留创建案件任务的能力；新任务统一使用案件的真实客户信息，并落库稳定案件记录 ID，避免请求中伪造客户或案号造成关联漂移。
- [已完成代码] 案件详情“新建案件任务”入口按同一 capability 显示，直接触发时也会给出无权限提示，避免页面异步加载、残留弹窗或脚本调用先展示越权入口。
- [已验证] 静态审计 `CASE_TASK_AUTHORIZATION_OK`、前端生产构建、Python 编译及完整 `verify-local.ps1` 均通过；API 冒烟实际覆盖主办律师创建成功且关联信息规范化、不存在案件 404、律师助理 403、无关人员 404，并输出 `SMOKE_API_OK: 19 groups passed` 与 `SMOKE_CLEANUP_OK`。
- [仍未证明] 本轮未操作浏览器；多角色页面点击证据仍需使用页面临时数据创建并精确清理后补录。

## 2026-07-28 工作台标签与旧路由语义对齐（本地）

- [已完成代码] 工作台恢复标签不再复用持久化数据中的内部路由名；初始化及菜单异步加载后均以当前导航菜单统一解析全部已打开标签，兼容旧路由映射后持久化回写中文语义标签。
- [已完成代码] 页面标题与面包屑改为复用同一解析器；`notary-import-info` 等旧路由不再因未出现在当前导航树而错误回退为“控制台”。
- [已验证] 菜单静态审计新增解析器、全部标签归一化及页头复用门禁；待浏览器恢复包含多个旧深层标签的工作台并刷新菜单后，补录“所有标签与页头均为中文业务语义”的点击证据。

## 2026-07-28 人事员工账号原子同步边界（本地）

- [已完成代码] 人事专用“转正/离职/停用”流转现以员工档案状态为唯一入口同步关联普通登录账号：转正恢复登录，离职或停用立即禁止登录，并写入同步时间和结果；仅 `role=user` 的员工账号参与同步，`admin` 与遗留高权限账号绝不因人事状态流转被修改。
- [已验证自动化] 冒烟先停用关联普通账号，再通过“试用→在职”确认账号恢复；完成“在职→离职”后确认账号不可登录。该数据按既有冒烟清理方案精确删除，不触及管理员或用户既有数据。
- [已完成代码] 人事中心“新建员工”选择“员工账号”时，员工档案和可登录系统用户在同一事务中创建；密码仅以安全哈希保存，新建账号固定为普通用户，员工职务岗位仍独立保存并继续作为调查等业务权限依据。
- [已完成代码] 对已存在但尚未建立人事档案的普通系统用户，新建员工会原子关联并同步姓名、部门、状态和人事资料，不重置原密码、不抬升角色；用户名已关联其他员工档案、格式不正确、或试图覆盖 `admin` 时均返回明确阻断。
- [已完成代码] 员工资料修改、账号改名、启用/停用会同步至关联登录账号；`admin` 不允许经人事入口覆盖、改名或停用。带有效登录账号的员工档案不允许从通用删除入口删除，必须走人事停用，避免遗留孤立账号；非员工账号不会隐式创建系统用户。
- [已验证自动化] `smoke-api.py` 覆盖：创建请求即使携带高权限角色仍得到普通用户、重复用户名/覆盖管理员/通用删除被阻断、改名后旧账号不能登录、停用和重新启用两侧状态同步。完整 `verify-local.ps1` 仍待本轮最终运行记录。
- [既有数据边界] 不对已有员工档案或已有系统账号做隐式回填、补建或改密。既有员工如需获得登录账号，应由管理员在页面以“员工账号”新建/关联并核对用户名；已有系统账号可在页面建立对应员工档案后关联。测试数据继续由冒烟脚本按精确 ID 清理用户和档案，不触碰既有数据。

## 2026-07-28 跨标签退出登录同步（本地）

- [已完成代码] 统一客户端会话清理：退出和登录过期均清除令牌、当前用户、工作台标签及案件/合同/客户/调查/任务等业务详情导航上下文，并将地址回到根路由，避免下一位登录人继承上一会话的深层页面或关联目标。
- [已完成代码] 新增 `storage` 监听：任一同源标签页移除 `access_token` 后，其他已打开标签同步回到登录页；不再依赖下一次接口 401 才清理旧的 React 会话状态。
- [已验证] 菜单静态审计新增 `WORKSPACE_LOGOUT_SYNC_OK` 门禁，守住会话清理、根路由替换及跨标签监听代码；本轮仍需由浏览器以两个标签页实测“标签 A 退出 → A/B 均回登录 → 普通用户重新登录无管理员菜单”，完成后再标记浏览器验收。

## 2026-07-28 案件列表与底部操作权限收敛（本地）

- [已完成代码] 案件列表、法律顾问列表、执行案件列表、归档列表以及底部“更多操作”统一读取 `GET /cases/{id}/action-capabilities` 的同一份 capability；不再仅凭菜单或页面角色预先展示越权入口。
- [已完成代码] 经办律师仅显示附件上传、提醒/日志详情入口、案件进展和开庭排期；律师助理仅显示附件上传、提醒/日志详情入口；案件分配、基本信息修改、案件费用、归档及归档审核不会向两类受派人员显示。无关人员既不能获取案件，也不会获得操作入口；管理员与负责人/部门负责人保留原有可用权限。
- [已完成代码] 列表行操作、底部“更多操作”、开庭排期快捷入口、执行案件“修改进度”、归档检查和法律顾问编辑均加入前端 capability 门禁；直接触发函数也会给出明确的无权提示，避免因页面异步加载或其他入口而先显示操作再由后端返回 403。
- [已验证] 菜单覆盖静态审计新增 `CASE_TEAM_ACTION_CAPABILITY_UI_OK`，用于守住 capability 拉取、行操作、上传控件、底部更多操作及归档门禁；前端生产构建、Python 编译、菜单覆盖审计和完整 `verify-local.ps1` 均已通过（`SMOKE_API_OK: 19 groups passed`、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`）。
- [仍未证明] 本轮按任务要求未操作浏览器；待以经办律师、律师助理和无关人员分别登录页面，验证隐藏/可见操作与真实点击结果，并在创建任何临时数据后按精确记录清理。

## 2026-07-28 关联字段浏览器抽检（本地）

- [已通过浏览器验证] 管理员从“我发起的任务”列表点击关联案号 `SHMS2600388`，进入真实“民事案件详情”抽屉；详情中客户“上海天路人造草坪有限公司”和合同号 `HT2026060097` 均为可点击的关联控件，文档、费用、提醒、日志、案件任务等详情页签同时正常加载。该操作只读取既有数据，未写入原系统或本地业务数据。
- [仍未证明] 本轮只覆盖管理员和既有数据的关联跳转；任务/调查/客户新修复入口及案件经办律师、律师助理、无关人员的浏览器多角色点击证据仍需后续页面创建临时样本后补齐，并精确清理。

## 2026-07-28 任务批量撤回页面闭环（本地）

- [已通过浏览器验证] 管理员仅从“我发起的任务”页面创建两条唯一临时任务 `RW20260728145507378623`、`RW20260728145514861770`；两条均真实回显“待接收”。同时选中两条后，页面才显示“批量任务流转”，菜单按当前页面语义仅提供“批量撤回任务”。
- [已通过浏览器验证] 批量撤回弹窗明确提示“任一任务不符合条件时整批不会提交”；空撤回原因点击“确认执行”显示“请填写撤回原因”且两条状态未改变。填写原因后，页面提示“批量撤回任务成功：2 条”，进行中计数从 38 回到 36、已撤回计数从 0 变为 2，证明调用真实专用批量生命周期而非前端假提示。
- [已通过精确清理验证] 仅按上述两个唯一编号回查并使用本地非生产受控测试清理入口删除；浏览器按其中一个编号重新查询显示“没有查询到符合条件的记录”，接口回查精确记录为 0，未触及既有任务。

## 2026-07-28 案件团队稳定身份与分层详情权限（本地）

- [已完成代码] 案件新建、人员分配、法律顾问基本信息与案件批量人员变更统一校验经办律师、律师助理、开庭律师和客户管理人为有效账号；兼容页面显示名称/用户名输入，并同步保存 `handling_lawyer_usernames`、`assistant_username` 与 `case_team_usernames`，避免用重名或可变展示名做授权判断。
- [已完成代码] 普通数据范围之外，受派经办律师和律师助理仅能看到自己位于 `case_team_usernames` 的案件；管理员、案件负责人和同部门负责人既有全量/负责人权限未降低。无关用户仍不能读取该案件。
- [已完成代码] 案件详情 capability 逐项返回并由前端真实展示：经办律师可办理附件、提醒、日志、案件进展和开庭排期；律师助理仅可办理附件、提醒和日志；人员分配、基本信息、办结归档、案件费用仍只对负责人/同部门负责人/管理员开放。附件删除端点同步修复为使用同一案件详情门禁，避免 capability 与实际接口不一致。
- [已验证] API 冒烟实际创建独立三角色数据并覆盖稳定用户名落库、经办律师可见/上传删除附件/登记进展/新增排期、助理可见/新增提醒和日志且进展与排期返回 403、无关用户读取返回 404、无效分配人员返回 422；`SMOKE_API_OK: 19 groups passed` 和 `SMOKE_CLEANUP_OK` 均通过，临时人员、案件、提醒、附件均无残留。
- [仍未证明] 仍需用浏览器分别登录经办律师、律师助理和无关人员，核对案件详情中的权限提示、按钮可见性与真实点击结果；该浏览器证据完成前不将多角色页面验收标为完成。

## 2026-07-28 调查子任务创建、接收与完成页面闭环（本地）

- [已通过浏览器验证] 管理员从“我发布的调查任务”页面创建唯一临时父调查 `DC20260728064208`，再在“待我分配的调查任务”中选择该父调查，页面先创建首个任务 `RW20260728144519250817`，并通过父任务下拉框创建关联子任务 `RW20260728144542782664`；抽屉表格真实回显父子关系、负责人、截止日期和“待接收”状态。
- [已通过浏览器验证] 刷新“我发布的调查子任务”后，两条任务均从真实列表回显。对子任务执行“接收任务”后状态变为“处理中”、办理入口变为“提交完成”；在完成弹窗空提交明确显示“请填写办理结果说明”，填写结果后状态变为“已完成”。首个任务也按同一真实状态机接收并完成，未通过通用记录接口绕过。
- [已通过精确清理验证] 页面验收结束后，仅对上述两个唯一任务和唯一父调查使用本地非生产、管理员受限的测试清理入口；按子任务编号重新页面查询显示“没有查询到符合条件的记录”，接口回查任务及父调查均为 0 条，未触及任何既有记录。

## 2026-07-28 任务撤回页面闭环验收（本地）

- [已通过浏览器验证] 以管理员身份在“我发起的任务”页面新建唯一临时任务 `RW20260728143851670464`，页面立即回显为“待接收”；勾选该行后显示“撤回任务”。
- [已通过浏览器验证] 撤回弹窗空原因点击“确认撤回”明确提示“请填写撤回原因”且不改变状态；填写原因后，页面提示“任务已撤回”，进行中数量从 37 回到 36，“进行中-已撤回”计数由 0 变为 1。
- [已通过精确清理验证] 仅以该唯一任务编号回查非生产受控清理接口并删除，随后再次查询为 0 条；未触及任何既有任务。该轮数据由页面创建，未通过数据库创建。

## 2026-07-28 案件详情写入门禁一致性（本地）

- [已完成代码] 案件附件上传/删除、案件提醒新增/删除、案件日志新增及智能文档回写案件附件，统一改由 `_require_case_detail_write_access` 校验：必须完成并通过案件新建审批，且仅案件负责人、同部门负责人或 `admin` 可办理；“待归档审核/已归档”一律阻断写入。
- [已完成代码] 新增只读 `GET /cases/{case_id}/action-capabilities`；案件详情加载真实 capability 后，按项隐藏上传、删除、提醒新增/删除和日志新增入口，不再以页面角色猜测或仅禁用按钮代替后端权限。
- [已验证] 冒烟新增可写案件 capability 为真、归档审核中 capability 全为假，以及归档中通过日志与通用附件接口写入均返回 409 的断言；仍保留原有附件格式、大小、归档材料同步和工作流审计。
- [仍未证明] 尚需在浏览器以负责人、同部门负责人、无权可见人员和 `admin` 分别打开同一案件详情，核对 capability 对应按钮可见性和实际提交结果；页面临时数据必须从页面创建并精确清理后才可标为完整验收。

## 2026-07-28 任务批量生命周期闭环（本地）

- [已完成代码] 新增专用 `POST /api/v1/tasks/batch-lifecycle`，支持批量接收、提交完成、交接和撤回；不复用通用记录流转或任务字段批量修改。每条任务分别核验负责人/发起人或管理员权限、状态机和接收人有效性，任何一条不符合时整批不落库。
- [已完成代码] 批量交接沿用单任务的“五日内未重新开始自动完成”规则；批量完成沿用“五日内发起人确认”规则；批量撤回强制原因并清除自动时点和待审特殊处理。每条任务均生成独立工作流事件和参与人通知，结果真实刷新列表。
- [已完成代码] 任务中心多选两条以上任务后，按“我接收/我发起/公司发起”页面语义显示“批量任务流转”，交接与撤回分别强制接收人、撤回原因；页面提示整批原子规则，不以局部成功伪造完成。
- [已验证] API 容器已重建并健康；`verify-local.ps1` 全量通过（前端生产构建、Python 编译、菜单/客户端 API 覆盖、18 组 API 业务闭环、零残留清理和 Compose 配置）。冒烟实际覆盖批量接收→交接→接收→完成、重复接收阻断、非发起人批量撤回阻断、空撤回原因阻断及批量撤回事件。
- [已通过浏览器验证] 已通过两条页面创建任务完成多选门槛、批量撤回原因必填、专用批量动作、状态计数刷新和精确清理；更复杂的批量接收、交接、完成仍按角色矩阵继续页面验收。

## 2026-07-28 调查子任务页面办理闭环（本地）

- [已完成代码] 调查子任务创建仍通过专用调查接口真实落库；“我发布的调查子任务 / 我的调查子任务”重载后均从 `task` 记录读取带 `investigation_record_id` 的数据，不依赖创建抽屉的临时状态。
- [已完成代码] 子任务列表新增真实状态和办理列：负责人（或 `admin`）仅在“待接收/待处理”时可打开“接收任务”，仅在“处理中”时可打开“提交完成”；分别调用既有专用 `/tasks/{id}/accept`、`/tasks/{id}/complete`，完成时强制填写办理结果说明，成功后重新请求列表。非负责人只明确显示无办理权限，不显示可操作的假按钮。
- [已验证（静态）] `audit-menu-coverage.py` 增加 `INVESTIGATION_SUBTASK_LIFECYCLE_OK` 静态回归断言；API 冒烟已新增“创建后重新 `GET /records` 回查父调查关联字段”断言，并保留创建调查任务 → 接收 → 提交完成 → 验收 → 关闭调查链路，待下一次带测试凭据的完整验收执行后标记为运行验证。
- [已验证] 已通过上述页面创建、刷新回显、接收、结果必填拦截、提交完成和精确清理，证明管理员本人可完成该子任务最小闭环。
- [仍未证明] 普通调查员与发起人分离后的子任务分配、接收、完成、验收/关闭，以及与线索/取证/公证的后续跨角色联动，仍需继续页面验收。

## 2026-07-28 任务发起人/管理员专用撤回闭环（本地）

- [已完成] 新增专用 `POST /api/v1/tasks/{id}/withdraw`：仅任务发起人或 `admin` 可执行；仅“待接收”（兼容历史“待处理”）和“处理中”可撤回，必须填写原因。接口将状态置为“已撤回”，清除交接/完成自动时点与待审特殊处理，写入撤回人、时间、原因、工作流事件“撤回任务”及参与人通知。
- [已完成] 任务通用 `POST /records/{id}/transition` 原本已拒绝 task 模块；本轮冒烟进一步断言任务撤回后无法借通用流转恢复为“处理中”，重复撤回返回 409，负责人但非发起人返回 403。
- [已完成] “我发起的任务/公司发起任务”列表底部与任务详情抽屉均按当前会话、发起人/admin 与实时状态显示“撤回任务”；确认弹窗强制填写原因，调用真实专用接口，成功后刷新列表并关闭已撤回任务详情。发起人“进行中-已撤回”页签可消费状态结果。
- [已验证] API 容器已重建并健康；`verify-local.ps1` 全量通过：前端生产构建、Python 编译、菜单/客户端 API 覆盖、18 组 API 业务闭环、零残留清理和 Compose 配置均通过。冒烟已实际覆盖普通发起人撤回待接收任务、管理员撤回处理中任务及越权/空原因/重复/通用接口绕过阻断。
- [已通过浏览器验证] 已从“我发起的任务”页面创建唯一临时任务，验证撤回原因必填、成功提示、“进行中-已撤回”页签计数变化，并在验收后精确清理；详细编号和证据见本文件首节。

## 2026-07-28 调查审核按钮与服务端真实权限对齐（本地）

- [已完成] 调查中心不再以 `admin/manager/auditor` 等展示角色猜测线索审核权限。新增只读 `GET /api/v1/investigations/action-capabilities`，按当前登录人、记录可见范围、岗位权限、记录负责人/部门范围及关联客户管理人返回逐记录操作能力；不返回或暴露岗位配置细节。
- [已完成] 线索列表/审核工作台的“内部审批”“客户审核”，以及公证列表的“审核”“登记公证书”均据该能力结果显示。批量工具栏同样先检查所选记录能力，避免隐藏行内入口但仍能打开审核弹窗。
- [已验证] 前端生产构建、Python 编译和菜单覆盖审计通过；本地 API 容器已重建并健康，OpenAPI 已包含上述接口，未认证访问返回 401。仍需在多岗位浏览器联动验收中分别证明：线索审批岗位、对应客户管理人、公证审核岗位和公证书号码登记岗位的按钮可见性与实际提交结果一致。

## 2026-07-28 案件详情文档最小闭环（本地）

- [已完成] 案件详情抽屉“文档信息”已补齐真实附件入口：可从文档目录选择分类后上传文件，调用既有 `POST /attachments` 并刷新同一案件详情；文件类型仍由服务端白名单和大小限制校验，记录可见性与数据范围继续由既有接口强制校验。
- [已完成] 文档表新增单文件“查看 / 下载”：PDF、图片通过已认证的真实附件响应在新窗口预览；其他文件格式会明确提示并转为真实下载。批量 ZIP 下载、选中删除及归档状态下的上传/删除阻断保留，文档表补齐横向滚动和操作列，避免长文件名遮挡操作。
- [已验证] `npm.cmd --prefix apps\\admin-web run build` 通过（TypeScript 与 Vite 生产构建）；尚未以浏览器创建临时案件和临时附件完成点击回归，故上传、预览、下载和精确清理仍标记为待浏览器证明。
- [待确认] 旧系统“转文 / 解锁”等文档动作在现有业务证据中未能确认其状态前提、权限和后续流转；本轮未以空弹窗或假按钮替代，待合伙人确认语义后另立专用流程验收。

## 2026-07-28 调查线索客户审核链路、专用创建阻断与宽表整改（本地）

- [已完成] 调查线索的客户审核规则已从父调查传递到子调查任务，再由服务端从来源任务强制回填给线索；客户端不能伪造或关闭该标记。内部审批通过后，需客户审核的线索进入“待客户审核”，客户审核确认通过后才进入“待取证”，驳回则回到“已驳回”。待审批、待客户审核及后续状态均禁止直接修改资料；驳回后仍可按专用修改入口补正并重新提交。
- [已完成] 通用 `POST /records` 已拒绝调查、线索、公证、证据四类模块；调查中心专用创建也拒绝将公证/证据当作泛化记录创建。线索创建必须关联真实调查任务，服务端回填来源任务、负责人、客户、部门和客户审核规则，避免绕开任务来源。
- [已完成] 调查线索行内操作补齐“内部审批”和“客户审核”入口，并按当前角色限制审核操作；线索宽表根据完整业务列和固定操作列启用 2920px 横向滚动，普通调查/公证列表使用 1900px，导入操作区支持换行。调查任务抽屉改为 760px、任务名称提供完整悬浮提示及 840px 容器内横向滚动；材料目录补齐长文件名悬浮提示、操作列宽和容器滚动，避免关键关联列、操作列或文字被遮挡。
- [已验证] `verify-local.ps1` 完整通过：前端生产构建、Python 编译、菜单/客户端 API 覆盖、18 组 API 业务闭环、`SMOKE_CLEANUP_OK` 和 Compose 配置均通过。冒烟新增断言覆盖通用创建线索被拒绝、父调查→任务→线索的客户审核标记继承、内部审批→待客户审核→客户确认→待取证以及待客户审核禁止直接修改。
- [已完成清理] 此前浏览器页面验收遗留的临时账号 `smoke_ui_investigator` 已通过“员工管理”页面精确删除；页面回显账号总数由 3 恢复至 2。未删除既有账号或业务数据。
- [已验证并清理] 管理员从页面创建带客户审核标记的父调查 `DC20260728052412`，创建并分配首个调查任务 `RW20260728132515395715` 给临时普通调查员；该调查员通过“新增线索”创建并提交线索。管理员在待审批页完成内部审批，页面将线索转入待客户审核；在客户审核页打开确认弹窗并验证通过/驳回文案与目标状态，随后以驳回分支提交，使线索进入已驳回。线索、任务、父调查均由各自页面的确认删除流程清理，临时账号 `smoke_ui_flow_0728` 也由“员工管理”删除，账号数恢复为 2，未删除任何既有数据。
- [已验证并清理] 为补足正向终态，管理员再次仅从页面创建父调查 `DC20260728053544`、首个任务 `RW20260728133558372124` 与线索 `XS20260728053612`，依次完成提交、内部审批通过、客户审核确认通过。线索真实出现在“待取证线索”页面，证明客户确认不是假提示或仅弹窗文案。因待取证线索不允许走普通页面删除，完成页面证据后仅在本地非生产环境对上述精确 `SMOKE-UI` 链路使用受管理员限制的测试清理接口；父调查、任务、线索复查均为 0，未触及既有数据。
- [已完成] 调查专员历史岗位的“调查取证”权限现在明确涵盖其不可拆分的专用“线索提交、扫描上传”动作；线索内部审批、公证审核、公证书号登记、调查材料上传和调查员分配均已改为服务端岗位/负责人校验，不能因页面可见或填写任意用户名绕过。
- [仍未证明] 当前“客户审核”由具审核权限的内部人员代录确认，尚未实现外部客户门户自行确认；是否需要外部客户登录、签署或消息回执，应由合伙人确认后另立客户门户验收项。客户确认通过进入待取证的页面正向终态已补证，但外部客户门户身份、签署和消息回执仍未证明。

## 2026-07-28 调查任务页面创建、关联与删除阻断验收（本地）

- 已验证：管理员通过“我发布的调查任务”页面新建临时调查 `DC20260728044143`，详情真实回显待分配状态、权利人、权利类型、负责人、授权日期和调查区域；随后在“待我分配的调查任务”中按调查编号查询并进入“新增子任务”，页面创建首个调查任务 `RW20260728124432836701`，抽屉表格真实回显任务名称、负责人、截止日期和待接收状态。
- 已验证：从“我的调查任务”按任务编号查询可回显上述子任务；从任务编号、权利人到对应只读详情的点击均可达。首次直接进入相关列表未回显新记录时，使用页面查询条件可正常回显，后续仍需作为列表初始化/刷新专项继续覆盖，不能据此视为无问题。
- 已验证并清理：尝试先删除父调查时，系统明确拒绝并提示“调查任务存在子任务，不能删除”；随后经“我发布的调查子任务”页面批量删除临时子任务，再回“我发布的调查任务”删除临时父调查，两个编号复查均为 0 条。未删除任何既有数据。
- 仍未证明：本轮只覆盖管理员本人创建、本人接收的首个调查任务；跨普通调查员、线索创建/驳回/重提、客户审核、取证、公证、费用、转案、关闭生成报告仍按角色矩阵继续页面验收。

## 2026-07-28 页面布局共性审查整改（本地）

- [已完成] 二次静态审查发现案件原案件列表、法律顾问列表和归档审核列表的横向滚动宽度小于显式列宽（含选择列），会挤压或遮挡固定右侧操作列；现分别按 2420、1460、1600/1700px 的完整列宽设置。调查中心不再为所有分支使用 1900/2920px 固定宽度，而是汇总当前分支真实列宽并仅在可选中行时计入选择列，消除了父/子任务、证据、公证等页面的大段空白横向滚动。
- [已验证] 该二次整改后执行完整 `verify-local.ps1` 通过：前端生产构建、Python 编译、菜单/客户端 API 覆盖、18 组 API 流程、测试数据清理和 Compose 配置均通过。
- 已完成：并行审查任务、财务、案件和合同中心的表格、操作区、详情抽屉及窄屏布局。任务、案件、合同列表的底部操作区不再用负边距覆盖分页或横向滚动条；合同列表仅保留底部真实汇总行，移除了重复的伪数据汇总行；案件宽表横向滚动宽度与右侧操作列匹配；案件详情在较窄屏幕下会改为两列或单列，内容不会被截断。
- 已完成：任务查询区、财务费用查询和未开票查询在 1080px 以下改为两列、640px 以下改为单列；财务审批抽屉表格改为容器内横向滚动，避免固定宽度溢出。合同新建表单与操作区恢复正常文档流，避免覆盖上一段表单。
- 已验证：前端生产构建通过（5479 modules）。
- 仍未证明：本项为代码和生产构建验证；需在本地浏览器以真实业务数据分别抽检任务、财务、案件详情和合同新建页的宽屏/窄屏点击、横向滚动与操作区可达性，不能据此宣称全站视觉验收完成。

## 2026-07-28 调查线索驳回后修改重提入口修复（本地）

- [已完成] 浏览器跨角色验收中发现：已驳回线索的“修改”弹窗错误调用通用 `PATCH /records/{id}`，该接口会按专用业务保护规则拒绝请求，导致页面虽显示修改入口但无法保存。现已统一改为调查中心专用 `PATCH /investigations/records/{id}`；状态仍不能通过资料修改改变，负责人字段改为禁用显示，并明确提示必须使用“分配调查员”入口变更，避免页面再给出无效操作。
- [已验证] 自动冒烟新增完整证据：通用修改线索返回阻断；草稿线索通过专用入口可补充资料；提交后被驳回；驳回状态下再次通过专用入口修改调查区域与标题；重新提交并经审批后进入“待取证”。本地 `verify-local.ps1` 已通过前端构建、Python 编译、菜单审计、18 组 API 闭环、精确清理和 Compose 校验。
- [已完成清理] 跨角色页面验收临时审批账号已从“员工管理”页面精确删除；完整验收复核 `SMOKE_CLEANUP_OK`，未残留自动测试数据。
- [仍未证明] 新专用修改入口已由端到端 API 覆盖；受本轮完整清理后页面样本被清除，后续应继续以页面创建的父调查、子任务和线索补录“驳回 → 修改 → 重提 → 待审列表可见”的单次浏览器闭环证据，并同步检查表格横向滚动和操作区不遮挡。

## 2026-07-28 调查子任务列表回显与路由状态修复（本地）

- 根因与修复：调查中心切换子任务路由时复用了旧路由查询条件和选中行，父调查的隐藏“授权日期”等条件会把子任务误过滤。现切换路由即清空查询/选择状态，查询表单按路由重新挂载；管理员对子任务不再使用普通用户负责人过滤。
- 已验证：本地 Web 重建后，管理员在“我的调查任务”真实回显 36 条子任务，编号、权利人、调查员列可见；审计新增 `INVESTIGATION_ROUTE_STATE_RESET_OK` 防回归。
- 仍未证明：基于新子任务完整执行“创建线索 → 驳回 → 修改 → 重提”的跨角色页面闭环，后续继续按页面创建和精确清理执行。

## 2026-07-28 任务公司范围重派与挂起恢复权限对齐（本地）

- [已验证并清理] 管理员从页面创建临时任务 `RW20260728134122700630`，普通负责人在“我接受的任务”中实际执行“拒绝任务”；管理员在“我发起的任务 → 进行中-拒绝”选中该记录后，真实打开“重新派发任务”弹窗并派回负责人，负责人重新登录后在页面点击“接受任务”，状态实际从“待接收”变为“处理中”。
- [已验证并清理] 同一页面创建任务继续由负责人提交“申请挂起”，管理员在发起任务页真实点击“通过特殊处理”，任务进入“已停止”；负责人在“我接受的任务 → 停止”中看到并点击“恢复挂起任务”，状态实际恢复为“处理中”。两条链的提示、状态页签、权限入口和后续列表回显均已取得浏览器证据。
- [已完成清理] 临时普通账号 `taskflow_ui_0728` 已由“员工管理”页面删除并回显账号数恢复为 2。任务完成高级生命周期后没有常规页面删除入口，因此页面证据完成后，只在本地非生产环境对精确编号 `RW20260728134122700630` 调用管理员受限测试清理接口，复查任务为 0；清理接口仅额外接受精确前缀 `UI任务流转验收-`，不放宽为泛化“验收”记录，未触及既有数据。
- 已修复待页面证明：后端已允许管理员对“公司发起的任务”中被拒绝的记录重新派发，但前端此前只在个人发起页展示入口；现管理员公司范围同样显示“重新派发”。
- 已修复待页面证明：“恢复挂起任务”入口现仅向实际任务负责人所在的接受视图或管理员公司发起视图展示，避免无权限角色看到操作后才收到接口拒绝。
- 已通过：前端生产构建、Python 编译和菜单静态审计；仍需用页面选取真实拒绝/挂起样本证明按钮出现条件、弹窗校验和状态同步。

## 2026-07-28 跨角色线索驳回与管理员范围修复（本地）

- 已验证：管理员从页面创建调查、调查任务和线索并提交；临时审计人员在“待审批线索”真实选择“驳回修改”、填写原因并提交，页面提示“线索已驳回”。管理员公司驳回列表回显该线索，服务端状态同时记录审核人、审核时间及驳回原因。
- 已修复并验证：管理员在“我的已驳回线索”原先被普通用户的个人负责人过滤误排除。管理员是全所最高权限，现该过滤仅作用于非管理员；本地重建后管理员个人驳回页真实回显同一线索。菜单审计新增管理员调查范围断言。
- 已完成清理：临时审计人员经系统账号管理页面删除；临时线索、任务和父调查均按精确 ID 清理，记录和账号复核为 0。
- 仍未证明：驳回后修改线索并再次提交、客户审核分支以及取证、公证、转案的完整后续链路仍需继续页面验收。

## 2026-07-28 跨角色客户公海领取（本地）

- 已验证：管理员页面创建临时客户后依次执行“客户删除 → 进入公海”；普通用户在“公海客户”真实看到该客户，选择“拾回”后系统提示领取成功，客户在其“我的客户”中回显，客户负责人和客户管理人均变为该普通用户。
- 已验证：领取后该客户不再出现在公海页，证明状态、归属和数据范围随同一次专用生命周期操作更新，而非仅前端提示。
- 已完成清理：临时账号经系统账号管理页面删除；临时客户按精确 ID 通过受保护本地测试清理接口删除，复核客户和账号残留均为 0。
- 仍未证明：公海客户的领取冲突、关联合同/案件后的释放阻断，以及部门负责人对公海/回收站的边界仍需页面验收。

## 2026-07-28 跨角色客户共享、回收与恢复（本地）

- 已修复：客户共享接口和共享弹窗此前已有实现，但“我的客户 → 更多操作”遗漏“共享客户”入口，导致页面无法触发真实共享。现已补齐入口，并由菜单审计断言持续覆盖。
- 已验证：管理员页面创建临时客户后，从“我的客户”选择“共享客户”并指定临时普通用户；该普通用户登录“我的共享客户”真实看到相同客户编号和名称。管理员将客户移入回收站后，接收人的共享列表不再显示该客户；管理员在个人回收站执行“客户恢复”后页面提示成功。
- 已完成清理：临时账号由系统账号管理页面删除；临时客户在恢复验证后仅按精确记录 ID 使用受保护本地测试清理接口删除，复核客户与账号残留均为 0。
- 仍未证明：共享客户的附件、联系人、合同/案件关联字段权限，以及“进入公海—领取”的跨角色写操作仍需逐页验收。

## 2026-07-28 跨角色调查线索审批与子任务父级关联（本地）

- 已验证：管理员页面创建并提交临时调查线索，审批人员在待审批页实际执行“审批通过”，系统将线索真实流转为“待取证”；审批人员直达系统账号管理被明确阻断。临时线索、调查、任务和账号均已精确清理。
- 已修复并验证：调查页“新增子任务”不再允许在已有父任务时生成空父级任务；首次创建首个调查任务后，后续子任务表单自动预填同一调查下的父任务，并通过必填校验保证关联落库。`audit-menu-coverage.py` 新增静态审计，防止该规则回退。
- 仍未证明：审批驳回、客户审核、取证、公证、转案以及不同部门/客户范围的全分支仍待页面验收。

## 2026-07-28 部门负责人客户分配与全所阻断（本地）

- 已验证：同一临时部门的普通用户从页面创建 `SMOKE-UI 部门客户写操作`；部门负责人登录“部门客户”后仅看到该部门记录，勾选后通过“更多操作 → 分配客户”选择本部门负责人并确认。列表真实回显客户管理人与案源人均变更为部门负责人，证明部门范围内写操作实际落库。
- 已验证：部门负责人直达“公司客户”页面时，列表不泄露全所记录，并明确提示“只有系统管理员可以查看公司客户”。
- 已完成清理：临时客户按精确记录 ID 由受保护本地测试接口删除；部门负责人和普通用户均由系统用户管理页面删除确认。未影响既有客户、账号或服务器数据。
- 仍未证明：本项只验证部门负责人对同部门客户的分配操作；客户分级/关键字段审批、回收恢复、共享、联系人与客户文档的跨角色写操作仍需逐项页面验收。

## 2026-07-28 普通用户客户创建与全所范围阻断（本地）

- 已验证：临时普通用户从“新建客户”页面真实创建 `SMOKE-UI 跨角色客户`，系统生成客户编号并在“我的客户”列表回显；管理员随后在“公司客户”看到同一记录，点击客户编号进入字段禁用的只读详情，回显客户管理人与来源，证明页面创建、全所可见和关联详情使用同一真实记录。
- 已修复并验证：普通用户直达“公司客户”时服务端已返回权限拒绝，但页面原先只显示笼统“客户数据加载失败”。现改为展示接口明确原因“只有系统管理员可以查看公司客户”；浏览器重建后以临时普通用户复测通过，未把权限阻断伪装成空数据或网络故障。
- 已完成清理：临时客户按精确记录 ID 由受保护本地测试清理接口删除；临时普通用户两次创建均经“系统用户管理 → 删除 → 确定”页面清理。未改动既有客户或服务器数据。
- 仍未证明：本项覆盖普通用户创建本人客户、管理员只读回看与全所范围阻断；共享、部门分配、回收/恢复、联系人和客户文档的跨角色写操作仍须逐页补测。

## 2026-07-28 跨角色任务交接与重新开始（本地）

- 已验证：管理员在“我发起的任务”页面创建带 `SMOKE-UI` 标识的临时任务，先在“我接受的任务”页面接受并进入“处理中”，再选择“转交任务”、指定临时普通用户。页面明确提示“转交后 5 天内接收人未开始，系统按交接规则自动完成”，提交后真实返回“任务已交接，5 日内未重新开始将自动完成”。
- 已验证：临时普通用户登录后，在“我接受的任务 → 待处理”仅看到这条本人接收的任务；选择后点击“接受任务”，系统按交接状态执行真实“重新开始”接口并提示“任务已重新开始”。切换到“进行中”后，该任务状态为“处理中”，负责人仍为该临时用户，发起人为管理员，证明交接后的重新开始没有退化成假提示或跨账号可见性泄露。
- 已完成清理：临时任务仅通过受保护的本地测试清理接口按精确记录 ID 删除；临时普通用户通过“系统用户管理 → 删除 → 确定”页面删除。未修改既有任务、客户、合同、案件或服务器数据。
- 边界说明：本轮只证明“页面创建 → 管理员交接 → 接收人重新开始”的跨角色闭环；五日自动完成与 30 日期限的补充页面证据见下一条。任务的拒绝、重新派发、挂起/取消和不同数据范围仍需按角色矩阵继续验收。

## 2026-07-28 任务期限阻断与五日自动完成（本地）

- 已验证：管理员打开“新增任务”日期控件，尝试选择 2026-08-28（相对 2026-07-28 超过 30 天）后日期面板保持打开、截止日期仍为默认允许的 2026-08-04；对照点击允许日期 2026-08-04 时面板关闭并完成选择。前端日期控件没有把超期日期提交到创建流程；服务端同一规则已由 API 冒烟覆盖为拒绝。
- 已验证：管理员页面创建 `SMOKE-UI 交接五日自动完成任务`，受理后从页面转交给临时普通用户。仅将该已页面创建任务的本地测试计时推进到过去日期，随后由“我发起的任务”页面真实查询触发系统自动处理。列表回显“已完成”，详情沟通记录新增系统事件“交接任务自动完成”，完整回显“待接收 → 已完成”及“交接满 5 天且未重新开始，系统自动完成”。
- 已完成清理：临时任务按精确记录 ID 使用受保护的本地测试清理接口删除，临时接收账号经“系统用户管理 → 删除 → 确定”页面删除；只读复核任务与账号均为 0，未修改既有业务数据。
- 仍未证明：本项只覆盖两条任务硬规则的页面正向证据；日期控件的可访问性树未暴露 disabled 属性，证据以“超期点击不生效、允许日期点击生效”的真实控件对照为准。任务的拒绝、重新派发、挂起/取消等其他状态组合仍应按任务台账继续验收。

## 2026-07-28 跨账号工作区标签隔离（本地）

- 已修复：退出、登录过期和下一次成功登录时，工作区会清除上一会话的本地标签、深层路由上下文和展开菜单，统一回到“控制台”；不同角色不再继承上一位用户的业务页面名称或路由标签。
- 已验证：完整 `verify-local.ps1` 通过（5479 个前端模块、Python 编译、菜单/页面审计、18 组 API 业务流和零测试残留）；Web 容器重新构建后健康。浏览器实际以管理员打开系统账号管理和公司客户，再退出、以临时普通用户登录，工作区仅剩“控制台”，没有管理员标签；临时账号已由页面删除。
- 已补门禁：菜单覆盖审计新增“工作区会话隔离”静态断言，后续若移除退出、过期或下一次登录的标签清理逻辑，本地验收将失败。
- 仍未证明：本项只解决工作区标签及路由上下文的跨账号残留；每个业务模块的服务端数据范围、字段权限和操作权限仍按角色矩阵逐项验收。

## 2026-07-28 普通用户客户数据范围（本地）

- 已验证：管理员通过“系统用户管理”页面创建临时普通用户；该用户登录后保留客户管理业务菜单，但不显示系统中心入口。
- 已验证：临时普通用户直达“公司客户”页面时，查询结果为“没有查询到符合条件的记录”，未泄露管理员和既有客户；直达“系统用户管理”路由时页面明确返回“无权访问”。
- 已完成清理：临时普通用户已通过系统用户管理页面“删除 → 确定”清理，列表复核仅保留既有账号；本轮未创建或修改客户、合同、案件及其他既有业务数据。
- 仍未证明：本条仅验证普通用户对未授权公司客户的空数据范围和系统管理阻断；本人创建客户、共享客户、部门客户、客户详情字段级权限，以及关联合同/案件的范围仍需独立页面闭环。

## 2026-07-28 审批人员角色访问与数据范围（本地）

- 已验证：管理员通过“系统用户管理”页面创建临时审计人员账号；该账号登录后，侧栏只显示其授权菜单，能够进入“合同审批 → 待审批合同”，且没有系统中心入口。
- 已验证：临时审计人员直达“系统用户管理”路由时，页面明确返回“无权访问”，未显示账号、角色或菜单配置内容；待审批合同页面实际加载，但因没有被指定为审批人而显示空列表，未泄露其他合同。
- 已完成清理：临时账号通过系统用户管理页面删除；临时客户在本地非生产环境中按精确记录 ID 通过受保护测试清理接口删除。只读复核临时账号和标识客户均为 0，未触及既有业务数据。
- 仍未证明：本轮浏览器控制接口无法为必传的本机文件控件写入附件，因此没有伪造合同提交或审批结果。完整的“页面上传附件 → 指定具备合同审批岗位权限的人员 → 审批通过/驳回 → 状态和审批痕迹同步”已在前述跨角色合同链路中有独立页面证据；审批人员自身的岗位权限配置与该完整链路仍需在可上传附件的页面会话中复验。

## 2026-07-28 部门负责人任务数据范围（本地）

- 已验证：管理员通过“系统用户管理”页面创建临时部门负责人、部门 A 普通用户和部门 B 普通用户；再通过“我发起的任务”页面创建两条真实任务，分别指派给部门 A、部门 B 用户。任务均真实进入“待接收”，并有各自任务编号。
- 已验证：以临时部门负责人登录“部门接受的任务”时，页面只显示部门 A 的测试任务及其负责人；部门 B 的测试任务没有泄露。直达“公司接受的任务”路由时，系统返回“只有系统管理员可以查看全所任务”，列表为空，说明全所范围未被部门角色绕过。
- 已完成清理：两条页面创建的临时任务因无专用删除入口，仅在本地非生产环境通过受保护测试清理接口按精确记录 ID 清理；三个临时账号均由“系统用户管理”页面逐一删除确认。API 复核临时任务和临时账号均为 0，Docker Compose 的 API、Web、PostgreSQL、Redis、MinIO、Worker 均保持运行健康。
- 仍未证明：本轮只证明任务的部门数据范围和全所范围阻断；部门负责人对部门任务的批量改派、验收、转交后重启，以及 5 天未开始自动完成和超过 30 天拒绝等规则，仍需以独立页面闭环补测。

## 2026-07-28 跨角色调查任务流转（本地）

- 已验证：管理员在页面创建临时调查任务，并在该调查任务内新建并分派一条办理任务给临时普通用户。普通用户登录后，调查任务页和事务中心的“我接受的任务”均只显示该本人任务，状态依次从“待接收”变为“处理中”。
- 已验证：普通用户直达“系统用户管理”路由时显示“无权访问”，系统管理菜单也不在其侧栏中；该用户可通过页面“接受任务 → 完成任务”提交完成，管理员在“我发起的任务”中看到“进行中-已完成”，再执行“确认完成”后真实转入“已验收”。
- 已完成清理：临时普通用户由系统用户管理页面删除；临时调查和任务因完成后无专用页面删除入口，仅在本地非生产环境用受保护测试清理接口按“任务 → 调查”顺序精确清理。数据库复核记录、工作流事件、附件、通知和临时账号均为 0。
- 仍未证明：本次只覆盖调查任务的分派、本人可见、系统管理阻断、接收、完成和发起人验收；转交后重新开始、交接满 5 天自动完成、超过 30 天拒绝，以及部门负责人和审计人员在任务链路中的数据范围仍需单独补测。

## 2026-07-28 智能文档关联公证页面闭环（本地）

- 已验证：通过页面把一条临时调查线索完成取证登记并建立公证记录；在“AI 智能文档”选择“民事起诉状”模板、关联该公证记录后，成功创建可编辑的智能文档任务。未配置 Dify 时，系统真实返回并展示“待配置”状态，而非伪造已生成结果。
- 已验证：智能文档列表中的关联公证编号是可点击对象；点击后实际进入“公证管理”并打开对应的只读“调查详情”，可查看编号、状态、事项、权利人、调查员及来源说明。
- 已验证：智能文档任务已在页面中执行删除并收到成功反馈；临时公证、线索、子任务和调查主记录随后使用受保护的本地测试清理接口精确清理。数据库复核上述四条业务记录、智能文档和附件均为 0。
- 仍未证明：当前环境尚未配置 Dify，故“实际模型生成—人工编辑确认—回写业务附件”链路仍未证明；需在 Dify 配置完成后，以同样的页面操作、关联跳转和精确清理标准补测。

更新时间：2026-07-21

## 2026-07-28 跨角色合同联动测试（本地）

- 已验证：通过页面创建临时普通用户、部门负责人和审计人员账号；普通用户在页面创建 `SMOKE-UI` 客户与合同、上传附件并提交合同审批。合同审批人来自人事页面创建的员工档案，岗位“合伙人律师”具有“合同审批”权限；仅配置系统角色而未配置岗位权限的账号不会出现在审批人候选项中。
- 已验证：部门负责人访问“待审批合同”时不显示该笔指定给他人的合同；指定审批人在待审批列表中看到该合同并通过当前节点。随后部门合同和管理员的公司合同页面均显示同一合同，状态同步为“已通过”。
- 已验证：本轮测试合同、客户、员工档案、附件及四个临时账号均已精确清理；数据库复核测试记录和临时账号均为 0，未删除既有业务数据。
- 仍未证明：本条仅覆盖合同“创建—指定审批—授权阻断—部门/全所可见—状态同步”的一条跨角色链路；案件、任务、用印、财务、人事、调查和平台角色的跨角色闭环仍需分别按相同标准执行。

## 2026-07-21 客户编号与客户名称列重叠修复

- 已完成：根据用户截图确认客户编号链接在窄可视区域突破第一列并覆盖客户名称。客户表已显式启用固定列布局；客户编号由 Ant Design 链接按钮改为受单元格宽度约束的原生按钮，按钮和内部文本双层限制宽度、裁切并显示省略号；所有客户表单元格统一 `box-sizing`、最小宽度和强制隐藏溢出，表头与数据继续采用相同居中规则。
- 已验证：前端生产构建通过（5473 modules），菜单与页面结构审计为 `264 nodes / 219 leaves / 0 unhandled`；完整 `verify-local.ps1` 通过，364 个前端 API 调用、18 组端到端业务冒烟和测试数据零残留均正常。
- 已验证：修复版已发布至 `/opt/sunhold-oa/releases/20260721-162804`，Web 镜像为 `sunhold-web:20260721-162804`，API 沿用 `sunhold-api:20260721-160926`；Web/API 健康，公网健康检查返回 200，运行镜像内的新客户页 JS 与 CSS 资源均已确认存在。
- 仍未证明：当前浏览器登录会话失效，因此没有在服务器真实客户页保存或修改任何数据；用户刷新后应按其实际窗口宽度再做一次视觉确认。

## 2026-07-21《新系统测试2》问题闭环

- 已完成：读取《新系统测试2.docx》正文和 5 张截图，将反馈归并为四类真实故障：员工编辑不能修改用户名；待审批用印通过/拒绝后未进入对应结果页；案件案由关键词和权利类型缺少选项；新建案件缺少可建类型、案源人以及合同关联约束，并因“民事案件”不在服务端允许集合而无法提交。
- 已完成：管理员员工编辑增加可修改用户名。服务端改名同时校验格式、重名和当前会话自改限制，并原子迁移员工档案 owner、业务 JSON 中的精确账号引用、审批人、通知、附件、财务、人事、用印保管人等账号关联；旧用户名不能继续登录，新用户名沿用原密码和权限。
- 已完成：用印审批列表接口同时返回待审批、待用印和已拒绝记录，审批动作保存审批人、审批时间与意见；前端“已审待用印”和“已拒绝用印”可按真实状态展示，不再从一个只含待审批数据的集合中二次过滤。
- 已完成：新增案件参考资料接口，返回五类可建案件、启用案由和权利类型；恢复“民事争议”新建入口。新建页可选择案件类型，案由支持关键词检索，权利类型可选，关联合同后自动带入并锁定客户、显示案源人；民事案件通过专用三步创建和立案审批，不再被服务端拒绝。
- 已验证：本机 API/Worker/Web 已重建并健康；前端生产构建通过（5473 modules），Python 编译通过，菜单审计为 `264 nodes / 219 leaves / 0 unhandled`，364 个前端 API 调用全部匹配 FastAPI 路由；18 组端到端冒烟全部通过，覆盖用户名真实改名、民事案件创建审批、案由/权利类型参考资料、用印通过/拒绝列表流转和审批元数据，最终输出 `VERIFY_LOCAL_OK` 与 `SMOKE_CLEANUP_OK`。
- 已验证：测试服务器已发布到 `/opt/sunhold-oa/releases/20260721-160926`，API/Worker 使用 `sunhold-api:20260721-160926`，Web 使用 `sunhold-web:20260721-160926`；六个容器运行正常，API/Web/PostgreSQL/Redis/MinIO 健康，公网 `http://150.158.3.104:8088/health` 返回 200，发布后近期日志未发现 Traceback、Exception、Critical 或 Error。浏览器会话在 Web 容器更新后回到登录页，因此未输入密码，也未对服务器业务数据执行页面写操作。
- 仍未证明：本轮只闭环《新系统测试2》列出的故障及其同类接口边界，不代表整套 OA 已完成合伙人逐角色、逐状态、逐按钮最终验收；服务器页面在现有登录会话恢复后仍应做一次只读视觉抽查。

## 2026-07-21 合同用印印章类型阻断修复

- 已完成：读取《新系统测试1.docx》正文与截图，确认合同审批已通过后第四步“选择印章”显示“暂无数据”。根因是正式/测试环境关闭演示数据时不会创建印章台账，而合同用印接口又必须关联真实 `SealAsset`，因此无法继续提交。
- 已完成：把合同章、公章、所函专用章、法人章、发票章、财务专用章、财务三排章七类印章改为所有环境都必须存在的基础资料；初始化只补缺，不覆盖管理员已经维护的保管人、位置、状态、使用次数和既有业务数据。旧系统默认的“合同专用章/律师事务所专用章”仅在识别为系统默认资料时迁移到新名称。
- 已完成：合同向导下拉优先显示印章类型、名称和编号；无可用印章时明确提示管理员到用印中心维护。用印中心新增/维护印章的类别下拉与七类业主口径统一，后端同时拒绝范围外类别；仍保留具体印章资产、可用状态、保管与用印次数等真实台账逻辑，没有退化为前端硬编码成功。
- 已验证：本机 API/Worker/Web 容器重建后健康；完整 `verify-local.ps1` 通过，包含 5473 modules、Python 编译、263/218/0 菜单覆盖、363 个前端 API 匹配、18 组端到端业务冒烟、合同向导真实创建用印申请、七类可用印章断言、测试数据零残留和 Compose 配置检查，最终输出 `VERIFY_LOCAL_OK`。
- 已验证：测试服务器已发布到 `/opt/sunhold-oa/releases/20260721-142953`，API/Worker 使用 `sunhold-api:20260721-142953`，Web 使用 `sunhold-web:20260721-142953`；六个服务正常，API/Web/PostgreSQL/Redis/MinIO 健康，公网 `/health` 返回 200，数据库查询确认七类印章均为“可用”。浏览器只读打开已审批合同 `HT20260721130518` 的合同用印步骤，展开下拉后七类印章全部可见；未选择、保存或提交，没有修改该合同或生成新用印申请。
- 仍未证明：本项证明了合同用印选择与接口闭环，不代表整套 OA 的所有页面均已完成最终业主验收；正式上线前仍需按真实印章台账校准各印章编号、保管人和存放位置。

## 2026-07-21 全前端表单“点击无响应”同类风险集中审查

- 已扫描 `apps/admin-web/src` 全部 TSX 表单保存流程，审查范围包括：未挂载/隐藏字段、直接日期 `.format()`、同名对象匹配、校验和数据组装位于异常捕获之外、附件保存后的重复创建风险及提交反馈。客户同名匹配风险仅在合同新建中发现并已改为唯一客户 ID；合同附件失败后的草稿 ID 已提前持久化，重试不会重复 POST。
- 新增统一 `formSafety.ts`：所有表单必填日期先检查空值与有效性，再格式化。案件、合同、财务、平台财务、人事、调查、公证、用印、任务、仓库、沟通日志和收发文共 11 个模块已经替换直接调用；全前端已不存在 `v/value/values.<field>.format()` 的不安全写法。
- 沟通日志、调查取证、公证书登记、调查任务和用印申请已把表单校验、日期转换及请求统一纳入异常处理；字段校验失败继续显示表单红字，运行时或接口失败显示明确消息，不再形成未处理 Promise 或无反馈按钮。
- `scripts/audit-menu-coverage.py` 已增加构建期回归门禁：以后任何 TSX 重新直接格式化表单日期，完整验收会立即失败并指出文件与行号。
- 完整 `verify-local.ps1` 通过：前端 5473 modules、Python 编译、263/218/0 菜单覆盖、362 个客户端 API 路由匹配、18 组端到端业务冒烟、测试数据零残留和 Compose 配置均通过。
- 测试服务器 Web 已独立重建，API 与数据库未重启。浏览器抽样验证：沟通日志缺少客户/内容会显示逐项提示；用印申请缺少标题/类型/印章/用途会显示逐项提示；均未写入测试数据。Web 与 API 容器保持健康。
- 本轮结论限于本次“隐藏字段/日期转换/同名关联/异常捕获”同类故障的全代码扫描和代表性浏览器抽样；不把它扩大表述为全系统每个按钮均已完成人工逐页验收，其他业务按钮仍按模块验收清单继续闭环。

## 2026-07-21 合同新建“下一步”无响应修复

- 已修复合同新建独立页面点击“下一步”后无响应：该页面没有渲染签订日期、合同编号、负责人和部门字段，旧实现却直接从表单返回值读取并调用 `signed_at.format()`，导致浏览器运行时异常。现改为从已保存草稿或当前登录人资料安全补齐隐藏字段，并把数据组装、接口提交和异常提示统一纳入真实保存流程。
- 客户下拉框已由“客户名称”改为唯一客户 ID；同名客户不再被错误关联。合同仍保存客户名称用于展示，同时落库客户 ID、客户编号和客户管理人。
- “下一步”和编辑保存按钮增加真实提交中状态，阻止重复点击；合同 API 成功后立即记录草稿 ID，附件上传失败时也不会再次 POST 产生重复合同。
- 测试服务器浏览器实测：精确选择 `KH2026072109492885555147B8` 客户、填写合同名称和备注、上传 PNG 后，成功从“① 合同基本信息”进入“② 提交审核”，页面显示合同编号、草稿状态、客户和合同类别。数据库核验合同关联 `customer_id=3`、客户编号一致，附件记录真实存在；临时合同、流程记录、附件数据库记录和物理文件均已清理，零测试残留。
- 用户要求再次连接浏览器复测后，重复完成同一真实操作闭环并生成临时合同 `HT20260721112646`；页面明确显示“合同草稿已保存，进入提交审核”，数据库再次确认客户 ID、客户编号和附件记录一致。复测合同、流程事件、附件记录和物理文件均已清理，页面已重置到空白新建状态。
- 完整 `verify-local.ps1` 通过：前端 5472 modules、Python 编译、263/218/0 菜单覆盖、362 个客户端 API 路由匹配、18 组 API 冒烟、测试数据零残留和 Compose 配置均通过。测试服务器 Web 容器已单独重建并恢复健康，API 容器全程保持健康且未重启。

## 2026-07-21 客户列表长文本布局修复

- 修复客户编号链接超出 149px 列宽后覆盖“客户名称”的问题：表格数据单元格统一启用边界裁切和省略号，链接按钮限制为单元格宽度，客户编号和客户管理人保留悬停完整内容。
- 外部测试页实测客户编号按钮右边界为 442px、客户名称列左边界为 447px，`noOverlap=true`；横向滚动、底部操作和分页继续正常，浏览器控制台零错误。
- 完整 `verify-local.ps1` 通过：前端 5472 modules、Python 编译、263/218/0 菜单覆盖、359 个客户端 API 路由匹配、18 组 API 冒烟、测试数据零残留及 Compose 配置通过。

## 2026-07-20 案件管理 PDF 业务蓝图最低功能集补齐

- 对齐原则已确定为“PDF 是最低功能集”：蓝图中缺少的功能全部补上，现有超出蓝图的客户、合同、案件、任务、财务、收发文、人事、仓储、报表等能力保留，不删减；用印继续作为独立模块，不并入调查流程。
- 客户补齐潜在/目标/签约三级、分级审批、客户名称与统一信用代码关键字段变更审批；签约客户可开通独立客户服务端，凭一次性激活码查看本客户合同、案件进度和可见文档、下载真实文件并提交真实服务需求任务，支持停用及重置。
- 合同补齐一份合同对应多个外部合同编号；合同变更先形成待审批内容，批准后才写入正式合同，不再直接覆盖。
- 案件补齐手工立案审批、自动生成“立案登记/送达跟踪”固定任务、固定任务验收自动推进案件阶段、未办结任务阻断案件办结；补齐特殊解档申请与审批。线索自动转案继续保留。
- 任务补齐到期前 3 天和 1 天提醒、逾期后每 3 天提醒及持久化绩效影响；挂起、取消均走申请审批，挂起批准后可以恢复。原有“超过 30 天拒绝、交接后可重新开始、交接满 5 天未开始自动完成”规则继续保留。
- 调查补齐“关闭调查并生成报告”：只有下属任务和线索均结束后才允许关闭，并生成真实 DOCX 报告附件。开庭短信补齐 3 天/1 天待发记录和可配置发送通道；未配置短信服务商时明确记录“通道未配置”，不伪造发送成功。
- 系统角色种子补齐蓝图的系统管理员、业务专员、客户主管、合同管理员、调查主管、调查员、公证员、案件主管、承办律师、财务专员、财务主管、档案管理员、普通用户，同时保留现有角色；`admin` 仍为全所最高权限。
- 自动验证已通过：前端生产构建、Python 编译、263 个菜单节点/218 个叶子节点/0 未处理、359 个前端 API 调用全部匹配 FastAPI 路由、18 组端到端业务冒烟、测试数据零残留。详细逐项关系见 `docs/案件管理PDF逻辑对齐清单.md`。

## 2026-07-17 全系统审计与第一批红线整改

- 已建立唯一问题台账 `docs/全系统问题审计与集中整改清单.md`，覆盖前端、后端、权限、状态机、分页导出、测试、Docker、迁移和生产交付，并按 P0/P1/P2 与六个整改批次管理。静态审计结果不等同于外部服务器、历史数据、性能、安全或业主验收完成。
- 通用记录 PATCH、transition、DELETE 已改为显式安全模块白名单，合同、用印、公证、案件、任务、财务、人事、仓储、收发文等受保护模块不能借通用接口改状态或物理删除；合同创建/草稿编辑和用印草稿编辑已切换为专用接口。收发文、调查等尚存的前端通用写入下一批继续替换，不能把 P0-01 写成全部完成。
- 财务流水禁止无关联付款、开票、退费，付款要求财务办理角色、关联费用和已审批状态；开票、退费流水只由相应专用流程生成。合同审批中归档已阻断，并复核所有审批节点均已通过。
- 智能文档新增“人工确认”状态以及确认人、确认时间、内容版本和内容哈希；修改、重试会自动撤销确认，未确认或确认后内容变化均不能回写业务附件，前端只在确认后开放回写。
- 案件归档不再接收客户端自报完成条件：服务端根据案件办结、委托材料/证据材料/诉讼文书/裁判文书、费用与应收、发票和退款流程实时计算，提交和审核通过均二次校验；前端增加专用“确认案件办结”，伪造四个布尔值的冒烟用例已通过。
- 默认管理员凭据已从源码、登录页预填和冒烟脚本移除，私有 `.env` 仅供本机注入且被忽略；初始账号支持首次登录强制改密，改密前业务接口统一阻断，前端不可跳过改密弹窗。生产部署仍必须注入并轮换独立强密码。
- `scripts/audit-client-api-coverage.py` 已纳入完整验收。本轮 `VERIFY_LOCAL_OK` 通过：5471 modules、Python 编译、263/218/0 菜单覆盖、343 个前端 API 调用匹配、17 组业务冒烟、测试数据零残留、Compose 配置通过。六个容器运行；Worker 的 root 警告、真实业务调度、MinIO 接入等仍在问题台账中。

## 2026-07-17 法律顾问案件新建两步流程

- 已完成列表与详情首轮：根据旧前端 `ConsultantCase/SearchCondition.cshtml`、`List.cshtml`、`ConsultantCase_View.cshtml` 和 `ConsultantCase_Basic_Edit.cshtml`，法律顾问路由已从通用诉讼表格拆出专属查询和列定义。查询覆盖客户、案号、关键字、顾问期间、顾问类型、案件阶段、经办律师、律师助理、文档名称；列表覆盖案件编号、顾问类型、客户、顾问期限、经办律师、律师助理、案源人、剩余时间，剩余不足 10 日红色显示，过期归零。
- 已完成真实详情与修改：案件编号和查看按钮读取 `/records/{id}`、历史、案件任务及案件附件，详情展示旧站法律顾问基本字段和文档、律所费用、平台费用、内部结算、系统日志、案件任务、客户任务七个真实数据页签。新增 `/cases/{id}/counsel-basic`，只允许法律顾问、已完成新建且未进入归档的可管理案件修改案件名称、顾问类型、顾问期限、经办律师和律师助理；客户保持只读，人员有效性、日期顺序、角色范围和流程历史由后端强制。
- 已完成：根据服务器归档旧前端 `ConsultantCase_Create.cshtml`、`ConsultantCase_Litigant.cshtml` 与 `Legal.Case.js` 的只读证据，将法律顾问案件新建改为“基本信息 → 当事人信息”两步；基本信息保留客户、合同号、顾问类型、顾问期限、案件名称、经办律师、律师助理，隐藏旧站对该类型不使用的客户诉讼地位、案由、案件阶段、权利类型和司法机关步骤。旧源码中指向不存在顾问法院页的失效“下一步”未复制。
- 已完成：前端提交顾问类型及起止日期，完成后进入公司法律顾问列表；后端新增法律顾问专用完成端点，强制顾问类型、完整日期区间及起止顺序，阻止非顾问案件写入顾问字段、阻止顾问案件误走司法机关端点，并保留完整流程历史。
- 已完成法律顾问专用服务端列表与导出：`POST /cases/counsel/search` 按我的、部门、全所范围强制数据权限，支持客户、案号、关键字、顾问期限、顾问类型、案件阶段、经办律师、律师助理和文档名称筛选，支持案号服务端排序、10/15/20/50/100/200 条分页及准确总数；`POST /cases/counsel/export` 支持导出选中和导出全部真实 UTF-8 BOM CSV，并拒绝空选、越权 ID 和非法律顾问案件混入。
- 已验证：前端生产构建 5471 modules、Python 编译、`CASE_NEW_COUNSEL_FLOW_OK`、`CASE_COUNSEL_LIST_DETAIL_OK`、`CASE_COUNSEL_SERVER_LIST_OK`、菜单审计 `263 nodes / 218 leaves / 0 unhandled`、含两条顾问案件跨页排序、文件名筛选、日期范围、选中/全部导出及越权阻断的 17 组 API 冒烟、`SMOKE_CLEANUP_OK` 与完整 `VERIFY_LOCAL_OK` 均通过；API/Web Docker 镜像已重建并运行。
- 已补并通过法律顾问案件提醒、案件日志、批量人员/阶段修改、批量费用、附件 ZIP 下载和受控删除的 API 冒烟；未复刻旧站没有脚本绑定的死“新增退费日志”按钮，真实退费继续走财务专用流程。仍未证明：旧库 `BAS_Case_ConsultantType` 仅确认存在 3 条记录，归档中未保存其名称，当前顾问类型采用真实文本录入而未伪造下拉选项；旧站线索信息页签及其他逐角色动作仍需继续闭环，不能据此声明法律顾问模块或整站全部完成。

## 2026-07-17 行政案件及国家赔偿新建流程

- 已完成阶段性实现：行政案件新建第一步保存 `right_type`，行政客户诉讼地位按“原告/申请人、被告/被申请人、第三人”预填到对应当事人；第三步隐藏刑事公安/检察院字段，仅保留法院信息；完成后进入公司行政案件列表。
- 后端已补行政专属校验：客户诉讼地位白名单、原告/被告必填、至少一项法院信息、禁止公安/检察院字段写入行政案件。
- 自动门禁新增并通过 `CASE_NEW_ADMINISTRATIVE_FLOW_OK`；完整 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 已通过，包含前端 5471 modules、Python 编译、结构审计 `263 nodes / 218 leaves / 0 unhandled`、17 组 API 冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。
- 本项当前可记为“行政案件新建三步业务闭环已验证”。仍不能声明整站或行政案件全部视觉完全复刻完成，因为原站行政案件非空样本、完整字段初始值、列表视觉和全部交互细节仍未直接取证。

## 已实现并验证

- 财务中心“结算管理 → 待结算”已完成首轮原系统只读逐项对照。原站 `/5001007001/FAM/Settlement/PendingList` 的 10 项筛选、勾选列、14 个命名表头加末尾占位列、表头下全局金额合计、回款方式/银行备注与回退审核备注两行上下文、默认 10 条及六档分页、三枚行内图标和底部四个按钮均已复现。原站“查看分配记录”实际为行内展开的 15 列费用明细，而非弹窗；原站只执行查看和展开，未提交结算。
- 待结算页新增 `/finance/general-settlements/pending`、`/apply`、`/export` 专用流程。候选直接来自银行回款及案件费用分配，结算金额、归档费和实际结算金额由明确分配明细汇总；申请会原子创建独立 `finance_settlement` 待审批记录并从候选移除，重复申请和无权记录被拦截。普通记录新增、编辑、流转、删除均不能绕过专用结算流程；结算清单、到账清单、案件清单均导出真实 `.xls`。
- 浏览器临时数据 `BROWSER-SETTLE-1784118171851` 验证金额 `500/500/0`、官费 `100`、代理费 `200`、其他费用 `200`、代理费结算 `160`、归档费 `16`、实际结算 `444`，并验证 15 列展开明细、空选提示、勾选、确认框和真实申请后移出列表。申请、回款、应收、案件、合同、客户均已按依赖顺序精确删除，四类业务记录与回款关键词复查均为零。
- 新增结构门禁 `FINANCE_SETTLEMENT_PENDING_OK` 和第 16 组 API 端到端流程；统一验收通过 5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、16 groups passed、测试数据零残留和 Docker Compose 配置。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 最近 10 分钟严重日志匹配为 0；Docker 生产页标题、筛选、表头及控制台零错误复验通过。
- 财务中心“结算管理 → 待审核”已完成首轮原系统只读逐项对照。原站 `/5001007002/FAM/Settlement/ProcessingList` 的 13 项筛选、勾选列、14 个命名表头加末尾占位列、提交时间/提交人/提交备注/回款方式/银行备注上下文、默认 10 条及六档分页、三枚行内图标和底部“同意结算/拒绝结算/导出结算清单/导出到账清单/导出案件清单”均已复现；“查看分配记录”继续使用 15 列行内展开。原站只打开同意与拒绝弹窗核对标题、备注和按钮，未提交任何写操作。
- 新增 `/finance/general-settlements/applications` 专用服务端分页/十三项筛选和 `/review` 批量审批接口。管理员、负责人、审核员的数据范围由后端强制，同意进入待付款、拒绝进入已拒绝并写入审核人/时间/意见及流程历史；空批次、重复审批、非法状态、越权和通用记录接口绕过均被拦截。待审核三类 `.xls` 导出可按勾选申请生成真实数据。
- 浏览器临时申请 `4017` 完成查询、15 列明细展开、空选提示、单条同意弹窗、批量拒绝弹窗、行内及底部三类导出和真实拒绝移出列表回归；申请、回款 `142`、应收 `152`、案件 `4016`、合同 `4015`、客户 `4014` 随后按依赖顺序精确删除。三类记录、回款关键词、待审批及已拒绝申请复查均为 `0`，页面控制台日志为 `0`。`FINANCE_SETTLEMENT_AUDIT_OK`、统一验收 5471 modules、265/220/0、44 个财务原布局路由、16 groups passed、零测试残留和 Docker Compose 配置均通过；Docker 六服务正常且近期严重日志为 `0`。
- 财务中心“结算管理 → 待付款”已完成首轮原系统只读逐项对照。原站 `/5001007003/FAM/Settlement/PendingPaymentList` 的 13 项筛选、14 个业务表头加末尾占位列、表头下全局金额合计、审核时间/提交时间/审核人/提交人/审核备注/提交备注/回款方式/银行备注上下文、15 列行内分配明细、默认 10 条和六档分页均已复现。原站静态页面与官方交互脚本确认每行依次为“查看分配记录/导出结算清单/回退结算/标记已支付”，底部为“标记已支付”及三类导出；空选原文为“请选择回款.”。原系统无待付款数据，未执行任何写操作。
- 待付款页改为读取 `/finance/general-settlements/applications?status=待付款` 专用申请数据，不再错误读取通用费用。新增 `/finance/general-settlements/applications/payment`：仅待付款申请可回退或支付，回退要求审核备注并进入已退回、使原回款重新进入待结算；支付进入已付款。两条流程均保存操作人/时间/备注和流程历史，空批次、重复处理、非法状态、越权及通用记录接口绕过均被后端拦截；三类 `.xls` 按申请快照导出真实数据。
- 浏览器临时数据 `UIPAY-210641` 完成 13 项筛选、金额合计 `500/500/0/100/200/200/(空)/16/(空)`、四个行图标、审核/提交/回款上下文、15 列分配明细、空选提示、回退备注校验、两种弹窗和四次导出触发。申请 `4154` 真实回退后变为已退回并使回款重新出现；删除后重新提交的申请 `4155` 真实标记已支付并进入已付款。两条申请、回款 `147`、应收 `157`、案件 `4153`、合同 `4152`、客户 `4151` 已精确清理，ID 均为 404，三类记录/回款/待付款/已付款/已退回关键词复查为 `0`，页面控制台日志为 `0`。`FINANCE_SETTLEMENT_PAYMENT_OK`、5471 modules、265/220/0、44 个财务原布局路由、16 groups passed、零测试残留和 Docker Compose 均通过；六个容器正常且近期严重日志为 `0`。
- 财务中心“结算管理 → 已付款”完成首轮原系统只读逐项对照。原站 `/5001007004/FAM/Settlement/ProcessedList` 实际有 5893 条，采用 14 组筛选（比待付款多“付款日期”）、14 个业务表头加末尾占位列、默认 10 条及 10/15/20/50/100/200 六档分页；每行只有“查看分配记录/导出结算清单/回退结算”三枚图标，底部只有三类导出。上下文比待付款多付款日期；官方脚本证明全局合计只显示回款/已分/未分/官费/代理费/其他费用/归档费，代理费结算与实际结算合计留空。原系统全程只读，未执行回退或导出。
- 已付款页已从错误的通用费用源改为 `/finance/general-settlements/applications?status=已付款` 专用申请列表，新增付款日期服务端范围筛选并移除会错误读取 `case_no` 的前端二次过滤。付款接口允许已付款申请真实回退至已退回，保留准确的 `已付款 → 已退回` 流程历史并恢复待结算候选；重复支付、空备注、非法状态和越权继续由后端拦截。结构门禁新增 `FINANCE_SETTLEMENT_PAID_OK`，API 冒烟新增付款日期命中/排除/倒序日期拦截、三类导出、已付款回退和候选恢复。
- 浏览器临时数据 `UIPD-121868` 验证 14 组筛选布局、案件编号和付款日期查询、金额合计 `500/500/0/100/200/200/(空)/16/(空)`、三枚行图标、付款上下文、15 列分配明细、空选提示、行内/底部结算清单及到账/案件清单导出、回退备注必填和真实回退。申请 `4362` 回退后从已付款移出且回款 `154` 恢复为待结算候选；随后申请、回款、应收 `164`、案件 `4361`、合同 `4360`、客户 `4359` 已按依赖顺序精确清理，ID 为 404，已付款/已退回/待结算及回款关键词复查均为 `0`。统一验收通过 5471 modules、265/220/0、44 个财务原布局路由、`FINANCE_SETTLEMENT_PAID_OK`、16 groups passed、零测试残留和 Docker Compose 配置；Docker 六服务运行，Web/API/PostgreSQL/Redis 健康且近期严重日志为 `0`。下一页继续“结算管理 → 已拒绝”，整体目标仍未完成。
- 财务中心“内部费用 → 待付款列表”完成原系统只读逐项对照。原站 `/5001004003/FAM/InternalFee/PaymentObjectList` 标题“待付款列表”，采用三列九项筛选、固定“待付款”状态、勾选列加 13 个业务表头、当前页实际提成合计、默认 15 条及 10/15/20/50/100/200 六档分页；底部真实入口为“打包付款”，提示原文为“提示:选择同一收款进行打包付款.”。原站空选择弹窗为“提示 / 请选择提成. / 确定”；同一收款人打包后进入“付款单打印”，展示打包流水号、打印日期、付款总金额、逐笔案件/金额/提成类型/收款人/备注/小计和三类签字栏。原系统仅完成只读取证，未点击“提交并打印”。
- 本地新增 `/finance/payment-packages/preview`、`/finance/payment-packages` 列表/创建及管理员撤销接口。预览不写库；空数组、异收款人、非内部费用、非待付款状态、重复提交及越权均被拦截。提交生成 `finance_package` 待核销记录，将费用原子更新为已付款并写入“创建付款包/打包付款”流程历史；撤销会把对应费用恢复为已审批并移除付款关联。通用记录创建、编辑、流转和删除均不能绕过付款包专用流程。
- 浏览器使用两条临时待付款提成完成列表、13 列、`83.00` 合计、15 条分页、空选提示、全选、付款单预览和“提交并打印”真实落库回归；付款单显示 `￥83.00` 总额及 `￥42.00/￥41.00` 两笔小计，提交后按钮变为“已提交”，返回列表后两条记录消失。临时费用 `2321/2322` 与付款包 `P260715-21098393` 随后精确删除，费用和付款包关键词复查均为零。
- 自动门禁新增 `FINANCE_INTERNAL_PAYMENT_OK`；完整验收为前端 5471 modules、Python 编译、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 groups passed 且测试数据零残留。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 近 10 分钟严重日志匹配为零。
- 财务中心“内部费用 → 内部提成(退费)-待审核”完成原系统只读逐项对照。原站 `/5001004014/FAM/InternalFee/PaymentAuditList` 标题“请款单审批”，采用三列九项筛选、默认待审批且状态可切换，原始选项为请选择/创建待提交/待审批/待付款/待核销/已付款/已驳回/已作废；表格为勾选列加操作和九项业务字段，默认 15 条分页，空结果不显示批量审批区。
- 退费审核页新增专用 `/finance/fees/refund-review-candidates` 接口，不再从普通费用前端猜测；只有负数内部费用或明确退费标识的记录可进入，正数内部提成和其他费用被隔离。负数费用创建时写入 `is_refund`，审核日志使用“内部提成退费审批通过/驳回”；非待审批记录的勾选框与审批区受控。
- 浏览器使用一条正数和一条负数内部费用验证：默认页仅显示负数待审批记录，切换“请选择”后仍排除正数记录，行内审批打开完整提成审批面板，拒绝后状态变为已驳回且默认待审批页不再显示。两条临时记录 `2144/2145` 已按 ID 删除，关键词复查为零；API 复查确认负数候选、正数排除和专用驳回日志。结构审计新增 `FINANCE_INTERNAL_REFUND_AUDIT_OK`，15 组接口冒烟及零残留通过。
- 退费页修正后完整验收通过：前端 5471 modules、Python 编译、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 组 API 流程与测试清理全部通过；Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，近 10 分钟严重日志匹配为零。Docker 生产页标题、九项筛选、默认待审批和控制台零错误复验通过。
- 财务中心“内部费用 → 内部提成-已作废”完成原系统只读逐项对照。原站真实入口为 `/5001004013/FAM/InternalFee/PaymentList`，标题“请款单列表”，包含三列九项筛选、默认“已作废”、11 个业务表头加末尾空占位列、当前页金额合计、默认 15 条及六档分页；每行有“查看”、请款单号和案件编号三个真实入口。
- 已作废页补齐缺失的“查看”操作；“查看”和请款单号均进入“申请付款”详情页，详情按原站恢复付款信息查看/提交申请/财务审批/财务付款四段流程、案件/合同/客户/四类已付提成/申请信息以及八列付款明细。浏览器使用精确临时记录验证列表、合计、15 条分页、查看入口、请款单号入口、返回列表和案件入口；记录 `2086` 随后按 ID 删除，关键词复查为零。结构审计新增 `FINANCE_INTERNAL_VOID_OK`。
- 已作废页修正后完整验收通过：前端 5471 modules、Python 编译、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 组 API 端到端流程及测试数据零残留全部通过；Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，近 10 分钟 API/Web/Worker 日志严重错误匹配为零。Docker 生产页九项筛选和默认已作废复验通过，生产控制台错误为零。
- 财务中心“内部费用 → 内部提成-已拒绝”完成原系统只读逐项对照。原站真实路由为 `/5001004012/FAM/InternalFee/PaymentList`，标题“请款单列表”，采用三列九项筛选、固定默认“已拒绝”、11 个业务表头加 17px 空白占位列、当前页申请金额合计和默认 15 条分页；行内仅有真实“作废”、请款单号详情和案件编号入口。本地已新增受权限、费用类型和状态约束的真实作废 API，作废后进入“已作废”页并写入流程日志，重复作废和非内部费用均被拦截。
- 已拒绝页使用本地临时记录完成浏览器回归：列表状态统一显示“已拒绝”，金额显示 `88.50` 而非货币前缀，案件阶段/名称来自关联案件，详情弹窗、作废确认、移出已拒绝列表、进入已作废列表、合计行和 15 条分页均已验证；临时记录随后删除，关键词复查为零。结构审计新增 `FINANCE_INTERNAL_REFUSED_OK`，端到端冒烟覆盖拒绝、作废、重复作废拦截、非内部费用拦截和流程留痕。
- 全局页面外壳已改为固定视口，左侧菜单由 `.sidebar > .ant-layout-sider-children`、右侧业务内容由 `.content` 分别承担纵向滚动；滚动任一侧不会带动另一侧，顶部导航保持固定，两侧均预留稳定滚动条槽以避免内容横向跳动。浏览器在控制台长页面实测左侧 `0→272` 时右侧保持 `0`，随后右侧 `0→300` 时左侧保持 `272`，`window.scrollY` 始终为 `0`；结构审计 `INDEPENDENT_SCROLL_OK` 防止后续改动退回到整页滚动。
- 财务中心“内部费用 → 内部费用-待审核”已按原系统只读证据恢复右侧 580px 无遮罩审批滑板、审批意见、同意/拒绝、十列提成明细与底部取消按钮；本地验收临时费用记录已删除，关键词复查为零残留。
- 财务中心“内部费用 → 内部提成-待结算”完成原系统只读对照后的首轮专用复刻：保留 9 项筛选、14 列表头、默认 15 条及 10/15/20/50/100/200 分页档位；标题、三行筛选坐标、表格起始位置和底部“导出/更多操作/标识提成已发”按原页面结构调整。
- 待结算页不再直接展示通用费用记录，新增专用待结算 API，将已付款且尚未发放提成的内部费用与案件人员、诉讼主体、案件阶段及银行回款分配信息关联；“标识提成已发”真实持久化操作人、时间和备注，并写入流程日志，重复标识、越权记录和非待结算费用均会被后端拦截。
- 待结算页已完成本地浏览器回归：9 个输入框、15 个表头（含勾选列）、三个底部按钮、空选提示“请选择案件费用。”及“确定”按钮均已验证；页面控制台零错误。自动审计新增 `FINANCE_INTERNAL_SETTLE_OK`，端到端冒烟覆盖列表、空数组拦截、真实标识、重复标识拦截、流程日志和测试数据清理。
- 待结算页“更多操作”已按原站补齐完整菜单：上传案件文档，新增官费/代理费/其他费用/内部费用，批量修改开庭律师/经办律师/律师助理/案件阶段，生成授权委托书/律所函/身份证明/结算提成表，以及案件任务和案件日志；各入口复用真实附件、费用、案件批量修改、智能文档、任务和流程日志接口，不使用假成功提示。浏览器已验证完整一级菜单，结构审计锁定全部子项，端到端冒烟覆盖新增费用类型和案件批量修改。
- 财务中心“内部费用 → 内部提成-待归档”完成原系统只读逐项对照。原站实际路由为 `/5001004010/FAM/InternalFee/PaymentAuditList`，页面标题为“请款单审批”；本地已恢复三列九项筛选、固定“待审批”状态、勾选列、操作加十项业务字段、默认 15 条及 10/15/20/50/100/200 分页、行内和底部批量“审批”入口，并移除原站不存在的“附件”操作。
- 待归档页“提成审批”弹窗已按原站恢复审批意见、同意/拒绝、案号/提成类型/支付对象/基数/基数提成/实际提成及四类已付费用十列表格和取消按钮。新增真实单条与批量审核 API；拒绝进入“已驳回”并写入流程日志，非待审批、空批次、重复审核和越权均由后端拦截。自动审计新增 `FINANCE_INTERNAL_ARCHIVE_OK`，本地临时记录完成列表、分页和弹窗浏览器验收后已删除，控制台零错误。
- 财务中心“内部费用 → 内部提成-待审核”完成原站只读逐项对照。真实路由为 `/5001004002/FAM/InternalFee/PaymentAuditList`；标题、三列九项筛选、固定待审批状态、勾选加十列业务表、默认 15 条分页和提成审批流程与同类审批页一致，差异点是“案件阶段”输入框禁用，且空列表不显示底部批量审批区。原站筛选控件位于 42/68/94px，查询按钮 120px，表格 149px；本地已修正表格 2px 偏移和空状态多余按钮。
- 待审核页已用本地临时费用完成行内审批、批量入口、15 条分页和“提成审批”弹窗回归；临时记录随后删除。结构审计新增 `FINANCE_INTERNAL_AUDIT_OK`，共享的单条/批量真实审核接口及非法状态拦截继续由 API 冒烟覆盖。
- 动态通知同步清理已修正：管理员删除或清理来源记录后，过期的任务、费用、合同和案件通知会同步删除；API 冒烟清理审计已增加通知残留检查，本轮结果为零残留。
- 财务中心“内部费用 → 我的请款单”完成原系统逐项只读复刻：移除原站不存在的标题图标、色条和空表头，恢复 41px 标题、三列九项筛选、24px 控件及原始纵向坐标；费用类型恢复为文本框，表头严格为 11 列，行操作只保留真实“查看”，详情弹窗展示当前记录字段，分页默认 15 条并提供 10/15/20/50/100/200 六档
- 该财务页已用本地临时记录完成浏览器交互验收：查看详情、费用类型命中/排除查询、分页选项、零控制台错误全部通过，验收记录随后删除；自动审计新增 `FINANCE_INTERNAL_MINE_OK`，锁定筛选控件、11 列表头、查看操作和分页契约
- 报表中心首轮逐页视觉复核：品牌资金运营情况统计已按原系统真实页面移除不存在的刷新、CSV 导出和统计明细表，筛选区恢复为两行宽选择器、两行三列条件及底部查询按钮，4 个图表面板恢复原站灰色标题栏、尺寸与间距；图表仍使用本地实时业务数据，不伪造数据
- 报表中心六个入口完成逐页只读对照：律师资金页保持客户、开庭律师、分组模式、律师助理、案源时间、调查员、法院和开庭时间 8 组条件及 4 个资金图表；退费页保持 4 个 348px 图表；执行进度 1/2/3 均按原站展示同一套完整 10 个阶段图表，不再错误拆分为 4/4/2，后端三个视图同步返回完整 10 项实时统计
- 管理员深层页面刷新保护：`admin` 在动态菜单尚未返回时也直接通过页面权限判断，不再短暂出现“无权访问”，菜单返回后继续展示全部 265 个节点

- 管理员登录、JWT 身份认证、退出登录；令牌有效期按系统安全策略动态生效
- 统一会话失效处理：非登录接口返回 401 时清理令牌并返回登录页，退出仅清理本系统会话，顶部显示真实登录用户名称；每次请求实时校验数据库账号状态和角色，停用账号后旧令牌立即失效，角色变更立即生效
- AdminLTE 绿色布局、左侧多级导航、顶部全局检索与真实未读消息角标
- 全局检索：按案号、案件/客户名称、文件名及模板名查询，并跳转对应业务页面
- 消息中心：自动汇总任务到期/逾期、费用待审批、合同逐级审批与次日开庭提醒，支持单条和全部已读
- 站内消息页：对照原系统补齐全部/未读/已读/已发送四类视图、日期/发送人/关键词/系统或用户通知筛选、多人发送、详情阅读和收发双方独立软删除
- 控制台以 PPT 原始截图为权威参考重构：顶部 4×2 指标卡/待办事项、中部开庭排期/案件趋势、底部最新案件/民事案件分布
- 看板导航按原系统恢复：200px 紧凑侧栏、侧栏控制台及办案辅助工具，顶部使用青绿色品牌栏与全局检索
- 看板业务入口联动：8 个指标卡、全部待办名称/数量、开庭及最新案件案号可跳转对应业务页面，支持键盘访问
- 控制台实时样例数据已按 2026-07-15 原系统逐条校准：13 条开庭排期及 13 条最新案件保留完整法院、当事人和办案人员信息
- 侧栏支持任意深度递归菜单；已按原系统补齐用印、事务、合同、案件、调查、财务、平台财务、用户、人事、系统、仓库和报表三级入口，当前注册 265 个菜单节点，细分入口可落到对应范围/状态视图
- 调查细分视图支持本人/公司范围及待提交、待审核、待取证、已取证、已拒绝、费用申请状态；财务细分视图支持回款认领/分配、费用类型、审批/付款和开票状态过滤
- 统一业务列表：关键词查询、状态筛选、分页、详情、新增、编辑、删除
- 客户、合同、案件、任务、调查线索、用印、费用、收发文的首批字段配置
- 客户中心 11 个菜单入口、公海/回收站/共享状态视图
- 客户独立操作：多客户管理人、联系人维护、跟进记录、公海领取与释放、多人共享、移入回收站、按原状态恢复及边界拦截
- 客户详情工作台：联系人、跟进记录、客户文档三个页签；客户主体/工商/授权/沟通/开票资料分类上传、权限下载、管理员删除和操作留痕
- 客户冲突检索：跨客户、合同、案件、调查线索及案件对方当事人检索
- 客户主体资料：统一社会信用代码、法定代表人、注册地址、发票抬头、纳税人识别号、开票地址/电话、开户行及银行账号可维护
- 客户“我的/部门/全所、我的/全所回收站、共享给我”按当前登录账号、姓名和部门动态过滤
- 原系统客户列表完成逐字段复刻：客户/当事人筛选、案源人、管理人、三类日期、联系/合同/民事案件数量、代理费/官费汇总、勾选查看及更多操作
- 合同应收：应收计划、应收/已收/未收/逾期金额看板、回款进度、回款登记和超额拦截
- 合同顺序多级审批：审批人配置、逐级通过、越级拦截、拒绝终止后续节点与重新提交
- 合同资料支持内部合同编号与外部合同号并行保存和列表展示
- “我的合同”按当前登录账号/姓名过滤，合同审批入口仅展示审批中记录
- 已审批合同变更：变更类型/原因、名称、金额、外部合同号和期限调整，完整记录变更前后值、操作人与时间
- 原系统合同列表完成逐字段复刻：九项组合查询、19 列资金信息、上下合计、合同查看/变更/用印/付款/开票/新建案件/调查任务/归档及原站三条对照数据
- 用印中心专用闭环：印章台账、申请、提交、审批/拒绝、实际盖章份数、归档编号、流程记录及不可用印章拦截
- 调查联动工作台：线索、公证、证据三个页面及新增记录
- 调查线索状态闭环：新建强制为草稿，须提交审批并由管理员/经理/审核员通过后进入待取证；暂存和驳回线索可修改，通用接口不能绕过审批
- 线索取证登记：审批通过后登记实际取证日期和公证机构，未来日期及未审批线索不可登记，完成后进入已取证
- 调查父子任务：线索、公证、证据均可创建调查任务，支持主任务/子任务层级、负责人、截止日、优先级和来源业务追溯
- 证据整理入卷：从已审批线索建立证据目录，依次完成待整理、已整理、已入卷；入卷前强制校验证据目录及原件或扫描件
- 调查线索 CSV 批量导入：模板下载、UTF-8/5MB 校验、单行单产品约束、重复拦截和逐行错误反馈
- 公证记录 CSV 批量导入：模板下载、来源线索存在性、审核期限、公证书编号/签发日/存放位置/实物签收、重复公证校验和逐行错误反馈
- 公证书登记：审核通过后登记唯一公证书编号、签发日期、实物或扫描件存放位置及纸质实物签收状态
- 调查线索批量转案件：仅已取证/待公证线索可勾选并关联已审批合同，每条线索先生成一个“等待公证书”案件；客户不一致及重复转案逐条拦截
- 调查材料目录：线索、公证、证据分别限定材料分类，支持上传、列表、下载、删除、数量回写和流程留痕
- 公证材料闭环：已取证线索建立公证记录，登记公证书/库位，上传公证书与发票扫描件后案件进入“等待审核公证书”；自上传日起 30 天内人工审核，审核通过或超期后进入“新案待分配”
- 同一线索重复申请公证、重复生成案件和重复审核拦截
- 案件中心：案件阶段、客户管理人/开庭律师/经办律师/助理分配、开庭排期、执行案件和归档审核
- 案件任务抽屉：每个案件可直接查看关联任务、自动/手工来源、发起验收人、负责人、期限与状态，并可在案件内新建任务
- 案件诉讼进展联动：案件内维护一审法院/案号、法庭、法官、书记员、裁判日期/文书号及二审法院/案号；一审案号、开庭排期、裁判文书和二审案号分别自动推进至一审立案受理、一审准备开庭、待上诉和二审，且不会被较早要素逆向降级
- 案件“我的/部门/全所”入口按当前账号、所属部门及全所范围分别过滤，统计卡片随入口同步
- 手工新建案件强制选择已审批合同，客户、部门、内部/外部合同号由合同自动带入；通用接口无法绕过该约束
- 公证书扫描件进入待审核满 30 天后自动将其已关联的“等待审核公证书”案件转为“新案待分配”；不能绕过已取证线索与合同先创建案件
- 案件归档四项校验：案件办结、费用结清、材料齐全、财务流程完成；条件不足禁止提交
- 两级归档审核：经办人填写归档号、纸质卷宗位置/卷数后提交待审核，管理员或部门负责人通过后正式归档；驳回恢复原案件阶段并保留驳回原因
- 普通案件列表默认隐藏已归档案件，可通过“显示已归档”显式查看；归档审核页不受影响
- 原系统公司民事案件列表完成复刻：案件阶段树、十二项高级查询、基本/当事人/法院/委托律师/阶段/任务复合信息列、导出/上传/删除/更多操作及原站首屏十条对照案件
- 事务中心：新增任务、任务看板、关键词检索、到期/逾期/提醒视图、开始及完成操作
- 完整任务闭环：发起人、负责人、协作人、关联案号；接收、带理由拒绝、重新派发、负责人转交、提交完成、发起人确认或退回重启，以及任务内沟通全程留痕
- 任务“我的/部门/全所”入口分别按参与人、本部门和全所过滤；部门范围仅管理员/部门负责人，全所范围仅管理员
- 任务到期前 1 天提醒、逾期每 3 天提醒、任务交接后可重新开始、交接满 5 天未重新开始自动完成
- 负责人提交完成后进入待确认，发起人满 5 日未退回重启自动确认完成；任务按参与人范围隔离
- 公证原件自动交接任务：含公证材料的案件分配到具体文书人员后，系统自动向扫描上传人生成公证书及公证费发票原件交接任务；扫描人提交完成后，接收人 5 日内未退回重启则自动确认
- 原系统“我发起的任务/我接受的任务”列表完成复刻：对应状态页签、四列十二项组合查询、14 个原始列表字段、勾选批量操作及新增/导入/接收/拒绝/转交/完成/重派/确认/重启/沟通入口
- 收发文台闭环：收文/发文草稿登记、关联客户与案号、文件日期、来文/送达单位、附件数量、完成登记、确认签收/送达、归档编号及存放位置、详情与办理记录；通用编辑/流转接口不能绕过专用流程
- 原系统收发文台完成首批逐页复刻：官文收文、我的收文、公司收文三个动态菜单入口；官文四项查询/八列/上传/删除/修改日期及原站三条对照数据；我的/公司收文十二项查询和十一列展示
- 文件中心：20MB 格式校验上传、业务关联、分类、列表、下载和管理员删除
- 文书模板：诉讼/非诉/合同/归档/内部表单分类、版本、字段配置、新增、编辑、停用/启用和删除；停用模板禁止发起智能文档生成
- 案件归档材料：委托材料、证据材料、诉讼文书、裁判文书四类完整度看板
- 四类归档材料与案件“材料齐全”条件自动联动，上传齐全后通过，删除缺项后恢复未通过
- 财务中心：官方费用、内部费用、结算费用、预损费用、归档费用创建、要素校验、提交与审批
- 官方费用提交执行案件付款三要素校验（人员、法院、缴费通知文档）；内部费用支持负数冲销，全部费用按分进位保留两位
- 回款管理闭环：银行到账登记、待认领、认领客户、分配合同应收/可选案件、部分与全额分配、应收同步及回款流水自动生成；客户/合同/案件不一致与超额分配拦截，禁止直接登记回款流水绕过认领
- 财务流水：审批后付款、部分付款、超额拦截、开票、自动生成回款、退费及删除后状态回算
- 财务凭证附件：按单笔开票/付款/回款/退费流水上传、分类、查看、下载、删除和级联清理
- 发票申请闭环：关联案件、抬头/税号/类型/内容/交付资料、提交审批、驳回、登记开票、扫描件归档、发票作废及负数流水冲销
- 诉讼费退款闭环：关联案件和法院、原缴费票号、退款账户/预计日期、提交审批、驳回、退款办理、实际到账及退费凭证归档
- 周/月对账：按日期范围汇总流水、重复周期拦截、差异登记与确认
- 平台财务中心：可见菜单与原站一致，覆盖回款、付款、开票、结算、归档费结算和费用查询；后端继续保留周/月对账单、银行差异登记、复核确认及管理员删除能力
- 用户中心：站内消息、沟通日志和账户管理按原站拆页；Email、办公电话、手机、菜单自动收缩及密码均通过后端持久化
- 沟通日志：按客户、联系人、电话或内容查询，支持新增、编辑、删除和记录时间；新增/修改/删除均与客户详情跟进记录同步，并继承客户数据权限
- 办案辅助工具：国家知识产权局商标局、专利局、全国组织机构查询、法律法规和裁判文书检索快捷入口
- 系统中心：管理员用户新增、编辑、密码重置、启停与删除，预置角色权限矩阵和账号自锁保护；安全设置支持配置最短密码长度、失败锁定次数、锁定时长和令牌有效期
- 系统参数：对照原系统补齐案件类型、费用类型、案件阶段、法院、公证处、案由和付款类型七类基础字典；支持分类查询、新增、编辑、启停、删除、扩展属性、重复拦截与自动缓存失效
- 原系统参数设置：客户共享时间的全部/立案/高级/中级/初级/共享六项天数可配置；公司名称、代码、简称、地址、电话、传真、邮箱、邮编及银行资料可维护
- 系统管理：真实参数字典缓存状态、条目统计与手工清理；菜单管理可维护已注册页面的名称、图标、父级层次、排序、显示和启停状态，保存后侧边栏即时刷新，并保留角色菜单权限隔离；系统名称、默认部门、分页条数、附件上限和维护模式配置持久化
- 登录安全闭环：连续输错密码自动锁定，锁定期间正确密码也不可登录；用户列表展示失败次数、锁定状态和最近登录时间，管理员可手工解锁
- 角色菜单权限闭环：四类角色的数据范围和可访问菜单可配置并持久化；登录、刷新、侧边栏和页面路由统一执行权限结果，非法配置由 API 拦截
- 数据范围权限闭环：账号维护所属部门；全所、本部门、授权审批、本人及共享四类范围作用于业务列表、导出、详情、历史、编辑和流转，越权记录统一按不可见处理
- 字段级权限：按角色配置客户开票/银行/法务、合同金额、财务金额和员工证件字段；未授权字段在业务列表及详情 API 响应中剔除
- 关联资源权限：公海客户可见可领取但共享客户不可越权修改；附件、智能文档、合同审批、案件分配/归档、开庭、公证、用印、应收和财务流水均继承关联业务的数据范围
- 银行到账与平台对账数据隔离：普通用户只能查看本人登记/认领或其可见客户的到账，部门负责人不能读取他人生成的平台对账单；金额字段继续受财务金额权限控制
- 全局搜索、消息提醒、报表统计、附件下载和 AI 文档上下文同步执行数据范围与字段权限，防止通过旁路接口读取隐藏记录或金额
- 人事中心：员工档案、在职/试用/离职/停用视图；请假、事项、员工文档和提成设定支持真实新增/修改/删除/上传下载；试用转正记录生效日期，离职/停用强制记录原因、交接人和办理人，通用状态接口不能绕过专用人事流程
- 组织架构：对照原系统补齐部门管理和岗位角色管理；支持代码、名称、负责人、排序、启停、岗位权限、新增、编辑、删除与重复拦截，预置原系统主要部门
- 组织数据联动：部门改名同步账号及全部业务记录，岗位角色改名同步员工档案；仍被账号/业务/员工使用时禁止删除，员工新增与编辑从启用的部门和岗位中选择
- 仓库管理：除通用物品借还外，证物按原站字段支持登记、编辑、入库、出库、重新入库、销毁和完整流程留痕；非法顺序及绕用普通借还/报废接口均被拦截
- 报表中心：可见页面按原站复刻为品牌资金、律师资金、退费进度及执行进度 1/2/3 六个统计页，图表使用本地真实业务数据
- 系统操作日志：跨模块记录检索、模块筛选、状态变化、操作人、审批意见与分页
- 客户 CSV 批量导入：UTF-8/5MB 校验、必填校验、已有及文件内重复拦截、逐行错误结果
- 合同、案件、任务、收发文、费用、人事、仓库和用印八类 CSV 批量导入：各模块独立模板下载、业务字段和关联记录校验、安全初始状态、逐行成功/失败反馈及导入流程留痕
- 通用模块 CSV 导出：包含公共字段、扩展数据及创建更新时间，可直接使用 Excel 打开
- 合同、用印、费用、线索、案件、任务等模块的状态流转规则
- 审批通过、驳回、撤回、办结操作
- 流程操作留痕：原状态、目标状态、操作人、时间、审批意见
- 非 Docker 本地环境：Node、Python、FastAPI、SQLite、Vite
- Docker Compose 底座：PostgreSQL/pgvector、Redis、Celery、MinIO、Nginx
- Docker 运行完善：精简构建上下文、锁定 npm ci、API/Web 健康检查、服务健康依赖、自动重启、Nginx 上传与缓存规则、附件独立持久化卷
- Docker 固件虚拟化预检和一键启动脚本；当前机器 BIOS SVM 已启用，六个容器已完成首次实机构建并通过 Web/API、PostgreSQL、Redis、Celery 和四个持久化卷验证
- 服务器迁移底座：生产 Compose 覆盖、无默认秘密的环境变量模板、仅 Web 回环端口暴露、镜像摘要锁定、资源/日志/安全限制，以及 PostgreSQL/附件/Redis/MinIO 的备份恢复脚本和 Linux 部署文档
- 小程序共用 REST API 契约及 Dify 智能体代理入口
- AI 智能文档：选择模板与业务记录生成任务、未配置 Dify 时生成可编辑字段提纲、重试、人工编辑审核、DOCX 下载及回写文件中心
- 当前已注册菜单均接入专用业务页面或对应范围/状态视图；通用业务页仅保留为未知路由安全兜底
- 48 个主要业务路由完成浏览器只读巡检，页面标题、内容渲染和返回控制台入口均通过
- Ant Design Drawer、Alert、Space、Statistic 弃用属性已统一升级，干净页面运行时零错误
- 可重复 API 端到端验收：15 组流程覆盖鉴权、用户、安全与权限、真实菜单 CRUD、八类业务 CSV 及银行到账 CSV 导入、客户/合同/案件/调查/任务/财务/收发文/文档/用印/报表、人事附属记录与状态流转、通用仓库及证物完整生命周期，并验证越权、非法流转和测试数据零残留
- 报表结构自动审计：`REPORT_LAYOUT_OK` 锁定 6 个报表入口、执行三个视图的完整 10 图表、原站不存在的工具栏/明细表不得回归
- 一键本地验收脚本：前端生产构建、Python 编译、API 冒烟流程和 Docker Compose 配置统一检查
- 生产前端按业务中心拆分为独立按需加载文件，首次模块切换浏览器验收通过，构建无大包或循环依赖警告
- 财务中心“内部费用 → 待核销列表”完成原系统只读逐项对照：原站 `/5001004004/FAM/InternalFee/PackingList` 标题为“付款单-核销”，包含付款包号码、付款日期、收款人、固定“请选择”的付款包状态、费用类型五项筛选；列表为操作、付款包号码、收款人、付款总金额、付款状态、付款日期、付款单据号、备注八列，默认 15 条并保留 10/15/20/50/100/200 六档分页和当前页金额合计。付款包号码进入只读付款单打印详情。
- 本地新增付款包专用核销接口与原样六字段“付款核销”弹窗：付款打包号和金额自动回填，付款日期默认当天，付款方式支持自动扣款/银行卡/现金，付款单据号必填并使用原站提示“请输入付款单据号.”；金额不一致、无权限、无效方式、重复核销和通用记录接口绕过均被拦截。核销后付款包及关联费用双向留痕；已核销付款包只能由管理员通过显式冲正恢复，避免普通删除破坏财务生命周期。
- 浏览器使用临时付款包 `P260715-29286234` 验证五项筛选、八列表格、`100.00` 合计、15 条分页、付款单打印、空单据号校验、真实核销和已核销页单据号/备注回显；验收发现并修正已核销页重复“核销”死入口为只读“查看”。临时费用 `2443/2444` 和付款包 `2445` 已精确冲正删除，三条记录复查均为 404。
- 自动门禁新增 `FINANCE_INTERNAL_WRITEOFF_OK`；完整验收结果为 5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 groups passed、测试数据零残留，Docker Compose 配置通过。
- 财务中心“内部费用 → 付款单查询”完成原系统只读逐项对照。原站真实入口 `/5001004005/FAM/InternalFee/PaymentList`，标题“请款单列表”；三列九项筛选为申请日期、付款状态、申请人、审核日期、案件编号、请款单号、客户名称、案件阶段、费用类型，付款状态选项为请选择/待审批/已审批/已拒绝/已作废。列表为操作、请款单号、状态、申请日期、审核日期、申请金额、案件编号、案件阶段、案件名称、付款日期、申请人及末尾空占位列，保留当前页金额合计、默认 15 条和 10/15/20/50/100/200 六档分页。
- 查询页三个只读入口已真实接通：行内“查看”和请款单号均进入“申请付款”详情，案件编号进入案件列表；详情按原站展示付款信息查看/提交申请/财务审批/财务付款四段流程，案件/合同/客户资料、四类已付提成、申请信息及八列付款信息。修正了待审批记录误显示审核日期、详情金额错误带货币前缀、查询页误用通用操作按钮和缺失末尾占位列等差异。
- 浏览器临时记录 `2505` 验证九项筛选、12 列、`64.30` 合计、15 条分页、查看入口、请款单号详情、案件编号入口、待审批审核日期留空以及详情数字格式；验收后已按 ID 删除并复查 404。自动门禁新增 `FINANCE_INTERNAL_QUERY_OK`，统一验收为 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、测试数据零残留。
- 应用外壳改为固定视口布局：顶部栏固定在视口上方，左侧导航栏与右侧业务内容区分别使用独立的纵向滚动容器，并阻止滚动链穿透。1280×720 浏览器实测左侧滚动到 `180` 时右侧保持 `0`，随后右侧滚动到 `320` 时左侧仍保持 `180`。结构门禁新增 `INDEPENDENT_SCROLL_OK`；统一验收通过 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、测试数据零残留和 Docker Compose 配置，六个容器运行正常且近期严重日志为 0。
- 财务中心“内部费用 → 已核销列表”完成原系统只读逐项对照。原站真实入口 `/5001004006/FAM/InternalFee/PackingList`，页面标题“付款单-查询”；虽然菜单名为“已核销列表”，原页付款包状态默认是可选的“请选择”，初始列表同时包含待核销与已付款数据，不能在前端强制只保留已付款。五项筛选为付款包号码、付款日期、收款人、付款包状态、费用类型；列表为操作、付款包号码、收款人、付款总金额、付款状态、付款日期、付款单据号、备注八列，保留全局金额合计、当前页金额合计、默认 15 条和六档分页。
- 本地已修正该页默认状态及数据范围，增加表头下全局金额合计；待核销与已付款行均只提供“查看”，行内“查看”和付款包号码均进入同一付款单打印页。浏览器临时费用/付款包 `2624–2627` 验证默认两种状态、总额 `175.00`、已付款筛选后上下合计 `75.00`、单据号/备注回显和两个详情入口；验收后待核销包正常撤销、已付款包显式冲正，四条记录均已删除并复查 404。
- 自动门禁新增 `FINANCE_INTERNAL_DONE_OK`，API 冒烟补充付款包核销后仍可由全状态列表查询且单据号/备注完整回显。统一验收通过：5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 groups passed、测试数据零残留和 Docker Compose 配置；六个容器正常，近期严重日志为 0。
- 财务中心“内部费用 → 内部费用明细/内部费用明细(公司)”完成原系统只读逐项对照。原站个人入口 `/5001004007/FAM/InternalFee/InternalFeeList`、公司入口 `/5001004008/FAM/InternalFee/InternalFeeList`，标题均为“内部费用查询”；页面为四列十二项筛选，案件阶段和费用类型使用带确定/取消的多选树，个人入口收款人固定为当前用户，公司入口可编辑。列表恢复勾选列、14 个业务列和末尾空占位列、表头下全局金额合计、页尾当前页合计、默认 15 条和六档分页；案号进入案件页，导出选中/全部均生成真实 Excel `.xls`。
- 新增专用 `/finance/internal-fees` 查询和 `/finance/internal-fees/export` 导出接口，支持个人/公司范围、十二项服务端筛选、服务端分页、付款流水汇总、字段权限过滤和 SpreadsheetML Excel 输出。浏览器以临时案件/费用 `2687–2689`、流水 `364` 验证个人仅见本人收款 `120.00`、公司总额 `200.00`、未付筛选 `80.00`、多选筛选、原样清空规则、案件跳转、空选导出提示和成功导出；临时数据已按依赖顺序删除，三条业务记录复查均为 404。
- 自动门禁新增 `FINANCE_INTERNAL_DETAIL_OK`，并继续锁定 `INDEPENDENT_SCROLL_OK`：应用固定视口，左侧菜单和右侧业务内容各自纵向滚动、互不带动。完整验收通过 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、测试数据零残留和 Docker Compose 配置。
- 财务中心“结算管理 → 已拒绝”完成原系统只读逐项对照。原站 `/5001007005/FAM/Settlement/RejectedList` 为十三组筛选、勾选列加十四个业务列和末尾占位、审核/提交/回款上下文、默认 10 条及六档分页；行内为“查看分配记录、重新申请结算、导出结算清单”，页尾为“重新申请”及三类清单导出。已拒绝页合计只显示回款、已分、未分、官费、代理费和其他费用六项，审核备注使用红色强调；原站未执行任何写操作。
- 本地已拒绝页改用结算申请专用数据源，支持 `已拒绝/已退回/已驳回` 服务端组合状态、十三项筛选、展开十五列分配明细、三类真实 `.xls` 导出和专用重新申请接口。重新申请要求非空备注，只允许拒绝类状态回到“待审批”，保存操作人、时间、备注及准确的“已拒绝 → 待审批”流程历史，重复提交和通用接口绕过均被拦截。
- 浏览器临时链路 `UIRJ-122620`（业务记录 `4497–4500`、到账 `159`、应收 `169`）验证金额 `500/500/0/100/200/200`、拒绝上下文、案件筛选、三条明细、空选择提示、空备注提示、行内/页尾导出和真实重新申请；提交后拒绝列表归零、待审核列表出现且待结算候选隐藏。全部临时数据已按依赖顺序删除并复查相关记录 404、四类列表为 0。门禁新增 `FINANCE_SETTLEMENT_REJECTED_OK`，API 冒烟现为 16 groups passed 且零残留。
- 财务中心“归档费结算 → 待归档”完成原系统只读逐项对照。原站 `/5001008001/FAM/ArchiveSettlement/PendingList` 当前为 296 条，采用案件类型、案件阶段、回款单位、回款时间、开庭律师、律师助理、提交人、结算支付日期、案件编号、客户名称、审核人十一项筛选；表格为 51px 勾选列、85px 操作列、案号/客户及十项业务列和 17px 末尾占位列，表头下只汇总回款金额与归档费金额，默认 10 条并提供 10/15/20/50/100/200 六档分页。每行只有“新建任务”和案号两个入口，页尾只有“导出选中”；原系统全程只读，未执行任何写操作。
- 本地新增 `/finance/archive-settlements/pending` 和 `/finance/archive-settlements/export` 专用接口：待归档数据由已付款结算申请的明确分配明细生成，按归档费拆分到案件费用，排除已进入归档阶段的案件；十一项服务端筛选、服务端分页、管理员全所范围、字段权限、全局金额合计、非法日期和空选导出均由后端约束，导出为真实 SpreadsheetML `.xls`。前端恢复原站四列三行查询区、精确列宽、合计行、六档分页、“新建任务”及案号跳转，不再使用通用费用记录猜测归档费数据。
- 浏览器临时链路 `UIAF-123487` 验证一条 `200.00` 回款、`16.00` 归档费、案件阶段/律师角色/回款方式、案件编号查询、案件类型六个选项、空选导出提示、勾选与真实导出触发、“新建任务”进入任务列表及案号进入案件列表。临时申请 `4638`、到账 `164`、应收 `174`、案件 `4637`、合同 `4636`、客户 `4635` 已按依赖顺序精确删除；归档费/到账复查为 0、业务记录为 404、应收记录不存在。结构门禁新增 `FINANCE_ARCHIVE_PENDING_OK`，API 冒烟补充待归档数据、合计、筛选、非法日期、Excel 导出与结算回滚断言，现为 16 groups passed 且零残留。
- 财务中心“归档费结算 → 待支付”完成原系统只读逐项对照。原站 `/5001008002/FAM/ArchiveSettlement/ProcessingList` 当前为 25 条，比待归档多“归档日期”筛选；表格沿用 51/85/136/255/136…/17px 精确列宽，末列为“支付时间”，表头下全局合计为 `690092.71/55208.00`。每行含“同意支付、拒绝支付、新建任务”，并固定展开归档审核人/申请人、审核/提交时间、审核/提交备注、归档号和审核状态四块上下文；页尾为“同意结算、拒绝结算、导出选中”。同意弹窗为“备注/同意/取消”，拒绝弹窗为“备注/提交/取消”，原系统未提交任何写操作。
- 本地新增 `/finance/archive-settlements/payment`、`/payment/review` 和 `/payment/export` 专用流程。只有已付款结算中归档费大于零且案件已通过归档审核的明细进入待支付；十二项服务端筛选、归档日期边界、分页、权限、合计和真实 `.xls` 均由后端处理。支付审核按明细原子创建受保护的 `finance_archive_settlement` 记录，拒绝必须填写备注；重复处理、空批次、越权、普通创建/编辑/流转/删除绕过均被拦截，删除源结算申请时级联清理支付决定及历史。
- 浏览器临时链路 `UIAP-116999` 验证 `200.00/16.00` 合计、12 项筛选、15 列精确列宽、归档审核四块上下文和本地时间格式、空选批量/导出提示、选中 Excel、任务/案件入口、同意/拒绝弹窗、拒绝空备注提示及真实同意支付后移出列表。支付决定 `4778`、申请 `4777`、到账 `169`、应收 `179`、案件/合同/客户 `4774–4776` 已按依赖顺序精确清理；决定和业务记录为 404，待归档/待支付/到账为 0，应收不存在。新增 `FINANCE_ARCHIVE_PAYMENT_OK`，16 组 API 冒烟和零残留通过。
- 待支付页统一验收通过：前端 5471 modules、Python 编译、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、`FINANCE_ARCHIVE_PENDING_OK`、`FINANCE_ARCHIVE_PAYMENT_OK`、16 groups passed、测试数据零残留和 Docker Compose 配置；Docker 六个服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 近期严重日志为 0。
- 财务中心“归档费结算 → 已支付”完成原系统只读逐项对照。原站 `/5001008003/FAM/ArchiveSettlement/ProcessedList` 当前 2700 条，十三项筛选比待支付页增加“归档费支付日期”；表格沿用 51/85/136/255/136…/17px 列宽，表头下全局合计为回款 `46001268.94`、归档费 `3680334.00`。每行只有“回滚归档费”，展开区按四块显示归档费审核、归档审核、归档申请三层人员/时间/备注及支付状态、归档号、归档审核状态；页尾为“回滚归档费结算、导出选中”，回滚弹窗为“备注/回滚/取消”。原系统仅打开弹窗取证，未提交写操作。
- 本地新增 `/finance/archive-settlements/paid`、`/paid/export` 和 `/paid/rollback` 专用接口。已支付决定使用服务端十三项筛选、分页、权限、金额合计和真实 `.xls`；回滚必须填写备注，只允许受保护的已支付归档费决定进入“已回滚”，记录操作人、时间、备注及准确历史，并令源明细重新回到待支付。再次同意支付复用原决定记录，避免重复流水；通用创建/编辑/流转/删除仍不能绕过专用流程。
- 浏览器已确认本地“已支付”深层路由自动展开左侧父菜单、13 项筛选、15 列表头和空状态布局；自动 API 链路验证“已支付 → 回滚 → 恢复待支付 → 再次支付”及同一决定 ID 复用、Excel、非法日期、空批次、空备注、错误记录和级联清理。新增 `FINANCE_ARCHIVE_PAID_OK`；统一门禁为 5471 modules、265/220/0、44 个财务原布局路由、16 groups passed、测试数据零残留。
- 财务中心“归档费结算 → 已拒绝”完成原系统只读逐项对照。原站 `/5001008004/FAM/ArchiveSettlement/RejectedList` 为案件类型、案件阶段、回款单位、回款时间、开庭律师、律师助理、提交人、提交日期、案件编号、客户名称、审核人、审核日期十二项筛选；列表为勾选列、操作、案号、客户及十二个业务列和末尾占位列，默认 10 条并提供六档分页。行内只有“回滚归档费”，展开区为归档费审核、归档审核、归档申请三层人员/时间/备注及支付状态、归档号、归档审核状态；页尾为“重新申请、导出选中”。官方脚本确认空选提示“请选择归档费.”/“请选择案件.”、回滚备注必填“请输入审核备注.”，重新申请备注可空；原站未执行写操作。
- 财务中心“费用查询”完成首轮原系统只读逐项对照与本地实现。页面恢复四列三行十二项筛选、费用类型确定/取消多选树、勾选列加十六个业务列、1904px 横向表格、全局/本页五项金额合计、默认 15 条及 10/15/20/50/100/200 六档分页；金额按原站显示纯数字。页尾“导出”与“更多操作”均为可点击下拉，包含导出选中/全部及原站十项案件操作。
- 新增 `/finance/fees/query` 和 `/finance/fees/query/export`：支持十二项服务端筛选、服务端分页、全局金额合计、范围边界校验、管理员全所范围、普通用户金额字段脱敏及真实 SpreadsheetML `.xls`。费用退费显式关联案件费用，通用财务写接口继续受专用生命周期约束。`FINANCE_FEE_QUERY_OK` 和费用查询 API 冒烟覆盖已加入。
- 本地浏览器复验费用类型树全部十四项、导出两项、更多操作十项及金额无货币前缀；统一验收为 5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、17 groups passed、测试数据零残留，Docker API/Web/PostgreSQL/Redis 健康。
- 本地新增 `/finance/archive-settlements/rejected`、`/rejected/export`、`/rejected/rollback` 和 `/rejected/reapply` 专用接口及对应页面。已拒绝决定支持十二项服务端筛选、权限、分页、两项金额合计和真实 `.xls`；拒绝回滚进入“已支付”并强制备注，重新申请进入可再次支付审核的“已回滚”状态且备注可空，均保存操作人、时间和准确历史。通用源结算回退现在会在关联归档费仍为“已支付/已拒绝”时拒绝，必须先走归档费专用回滚或重新申请，避免绕过生命周期。
- 浏览器同屏核对本地空状态与原站：四列三行 12 项筛选、查询按钮、勾选/操作/14 个业务表头、精确列宽和横向滚动一致，深层路由自动展开父菜单；左侧导航与右侧内容继续使用独立滚动容器。自动链路覆盖“已支付 → 已回滚 → 已拒绝 → 拒绝回滚 → 再次拒绝 → 重新申请 → 再支付 → 释放源结算回退”、同一决定 ID 复用、筛选、日期边界、Excel、空/重复/错误记录和通用接口防绕过。新增 `FINANCE_ARCHIVE_REJECTED_OK`；统一验收为 5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、16 groups passed、测试数据零残留，API/Web/Worker 新镜像已重建且六个容器运行正常。

## 正在实现

- 2026-07-16 已完成“事务中心 → 我的任务 → 我发起的任务”首轮原系统只读对照与本地闭环。原站 `/9001001010/TP/Task/TaskList` 的 6 个状态页签、4×3 共 12 项筛选、勾选列加 14 个业务字段、默认 15 条及 10/15/20/50/100/200 六档分页、11 项“更多操作”、任务编号“案件任务”四阶段详情和沟通记录均已取证；原站全程未提交写操作。
- 本地 `/tasks` 已补齐 initiated/owned/collaborating 关系范围、组合筛选、状态计数和服务端分页；任务状态闭环统一为“处理中 → 已完成待验收 → 已验收”，保留旧“待确认”兼容。逾期仅作为展示状态，实际办理继续依据 `workflow_status`；验收日期使用 `verified_at`。创建、交接、重新派发、批量修改均校验真实启用用户，发起人可批量修改本人任务，终态不可批量修改，通用记录创建/编辑/流转不能绕过任务专用流程。
- 三条不可破坏规则已继续锁定：截止日期超过 30 天拒绝；交接后负责人可以重新开始；交接满 5 天仍未开始自动完成。新增任务和批量修改的日期控件同步限制 30 天，任务写操作增加防重复提交锁；自动完成任务不再显示必然失败的“退回重启”。
- 新增 `TASK_MY_CREATED_OK` 结构门禁。完整验收通过 5471 modules、Python 编译、265 nodes / 220 leaves / 0 unhandled、17 groups passed、测试数据零残留和 Docker Compose 配置；API/Web/Worker 已重建，六容器运行正常，API/Web/PostgreSQL/Redis 健康，近 10 分钟严重日志匹配为 0。Docker 生产页深层路由、父菜单展开、6 页签、12 筛选、14 字段、空态及 11 项更多操作均已浏览器复验，生产页控制台错误为 0。
- “事务中心 → 我的任务 → 我接受的任务”已完成本轮只读取证与本地实现：原系统确认 4 个状态页签、12 项筛选、可勾选/四列排序的 14 列表格、15 条默认分页及 6 种页容量、固定“接受任务”与 11 项更多操作、四阶段详情和逐条“标记未读”。本地同步了整格标题/状态色、原格式日期和 `N/天`，管理员关系查询保持全所数据，任务排序、历史标记未读、终态提醒清理及专用生命周期权限均落到真实 API。
- `TASK_MY_ACCEPTED_OK` 与完整 `verify-local.ps1` 已通过：5471 modules、265/220/0、17 groups passed、测试数据零残留和 Docker Compose 配置通过。API/Web/Worker 已重建，本地生产页实看通过筛选、表格、11 项菜单和任务详情。
- “事务中心 → 我的任务 → 我协作的任务”已完成首轮原站取证和本地闭环：精确两个页签、12 项筛选、14 列/四排序、空结果表尾行为、详情沟通、管理员全所范围和纯协作权限均有门禁；已修复管理员表格/选中双源、静态备用菜单和错误批量入口。
- 原站 11 个顶层“更多操作”及二级菜单已对齐：律所/平台费用各四种子类型，批量修改为关联案件的开庭律师/经办律师/律师助理/案件阶段。本地费用分类和案件按案号修改均使用真实接口，旧 `/tasks/batch-update` 前端入口已删除。`TASK_MY_COLLABORATING_OK`、5471 modules、17 groups passed、零残留及 Docker 生产页实看通过。
- “事务中心 → 我的任务 → 未读新消息的任务”已完成原站 `/9001001040/TP/Task/TaskList` 只读取证。原页标题为“任务列表”且无页签，保留 12 项查询、勾选列加 14 个通用列、默认 15 条及六档分页和 11 项更多操作；未读消息/发送人/时间分别投影到状态/优先级/标题，后六格留空。0 条时使用精确空文案并隐藏表尾分页和操作区；原站全程未执行业务写操作。
- 本地新增专用 `GET /tasks/unread-messages` 与 `POST /tasks/{task_id}/messages/read`；仅聚合当前接收人的未读 `task-message-*`/`task-history-*`，排除到期提醒，管理员也不扩大个人未读范围。任务创建、批改、接收、拒绝、重派、沟通、交接、重启、完成、验收及两类自动完成都会生成幂等的接收人消息。浏览器验收另发现并修复了跨组件徽标滞后：读取任务消息或重新标记未读后会派发 `sunhold:notifications-updated`，顶部铃铛立即重载未读数，不再等待 60 秒轮询。`TASK_MY_UNREAD_OK`、5471 modules、265/220/0、17 groups passed 和测试数据零残留已通过；菜单页键同步到 `?page=` 且前进/后退会恢复页面与两级父菜单。
- Docker 生产页完成隔离账号最终复验：角标 `2` 与一条真实未读任务正常显示，点击任务后列表即时清空、详情打开、角标即时归零。临时任务/账号/通知清理后四项数据库残留计数均为 `0`；`verify-delivery.ps1` 完整门禁退出码 `0`，六服务、健康端点、生产安全、交付/备份/恢复产物和近期严重日志均通过。该状态可作为明日 Docker 演示/UAT 候选版，尚未逐页证明的叶子页面继续保留为未完成。
- “事务中心 → 部门任务 → 部门发起的任务”完成原站 `/9001002010/TP/Task/TaskList` 只读取证：6 页签、12 筛选、勾选列加 14 列、4 个排序字段、15 条/六档分页、11 项更多操作均已核对；隐藏 `Initiator_DepartmentId=6` 证明发起部门范围。非空页没有个人“新增任务”，0 条时精确空文案并隐藏分页/操作区。
- 本地后端已按发起部门/负责人部门/协作人部门分别实现部门任务三种关系，普通用户 403、管理员全所权限不降级；前端取消部门页个人关系二次过滤，并移除部门发起页个人新增与生命周期控制。Docker 页非空/空态均已浏览器实看，控制台错误为 0；`TASK_DEPT_CREATED_OK`、5471 modules、Python 编译、265/220/0、17 groups 与零残留通过。下一页继续“部门接受的任务”，完整复刻仍未完成。
- “部门接受的任务”后端权限完成专项复核：`scope=department&relation=owned` 对部门负责人按当前任务负责人所属部门取数，普通用户返回 403，`admin` 仍可读取全所；部门列表读取权不等于负责人写权限。修复同部门经理可通过调查员分配、任务批量修改或调查批量删除旁路替换/删除他人任务的问题；接收、拒绝、完成、交接继续只允许当前负责人或最高管理员，重启按任务状态只允许当前负责人或发起人（最高管理员保留覆盖），批量修改/调查分配只允许负责人、发起人或最高管理员。新增同部门第二经理反越权断言，Python 编译、17 组 API 冒烟和测试数据零残留通过；该页面的完整视觉与交互结论仍以本轮前端/浏览器验收为准。
- 关联入口统一整改：任务列表、任务沟通详情与任务费用申请弹窗的案号均先按当前账号范围精确校验案件，目标不存在或无权时在来源页明确提示；调查列表、调查详情中的权利人、来源任务、线索、案号和公证号统一使用可截断的真实跳转控件，取消 `href="#"` 假链接；客户列表名称与客户编号均可进入同一客户详情。菜单静态审计新增 `RELATION_PREFLIGHT_AND_DETAIL_LINKS_OK`。本轮未做浏览器点击验收，待与多角色案件页面验收一并补证。
- 原系统 `/9001002020/TP/Task/TaskList` 已只读取证：四状态页签为待处理/进行中/完成/停止，12 项筛选、选择框加 14 列、4 个可排序时间/天数列、默认 15 条及 10/15/20/50/100/200 六档分页；隐藏条件明确为 `SearchCondition_Officer_DepartmentId=6`。有数据时底部只有“更多操作”和分页，不显示个人接受/拒绝/完成/交接；更多操作共 11 项。临时标题筛选验证空态精确文案“没有查询到符合条件的记录 。”且底部隐藏，随后已恢复筛选，原系统无业务写入。
- 本地 Docker 成品页已验证 4 状态、12 筛选、14 列、真实部门数据、分页与“更多操作”；部门查看页不再暴露个人生命周期按钮，零结果精确显示原站空态且隐藏底部，刷新后恢复默认列表。新增 `TASK_DEPT_ACCEPTED_OK` 门禁；完整验收通过 5471 modules、265/220/0、17 groups、测试数据零残留、六个 Docker 服务及近期错误日志检查。下一页继续“部门任务 → 部门协作的任务”，完整复刻仍未完成。
- 原系统 `/9001002030/TP/Task/TaskList` 已完成只读取证：隐藏范围为 `SearchCondition_Participant_DepartmentId=6`，即参与人/协作人部门；页面为进行中/完成两页签（取证时 2/165）、12 项筛选、选择框加 14 列、发起/截止/剩余/更新 4 个排序列、默认 15 条及 10/15/20/50/100/200 六档分页。有数据时底部只有 11 项“更多操作”和分页，不出现个人生命周期按钮；临时标题筛选验证空态精确文案且底部隐藏，随后已清除筛选，原系统无业务写入。
- 本地“部门协作的任务”成品页已验证两页签、12 筛选、14 列、2 条真实部门协作数据、六档分页及 11 项更多操作，空态和底部行为与原站一致，恢复默认列表后浏览器错误为 0。后端新增跨部门协作任务与纯协作者反越权证据：部门经理按任一协作人所属部门读取，普通用户 403、`admin` 全所；旁观经理和纯协作者均不能借生命周期、批量、调查或通用接口改负责人/状态或删除任务。新增 `TASK_DEPT_COLLABORATING_OK`；5471 modules、265/220/0、17 groups、零残留、六服务与近期日志完整门禁通过。下一页继续“公司任务 → 公司发起的任务”，完整复刻仍未完成。
- 原系统 `/9001003010/TP/Task/TaskList` 已完成只读取证：发起人公司隐藏条件为 `SearchCondition_Initiator_CompanyId=1`；六页签依次为进行中、进行中-已完成、进行中-拒绝、进行中-已停止、进行中-已撤回、已验收，取证计数为 1020/122/318/697/2936/44505。页面保留 12 筛选、选择框加 14 列、4 排序列、默认 15 条及六档分页、11 项更多操作。与部门页不同，所有非空页签底部固定显示“完成任务、转交任务、更多操作”；空结果时全部底部隐藏。临时标题筛选和页签已恢复，原系统无业务写入。
- 本地公司发起页已补齐管理员专用“完成任务/转交任务”，直接复用受保护的真实任务接口；未选择或多选时通过 `requireOne` 明确提示，不是死按钮。零结果隐藏底部与原站一致。本地 Docker 页已实看六页签、12 筛选、14 列、真实全所数据、三枚底部按钮、空态与恢复状态，控制台错误为 0。后端门禁证明 company+initiated 仅当前数据库角色为 `admin` 可访问，经理/普通用户 403；管理员可见跨部门、跨身份发起任务，旧 JWT 在账号升降级后立即按数据库当前角色改变权限。新增 `TASK_COMPANY_CREATED_OK`；5471 modules、265/220/0、17 groups、零残留、六服务与近期日志完整验收通过。下一页继续“公司任务 → 公司接受的任务”，完整复刻仍未完成。
- 原系统“公司任务 → 公司接受的任务”已完成只读取证：准确地址为 `/9001003020/TP/Task/TaskList`，`PageId=9001003020`。待处理/进行中/完成/停止四个页签本次均为 0；页面保留 12 项筛选、勾选列加 14 个业务列、发起时间/截止时间/剩余天数/最后更新时间 4 个排序字段、默认 15 条及 10/15/20/50/100/200 六档分页。空态精确显示“没有查询到符合条件的记录 。”并隐藏整个底栏；DOM 仅定义隐藏的“更多操作”，其 11 项为新增律所费用、平台费用、内部费用、批量修改、生成授权委托书、律所函、身份证明、结算提成表、案件任务、案件日志、导出案件打印表，没有接受/拒绝/完成/转交等生命周期按钮。原系统无业务写入。
- 本页所有 `Initiator/Officer/Participant` 的 company/department 隐藏值均为空，因此本轮不得推断或记录原站 `CompanyId=1`。本地 `company + owned` 严格按当前数据库角色放行，仅 `admin` 可访问；账号晋升或降级后旧 JWT 立即按当前角色生效。自动流程证明管理员能够完整执行“拒绝 → 重派 → 交接 → 接收 → 完成 → 验收”，错误状态重复操作返回 409；前端公司接受页空态隐藏分页和底栏，非空页保留 11 项更多操作且不暴露原站不存在的生命周期按钮。下一页继续“公司任务 → 公司协作的任务”，完整复刻仍未完成。
- 原系统“公司任务 → 公司协作的任务”已完成只读取证：准确地址 `/9001003030/TP/Task/TaskList`，`PageId=9001003030`；进行中/完成两个页签取证计数为 187/11215。页面具有 12 项筛选、勾选列加 14 个业务列、发起时间/截止时间/剩余天数/最后更新时间 4 个排序字段、默认 15 条及 10/15/20/50/100/200 六档分页；当前非空页脚总计原样显示 11670 条。底部仅有“更多操作”，11 项与共享清单一致，没有个人协作生命周期写按钮。临时筛选验证空态精确为“没有查询到符合条件的记录 。”且分页和整个底栏隐藏，随后已恢复；原系统全程未执行业务写操作。
- 原页所有 `Initiator/Officer/Participant` company/department 隐藏字段均为空，不能据此声称原站存在 `CompanyId` 条件。本地 `company + collaborating` 仅允许当前数据库角色为 `admin`，账号升降级后旧 JWT 立即按当前角色生效；纯协作者只允许沟通和查看历史，任务生命周期、通用记录和调查接口旁路均被拦截。管理员自动流程已证明“接收 → 交接 → 再接收 → 完成 → 验收”，非法状态返回 409。本地浏览器实看两页签计数 2/0、12 筛选、含勾选的 15 列、仅更多操作、精确空态及底栏隐藏，控制台错误为 0；结构门禁为 `TASK_COMPANY_COLLABORATING_OK`。原系统事务中心侧栏任务分组至此已核对完毕，完整复刻仍未完成。
- 原系统事务中心侧栏只有“我的任务、部门任务、公司任务”三个分组，共 10 个任务叶子，不存在可见的独立“任务提醒”菜单。本地已同时移除静态备用菜单和动态配置中的 `task-reminders` 可见项，但通知中心仍保留该内部兼容路由；进入兼容路由时 `/tasks` 会真实携带 `reminder_only=true`，不会混入全部任务，空结果隐藏表尾。
- 后端任务提醒按当前接收人严格隔离，管理员也不会扩大个人提醒范围；提醒查询的过滤、分页和权限边界已有接口验证。当前结构审计目标为 264 个菜单节点、219 个叶子、0 个未处理路由；API 冒烟为 17 组通过且测试数据零残留。
- 因明日交付要求，新增交付/生产预检、强密钥和一次性管理员门禁、生产禁用演示数据、安全迁移包、停写一致性备份、强制 SHA256 恢复校验和 Caddy HTTPS 模板。明日可交付核心业务候选版，但整个 220 个叶子页仍未逐页证明，不能宣称全站 100% 完成。
- “客户管理 → 新建客户”已完成本轮原站只读取证、本地实现与自动验收。原站准确地址为 `/6001000/CRM/Customer/CreateUpdate`；本地按原页保留基本信息、法人信息、开票信息、控制信息四个可见区块，不展示原 DOM 中默认 `display` 隐藏且高度为 0 的“专利必填信息/附加信息”，也不虚构“所属部门”输入。客户名称必填，客户编号初始保持空白并标注“自动生成”；控制信息默认建档日期为当天、客户来源“管理者”、是否共享“否”、客户等级“立案客户”、上海市资助信息“否”，客户管理人使用真实用户目录且至少一人。
- 联系人、事项记录、客户文档三个标签的空状态分别精确为“没有查询到联系人，可以去 新建联系人”、“没有查询到事项记录，可以去 新建”、“没有查询到客户文件，可以去 上传客户文件”；三张空表分别锁定 12 列、5 列、6 列原始表头顺序。未保存时点击任一关联新增入口均打开“提示”弹窗，正文为“请先保存客户基本资料.”，按钮为“确定”。保存使用专用 `POST /customers`，服务端自动生成客户编号并强制客户权限、管理人和必填规则；通用记录创建/编辑/流转/删除不能绕过客户专用流程。
- 本轮门禁通过：前端生产构建 5471 modules、菜单结构审计 264 nodes / 219 leaves / 0 unhandled、17 组 API 冒烟且测试数据零残留。原站全程无保存、提交、上传、修改或删除。这里只证明“新建客户”本轮范围，不代表整站完全一致；下一页继续“客户管理 → 我的客户” `/6001001/CRM/Customer/CustomerList`，整体复刻仍未完成。
- “客户管理 → 我的客户”已完成原站列表及查看页只读取证。原站准确地址为 `/6001001/CRM/Customer/CustomerList`：筛选仅有客户名称、客户/当事人和禁用的客户管理人；表格为勾选列加客户编号、客户名称、案源人、客户管理人、三类日期、联系/合同/民事案件数量、代理费/待收金额、官费/未到金额、客户状态 13 个业务列，并在数据上下显示金额合计。底部只有“客户查看”和“更多操作”，后者包含客户删除、客户编辑、新增合同；分页默认 15 条并提供 10/15/20/50/100/200 六档。选中现有客户后，“客户查看”在系统页签中打开只读 `/6001001/CRM/Customer/View?customerId=1729`，显示基本信息、法人信息、开票信息、控制信息及联系人/事项记录/客户文档三个标签，未出现新增、上传或保存入口。原系统全程未执行写操作。
- 本地“我的客户”已改用专用 `GET /customers?scope=mine` 服务端查询：普通用户严格按本人案源人或管理人精确取数，避免用户名子串误命中；`admin` 按最高权限查看全所在管客户与全部字段；两类身份均排除已回收及公海客户。接口同时完成客户/当事人、名称和管理人筛选、六档分页及全量筛选结果金额合计。前端已复现选择、上下合计、原表头/日期格式、精确空态、底部按钮、更多操作和分页，并实现与原站字段结构一致的内嵌只读客户查看页；结构门禁新增 `CUSTOMER_MINE_OK`。Docker 成品浏览器已复验四个只读区块、“请选择”状态、开票地址 `test`、联系人/事项记录/客户文档三标签各自表头与空态、无写入口、关闭返回列表及控制台零错误。当前前端生产构建通过 5471 modules，菜单审计为 264 nodes / 219 leaves / 0 unhandled，17 组 API 冒烟通过且测试数据零残留。本页本轮范围已实现并验证，但整站目标仍未完成。
- “客户管理 → 个人回收站”已完成原站 `/6001002/CRM/Customer/CustomerList` 只读逐项取证。原页与“我的客户”共用三项筛选、勾选加 13 个业务列、上下金额合计和默认 15 条六档分页，但分页多末页 `»`；取证时唯一记录 `SHKH1910235` 状态显示“已删除”。非空底栏为“客户查看 / 更多操作”，菜单仅“客户恢复、进入公海”；使用临时不命中标题验证空结果只显示“没有查询到符合条件的记录 。”，整个底栏与分页隐藏，随后已清除筛选。原系统未执行恢复、进入公海或其他写操作。
- 本地个人回收站改为专用 `GET /customers?scope=recycle` 服务端范围、筛选、分页和金额合计；普通用户按本人案源人或客户管理人精确取数，`admin` 保留全所与全部字段权限。前端恢复原表头、状态“已删除”、多管理人逗号、`« ‹ 页码 › » + GO`、空态隐藏底栏和共用只读客户查看。临时客户 `9385` 在 Docker 成品中完成选择、两项菜单、只读查看、真实恢复、再次回收后真实进入公海；随后精确删除并确认 404，关键词残留为 `0`，清理后空页控制台错误为 `0`。`CUSTOMER_RECYCLE_OK`、5471 modules、264/219/0、17 组 API 冒烟及零测试残留均通过。本页本轮范围已实现并验证；下一页继续“客户管理 → 部门客户”，整站目标仍未完成。
- “客户管理 → 部门客户”已完成原系统 `/6001003/CRM/Customer/CustomerList` 只读取证。原页筛选为客户名称、客户/当事人和可编辑的客户管理人；表格为勾选加 13 个业务列、上下金额合计，取证时共 7 条且合计为 `48600.00/-7401.00`。底栏为“客户查看 / 更多操作”，更多操作仅“分配客户”；分页为 15 条默认、六档页容量和 `« ‹ 1 › » + GO`。分配弹窗标题“客户分配”，依次显示只读客户编码、只读客户名称、只读原客戶管理人、单一现客戶管理人及确定/取消。临时不命中筛选确认空态仅显示“没有查询到符合条件的记录 。”，没有合计、底栏或分页；筛选随后已恢复，原系统未执行分配或其他写操作。
- 本地新增专用 `GET /customers?scope=department`：普通用户 403，部门负责人只读取本部门在管客户且不会因跨部门第二管理人串入，`admin` 保留全所在管客户和全部字段；名称、客户类型、管理人精确筛选、服务端分页及全筛选集金额合计均已覆盖。前端复现 13 列、上下合计、六档分页、末页 `»`、只读客户查看和单选分配弹窗，真实 `PUT /customers/{id}/managers` 受负责人/部门负责人/管理员权限保护；部门负责人真实分配后已恢复，测试数据零残留。Docker 成品浏览器已复验非空列表、分页、分配弹窗四字段、四区块只读查看，以及无合计/底栏/分页的精确空态。`CUSTOMER_DEPT_OK`、5471 modules、264/219/0、17 组 API 冒烟、`VERIFY_LOCAL_OK` 和六服务运行均通过；下一页继续“客户管理 → 部门回收站”，整站目标仍未完成。

- 对 264 个本地菜单节点继续逐页核对原系统字段、按钮、弹窗、数据范围及视觉差异；财务 44 个叶子页已完成结构覆盖，原系统事务中心的 10 个可见任务叶子已完成本轮核对，不再把内部兼容路由 `task-reminders` 计作菜单叶子；其余页面仍需按原站实际数据逐页核对。

## 后续实现

- 微信小程序端页面
- 配置真实 Dify 地址与密钥，验证外部模型生成结果、会话续接与异常重试
- 客户生产界面的“客户删除”已改为受保护的 `/customers/{id}/recycle`，不再物理删除；通用管理员 `DELETE /records/{id}` 仍被本地端到端测试和临时数据清理大量使用。正式生产前必须把 customer 物理删除改为测试环境专用清理能力或增加明确环境门禁，避免 API 客户端绕过回收站生命周期。
- 生产上线仍需：接入真实域名 HTTPS、首次强制修改管理员密码、引入 Alembic 版本迁移，并在目标 Linux 服务器完成一次备份恢复演练
- 按参考系统逐页视觉对比和最终验收
- 2026-07-15 再次验收页面独立滚动：在 1280×720 的 `http://127.0.0.1:5173/` 控制台长页面中，左侧导航从 `240` 单独滚动到 `272` 时右侧保持 `360`；右侧从 `180` 滚动到 `360` 时左侧保持 `240`。根页面不滚动、顶部栏固定，`INDEPENDENT_SCROLL_OK` 继续通过；5173 已确认是本项目当前 Vite 开发服务，Docker Web 的 80 端口也健康。
- 财务中心“开票管理 → 我的开票”已完成首轮原系统只读对照和本地闭环：八项筛选、十列加末尾占位列、有效发票全局/本页合计、15 条六档分页、查看详情、撤回、撤回/作废删除线及真实 Excel 导出均已实现；结构门禁 `FINANCE_INVOICE_MINE_OK` 和 15 组 API 冒烟通过。本轮临时发票记录 `2958/2959/2960` 已精确删除并逐条确认 404；整体逐页复刻目标仍未完成。
- 财务中心“开票管理 → 待处理开票”已完成首轮原系统只读对照与本地闭环。原站真实入口 `/5001006002/FAM/InvoiceProcess/PendingList` 已确认九项筛选、勾选列加八个业务列和末尾占位列、上下两处开票/高开合计、默认 15 条及 10/15/20/50/100/200 六档分页、导出全部/导出选中；列表只显示“待开票”，行内“开票”和请票单号均进入 `/FAM/InvoiceProcess/Process?invoiceId=...`。
- 本地新增待处理专用服务端列表范围和申请人筛选、真实 `.xls` 导出、完整“开票处理”页面及专用驳回接口。处理页按原站复现开票结果、领票人、高开金额、发票号、开票意见、发票内容、申请信息、服务项和合同信息；空发票号提示“请输入发票号码.”，空驳回原因提示“请输入驳回原因.”。开票写入领票人、高开金额、开票意见和流水，驳回回到“已驳回”并保留流程历史；通用接口不能绕过。
- 浏览器临时记录 `3083/3084` 分别完成真实驳回和真实开票，选中导出成功；两条记录及 `BROWSER-INV-0715` 开票流水随后精确清理并确认 404/零残留。门禁新增 `FINANCE_INVOICE_PENDING_OK`；统一验收通过 5471 modules、265/220/0、44 个财务原布局路由、15 groups passed、测试数据零残留及 Docker Compose 配置，六个容器运行正常且近期严重日志为 0。整体逐页复刻目标仍未完成。
- 财务中心“开票管理 → 公司开票”完成首轮原系统只读逐项对照。原站真实入口 `/5001006003/FAM/InvoiceProcess/InvoicedList`，页面为三列九项筛选、勾选列、操作加十一个业务列和末尾空占位、表头下全局开票/高开合计、页尾当前页合计、默认 15 条及 10/15/20/50/100/200 六档分页，底部依次为“导出全部/导出选中”。非撤回/作废行提供“查看、作废、修改发票号、修改发票日期”四个入口；原站仅执行页面、弹窗和脚本的只读取证。
- 本地公司开票已改为专用服务端范围、九项筛选和分页，不再使用通用 `/records` 前端过滤；新增公司格式 SpreadsheetML `.xls` 导出、完整“开票信息”查看/作废页面，以及 `/finance/invoices/{id}/change-number`、`/change-date` 两个专用接口。修改发票号同步原开票流水并防重复，修改日期同时更新申请日期、开票日期和流水日期，二者均写入流程历史；撤回/作废记录被禁止继续修改。空值提示与原站一致为“请输入新发票号码.”、“请输入发票申请日期.”、“请输入发票开票日期.”和“请输入作废原因.”。
- 浏览器临时记录 `3208` 完成列表上下合计、四个操作入口、查看页、发票号真实修改、申请/开票日期真实修改、完整作废页、空值提示及真实作废；作废后行内只保留“查看”。准备阶段未提交成功的临时记录 `3207` 与验收记录 `3208` 随后均按 ID 删除并确认 404，`BROWSER-COMPANY-INVOICE` 关键词记录与关联流水复查均为 0。自动门禁新增 `FINANCE_INVOICE_COMPANY_OK`，15 组 API 冒烟覆盖公司查询/导出、重复号拦截、号码/日期同步、历史留痕、作废后禁止修改及零残留。下一页继续“开票管理 → 未开票”；整体 220 个叶子页面仍未全部逐页证明，不能宣称完整复刻完成。
- 财务中心“开票管理 → 未开票”已完成首轮原系统只读逐项对照。原站真实入口 `/5001006004/FAM/CaseFee/InvoceCaseFeeList` 的十五项筛选、费用类型多选树、勾选列加十八个业务列和末尾占位、金额/开票金额/到账金额/付款金额上下合计、默认 15 条六档分页、“导出选中/导出全部”及十项“更多操作”均已确认；原站未执行写操作。
- 本地新增 `/finance/case-fees/invoice-status` 与 `/export` 专用接口，将案件费用、发票、回款分配和付款流水真实关联；支持个人/公司范围、十五项筛选、服务端分页、四项合计和 SpreadsheetML `.xls`。发票申请显式保存 `case_fee_ids`，避免同案多笔费用被错误合并；通用状态不再误显示为付款状态。浏览器已验证未开票/已开票切换、费用树、导出菜单及空选提示、发票凭证弹窗、清空默认值和左右独立滚动。
- 本轮浏览器验收记录 `3483–3487` 及关联流水 `463` 已逐条精确删除，五条业务记录复查均为 404、关联流水为 0。结构门禁新增 `FINANCE_INVOICE_UNISSUED_OK`；统一验收通过 5471 modules、265 nodes / 220 leaves / 0 unhandled、44 个财务原布局路由、15 groups passed、测试数据零残留及 Docker Compose 配置。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，最近严重日志为 0。下一页继续“开票管理 → 公司未开票”；整体目标仍未完成。
- 财务中心“开票管理 → 公司未开票”已完成首轮原系统只读逐项对照。原站 `/5001006005/FAM/CaseFee/InvoceCaseFeeList` 当前为 491 条、全局金额 `7050070.35`，个人 `/5001006004/...` 同时只有 2 条、合计 `11000.00`，证明两页必须使用不同数据范围；公司页的十五项筛选、十八列加末尾占位、四项上下合计、15 条六档分页、导出菜单和十项更多操作与个人页同构，原站未执行写操作。
- 修正本地 `scope` 参数原先未生效的问题：`mine` 现在严格限定费用负责人为当前账号，`company` 在管理员下保留全所范围。API 冒烟新增第三方负责人费用，锁定公司未开票 `685.00`、个人未开票 `410.00` 及发票作废后范围恢复，防止管理员个人页再次退化为全所列表。
- 浏览器临时费用 `3615/3616` 验证公司页显示两名负责人共 2 条、合计 `10303.00`，个人页仅显示管理员负责人 `5101.00`；选中导出、空选提示、更多操作、案件入口和案件日志均通过。表格改为固定布局，到账单位长文本由 275px 恢复为原站 155px 并使用省略显示，全部业务列宽与原站一致。两条费用已精确删除并确认 404/关键词 0；统一验收、15 组冒烟、零残留、Docker 六服务健康及严重日志 0 均通过。下一页继续“结算管理 → 待结算”，整体目标仍未完成。

## 2026-07-16 客户管理补充

- “客户管理 → 部门回收站”已完成原站 `/6001004/CRM/Customer/CustomerList` 取证与本地闭环。原页为客户名称、客户/当事人、可编辑客户管理人三项筛选，勾选列加 13 个业务列，状态显示“已删除”，多管理人以半角逗号连接；非空页显示上下金额合计、默认 15 条及 10/15/20/50/100/200 六档分页，并保留 `« / ‹ / 1 / › / »`，底栏仅“客户查看”和“客户恢复/进入公海”。精确空态为“没有查询到符合条件的记录 。”，且不显示合计、底栏或分页。
- 本地新增 `GET /customers?scope=department_recycle`：普通用户拒绝，部门负责人只看本部门已回收客户，`admin` 保留全所数据；名称、客户类型、管理人精确筛选、服务端分页与金额合计已覆盖。客户恢复、重新回收、进入公海均使用受保护专用接口，跨部门操作返回 404；通用记录编辑和流转不能绕过客户生命周期。前端已复现 13 列、状态、多管理人分隔符、只读查看、双动作菜单、空态隐藏规则及末页 `»`。
- 统一验收通过：前端 5471 modules、菜单审计 264 nodes / 219 leaves / 0 unhandled、Python 编译、17 组 API 冒烟、测试数据零残留、`VERIFY_LOCAL_OK`；Web 镜像已重建并启动。本地浏览器复验部门回收站空态无合计、底栏和分页。
- 原系统取证事故记录：检查“客户恢复”时误判其有确认步骤，实际对 `SHKH1910235` 立即执行了恢复。发现后锁定唯一内部记录 `2148`，从“我的客户”精确删除回部门回收站；复核“我的客户”结果为 0，部门回收站恢复 3 条且状态“已删除”。原系统“最后修改日期”因此由 `2026-04-16` 不可逆更新为 `2026-07-16`。此事故不得写成原站全程无写入；后续原站操作按钮只允许静态脚本或只读证据检查，不再点击写动作。
- 下一页继续“客户管理 → 公司客户” `/6001005/CRM/Customer/CustomerList`；整站仍未完成。

## 2026-07-16 公司客户

- “客户管理 → 公司客户”已完成原站 `/6001005/CRM/Customer/CustomerList` 只读取证。PageId 为 `6001005`；筛选为客户名称、客户/当事人和可编辑客户管理人，列表为勾选加 13 个业务列。取证时共 411 条，首行 `SHKH1910235` 状态“已删除”，证明公司客户同时包含正常与已回收客户；公海客户仍使用独立菜单。全局合计为 `2727170.67 / 5300956.13`，当前页合计为 `52394.00 / 319293.00`。多管理人用半角逗号连接。
- 原页默认 15 条并提供 10/15/20/50/100/200 六档分页，分页为 `« / ‹ / 页码 / … / 末页 / › / » + GO`。非空底栏仅“客户查看”和“更多操作”，更多操作唯一项目为“分配客户”；分配弹窗与部门客户同构，依次为客户编码、客户名称、原客戶管理人、现客戶管理人及确定/取消，现管理人为单值。客户查看为基本信息、法人信息、开票信息、控制信息及联系人/事项记录/客户文档三个标签的完全只读页。精确空态“没有查询到符合条件的记录 。”不显示合计、底栏或分页。原站本页未执行任何写操作。
- 本地新增 `GET /customers?scope=company`：仅 `admin` 使用公司全所范围，包含正常与已回收客户、排除公海客户，保留管理员全部字段；名称、客户类型、管理人精确筛选、服务端分页与全筛选集合金额合计均已覆盖。普通用户和部门负责人返回 403；管理人子串不能误命中。公司客户真实跨部门分配及恢复原管理人已由冒烟验证，通用客户状态流转旁路保护继续通过。
- 前端 `customer-company` 已接入 `scope=company`，复现原式上下合计、六档分页和末页 `»`、正常/已删除混合状态、半角逗号管理人、只读查看、唯一分配菜单、四字段单值分配弹窗及精确空态隐藏规则。新增结构门禁 `CUSTOMER_COMPANY_OK`。
- Docker 成品浏览器以临时客户 `10362/10363` 验证正常与已删除同页、金额 `321.45/67.89` 与 `11.10/-2.20`、选择高亮、分配弹窗、只读详情及空态；未提交分配。两条临时客户已删除，ID 均返回 404，关键词 `UI-COMPANY-1784194651` 残留为 0。
- 完整验收通过：5471 modules、264 nodes / 219 leaves / 0 unhandled、`CUSTOMER_COMPANY_OK`、17 组 API 冒烟、测试数据零残留和 `VERIFY_LOCAL_OK`。下一页继续“客户管理 → 公海客户” `/6001006/CRM/Customer/CustomerList`；整站仍未完成。

## 2026-07-16 公海客户

- “客户管理 → 公海客户”已完成原站 `/6001006/CRM/Customer/CustomerList` 只读取证。PageId 为 `6001006`；当前管理员账号下为 0 条，筛选为客户名称、客户/当事人和可编辑客户管理人，列表模板为勾选加 13 个业务列。精确空态为“没有查询到符合条件的记录 。”，且隐藏金额合计、客户查看、更多操作和分页。静态页面与官方脚本确认非空底栏更多操作仅有“拾回”，调用 `CustomerClose` 且无确认；原站未点击该写动作。只读详情确认基本信息、法人信息、开票信息、控制信息和联系人/事项记录/客户文档三标签。非空默认 15 条及六档分页来自同族共享模板证据，因原站当前空列表未直接显示。
- 后端新增 `GET /customers?scope=public`：仅返回状态“公海”的全所客户，`admin`、负责人和普通用户可读，其他角色 403；名称、类型、管理人精确筛选、服务端分页/合计和角色字段脱敏均已覆盖。未领取前即使仍是旧管理人也不能编辑、回收或修改管理人；公海只能通过专用 `claim` 领取。领取使用 PostgreSQL 行锁，原子改为“潜在”、负责人/管理人为领取人并同步领取人部门，重复领取返回 409。
- 前端 `customer-public` 已接入 `scope=public`，复现三筛选、13 列、半角逗号管理人、只读四区块三标签详情、唯一无确认“拾回”动作、上下合计、同族分页和精确空态隐藏规则。新增结构门禁 `CUSTOMER_PUBLIC_OK`。
- Docker 成品浏览器以临时客户 `10564`（`UI-PUBLIC-1784195440`）真实验证非空列表、只读详情、唯一“拾回”菜单和领取成功后精确空态；API 复核领取后进入管理员“我的客户”且状态为“潜在”。临时记录随后精确删除，ID 为 404、关键词残留为 0。
- 完整验收通过：前端 5471 modules、菜单审计 264 nodes / 219 leaves / 0 unhandled、`CUSTOMER_PUBLIC_OK`、Python 编译、17 组 API 冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 最近严重日志匹配为 0。下一页继续“客户管理 → 我的共享客户” `/6001007/CRM/Customer/CustomerList`；整站仍未完成。

## 2026-07-16 我的共享客户

- “客户管理 → 我的共享客户”已完成原站 `/6001007/CRM/Customer/CustomerList` 只读取证。PageId 为 `6001007`，查询表单以 GET 提交到同页；隐藏初始页码/页长为 `1/15`。三项筛选为客户名称、客户/当事人（默认客户）、客户管理人，其中 BusinessOwner 与 BusinessOwnerName 都禁用且当前显示空值。列表为勾选加 13 个客户业务列。当前管理员账号为 0 条，精确空态“没有查询到符合条件的记录 。”，且客户查看、更多操作、合计和分页全部不可见。
- 空页隐藏模板保留“客户查看”和“更多操作”外壳，但更多操作菜单 `ul` 为空，右键菜单也只有客户查看；本地按质量纪律不制造空下拉死按钮，非空页仅保留真实“客户查看”。直接读取 `/6001007/CRM/Customer/View?customerId=17063` 确认基本信息、法人信息、开票信息、控制信息及联系人/事项记录/客户文档三标签，主字段全部禁用。事项和文档空模板中的“新建/上传客户文件”无内联或元素级事件证据，本地仅复现为不可点击提示文本，不开放共享写入口。非空金额合计、六档分页和多管理人分隔符因原站当前空列表无法直接观察，明确沿用同族模板作为间接证据。
- 后端新增 `GET /customers?scope=shared`：管理员审计全所存在共享接收人的正常客户，负责人/普通用户只看精确共享给本人的正常客户；排除公海与已回收，支持名称、类型、管理人精确筛选、服务端分页、全筛选集合合计和字段脱敏。修复了 JSON 字符串子串匹配导致 `ann` 误命中 `joann` 的越权，并使跨部门共享收件人在各数据范围下都能读取详情和附件但不能写入。
- `/share` 现在校验启用用户、拒绝负责人/管理人冗余共享、重复共享保持幂等，并以 PostgreSQL 行锁保护并发追加；共享时同步 `is_shared=是`，领取、进入公海和回收时清空共享关系并设为“否”。共享收件人不能编辑客户、分配/二次共享、回收/释放、新增删除联系人或事项、写沟通日志、上传删除附件。
- 同步修复两条旁路：沟通日志旧操作人在降为共享或撤权后不能继续 PATCH/DELETE；客户关联 Dify 智能文档只允许客户负责人、管理人或管理员访问，共享人不能读取历史全文、下载、创建、重试、编辑、回写或删除，旧创建人撤权后也不能绕过客户权限。相关边界、跨部门经理、附件只读、用户名子串、并发共享和生命周期清权均进入 17 组冒烟。
- Docker 成品浏览器以临时客户 `11086`（`UI-SHARED-1784196888`）、用户 `978` 和附件 `2434` 验证 13 列、金额 `345.67/-45.60`、锁定空管理人筛选、唯一客户查看、四区三标签、联系人/事项/文档真实只读数据及无更多操作。客户、附件和用户随后精确删除，记录与附件为 404，共享关键词和用户残留均为 0。
- 完整验收通过：5471 modules、264 nodes / 219 leaves / 0 unhandled、`CUSTOMER_SHARED_OK`、Python 编译、17 组 API 冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 最近严重日志为 0。下一页继续“客户管理 → 最近联系的客户” `/6001008/CRM/Customer/CustomerList`；整站仍未完成。

## 2026-07-16 最近联系的客户

- 原站 `/6001008/CRM/Customer/CustomerList` 当前管理员为 0 条。只读取证确认 GET 表单、三项筛选、禁用且空的客户管理人、勾选加 13 列、精确空文案，以及空时隐藏合计、底栏和分页。隐藏操作模板仅为“客户查看”和“更多操作 → 客户编辑”；直接 View 页确认四区块字段全部禁用，并有“联系人 / 事项记录 / 客户文档”三个页签。原站未执行任何写操作。
- 本地新增 `scope=recent_contact`，按可见客户中有效 `last_contact_at` 投影，排除已回收客户，使用统一解析后的时间轴倒序，并提供服务端名称/类型/管理人筛选、分页、合计和字段权限。管理员全所数据权限不降级；普通用户继续受通用客户可见范围约束。
- 联系时间与次数改由真实沟通/事项记录统一重算：新增联系人不再误更新；较旧沟通不会覆盖较新时间；修改、删除最新记录后会回退到剩余最大事件时间；`Z`、`+08:00` 和无时区值按统一时间轴排序。新建客户和通用记录修改均不能伪造、清空或覆盖 `notes`、`last_contact_at`、`contact_count`。
- 前端已接入专用 API，恢复禁用空管理人、服务端六档分页、精确空态、只读详情及唯一编辑菜单。Docker 浏览器临时客户 `11300–11302` 验证新旧联系排序、无联系客户排除、客户查看、真实编辑保存和空态隐藏；临时数据已全部删除，关键词残留为 0。
- 新增 `CUSTOMER_RECENT_CONTACT_OK`；统一验收通过 5471 modules、264 nodes / 219 leaves / 0 unhandled、17 组 API 冒烟、`SMOKE_CLEANUP_OK` 和 `VERIFY_LOCAL_OK`。原站当前账号无非空数据，因此“普通事项是否都计为联系”、是否存在额外时间窗口、公海及各角色精确范围仍属未直接证明的业务语义，后续获得原站角色/非空样本时继续校准。
- 下一页继续“客户管理 → 最近更新的客户” `/6001009/CRM/Customer/CustomerList`；整站仍未完成。

## 2026-07-16 最近更新的客户

- 原站 `/6001009/CRM/Customer/CustomerList` 完成只读取证：GET 表单、PageId `6001009`、锁定且为空的客户管理人、勾选加 13 列、上下金额合计、默认 15 条与 10/15/20/50/100/200 六档分页、`« / ‹ / 1 / › / » + GO`、只读客户查看，以及唯一“客户编辑”。原页当前恰有 1 条 `SHKH1910235`，状态“已删除”、最后修改日期 `2026-7-16`，正是当前管理员曾恢复再回收的记录；公司客户页同日另有两条由其他人更新的正常客户却未进入本页，证明这是当前登录人最后修改投影而非全所更新时间列表。安全无命中查询确认精确空态隐藏合计、操作、总数和分页，查询条件随后已恢复；原站本轮没有写操作。
- 后端新增 `scope=recent_update`：严格匹配当前身份最后修改者，管理员也保持该业务语义；包含已回收、排除公海，非管理员再叠加客户可见范围；按权威 `BusinessRecord.updated_at DESC, id DESC` 服务端排序、筛选、分页和合计。历史无 `last_modified_by` 的记录从限定客户修改动作的最新 `WorkflowEvent.operator` 回退，不把附件事件误判为客户更新。
- 新建、CSV 导入、通用资料编辑及领取/释放/共享/回收/恢复、联系人、管理人、事项、沟通等现有客户行变更均维护服务端 `last_modified_by`；创建请求会剥离伪造修改人/日期，通用 PATCH 伪造或清空系统维护字段返回 409。最近联系页同时修复公海客户误入旁路。
- 前端接入专用服务端数据源，移除通用前 100 条和客户端排序；恢复锁定空管理人、已删除状态、半角逗号、上下合计、精确空态、四区三标签只读详情、唯一编辑菜单、六档分页和末页 `»`。“最后修改日期”只使用权威 `updated_at`，不信任扩展数据中的旧日期字段。结构门禁新增 `CUSTOMER_RECENT_UPDATE_OK`。
- Docker 浏览器以临时客户 `11641–11643` 和用户 `1030` 验证：管理员仅见自己最后修改的已回收客户，其他用户同日修改客户和公海客户被排除；金额 `111.00/-10.00`、多管理人、已删除、客户查看、客户编辑真实保存和精确空态均通过。三条客户和账号已精确删除，记录与用户关键词残留均为 0。
- 完整验收通过：5471 modules、Python 编译、264 nodes / 219 leaves / 0 unhandled、17 组 API 冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。直接原站证据只证明恢复/回收会成为最近更新；联系人、事项、沟通和导入是否也全部计入原站尚无逐路径非空样本，本地统一按真实客户行变更标记属于合理实现，不能写成全部已由原站直接证明。
- 下一页继续“客户管理 → 公司回收站”；整站仍未完成。

## 2026-07-16 公司回收站阶段交接

- 原站 `/6001010/CRM/Customer/CustomerList` 已完成只读取证：当前 17 条全所已删除客户，三项筛选、勾选加 13 列、上下金额合计、默认 15 条及六档分页、末页 `»`、四区三标签只读详情，以及“客户恢复/进入公海”双动作均已确认；精确无匹配时隐藏合计、底栏和分页。原站未执行任何写操作。
- 后端新增受管理员保护的 `scope=company_recycle` 全所回收范围、服务端筛选/分页/合计和显示姓名安全解析。创建与通用编辑现共用 `CUSTOMER_SYSTEM_DATA_FIELDS`，回收/恢复/释放/领取/共享/联系/修改审计字段不能伪造；负责人、管理人和生命周期变更必须走专用接口。
- 前端已复现已删除状态、半角逗号管理人、可编辑管理人筛选、金额合计、精确空态、只读详情、恢复/进入公海和完整分页。服务端分页在末页最后记录被移出后会自动收敛到新的末页，避免停留在无分页的假空态；结构门禁为 `CUSTOMER_COMPANY_RECYCLE_OK`。
- “我的客户 → 客户删除”已由物理删除改为 `/customers/{id}/recycle`，确认文案明确可在回收站恢复或进入公海，生产客户 UI 已无通用硬删除调用。浏览器临时客户 `12363` 已证明“我的客户删除 → 公司回收站已删除”；临时客户 `12229–12244` 已证明 16 条两页、第二页恢复自动回第一页、进入公海、只读详情和精确空态，全部清理后关键词残留为 0。
- 完整验收通过：5471 modules、Python 编译、264 nodes / 219 leaves / 0 unhandled、17 组 API 冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`；六个 Docker 服务运行，API/Web/PostgreSQL/Redis 健康，最近严重日志为 0。
- 整站目标仍继续进行；下一页为“客户管理 → 客户利益检索”。管理员通用 `DELETE /records/{id}` 对 customer 的物理删除仍被本地测试清理依赖，生产发布前必须改成测试专用清理能力或增加环境门禁。

## 2026-07-16 客户利益检索

- 原站 `/6001011/CRM/Customer/CustomerSearching` 已完成严格只读取证。页面标题为“客户利益冲突检索”，包含“1.输入完整企业名称 / 2.企业信息”两步、固定温馨提示、企业名称与“检索”按钮；回车不触发查询，非完整名称精确提示“未找到该企业基本信息.”。完整名称命中后只显示企业名称、最新立案号、最新立案日期、原告、被告、第三人、我方客户、客户管理人八个只读字段，没有风险表、记录列表、分页、抽屉或返回按钮。原站空名称会意外泄露记录，本地没有复制该缺陷；原站本轮没有写操作。
- 后端新增 `GET /customers/conflicts?name=`，执行标准化后的完整企业名称等值匹配，并在全所案件中按立案日期、记录 ID 选择最新案件；返回固定最小字段，不暴露内部记录 ID。原告/被告/第三人兼容现有案件字段，客户管理人按所检索企业的最新有效客户记录解析，而不是错误沿用案件我方客户。空名称为 422；非管理员必须具有独立 `customer-conflict` 叶子权限，管理员最高权限不受配置降级。
- 前端新增独立 `CustomerConflictPage` 及测量样式，复现两步状态、固定提示、单列八字段、原站日期去零格式和按钮点击语义；移除了此前臆造的风险等级、冲突记录、详情抽屉和分页。无叶子权限时菜单隐藏且深链接拒绝；并发查询、失败和旧结果不会污染当前页面。结构门禁新增 `CUSTOMER_CONFLICT_OK`。
- Docker 成品浏览器以案件 `12806/12807`、客户 `12803/12804`、合同 `12805`、用户 `1158` 完成回归：回车不触发、部分名称未找到、空名称只提示输入、完整被告命中最新案件 `UIC-CASE-NEW-200158` 与日期 `2026-7-6`；同案查询被告显示“利益对方管理员”，查询原告显示“管理者”，证明管理人随所检索企业变化。5 条业务记录与 1 个用户已按依赖顺序精确删除，全部记录为 404、两项关键词查询未命中、用户残留为 0。
- 完整验收通过：前端 5471 modules、Python 编译、264 nodes / 219 leaves / 0 unhandled、`CUSTOMER_CONFLICT_OK`、17 组 API 冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。Web 镜像已重建，整站目标仍在进行；客户管理 12 个叶子入口阶段完成，下一目标先对“案件中心”首个叶子进行原站只读取证，不能在未取证前宣称完全一致。

## 2026-07-16 案件中心首叶：新建刑事案件（基本信息）

- 原站 `/1001000002/Legal/Case/Create?caseTypeId=120` 已严格只读取证：独立“新建案件”页，三步为基本信息/当事人信息/司法机关信息；首步依次为客户、客户诉讼地位、合同号、罪名、案件名称、案件阶段、经办律师、律师助理，默认诉讼地位“被告人/犯罪嫌疑人”、阶段“待分配”、经办律师和助理为当前管理者，且仅有“下一步”。空表点击精确弹出“提示 / 请选择客户. / 确定”。原站没有路由弹窗、无合同提示条或保存/取消 footer；本轮未在原站写入数据。
- 原站“新建案件”真实子叶只有刑事案件、行政案件及国家赔偿、法律顾问、仲裁四项；本地已移除错误可见的“民事争议”，旧 capability 仅兼容历史配置并由启动迁移隐藏。当前可见菜单为 263 nodes / 218 leaves / 0 unhandled。
- 前端刑事首屏已改为独立页面，标题、三步条、八字段顺序、默认值、唯一下一步及精确空值提示均经 Docker Chrome 复验；不存在路由 Modal footer 或保存后空白页。第二、三步目前仅为兼容骨架，因缺少原站刑事非空样本，不能宣称完全复刻。
- 后端新建案件现强制四种真实类型、叶子权限、有效审批合同与合同所属启用部门、有效负责人、非空罪名/案由与经办律师、固定“新案待分配”及唯一案号并发 409；非管理员负责人固定为当前用户，部门始终取合同。新增当事人专用写入口；案件通用 PATCH 和 transition 均返回 409，不能绕过专用生命周期。
- 完整验收通过：5471 modules、Python 编译、`CASE_NEW_CRIMINAL_STEP1_OK`、263/218/0、17 groups passed、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。API/Web 镜像已重建，六服务运行，API/Web/PostgreSQL/Redis 健康，最近严重日志 0。整站目标仍未完成；下一步继续取证并实现刑事案件当事人、司法机关两步及阶段式保存/跳转。

## 2026-07-17 新建行政案件及国家赔偿流程推进

- 本地行政案件新建流程已从刑事复用中拆出关键差异：默认客户诉讼地位为“原告/申请人”，可选“原告/申请人、被告/被申请人、第三人”；客户会按所选诉讼地位预填到对应当事人组；首步提交保留 `right_type`；完成后跳转行政案件列表，不再固定进入刑事案件列表。
- 行政案件第三步不再展示刑事专用公安机关和三级检察院字段；后端新增行政案件客户诉讼地位校验、行政司法阶段至少一个法院信息校验，并拒绝行政案件提交刑事公安/检察院字段。第二步“直接完成”入口对行政案件隐藏，避免跳过司法机关信息。
- 自动门禁已补行政案件结构断言和 API 冒烟路径：创建行政案件、校验非法诉讼地位、提交原告/被告/第三人、拒绝空司法机关、拒绝刑事字段混入、通过一审法院信息完成三步。2026-07-17 本轮已通过 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1`：前端 5471 modules、Python 编译、`CASE_NEW_ADMINISTRATIVE_FLOW_OK`、`263 nodes / 218 leaves / 0 unhandled`、17 组 API 冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。
- 本轮修正验收稳定性：`scripts/smoke-api.py` 默认 API 地址改为 `http://localhost:8000`。原因是本机 Windows 上 `127.0.0.1:8000` 曾出现端口代理残留，导致 API 请求没有命中 Docker Postgres，从而让任务提醒/交接规则误报；`localhost` 和 `::1` 已验证写入 Docker Postgres。Docker 六服务运行，API/Web/PostgreSQL/Redis 健康，API/Web/Worker 最近 10 分钟严重日志匹配为 0。
- 未证明：仍未取得原站行政案件非空样本，无法宣称行政案件所有字段初始值、阶段动态隐藏和列表视觉已完全一致；当前结论是本地行政新建三步业务闭环已实现并通过自动验收，整站目标仍未完成。
## 2026-07-21《新系统测试》问题整改

- [已完成并验证] 浏览器刷新不再退出系统；测试机使用 `http://oa-test.sunhold.local/` 可避开浏览器对公网 IP 地址的风险提示，刷新后登录态与已打开页签均保留。
- [已完成并验证] 员工管理同时读取人事档案与系统账号，未建立独立人事档案的 `admin` 也会显示为员工，并可查看、修改账号资料。
- [已完成并验证] 新建员工保存后提供“继续新建员工”，会清空上一名员工的表单和记录 ID，能够连续新增；未在测试服务器制造验收数据，完整保存闭环由 API 冒烟测试证明。
- [已完成并验证] 主内容区新增可关闭的多页面页签，菜单跳转会累计页签，切换、关闭、刷新恢复均已在测试服务器验证。
- [已完成并验证] 岗位角色权限由宽泛文本拆分为客户与合同、案件与调查、任务与文档、财务与用印、组织与系统五组具体业务动作；菜单、数据范围和敏感字段仍由系统角色权限统一控制。
- [已完成并验证] 客户列表客户编号列加宽，完整编号与客户名称不再重叠；客户管理人继续按当前账号可见范围返回。
- [已完成并验证] 合同新建的客户字段改为可检索的真实客户下拉；从客户页发起时自动带入客户名称和编号；后端拒绝未注册或无权查看的手工客户名，下一步校验失败会给出明确提示。
- [验证证据] `verify-local.ps1` 全量通过：前端 5472 modules、Python 编译、263 nodes / 218 leaves / 0 unhandled、362 个前端调用匹配 FastAPI、18 组业务冒烟、零测试数据残留、`VERIFY_LOCAL_OK`。
- [测试服务器] 发布版本 `20260721-102324` 已运行，六个容器正常；页面验收确认管理员员工行、刷新保持登录、多页签累计、完整客户编号、客户带入合同向导和细分权限矩阵。
- [二次布局整改] 客户列表全部业务列已统一为表头、数据行和合计行同列居中，覆盖客户编号、名称、案源人、管理人、日期、数量、金额和状态；代理费、官费的顶部合计、数据金额和底部合计均使用正确列索引并居中。测试服务器右侧金额区截图确认四层中心线一致，完整回归仍为 `VERIFY_LOCAL_OK`。
## 2026-07-21 合同审批人选择与管理员创建闭环

- 修复合同新建第二步“审批人”无法选择：人员目录 API 现在明确返回 `is_active`，前端仅排除明确停用的账号，兼容旧响应，不再因字段缺失误删全部可选人员。
- 只有 `admin` 在审批人字段旁显示“新增审批人”；可填写用户名、姓名、部门、审批角色和初始密码，创建成功后新账号立即加入目录并自动选中。
- 新建账号默认启用并强制首次登录修改密码。普通用户既看不到创建入口，也无法绕过页面调用 `/system/users`；后端统一返回 403。
- 自动验收已覆盖：人员目录启用状态、管理员创建用户、首次改密、停用账号阻断、普通用户创建 403、测试用户清理。完整 `verify-local.ps1` 通过：前端 5473 modules、菜单 263/218/0、客户端 API 363、18 组业务冒烟、零测试数据残留、`VERIFY_LOCAL_OK`。
- 测试服务器浏览器实测：`admin` 登录后，审批人下拉显示 9 名启用人员，“新增审批人”弹窗完整显示账号、姓名、部门、角色、初始密码及首次改密说明；未实际创建测试人员。浏览器验收产生的合同草稿 `16 / HT20260721134006` 与 1 个附件已精确清理，复查残留为 0。

## 2026-07-21《新系统测试1》四项问题闭环

- [已完成并验证] 合同客户选择器只显示客户名称，不再拼接客户编号；对名称执行 NFKC、首尾空白及大小写规范化去重，测试库中既有两条同名“测试客户1”不会再显示两个选项。后端在客户新建、通用改名、关键字段变更提交和审批落地四条路径统一拒绝同名客户，返回 409；未删除或合并任何既有客户数据。
- [已完成并验证] 员工“修改”由少量账号字段扩展为完整资料表单，覆盖人员角色、部门职务、身份证、学历学校、社保、代理人/律师证照、执业日期、合伙人日期、入离职、年休、提成、合同到期、联系方式、系统权限与启停状态；保存同时更新系统账号与人事档案。浏览器只读打开 `admin / SYS-0001` 修改弹窗，确认完整字段和“保存全部修改”，未点击保存。
- [已完成并验证] 合同审批改为且只允许一名部长。人员目录返回岗位与人员角色，前端只展示 `position/staff_role` 含“部长”的启用账号，提交值为单个账号；后端拒绝普通账号、停用/不存在账号以及多人审批。管理员可在审批步骤创建“诉讼部部长/调查部部长”账号并自动选中，普通角色无创建权限。
- [已完成并验证] 合同用印可选择合同章、公章、所函专用章、法人章、发票章、财务专用章、财务三排章；测试服务器浏览器确认实际印章库存下拉可用。
- [自动证据] `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1` 最终通过：前端生产构建、Python 编译、263 nodes / 218 leaves / 0 unhandled、363 个前端 API 调用覆盖、18 组业务冒烟、`SMOKE_CLEANUP_OK`、`VERIFY_LOCAL_OK`。冒烟明确覆盖同名客户 409、普通审批人 422、多人审批 422、单一部长审批完成和员工扩展资料落库读回。
- [测试服务器] 发布版本 `20260721-151315`；API/Web/PostgreSQL/Redis/MinIO 健康，Worker 正常。浏览器复核合同客户下拉仅显示一个“测试客户1”，员工完整修改字段齐全；复核未提交表单、未新增账号、未改变业务数据。
- [仍未证明] 本节只证明《新系统测试1》所列四项问题已闭环，不代表整个 OA 所有模块已经完成最终业主验收。测试库中的历史同名客户仍保留原数据，仅在合同选择器中去重；如需主数据合并，必须由业主确认保留主体与关联迁移规则后单独执行。

## 2026-07-21 合同完成后连续新建修复

- [已完成并验证] 修复合同完成后再次进入“合同新建”仍恢复上一份已完成合同的问题。根因是合同向导把合同 ID 持久化在浏览器本地存储中，用印申请生成后未清除，刷新、关闭页签或再次点击菜单都会恢复该 ID。
- [已完成并验证] 生成用印申请成功后立即清除向导 ID；如果浏览器仍残留旧版本保存的已完成合同，进入新建页时会识别已有用印申请并自动回到空白第一步，不再卡在完成页。
- [已完成并验证] 完成页增加“继续新建合同”，并支持再次点击当前已选中的“合同新建”菜单强制清空合同、附件、审批、用印及表单状态。该重置只影响当前浏览器中的新建向导，不删除已完成合同或用印申请。
- [自动证据] 前端生产构建通过（5473 modules）；Python 编译、菜单覆盖 263/218/0、363 个前端 API 调用覆盖、18 组业务冒烟、零测试数据残留全部通过，最终输出 `VERIFY_LOCAL_OK`。菜单审计新增连续新建、旧状态自动清理及当前菜单重选三项静态门禁。
- [测试服务器] Web 已发布为 `sunhold-web:20260721-154607`，稳定软链 `/opt/sunhold-oa/current` 指向 `/opt/sunhold-oa/releases/20260721-154607`；本轮没有后端改动，API/Worker 继续使用 `sunhold-api:20260721-151315`。六个服务正常，健康接口返回 `{"status":"ok"}`。
- [仍未证明] 本项只闭环合同连续新建故障，不代表整站最终业主验收已经完成。

## 2026-07-24 财务中心“我的请款单”资源加载崩溃修复

- [已完成并验证] 测试服务器 `finance-internal-mine` 在页面加载后因未配置原始布局数据仍直接读取 `activeRouteConfig.source` 而发生运行时异常，页面显示“页面资源加载失败”。前端已将所有该对象的条件读取改为可空安全访问，使“我的请款单”可沿用内部费用的真实数据分支而不因缺少布局配置崩溃。
- [自动证据] 仅包含 17 处可空访问替换的隔离热修复通过 `npm --prefix .\\apps\\admin-web run build`、`python -m compileall apps\\api-server\\app scripts` 与 `python .\\scripts\\audit-menu-coverage.py`（264 nodes、219 leaves、0 unhandled）。
- [仍未证明] 发布后仍需用测试服务器浏览器实际确认页面加载，并继续使用页面创建、流转和清理临时财务数据，不能据此宣称付款、核销或归档费业务闭环已完成。
## 2026-07-28 普通案件基本信息编辑分支只读台账

- [旧源码证据] `Legal.Case.js` 的统一 `CaseBasicEdit.Open()` 会按案件 ID/案号加载 `CaseBasicEdit`；归档源码实际保存五个类型专用局部页：`CivilDisputeCase_Basic_Edit.cshtml`、`CriminalCase_Basic_Edit.cshtml`、`AdministrativeCase_Basic_Edit.cshtml`、`ArbitrationCase_Basic_Edit.cshtml`、`ConsultantCase_Basic_Edit.cshtml`。PPT 仅能佐证案件详情存在“修改基本信息”场景，字段级分支以下述旧源码视图为准。因此该功能不是仅法律顾问可用，旧系统按案件类型分支。
- [民事真实编辑范围] 客户、案由、案件名称、案件阶段、经办律师、律师助理、案源人、调查线索多选、调查员；当前新系统详情仅展示客户/合同/线索/案源等关联或文本，缺少该类型专用编辑表单及专用保存接口。
- [刑事真实编辑范围] 客户、罪名/案由、案件名称、案件阶段、经办律师、律师助理、调查线索多选、调查员；当前新系统缺少刑事专用编辑表单及保存接口。已存在的当事人、司法机关、进展接口不能替代该基本信息分支。
- [行政及国家赔偿真实编辑范围] 客户、案由、案件名称、案件阶段、经办律师、律师助理、调查线索多选、调查员；当前新系统缺少行政专用编辑表单及保存接口。`right_type`、当事人和司法机关在新建流程已受专用校验，但并不构成已办结案件的基本信息修改入口。
- [仲裁旁证] 旧源码也有仲裁专用基本信息页，其共性字段与行政/刑事相近；本轮不把它混入民事、刑事、行政的完成度。
- [当前实现边界] 仅法律顾问已实现 `/cases/{id}/counsel-basic` 与对应表单；普通案件目前仅有当事人、司法机关、进展、人员分配、归档等各自专用动作。前端已隐藏会误调用顾问端点的通用“修改基本信息”按钮，避免伪完成；上述三类专用编辑接口、字段变更审计、权限/归档阻断及浏览器闭环均待后续专项实现。

## 2026-07-28 普通案件基本信息修改闭环（本地）

- [已完成] 民事、刑事、行政及国家赔偿案件新增独立 `PUT /cases/{id}/normal-basic`，不复用法律顾问接口，也不能通过通用记录 PATCH 绕过。接口按旧源码字段保存真实客户、案件名称、案件阶段、案由/罪名、经办律师、律师助理、调查员、调查线索多选；民事另含案源人，行政及国家赔偿另含权利类型。
- [权限与阻断] 仅案件负责人、同部门负责人或 `admin` 可保存；必须完成案件创建并通过立案审批；待归档审核、已归档、不可见客户/线索、回收站或公海客户、无效阶段、无效人员及错误案件类型均被服务端明确阻断。每次成功修改写入“修改普通案件基本信息”审计事件，并保留修改前摘要。
- [前端] 案件详情按案件类型显示正确的“修改基本信息”入口：法律顾问继续使用顾问专用表单，三类普通案件使用独立表单。客户、关联调查线索均为受数据范围保护的真实选择器；详情同步展示案源人、调查员、权利类型和关联线索。
- [已验证] 前端生产构建、Python 编译、菜单/静态审计均通过；Docker API 已重新构建且健康。API 冒烟已覆盖普通案件成功保存、客户与人员字段落库、审计事件、非法阶段 422、错误使用顾问接口 409、经办律师越权 403，并以 `SMOKE_API_OK: 19 groups passed`、`SMOKE_CLEANUP_OK` 完成验证。
- [仍未证明] 本轮未在浏览器逐个以民事、刑事、行政实际样本完成打开表单、修改、刷新回显和归档阻断截图；后续页面验收须以页面创建的临时数据逐类型补证并精确清理，不能以 API 冒烟代替浏览器证据。

## 2026-07-28 案件详情费用编号精确跳转（本地）

- [旧源码证据] `Legal.Case.js` 的 `CaseFees` 在案件详情内分别加载案件费用、平台费用和内部费用；费用对象具备编辑、付款标记、删除等真实办理入口。因此新系统费用编号不能只作为费用分区中的展示文本。
- [已完成] 案件详情“律所费用、平台费用、内部结算”三个子表的费用编号均统一为可点击对象，仅对真实 `finance` 记录展示；点击前以受数据范围保护的 `GET /records/{id}` 预检记录模块和可见性，再通过现有 `businessRecordDetailNavigation` 将稳定费用 ID 交给费用查询页的真实“请款单详情”弹窗。退款记录不再误混入费用编号表，避免把非费用记录交给费用详情消费端。
- [权限与异常] 缺少 ID、目标不存在、记录模块错误或当前账号无权读取时均显示服务端真实原因或明确提示，不创建假详情；费用详情本身继续通过受权限保护的 `/records/{id}` 读取。
- [已验证] 前端生产构建、Python 编译和菜单静态审计通过；最小 API 断言确认关联案件费用可按稳定 ID 读取且模块为 `finance`。本轮误跑了既有全量 API 冒烟，但已完成并清理：`SMOKE_API_OK: 19 groups passed`、`SMOKE_CLEANUP_OK`。
- [仍未证明] 尚未以浏览器实际点击三个费用子表的编号并确认费用查询页弹出对应详情；后续页面验收应使用已有可见费用或一次性页面临时数据补证，勿为已验证后端链路重复造数。

## 2026-07-28 仲裁案件基本信息修改闭环（本地）

- [旧源码证据] 归档旧源码保存独立 `ArbitrationCase_Basic_Edit.cshtml`；字段包括客户、案由、案件名称、案件阶段、经办律师、律师助理、调查线索多选和调查员，不能混入民事/刑事/行政或法律顾问编辑接口。
- [已完成代码] 新增专用 `PUT /cases/{id}/arbitration-basic` 与仲裁专用详情表单；仅仲裁案件显示入口，字段和保存动作与普通案件、顾问案件隔离。保存写入“修改仲裁案件基本信息”审计事件。
- [阻断与已验证] 服务端限制负责人/部门负责人/管理员、案件创建审批完成、非归档状态、可见有效客户和线索、有效案件阶段及在职办理人员；错误案件类型、归档案件及无权对象会真实拒绝。前端生产构建、Python 编译、菜单静态审计已通过。
- [仍未证明] 受“不得重复造数”纪律约束，本轮未创建新的仲裁页面/API临时数据；需在下一次统一浏览器验收中，以一条页面创建的仲裁临时案件补提交、刷新回显、归档阻断和清理证据。

## 2026-07-28 刑事案件新建及详情只读复核

- [结论：新建第二、三步不是确凿缺口] 旧源码 `CriminalCase_Litigant.cshtml` 明确第二步为受害人、被告/犯罪嫌疑人、第三人及各自代理人的当事人表单；当前新系统已通过专用 `/cases/{id}/litigants` 保存同类多值字段。旧 `CriminalCase_Court.cshtml` 的第三步是公安机关、各级检察院以及一审/二审/再审法院、案号、法官、书记员等司法机关资料；当前新系统已通过专用 `/cases/{id}/judicial` 保存该组字段、限制公安/检察院仅刑事案件可写，并在创建完成前阻断后续流程。因此不能因缺少旧站非空样本把新建二、三步记为未实现。
- [唯一明确剩余缺口] 旧案件详情存在创建后的独立编辑动作：`PublicSecurityUnitEdit`、`First/Second/LastProcuratorateUnitEdit`、`First/Second/Execution/LastCourtEdit`，各自以弹窗“确定”保存。新系统 `/cases/{id}/judicial` 目前只允许 `case_creation_step == litigants`，完成创建后无刑事专用“修改公安机关 / 修改检察院 / 修改法院”入口及端点；现有登记进展、开庭排期不能替代这些完整字段编辑。应作为下一项刑事案件详情闭环实施，不应通过放宽新建司法机关接口或通用 PATCH 绕过。
- [次要待证] 刑事详情的创建后当事人修改也有旧 `LitigantEdit` 证据；当前接口同样仅限新建阶段。该项与上述司法机关详情编辑同属“已创建案件资料维护”专题，暂不误报已完成或与新建第二步混为一谈。

## 2026-07-28 任务反馈附件闭环

- [已完成，待浏览器页面回归] 任务详情的参与人现在通过单一 `/tasks/{id}/feedback` 事务接口提交文字反馈和多个附件：全部校验通过后才写入沟通事件、附件、通知；任一附件格式、大小或持久化失败时会回滚已写事件与文件，不再出现“文字已保存、页面却提示失败”的半截结果或重试重复。前端显示真实提交结果。
- [已完成] 任务反馈附件的列表、上传、下载统一先按任务参与人（发起人、负责人、协作者、`admin`）授权，不能被通用记录的 owner 数据范围错误拦截；加载沟通记录和附件使用独立失败提示，不再静默清空成“暂无数据”。任务记录只接受“任务反馈附件”分类；上传人可删除本人附件，`admin` 可删除任一任务反馈附件，均留真实任务消息。
- [已完成] 特殊处理申请审批已收紧为 `admin`、任务发起人或任务所属部门的部门负责人；不同部门负责人即使知道任务 ID 也不能审批。前端按同一部门条件隐藏审批入口。
- [已验证 API 冒烟] 纯协作者跨通用记录范围提交“文字 + 两附件”、列表、下载、本人删除及通用附件入口均通过；非法后缀的多附件提交不会新增任何任务历史；跨部门经理审批返回 403，同部门经理可审批。临时任务、附件、通知和账号由既有精确清理链路复核。
- [已验证] 前端生产构建与 Python 编译通过；API 容器已重建并健康。冒烟脚本补充了参与人成功上传/列表/下载、错误分类 422 与旁观经理 list/download/upload 403 的覆盖。完整 verify 受既有客户最近更新排序断言波动影响，在第 1 组中断，未到达新增段；定向测试数据均已通过受保护测试清理接口删除并复核无残留。
