# 腾讯云宝塔轻量服务器连接说明

更新时间：2026-07-20

## 服务器身份

- 标注名称：**腾讯云宝塔轻量服务器**
- 腾讯云实例 ID：`lhins-56emf98u`
- 实例名称：`宝塔Linux面板-KtqG`
- 地域：上海二区（`ap-shanghai`）
- 公网 IP：`150.158.3.104`
- 主机名：`VM-0-17-opencloudos`
- 系统：OpenCloudOS 9.4
- 配置：2 核、2GB 内存
- 系统盘：SSD 云硬盘 50GB；首次检查时已用约 12GB、可用约 39GB
- 云端原密钥：`lhkp-gewq563o`
- 登录用户：`root`

## SSH 连接

当前电脑已配置 SSH 别名：

```powershell
ssh baota-lighthouse
```

SFTP 与文件传输：

```powershell
sftp baota-lighthouse
scp 本地文件 baota-lighthouse:'/root/目标目录/'
scp baota-lighthouse:'/root/目标文件' 本地目录
```

SSH 配置文件：`C:\Users\Administrator\.ssh\config`

```sshconfig
Host baota-lighthouse
    HostName 150.158.3.104
    User root
    IdentityFile ~/.ssh/tencent_lighthouse_150_158_3_104_ed25519
    IdentitiesOnly yes
    ConnectTimeout 10
    ConnectionAttempts 3
    ServerAliveInterval 30
    ServerAliveCountMax 3
    TCPKeepAlive yes
```

## SSH 密钥

- 新增私钥：`C:\Users\Administrator\.ssh\tencent_lighthouse_150_158_3_104_ed25519`
- 新增公钥：`C:\Users\Administrator\.ssh\tencent_lighthouse_150_158_3_104_ed25519.pub`
- 新增公钥指纹：`SHA256:XG8co537k0m49x+OVl53OjvFctXzIiiL7ULeKRsXHJo`
- 服务器 ED25519 主机密钥指纹：`SHA256:ohkfHqeBxZ4lTEBN/j6bIjqSFxzocqHccgq2JtblrFQ`
- 云端原密钥 `lhkp-gewq563o` 已保留，没有被覆盖。

服务器允许密码认证，但本轮没有取得或保存该服务器的 root 密码；当前使用独立密钥登录。私钥内容不得写入项目、Markdown、聊天记录或普通迁移包。

## 首次接入记录

- TCP 22 和 OpenSSH 9.3 正常。
- 腾讯云 TAT 自动化助手版本 `1.2.1` 在线。
- 通过 TAT 临时命令把本机公钥追加到 `/root/.ssh/authorized_keys`，未覆盖原密钥、未重启实例。
- 已使用新密钥实际登录并确认身份为 `root`。
- 腾讯云 API Secret 仅在当前进程临时使用，未写入文件或文档；由于 Secret 曾出现在聊天记录中，应尽快轮换。

## 当前资源提示

- 首次登录时物理内存约 1.9GiB，可用约 1.2GiB；1GiB Swap 已使用约 557MiB，说明服务器存在一定内存压力。
- 该机器磁盘空间比 sfhc 测试服充足，但2GB内存不适合直接运行完整 OA 六容器；部署前应先盘点现有宝塔网站和服务，或升级内存。

## 快速健康检查

```powershell
ssh -o BatchMode=yes -o ConnectTimeout=10 baota-lighthouse "echo SSH_OK; hostname; whoami; free -h; df -hT /"
```

正常结果应包含主机名 `VM-0-17-opencloudos` 和用户 `root`。

## 2026-07-19 OA 部署只读评估

- 服务器当前为 2 核、约 1.9GiB 内存、1GiB Swap；检查时已用约 685MiB 物理内存、556MiB Swap，可用内存约 1.25GiB。系统已持续运行约 115 天，负载约 0.15，CPU 当前不是瓶颈。
- 根盘 50GB，已用约 12GB、可用约 39GB，足够进行初始测试部署；正式长期运行仍应为 PostgreSQL、MinIO 文件和备份设置容量监控及清理策略。
- Docker、Docker Compose 和 containerd 当前均未安装。
- 现有服务包括宝塔面板、Nginx、MariaDB、`couple-diary.service`、`site_total.service` 等；80/443 已由 Nginx 占用，8888 为宝塔面板，127.0.0.1:8000 为现有 Couple Diary 应用。现有应用不能被 OA 覆盖或停用。
- OA 本地六容器同一时点实测约占 841MiB：API 108MiB、MinIO 179MiB、PostgreSQL 86MiB、Redis 14MiB、Web 15MiB、Celery Worker 439MiB。目标机已存在明显 Swap 使用，若完整 OA 与现有应用并行，峰值时存在持续换页、响应变慢或 OOM 的高风险。
- `compose.prod.yml` 已将 OA Web 默认绑定到 `127.0.0.1:8080`，可由现有宝塔 Nginx 反向代理，不会直接抢占 80/443；数据库、Redis、MinIO 和 API 在生产覆盖中不对公网发布端口。
- 结论：网络、系统架构、端口方案和磁盘均允许部署，但当前 2GB 配置不满足“保留现有服务并稳定运行完整 OA 六容器”的上线条件。建议至少升级到 4GB 内存后用于测试，正式交付优先 8GB；升级后再安装 Docker、生成独立生产密钥、构建镜像、配置反向代理与 HTTPS，并执行备份和完整验收。
- 本次只执行 SSH 只读盘点，没有安装 Docker、上传 OA、修改 Nginx、停止服务、创建数据库或开放端口。

## 2026-07-20 OA Docker 测试部署

- 已安装并启用 Docker Engine `29.6.2`、Docker Compose `5.3.1`，Docker 服务已设置开机启动。Docker 官方软件源在服务器网络中连接失败，最终使用腾讯云 Docker CE 镜像源完成安装；原仓库文件备份为 `/etc/yum.repos.d/docker-ce.repo.before-sunhold-20260720`。
- 当前干净源码发布目录：`/opt/sunhold-oa/releases/20260721-102324/sunhold-oa-deploy-20260721-102324`；稳定入口软链接：`/opt/sunhold-oa/current`。
- 私有配置：`/opt/sunhold-oa/current/.env.production`，权限 `0600`。数据库、Redis、MinIO、应用和初始管理员均使用服务器端随机生成的独立密钥；秘密值没有写入项目文档。
- 测试管理员用户名为 `admin`。随机一次性密码已由用户在 2026-07-20 完成修改，服务器上的 `/root/sunhold-oa-test-admin.txt` 已删除；当前密码不写入项目文档，如遗忘须按受控流程重置。
- 六个容器已启动：PostgreSQL、Redis、MinIO、FastAPI、Celery Worker、Web。API、Web、PostgreSQL、Redis、MinIO 均通过健康检查；Celery 使用 `--concurrency=1`。
- 低内存覆盖文件为 `compose.test-server.yml`。稳定时六容器实测内存约 326MiB；主机约 1.9GiB 内存仍有约 869MiB available，Swap 使用约 690MiB。该配置仅供 1～3 人测试，不是正式交付规格。
- PostgreSQL 为全新测试库；部署复核为 `users=1`、`business_records=0`，没有迁入旧 OA 或本地 SQLite 业务数据。
- Docker Web 仅绑定 `127.0.0.1:8080`；主机 Nginx 配置 `/etc/nginx/conf.d/sunhold-oa-test.conf` 通过独立主机名转发。当前电脑已在 Windows hosts 中加入 `150.158.3.104 oa-test.sunhold.local`，测试入口为 `http://oa-test.sunhold.local/`。
- 2026-07-20 用户已在腾讯云防火墙放行 TCP 8088；外部复验 `http://150.158.3.104:8088/health` 返回 `{"status":"ok"}`，首页返回 HTTP 200。公网测试入口为 `http://150.158.3.104:8088/`；正式上线必须改用真实域名与 HTTPS。
- 部署包安全扫描已补充排除 `*.db`、`*.sqlite`、`*.sqlite3`。首次候选包中误带的本地 SQLite 开发库只进入 root 私有发布目录，未进入镜像、PostgreSQL 或公网；该服务器文件及含它的旧 ZIP 已删除，当前 `20260720-0030` 发布包复查无数据库文件。

启动与状态检查：

```bash
cd /opt/sunhold-oa/current
docker compose --env-file .env.production \
  -f docker-compose.yml -f compose.prod.yml -f compose.test-server.yml up -d --no-build
docker compose --env-file .env.production \
  -f docker-compose.yml -f compose.prod.yml -f compose.test-server.yml ps
curl -fsS http://127.0.0.1:8080/health
```

仅停止测试 OA（保留 PostgreSQL、附件和对象存储卷）：

```bash
cd /opt/sunhold-oa/current
docker compose --env-file .env.production \
  -f docker-compose.yml -f compose.prod.yml -f compose.test-server.yml stop
```
## 2026-07-21 新系统测试问题发布

- 当前版本：`20260721-102324`；`/opt/sunhold-oa/current` 已切换到该发布目录。
- 容器项目名：`sunhold-legal-platform`；PostgreSQL、Redis、MinIO、API、Celery Worker、Web 六个服务均已恢复并运行。
- 启动或重建必须携带 `--env-file .env.production`，并同时叠加三个 Compose 文件；遗漏基础文件或环境文件会造成配置不完整，禁止只执行单个覆盖文件。
- 当前电脑优先使用 `http://oa-test.sunhold.local/` 验收，可避免浏览器对裸 IP 地址的中间风险提示；公网裸 IP 入口仍只是 HTTP 测试入口，不能作为正式域名。
- 本次发布已通过 `/health`、容器状态、完整本地验收和服务器浏览器逐项验收；没有迁移或删除现有 PostgreSQL、Redis、MinIO 数据。
