# 人事权限本地会话验收（2026-08-01）

本轮仅使用本地 `127.0.0.1:5173`，旧系统未操作；测试密码只在当前内存 HTTP 会话使用，未写入文件、文档、日志或提交。

## 临时对象

- 前缀：`CODEX-HR-ACCESS-D-`
- 经理账号：`codex_hr_access_d_mgr`
- 同部门员工：`CODEX-HR-ACCESS-D-SAME`，员工 ID `29`，部门“行政流程部”
- 异部门员工：`CODEX-HR-ACCESS-D-DIFF`，员工 ID `30`，部门“诉讼一部”
- 普通用户：`codex_hr_access_d_user`

## 会话证据

- 经理会话查询人事记录：同部门员工可见，异部门员工不可见。
- 经理对同部门员工办理“停用”：HTTP `200`，响应状态为“停用”，真实回显成功。
- 经理列表不可见异部门员工，直接访问办理接口返回 HTTP `404` 且无数据泄露；按项目经理判定视为有意隐藏资源存在性的权限拦截。
- 普通用户对同部门员工办理状态：HTTP `403`。

## 清理与复核

已通过本地管理员会话删除两名临时员工及其关联账号，并用 SQLite 只读查询确认：`users` 中 `codex_hr_access_d_%` 为 0 条，`business_records` 中 `CODEX-HR-ACCESS-D-%` 为 0 条。未修改任何既有账号密码/hash，未部署服务器。

针对性测试：`node --test apps/admin-web/hrAccessGuard.test.mjs`；前端构建：`npm.cmd run build`。
