# 9.16 财务整改（已确认第 2–8 行）

范围为用户已确认的七条问题，原文及 12 张逐图核对记录保存在工作站证据目录。发布前线上复核：第 2–8 行内容未变；后来新增第 9–12 行属于新增案件中心问题，未纳入本次发布，不标记完成。

| 行 | 根因与修改 | 验证 |
|---|---|---|
| 2 | 编辑、普通/内部/平台查询分别使用固定类型。统一读取系统费用目录，分类树保留大类和子类，编辑保存末级 ID/代码/名称快照，后端根据目录校验新增叶节点，查询精确匹配多选类型。 | 目录树、末级/归属校验、编辑后重读、新类型保存、查询多选和精确匹配；既有四类费用和批量费用回归。 |
| 3 | 提成预览跳过缺失人员而允许创建部分结果。GET/POST 预览及提交统一校验开庭律师、经办律师、助理的有效员工和账号；原草稿人员维护重算保持既有行为。 | 各角色缺失、无效人员、预览后人员变化阻止提交、失败零新增、有效人员创建持久化。 |
| 4 | 普通回款入口缺招行，已在前序版本补齐菜单、权限、原生模板和银行隔离。 | 四银行 API/前端隔离；用户提供的四个银行文件仅本地解析，不导入生产。 |
| 5 | 待结算只读案件上的旧管理人字段。改从客户稳定关联及唯一旧名称读取当前管理人，兼容 owner 和多人；列表筛选使用相同投影。 | 当前管理人、多人、关联数据持久化读取；总计对齐共用下两行实现。 |
| 6–7 | 总计右对齐，正文金额列沿用默认对齐。统一金额列标记、右对齐与 5px 内距，保留同一表格列序和选择列。 | 普通结算各状态、归档、内部费用及回款列构造/真实 render 回归；生产构建。页面最终由用户验收。 |
| 8 | 待结算取审核备注，已拒绝路由包含已退回。按实际动作时间取对应回退/拒绝备注，已拒绝仅查询审核拒绝；保留审核历史。 | 已付款回退→待结算→重新申请、重复回退、空备注、关联归档阻断、真实拒绝保留、状态和事件持久化。 |

自动化均使用进程内隔离 SQLite，测试结束释放数据库及客户端，不创建生产测试记录。核心 API 回归 9 项、费用目录回归 4 项、旧费用分类回归 4 项、前端回归 7 项通过；四银行原文件分别解析 9/10/11/6 条。原目录测试遗漏逐案合同必填关联，补齐夹具后通过，未放宽业务合同规则。

纯代码发布，无数据库迁移、业务数据补丁或整库备份。只做服务、版本、静态文件及健康检查，不做线上业务操作。回滚基线 v1.1.143。所有行状态为待用户验收，不能据自动化结果标记已验收。
