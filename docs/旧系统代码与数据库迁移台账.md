# 旧系统代码与数据库迁移台账

## 2026-08-13 8.12 行 2 列表滚动区域验收（已部署，待登录后 8089 浏览器复验）

- 需求：所有带滚动的数据页面固定标题、筛选条件和操作区域，只滚动下方数据表。
- 实现：客户、合同、普通案件、调查大厅四类核心列表均使用独立 Ant Table 纵向滚动区；列表工作台/卡片改为高度受限的弹性容器，避免页面外层滚动带走筛选区。客户、合同、调查表格的查询区与表头保持在表格滚动区外；案件列表保持同一工作台布局，并为其表格配置纵向滚动高度。
- 本地验证：`npm.cmd run build` 通过（5624 modules transformed，仅既有 chunk size warning）。Codex 内置浏览器实际打开本地 5173：客户表格体 `overflow-y: scroll`、`clientHeight 400 < scrollHeight 482` 且工作台 `overflow: hidden`；合同 `315 < 586`、调查子任务 `310 < 1532`，均为独立表格滚动，案件列表当前样本数据不足以溢出但容器已启用。
- 发布：本地精确提交 `84a31d0`（`fix(ui): keep list filters fixed while scrolling`），服务器工作树因并行修改较多，使用同一提交的最小文本补丁合并对应滚动配置，不覆盖其余差异；服务器前端版本已为 `1.0.158`。服务器 `npm run build` 通过（5624 modules transformed，仅 chunk size warning），`sunhold-dev-api`、`sunhold-dev-web` 均为 `active`，`/health` 返回 `{"status":"ok"}`，公网 `http://150.158.3.104:8089/?page=customer-mine&_v=158` 返回 HTTP 200。
- 浏览器状态：Codex 内置浏览器已实际打开上述 8089 地址，但当前是登录页，不能证明登录后的表格体滚动；待恢复测试登录态后，必须复验客户、合同、案件、调查列表的筛选区固定和表格体滚动，再将本行标记为已验收。

状态：进行中。本文件只记录从本地旧系统代码和 SQL Server 数据库读取到的事实，不以页面截图推断需求。

## 2026-08-13 8089 线上数据库结构核对

- 只读核对对象：公网 `150.158.3.104:8089` 对应的 `sunhold-dev-api`，实际运行目录为 `/opt/sunhold-oa/worktrees/release-v1.0.60/apps/api-server`，服务状态为 `active`；未重启、未修改线上数据。
- 线上实际数据库为 PostgreSQL，当前可见 49 张表。线上 `release-v1.0.60` 的模型和数据库均缺少本地本轮新增的六张兼容投影：`Legal_Case_File`、`Legal_Case_Participant`、`Legal_Case_Log`、`Legal_Investigation_Clue_Evidence`、`Legal_Investigation_Clue_Evidence_File`、`Legal_Investigation_Clue_File`。
- 结论：本地代码与 8089 的表结构确实不一致；线上不能验证本轮案件关系和调查取证投影功能。当前本地 API 启动阶段会执行 `Base.metadata.create_all`，部署包含这些模型的新版本后可创建缺失表，但部署前不得把线上状态标记为已同步或已验收。
- 待办：将本地案件关系/调查取证模型和同步逻辑按版本提交并部署到 8089；部署后只读复核六张表的字段、索引与服务健康，再用 Codex 内置浏览器验收对应页面流程。

## 边界

- 旧系统源码只读：`C:\Users\Administrator\Desktop\OA系统\legacy-gdcrm-101-local-20260812\source\GD.CRM.WEB`
- 旧数据库只读：SQL Server 数据库 `PRD_CRM_GD_20200211`
- 新系统唯一修改目标：本工作区的本地服务 `127.0.0.1:5173` / `127.0.0.1:8000`
- `8090` 不读取、不修改、不重启。
- `8089` 在用户确认前不部署、不重启。

## 已确认的结构事实

1. 旧系统主业务数据不是统一 JSON 记录表，而是按领域拆分的 SQL Server 表。
2. 查询 `sys.foreign_keys` 未发现本批核心业务表的物理外键；`CustomerNo`、`ContractNo`、`CaseNo`、`InvestigationNo`、`InvestigationTaskNo` 和人员账号字段承担软关联职责。
3. 新系统当前 `BusinessRecord` 只有 `module`、`serial_no`、`title`、`customer`、`status`、`owner`、`department`、`description` 和 `data` JSON，不能直接证明可保留旧表的字段长度、索引、软关联语义及跨模块查询。
4. 后续不能把旧编号关系改成只保存新的数值主键。新表/API 必须同时保存并查询旧业务编号文本。

## 2026-08-13 调查取证证据结构差异核对

- 新增只读核对工具：`apps/api-server/scripts/audit_legacy_schema.py`。工具以 Windows 集成认证读取本机 SQL Server 的 `sys.columns`，不写入旧库；报告兼容投影的缺表、缺列与字段元数据，避免用页面或 JSON 推断旧库结构。
- 已核对旧表实际数据量：`Legal_Investigation_Clue_Evidence` 4,006 条、`Legal_Investigation_Clue_Evidence_File` 2,488 条、`Legal_Investigation_Clue_File` 31,933 条，三者不是可省略的历史附属表。
- 已补齐三张 SQLAlchemy 兼容投影，字段数与旧库完全一致：证据主表 24 列、证据文件 15 列、线索文件 14 列；保留 `ClueGuid`、`EvidenceGuid` 软关联，以及公证机构、证据地址、存放位置、付款状态和文件路径元数据。
- 验证：只读审计报告三表均为 `missing_table=false`、`missing_columns=0`、`extra_projection_columns=0`；`legacy_investigation_evidence_schema_contract_test.py` 2/2 通过；内存 SQLite `Base.metadata.create_all` 通过。
- 状态：结构投影已核对。尚未完成证据主表/附件从新业务记录的同步写入、样本包导入、接口读取与浏览器业务闭环，因此本项不得标记为调查取证功能已验收。

## 2026-08-13 调查线索取证写入兼容投影

- 真实入口：`POST /api/v1/investigations/clues/{clue_id}/collect`。原有页面已在这里写入取证日期、公证机构、公证书号、发票号、存放位置、证物状态和选择的取证附件。
- 修复：该入口现同步 `Legal_Investigation_Clue_Evidence`；使用由线索记录 ID 确定的 `EvidenceGuid` 与 `ClueGuid`，以保证同一线索重复提交或服务重试不产生重复证据。为不占用导入历史 SQL Server 的正主键，新建投影使用稳定负 `EvidenceId`。
- 附件关系：用户选择的 `collection_file_ids` 同步到 `Legal_Investigation_Clue_Evidence_File`；同一线索其余附件同步到 `Legal_Investigation_Clue_File`，均保留旧表文件 ID、路径、大小、上传人、上传时间与 GUID 软关联。
- 自动验证：`investigation_87_contract_test.py` 9/9 通过，覆盖取证提交后的公证机构、公证书号、发票、证物状态，以及取证附件/普通线索附件分别落入两张旧附件表；`legacy_investigation_evidence_schema_contract_test.py` 2/2 通过，`py_compile` 通过。
- Codex 内置浏览器：已实际打开本地 `http://127.0.0.1:5173/?page=clue-my-collect`，页面显示待取证线索筛选、旧式列表列与“取证”操作入口。当前本地没有待取证样本，未创建临时业务数据，故尚未取得提交弹窗和成功提交的浏览器闭环证据。
- 状态：接口与投影回归通过，浏览器仅完成页面入口验收；待以唯一 `CODEX-*` 线索完成一次真实页面提交、核对投影后精确清理，才可标记本流程浏览器验收完成。

## 2026-08-13 取证浏览器提交环境核对

- 尝试按验收规则创建唯一临时线索 `CODEX-EVIDENCE-BROWSER-20260813` 和两条附件元数据；本地工作树数据库中创建后立即核对并清理，未进入业务提交步骤，也未遗留记录。
- Codex 内置浏览器的 `127.0.0.1:5173` 前端代理配置指向 `127.0.0.1:8000`，但该端口由另一套已有的多进程热重载服务占用，读取的数据源不是当前工作树 `apps/api-server/legal_platform.db`。浏览器未显示该临时记录，已据此停止操作，避免向错误运行实例写入数据。
- 尝试启动隔离的当前工作树 API `8011` 与前端 `5174` 被本执行环境的后台进程策略拒绝；没有终止、重启或覆盖用户现有 `5173/8000` 服务。
- 结论：本批不能以现有 `5173` 标签作为当前代码的浏览器提交验收。自动接口回归仍已通过；待当前工作树启动到独立可控端口，或用户明确允许接管/重启 `5173/8000` 并确认其工作目录后，再执行同一 `CODEX-*` 浏览器提交和精确清理。

## 2026-08-13 取证附件重提一致性与样本边界

- 重提同一线索取证时，附件在 `Legal_Investigation_Clue_Evidence_File` 与 `Legal_Investigation_Clue_File` 间改为互斥迁移：从普通线索附件选择为取证附件后，旧普通附件表残留行会被删除，反向亦然，避免同一 `FileId` 双表重复。
- 自动验证：`investigation_87_contract_test.py` 10/10 通过，新增回归覆盖附件二次选择后的单表唯一映射；证据结构回归 2/2 和 Python 编译均通过。
- 样本边界已核对：`SH_latest50_dataset.xml.gz.b64` 仅含 `Legal_Investigation_Clue`，调查依赖包仅含 `Legal_Investigation`、`Legal_Investigation_Task` 和人员；三张取证证据/附件表不在该导出包内。因此不得将 latest50 导入宣称为取证证据历史已迁移，后续需单独从旧库只读导出这些表后再导入。

## 首批核心表

| 领域 | 旧表 | 关键主键/软关联 | 旧库实际字段范围 | 新系统现状 | 迁移结论 |
| --- | --- | --- | --- | --- | --- |
| 客户 | `CRM_Customer` | `CustomerId`、`CustomerNo`、`BusinessOwner`、`CustomerOwner` | 客户基础、工商、银行、联系人、负责人、共享、审计字段，共 53 列 | `BusinessRecord(module=customer)` + JSON | 必须建立客户专表或兼容投影；至少保留客户编号、工商和负责人字段，不能只按客户名称关联 |
| 客户联系人 | `CRM_Customer_Contacts` | `CustomerNo` | 联系人独立表 | JSON/通用记录 | 必须保持一客户多联系人及账号软关联 |
| 合同 | `FCM_Contract` | `ContractId`、`ContractNo`、`CustomerId`、`CustomerNo`、`BusinessOwner` | 合同名称、类型、收费、金额、税率、审批流、状态、变更、审计字段，共 29 列 | `BusinessRecord(module=contract)` + 合同审批专表 | 必须保留 `CustomerNo` 与 `ContractNo` 双编号关系、审批字段和变更标记 |
| 合同附件/审核 | `FCM_Contract_File`、`FCM_Contract_Audit` | `ContractNo` | 文件与审批历史专表 | `FileAttachment` / `WorkflowEvent` | 需按旧编号保留文件、审批轮次与审核人可查询投影 |
| 案件 | `Legal_Case` | `CaseId`、`CaseNo`、`CustomerNo`、`ContractNo`、`InvestigationClueNos` | 诉讼主体、法院三审/执行、人员、归档、公安/检察、线索、调查、金额等 143 列 | `BusinessRecord(module=case)` + 少量专表 | 不能继续把全部案件结构长期放在 JSON；应创建案件兼容专表并保存文本编号关联 |
| 调查任务 | `Legal_Investigation` | `InvestigationNo`、`ContractNo`、`BusinessOwner`、`Auditor` | 授权起止、范围、省市、审批、客户审核、合同编号，共 23 列 | `BusinessRecord(module=investigation)` + JSON | 必须保留调查编号与合同编号，不以页面选择值替代 |
| 调查子任务 | `Legal_Investigation_Task` | `TaskNo`、`InvestigationNo`、`Investigator`、`Assistant` | 父任务、调查员、助理、起止、区域、状态、审计字段，共 21 列 | `BusinessRecord(module=investigation_task)` + JSON | 必须按 `InvestigationNo` 建立父子任务软关联并保留人员账号字段 |
| 调查线索 | `Legal_Investigation_Clue` | `ClueNo`、`InvestigationTaskNo`、`InvestigationNo`、`CaseNo` | 线索地点、商品、侵权、审核、客户审核、案件、调查员字段，共 44 列 | `BusinessRecord(module=clue)` + JSON | 必须保留任务编号、调查编号、案件编号及审核状态，不允许只保留显示名称 |
| 用印 | `AWS_OfficialDocument` | `OfficialDocumentNo`、`CaseNo`、`ContractNo`、`CustomerNo`、`BusinessOwner` | 用印申请、电子/线下盖章、印章类型、审批流、审核信息，共 36 列 | `BusinessRecord(module=seal)` + `SealAsset` | 必须保留旧编号关系与审批/用印字段；印章资产可作为扩展表，不能替代旧申请语义 |
| 用印审核/文件 | `AWS_OfficialDocument_Audit`、`AWS_OfficialDocument_File` | `OfficialDocumentNo` | 审批记录与申请文件独立表 | `WorkflowEvent` / `FileAttachment` | 需提供按用印编号的兼容查询和历史投影 |

## 本批旧库字段证据

已通过 SQL Server 元数据读取以下表完整字段定义：

- `CRM_Customer`
- `FCM_Contract`
- `Legal_Case`
- `Legal_Investigation`
- `Legal_Investigation_Task`
- `Legal_Investigation_Clue`
- `AWS_OfficialDocument`

旧库记录量还证明这些不是空壳表：`FCM_Contract` 23 条、`Legal_Case` 51 条、`Legal_Investigation` 126 条、`Legal_Investigation_Task` 963 条、`Legal_Investigation_Clue` 37 条，且附件/消息/节点表存在大量历史数据。

## 2026-08-13 核心兼容表全量字段审计基线

- 通过 `scripts/audit_legacy_schema.py` 对 SQL Server `sys.columns` 和当前 SQLAlchemy 投影完成只读比较。以下 12 张核心表均为 `missing_table=false`、`missing_columns=0`、`extra_projection_columns=0`：客户、客户联系人、合同、合同审核、合同文件、案件、调查、调查子任务、调查线索、调查线索取证证据、取证证据文件、线索文件。
- 字段数逐表一致：`53/31/29/14/13/143/23/21/44/24/15/14`。该结论仅证明字段层兼容投影已建立；不证明全部历史数据已导入，也不替代流程、权限、接口与浏览器验收。

## 2026-08-13 案件文件、参与人和日志兼容投影

- 旧库关系事实：`Legal_Case_File` 96 条（25 列）、`Legal_Case_Participant` 144 条（8 列）、`Legal_Case_Log` 277 条（10 列）。三表此前没有当前系统的兼容投影。
- 已补齐三表模型并通过只读 SQL Server 字段审计：三表均 `missing_table=false`、`missing_columns=0`、`extra_projection_columns=0`。参与人表没有独立 ID，按旧库语义采用 `CaseNo + StaffName` 复合主键。
- 案件主表同步现在联动写入：通用案件附件进入旧案件文件表；案件负责人、律师、助理、调查员进入旧参与人表；案件 `WorkflowEvent` 进入旧案件日志表。新记录使用稳定负文件/日志 ID，以避免与未来导入的旧库正 ID 冲突。参与人员明确优先保存账号字段，展示姓名仅作无账号时回退，符合旧 `StaffName` 的软关联用途。
- 自动验证：案件兼容回归 `8/8`，其中运行时断言覆盖案件附件的路径/大小/传输状态、账号参与人和流程日志；调查取证回归 `10/10`、证据结构回归 `2/2`、Python 编译及 `git diff --check` 均通过。
- 未完成：现有 latest50 导入目前仅写入案件文件元数据，不包含 `Legal_Case_Participant` 和 `Legal_Case_Log` 的历史投影；历史导入需在单独批次扩展，不能据此宣称案件历史关系已完整迁移。

## 已完成验收

| 日期 | 模块 | 本地实现/核对 | 自动测试 | Codex 内置浏览器验收 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-12 | 用印 | 增加 `AWS_OfficialDocument`、`AWS_OfficialDocument_Audit`、`AWS_OfficialDocument_File` 兼容投影；保留 `CaseNo`、`ContractNo`、`CustomerNo`、文件 GUID 与旧状态码 | `legacy_official_document_schema_contract_test.py` 4/4 通过，包含草稿、提交、审批、用印完整落表链 | `127.0.0.1:5173/?page=seal-my-pending`：申请弹窗显示案件用印、合同章、1 份、现场用印、电子印章“是”、打印盖章“需要”；未提交测试数据 | 已验收 |
| 2026-08-12 | 合同 | 增加 `FCM_Contract`、`FCM_Contract_Audit`、`FCM_Contract_File` 兼容投影；保留 `ContractNo`、`CustomerNo`、`ContractGuid`、金额、税率、状态、审核轨迹与附件 GUID | `legacy_official_document_schema_contract_test.py` 5/5 通过，包含草稿、提交、审批、附件和旧字段落表 | `127.0.0.1:5173/?page=contract-new`：合同中心直接新建入口无客户/合同名称预填，需选择客户后新建；未保存测试数据 | 已验收 |
| 2026-08-12 | 客户与联系人 | 增加 `CRM_Customer`、`CRM_Customer_Contacts` 兼容投影；保留客户编号、客户 GUID、状态、负责人、客户资料，以及独立的一客户多联系人、主联系人与停用状态 | `legacy_official_document_schema_contract_test.py` 6/6 通过，验证两联系人同时落表、主联系人切换、联系人软删除和客户回收 | `127.0.0.1:5173/?page=customer-mine`：客户详情存在独立联系人列表和新建入口；联系人账号使用多选及全部中文姓名拼接。现有本地样本无联系人，未写入测试业务数据 | 部分验收，待具备多联系人样本后补浏览器数据验收 |

## 2026-08-12 8091 latest50 sample import

- Source: `legacy-gdcrm-101-local-20260812/backup/SH_latest50_dataset.xml.gz.b64`, `SH_latest50_dependencies.xml.gz.b64`, and `SH_latest50_investigation_dependencies.xml.gz.b64`.
- Counts imported into the local 5173 database: 50 `Legal_Case`, 21 `CRM_Customer`, 22 `FCM_Contract`, 16 `Legal_Investigation`, 21 `Legal_Investigation_Task`, 37 `Legal_Investigation_Clue`, 6 customer contacts, and 86 case-file metadata rows.
- A local database backup was created before import: `apps/api-server/legal_platform.before-sh_latest50-20260812231541.db`.
- Idempotency check: rerun dry-run found all 50/21/22/16/21/37 source identifiers already present; no duplicate insert planned.
- Relationship check: 47 newly projected cases have customer and contract identifiers, 21/21 tasks have investigation parent links, and 37/37 clues have task or investigation links.
- Codex in-app browser acceptance on `http://127.0.0.1:5173/?page=case-company` opened sample `SHMS2600395`; the detail view showed customer `上海聚润宾馆（普通合伙）`, contract `SHHT2610053`, document folders, and 3 migrated legacy file metadata rows.

## 2026-08-12 latest50 investigation view repair

- Issue found by local browser acceptance: the migrated 16 investigation rows existed, but administrator "我发布的调查任务" was empty because both the browser and `/records?investigation_view=published` restricted the view to the current publisher.
- Repair: administrators now retain the legacy all-visible behavior for published investigation and published child-task views; non-administrators remain publisher-scoped.
- Import projection now refreshes existing sample records by source identifier and maps `AuthorizationBeginTime`, `AuthorizationEndTime`, `CaseTypeId`, `InvestigationScope`, customer-audit flag, publisher, source owner and task schedule fields to the current list-data keys.
- Verification: `investigationAdminPublishedVisibility.test.mjs` passed; `investigation_87_contract_test.py` passed 8/8; combined investigation, assignee and compatibility regression suite passed 18/18; production frontend build passed.
- Codex in-app browser acceptance: `http://127.0.0.1:5173/?page=investigation-task-published` now displays the 16 source rows, including `RW2614153786523` with authorization dates `2026-07-10` to `2029-07-09`.

## 2026-08-12 latest50 staff display-name repair

- Imported the 15 referenced `HR_Staff` rows from the local 8091 sample bundle into local `users`, preserving only login name, Chinese display name, staff number, and department reference.
- Legacy password fields were deliberately not read or copied. Each migrated account receives a generated one-time password hash and `must_change_password=true`; an administrator must set an actual login password in employee management.
- Import rerun result: 15 staff accounts created, with all existing 50/21/22/16/21/37 case/customer/contract/investigation/task/clue projections refreshed idempotently.
- Codex in-app browser acceptance: `http://127.0.0.1:5173/?page=investigation-task-published` shows Chinese names for historical source owners, including `张美莹`, `朱淑旖`, `卢愿`, `朱菁芸`, and `王晓英`; no username or `姓名待维护` placeholder appeared in the displayed source-owner cells.

## 2026-08-12 investigation subtask legacy-field parity

- Legacy source evidence: `Areas/CIT/Views/InvestigationTask/Create.cshtml` and `List.cshtml` bind each child task to `InvestigationNo` and require investigator, region, start/end dates, and remark; the selected dates are constrained by the parent authorization period.
- New-system repair: child-task creation now persists parent investigation number, parent task number, independent `start_date`/`end_date`, `province`/`city`/`district`, region, authorization scope and the existing contract binding. Inheritance order is request, then parent task, then root investigation.
- Compatibility projection: `Legal_Investigation_Task.BeginTime`, `EndTime`, `Province`, `City`, and `District` are updated from the same child-task fields, retaining the old `InvestigationNo` soft relation.
- Regression: `investigation_87_contract_test.py` passed 9/9, including a create-and-project assertion for parent schedule and old-table fields. Frontend production build passed.
- Codex in-app browser acceptance: local `http://127.0.0.1:5173/?page=investigation-task-sub-published` displayed 21 migrated child tasks with clickable `父调查编号`, Chinese investigator names, and start/end columns. Sample regions are blank in the 8091 export and render as `—`; the new form exposes province, city, district and start/end fields for records that have those values.

## 2026-08-13 investigation subtask server acceptance

- Release: commit `a38eec1` (`fix(investigation): align child task fields`), public version `1.0.157`.
- Deployment target: non-Docker worktree `/opt/sunhold-oa/worktrees/release-v1.0.60`; restarted `sunhold-dev-api` and `sunhold-dev-web`. API health returned `{"status":"ok"}` and public `http://150.158.3.104:8089/` returned HTTP 200.
- Server verification: `/opt/sunhold-oa/dev-runtime/venv/bin/python -m py_compile app/main.py`, `investigation_87_contract_test.py` 7/7, and `npm run build` all passed. The only build notice was the existing large-chunk warning.
- Codex in-app-browser acceptance: `http://150.158.3.104:8089/?page=investigation-task-sub-published&_v=157` displayed the columns `任务编号`、`父调查编号`、`权利人`、`权利类型`、`调查员`、`调查区域`、`开始时间`、`结束时间`、`案源人`、`状态`、`办理`. Historical rows showed Chinese personnel names and populated parent identifiers, region and schedule values; the parent identifier was rendered as a clickable relation button. Example child task `RW20260806145800390207` showed parent `RW20260726055721981317`, `上海市测试区域`, `2026-07-26`, and `2026-08-02`.
- Test data: temporary browser-only administrator `CODEX-BROWSER-157` was removed after acceptance; a database query confirmed no residual account.

## 执行顺序

1. 抽取上述旧表的唯一索引、默认值、存储过程和 Controller 写入入口。
2. 在新系统引入兼容领域表和版本化迁移，保留原有 API 的读取投影，避免一次性打断前端、钉钉和智能体。
3. 先迁移客户、合同、案件、调查/线索、用印五组主表及附件/审核历史。
4. 每组完成后执行旧库抽样数据核对、本地 API 回归及 Codex 内置浏览器验证；在用户确认前不发布到 `8089`。
