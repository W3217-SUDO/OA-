# Frontend

Vue 3 will reproduce old-system routes and layouts. Each page is implemented
only after its legacy screenshot, source route, fields, permissions, and
workflow are registered in `docs/parity-ledger.md`.
