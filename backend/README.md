# Backend

FastAPI will map the legacy SQL Server schema directly. During parity capture,
the backend is read-only. No database mutation endpoint may be added before the
matching legacy workflow and rollback behavior have been documented.

