# 第37行旧系统 Chrome 只读实跑

- 浏览器：Google Chrome（共享槽位串行使用）。
- 地址：`http://localhost:8091/`。
- 原问题入口：案件中心 -> 案件详情 -> 文档信息。
- 目标记录：优先截图中的 `SHMS2500842` / `SHMS2500709A`。

## 实跑步骤与结果

1. 新建且仅保留第37行标签，进入 `http://localhost:8091/`。
2. 页面停留在“登录 - 律师协作平台”，显示用户名、密码和登录入口。
3. 本批次此前获准复用的本地临时凭据已按第34行清理指令删除；Chrome 中也没有仍有效的旧系统登录态。因此无法以对应业务角色进入案件中心。
4. 未填写或提交未知凭据，未尝试重置账号，未打开其他系统；未执行生成、删除、移动、申请用印等写操作。
5. 保存认证阻断证据 `old-system/login-blocked.png`，随后关闭全部本行标签并释放 Chrome 槽位。

## 门禁结论

- 原记录、等价记录及原入口业务页面均因认证阻断而不可达，十项生成菜单及“删除/申请用印/更改文档目录”弹窗/跳转未能动态复现。
- **旧系统动态数据未完整复现**；不得把登录页、原截图或源码审计表述为旧系统浏览器通过。
- 旧系统可核对部分仍仅限：D37 两张原系统目标截图，以及旧视图/控制器/服务调用链的只读证据。
- 新系统验收仍须回原问题案件，不得用替代记录冒充闭环。
