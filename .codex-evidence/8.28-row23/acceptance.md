# 8.28 第23行本地验收

- 问题：合同中心“我的合同”按合同编号或关键字查询后，列表未过滤。
- 根因：前端已发送 `serial_no`、`title` 等合同查询参数，但 `/records` 未声明也未应用这些参数，FastAPI 将额外查询参数静默忽略。
- 修复：合同列表接口在数据分页前应用合同名称、合同编号、合同类型、案件编号、收费类型、合同主体、案源人和签订日期条件；通用关键字继续匹配合同号、名称、客户及负责人。
- 后端：本行及相邻部门合同范围测试 4/4 通过，`py_compile` 通过。
- 前端：本行查询参数测试 2/2 通过。
- 扩展回归：两条既有静态测试因断言仍要求旧函数排版/旧分页 JSX 而失败，与本行差异无关；未修改无关功能。
- 生产构建：通过，Vite 转换 5637 个模块；正式版本保持 `1.1.27`。
- 本地浏览器：合同号 `CODEX-828-R23-HT-001` 查询后仅显示目标合同且总数1；合同名称关键字“目标商标”查询后同样仅显示目标合同；清空后恢复两份测试合同及原列表共5条。
- 证据：`browser-contract-number-filter.png`、`browser-contract-title-filter.png`。
- 清理：两份 `CODEX-828-R23-*` 测试合同已删除且残留0；8098/5301服务停止，浏览器页签清空。

结论：本地浏览器验收通过，待本批次统一部署后线上验收。
