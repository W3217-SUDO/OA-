# 9.1 第 12 行处理结果

- 状态：代码与聚焦测试完成；未浏览器验收、未构建、未部署，不能标记线上闭环。
- 返工追因：`57fc0d7`（8.28 第21行）把“案件任务发布后立即计时/发起人侧显示进行中”错误下沉为数据库 `initial_status="处理中"`，使负责人跳过旧系统的待处理与接受动作。误判类别为需求读取与实现范围：上次只验收发起人案件页，未用独立负责人账号验收“我接受的任务”。
- 旧系统目标：`Areas/TP/Views/Task/TaskList.cshtml` 与任务动作链保留待处理列表及接受动作；原系统截图 D_image429.png 直接显示待处理列表和“接受任务”。
- 修改：案件任务新建持久化为“待处理”；负责人 owned 视图保持待处理，接受接口后转“处理中”；发起人 initiated 视图与案件详情任务列表在尚未接受时仍展示“进行中”，以表达已发出并开始计时，同时保留 `workflow_status=待处理`。
- owned files：`apps/api-server/app/main.py`、`apps/api-server/task_case_acceptance_status_row12_test.py`、`apps/api-server/case_task_detail_row21_test.py`、`.codex-evidence/9.1-row12/*`。
- 测试：`python -m unittest task_case_acceptance_status_row12_test.py task_serial_number_row11_test.py case_task_creation_approval_independence_row7_test.py case_task_detail_row21_test.py`，6/6 通过；Python 编译与 `git diff --check` 通过。
- 数据库补丁建议：建议对 8.28 修复发布后创建、仍为“处理中”且没有“接收任务”工作流事件的人工案件任务做一次可审计回填为“待处理”；已有接收事件或后续完成/撤回状态的任务不得回退。补丁应先输出候选 ID/任务号供核对，再事务更新。
- 主会话 Chrome 验收：用发起人 A 从案件详情向负责人 B 发任务；A 的案件任务列表和“我发起的任务”显示进行中且剩余时间开始计算；切 B 的“我接受的任务”，待处理计数 +1、进行中不增加；B 点击接受后刷新，待处理 -1、进行中 +1；回到 A 确认同一任务仍为进行中，详情历史新增“接收任务”。

## 返工追因门禁补录（2026-09-01 本地旧系统 Chrome 只读实跑）

### 原整改意见与全部返工意见

- 原整改（8.28 第21行）：任务发布后立即计时；发起人案件页显示“进行中”，并在案件页右侧打开任务详情。上次据此把案件任务的持久化初始状态直接设为“处理中”。
- 本次返工（9.1 第12行）：上述“进行中”只描述发起人看到任务已发出且开始计时，不代表负责人界面跳过接收。负责人必须先在“我接受的任务 → 待处理”看到任务，点击“接受任务”后才进入“进行中”；两种关系视图不要求强制显示相同状态。
- 本行 Excel 未提供额外 F-I 返工文字；以上为本批次全部可见返工意见。

### 旧系统实跑步骤与结果

1. 获得独占 Chrome 槽位后，在 Google Chrome 打开 `http://localhost:8091/`；成功进入本地旧系统，页面显示当前身份“罗胡靖雯”。
2. 沿原入口进入“事务中心 → 我的任务 → 我接受的任务”，实际地址为 `http://localhost:8091/9001001020/TP/Task/TaskList`。
3. 只读查看四个分栏：待处理(0)、进行中(0)、完成(0)、停止(0)；列表提示“没有查询到符合条件的记录”。未点击接受、完成、转交等业务按钮，未填写或提交表单。
4. 原系统目标图 `D_image429.png` 的角色为“江彤”，在同一入口显示待处理(84)，原记录示例包括 `(自动)15444058193 / SHMS2200363`，并显示“接受任务 / 完成任务 / 转交任务 / 更多操作”。
5. 本地会话没有“江彤”身份且当前身份查询不到原记录，因而无法在对应角色、原业务记录上继续完成接收前/接收后的同类流程；未尝试读取或篡改 Cookie、伪造身份、退出登录或猜测凭据。
6. 保存阻断证据 `legacy-local-role-blocked.png` 后关闭本行标签并释放 Chrome 槽位。

### 上次误判原因

- 上次只在发起人案件页验证“发布即计时/显示进行中”，没有切换独立负责人角色进入“我接受的任务”。
- 将关系视图的显示语义错误下沉成全局持久化状态，导致负责人侧也直接进入处理中。
- 上次浏览器验收还写入留言、上传附件并撤回，验证范围偏向详情交互，没有覆盖负责人接收状态机。

### 逐项确认单

- [x] 本行完整文字、新系统图、旧系统目标图已读取并区分。
- [x] 原整改意见与本次全部返工意见已合并列出。
- [x] 上次代码、测试与验收记录已审计，误判原因已定位。
- [x] Google Chrome 本地旧系统原入口已实际跑通。
- [ ] 对应原角色“江彤”实跑：本地会话身份不符，未获得可用的只读角色会话。
- [ ] 原业务记录实跑：当前会话四栏均为 0，原记录不可见。
- [ ] 同一原记录接收前/接收后状态核对：禁止写库且缺少原角色，因此未执行接受动作。
- [x] 旧系统页面结构与目标图一致：存在待处理/进行中分栏，目标图待处理列表底部存在“接受任务”。
- [x] 新代码聚焦测试覆盖：负责人待处理、发起人关系视图进行中、接受后负责人进入进行中。
- [ ] 本地新系统浏览器验收：本会话明确禁止构建、部署和新系统 Chrome 验收。

### 门禁结论

- 代码和自动化测试已完成，但“对应角色 + 原业务记录 + 原入口”的旧系统实跑尚缺前两项。
- 按更新后的返工追因门禁，本行不得提交为已闭环；需主会话提供旧系统“江彤”的已登录只读 Chrome 会话（或其他经授权的等价原负责人会话）后补验。

## 非 Chrome 本地账号/数据路径审计

- 实际运行服务：IIS Express 指向 `legacy-gdcrm-101-local-20260812/source/GD.CRM.WEB`，端口 8091；不是同归档中的 `GD.CRM.WEB.VIP`。
- 数据源：`GD.CRM.WEB/Web.config` 指向本机 SQL Server 数据库 `PRD_CRM_GD_20200211`。仅通过 `READ UNCOMMITTED` SELECT 查询元数据和业务数据，未执行写入。
- 账号查询：`HR_Staff` 中按 `StaffName / StaffChName / StaffNo` 精确查“江彤”无结果；模糊查“江”也没有员工账号。当前页面身份对应本地账号 `luohjw / 罗胡靖雯`，角色为“律师助理”。
- 原记录查询：`Legal_Case_Task` 中任务号 `15444058193` 和案号 `SHMS2200363` 均无结果；本地表合计仅 23 条任务，没有 `Officer / FirstOfficer / Initiator` 含“江”的任务。
- 数据归档：工作区只发现旧库备份 `backup/PRD_CRM_SH_20190320_20260802.bak`，未发现更新到截图日期或明确包含江彤/原任务的第二份本地数据库文件。
- 登录机制：`HomeController.UserLogin` 只支持用户名 + 明文密码经 MD5 校验后建立 session；账号表只保存散列，既有材料未记录“江彤”的账号或可用凭据。
- 身份切换：源码和页面脚本未发现 `SwitchUser / LoginAs / Impersonate` 等管理员只读切换入口；只发现会修改密码的“重设密码”，该动作会写库且不在授权范围，不能用于验收。
- 既有验收记录：只找到新系统历史案件页面曾显示“律师助理：江彤”的展示证据，不能证明本地旧库存在对应登录账号，也不能替代旧系统原角色实跑。

### 路径结论

- 当前本地旧库中既没有“江彤”账号，也没有截图中的同一任务，因此不存在可安全登录且能看到同一任务的对应角色账号。
- 旧系统管理员也没有无写入的身份切换功能；重设密码、补账号、导入任务或修改 session/cookie 都会改变状态或伪造身份，均未执行。
- 暂不再次申请 Chrome 槽位。若主会话后续提供包含原记录的更新本地旧库，或“江彤”已登录且可只读接管的 Chrome 标签，再重新申请槽位补验。

## 更新后“原记录缺失边界”复核

- 已重新读取 skill：原记录经原入口和旧库只读确认确实缺失后，只允许使用“同角色、同模块、同状态”的既有记录；必须记录替代编号和等价依据，不同角色不得替代。
- 角色映射补证：既有生产迁移 dry-run 映射明确给出旧账号 `jiangt -> 江彤`；历史案件验收记录将江彤标识为“律师助理”。本地 `HR_Role` 中存在“律师助理”角色，当前 `luohjw / 罗胡靖雯` 也是该角色，但不能仅凭同角色替代，仍须同模块、同状态既有记录。
- 候选查询：只读联查 `Legal_Case_Task -> HR_Staff -> HR_Role`。本地状态 2 的案件任务共有 12 条，负责人角色分别为财务审核管理、知产中心负责人、调查取证员、诉讼部部长、调查部部长；没有律师助理角色记录。
- 全量等价查询：本地 `Legal_Case_Task` 23 条记录中，负责人属于“律师助理”的记录数量为 0；因此不存在“律师助理 + 案件任务 + 接收前状态”的合格替代记录。
- 虽然本地有多名启用的律师助理账号，当前会话也属于律师助理，但这些账号均看不到同角色同模块记录，不能用空列表或其他角色任务替代旧系统流程。

### 更新后结论

- 原记录缺失边界已适用，但未找到合格替代记录，无法填写替代记录编号。
- 不再次申请 Chrome 槽位；第12行保持未提交、未完成。需要更新的本地旧库/备份包含 `jiangt` 或至少一条律师助理负责的接收前案件任务，或已有该角色且能看到合格记录的只读登录会话，才能继续。

## 业务角色等价口径再审计

- 纠正：本问题的对应角色不是 HR 职位“律师助理”，而是旧系统“案件任务负责人/可接收并处理任务的人”。判等条件为：具备“我接受的任务”菜单；列表按当前登录账号写入 `Officer` 数据范围；当前流程节点为 `NodeStatus.Pending`；详情对当前 `Officer` 且 Pending 的任务显示接收动作。
- 源码依据：`TaskController.TaskList` 在页面键 `9001001020` 下强制 `NodeStatus=Pending` 且 `Officer=LoginHelper.GetUserName()`；`SYS_Menu` 中 `9001001020` 为启用的“我接受的任务”；任务详情仅当当前登录人等于负责人且任务/节点处于待处理位置时提供接受处理入口。
- 状态依据：旧系统待处理/进行中分栏按节点状态而不是 HR 职位或任务标题判断。`NodeStatus=1` 为待处理，`NodeStatus=2` 为进行中。
- 旧库候选复核：对本地 23 条 `Legal_Case_Task`，分别按当前任务 `TaskGuid` 联查 `Legal_Case_Task_Node` 的 `NodeStatus=1`，结果为 0 条；因此没有任何现有任务负责人账号同时具备“同模块 + 同待处理流程位置”的可复验记录。
- 表中虽然有 12 条 `TaskStatus=2` 的案件任务及负责人账号（如 `fanyg / 10424616446`、`shenhe / 14065225191`），但它们是任务级“处理中”，且没有可关联的 Pending 当前节点；不能冒充待处理接收场景。
- 本地节点表虽有历史 `NodeStatus=1` 记录 714 条，但无法与当前 23 条任务通过 `TaskGuid` 关联，缺少可进入详情并核对的现存案件任务编号，不能作为替代记录。

### 业务角色口径结论

- 不再以“律师助理”作为阻断理由；真正阻断是本地 23 条现存案件任务中没有一条处于待处理接收节点。
- 因缺少同业务角色、同模块、同状态的现存记录，当前无法提供合格替代编号，不申请新的 Chrome 槽位。

## 本地旧库样本缺失条款结论

- 已亲自用 Google Chrome 打开旧系统原入口 `事务中心 → 我的任务 → 我接受的任务`，执行到可达的待处理列表步骤并保存 `legacy-local-role-blocked.png`；页面四栏均为 0，未执行任何写动作。
- 已通过旧库只读查询证明原记录与等价待处理记录均不存在，并通过原截图、旧源码、控制器、菜单与数据库结构补齐负责人接收状态机依据。
- **旧系统动态数据未完整复现**：空列表仅作为“样本缺失/空结果”证据，不记为旧系统浏览器通过；原截图中的待处理记录及点击接受后的动态迁移未能在本地旧库复现。
- 按更新后的 skill，样本缺失不再冻结代码与提交。代码可进入聚焦回归和独立提交，但第12行仍不能标记为浏览器闭环或台账完成。
- 新系统最终验收必须回到原问题记录，分别核对发起人关系视图、负责人待处理视图、接收动作、接收后进行中状态及工作流历史；本会话禁止构建、部署和新系统 Chrome，因此该验收留给主会话。

## 隔离本地新系统 Chrome 验收（2026-09-01）

- 环境：本工作树 Web `127.0.0.1:15312` 代理本工作树 API `127.0.0.1:18312`，独立 SQLite；没有使用共享数据库。
- 原问题记录：发起人“管理者”从“公司案件 → 民事争议 → SHMS2500149 → 案件任务”发布 `CODEX-901-R12-ORIGINAL`，任务号 `RW260901001`，负责人为独立账号 `row12owner / 第12行负责人`。
- 发起人结果：案件详情立即显示该任务为“进行中”，开始/截止时间及“剩余 7 天”已计算，符合“已发出并开始计时”的关系视图语义。
- 负责人权限：测试账号绑定 `ASSISTANT / 律师助理` 权限角色，实际拥有“事务中心 → 我的任务 → 我接受的任务”菜单；不是使用发起人账号替代负责人。
- 接受前：负责人原入口显示“待处理(1)、进行中(0)”，同一任务行状态为“待处理”，显示“接受任务”动作；截图 `local-new-system-pending.png`。
- 接受动作：负责人本人勾选该任务并点击“接受任务”，页面提示“任务已接收”；API 日志记录 `POST /api/v1/tasks/28/accept HTTP/1.1` 返回 `200 OK`。
- 接受后：计数变为“待处理(0)、进行中(1)”，进行中列表显示同一任务状态“进行中”；详情历史记录“接收任务 / 第12行负责人 / 待处理 → 处理中 / 任务已接收”；截图 `local-new-system-accepted.png`。
- 进程命中：API 日志同时记录 `POST /api/v1/tasks` 为 `201 Created`、负责人 `relation=owned` 的待处理与处理中查询均为 `200 OK`，确认全部浏览器请求命中本工作树 18312 进程。
- 聚焦回归：`python -m unittest task_case_acceptance_status_row12_test.py task_serial_number_row11_test.py case_task_creation_approval_independence_row7_test.py case_task_detail_row21_test.py`，6/6 通过。
- 清理：Chrome 标签关闭后停止隔离 Web/API，删除隔离 SQLite（含临时账号、任务及权限夹具）；未 commit、未构建、未部署、未改版本、未写线上数据库。
