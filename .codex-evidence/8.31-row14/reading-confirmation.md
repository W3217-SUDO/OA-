# 8.31 第14行读取确认

工作表：8.31

行号：14

模块：案件中心

问题文字：案件里，不管是律所费用还是平台费用里面哪个费用类型需要申请付款，这个界面都是有问题的，这里需要重新做一个界面，和老系统一样，需要注意的是这里的收款单位是自动识别的，如果系统里有这个收款单位输入关键字就能识别，如果系统没有则需要跳出新增收款单位，这里新增的收款单位需要保存到系统-系统参数-付款类型里面

补充/返工文字：无。

新系统现状图：1张，C列 `26_row14_colC.png`。当前“申请付款”抽屉使用横向宽表，收款单位是普通文本输入，未体现系统参数匹配、候选选择、缺失时新增及付款单位资料维护流程。

原系统目标图：2张，D列 `27_row14_colD.png`、`28_row14_colD.png`。旧系统申请付款列表按费用展示付款金额、备注、收款单位和操作；收款单位支持识别已有记录，操作入口可打开“付款单位”弹窗，弹窗字段为性质、收款单位、开户行、账号信息，确认后回到付款申请流程。

文字要求 -> 当前错误 -> 旧系统目标 -> 验收动作：

1. 律所费用与平台费用的任意费用类型都能申请付款 -> 当前共用界面仅提供普通文本字段，缺少统一的收款单位参数流程 -> 两类费用使用相同付款申请能力 -> 分别从律所费用和平台费用进入申请付款，验证字段和提交链路一致。
2. 输入收款单位关键字自动识别系统已有单位 -> 当前可输入任意文本且没有候选识别 -> 旧系统从付款单位参数匹配 -> 输入部分关键字，只出现匹配的有效付款单位并可选择、回填。
3. 系统没有收款单位时跳出新增 -> 当前没有缺失识别和新增付款单位弹窗 -> 旧系统通过操作入口打开付款单位弹窗 -> 输入无匹配关键字，触发新增入口并打开包含性质、收款单位、开户行、账号信息的表单。
4. 新增单位保存到系统-系统参数-付款类型 -> 当前付款申请与参数管理未形成持久关联 -> 旧系统新增后可复用 -> 新增后核对数据库/参数接口、刷新付款申请候选、再到系统参数付款类型页面确认同一记录可见。
5. 后端不得接受脱离有效参数的伪造收款单位 -> 当前自由文本可能绕过参数归属 -> 付款申请保存稳定参数标识与快照 -> 验证正常选择/新增成功，并验证无效单位或越权请求被后端拒绝且无部分写入。

读取状态：已读文字；已读新系统现状图；已读原系统目标图。
