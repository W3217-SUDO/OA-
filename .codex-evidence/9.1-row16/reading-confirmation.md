# 9.1 第 16 行读取确认

工作表：9.1  
行号：16  
模块：调查大厅  
问题文字：选中调查任务时，该任务对应的草稿合同由于被删除了，所以该条任务也是没有用的，想删除，但是显示只有管理员可以删除，设置没问题，但是我登录的就是管理员账号，还跳出只有管理员删除这就有问题了  
补充/返工文字：无；F/G/H/I 为空。  
新系统现状图：C 列 1 张 `C_image435.png`（C16）。管理员登录（右上 Fwl 范文林），待我分配的调查任务点击删除后提示“删除 0 条，失败 1 条：仅管理员可以删除调查任务”。  
原系统目标图：D 列无图。文字认可“仅管理员删除”的权限规则，目标是管理员身份判断正确并能删除无效调查任务。
已读文字：是。  
已读新系统现状图：是。  
已读原系统目标图：是；该列无图。  

文字要求 -> 当前错误 -> 目标 -> 验收动作：

1. 仅管理员可删除 -> 当前管理员仍被拒绝 -> 使用统一最高权限身份/角色集合判断，不依赖单一旧 role 字符串 -> 管理员成功删除自建无效任务，普通用户 403。
2. 删除无效调查任务 -> 必须检查子任务、线索、附件、事件等依赖 -> 无依赖可删；有真实下游依赖应原子阻断或完整清理本任务自建依赖 -> 聚焦测试覆盖成功、权限失败和依赖门禁。
