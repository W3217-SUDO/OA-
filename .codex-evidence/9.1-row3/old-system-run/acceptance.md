# 9.1 第3行旧系统只读核对

- 登录角色：旧系统本地 `Admin / 管理者`。
- 原问题记录：`SHMS2500615`。从案件列表搜索无结果，直接打开 `CaseView?caseNo=SHMS2500615` 返回“未找到案件.SHMS2500615”；证据为 `original-case-missing.png`。
- 等价记录：`SHMS2500709D`，民事案件、一审准备开庭，旧系统详情可正常打开。
- 等价入口：案件详情 -> 律所费用。
- 页面字段：合同编号、费用类型、金额、退费、提交人、提交日期、回款日期、回款金额、开票日期、发票号、申请付款金额。
- 等价记录当前无费用数据；证据为 `equivalent-case-fee-layout.png`。
- 结论：原记录缺失，等价记录只能证明旧系统字段和布局，不能动态证明原截图中的回款值。旧系统动态数据未完整复现；不得把空费用列表写成旧系统浏览器通过。
- 边界：全程只读，未保存或写入旧库；本次 Chrome 标签已关闭。
