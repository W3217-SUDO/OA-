# 9.1 第 11 行读取确认

工作表：9.1  
行号：11  
模块：事务中心  
问题文字：案件中心，发起任务，这个任务编号太长了，任务编号重新修改一下  
补充/返工文字：无（F 测试、G 截图、H 完成状态、I 完成时间均为空）  
格式与状态：行高 56.25，非隐藏；非空业务单元格无批注、超链接或特殊填充；完成状态为空。  
新系统现状图：截图列 C 共 1 张，锚点 C11，文件 `C_image427.png`。图中“事务中心 → 我的任务 → 我发起的任务”的任务编号列出现 `(人工)RW20260901134253891085`、`(人工)RW20260831134920308173` 等过长编号；红框标出多条人工任务编号。  
原系统目标图：原系统列 D 无图。旧系统目标需由 localhost:8091 对应源码链路只读审计；本会话按派工不启动浏览器。  
已读文字：是。  
已读新系统现状图：是，原始分辨率逐张查看。  
已读原系统目标图：是；该列无图。  

文字要求 -> 当前错误 -> 旧系统目标 -> 验收动作：

1. “发起任务，任务编号重新修改一下” -> 人工任务编号由 `RW + 14 位时间 + 8 位随机/序列` 组成，视觉与抄录成本过高 -> 从旧系统任务编号生成源码确认其短编号规则；若旧系统无明确规则，则保留业务可识别前缀并采用仓库既有短号生成基础设施 -> 聚焦测试证明新建人工任务编号显著缩短、格式稳定且唯一。
2. 截图显示历史长编号 -> 修改不得破坏既有任务查询与详情链接 -> 新旧编号均可作为字符串继续查询/展示 -> 回归测试证明历史长编号仍可读取，新创建任务使用新格式。

浏览器/构建/部署约束：本会话不启动浏览器、不构建、不部署、不改版本、不写线上数据库；最终由主会话按步骤在 Chrome 验收。
