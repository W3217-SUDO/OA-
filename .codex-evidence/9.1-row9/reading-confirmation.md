# 9.1 第9行读取确认

- A9：人员管理。
- B9：修改人员信息时提示“中文姓名已存在”，但该中文姓名实际只有当前这一人。
- C9：2 张锚定图片，均为新系统问题图。
  - `img10_r9_c3.png`：员工完整资料修改 415，用户名 `fwll`、姓名“范文林”，保存时顶部提示“中文姓名已存在”。
  - `img11_r9_c3.png`：员工列表按 `fwl` 查询，共 3 条：用户名 `fwl` 的“范文林”、用户名 `fwl` 的“范文玲”、用户名 `fwll` 的“范文玲玲”；红框标出“范文林”，用于说明没有另一个完全相同的中文姓名，同时也暴露迁移数据中同一登录账号可能对应多条员工档案。
- D9：无文字、无锚定图片，没有旧系统目标图。
- E9：2026.9.1。
- 截图交叉核对：弹窗中的用户名与列表里姓名“范文林”对应的用户名不同；列表还显示另一个用户名对应“范文玲玲”。因此截图证明系统中确有另一个账号占用了“范文林”，不能把该 409 直接判为误报。
- 目标确认：编辑当前人员并保持本人中文姓名时不得报重复；改成其他账号已占用的精确同名必须阻断；仅相似姓名不得误判。
- 返工识别：8.28/8.31 未发现同项，本行按 9.1 新问题处理，不触发旧系统 Chrome 返工实跑槽位。
