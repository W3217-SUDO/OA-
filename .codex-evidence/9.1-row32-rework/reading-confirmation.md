# 9.1 第32行读取确认

- 权威工作簿：`C:\Users\Administrator\Desktop\OA系统\问题\OA系统对接9.1.xlsx`（2026-09-02 02:26:51，49,792,121 bytes）
- 工作表：`9.1`
- 行号：`32`
- 扫描范围：`A32:I32`（工作表实际使用范围 `A1:I37`）
- 结构读取：已分别以公式模式和缓存值模式读取；本行无公式，二者值一致；无合并单元格、批注、超链接、隐藏列或隐藏行，行高 `122.25`。
- 当前完成判断：F32 存在红字返工文字，故 H32 的历史数字 `1` 已被返工门禁撤销，本轮不得沿用先前完成结论；I32 为空。

## 逐列读取清单

| 列 | 表头 | 完整文字/内容 | 格式与优先级 | 图片 | 业务用途 |
|---|---|---|---|---:|---|
| A32 | 模块 | 案件中心 | 样式 16；四边框；无批注/超链接/合并 | 0 | 业务模块 |
| B32 | 问题 | 案件信息里，线索信息，点击线索编号，现在出现的界面是直接跳到了调查大厅里，实际上，不仅仅要查看这条线索相关的信息，还需要有可以修改公证书的功能，对照老系统，可以选择一条取证信息，修改取证机构、公证书号等等，所以，这里的功能需要复制老系统界面 | 样式 18；四边框；无批注/超链接/合并 | 0 | 指定入口、现象和旧系统目标能力 |
| C32 | 截图 | 空文本，含新系统截图 | 样式 16；四边框；无批注/超链接/合并 | 2 | 新系统当前行为：案件详情线索编号；点击后跳至调查大厅详情弹窗 |
| D32 | 原系统 | 空文本，含旧系统截图 | 样式 16；四边框；无批注/超链接/合并 | 2 | 旧系统目标：案件详情内右侧线索信息面板、线索文件、取证信息及修改弹窗 |
| E32 | 发起时间 | 2026.8.31 | 样式 16；四边框；无批注/超链接/合并 | 0 | 提交日期 |
| F32 | 测试 | 是可以查看了，也可以修改了，但是界面直接跳到调查大厅去了，这里仅仅在案件信息里跳出来即可，不要到处跳，界面很乱，复制老系统界面 | 样式 24；字体色 `FFFE0300`（红字）；四边框；无批注/超链接/合并；最高优先级返工原因 | 0 | 撤销先前完成判断，要求保持案件详情上下文并复制旧系统交互载体 |
| G32 | 截图 | 空 | 无文字/公式/批注/超链接；无新增锚定图片 | 0 | 无新增返工截图 |
| H32 | 完成状态 | 1 | 数字；与 F32 返工文字冲突，按返工规则视为未完成 | 0 | 历史状态，不作为完成依据 |
| I32 | 完成时间 | 空 | 无文字/公式/批注/超链接 | 0 | 无完成时间 |

## 原文与返工确认

- 模块：案件中心
- 问题文字：案件信息里，线索信息，点击线索编号，现在出现的界面是直接跳到了调查大厅里，实际上，不仅仅要查看这条线索相关的信息，还需要有可以修改公证书的功能，对照老系统，可以选择一条取证信息，修改取证机构、公证书号等等，所以，这里的功能需要复制老系统界面
- 补充/整改文字：F32（测试/返工）“是可以查看了，也可以修改了，但是界面直接跳到调查大厅去了，这里仅仅在案件信息里跳出来即可，不要到处跳，界面很乱，复制老系统界面”
- 返工原因/返工备注：F32 为红字 `FFFE0300`，明确否定先前“跳转调查大厅”的验收路径；无其他批注或备注。
- 已读文字：已按 A:I 全列结构化读取并逐字保留 B32 与 F32。
- 已读新系统现状图：C32 共 2 张，见下方逐图记录。
- 已读原系统目标图：D32 共 2 张，见下方逐图记录。
- 返工截图：G32 无图；返工要求由 F32 红字文字给出。

## OOXML 图片锚点确认

| 文件 | 工作簿媒体 | 锚点 | 表头 | 含义 |
|---|---|---|---|---|
| `row32-col3-image45.png` | `xl/media/image392.png` | C32 | 截图 | 新系统案件详情线索列表入口 |
| `row32-col3-image46.png` | `xl/media/image393.png` | C32 | 截图 | 新系统错误跳转后的调查大厅详情 |
| `row32-col4-image47.png` | `xl/media/image394.png` | D32 | 原系统 | 旧系统案件详情内线索主从工作区 |
| `row32-col4-image48.png` | `xl/media/image395.png` | D32 | 原系统 | 旧系统案件详情上方取证编辑模态框 |

## 图片语义比对

1. `row32-col3-image45.png`：新系统案件详情“线索信息”页签，线索编号为可点击链接。
2. `row32-col3-image46.png`：点击链接后离开案件详情上下文，跳到调查大厅“待提交线索”，再弹出通用“调查详情”对话框；这是返工明确否定的行为。
3. `row32-col4-image47.png`：旧系统仍停留在案件详情，右侧以局部面板展示“线索信息”，其下包含“线索文件”和“取证信息”，取证记录可勾选并提供删除、修改操作。
4. `row32-col4-image48.png`：旧系统点击取证记录“修改”后，在案件详情上方打开“取证”模态框，可编辑取证机构、公证书号、取证时间、发票号码、证物存放处、证物状态和证据文件。

## VibeHub 与专业定位表达

- 脱敏候选：`上下文弹窗`、`关联记录编辑`、`主从视图`。
- Resolver：2026-09-02 审计时再次批量调用；`上下文弹窗`、`关联记录编辑`、`主从视图` 均返回 `count: 0`，无可靠词条，因此无需补充外部术语链接。
- 专业定位表达：案件详情的线索编号应触发**上下文内主从详情视图**，在不改变案件页面载体和导航上下文的情况下展示关联线索、线索文件和取证记录；取证记录应支持选择后进行**关联记录编辑**，并保持保存后的数据持久化和当前案件上下文。
- 源码检索词：案件详情线索信息、线索编号、取证信息、修改取证、notary/evidence/clue detail、drawer/modal。

## 文字粗定位（不含根因或方案）

- 模块与原入口：案件中心 → 案件信息 → 线索信息 → 点击线索编号。
- 对象：当前案件关联线索、线索文件、该线索的取证记录及公证/证物字段。
- 当前现象：入口离开案件详情上下文并进入调查大厅通用详情；虽能查看、修改，但返工文字明确否定该导航载体。
- 可观察期望：保持当前案件详情，在案件信息内弹出与旧系统对应的线索主从工作区；可选择一条取证信息打开修改表单，保存后仍处于当前案件上下文并回读持久化数据。

## 原问题/返工要求到验收动作映射（定位阶段）

| 原子要求 | 当前错误 | 旧系统截图目标 | 后续验收动作 |
|---|---|---|---|
| 点击案件信息中的线索编号查看关联线索 | 跳到调查大厅并叠加通用详情弹窗 | 案件详情右侧显示线索信息 | 从同一案件详情点击同一线索编号，确认 URL/顶部案件页签/左侧案件内容不被替换 |
| 不仅查看线索，还能处理关联取证信息 | 通用详情只展示汇总字段，未呈现旧系统分区结构 | 线索信息下依次显示线索文件、取证信息 | 核对三个分区、字段顺序、文件操作和取证列表均在上下文内出现 |
| 选择一条取证信息后修改取证机构、公证书号等 | 先前修改入口位于调查大厅跳转后的界面 | 勾选取证记录 → 修改 → “取证”模态框 | 使用本任务自建/可安全修改的取证记录，逐字段编辑并保存 |
| “这里仅仅在案件信息里跳出来即可，不要到处跳” | 当前导航载体与返工要求冲突 | 旧系统保持案件详情，右侧面板 + 上层模态框 | 证明不进入调查大厅路由、不新增无关工作页签，关闭弹层后仍回到原案件线索页签 |
| 保存结果真实持久化 | 历史状态 `1` 与返工冲突，先前证据不能证明本轮目标 | 取证列表应回显更新值且关系不变 | 刷新/重新进入同一案件与线索，核对字段回读、案件—线索—取证关联及失败路径零部分写入 |

## 分层定位门禁与旧系统动态复现

- 图片语义比对：已完成 4/4 原图逐张复核。
- 同角色/同入口：已在 `http://localhost:8091` 使用截图对应的 `Admin 管理者`，从顶部案号搜索进入案件详情，再点击“线索信息”中的线索号。
- 原截图记录缺失：旧库只读查询 `Legal_Investigation_Clue`，原案号 `SHMS2300502` 与原线索号 `P23034184` 均为 `0` 条；页面真实搜索同样无结果。因此未伪造原记录动态成功。
- 等价动态样本：同模块现存案号 `SHMS2600426`、线索号 `P26085929`；点击后案件详情保持不变，右侧出现“线索信息”侧栏，依次呈现线索基本信息、线索文件、取证信息。证据：`old-system/01-equivalent-case-inline-clue-panel.png`（127,747 bytes，已视觉复核）。
- 旧库数据完整性限制：旧库当前有 `37` 条有效案件线索、`4006` 条取证记录、`31933` 条线索文件、`2488` 条取证文件，但现存 37 条案件线索与取证/文件表无可关联记录，等价动态样本只能证明侧栏层级，无法在本地旧库真实点击已有取证记录修改；字段和操作以原图及旧源码补齐，未冒充动态成功。
- 旧源码/数据依据：已在动态复现后完成追踪；新系统源码/数据也已随后追踪；门禁顺序完整。

## 旧系统权威来源追踪

- 案件线索列表：`Areas/Legal/Controllers/CaseClueController.cs` 的 `CaseClueList` 按 `CaseNo` 调 `InvestigationClueService.GetInvestigationClueList`；`Areas/Legal/Views/CaseClue/ClueList.cshtml` 将线索号绑定到 `investigation.InvestigationClue.View(clueNo)`。
- 案件内局部载体：`Scripts/CIT/Investigation/CIT.Investigation.js` 的 `InvestigationClue.View` 请求 `/CIT/InvestigationClue/Clue`，清空并写入 `.sidebox-content` 后只执行 `#sidebox.show()`，没有跳转调查大厅。
- 分区结构：`Areas/CIT/Views/InvestigationClue/PartialView/Clue.cshtml` 先呈现线索基本信息，再呈现“线索文件”和“取证信息”。
- 取证单选与修改：同一脚本要求恰好选择 1 条取证记录；0 条提示“请选择取证信息”，多条提示“只能选一个取证信息”；`Edit` 请求 `/CIT/InvestigationClueEvidence/Edit` 并显示 `#investigationClueEvidenceModal`。
- 取证列表：`Areas/CIT/Views/InvestigationClueEvidence/PartialView/List.cshtml` 的顺序为公证书号、取证时间、取证机构、发票号、仓库、库位、状态、文件，底部为删除、修改。
- 编辑表单：`Areas/CIT/Views/InvestigationClueEvidence/PartialView/Create.cshtml` 包含取证机构、公证书号、取证时间、发票号码、证物存放处、证物状态、证据文件及确定/取消。旧源码中“取证机构”为 `readonly=true`；但 B32 明确要求可修改取证机构，故本轮按用户原始要求保留该字段可编辑，并在此记录与旧源码的差异。
- 保存校验：旧脚本 `CreateUpdate` 至少校验公证书号和取证机构非空，保存后关闭模态框并重新加载取证列表。

## Legacy UI equivalence matrix

| 项目 | 旧系统动态/源码依据 | 本轮实现与验收 |
|---|---|---|
| 导航载体 | 案件详情右侧 `.sidebox`，不调用调查大厅导航 | `case-clue-context-panel` 是案件详情 `case-detail-workbench` 的直接子级；无 `onNavigate`/调查大厅上下文 |
| 页面标题 | 线索信息；取证 | 侧栏“线索信息”；上层“修改取证信息”模态框 |
| 步骤与转换 | 点击线索号 -> 侧栏；单选取证 -> 修改 -> 保存 | 同入口、同顺序完成 Chrome 真实操作 |
| 分区层级 | 线索基本信息 -> 线索文件 -> 取证信息 | 三段均在案件内侧栏，截图逐项可见 |
| 列顺序 | 公证书号、取证时间、取证机构、发票号、仓库、库位、状态、文件 | 已按旧列表顺序排列 |
| 只读/可编辑 | 线索只读；旧源码取证机构只读，其余修改字段按表单 | 线索只读；按 B32 明确要求允许修改取证机构及其他取证字段 |
| 默认值 | 现有取证值回填 | `CODEX-901-R32-EVIDENCE` 的旧值完整回填 |
| 操作 | 文件下载；取证删除/修改；确定/取消 | 文件下载、删除、修改、确定/取消均在侧栏/模态框内 |
| 校验 | 取证机构/公证书校验；单选限制 | 前端必填 + 后端证物状态白名单；本轮真实触发“证物状态无效”，无写入 |
| 持久化 | 保存后重载取证列表 | 保存后列表立即回读，刷新案件页再打开仍回读新值 |
| 下游结果 | 案件/线索关系不变 | 案号 `SHMS2300502`、线索 `CODEX-901-R32-CLUE`、取证 `CODEX-901-R32-EVIDENCE` 关系保持 |

## 新系统来源、数据与误验收审计

- 前次实现来源：提交 `92c2e05b`（`fix: keep clue evidence editing inside cases`）新增 `/investigations/clues/{id}/workspace`、取证修改接口调用和页面根部的 `width={980}` Ant Design `Drawer`。
- 前次正确部分：`openRelatedClue` 已不调用 `onNavigate` 或 `rememberInvestigationDetailTarget`，接口也能返回关联线索、文件与取证数据。
- 前次遗漏：线索工作区仍作为 `CaseCenterPage` 根部全局抽屉渲染，而不是案件详情工作台内部子视图；980px 抽屉覆盖/推移案件详情，视觉载体仍像切换到另一个业务页面。先前静态测试只断言“不出现 onNavigate”和存在查看/修改字段，没有断言工作区在案件详情 DOM 内，因而误判通过。
- 新接口权威数据：`apps/api-server/app/main.py` 的 `get_investigation_clue_workspace` 按可见范围查询线索、关联取证及附件；`update_evidence_record` 要求记录所有者/管理角色，写入取证数据、流程日志并同步主取证记录到线索数据。
- 独立新库：`runtime/row32.sqlite`，只用于本任务本地验收；测试前缀 `CODEX-901-R32-`，案件 `17`、线索 `9003201`、取证 `9003202`。

## 具体根因与修改

- 根因：前次把“不要跳调查大厅”理解为仅删除路由跳转，未把 F32 的“仅在案件信息里跳出来、复制老系统界面”落实为**案件详情容器内的上下文侧栏**；同时回归测试没有验证 DOM 所属层级与旧系统列序/操作，导致全局抽屉被误验收。
- `apps/admin-web/src/CaseCenterPage.tsx`：将线索查看区从页面根部全局 Drawer 移入 `case-detail-workbench`；补齐旧系统线索字段、线索文件、取证表列序、删除/修改操作；关闭侧栏时清理选中与编辑状态。
- `apps/admin-web/src/case-center.css`：新增案件内右侧侧栏的绝对定位、层级、滚动、表格和响应式规则，使其只覆盖当前案件工作台。
- `apps/api-server/case_clue_inline_workspace_row32_test.py`：新增“侧栏必须在案件详情 Drawer 结束前渲染”的层级断言，并覆盖删除/修改及旧列表字段。

## 测试与独立 Chrome 验收

- 聚焦测试：`python -m unittest apps.api-server.case_clue_inline_workspace_row32_test`，4/4 通过。
- 生产构建：`apps/admin-web` 执行 `npm run build`，TypeScript 与 Vite 构建通过（仅既有 chunk size warning）。
- 隔离环境：前端 `127.0.0.1:15432`；API `127.0.0.1:18032`；独立 SQLite、上传目录、日志、Chrome 页签均位于本任务范围。
- Chrome 入口：`Admin 管理者` -> 案件中心 -> 我的案件 -> 民事争议 -> 搜索 `SHMS2300502` -> 案件详情 -> 点击 `CODEX-901-R32-CLUE`。
- `new-system/01-case-inline-clue-panel.png`：案件详情仍在左侧，右侧为局部“线索信息”侧栏，未出现调查大厅页签；已视觉复核。
- `new-system/02-evidence-edit-dialog.png`：选择取证记录后，在案件详情/侧栏之上打开修改模态框，字段回填完整；已视觉复核。
- 失败路径：夹具初始证物状态 `Stored` 不在后端白名单，点击确定真实返回“证物状态无效”；数据库无修改日志/无部分字段写入。随后选择合法“已入库”再保存。
- `new-system/03-evidence-updated.png`：保存后侧栏立即回显 `CODEX-901-R32-CERT-NEW`、`Row32 Notary Office Updated`、`CODEX-901-R32-INV-NEW`；已视觉复核。
- `new-system/04-refresh-persisted.png`：刷新当前案件页、重新点击同一线索后仍回显新值；已视觉复核。
- SQLite 回读：线索与取证均持久化新取证机构、公证书号、发票号、存放处和“已入库”；`workflow_events` 仅有一次成功的“修改证据信息”日志。
- 清理：已从 `business_records`、`workflow_events`、`investigation_evidences`、`Legal_Investigation_Clue`、`Legal_Investigation_Clue_Evidence` 清除全部 `CODEX-901-R32-` 夹具/投影，五处复核计数均为 `0`；已关闭本行旧/新系统 Chrome 页签；已停止 `15432`/`18032` 服务并确认端口不再监听；已删除临时 `.env.local`。截图与读取确认保留在本行证据目录，隔离 SQLite、日志、PID 文件、上传目录及整个 `runtime` 目录已精确删除且不进入 Git。
