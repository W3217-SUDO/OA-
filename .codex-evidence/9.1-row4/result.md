# 9.1 第 4 行结果

- reading-confirmation：已保存于同目录 `reading-confirmation.md`；已读文字、C4 新系统现状图 1 张、D4 原系统列无图。
- 历史返工审计：8.28/8.31 工作表未命中“立案日期/线索生成案件/已取证/生成案件”的同项返工文字。8.31 第 9 行既有代码与测试仅补齐线索转案的案由、经办律师、律师助理，未覆盖立案日期。
- 旧系统证据：只读源码 `Areas/Legal/Controllers/CaseController.cs` 的 `ClueToCaseCreateUpdate` 在新建线索案件时执行 `c.Basic.CaseRegisterDate = DateTime.Now`。
- 根因：新系统 `batch_create_cases_from_clues` 创建案件时遗漏 `case_register_date/filing_date`；详情页读取链路存在，故 UI 显示 `—`。
- 修改：转换动作在创建案件数据时将当天生成日期同时持久化为 `case_register_date` 和兼容别名 `filing_date`。
- owned files：`apps/api-server/app/main.py`；`apps/api-server/investigation_clue_case_register_date_row4_test.py`。
- 聚焦测试：行 4 历史回填/新流程写入/兼容字段边界测试 + 8.31 行 9 回归 + 8.13 行 12 回归，最终共 7 项通过；`py_compile` 通过；`git diff --check` 通过。
- 构建/浏览器/部署/版本/线上数据库：依用户约束均未执行。
- 数据库补丁建议：历史已由线索生成且立案日期为空的案件需要独立、幂等回填；以案件的“线索生成案件”工作流事件时间转换为业务时区日期，并仅填空值，不覆盖人工已有日期。发布前先输出候选案号、来源线索号、事件时间和拟回填日期供审计。
- 线上原记录只读导出：使用用户授权的 8089 Admin 陶威账号，仅调用登录与 GET 接口。确认案件 `47150/SHMS2600438`、线索 `4111/M26085930`、来源任务 `3993/RW24154948210`、取证记录 `47149/ZJ-M26085930`、固定任务 `47151/47152`、附件 `668`、生成事件 `4753/4754` 及全部相关工作流事件；未调用线上写接口。原案件 `case_register_date/filing_date` 均缺失。原线索的 `investigation_record_id=28383` 在线上实际解析为仓库记录 `WMS-LEGACY-2690`，不是调查记录；隔离恢复原样保留该既有异常，没有伪造父调查。
- 历史根因补充：首次提交只修复后续新转案，不能闭环截图中的既有原案件。现增加幂等启动回填：处理 `batch_converted=true` 且 `case_register_date`、`filing_date` 任一兼容字段缺失的案件，仅补缺失字段；其中一个字段已有时优先复用已有值补齐另一个，绝不覆盖已有值；只有两个字段皆空时，才优先采用“线索生成案件”事件时间（无事件则采用 `created_at`），按 Asia/Shanghai 转成业务日期后补齐。
- 本地隔离恢复：API `19014`、Vite 前端 `19015`、独立 SQLite；恢复原案号/线索/任务/取证/附件实体/用户/事件。启动后原案件两个日期键均为 `2026-09-01`。
- Chrome 原记录验收：Admin 陶威登录本地隔离实例，打开同一页面 `case-detail-47150-SHMS2600438`；案件信息同时显示案号 `SHMS2600438`、线索号 `M26085930`、立案日期 `2026-09-01`。系统日志保留 `2026-09-01T07:51:06.288426` 的“线索生成案件”及来源线索。刷新并重新加载后日期保持；截图 `local-original-record-accepted.png`。
- 失败路径：对原已转案件线索再次调用生成案件，返回 created=0、failed=1、“线索已经转为案件”；案件总数前后均为 1，无重复案件、任务或事件。
- 新增聚焦测试：覆盖历史原记录按生成事件业务日期回填、新流程写入，以及“已有 `filing_date` 不被覆盖且补齐 `case_register_date`”兼容字段边界；含相关回归最终共 7 项通过。
- Chrome/服务清理：验收后关闭本行 Chrome 标签并释放槽位；停止 `19014/19015`；隔离 DB、下载附件、日志及临时依赖链接已移入 Windows 回收站（可恢复），临时前端配置已删除。线上数据始终只读。
