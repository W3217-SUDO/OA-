# 9.1 第4行 Chrome 槽位准备单

## 原问题与记录

- 原文：案件中心，案件信息里立案日期不显示，调查员在选择已取证的线索生成案件的日期就是该案的立案日期。
- C4 原图：`img4_r4_c3.png`，案件 `SHMS2600438`，来源线索 `M26085930`；系统日志“线索生成案件”时间 `2026-09-01T07:51:06.288426+00:00`，操作人陶威，案件信息“立案日期”为 `—`。
- D4 无原系统目标图。旧源码 `CaseController.ClueToCaseCreateUpdate` 以 `DateTime.Now` 写 `CaseRegisterDate`。
- 2026-08-24 只读合并审计快照中，原线索为 id 4111、owner `fwl`、调查取证部、状态“待取证”、客户“测试555”、来源任务 `RW24154948210`；该快照早于 2026-09-01 转案。
- 当前指定的本地开发 SQLite 及扫描到的其他本地 `legal_platform.db` 均未找到 `SHMS2600438` 或 `M26085930`。因此必须先取得原记录的只读导出并恢复到隔离库；不得用另一个 `CODEX` 案件冒充原记录闭环。

## 所需账号

- 主验收账号：原线索 owner `fwl` 对应的启用账号，且菜单含“调查大厅 → 我的调查线索 → 已取证线索”，数据范围能看见 `M26085930`，具“生成案件”动作；这是调查员业务角色。
- 原历史结果核对账号：原截图操作人“陶威”对应启用账号，需能看见案件 `SHMS2600438` 及系统日志。若其菜单权限/数据范围/流程位置与调查员不等价，只用于历史详情核对，不能代替 `fwl` 执行生成动作。
- 失败权限路径账号：一个能登录但没有原线索数据范围或没有调查线索写权限的启用普通账号，用于验证不可见/不可执行；不得用管理员代替该失败角色。
- 登录凭据从隔离库既有种子账号或已保存会话确认，不在证据中记录明文。

## 隔离运行配置（仅计划，尚未创建或启动）

- 代码：当前工作树 `C:\Users\Administrator\Desktop\OA系统\.codex-worktrees\901-rows4-10`。
- 隔离数据库建议：`C:\Users\Administrator\Desktop\OA系统\.codex-worktrees\901-rows4-10\.codex-runtime\row4\legal_platform-row4.db`。
- 数据来源：复制本地开发基线库后，只向隔离副本恢复原案件 `SHMS2600438`、原线索 `M26085930`、来源任务/合同、用户、角色权限、案件固定任务和两条记录的工作流事件。原始库和线上库保持只读。
- API：`127.0.0.1:19014`；通过独立 `DATABASE_URL=sqlite+aiosqlite:///.../legal_platform-row4.db` 启动。
- 前端：`127.0.0.1:19015`；API 指向 `http://127.0.0.1:19014/api/v1`。
- 端口准备检查时 `19014/19015` 均无监听；旧系统 `8091` 已监听。本轮未启动任何服务、未打开 Chrome。

## 成功验收

### A. 原历史问题记录（闭环必做）

1. 启动隔离实例后，以陶威对应可见账号登录，搜索并打开原案件 `SHMS2600438`，确认 URL/页面案号一致，不接受替代案号。
2. 在系统日志定位“线索生成案件”，核对来源线索 `M26085930` 与原事件时间；截图保存案号、事件、时间和来源线索。
3. 核对案件信息“立案日期”。预期为生成动作在业务时区的日期 `2026-09-01`，不得取线索创建日、取证日或浏览器当天。
4. 刷新页面并重新从案件列表进入，立案日期仍为 `2026-09-01`。
5. 只读核对隔离 DB 中该案件 `data.case_register_date` 与 `data.filing_date` 均为 `2026-09-01`，且原工作流事件未被改写。
6. 若原案件仍显示 `—`，立即判失败：当前提交只覆盖新转换，尚缺原历史记录幂等回填，不能标记第4行完成。

### B. 新流程成功路径（原记录闭环后补测）

1. 以 `fwl` 调查员角色进入“我的调查线索 → 已取证线索”，选择一条隔离副本内 `CODEX-901-R4-*` 已取证线索。
2. 点击“生成案件”，第一步填写客户诉讼地位、案由、案件阶段、经办律师、律师助理、案件类型；记录点击最终“生成案件”的本地业务日期。
3. 生成结果必须为成功 1、失败 0；线索状态变为“已转案件”，并显示生成案号。
4. 打开生成案件详情，确认“立案日期”等于最终生成动作日期；系统日志来源线索号正确。
5. 刷新并重新进入，日期保持；只读核对两个日期键一致。

## 失败路径

1. 已转案件：再次对 `M26085930` 发起转换必须被拒绝为“线索已经转为案件”，不得新建第二案件或改写原立案日期。
2. 状态约束：对隔离测试的“草稿/待审批/待取证”线索执行生成案件，必须提示完成取证后才能转案，数据库无新增案件、固定任务或事件。
3. 必填校验：已取证测试线索缺案由、经办律师或律师助理时，前端不得进入最终成功；绕过 UI 调 API 也应返回对应错误且无部分写入。
4. 人员有效性：选择停用或不存在的经办律师/助理必须失败，不得生成只有展示名、没有账号身份的案件。
5. 权限/范围：无原线索数据范围或无调查线索写权限的普通账号不得看见或转换该线索；失败后数据库保持不变。

## 清理

1. 原案件 `SHMS2600438`、原线索 `M26085930`、原来源任务/合同及原工作流事件均为验收基准，不删除、不改编号、不清理。
2. 仅删除本轮 `CODEX-901-R4-*` 测试线索生成的案件、案件固定任务、案件/线索工作流事件、线索 `converted_case_id/converted_case_no` 回填、附件及测试线索本身。
3. 删除前按前缀和精确 ID 输出清单，确认所有目标都位于隔离 DB；不得使用模糊案号或递归删除。
4. 关闭第4行所有 Chrome 标签，停止 `19014/19015` 隔离进程；隔离 DB 和截图保留为证据，待主会话审查后再决定回收。

## 当前门禁结论

- 尚未获得 Chrome 槽位，未打开 Chrome。
- 原记录本地动态数据缺失。若正式槽位前仍未获得原案件/线索及关联事件的只读导出，只能验证入口和新流程，不能完成原记录闭环，也不能宣称第4行浏览器通过。
