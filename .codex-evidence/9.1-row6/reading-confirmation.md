# 9.1 第 6 行读取确认单

工作表：9.1

行号：6

模块：客户管理

问题文字：客户里面新建合同，合同提交审批，提交用印，但是在待用印里面和审批人的待审批用印里面都没有这个数据

补充/返工文字：无（F6“测试”、G6“截图”、H6“完成状态”、I6“完成时间”均为空；无批注、超链接、隐藏列、合并单元格或特殊填充）

已读文字：是。已从工作簿结构逐字读取，发起时间 2026.9.1，完成状态为空。

已读新系统现状图：是。C 列 2 张：

1. `img6_r6_c3.png`（1917×927，锚点 C6）：合同 `SHHT2673411` 的新建合同第 4 步“合同用印”，已选择同步用印并生成用印申请 `YY202609011322136E1`，时间线显示“配置同步用印”“生成用印申请…保存为草稿”“提交合同审批（草稿→审批中）”，页面提示合同审批通过后自动提交用印审批。
2. `img7_r6_c3.png`（1920×841，锚点 C6）：用印中心“待审批用印”只有 3 条旧数据，没有合同 `SHHT2673411` / 用印申请 `YY202609011322136E1`。

已读原系统目标图：是。D 列该行无图。目标由问题文字及业务路径明确：客户新建合同同时提交合同审批和用印后，用印数据应进入申请人的待用印范围，并进入指定审批人的待审批用印范围，不能只停留在不可见草稿。

文字要求 -> 当前错误 -> 旧系统目标 -> 验收动作：

1. 客户新建合同提交审批并同步提交用印 -> 当前已生成用印申请编号但状态是草稿/不可见 -> 同步用印动作应形成可追踪、可办理的真实申请 -> 完成真实合同/用印提交后，用申请编号在“我的用印申请/待用印”查询命中。
2. 审批人应收到待审批用印 -> `YY202609011322136E1` 未出现在审批人的待审批列表 -> 审批任务、审批人和用印申请状态必须一致生成 -> 切换到指定审批人，按同一申请编号命中且可执行审批；非审批人不可越权办理。
3. 合同与用印状态联动 -> 时间线宣称合同通过后自动提交，但当前链路未证明后续真正发生 -> 合同审批状态变化必须原子地驱动用印提交或明确阻断 -> 审批合同后刷新两类用印列表、API 和数据库，确认同一申请从草稿进入待审批且审批任务存在；失败时不得留下半状态。

当前阶段限制：不启动浏览器、不构建、不部署、不改版本、不写线上数据库；只做源码链路、聚焦测试、证据与独立提交。
