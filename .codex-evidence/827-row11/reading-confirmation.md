# OA系统对接8.27.xlsx 第11行阅读确认

- 原始工作簿：`C:\Users\Administrator\Desktop\OA系统\问题\OA系统对接8.27.xlsx`
- 工作簿 SHA-256：`0224a8ca7fb7ce729be25546824c5d789e8fc3fd07a71300d4b0df1b5297fd6f`
- 工作表：`8.27`
- 行号：`11`
- 模块：`案件中心`
- 问题文字：`案件中心，我的案件（部门案件、公司案件），进入民事争议后，左侧会有一列关于案件阶段的数据，但是目前有个别数据存在问题，匹配不到与老系统一样的阶段，我已经逐个比对过了，麻烦修改时认真比对一下，这里数据太多，容易出错`
- 补充/返工文字：无。F（测试）、G（测试截图）、H（完成状态）、I（完成时间）均为空；无批注、超链接、隐藏列、合并单元格或额外红字/填充色返工语义。

## 已读文字

问题范围是“我的案件、部门案件、公司案件”三个民事争议入口左侧的完整案件阶段树。要求不是修复单个计数，而是逐项核对阶段节点的名称、父阶段归属、状态映射、数据范围和计数，使其与旧系统一致。

## 已读新系统现状图

OOXML 映射：工作表部件 `xl/worksheets/sheet4.xml`，绘图部件 `xl/drawings/drawing4.xml`。第 11 行全部图片均锚定 C 列（表头“截图”），共 4 张：

1. `xl/media/image118.png` → `source-images/8.27_row11_C_20.png`（1675×975）：新系统前景在“一审阶段”下标红显示“一审待执行 [38]”；旧系统背景把待执行节点归入“执行阶段”，说明父阶段归属/状态映射错误。
2. `xl/media/image119.png` → `source-images/8.27_row11_C_21.png`（1514×927）：新系统前景在“二审阶段”下标红显示“二审待执行 [8]”；旧系统背景把“二审待执行”归入“执行阶段”，说明父阶段归属/状态映射错误。
3. `xl/media/image120.png` → `source-images/8.27_row11_C_22.png`（1655×993）：新系统前景执行阶段仅显示“执行立案、执行受理、执行中止、执行结案、执行终本、终结执行、执行和解中”等节点；旧系统背景执行阶段还包含“一审待执行、二审待执行、准备材料、提交法院、执行亏损、执行异议”等节点，且“执行终结/终结执行”等命名与状态计数映射明显错位。
4. `xl/media/image121.png` → `source-images/8.27_row11_C_23.png`（1562×1008）：新系统前景归档阶段显示“归档审核、已归档、归档拒绝、亏损审核、亏损归档”，旧系统背景包含“亏损申请、亏损拒绝”等独立节点；标红区域表明新系统缺少或错误合并了旧系统亏损拒绝阶段语义。

四张图片都是“旧系统背景 + 新系统前景”的对照合成图；按表头语义，它们仍属于 C 列的新系统问题现状证据。

## 已读原系统目标图

D 列（表头“原系统”）无独立图片。旧系统局部目标已出现在上述四张 C 列对照合成图的背景中，但后续必须在只读本地旧系统及源码中复现并追踪完整阶段树、查询条件、状态代码和数据范围，不能把合成图当作全部业务逻辑证据。

## 文字要求 -> 当前错误 -> 旧系统目标 -> 验收动作

1. 三个入口阶段树一致性 -> 新系统可能由入口各自过滤或共享错误映射造成不一致 -> 旧系统“我的/部门/公司民事争议”按相同阶段定义、各自数据范围计数 -> 用同一真实角色逐一打开三个入口，记录节点全集、顺序、父阶段、计数与点击后的案件集合。
2. 一审/二审待执行归属 -> 当前落在一审/二审阶段 -> 旧系统归入执行阶段 -> 页面确认原父阶段不再出现，执行阶段出现对应节点；点击后 API 查询只返回目标状态案件。
3. 执行阶段全集与映射 -> 当前节点缺失、命名和计数错位 -> 对齐旧系统待执行、准备材料、提交法院、受理、中止、结案、终本、终结、亏损、异议、和解等完整状态定义 -> 对每个节点核对前端标签、后端查询值、数据库状态/阶段字段及点击结果，不能只比总数。
4. 归档/亏损阶段 -> 合成图前后景遮挡，不能仅凭可见文字确定完整权威目录 -> 继续以旧阶段主数据和源码核定准确名称、父级与顺序 -> 核对状态机、权限、数据筛选及各节点计数，特别验证亏损拒绝不会被吞并到亏损归档。
5. 大数据量防错 -> 当前计数无法证明记录归类正确 -> 旧系统状态代码与查询条件是一一对应的权威依据 -> 增加聚焦测试覆盖节点定义和状态映射，并用数据库分组查询与浏览器点击结果交叉核对。
