# 9.1 第 14 行读取确认

工作表：9.1  
行号：14  
模块：调查大厅  
问题文字：这个地方也是的，关键字无法匹配到所有对应人员，问题同第八条，说明人员匹配不仅仅是这两个地方，其他地方也要同时筛查  
补充/返工文字：无；F/G/H/I 为空。按用户边界不得读取未分配第 8 行，仅以本行文字明确的“全局人员匹配”要求审计共享人员选择器。  
新系统现状图：C 列 1 张 `C_image432.png`（C14）。待我分配的调查任务→新增子任务抽屉，负责人输入“范”后仅出现部分范姓人员，红框标出候选列表。  
原系统目标图：D 列无图。目标由文字确定为关键字匹配所有符合条件的系统人员，并同步审计其他人员选择入口。  
已读文字：是。  
已读新系统现状图：是。  
已读原系统目标图：是；该列无图。  

文字要求 -> 当前错误 -> 旧系统目标 -> 验收动作：

1. 负责人关键字匹配全部对应人员 -> 候选源/前端过滤截断 -> 对启用人员按中文姓名和账号完整包含匹配 -> 输入姓氏及姓名中段，候选集合等于 API 全部命中。
2. 其他人员选择处同步筛查 -> 不能只修当前抽屉 -> 共用人员 API/选择器避免入口各自截断 -> 静态审计调查、任务、案件等共用入口并补共享契约测试。
