# 9.1 第14行结构化读取确认（2026-09-02）

## 全列原文

- A：`调查大厅`
- B：`这个地方也是的，关键字无法匹配到所有对应人员，问题同第八条，说明人员匹配不仅仅是这两个地方，其他地方也要同时筛查`
- C：空文字；锚定图片 `C_image432.png`，1916×936。图中为“待我分配的调查任务 → 新增子任务”，负责人输入“范”后的候选列表。
- D：空，无图片。
- E：`2026.9.1`
- F/G/I：空。
- H：`1`（本次委托明确返工，不作为完成依据）。

## 批注、格式和图形

- A:I 无批注、无超链接、无合并单元格；第14行行高 56.25。
- A/B/C/E 为细边框表格样式；B 自动换行且垂直居中；D/F/G/I 为空白默认样式；H 为普通数值格式。
- 本行只有 C14 一张锚定图片，已按原始分辨率核对，无遗漏浮动图形。
- 为排除指代歧义，也结构化读取了当前工作簿第8行；其内容是“新增回款单位与客户名称不应绑定”，与本行人员匹配无关，因此本行“同第八条”指向的是此前沟通问题编号，不用无关的工作簿第8行替换本行明确的全局人员匹配要求。

## VibeHub 脱敏术语核对

- 脱敏候选：`人员选择器`、`模糊匹配`、`显示名称`。
- resolver revision：`d2034b1998eaaa2f`；均为 0 候选。
- 记录：核对已执行，无需添加可靠术语，原文保持不变。
- 专业定位表达：共享人员选择器的搜索索引必须覆盖启用人员的用户名、系统显示名和在职人事中文姓名；选择值、保存值和刷新后的显示值应使用同一账号映射，且所有复用人员候选 API 的入口都应一致生效。

## 后续强制核验

- 旧系统真实入口：以当前旧系统调查任务分配角色进入 `调查任务 → 新增调查员/任务` 的创建页，父调查编号 `RW2614153786523`，在“调查员”字段输入“范”；证据 `legacy-investigator-search-fan.png`。当前账号数据下未弹出候选，因此只把它作为同入口动态复现证据，不伪造旧系统动态命中结论。
- 旧源码：`Areas/CIT/Views/InvestigationTask/Create.cshtml` 提供调查员字段；`Scripts/CIT/Investigation/CIT.Investigation.js` 调用通用 `AssociateAvailableUser`；`Scripts/Common/base.js` 向 `/Account/UserCenter/AvailableUsers` 发起关键字查询；`Areas/Account/Controllers/UserCenterController.cs` 以 `StaffChName.Contains(userName)` 检索，并返回用户名与中文名；`InvestigationTaskController.CreateUpdate` 持久化调查员用户名，并通过 HR 服务回写 `UserChName`。
- 旧数据只读：旧库 `PRD_CRM_GD_20200211.HR_Staff` 以中文姓“范”查询到 4 个启用人员：范明明、范文玲、范应根、范雨星；未写旧库。
- 新源码：共享 `/api/v1/people/options` 仅返回启用系统用户，以非离职/非停用 HR 档案的中文姓名为首选标签，并把 HR 中文名、`User.display_name`、用户名共同写入 `search_text`；`InvestigationCenterPage.tsx` 的人员选择器按 `search_text` 过滤并保存用户名；`_task_display_dicts` 再按同账号的在职 HR 档案覆盖任务负责人显示名。
- 新数据：隔离 SQLite 中账号 `row14fan` 的系统显示名为 `FAN-WL`，在职 HR 档案标题为“范文玲”；保存后的任务负责人仍为 `row14fan`，刷新回显为“范文玲”。
- 具体根因：原共享人员候选只建立了不完整的显示字段索引，且任务保存后的回显仍优先使用系统显示名，造成同一账号在关键字搜索与保存回显间不一致；此前修复已补齐 HR 中文名、系统显示名和用户名三类稳定检索字段，并统一任务显示映射。本轮审计未发现需要再次改写代码的缺口。
